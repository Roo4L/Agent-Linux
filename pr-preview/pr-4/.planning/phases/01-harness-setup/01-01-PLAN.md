---
phase: 01-harness-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/bin/agentlinux-install
  - plugin/lib/.gitkeep
  - plugin/provisioner/.gitkeep
  - plugin/cli/package.json
  - plugin/cli/tsconfig.json
  - plugin/cli/biome.json
  - plugin/cli/src/.gitkeep
  - plugin/cli/test/.gitkeep
  - plugin/catalog/schema.json
  - plugin/catalog/agents/.gitkeep
  - packaging/curl-installer/.gitkeep
  - packaging/deb/.gitkeep
  - tests/bats/helpers/.gitkeep
  - tests/bats/.gitkeep
  - tests/docker/.gitkeep
  - tests/qemu/cloud-init/.gitkeep
  - tests/qemu/.gitkeep
  - tests/harness/.gitkeep
  - tests/mutation/.gitkeep
  - docs/README.md
  - docs/decisions/000-template.md
  - docs/decisions/001-pivot-distro-to-plugin.md
  - docs/decisions/002-behavior-contract-framing.md
  - docs/decisions/003-no-default-agents-installed.md
  - docs/decisions/004-per-user-npm-prefix.md
  - docs/decisions/005-system-nodejs-over-version-managers.md
  - docs/decisions/006-curl-pipe-bash-plus-deb.md
  - docs/decisions/007-docker-plus-qemu-harness.md
  - docs/decisions/008-commander-js-for-cli.md
  - docs/decisions/009-snap-disqualified.md
  - docs/decisions/010-review-loop-via-claude-md.md
  - docs/research/v0.3.0/STACK.md
  - docs/research/v0.3.0/FEATURES.md
  - docs/research/v0.3.0/ARCHITECTURE.md
  - docs/research/v0.3.0/PITFALLS.md
  - docs/research/v0.3.0/SUMMARY.md
  - docs/research/v0.2.0/STACK.md
  - docs/research/v0.2.0/FEATURES.md
  - docs/research/v0.2.0/ARCHITECTURE.md
  - docs/research/v0.2.0/PITFALLS.md
  - docs/research/v0.2.0/SUMMARY.md
  - docs/proposals/.gitkeep
  - docs/analysis/.gitkeep
  - docs/reviews/.gitkeep
  - CLAUDE.md
autonomous: true
requirements: [HRN-01, HRN-03, HRN-04, HRN-05]

must_haves:
  truths:
    - "Every directory named in docs/HARNESS.md §1 exists on disk"
    - "CLAUDE.md exists at repo root and is strictly under 150 lines"
    - "docs/decisions/ holds ADR-001..ADR-010 as separate markdown files"
    - "Research for v0.2.0 and v0.3.0 is reachable under docs/research/vX.Y.Z/"
    - "Legacy v0.1.0 site files and packer/ directory are untouched"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project-scoped Claude Code context"
      contains: "review loop"
    - path: "plugin/bin/agentlinux-install"
      provides: "Installer entrypoint stub (no installer logic)"
    - path: "plugin/catalog/schema.json"
      provides: "Catalog JSON Schema stub (validates empty catalog)"
    - path: "plugin/cli/package.json"
      provides: "Node CLI package manifest baseline"
    - path: "docs/decisions/001-pivot-distro-to-plugin.md"
      provides: "ADR-001 seeded verbatim from HARNESS.md §2.3"
    - path: "docs/decisions/010-review-loop-via-claude-md.md"
      provides: "ADR-010 locking review loop to CLAUDE.md instruction"
    - path: "docs/research/v0.3.0/SUMMARY.md"
      provides: "v0.3.0 research migrated out of .planning/"
    - path: "docs/research/v0.2.0/SUMMARY.md"
      provides: "v0.2.0 research migrated out of .planning/milestones/"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/HARNESS.md §6"
      via: "content matches (project identity, critical rules, review-loop, commands, pointers)"
      pattern: "docs/HARNESS.md"
    - from: "docs/decisions/"
      to: "docs/HARNESS.md §2.3"
      via: "10 ADR files, each using the §2.3 template header"
      pattern: "ADR-0"
---

<objective>
Create the complete directory skeleton, CLAUDE.md, ADRs, and research migration required by `docs/HARNESS.md` §1, §2.3, and §6. This plan lands **only** scaffolding files and reference documentation — no installer code, no CLI logic, no catalog entries beyond the schema stub.

Purpose: Establish the copy-of-truth layout so every subsequent phase has a stable place to land code and tests.
Output: Full repository skeleton matching HARNESS.md §1; CLAUDE.md (<150 lines); 10 seeded ADRs; research files relocated from `.planning/` to `docs/research/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-harness-setup/01-CONTEXT.md
@docs/HARNESS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repo skeleton + stub entrypoint + catalog schema</name>
  <files>
    plugin/bin/agentlinux-install,
    plugin/lib/.gitkeep,
    plugin/provisioner/.gitkeep,
    plugin/cli/package.json,
    plugin/cli/tsconfig.json,
    plugin/cli/biome.json,
    plugin/cli/src/.gitkeep,
    plugin/cli/test/.gitkeep,
    plugin/catalog/schema.json,
    plugin/catalog/agents/.gitkeep,
    packaging/curl-installer/.gitkeep,
    packaging/deb/.gitkeep,
    tests/bats/helpers/.gitkeep,
    tests/bats/.gitkeep,
    tests/docker/.gitkeep,
    tests/qemu/cloud-init/.gitkeep,
    tests/qemu/.gitkeep,
    tests/harness/.gitkeep,
    tests/mutation/.gitkeep,
    docs/proposals/.gitkeep,
    docs/analysis/.gitkeep,
    docs/reviews/.gitkeep
  </files>
  <read_first>
    docs/HARNESS.md (§1 — the target layout is the copy-of-truth. Read §1.1 and the layout tree in full.),
    .planning/phases/01-harness-setup/01-CONTEXT.md,
    .gitignore (confirm .gitkeep is not ignored; add an exception if needed)
  </read_first>
  <action>
Create every directory and placeholder file in the repository listed in docs/HARNESS.md §1.1 that does not yet exist. For each directory that would otherwise be empty, add an empty `.gitkeep` file so git tracks it.

Do **NOT** create installer logic, CLI command source, catalog agent recipes, or provisioner scripts — those are Phase 2+.

Specific files and their exact contents:

1. `plugin/bin/agentlinux-install` — bash stub, chmod 755, exact body:
```
#!/usr/bin/env bash
# AgentLinux installer entrypoint (stub — real provisioning lands in Phase 2+).
set -euo pipefail
echo "agentlinux-install: stub entrypoint (harness phase); real installer arrives in Phase 2+"
exit 0
```

2. `plugin/cli/package.json` — Commander.js + node:test baseline, no source yet:
```json
{
  "name": "@agentlinux/cli",
  "version": "0.0.0",
  "private": true,
  "description": "AgentLinux registry CLI (skeleton — real commands arrive in Phase 4)",
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-test-coverage test/",
    "lint": "biome check src/ test/",
    "format": "biome format --write src/ test/"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
```

3. `plugin/cli/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noImplicitAny": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "outDir": "dist",
    "rootDir": "src",
    "skipLibCheck": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "test"]
}
```

4. `plugin/cli/biome.json`:
```json
{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "organizeImports": { "enabled": true },
  "linter": {
    "enabled": true,
    "rules": { "recommended": true }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  }
}
```

5. `plugin/catalog/schema.json` — JSON Schema 2020-12 stub that a real Phase 4 catalog can conform to. Must parse as valid JSON:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentlinux.org/schemas/catalog.json",
  "title": "AgentLinux Catalog",
  "description": "Stub schema for the v0.3.0 catalog. Real entries arrive in Phase 4.",
  "type": "object",
  "required": ["version", "agents"],
  "properties": {
    "version": { "type": "string" },
    "agents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "description", "install"],
        "properties": {
          "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
          "description": { "type": "string" },
          "install": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
```

6. All other listed paths: create empty `.gitkeep` file so git tracks the directory.

Do NOT touch: `index.html`, `assets/`, `CNAME`, `sitemap.xml`, `robots.txt`, `packer/` — these are legacy and stay as-is.
  </action>
  <verify>
    <automated>test -x plugin/bin/agentlinux-install && test -f plugin/cli/package.json && node -e "JSON.parse(require('fs').readFileSync('plugin/cli/package.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('plugin/cli/tsconfig.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('plugin/cli/biome.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('plugin/catalog/schema.json','utf8'))" && test -d plugin/lib && test -d plugin/provisioner && test -d plugin/cli/src && test -d plugin/cli/test && test -d plugin/catalog/agents && test -d packaging/curl-installer && test -d packaging/deb && test -d tests/bats/helpers && test -d tests/docker && test -d tests/qemu/cloud-init && test -d tests/harness && test -d tests/mutation && test -d docs/proposals && test -d docs/analysis && test -d docs/reviews && test -f index.html && test -d packer</automated>
  </verify>
  <acceptance_criteria>
    - `test -x plugin/bin/agentlinux-install` exits 0 (file exists, executable)
    - `bash plugin/bin/agentlinux-install` exits 0 and prints "stub entrypoint"
    - `node -e "JSON.parse(require('fs').readFileSync('plugin/catalog/schema.json','utf8'))"` exits 0 (valid JSON)
    - `node -e "JSON.parse(require('fs').readFileSync('plugin/cli/package.json','utf8'))"` exits 0
    - All 9 required directories exist: `plugin/lib`, `plugin/provisioner`, `plugin/cli/src`, `plugin/cli/test`, `plugin/catalog/agents`, `packaging/curl-installer`, `packaging/deb`, `tests/bats/helpers`, `tests/mutation`
    - `grep -q "commander" plugin/cli/package.json`
    - Legacy file `index.html` still exists at repo root (not touched)
    - Legacy directory `packer/` still exists (not deleted)
  </acceptance_criteria>
  <done>Full project directory skeleton exists on disk per HARNESS.md §1.1; stub entrypoint and schema files parse cleanly; no legacy files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write CLAUDE.md at repo root (<150 lines) per HARNESS.md §6</name>
  <files>CLAUDE.md</files>
  <read_first>
    docs/HARNESS.md (§6 in full — this dictates every section CLAUDE.md must contain),
    docs/HARNESS.md (§4 — review loop mechanics to summarize in CLAUDE.md),
    .planning/PROJECT.md (for "What This Is" wording)
  </read_first>
  <action>
Write `CLAUDE.md` at the repo root. The file MUST be under 150 lines (strictly less than 150, not ≤150). It MUST contain, in order, every section listed in docs/HARNESS.md §6:

1. **Project identity** (1 paragraph): "AgentLinux v0.3.0 — installable Ubuntu plugin. Provisions an agent user with correctly-owned Node.js runtime + a registry CLI for installing agent tools. Pivoted from custom distro (v0.2.0) on 2026-04-18."

2. **Where things live** (bullet list): `plugin/`, `tests/bats/`, `tests/harness/`, `docs/`, `.planning/`, `.claude/agents/`, `.claude/skills/`, `packaging/`, `.github/workflows/`. One line per pointer.

3. **Critical rules** (bullet list — copy from §6 "Critical rules (non-obvious)"):
   - Never `sudo npm install -g` anywhere. Always `sudo -u agent -H npm install -g`.
   - Behavior tests in `tests/bats/` are the spec; implementation may change freely while the suite is green.
   - No agent installed by default; users opt in via `agentlinux install <name>`.
   - Docker-only test runs are insufficient; QEMU suite must be green before any release.
   - Every release tarball ships with a sibling `.sha256`; `packaging/curl-installer/install.sh` must verify.
   - No wrapper shims at `/usr/local/bin/` pointing to agent-owned binaries (the exact anti-pattern that breaks Claude Code self-update).

4. **Review loop rule** (1 paragraph): "Before reporting any task complete, run the review feedback loop on all changed files per docs/HARNESS.md §4. Triggered by this instruction — not a Stop hook (ADR-010). See `.claude/skills/review/SKILL.md` for the convention."

5. **Commands** (code block):
   - `./tests/docker/run.sh ubuntu-24.04` — run bats inside Docker
   - `cd plugin/cli && pnpm test` — CLI unit tests
   - `pre-commit run --all-files` — lint bash + TS + catalog schema
   - `./scripts/build-release.sh vX.Y.Z` — build the release tarball
   - `bash tests/harness/run.sh` — run harness meta-tests (Phase 1)

6. **Pointers** (bullet list):
   - `@.planning/ROADMAP.md`
   - `@.planning/REQUIREMENTS.md`
   - `@docs/HARNESS.md` (authoritative harness spec)
   - `@docs/research/v0.3.0/SUMMARY.md`
   - `@docs/decisions/` (ADR-001..ADR-010)
   - Skills: `.claude/skills/agentlinux-installer/`, `.claude/skills/behavior-test-contract/`, `.claude/skills/catalog-schema/`, `.claude/skills/qemu-harness/`, `.claude/skills/review/`

Keep prose tight. No historical v0.2.0 essays, no research findings, no catalog schema details — those load on demand from skills/docs.

**Verify line count before finishing:** `wc -l CLAUDE.md` must report a number strictly less than 150.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && wc -l CLAUDE.md | awk '{ if ($1 < 150) { print "pass" } else { print "fail: " $1 " lines" } }' | grep -q pass && grep -q "AgentLinux v0.3.0" CLAUDE.md && grep -q "review" CLAUDE.md && grep -qi "sudo npm install -g" CLAUDE.md && grep -q "HARNESS.md" CLAUDE.md && grep -q "ROADMAP.md" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CLAUDE.md` exits 0
    - `wc -l CLAUDE.md` reports a value strictly less than 150
    - `grep -q "AgentLinux v0.3.0" CLAUDE.md` (project identity present)
    - `grep -qi "sudo npm install -g" CLAUDE.md` (critical rule about not sudoing npm -g)
    - `grep -qE "review.loop|review feedback|/review" CLAUDE.md` (review loop instruction present)
    - `grep -q "HARNESS.md" CLAUDE.md` (pointer to harness doc)
    - `grep -q "ROADMAP.md" CLAUDE.md` (pointer to roadmap)
    - `grep -qi "qemu" CLAUDE.md` (QEMU-before-release rule present)
    - `grep -qE "agentlinux[- ]?installer|behavior[- ]?test[- ]?contract|catalog[- ]?schema|qemu[- ]?harness" CLAUDE.md` (skill pointers present)
  </acceptance_criteria>
  <done>CLAUDE.md exists at repo root with all six sections from HARNESS.md §6, strictly under 150 lines, grep-verifiable content.</done>
</task>

<task type="auto">
  <name>Task 3: Seed ADR-001..ADR-010 + docs/README.md + ADR template + migrate research</name>
  <files>
    docs/README.md,
    docs/decisions/000-template.md,
    docs/decisions/001-pivot-distro-to-plugin.md,
    docs/decisions/002-behavior-contract-framing.md,
    docs/decisions/003-no-default-agents-installed.md,
    docs/decisions/004-per-user-npm-prefix.md,
    docs/decisions/005-system-nodejs-over-version-managers.md,
    docs/decisions/006-curl-pipe-bash-plus-deb.md,
    docs/decisions/007-docker-plus-qemu-harness.md,
    docs/decisions/008-commander-js-for-cli.md,
    docs/decisions/009-snap-disqualified.md,
    docs/decisions/010-review-loop-via-claude-md.md,
    docs/research/v0.3.0/STACK.md,
    docs/research/v0.3.0/FEATURES.md,
    docs/research/v0.3.0/ARCHITECTURE.md,
    docs/research/v0.3.0/PITFALLS.md,
    docs/research/v0.3.0/SUMMARY.md,
    docs/research/v0.2.0/STACK.md,
    docs/research/v0.2.0/FEATURES.md,
    docs/research/v0.2.0/ARCHITECTURE.md,
    docs/research/v0.2.0/PITFALLS.md,
    docs/research/v0.2.0/SUMMARY.md
  </files>
  <read_first>
    docs/HARNESS.md (§2.3 — ADR template + full ADR-001..ADR-010 titles/summaries; use as copy-of-truth),
    docs/HARNESS.md (§2.2 — docs/ routing rule to include in docs/README.md),
    .planning/STATE.md (Decisions section — cross-reference matches §2.3),
    .planning/PROJECT.md (Key Decisions table),
    .planning/research/SUMMARY.md (v0.3.0 research root — to be moved),
    .planning/milestones/v0.2.0-research/SUMMARY.md (v0.2.0 research root — to be moved)
  </read_first>
  <action>
Three sub-actions.

**A. Write `docs/README.md`** — index of the docs/ tree:
```markdown
# AgentLinux Documentation

This directory holds all reference documentation. `.planning/` holds GSD workflow
state (plans, STATE.md, config) — not documentation. If the output of a task is
a document intended to be read later (ADR, research report, design proposal,
review summary), it goes here.

## Layout

- `HARNESS.md` — authoritative project harness spec (§1 layout, §2 docs,
  §3 systems access, §4 review loop, §5 skills, §6 CLAUDE.md, §7 checklist,
  §8 success criteria).
- `decisions/` — Architecture Decision Records (ADRs). ADR-001..ADR-010 seeded
  in Phase 1 per `HARNESS.md` §2.3. New ADRs land as decisions resolve.
- `research/v0.3.0/` — v0.3.0 research outputs (STACK, FEATURES, ARCHITECTURE,
  PITFALLS, SUMMARY).
- `research/v0.2.0/` — archived v0.2.0 research (carry-forward reference).
- `proposals/` — design proposals pre-ADR.
- `analysis/` — gap analyses, comparison studies.
- `reviews/` — review-loop outputs worth preserving across sessions.
```

**B. Create `docs/decisions/000-template.md`** — the ADR template from HARNESS.md §2.3:
```markdown
# NNN: [Title]

**Status:** Proposed | Accepted | Superseded by NNN
**Date:** YYYY-MM-DD

## Context

Why this decision was needed (2–3 sentences).

## Decision

What we decided (1–2 sentences).

## Consequences

What changes as a result; what trade-off was accepted.
```

**C. Write ADR-001..ADR-010** — each file uses the template header and reproduces the decision summary from STATE.md > Decisions (cross-ref with HARNESS.md §2.3). Status: Accepted. Date: 2026-04-18.

Use these EXACT file names (order matters — they map to the HARNESS §2.3 list):

| File | Title |
|---|---|
| `001-pivot-distro-to-plugin.md` | Pivot from custom distro to installable Ubuntu plugin (v0.2.0 → v0.3.0) |
| `002-behavior-contract-framing.md` | Behavior-contract framing — requirements are BHV-XX, not INST-XX; tests are the spec |
| `003-no-default-agents-installed.md` | No default agents installed in v0.3.0 |
| `004-per-user-npm-prefix.md` | Per-user npm prefix as the keystone ownership decision |
| `005-system-nodejs-over-version-managers.md` | System Node.js (NodeSource) over version managers (nvm/fnm/volta) |
| `006-curl-pipe-bash-plus-deb.md` | curl-pipe-bash primary + optional .deb distribution |
| `007-docker-plus-qemu-harness.md` | Docker (fast) + QEMU (release gate) test harness; Docker-only is disqualified |
| `008-commander-js-for-cli.md` | Commander.js for the registry CLI |
| `009-snap-disqualified.md` | Snap is structurally disqualified as a distribution mechanism |
| `010-review-loop-via-claude-md.md` | Review loop triggered by CLAUDE.md instruction, not a Stop hook |

For each ADR body, write 2–3 sentence Context, 1–2 sentence Decision, and a brief Consequences paragraph. Keep bodies short; the titles and statuses are what's load-bearing in Phase 1. Pull source material from:
- `.planning/STATE.md` § Decisions (lines ~55–66)
- `.planning/PROJECT.md` § Key Decisions table
- `docs/HARNESS.md` §2.3

For ADR-005 (System Node.js over version managers), the consequences MUST mention: "nvm/fnm/volta shell-hook activation breaks cron, systemd, and non-interactive SSH — the exact invocation modes BHV-02..06 require."

For ADR-010 (Review loop via CLAUDE.md), the consequences MUST mention: "Stop hooks fire on every stop (user interrupts, context limits, errors) — not just task completion. Subjective LLM review in a Stop hook wastes tokens. Deterministic checks (pre-commit) may still live in a future Stop hook."

**D. Migrate research files** — copy (do not move; leave `.planning/` copies intact so GSD tooling that references them still works):
- `.planning/research/STACK.md` → `docs/research/v0.3.0/STACK.md`
- `.planning/research/FEATURES.md` → `docs/research/v0.3.0/FEATURES.md`
- `.planning/research/ARCHITECTURE.md` → `docs/research/v0.3.0/ARCHITECTURE.md`
- `.planning/research/PITFALLS.md` → `docs/research/v0.3.0/PITFALLS.md`
- `.planning/research/SUMMARY.md` → `docs/research/v0.3.0/SUMMARY.md`
- `.planning/milestones/v0.2.0-research/STACK.md` → `docs/research/v0.2.0/STACK.md`
- `.planning/milestones/v0.2.0-research/FEATURES.md` → `docs/research/v0.2.0/FEATURES.md`
- `.planning/milestones/v0.2.0-research/ARCHITECTURE.md` → `docs/research/v0.2.0/ARCHITECTURE.md`
- `.planning/milestones/v0.2.0-research/PITFALLS.md` → `docs/research/v0.2.0/PITFALLS.md`
- `.planning/milestones/v0.2.0-research/SUMMARY.md` → `docs/research/v0.2.0/SUMMARY.md`

Use the shell `cp` command (or Bash tool) — do not hand-retype; preserve original content byte-for-byte. HRN-05 says "migrated" — a copy plus a follow-up archive sweep in a later phase satisfies "research is findable under `docs/research/vX.Y.Z/`."
  </action>
  <verify>
    <automated>test -f docs/README.md && test -f docs/decisions/000-template.md && ls docs/decisions/ADR-*.md 2>/dev/null | wc -l | tr -d ' ' || ls docs/decisions/0[0-9][0-9]-*.md | wc -l | awk '{ if ($1 >= 10) { print "pass" } else { print "fail: " $1 " adrs" } }' | grep -q pass && test -f docs/decisions/001-pivot-distro-to-plugin.md && test -f docs/decisions/010-review-loop-via-claude-md.md && grep -q "Accepted" docs/decisions/001-pivot-distro-to-plugin.md && grep -qi "stop hook" docs/decisions/010-review-loop-via-claude-md.md && grep -qi "cron\|systemd\|non-interactive" docs/decisions/005-system-nodejs-over-version-managers.md && test -f docs/research/v0.3.0/SUMMARY.md && test -f docs/research/v0.3.0/STACK.md && test -f docs/research/v0.3.0/ARCHITECTURE.md && test -f docs/research/v0.3.0/FEATURES.md && test -f docs/research/v0.3.0/PITFALLS.md && test -f docs/research/v0.2.0/SUMMARY.md && test -f docs/research/v0.2.0/STACK.md && test -f docs/research/v0.2.0/ARCHITECTURE.md && test -f docs/research/v0.2.0/FEATURES.md && test -f docs/research/v0.2.0/PITFALLS.md && diff -q .planning/research/SUMMARY.md docs/research/v0.3.0/SUMMARY.md && diff -q .planning/milestones/v0.2.0-research/SUMMARY.md docs/research/v0.2.0/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/decisions/0[0-9][0-9]-*.md | wc -l` returns a value ≥ 11 (template + 10 ADRs)
    - `test -f docs/decisions/001-pivot-distro-to-plugin.md` exits 0
    - `test -f docs/decisions/010-review-loop-via-claude-md.md` exits 0
    - Every ADR file contains the line `**Status:** Accepted`
    - `grep -qi "stop hook" docs/decisions/010-review-loop-via-claude-md.md` (ADR-010 explains why not a Stop hook)
    - `grep -qEi "cron|systemd|non-interactive" docs/decisions/005-system-nodejs-over-version-managers.md` (ADR-005 explains invocation-mode blast radius)
    - All 5 v0.3.0 research files exist under `docs/research/v0.3.0/`
    - All 5 v0.2.0 research files exist under `docs/research/v0.2.0/`
    - `diff -q .planning/research/SUMMARY.md docs/research/v0.3.0/SUMMARY.md` returns no output (files match)
    - `diff -q .planning/milestones/v0.2.0-research/SUMMARY.md docs/research/v0.2.0/SUMMARY.md` returns no output
    - `test -f docs/README.md && grep -q "HARNESS.md" docs/README.md`
  </acceptance_criteria>
  <done>10 ADRs seeded verbatim from HARNESS.md §2.3; docs/README.md indexes the tree; ADR template available; v0.3.0 and v0.2.0 research copied into `docs/research/vX.Y.Z/`.</done>
</task>

</tasks>

<verification>
- `test -d plugin/bin && test -d plugin/lib && test -d plugin/provisioner && test -d plugin/cli/src && test -d plugin/catalog/agents && test -d tests/bats && test -d tests/docker && test -d tests/qemu && test -d tests/harness && test -d tests/mutation && test -d packaging/curl-installer && test -d packaging/deb && test -d .claude/agents || mkdir -p .claude/agents && test -d .claude/skills || mkdir -p .claude/skills`
- `wc -l CLAUDE.md | awk '{ exit ($1 < 150) ? 0 : 1 }'`
- `ls docs/decisions/0[0-9][0-9]-*.md | wc -l | awk '{ exit ($1 >= 11) ? 0 : 1 }'`
- `test -f docs/research/v0.3.0/SUMMARY.md && test -f docs/research/v0.2.0/SUMMARY.md`
- Legacy files untouched: `test -f index.html && test -d packer && test -f CNAME`
</verification>

<success_criteria>
- HRN-01 partially satisfied (project layout directories exist per HARNESS.md §1; Plans 02–04 fill in the workflow + agent + skill files)
- HRN-03 satisfied (CLAUDE.md under 150 lines at repo root per §6)
- HRN-04 satisfied (ADR-001..ADR-010 exist in `docs/decisions/`)
- HRN-05 satisfied (research files findable under `docs/research/v0.2.0/` and `docs/research/v0.3.0/`)
- Legacy v0.1.0 site files and `packer/` untouched
- No installer logic, no CLI commands, no catalog entries beyond schema stub
</success_criteria>

<threat_model>
Security enforcement for this plan: infrastructure/scaffolding only; no runtime code paths, no external-facing endpoints, no secret handling. STRIDE register is narrow but explicit.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo contributor → repo HEAD | Untrusted PR author may commit files that later scripts consume (ADR content, research docs, catalog schema stub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `plugin/bin/agentlinux-install` stub | mitigate | Stub body is fixed to an `exit 0` echo; real installer logic is out of scope for Phase 1 so there is nothing to tamper with; shellcheck will catch shell-injection attempts when the real installer lands in Phase 2. |
| T-01-02 | Information Disclosure | ADRs / research migration | accept | ADRs and research are public-by-design project documentation; no credentials or PII are migrated. Cross-checked: `.planning/research/` contains no secrets. |
| T-01-03 | Denial of Service | `plugin/catalog/schema.json` stub | mitigate | Schema uses `additionalProperties: false` and a strict `pattern` on agent names so malformed future catalog entries fail fast in pre-commit (wired in Plan 02). |
</threat_model>

<output>
After completion, create `.planning/phases/01-harness-setup/01-01-SUMMARY.md` summarizing what was created, any deviations from the HARNESS.md §1 layout, and any cross-references for Plan 02 (which will add `.pre-commit-config.yaml` + 4 workflows + mutation scaffolding on top of this skeleton).
</output>
