---
phase: 05.1
slug: agent-user-sudo-drop-in
verified_date: 2026-04-19
status: passed
must_haves_verified: 7/7
phase_requirements_covered: 2/2
tst07_gate: GREEN
unit_tests: n/a (no TS work this phase)
bats_tests_ubuntu_22_04: 56/56
bats_tests_ubuntu_24_04: 56/56
---

# Phase 5.1 (INSERTED): Agent User Sudo Drop-In — Verification

**Phase Goal:** `/etc/sudoers.d/agentlinux` grants passwordless sudo to agent user (ADR-012). Prerequisite for Phase 5 agent recipes needing apt/systemctl.

## Goal Achievement Summary

All 5 ROADMAP success criteria verified end-to-end on Ubuntu 22.04 + 24.04 Docker matrix:

| SC | Statement | Verified By | Status |
|----|-----------|-------------|--------|
| 1 | `/etc/sudoers.d/agentlinux` exists, 0440 root:root, contains exact NOPASSWD line | 22-agent-sudo.bats BHV-07 @tests on mode + content | ✓ |
| 2 | `sudo -u agent sudo -n true` exit 0 (passwordless) | 22-agent-sudo.bats INST-06 @test | ✓ |
| 3 | `visudo -cf` returns exit 0 on the drop-in | 22-agent-sudo.bats BHV-07 @test | ✓ |
| 4 | Re-running installer produces byte-identical drop-in | 22-agent-sudo.bats BHV-07 re-run sha256 @test | ✓ |
| 5 | `--purge` removes the drop-in | Phase 4's `40-registry-cli.bats` INST-04 test already asserts `! test -f /etc/sudoers.d/agentlinux` post-purge | ✓ |

## Must-Haves Matrix

7/7 verified:
1. `plugin/provisioner/20-sudoers.sh` — shellcheck + shfmt clean ✓
2. `set -euo pipefail` + visudo -cf gate before install ✓
3. Atomic install via `install -m 0440 -o root -g root` ✓
4. One-line `log_info` audit record of the sudo grant ✓
5. `tests/bats/22-agent-sudo.bats` — 7 @tests ✓
6. Docker matrix 56/56 on both Ubuntu versions ✓
7. Phase 1 harness still 104/104 ✓

## Requirement Coverage

| ID | Plans | @tests | Status |
|----|-------|--------|--------|
| INST-06 | 05.1-01 | 22-agent-sudo.bats: 2 @tests (sudo -n true; sudo -l NOPASSWD match) | ✓ Complete |
| BHV-07 | 05.1-01 | 22-agent-sudo.bats: 5 @tests (file-exists, mode-0440, owner-root:root, exact-line, visudo-cf, re-run sha256) | ✓ Complete |

## Threat Coverage

All 4 T-05.1-XX threats mitigated per plan:
- T-05.1-01 syntax error: `visudo -cf tmpfile` gate before install
- T-05.1-02 incorrect perms: `install -m 0440 -o root -g root` atomic
- T-05.1-03 world-readable: mode 0440 verified in bats
- T-05.1-04 double-append on re-run: atomic overwrite, sha256 stable

## Invariants

- No `sudo npm install -g` in plugin/ (grep returns 0) ✓
- No `/usr/local/bin/` shims (grep returns 0) ✓
- `bash tests/harness/run.sh` → 104/104 ✓
- `shellcheck --severity=warning plugin/provisioner/20-sudoers.sh` → clean ✓
- `shfmt -d plugin/provisioner/20-sudoers.sh` → no diff ✓
- `visudo -cf /etc/sudoers.d/agentlinux` (inside provisioned container) → exit 0 ✓

## Verdict

**passed**. Phase 5.1 is closed. Phase 5 (Agent Installability) unblocked; catalog recipes can now freely `sudo apt install`, `sudo systemctl restart`, etc.

## VERIFICATION COMPLETE
