---
phase: 03-nodejs-runtime-per-user-npm-prefix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/provisioner/30-nodejs.sh
  - plugin/provisioner/40-path-wiring.sh
autonomous: true
requirements:
  - RT-01
  - RT-04
tags:
  - installer
  - nodejs
  - npm-prefix
  - path-wiring
  - ubuntu
  - bash
user_setup: []

must_haves:
  truths:
    - "After `agentlinux-install` runs on a fresh Ubuntu 22.04 or 24.04 image, `node --version` returns a v22.x string (RT-01, Roadmap success criterion 1)."
    - "After install, `/home/agent/.npm-global` exists, is agent:agent owned, mode 0755, with empty `bin/` and `lib/` subdirectories."
    - "After install, `/home/agent/.npmrc` exists, is agent:agent owned, mode 0644, and contains exactly one line `prefix=/home/agent/.npm-global` (RT-04, Roadmap success criterion 4)."
    - "After install, the literal string `/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin` appears as a `PATH=` line in BOTH `/etc/agentlinux.env` AND `/etc/cron.d/agentlinux` (Pitfall 4 prepend order)."
    - "After install, `/etc/agentlinux.env` contains a `NPM_CONFIG_PREFIX=/home/agent/.npm-global` line (Pitfall 5 belt-and-braces — Roadmap success criterion 4 resilience)."
    - "Re-running `agentlinux-install` on the same host is byte-stable across `/home/agent/.npmrc`, `/etc/apt/sources.list.d/nodesource.sources`, `/etc/agentlinux.env`, `/etc/cron.d/agentlinux`, and `/etc/profile.d/agentlinux.sh` (Roadmap success criterion 5 — idempotency extends INST-02 contract to Phase 3 artefacts)."
    - "The installer transcript at `/var/log/agentlinux-install.log` contains no `EACCES` or `permission denied` lines after Phase 3 adds Node/npm install steps."
  artifacts:
    - path: plugin/provisioner/30-nodejs.sh
      provides: "NodeSource Node 22 LTS install + per-user npm prefix configuration"
      contains: "log_info \"30-nodejs: starting\""
      contains_also: "ensure_line_in_file 'prefix=/home/agent/.npm-global' /home/agent/.npmrc"
    - path: plugin/provisioner/40-path-wiring.sh
      provides: "PATH wiring extended with /home/agent/.npm-global/bin prepend + NPM_CONFIG_PREFIX env"
      contains: "/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin"
      contains_also: "NPM_CONFIG_PREFIX=/home/agent/.npm-global"
  key_links:
    - from: plugin/bin/agentlinux-install
      to: plugin/provisioner/30-nodejs.sh
      via: "run_provisioners glob (numeric dispatch 10 → 30 → 40)"
      pattern: "compgen -G .*\\[0-9\\]\\[0-9\\]-\\*\\.sh"
    - from: plugin/provisioner/30-nodejs.sh
      to: /home/agent/.npmrc
      via: "ensure_line_in_file primitive from plugin/lib/idempotency.sh"
      pattern: "ensure_line_in_file 'prefix=/home/agent/\\.npm-global'"
    - from: plugin/provisioner/40-path-wiring.sh
      to: /etc/agentlinux.env
      via: "install -m 0644 /dev/stdin <<'ENVFILE' heredoc (single-quoted)"
      pattern: "PATH=/home/agent/\\.npm-global/bin:/home/agent/\\.local/bin"
---

<objective>
Ship the Phase 3 provisioner surface: a new `plugin/provisioner/30-nodejs.sh` that installs Node.js 22 LTS via NodeSource and configures the per-user npm prefix under `/home/agent/.npm-global`, and an extension to `plugin/provisioner/40-path-wiring.sh` that prepends `/home/agent/.npm-global/bin` to every PATH-carrying artefact and adds a belt-and-braces `NPM_CONFIG_PREFIX` to `/etc/agentlinux.env`.

Purpose: This is the keystone ownership proof (ADR-004 + ADR-005). After this plan lands, the agent user has a system Node 22 LTS on PATH (via `/usr/bin/node`) AND a writable npm global prefix under its own home, with no `sudo`, no `EACCES`, and no `/usr/local/bin/` shim anywhere. Plan 03-02 then proves the behavior with bats.

Output:
- `plugin/provisioner/30-nodejs.sh` (NEW, ~80 LOC)
- `plugin/provisioner/40-path-wiring.sh` (EXTENDED — three heredoc artefacts prepend `/home/agent/.npm-global/bin`; one new `NPM_CONFIG_PREFIX` line in `agentlinux.env`)
- Two atomic commits (Task 1 `feat(03-01)`, Task 2 `feat(03-01)`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-CONTEXT.md
@.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md
@.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-VALIDATION.md
@CLAUDE.md
@docs/HARNESS.md
@docs/decisions/ADR-004.md
@docs/decisions/ADR-005.md
@.claude/skills/agentlinux-installer/SKILL.md
@.claude/skills/review/SKILL.md

# Phase 2 artefacts being extended / reused:
@plugin/bin/agentlinux-install
@plugin/lib/log.sh
@plugin/lib/as_user.sh
@plugin/lib/idempotency.sh
@plugin/lib/distro_detect.sh
@plugin/provisioner/10-agent-user.sh
@plugin/provisioner/40-path-wiring.sh

<interfaces>
<!-- Contracts downstream tasks rely on. Extracted from Phase 2 source. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

Reusable library primitives (from plugin/lib/*.sh — Phase 2):

```bash
# plugin/lib/log.sh
log_info  <msg>   # INFO level, prefixed timestamp, tee'd to install log
log_warn  <msg>   # WARN level
log_error <msg>   # ERROR level
log_debug <msg>   # DEBUG (only emitted if --verbose)

# plugin/lib/as_user.sh — the keystone helper
as_user        <user> -- <cmd> [args…]   # runs cmd as user via sudo -u -H (HOME forced)
as_user_login  <user> -- <cmd> [args…]   # same but via `bash --login -c` (sources profile.d)

# plugin/lib/idempotency.sh — primitives with arg-count guards + EX_USAGE=64
ensure_dir           <path> <mode> <owner>:<group>
ensure_line_in_file  <literal-line> <path>             # grep-before-append; no duplicates
ensure_marker_block  <path> <tag> [--top|--bottom] <<EOF ... EOF
ensure_user          <user> <uid> <gid> <shell> <home>
visudo_validate      <sudoers-file>                    # not used in Phase 3

# plugin/lib/distro_detect.sh
detect_distro        # sets AGENTLINUX_DISTRO_VERSION in caller (e.g. "ubuntu-24.04")
```

Entrypoint dispatch (plugin/bin/agentlinux-install lines 167-172):
- Sources `plugin/lib/{log,distro_detect,idempotency,as_user}.sh` IN ORDER.
- Sources every `plugin/provisioner/[0-9][0-9]-*.sh` in numeric order (glob via `compgen -G`).
- Sets `set -euo pipefail` and an ERR trap BEFORE sourcing any provisioner.
- Redirects stdout+stderr to `/var/log/agentlinux-install.log` via `exec > >(tee -a "$LOG") 2>&1`.
- **Consequence for 30-nodejs.sh:** do NOT add `set -euo pipefail`; do NOT add a shebang execution pattern; DO add `#!/usr/bin/env bash` as a shebang for editor/shellcheck compatibility (Phase 2 precedent, see 40-path-wiring.sh line 1).

Existing 40-path-wiring.sh heredoc anchors (the exact strings to search for when patching):

```bash
# Artefact 1 anchor (profile.d case-prepend):
*:/home/agent/.local/bin:*) : ;;
*) PATH="/home/agent/.local/bin:${PATH}" ;;
# EXTEND above this with a .npm-global case block (prepended first).

# Artefact 3 anchor (/etc/agentlinux.env heredoc body):
PATH=/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin
LANG=C.UTF-8
# REPLACE PATH line with npm-global-prefixed form; ADD NPM_CONFIG_PREFIX line.

# Artefact 4 anchor (/etc/cron.d/agentlinux heredoc body):
PATH=/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin
LANG=C.UTF-8
# REPLACE PATH line with npm-global-prefixed form.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin/provisioner/30-nodejs.sh (NodeSource Node 22 + per-user npm prefix)</name>
  <files>plugin/provisioner/30-nodejs.sh</files>
  <read_first>
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Code Examples §Example 1 (complete ~80-LOC skeleton with inline rationale) — this is the authoritative implementation target
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Common Pitfalls §Pitfall 1 (nodesource.sources vs. nodesource.list — the CONTEXT-locked legacy path is wrong; gate on BOTH but primary is `.sources`)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Common Pitfalls §Pitfall 6 (DEBIAN_FRONTEND=noninteractive required)
    - `plugin/provisioner/10-agent-user.sh` — file header contract (shebang + block comment + bookend `log_info NN-name: starting/done`), lines 1–35 of the file
    - `plugin/provisioner/40-path-wiring.sh` lines 1–40 — Phase 2 provisioner header template to follow verbatim for comment style
    - `plugin/lib/idempotency.sh` — `ensure_dir`, `ensure_line_in_file` signatures; they have arg-count guards (EX_USAGE=64) and re-assert mode+ownership, so re-runs correct any drift
    - `plugin/lib/as_user.sh` header — `as_user -H` forces `HOME=/home/agent` (load-bearing for future npm invocations; Phase 3 provisioner itself does NOT call npm — but the .npmrc is primed for npm calls at test time)
    - `.claude/skills/agentlinux-installer/SKILL.md` — non-negotiable rules (strict mode inherited from entrypoint; every state mutation through `plugin/lib/idempotency.sh` primitives; no raw echo >>; no sed -i)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Assumptions Log A1, A2, A4, A9, A10 — assumption invariants
    - Threat model T-03-01 (supply-chain: curl | bash to nodesource.com), T-03-02 (.npmrc double-append via echo), T-03-04 (byte-instability on re-run)
  </read_first>
  <action>
    Create `plugin/provisioner/30-nodejs.sh`. Follow the 10-agent-user.sh / 40-path-wiring.sh file header contract:

    1. `#!/usr/bin/env bash` shebang on line 1 (for editor + shellcheck; strict mode is inherited from the entrypoint — do NOT add `set -euo pipefail`).
    2. Block comment naming: sourced-by (`plugin/bin/agentlinux-install`), inherited strict mode (`set -euo pipefail`, ERR trap, tee redirect), requirements satisfied (RT-01, RT-04), ordering rationale (runs AFTER 10-agent-user.sh, BEFORE 40-path-wiring.sh — numeric 10 → 30 → 40), and the "every state mutation through idempotency primitives" invariant (per the agentlinux-installer SKILL).
    3. Bookend `log_info "30-nodejs: starting"` at the top of executable code and `log_info "30-nodejs: done"` at the bottom — greppable transcript boundaries matching 10-agent-user.sh and 40-path-wiring.sh.

    Six executable steps (see §Code Examples §Example 1 for the reference shape; keep under 100 LOC):

    **Step 1 — Pre-reqs (NodeSource's setup_22.x installs these itself; we pre-install for log visibility per §Open Questions Q2):**
    ```bash
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
      curl gnupg ca-certificates apt-transport-https
    ```
    Inline comment: "Pitfall 6 mitigation: DEBIAN_FRONTEND=noninteractive prevents dpkg interactive prompts in the CI Docker harness." Inline comment: "per D-02 [CONTEXT Node.js Install Path] + Research §Open Questions Q2 recommendation (pre-install for log visibility)."

    **Step 2 — Idempotent NodeSource repo add (Pitfall 1 — check `nodesource.sources` PRIMARY + legacy `nodesource.list` for defense-in-depth):**
    ```bash
    if [[ -f /etc/apt/sources.list.d/nodesource.sources ]] || \
       [[ -f /etc/apt/sources.list.d/nodesource.list ]]; then
      log_info "NodeSource apt repo already configured (gate: nodesource.sources/list)"
    else
      log_info "NodeSource apt repo absent — running setup_22.x"
      curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
    fi
    ```
    Inline comment: "T-03-01 mitigation: curl-pipe-bash is acceptable here per `.claude/skills/agentlinux-installer/SKILL.md` §'curl-pipe-bash at outermost entrypoint OR for pinned-URL trusted upstream'. NodeSource is the ADR-005-blessed upstream. SHA-256 verification of the script body is NOT performed (NodeSource does not publish a sibling .sha256); the GPG-signed apt repo the script installs provides ongoing package integrity. This is an accepted tradeoff per ADR-005." Inline comment: "T-03-04 mitigation: the setup script itself rm -f's both legacy and modern filenames before re-creating them, so even if our gate misses (e.g., stale nodesource.list), the script self-heals without leaking byte drift; our gate just prevents the wasted re-run (~3s)."

    **Step 3 — Install nodejs package (idempotent apt-get install):**
    ```bash
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends nodejs
    ```

    **Step 4 — Post-install RT-01 verification:**
    ```bash
    node_major=$(node --version 2>/dev/null | sed 's/^v\([0-9]*\)\..*$/\1/')
    if [[ "${node_major:-0}" -lt 22 ]]; then
      log_error "node v${node_major:-unset} installed but v22 LTS required (RT-01)"
      return 1
    fi
    log_info "Node.js $(node --version) installed (RT-01 — v22 LTS)"
    ```
    Use `return 1` (NOT `exit 1`) — the provisioner is sourced, so `return 1` trips the entrypoint's `set -euo pipefail` ERR trap with proper `src:line` attribution (pattern from 10-agent-user.sh decision).

    **Step 5 — Prefix directory layout (RT-04):**
    ```bash
    ensure_dir /home/agent/.npm-global 0755 agent:agent
    ensure_dir /home/agent/.npm-global/bin 0755 agent:agent
    ensure_dir /home/agent/.npm-global/lib 0755 agent:agent
    ```
    Inline comment: "Pitfall 4 defense: bin/ and lib/ created agent-owned BEFORE any `as_user -- npm install -g` fires, preventing npm from creating them with wrong ownership if it ever raced the install." Inline comment: "per D-03 [CONTEXT Per-User npm Prefix Layout: /home/agent/.npm-global]."

    **Step 6 — ~agent/.npmrc with prefix line (RT-04, T-03-02 mitigation):**
    ```bash
    if [[ ! -f /home/agent/.npmrc ]]; then
      install -m 0644 -o agent -g agent /dev/null /home/agent/.npmrc
    fi
    ensure_line_in_file 'prefix=/home/agent/.npm-global' /home/agent/.npmrc
    chown agent:agent /home/agent/.npmrc
    chmod 0644 /home/agent/.npmrc
    log_info "wrote ~agent/.npmrc (prefix=/home/agent/.npm-global — RT-04)"
    ```
    Inline comment: "T-03-02 mitigation: use `ensure_line_in_file` (grep-before-append) so re-runs produce zero diff — raw `echo >>` would double-append on Pitfall 1 misfire." Inline comment: "chown+chmod re-assertion AFTER ensure_line_in_file: the primitive's internal `install` may leave root-owned artefacts on atomic-rename; same pattern as 10-agent-user.sh's post-`ensure_marker_block` chown (verified in plan 02-03 execution notes — STATE.md 'New decisions from Plan 02-03 execution')."

    Keep the file under 100 LOC. No stray debug output. Match the comment density and rationale-inline style of `40-path-wiring.sh` — every non-obvious choice gets a short inline comment pointing to the pitfall or decision that drove it.

    **Review loop (mandatory before commit):** After writing, dispatch `bash-engineer`, `security-engineer`, and `qa-engineer` per `.claude/skills/review/SKILL.md`. Triage findings, fix actionable ones (re-stage + re-commit if the fix produces a changed file), skip noise. Document any non-fixed findings in the commit message or SUMMARY if closed.

    Commit atomically with a real `git add plugin/provisioner/30-nodejs.sh && git commit --no-gpg-sign -m 'feat(03-01): add 30-nodejs.sh — Node 22 LTS + per-user npm prefix (RT-01, RT-04)'`.
  </action>
  <acceptance_criteria>
    - File `plugin/provisioner/30-nodejs.sh` exists and is readable.
    - `shellcheck --severity=warning --shell=bash --external-sources --source-path=plugin/lib plugin/provisioner/30-nodejs.sh` exits 0.
    - `shfmt -i 2 -ci -bn -d plugin/provisioner/30-nodejs.sh` exits 0 (no diff).
    - `bash -n plugin/provisioner/30-nodejs.sh` exits 0 (syntax check).
    - `grep -c '^' plugin/provisioner/30-nodejs.sh` ≤ 100 (LOC budget).
    - File starts with `#!/usr/bin/env bash` (grep -Pq '^#!/usr/bin/env bash' line 1).
    - File does NOT contain `set -euo pipefail` (grep -L 'set -euo pipefail' confirms — inherited from entrypoint).
    - File contains `log_info "30-nodejs: starting"` AND `log_info "30-nodejs: done"` (bookend contract).
    - File contains `ensure_line_in_file 'prefix=/home/agent/.npm-global' /home/agent/.npmrc` (exact string match — RT-04 proof).
    - File contains `ensure_dir /home/agent/.npm-global 0755 agent:agent` (RT-04 proof).
    - File contains `DEBIAN_FRONTEND=noninteractive` at least twice (both apt-get install lines — Pitfall 6).
    - File contains BOTH `nodesource.sources` AND `nodesource.list` strings (Pitfall 1 dual-gate).
    - File does NOT contain `sudo npm install -g` (security-engineer rubric — this string is NEVER correct in Phase 3 source).
    - File does NOT contain `echo ` followed by `>>` (no raw append — pattern `grep -Eq 'echo[^$]*>>'` returns empty).
    - File does NOT contain `sed -i` (no in-place editing).
    - File does NOT contain `/usr/local/bin/` (no wrapper shim path — DOC-02).
    - Review loop executed: `bash-engineer`, `security-engineer`, `qa-engineer` findings triaged with no actionable-and-unfixed findings remaining.
    - Atomic commit created: `git log -1 --pretty=%s` matches `feat(03-01):.*30-nodejs\.sh` and touches EXACTLY `plugin/provisioner/30-nodejs.sh` (verify via `git show --stat HEAD | grep -c '^ plugin/'` = 1).
  </acceptance_criteria>
  <verify>
    <automated>
    set -e
    cd /home/agent/agent-linux
    test -r plugin/provisioner/30-nodejs.sh
    shellcheck --severity=warning --shell=bash --external-sources --source-path=plugin/lib plugin/provisioner/30-nodejs.sh
    shfmt -i 2 -ci -bn -d plugin/provisioner/30-nodejs.sh
    bash -n plugin/provisioner/30-nodejs.sh
    loc=$(grep -c '^' plugin/provisioner/30-nodejs.sh); [ "$loc" -le 100 ] || { echo "LOC budget exceeded: $loc"; exit 1; }
    head -1 plugin/provisioner/30-nodejs.sh | grep -Pq '^#!/usr/bin/env bash'
    ! grep -q 'set -euo pipefail' plugin/provisioner/30-nodejs.sh
    grep -q '^log_info "30-nodejs: starting"' plugin/provisioner/30-nodejs.sh
    grep -q '^log_info "30-nodejs: done"' plugin/provisioner/30-nodejs.sh
    grep -qF "ensure_line_in_file 'prefix=/home/agent/.npm-global' /home/agent/.npmrc" plugin/provisioner/30-nodejs.sh
    grep -qF "ensure_dir /home/agent/.npm-global 0755 agent:agent" plugin/provisioner/30-nodejs.sh
    [ "$(grep -c 'DEBIAN_FRONTEND=noninteractive' plugin/provisioner/30-nodejs.sh)" -ge 2 ]
    grep -q 'nodesource\.sources' plugin/provisioner/30-nodejs.sh
    grep -q 'nodesource\.list' plugin/provisioner/30-nodejs.sh
    ! grep -q 'sudo npm install -g' plugin/provisioner/30-nodejs.sh
    ! grep -Eq 'echo[^$]*>>' plugin/provisioner/30-nodejs.sh
    ! grep -q 'sed -i' plugin/provisioner/30-nodejs.sh
    ! grep -q '/usr/local/bin/' plugin/provisioner/30-nodejs.sh
    git log -1 --pretty=%s | grep -Eq 'feat\(03-01\):.*30-nodejs\.sh'
    [ "$(git show --stat HEAD --name-only --format='' | grep -c '^plugin/provisioner/30-nodejs\.sh$')" -eq 1 ]
    </automated>
  </verify>
  <done>
    New provisioner in place, dispatched automatically by the installer entrypoint's numeric glob, passes all lints and the forbidden-substring grep chain. Commit touches exactly one file. Review loop ran clean or fixes committed separately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend plugin/provisioner/40-path-wiring.sh — prepend /home/agent/.npm-global/bin + NPM_CONFIG_PREFIX</name>
  <files>plugin/provisioner/40-path-wiring.sh</files>
  <read_first>
    - `plugin/provisioner/40-path-wiring.sh` lines 1–153 (the entire file — three heredoc artefacts to patch + one unchanged .bashrc block)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Code Examples §Example 2 (the authoritative diff)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Common Pitfalls §Pitfall 4 (PATH prepend order — npm-global FIRST, before .local; matters because any stray /usr/local/bin shim must lose)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-RESEARCH.md` §Common Pitfalls §Pitfall 5 (NPM_CONFIG_PREFIX as belt-and-braces env-var fallback — Pitfall-5-mitigation — per CONTEXT 03-08 `HOME=/home/agent` in systemd/cron; NPM_CONFIG_PREFIX covers the edge case where HOME lookup regresses)
    - `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-CONTEXT.md` §specifics paragraphs about `.npmrc` precedence and the belt-and-braces rule
    - Threat model T-03-03 (NPM_CONFIG_PREFIX must EQUAL .npmrc prefix — split-brain avoidance)
  </read_first>
  <action>
    Edit `plugin/provisioner/40-path-wiring.sh` to extend three of its four heredoc artefacts. `.bashrc` marker block (Artefact 2) stays UNCHANGED — it sources profile.d, which now carries the npm-global prepend. Only three edits, surgical:

    **Edit A — Artefact 1 (`/etc/profile.d/agentlinux.sh` heredoc, lines ~60–78):**
    Currently the heredoc body has a single `case` block prepending `/home/agent/.local/bin`. PREPEND (physically before the existing `case`) a new case block for `/home/agent/.npm-global/bin`. The stack-order is important: npm-global gets case-prepended FIRST so it lands FIRST in the final PATH (case-prepend stacks LIFO — first case-block runs first, prepends first, then second case-block prepends in front of it — final order has the LAST prepended first; verify by reading the pattern in §Code Examples §Example 2 literal diff).

    Correct stacking pattern (exactly as shown in Research §Example 2):
    ```sh
    case ":${PATH}:" in
      *:/home/agent/.npm-global/bin:*) : ;;
      *) PATH="/home/agent/.npm-global/bin:${PATH}" ;;
    esac
    case ":${PATH}:" in
      *:/home/agent/.local/bin:*) : ;;
      *) PATH="/home/agent/.local/bin:${PATH}" ;;
    esac
    export PATH
    ```
    Final runtime PATH (after both blocks): `/home/agent/.local/bin:/home/agent/.npm-global/bin:...` — the `.local/bin` case runs SECOND and prepends LAST, so it ends up FIRST. **That is wrong for this phase.** Read Pitfall 4 again: we need `/home/agent/.npm-global/bin` FIRST. Therefore the correct stacking order is REVERSED: put the `.local/bin` case BLOCK FIRST, the `.npm-global/bin` case SECOND. That way `.npm-global/bin` runs last and lands first in PATH.

    **FINAL CORRECT PATTERN (use this verbatim, overriding the diff shown in §Example 2 if it disagrees — honor the invariant from Pitfall 4, NOT the literal diff):**
    ```sh
    # Prepend in order so npm-global lands FIRST in PATH (Pitfall 4 — a stray
    # /usr/local/bin shim must lose to the agent-owned binary). Case-guards
    # prevent double-prepend on re-source (INST-02 idempotency).
    case ":${PATH}:" in
      *:/home/agent/.local/bin:*) : ;;
      *) PATH="/home/agent/.local/bin:${PATH}" ;;
    esac
    case ":${PATH}:" in
      *:/home/agent/.npm-global/bin:*) : ;;
      *) PATH="/home/agent/.npm-global/bin:${PATH}" ;;
    esac
    export PATH
    ```
    Also update the "Phase 3 will extend this case…" comment (lines 71–72 of the current file) to the completed state: `# PATH prepend stack: .npm-global/bin ends first in PATH (RT-02 / Pitfall 4), .local/bin second, system last. Case-guards are idempotent (INST-02).`

    **Edit B — Artefact 3 (`/etc/agentlinux.env` heredoc, lines ~124–128):**
    Replace the `PATH=...` line and ADD a `NPM_CONFIG_PREFIX` line between PATH and LANG:
    ```
    PATH=/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin
    NPM_CONFIG_PREFIX=/home/agent/.npm-global
    LANG=C.UTF-8
    LC_ALL=C.UTF-8
    ```
    Update the existing heredoc comment to note "NPM_CONFIG_PREFIX as belt-and-braces fallback: systemd EnvironmentFile sees this even if ~agent/.npmrc read regresses (Pitfall 5). T-03-03 mitigation: value MUST be byte-identical to the prefix line in ~agent/.npmrc (/home/agent/.npm-global) — split-brain avoidance."

    **Edit C — Artefact 4 (`/etc/cron.d/agentlinux` heredoc, lines ~140–149):**
    Replace the `PATH=...` line with the npm-global-prefixed form (DO NOT add `NPM_CONFIG_PREFIX` — cron.d's vixie-cron parser only honors the environment-like lines for a specific set of keys; PATH is the reliable one. Env var fallback for cron is unnecessary because cron jobs invoke bash which sources agent's .bashrc → profile.d):
    ```
    PATH=/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin
    LANG=C.UTF-8
    LC_ALL=C.UTF-8
    ```
    Keep the existing comment block above the PATH line intact (the "AgentLinux cron environment" preamble + example shape + "Phase 2 ships NO default jobs" lines).

    **Update the file-header block comment (lines ~21–25):** Change the line `# Phase 3 will prepend $HOME/.npm-global/bin to all three files that carry a literal PATH.` to `# Phase 3 extended this file: /home/agent/.npm-global/bin is prepended in all three literal-PATH artefacts (profile.d case-stack, agentlinux.env, cron.d); NPM_CONFIG_PREFIX=/home/agent/.npm-global ships in agentlinux.env as a belt-and-braces Pitfall 5 mitigation (env var precedence over .npmrc per npm docs).`

    **Update the file-header "Requirements satisfied" block:** Add two lines under the existing BHV-02..06 list:
    ```
    #   RT-02 — /home/agent/.npm-global/bin is on PATH in every invocation mode
    #   RT-04 — NPM_CONFIG_PREFIX env var (belt-and-braces for Pitfall 5)
    ```

    The existing "Four artefacts cover all six invocation modes" list stays correct — still four artefacts, each still covers its mode(s); Phase 3 only extends their PATH content.

    **Review loop (mandatory):** Dispatch `bash-engineer`, `security-engineer`, `qa-engineer` per `.claude/skills/review/SKILL.md`. Verify: (a) the three single-quoted heredoc tags (`<<'PROFILE'`, `<<'ENVFILE'`, `<<'CRON'`) are UNCHANGED (no shift to double-quoted — would break T-03-04 byte idempotency); (b) the two case-blocks in profile.d are correctly nested (no syntax error); (c) no new forbidden substrings introduced (`sudoers.d` path literal, `/usr/local/bin/` as a shim-target, `sudo npm install -g`).

    Atomic commit: `git add plugin/provisioner/40-path-wiring.sh && git commit --no-gpg-sign -m 'feat(03-01): extend 40-path-wiring.sh — prepend .npm-global/bin + NPM_CONFIG_PREFIX (RT-04)'`.
  </action>
  <acceptance_criteria>
    - `shellcheck --severity=warning --shell=bash --external-sources --source-path=plugin/lib plugin/provisioner/40-path-wiring.sh` exits 0.
    - `shfmt -i 2 -ci -bn -d plugin/provisioner/40-path-wiring.sh` exits 0 (no diff).
    - `bash -n plugin/provisioner/40-path-wiring.sh` exits 0.
    - The file contains two `case ":${PATH}:" in` blocks (one for `.local/bin`, one for `.npm-global/bin`) in that source order (verify with `grep -A1 -B0 'case ":\${PATH}:"'` showing both patterns).
    - The file contains `PATH=/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin` as a literal line AT LEAST TWICE (once in the ENVFILE heredoc, once in the CRON heredoc).
    - The file contains `NPM_CONFIG_PREFIX=/home/agent/.npm-global` EXACTLY ONCE (in the ENVFILE heredoc body).
    - The file does NOT contain the old literal `PATH=/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin` (the un-extended Phase 2 PATH line) — grep confirms removal from both ENVFILE and CRON heredoc bodies.
    - The three heredoc tags are still single-quoted: `grep -c "<<'PROFILE'"` = 1, `grep -c "<<'BASHRC'"` = 1, `grep -c "<<'ENVFILE'"` = 1, `grep -c "<<'CRON'"` = 1 (all four present, single-quoted — T-03-04 mitigation).
    - The file-header block comment no longer contains the string `Phase 3 will prepend` (must reference completion, not future tense).
    - The file-header "Requirements satisfied" list now names RT-02 AND RT-04.
    - `.bashrc` heredoc body is UNCHANGED: grep for `AgentLinux PATH guard — sources /etc/profile.d/agentlinux.sh unconditionally` still returns a match (the descriptive comment Phase 2 wrote).
    - File does NOT contain `sudo npm install -g`, `/usr/local/bin/agentlinux`, or `sudoers.d` (security-engineer rubric — same forbidden-substring set as Phase 2 positive-verify chain).
    - Cross-grep: the PATH literal string in `/etc/agentlinux.env` heredoc body is byte-identical to the PATH literal in the `/etc/cron.d/agentlinux` heredoc body (count ≥ 2 for the exact literal).
    - Review loop executed and no actionable-unfixed findings remain.
    - Atomic commit created: `git log -1 --pretty=%s` matches `feat\(03-01\):.*40-path-wiring.*RT-04` and touches EXACTLY `plugin/provisioner/40-path-wiring.sh` (one file).
  </acceptance_criteria>
  <verify>
    <automated>
    set -e
    cd /home/agent/agent-linux
    shellcheck --severity=warning --shell=bash --external-sources --source-path=plugin/lib plugin/provisioner/40-path-wiring.sh
    shfmt -i 2 -ci -bn -d plugin/provisioner/40-path-wiring.sh
    bash -n plugin/provisioner/40-path-wiring.sh
    [ "$(grep -c 'case ":\${PATH}:"' plugin/provisioner/40-path-wiring.sh)" -ge 2 ]
    [ "$(grep -cF 'PATH=/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin' plugin/provisioner/40-path-wiring.sh)" -ge 2 ]
    [ "$(grep -cF 'NPM_CONFIG_PREFIX=/home/agent/.npm-global' plugin/provisioner/40-path-wiring.sh)" -eq 1 ]
    ! grep -qF 'PATH=/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin' plugin/provisioner/40-path-wiring.sh
    [ "$(grep -cF "<<'PROFILE'" plugin/provisioner/40-path-wiring.sh)" -eq 1 ]
    [ "$(grep -cF "<<'BASHRC'" plugin/provisioner/40-path-wiring.sh)" -eq 1 ]
    [ "$(grep -cF "<<'ENVFILE'" plugin/provisioner/40-path-wiring.sh)" -eq 1 ]
    [ "$(grep -cF "<<'CRON'" plugin/provisioner/40-path-wiring.sh)" -eq 1 ]
    ! grep -q 'Phase 3 will prepend' plugin/provisioner/40-path-wiring.sh
    grep -q 'RT-02' plugin/provisioner/40-path-wiring.sh
    grep -q 'RT-04' plugin/provisioner/40-path-wiring.sh
    grep -q 'AgentLinux PATH guard' plugin/provisioner/40-path-wiring.sh
    ! grep -q 'sudo npm install -g' plugin/provisioner/40-path-wiring.sh
    ! grep -q '/usr/local/bin/agentlinux' plugin/provisioner/40-path-wiring.sh
    ! grep -q 'sudoers.d' plugin/provisioner/40-path-wiring.sh
    git log -1 --pretty=%s | grep -Eq 'feat\(03-01\):.*40-path-wiring.*RT-04'
    [ "$(git show --stat HEAD --name-only --format='' | grep -c '^plugin/provisioner/40-path-wiring\.sh$')" -eq 1 ]
    </automated>
  </verify>
  <done>
    40-path-wiring.sh extended with npm-global prepend across three artefacts, NPM_CONFIG_PREFIX added to agentlinux.env, `.bashrc` block unchanged, heredoc tags still single-quoted, idempotency preserved. Commit touches exactly one file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external-network → installer | `curl -fsSL https://deb.nodesource.com/setup_22.x | bash` fetches + executes arbitrary bash from NodeSource over HTTPS. No local integrity verification. |
| installer (root) → agent-owned files | Provisioner writes `/home/agent/.npmrc` and creates `/home/agent/.npm-global/` as root, then chowns to agent:agent. Any mis-ownership leaves an EACCES landmine for future agent npm calls. |
| installer (root) → system apt state | Modifies `/etc/apt/sources.list.d/`, `/etc/apt/preferences.d/`, `/usr/share/keyrings/`. Pin-Priority 600 from NodeSource overrides Ubuntu's default 500 for the `nodejs` package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Supply-chain | `30-nodejs.sh` curl-pipe-bash to deb.nodesource.com | accept (primary) + mitigate (secondary) | Accept the curl-pipe-bash as the ADR-005-blessed upstream path (no sibling .sha256 available for the setup script). Mitigate supply-chain further: use HTTPS origin (cert-verified by `curl -f`), rely on NodeSource's apt-repo GPG signing for ongoing package integrity (installed keyring at `/usr/share/keyrings/nodesource.gpg` via `gpg --dearmor` inside the setup script itself), gate invocation behind existence of `/etc/apt/sources.list.d/nodesource.sources` so re-runs do NOT re-fetch-and-execute. |
| T-03-02 | Tampering / Idempotency | `30-nodejs.sh` write to `~agent/.npmrc` | mitigate | Use `ensure_line_in_file 'prefix=/home/agent/.npm-global' /home/agent/.npmrc` (grep-before-append primitive from plugin/lib/idempotency.sh). Re-runs leave zero diff. Raw `echo >>` is forbidden by the plan's positive-verify grep chain. |
| T-03-03 | Tampering / Configuration split-brain | `NPM_CONFIG_PREFIX` env var vs. `.npmrc` prefix line | mitigate | Both ship the byte-identical value `/home/agent/.npm-global`. The plan's cross-grep acceptance criterion asserts this (one literal line in `.npmrc`, one literal line in `/etc/agentlinux.env`; a future accidental divergence would fail the check). npm precedence: env > user .npmrc > global > builtin — so agreement means no split-brain. |
| T-03-04 | Tampering / Byte-instability on re-run | `30-nodejs.sh` NodeSource repo add | mitigate | NodeSource's setup script is self-healing (`rm -f` both legacy and modern filenames before recreating). Our idempotency gate (dual-check `nodesource.sources` + `nodesource.list`) prevents re-invocation when either exists. INST-02 contract extended in Plan 03-02 (sha256 diff of `~agent/.npmrc` + `/etc/apt/sources.list.d/nodesource.sources` across re-runs). |

T-03-05..T-03-08 are bats/test-surface threats and are addressed in Plan 03-02's threat model.

## ASVS L1 Alignment

| Control | Plan Coverage |
|---------|---------------|
| V14.2.3 — Third-party package dependencies are verified | NodeSource apt repo is GPG-signed; Pin-Priority 600 pins to NodeSource's variant; setup_22.x fetch over HTTPS. Accepted tradeoff: setup script itself is not sha256-verified (not published). |
| V4.1.1 — Least privilege | Every npm config write routes through `as_user` or `ensure_line_in_file` with explicit chown; zero raw `sudo npm` invocation anywhere. Prefix directory agent-owned before any agent npm call. |
| V10.3.2 — Dependency monitoring | Deferred to v0.4+ (Dependabot / Renovate). Phase 3 pins by major (22 LTS); NodeSource tracks patch/minor automatically. |
</threat_model>

<verification>
**Phase-level (run after BOTH tasks commit):**

1. **Provisioner numeric dispatch order**: `ls plugin/provisioner/*.sh | grep -E '[0-9]{2}-' | sort` → must show `10-agent-user.sh`, `30-nodejs.sh`, `40-path-wiring.sh` in that order.
2. **Idempotent installer syntax-pass**: `bash -n plugin/bin/agentlinux-install` + `bash -n plugin/provisioner/*.sh` all exit 0.
3. **Phase 2 tests still green**: `./tests/harness/run.sh` → 104/104 still pass (no regression in harness suite from provisioner additions).
4. **Docker smoke end-to-end** (deferred to post-Plan-03-02 because 30-runtime.bats doesn't exist yet — but the provisioner itself runs and exits 0 on a fresh container):
   ```bash
   ./tests/docker/run.sh ubuntu-24.04
   ```
   Expected in this plan's context: installer log shows `30-nodejs: starting` → `Node.js v22.X.Y installed (RT-01 — v22 LTS)` → `wrote ~agent/.npmrc (prefix=/home/agent/.npm-global — RT-04)` → `30-nodejs: done` followed by the existing Phase 2 40-path-wiring markers. Total end-to-end still completes in < 5 minutes. Phase 2 bats (22 @tests) still green. (30-runtime.bats is added by Plan 03-02 — this plan alone only verifies the provisioner runs without errors and the Phase 2 suite doesn't regress.)
5. **Security-engineer forbidden-substring sweep** across BOTH files:
   - `grep -r 'sudo npm install -g' plugin/` returns 0 matches
   - `grep -rn '/usr/local/bin/' plugin/provisioner/` returns 0 matches
   - `grep -rn 'sudoers.d' plugin/provisioner/30-nodejs.sh plugin/provisioner/40-path-wiring.sh` returns 0 matches
6. **INST-02 idempotency spot-check** (manual in Docker): run installer twice; diff `/home/agent/.npmrc` and `/etc/agentlinux.env` between runs — should be byte-identical. (Automated version lands in 03-02.)

**Commit hygiene:**
- Exactly TWO commits landed on this plan: one for `30-nodejs.sh` creation, one for `40-path-wiring.sh` extension.
- Neither commit touches any file under `tests/` (tests are Plan 03-02's concern).
- Commit messages match the pattern `feat(03-01): ...`.
</verification>

<success_criteria>
**Roadmap §Phase 3 success criteria addressed by this plan:**

1. ✅ RT-01 — `node --version` returns v22.x: satisfied by Task 1 Step 3 (`apt-get install -y nodejs` after NodeSource repo) + Step 4 (hard-fail gate if major < 22). Observable proof lands in Plan 03-02 bats.
2. ⏸ RT-02 — `npm install -g cowsay` across six modes: prerequisites shipped here (PATH wiring + prefix config); observable proof is Plan 03-02's responsibility.
3. ⏸ RT-03 — `npm uninstall -g cowsay` byte-clean: observable proof is Plan 03-02.
4. ✅ RT-04 — `npm config get prefix` under `/home/agent/`: satisfied by Task 1 Step 5+6 (`ensure_dir /home/agent/.npm-global` + `ensure_line_in_file 'prefix=/home/agent/.npm-global'`) plus Task 2 Edit B (belt-and-braces `NPM_CONFIG_PREFIX` in `/etc/agentlinux.env` for Pitfall 5 resilience). Observable proof: Plan 03-02 bats via `assert_user_prefix_in_home`.
5. ⏸ Bats matrix extended with RT-01..04: Plan 03-02 ships `tests/bats/30-runtime.bats`.

**Intrinsic criteria (this plan):**
- Provisioner compiles clean under shellcheck, shfmt, bash -n.
- Under 100 LOC for 30-nodejs.sh.
- Zero forbidden substrings introduced anywhere in Phase 3 provisioner surface.
- Phase 2 harness/bats suites still green (no regression).
- Two atomic commits, each touching exactly one file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nodejs-runtime-per-user-npm-prefix/03-01-SUMMARY.md` using the summary template. Document:

- Files created/modified (with LOC counts).
- Inline decisions taken ("pre-install curl/gnupg via apt vs. let NodeSource do it" → pre-install; "profile.d case stacking order to land npm-global first" → `.local/bin` case first, `.npm-global/bin` case second; any other sub-decisions).
- Review-loop findings triaged (bash-engineer / security-engineer / qa-engineer output summarized).
- Threat-model dispositions confirmed (T-03-01..04 status).
- Any deviation from the plan's acceptance criteria (and why) — including any shellcheck suppressions added.
- Next-step handoff to Plan 03-02 (which bats will be added, which helpers touched).
</output>
