---
phase: 06-distribution-release-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/STABILITY-MODEL.md
autonomous: false
requirements:
  - DOC-01
tags:
  - release
  - docs
  - phase-close

must_haves:
  truths:
    - "README.md exists at repo root with (in order): one-paragraph pitch, Install section (curl-pipe-bash one-command), Verify section (agentlinux list + install claude-code + claude --version), Uninstall section (agentlinux-install --purge and --purge --remove-nodejs), Stability model section (ADR-011 summary), Escape hatch (agentlinux pin example), Requirements (Ubuntu 22.04/24.04 x86_64 + root), Links (docs/decisions + releases + issue tracker)."
    - "README.md contains a version stamp marker of the form <!-- VERSION_START -->vX.Y.Z<!-- VERSION_END --> that scripts/build-release.sh can sed-update at release time."
    - "README.md contains the literal install command: `curl -fsSL https://agentlinux.org/install.sh | sudo bash` as a copy-pasteable code block."
    - "README.md references docs/decisions/011-stability-first-version-pinning.md for users who want the full stability model."
    - "docs/STABILITY-MODEL.md exists as a user-facing one-page companion to ADR-011, covering: curated combos, agentlinux list showing pinned versions, agentlinux upgrade for divergence reconciliation, agentlinux pin for sticky overrides, and a worked example of the `claude update -> agentlinux upgrade` flow."
    - "A behavior-coverage-auditor run at phase-close (after all 4 implementation plans land) emits `TST-07 gate: GREEN` — every Phase 6 req ID (INST-03, TST-03, TST-05, TST-08, CAT-05, DOC-01) has >=1 bats @test citing its req ID OR is satisfied by a non-bats CI gate (TST-05 via release.yml job + bats tests/bats/51-*.bats selector; TST-08 via gate-4-pinned-combo job)."
  artifacts:
    - path: "README.md"
      provides: "User-facing entry point documenting install / verify / uninstall / stability per DOC-01; landing page on github.com/<org>/agent-linux"
      min_lines: 60
      contains: "curl -fsSL https://agentlinux.org/install.sh"
    - path: "docs/STABILITY-MODEL.md"
      provides: "One-page user-friendly companion to ADR-011; diverge + pin semantics"
      min_lines: 40
      contains: "curated combo"
  key_links:
    - from: "README.md Install section"
      to: "packaging/curl-installer/install.sh (via agentlinux.org/install.sh)"
      via: "copy-pasteable curl-pipe-bash command"
      pattern: "curl -fsSL.*install\\.sh.*\\| sudo bash"
    - from: "README.md Stability model section"
      to: "docs/decisions/011-stability-first-version-pinning.md"
      via: "Markdown link"
      pattern: "decisions/011-stability"
    - from: "README.md Stability model section"
      to: "docs/STABILITY-MODEL.md"
      via: "Markdown link for the user-friendly one-page summary"
      pattern: "STABILITY-MODEL\\.md"
    - from: "README.md Uninstall section"
      to: "sudo agentlinux-install --purge / --purge --remove-nodejs"
      via: "documented teardown commands (INST-04 surfacing)"
      pattern: "agentlinux-install --purge"
---

<objective>
Phase 6 Plan 05 — Ship the user-facing README.md + one-page STABILITY-MODEL.md companion to ADR-011. After README content lands AND all other Phase 6 plans complete, run the phase-close TST-07 behavior-coverage-auditor against all of Phase 6 to assert every new requirement ID has test coverage; this closes the phase.

Purpose: DOC-01 is the last v0.3.0 requirement. The README is GitHub's landing page — the first thing a user sees when they land on the repo. It has to be copy-pasteable, accurate, and match the exact commands Plan 06-02 (curl-installer) and Plan 06-04 (release pipeline) implement.

Output:
- `README.md` at repo root (Pattern 7 shape, 06-RESEARCH.md lines 680-740).
- `docs/STABILITY-MODEL.md` as the ADR-011 user-friendly one-pager.
- Phase-close TST-07 behavior-coverage-auditor run emitting `TST-07 gate: GREEN`, logged in 06-05-SUMMARY.md.

Constraints:
- `autonomous: false` — includes Task 3 checkpoint for phase-close review (behavior-coverage-auditor is a subagent spawn; its pass/fail is the phase-close acceptance criterion).
- Wave 1 assignment even though this plan references Plan 06-02's install.sh command — the README's copy-pasteable snippets document a URL stable under the ADR-006 contract (`curl -fsSL https://agentlinux.org/install.sh | sudo bash`). This command's bytes do not depend on 06-02's exact implementation — just that the URL resolves. Wave 1 parallel with 06-01 + 06-02 is fine. The Task 3 phase-close checkpoint WAITS for 06-01..06-04 completion before running.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 6 locked decisions + research
@.planning/phases/06-distribution-release-pipeline/06-CONTEXT.md
@.planning/phases/06-distribution-release-pipeline/06-RESEARCH.md
@.planning/phases/06-distribution-release-pipeline/06-VALIDATION.md

# ADR the stability model documents
@docs/decisions/011-stability-first-version-pinning.md
@docs/decisions/006-curl-pipe-bash-plus-deb.md

# TST-07 gate mechanism (from Phase 1 Plan 01-03)
@.claude/agents/behavior-coverage-auditor.md
@.claude/skills/review/SKILL.md

<interfaces>
<!-- Copy-paste surfaces. Every literal command below must match the implementation exactly. -->

**Canonical install command (Plan 06-02 implementation — DO NOT DRIFT from this):**
```
curl -fsSL https://agentlinux.org/install.sh | sudo bash
```

Alternative form (shown as backup):
```
sudo bash -c "$(curl -fsSL https://agentlinux.org/install.sh)"
```

Version pin:
```
AGENTLINUX_VERSION=v0.3.0 curl -fsSL https://agentlinux.org/install.sh | sudo bash
```

**Verify commands (Phase 4/5 behaviors — must match existing CLI):**
```
agentlinux list
agentlinux install claude-code
claude --version    # 2.1.98 for v0.3.0 (pinned_version in catalog.json)
```

**Uninstall (Phase 4 Plan 04-06 --purge semantics):**
```
sudo agentlinux-install --purge
sudo agentlinux-install --purge --remove-nodejs
```

**Version stamp shape:** paired markers `<!-- VERSION_START -->v0.3.0<!-- VERSION_END -->`. Plan 06-01's scripts/build-release.sh does NOT mutate README in v0.3.0 (per Research §Open Question #2 — ship with a link, not auto-updated stamp). Markers are informational, future-use.

**Phase 6 req IDs the phase-close auditor must verify:**
- INST-03 — tests/bats/60-curl-installer.bats (Plan 06-02 — 3 @tests: main-wrapper, good-sha, tampered-sha)
- CAT-05 — tests/bats/10-installer.bats (Plan 06-01 — 2 @tests: snapshot exists, byte-stable)
- TST-03 — .github/workflows/nightly-qemu.yml + tests/qemu/boot.sh + green workflow_dispatch (Plan 06-03)
- TST-05 — .github/workflows/release.yml gate-2 (Docker) + gate-3 (QEMU) both run tests/bats/51-*.bats (Plan 06-04)
- TST-08 — .github/workflows/release.yml gate-4-pinned-combo (Plan 06-04)
- DOC-01 — README.md + docs/STABILITY-MODEL.md (this plan)

The behavior-coverage-auditor's rubric (`.claude/agents/behavior-coverage-auditor.md`) handles "req satisfied by CI gate" vs "req satisfied by bats @test" — it reads Phase 6's frontmatter requirements plus each plan's SUMMARY.md and verifies coverage per req ID.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md at repo root — Pattern 7 shape + version-stamp markers</name>
  <files>README.md</files>
  <action>
    Create README.md using Pattern 7 (06-RESEARCH.md lines 682-740). The file structure follows (in order):

    1. Version stamp marker on line 1: `<!-- VERSION_START -->v0.3.0<!-- VERSION_END -->`
    2. `# AgentLinux` title + one-line tagline "Agent-ready Ubuntu, one command."
    3. One-paragraph pitch: "AgentLinux provisions a dedicated `agent` user with a correctly-owned Node.js runtime so agent tools like Claude Code and GSD self-update without EACCES or sudo fights. Curated stable versions; explicit override with `agentlinux pin`."
    4. Two GitHub Actions badges (test + release workflows).
    5. `## Install` section with the canonical curl-pipe-bash command in a fenced bash code block, PLUS the alternative `sudo bash -c "$(...)"` form, PLUS the AGENTLINUX_VERSION env-pinned form. One short paragraph after the commands: "The installer downloads the release tarball and verifies its SHA256 against a sibling asset on GitHub Releases before executing anything. A tampered or partially-downloaded tarball aborts the install with a clear error."
    6. `## Verify` section: fenced code block with `agentlinux list`, `agentlinux install claude-code`, `claude --version`. One sentence: "`agentlinux list` also shows each agent's `pinned_version` — the curated version AgentLinux tests end-to-end together before each release."
    7. `## Uninstall` section: fenced code block with `sudo agentlinux-install --purge` + `sudo agentlinux-install --purge --remove-nodejs`. One short paragraph describing what --purge removes (agent user home, /etc/profile.d wiring, sudoers drop-in, catalog staging, install root) and noting re-run starts clean.
    8. `## Stability model` section: 2-3 paragraph summary of ADR-011 per Pattern 7 (research lines 718-728). Link to `docs/STABILITY-MODEL.md` and `docs/decisions/011-stability-first-version-pinning.md`.
    9. `### Escape hatch` sub-section: fenced code block with `agentlinux pin claude-code=latest` / `=curated` / `gsd=1.38.0` examples.
    10. `## Requirements`: bullet list — Ubuntu 22.04 LTS or 24.04 LTS (x86_64); root/sudo; curl preinstalled. Note: "Not yet supported in v0.3.0: ARM, Fedora/Alma/Arch (v0.4+)."
    11. `## Security` section: one paragraph describing main-wrapper safety + HTTPS + SHA256 + GPG on v0.4+ roadmap. Link to Security tab.
    12. `## Links` section: source+issues, releases, architecture decisions (docs/decisions/), docs/HARNESS.md.

    **Literal commands that MUST appear verbatim:**
    - `curl -fsSL https://agentlinux.org/install.sh | sudo bash`
    - `sudo agentlinux-install --purge`
    - `sudo agentlinux-install --purge --remove-nodejs`
    - `agentlinux list`
    - `agentlinux install claude-code`
    - `claude --version`
    - `agentlinux pin claude-code=latest`
    - `agentlinux pin claude-code=curated`

    **Notes:**
    - Replace `agentlinux/agent-linux` everywhere with the actual GitHub org/repo. Grep `.github/workflows/*.yml` for canonical `${{ github.repository }}` shape.
    - If an existing `index.html` at repo root has content worth preserving (`test -f index.html`), add an "About" section at the BOTTOM (after Links).
    - **NO EMOJIS** per CLAUDE.md emoji policy — no emoji in section headers, no emoji anywhere.
    - Run `pre-commit run --files README.md` if markdownlint is configured; otherwise rely on technical-writer review in Task 3.

    Per-task atomic commit: `docs(06-05): add README.md with install/verify/uninstall/stability sections (DOC-01)`.
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'curl -fsSL https://agentlinux.org/install.sh' README.md && grep -q '## Install' README.md && grep -q '## Verify' README.md && grep -q '## Uninstall' README.md && grep -q '## Stability model' README.md && grep -q 'agentlinux-install --purge' README.md && grep -q 'agentlinux pin' README.md && grep -q 'VERSION_START' README.md && grep -q 'docs/decisions/011-stability-first-version-pinning.md' README.md && ! grep -qP '[\x{1F300}-\x{1F9FF}]' README.md</automated>
  </verify>
  <done>
    README.md exists at repo root with all required sections (pitch, Install, Verify, Uninstall, Stability, Escape hatch, Requirements, Security, Links). Contains the canonical curl-pipe-bash command verbatim. Contains both --purge flavors. Contains the VERSION_START marker. References ADR-011. No emojis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/STABILITY-MODEL.md — ADR-011 user-friendly companion</name>
  <files>docs/STABILITY-MODEL.md</files>
  <action>
    Create `docs/STABILITY-MODEL.md` as a one-page user-friendly explanation of ADR-011. Sections:

    1. Title: `# AgentLinux Stability Model`, tagline quoting: "> The TL;DR of [ADR-011](decisions/011-stability-first-version-pinning.md)."
    2. 1-paragraph intro: "AgentLinux ships *curated combos*: every catalog agent pinned to an exact version that we test together end-to-end before each release. You install one combo and everything just works. When you want to run ahead of the curated pin, you can — and `agentlinux upgrade` + `agentlinux pin` give you a clean way to reconcile."
    3. `## What's a curated combo` — mention `/opt/agentlinux/catalog/<version>/catalog.json` stage path, then bullet-list v0.3.0 pins: claude-code 2.1.98, gsd 1.37.1, playwright 1.59.1. Mention TST-08 blocks red combos.
    4. `## The three divergence states` — describe `installed` vs `curated` vs `upstream latest` comparison. List results: `synced`, `override-ahead`, `override-behind`.
    5. `## Worked example: "I ran claude update"` — fenced code block simulating the `claude update` → `agentlinux upgrade` workflow with three-way reconcile prompt.
    6. `## Escape hatch: agentlinux pin` — fenced code block showing `pin claude-code=latest`, `pin claude-code=curated`, `pin gsd=1.38.0`.
    7. `## Why pin at all (the trade-off)` — explain what happens without pinning (Playwright/Chromium drift, breaking changes sneak in). Conclusion: "Pinning is the explicit contract: we test exactly what we ship, and you decide when to move."
    8. `## Related` — links to ADR-011, ADR-006, README.md.

    **Literal values MUST appear verbatim:**
    - `2.1.98` (claude-code pinned_version)
    - `1.37.1` (gsd pinned_version)
    - `1.59.1` (playwright pinned_version)
    - `agentlinux pin`
    - `agentlinux upgrade`
    - `decisions/011-stability-first-version-pinning.md`

    **No emojis** per CLAUDE.md policy.

    Per-task atomic commit: `docs(06-05): add docs/STABILITY-MODEL.md — user-friendly ADR-011 companion (DOC-01)`.

    **Why a separate file from ADR-011:** ADRs are decision records for maintainers/future architects. The stability-model page is for users who just curled-and-bashed their Ubuntu and want to know "why do I have to think about pinning?" Two audiences, two files.
  </action>
  <verify>
    <automated>test -f docs/STABILITY-MODEL.md && grep -q 'curated combo' docs/STABILITY-MODEL.md && grep -q '2.1.98' docs/STABILITY-MODEL.md && grep -q '1.37.1' docs/STABILITY-MODEL.md && grep -q '1.59.1' docs/STABILITY-MODEL.md && grep -q 'agentlinux pin' docs/STABILITY-MODEL.md && grep -q 'agentlinux upgrade' docs/STABILITY-MODEL.md && grep -q 'decisions/011-stability-first-version-pinning.md' docs/STABILITY-MODEL.md && ! grep -qP '[\x{1F300}-\x{1F9FF}]' docs/STABILITY-MODEL.md</automated>
  </verify>
  <done>
    docs/STABILITY-MODEL.md exists with all 8 sections. All three v0.3.0 pinned_versions are cited verbatim. `agentlinux upgrade` + `agentlinux pin` commands shown. Links to ADR-011 and ADR-006. No emojis.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - README.md (user-facing landing page per DOC-01)
    - docs/STABILITY-MODEL.md (ADR-011 user companion)
    - Technical-writer + fact-checker review of both docs per CLAUDE.md Review Loop table (docs → technical-writer + fact-checker)
    - behavior-coverage-auditor spawn at phase-close against ALL of Phase 6 (every req ID + every plan SUMMARY)
  </what-built>
  <how-to-verify>
    **Part A — README + STABILITY-MODEL review loop:**
    1. technical-writer subagent reviews README.md for prose clarity, section ordering, and command accuracy (every literal command copy-pastes and matches the 06-02 / 06-04 implementations).
    2. fact-checker subagent reviews both files for claim correctness: pinned_version values match catalog.json; install.sh URL matches what deploy.yml publishes; `--purge` semantics match Plan 04-06 implementation; `agentlinux upgrade` divergence labels match Plan 04-04 implementation.
    3. Render README.md via `gh browse` (or a local markdown preview) on the branch — expect clean rendering, no broken links, all code blocks properly fenced.

    **Part B — Phase-close TST-07 behavior-coverage-auditor (MANDATORY, per orchestrator quality-gate "06-05 (phase-close) explicitly spawns behavior-coverage-auditor for TST-07"):**

    **This step WAITS for Plans 06-01, 06-02, 06-03, 06-04 all to complete.** Run AFTER all four are merged and their SUMMARYs are in place.

    Spawn the behavior-coverage-auditor with this prompt:
    ```
    Audit Phase 6 (.planning/phases/06-distribution-release-pipeline/). Verify every
    requirement ID listed in the phase's REQUIREMENTS.md allocation (INST-03, TST-03,
    TST-05, TST-08, CAT-05, DOC-01) has either:
      (a) at least one bats @test citing the req ID in tests/bats/*.bats, OR
      (b) a non-bats CI gate citing the req ID in a workflow-job name or step-name
          comment (acceptable for TST-03, TST-05, TST-08, DOC-01 where the req is
          pipeline-level or docs-level, not bats-level).
    Emit `TST-07 gate: GREEN` on pass, `TST-07 gate: RED` with the missing req IDs
    on fail.
    ```

    Expected output shape (per HARNESS.md §4 + Phase 1 Plan 01-03 subagent contract):
    ```
    Phase 6 requirement coverage audit
    INST-03 — tests/bats/60-curl-installer.bats (3 @tests) — COVERED
    CAT-05  — tests/bats/10-installer.bats (2 @tests) — COVERED
    TST-03  — .github/workflows/nightly-qemu.yml + tests/qemu/boot.sh — COVERED (CI-gate)
    TST-05  — .github/workflows/release.yml gate-3-qemu + gate-2-docker run 51-*.bats — COVERED (CI-gate)
    TST-08  — .github/workflows/release.yml gate-4-pinned-combo — COVERED (CI-gate)
    DOC-01  — README.md + docs/STABILITY-MODEL.md — COVERED (docs-gate)

    TST-07 gate: GREEN
    ```

    **If TST-07 gate: RED**, this checkpoint does NOT resume — the gap-closure workflow is invoked to plug the missing coverage before phase can close. A RED gate means Phase 6 is not shippable.

    **Part C — final ship smoke (OPTIONAL but highly recommended):**
    On a fresh Ubuntu 22.04 or 24.04 cloud VM (or GitHub Codespaces, or local KVM guest), run:
    ```
    AGENTLINUX_VERSION=v0.3.0-rc1 curl -fsSL https://agentlinux.org/install.sh | sudo bash
    sudo -u agent agentlinux list
    sudo -u agent agentlinux install claude-code
    sudo -u agent claude --version   # expect 2.1.98
    sudo -u agent claude update      # AGT-02: should succeed with no EACCES
    ```
    Record the transcript in 06-05-SUMMARY.md as proof the end-to-end story works from the user's perspective — this IS DOC-01's validation (the README instructions produce the documented outcome).

    **Why this checkpoint is blocking:** DOC-01 + TST-07 both need human judgment. The TST-07 auditor is the canonical Phase-close gate per TST-07 requirement; no autonomous signal is authoritative here. The final ship-smoke in Part C is the user-level DOC-01 validation — if the README's literal commands don't produce the promised outcome, DOC-01 fails.
  </how-to-verify>
  <resume-signal>
    Type "approved" with:
    (a) behavior-coverage-auditor output pasted showing `TST-07 gate: GREEN`
    (b) Part C ship-smoke transcript or a documented reason it was deferred post-tag
    (c) technical-writer + fact-checker triage notes
    Else describe the blocking failure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README user → agentlinux.org/install.sh | User reads the README, copies the exact command, pipes it through sudo bash. The command's bytes must match deploy.yml's served install.sh for the user's trust to hold. |
| README claims → v0.3.0 implementation | Every literal command must be tested and match what 06-02 (curl-installer) + 06-04 (release.yml) actually produce. Drift between README and code breaks DOC-01. |
| docs/STABILITY-MODEL.md → ADR-011 | The companion claims must not contradict the authoritative ADR. fact-checker verifies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering / Trust | README accuracy drift | mitigate | fact-checker reviews every literal command against the implementations; technical-writer reviews prose; Part C ship-smoke exercises the README end-to-end from a fresh Ubuntu VM. |
| T-06-README (drift over time) | Integrity | README as v0.3.x evolves | accept | v0.3.0 snapshot ships; v0.3.1 + minor bumps update via standard PR flow. Pre-commit shellcheck + pre-commit markdownlint (if configured) catches basic regressions. No auto-versioning stamp yet (deferred per Research §Open Question #2). |
| T-06-TST07 | Process / Integrity | Phase-close coverage skip | mitigate | Task 3 checkpoint EXPLICITLY spawns behavior-coverage-auditor per TST-07 contract from Phase 1 Plan 01-03. RED gate blocks phase close; GREEN is the authoritative signal that every Phase 6 req has coverage. |
</threat_model>

<verification>
Phase-level checks before marking plan + phase DONE:

1. README.md + docs/STABILITY-MODEL.md exist and satisfy each other's verify-step patterns (commands above).
2. technical-writer + fact-checker reviews both files; triaged comments tracked in 06-05-SUMMARY.md.
3. behavior-coverage-auditor emits `TST-07 gate: GREEN` on the whole Phase 6.
4. Part C ship-smoke (optional but recommended) — real curl-pipe-bash on a fresh Ubuntu produces the documented outcome.
5. `grep -qP '[\x{1F300}-\x{1F9FF}]'` on both docs returns non-zero (no emojis).
6. All markdown links resolve (pre-commit markdownlint if configured; else manual review).
</verification>

<success_criteria>
- README.md is the canonical v0.3.0 landing page with accurate install/verify/uninstall/stability content; no emojis; VERSION_START marker in place.
- docs/STABILITY-MODEL.md is a user-friendly one-pager linking to ADR-011 with concrete worked example.
- behavior-coverage-auditor gate GREEN — every Phase 6 req ID (INST-03, TST-03, TST-05, TST-08, CAT-05, DOC-01) has coverage (bats @test OR CI gate OR docs presence).
- DOC-01 requirement advanced: a user can install, verify, and uninstall AgentLinux using only the README's commands.
- Phase 6 closeable: all 6 req IDs covered across 5 plans.
- Review-loop comments triaged (technical-writer + fact-checker).
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-release-pipeline/06-05-SUMMARY.md` with:
- README.md final line count
- docs/STABILITY-MODEL.md final line count
- technical-writer + fact-checker triage notes
- behavior-coverage-auditor output pasted (must contain `TST-07 gate: GREEN`)
- Part C ship-smoke transcript OR a documented reason for deferral (e.g., "v0.3.0-rc1 hasn't cut yet; will run Part C against v0.3.0 post-tag")
- Confirmation all 6 Phase 6 req IDs are covered: INST-03, TST-03, TST-05, TST-08, CAT-05, DOC-01
- Phase 6 ship-status: READY or BLOCKED (with blocker details)
</output>
