---
phase: 01-complete-website
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - PROB-01
  - FEAT-01
  - FEAT-02
  - COMP-01

must_haves:
  truths:
    - "Scrolling past the hero reveals a problem section describing pain points with local machine, Docker, and generic VMs"
    - "A features section displays AgentLinux capabilities in a grid with SVG icons"
    - "A comparison section narratively walks through alternatives and how AgentLinux is better"
    - "Each feature has a Lucide-style SVG icon, a title, and a technical description"
  artifacts:
    - path: "index.html"
      provides: "Problem, features, and comparison content sections"
      contains: "problem"
  key_links:
    - from: "nav #problem link"
      to: "problem section"
      via: "anchor id"
      pattern: "id=\"problem\""
    - from: "nav #features link"
      to: "features section"
      via: "anchor id"
      pattern: "id=\"features\""
---

<objective>
Build the three main content sections: problem, features, and comparison.

Purpose: These sections form the core persuasion arc of the landing page — identify the pain (problem), present the solution (features), and prove it's better (comparison). Together they take a visitor from "I'm struggling with agent runtimes" to "AgentLinux solves this."

Output: Three fully styled content sections with copy, SVG icons, and responsive layout inserted into the existing `index.html`.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-complete-website/01-CONTEXT.md
@.planning/phases/01-complete-website/01-RESEARCH.md
@.planning/phases/01-complete-website/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build problem section with narrative walkthrough of agent runtime pain points</name>
  <files>index.html</files>
  <action>
Replace the `#problem` placeholder section in `index.html` with the full problem section content.

**Section structure:**
- Section heading: something like "The problem with running agents today" (Claude drafts exact copy)
- Brief intro paragraph setting up the narrative
- Three subsections, each walking through an alternative and its friction:
  1. **Local machine** (merged local + sandboxed per user decision): Agents sharing your filesystem, eating your CPU, no isolation, security risks, "one wrong `rm -rf` away from disaster"
  2. **Docker**: Not designed for long-running agent sessions, no persistent desktop environment, networking complexity, agents can't install packages without rebuilding images, "containers are for apps, not residents"
  3. **Generic VMs**: Heavy setup, manual provisioning, not optimized for agent workloads, no agent-specific tooling, "you become the sysadmin for your agent"

**Narrative style:**
- Walk through each alternative as a story of what goes wrong (per user decision: narrative walkthrough, NOT a table)
- Use dual-perspective storytelling: the engineer's frustration AND the agent's experience (per user decision)
- Keep copy "mostly calm and factual, but with strategic selling punches" (per user decision)
- Developer-level technical specifics: mention concrete things like filesystem access, process isolation, package management
- After walking through all three pain points, a brief bridging statement toward "there's a better way" leading into features

**Styling:**
- Full-viewport section (`min-height: 100vh`)
- Use `--bg-secondary` background to differentiate from hero (alternating section backgrounds per research pitfall #6)
- Each pain point subsection could have a small Lucide SVG icon (e.g., monitor for local, box for Docker, server for VMs)
- Adequate spacing between subsections
- Consider using a subtle code-style element (like a `<code>` tag or monospace highlight) for technical terms to reinforce terminal aesthetic without overdoing it
  </action>
  <verify>
    <automated>grep -q 'id="problem"' index.html && grep -q "Docker" index.html && grep -q "VM" index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Problem section displays three runtime alternatives (local machine, Docker, generic VMs) with narrative descriptions of friction for each, using dual-perspective storytelling and technical specifics</done>
</task>

<task type="auto">
  <name>Task 2: Build features grid and comparison narrative sections</name>
  <files>index.html</files>
  <action>
Replace the `#features` and `#comparison` placeholder sections in `index.html`.

**Features section (`#features`):**
- Section heading: e.g., "Built for agents from the ground up" (Claude drafts)
- CSS Grid layout: `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`, `gap: 2rem`
- Mobile: single column at 640px breakpoint
- 6-8 features, each as a card/item with:
  1. Inline Lucide SVG icon (24x24, `stroke="currentColor"`, `stroke-width="2"`) — copy SVG markup from lucide.dev
  2. Feature title (bold)
  3. Technical description (1-2 sentences with concrete details per user decision)
- Suggested features (Claude drafts final list):
  - **Non-root agent user**: Dedicated agent user account with scoped permissions
  - **Pre-configured toolchains**: apt package groups for common agent tasks (git, build tools, language runtimes)
  - **QEMU micro-VM images**: Lightweight VM images for instant agent environments
  - **Snapshot & rollback**: Filesystem snapshots for safe experimentation
  - **Agent-optimized networking**: Pre-configured network access without manual firewall rules
  - **Persistent workspaces**: Survive reboots and session restarts
  - **Headless-first design**: No desktop environment overhead, all resources for the agent
  - **Security boundaries**: Process isolation and resource limits built in
- Each feature card: `--bg-tertiary` background with subtle border, padding, rounded corners (small radius like 4px to stay modern, not bubbly)
- Icons in `--text-secondary` color, titles in `--accent` (white)

**Comparison section (`#comparison`):**
- Section heading: e.g., "Why not just use what exists?" (Claude drafts)
- Narrative walkthrough style (NOT a comparison table — per user decision)
- Walk through each alternative again but this time contrasting with how AgentLinux solves it:
  - Local machine friction -> AgentLinux's isolated agent user and security boundaries
  - Docker friction -> AgentLinux's persistent, full-OS environment
  - Generic VM friction -> AgentLinux's agent-optimized, zero-config images
- End with a strong closing statement: the "strategic selling punch" — something like "a go-to Linux distro choice for running your agents" (per user-provided phrase)
- Use `--bg-secondary` background (alternating from features which uses `--bg-primary`)

**Icon selection — use these Lucide SVGs (copy markup from lucide.dev):**
- terminal, shield, package, hard-drive, wifi, folder-open, layers, lock (or similar — Claude picks best matches for each feature)
  </action>
  <verify>
    <automated>grep -q 'id="features"' index.html && grep -q 'id="comparison"' index.html && grep -q "grid" index.html && grep -c '<svg' index.html | xargs test 5 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Features section displays 6-8 capabilities in a responsive grid, each with an inline Lucide SVG icon, title, and technical description. Comparison section walks through alternatives narratively and contrasts with AgentLinux solutions. Both sections styled consistently with the dark theme.</done>
</task>

</tasks>

<verification>
- Scrolling past hero shows problem section with three runtime pain points
- Features section shows a responsive grid of capabilities with SVG icons
- Comparison section reads as a narrative, not a table
- Nav links for #problem and #features scroll to correct sections
- All content uses black/white/gray palette only
- Copy voice is factual with strategic selling punches, not hype
- Technical specifics mentioned (non-root users, QEMU, apt packages, etc.)
</verification>

<success_criteria>
1. Problem section has three subsections: local machine, Docker, generic VMs — each with narrative friction description
2. Features section has 6-8 items in CSS Grid with inline SVG icons, titles, and descriptions
3. Comparison section uses narrative walkthrough (no comparison table)
4. All sections have correct anchor IDs matching nav links
5. Alternating section backgrounds for visual separation
6. Copy follows dual-perspective storytelling and "calm factual + strategic punches" voice
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-website/01-02-SUMMARY.md`
</output>
