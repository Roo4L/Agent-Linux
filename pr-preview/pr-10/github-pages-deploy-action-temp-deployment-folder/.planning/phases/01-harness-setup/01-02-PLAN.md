---
phase: 01-harness-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
  - plugin/cli/scripts/validate-catalog.mjs
  - .github/workflows/test.yml
  - .github/workflows/nightly-qemu.yml
  - .github/workflows/nightly-mutation.yml
  - .github/workflows/release.yml
  - plugin/cli/stryker.config.json
  - tests/mutation/bash-mutator.sh
  - tests/mutation/README.md
autonomous: true
requirements: [HRN-02, HRN-08, TST-06]

must_haves:
  truths:
    - "pre-commit configuration wires shellcheck, shfmt, biome, and catalog JSON Schema validation"
    - "Four GH Actions workflows (test.yml, nightly-qemu.yml, nightly-mutation.yml, release.yml) exist and parse as valid YAML"
    - "Each workflow is authored to pass on an empty-plugin commit — no missing-file or missing-tool errors"
    - "Mutation scaffolding (stryker config + bash-mutator.sh) is runnable but advisory (does not block merge)"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit pipeline covering all three languages + catalog schema"
      contains: "shellcheck"
    - path: ".github/workflows/test.yml"
      provides: "PR CI — pre-commit + CLI unit tests + Docker bats matrix (matrix empty-skip OK for Phase 1)"
    - path: ".github/workflows/nightly-qemu.yml"
      provides: "Nightly QEMU release-gate workflow skeleton"
    - path: ".github/workflows/nightly-mutation.yml"
      provides: "Nightly stryker + bash-mutator runs, advisory only"
    - path: ".github/workflows/release.yml"
      provides: "Tag → tarball + .deb + sha256 → GitHub Release skeleton"
    - path: "plugin/cli/stryker.config.json"
      provides: "Stryker mutation-testing config for plugin/cli/src (threshold 75 advisory)"
    - path: "tests/mutation/bash-mutator.sh"
      provides: "In-house bash mutator script — runnable, advisory, target 60% score"
    - path: "plugin/cli/scripts/validate-catalog.mjs"
      provides: "Catalog JSON Schema validator wired to the local pre-commit hook"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "plugin/cli/scripts/validate-catalog.mjs"
      via: "local hook `catalog-schema-validate`"
      pattern: "validate-catalog.mjs"
    - from: ".github/workflows/nightly-mutation.yml"
      to: "tests/mutation/bash-mutator.sh"
      via: "nightly run invokes bash-mutator"
      pattern: "bash-mutator.sh"
    - from: ".github/workflows/nightly-mutation.yml"
      to: "plugin/cli/stryker.config.json"
      via: "nightly run invokes stryker"
      pattern: "stryker"
---

<objective>
Land the continuous-integration plumbing required by HARNESS.md §1.2 (pre-commit) and §8 (four workflows), plus the advisory mutation harness scaffolding per HARNESS.md §1.3 and TST-06. Every workflow must pass on an empty-plugin commit; mutation runs must be advisory (non-blocking).

Purpose: A contributor cloning the repo can run `pre-commit run --all-files` and get a clean green; every PR exercises CI; nightly jobs exist but cannot break the `master` branch.
Output: `.pre-commit-config.yaml`, four `.github/workflows/*.yml`, `stryker.config.json`, `tests/mutation/bash-mutator.sh`, `plugin/cli/scripts/validate-catalog.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-harness-setup/01-CONTEXT.md
@docs/HARNESS.md

**Note:** This plan is in Wave 1 and has no dependency on Plan 01 — `files_modified` do not overlap with Plan 01's. Pre-commit hooks reference `plugin/catalog/schema.json` and `plugin/cli/` paths that Plan 01 creates; since Plan 01 runs in the same wave, the execute-phase orchestrator will schedule them so both complete before any later task runs pre-commit. If execution strictness requires, the executor may optionally run after Plan 01 completes — but this plan's tasks do not themselves touch any Plan 01 file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .pre-commit-config.yaml + catalog-schema validator</name>
  <files>.pre-commit-config.yaml, plugin/cli/scripts/validate-catalog.mjs</files>
  <read_first>
    docs/HARNESS.md (§1.2 — the canonical pre-commit config is already specified verbatim; copy from there),
    .planning/phases/01-harness-setup/01-CONTEXT.md (pre-commit coverage: shellcheck, shfmt, biome, catalog JSON Schema validation),
    plugin/catalog/schema.json (to understand what the validator must check — created by Plan 01 in Wave 1)
  </read_first>
  <action>
Two files.

**A. `.pre-commit-config.yaml`** — copy verbatim from `docs/HARNESS.md` §1.2 (the YAML block). Use these exact hook versions and args. After writing, the file must:
- Include the `pre-commit/pre-commit-hooks` repo at `rev: v5.0.0` with hooks: `check-added-large-files`, `check-merge-conflict`, `check-json`, `check-yaml`, `detect-private-key`, `end-of-file-fixer`, `trailing-whitespace`.
- Include `koalaman/shellcheck-precommit` at `rev: v0.10.0` with args `[--severity=warning, --shell=bash, --external-sources]`.
- Include `scop/pre-commit-shfmt` at `rev: v3.9.0-1` with args `[-i, '2', -ci, -bn]`.
- Include `biomejs/pre-commit` at `rev: v1.9.4` with hook `biome-check`, `files: ^plugin/cli/`.
- Include a `local` repo with a hook `catalog-schema-validate` whose `entry` is `node plugin/cli/scripts/validate-catalog.mjs`, `language: system`, `files: ^plugin/catalog/(catalog|agents/.*/recipe)\.json$`, `pass_filenames: false`.
- Set `default_language_version.node: '22'`.

**B. `plugin/cli/scripts/validate-catalog.mjs`** — a small Node ESM script that:
1. Reads `plugin/catalog/schema.json` and parses it as JSON.
2. Reads `plugin/catalog/catalog.json` if it exists; if it does not exist, exits 0 with a message `catalog-schema-validate: no catalog.json yet (Phase 1 skeleton) — skipping`. This is required so the hook passes on an empty-plugin commit.
3. For any `plugin/catalog/agents/*/recipe.json`, parses it and validates against the schema. If no recipe files exist, skip.
4. Validation uses the standard library only (no npm deps installed in Phase 1): a **structural** check — every required top-level field from the schema is present; every `agents[]` entry has a `name` matching `^[a-z][a-z0-9-]*$`, a non-empty `description`, and a non-empty `install`. No need to pull in ajv — Phase 4 can upgrade. The script must be under 80 lines.
5. Exits 0 on success, non-zero with a clear error message on failure.

Exact shape of `validate-catalog.mjs`:
```javascript
#!/usr/bin/env node
// Phase 1 catalog-schema validator: zero-dep structural check.
// Real ajv-based validation arrives with Phase 4 when catalog entries ship.
import { readFileSync, existsSync } from 'node:fs';
import { globSync } from 'node:fs';

const SCHEMA_PATH = 'plugin/catalog/schema.json';
const CATALOG_PATH = 'plugin/catalog/catalog.json';

function fail(msg) { console.error(`catalog-schema-validate: ${msg}`); process.exit(1); }

if (!existsSync(SCHEMA_PATH)) fail(`missing schema at ${SCHEMA_PATH}`);
const schema = JSON.parse(readFileSync(SCHEMA_PATH, 'utf8'));
if (!schema.properties?.agents) fail(`schema at ${SCHEMA_PATH} is malformed`);

if (!existsSync(CATALOG_PATH)) {
  console.log('catalog-schema-validate: no catalog.json yet (Phase 1 skeleton) — skipping');
  process.exit(0);
}

const catalog = JSON.parse(readFileSync(CATALOG_PATH, 'utf8'));
if (!catalog.version || !Array.isArray(catalog.agents)) fail(`${CATALOG_PATH} missing version or agents[]`);
const namePattern = /^[a-z][a-z0-9-]*$/;
for (const a of catalog.agents) {
  if (!a.name || !namePattern.test(a.name)) fail(`agent name "${a.name}" fails pattern ${namePattern}`);
  if (!a.description) fail(`agent ${a.name}: missing description`);
  if (!a.install) fail(`agent ${a.name}: missing install`);
}
console.log(`catalog-schema-validate: ${catalog.agents.length} entries OK`);
```

Use `globSync` if available in Node 22 (`node:fs` exports it); otherwise omit per-recipe iteration — Phase 4 will extend.

Make the validator executable: `chmod +x plugin/cli/scripts/validate-catalog.mjs`.
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml && python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" && grep -q "shellcheck" .pre-commit-config.yaml && grep -q "shfmt" .pre-commit-config.yaml && grep -q "biome" .pre-commit-config.yaml && grep -q "catalog-schema-validate" .pre-commit-config.yaml && test -x plugin/cli/scripts/validate-catalog.mjs && node plugin/cli/scripts/validate-catalog.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .pre-commit-config.yaml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"` exits 0 (valid YAML)
    - `grep -q "shellcheck" .pre-commit-config.yaml` (shellcheck hook present)
    - `grep -q "shfmt" .pre-commit-config.yaml` (shfmt hook present)
    - `grep -q "biome" .pre-commit-config.yaml` (biome hook present)
    - `grep -q "catalog-schema-validate" .pre-commit-config.yaml` (catalog hook present)
    - `test -x plugin/cli/scripts/validate-catalog.mjs` (script is executable)
    - `node plugin/cli/scripts/validate-catalog.mjs` exits 0 on an empty-plugin commit (prints "skipping")
  </acceptance_criteria>
  <done>Pre-commit config wires all four required tools; catalog validator runs on empty-plugin and skips cleanly; all hook versions match HARNESS.md §1.2.</done>
</task>

<task type="auto">
  <name>Task 2: Write four GH Actions workflows (test, nightly-qemu, nightly-mutation, release)</name>
  <files>.github/workflows/test.yml, .github/workflows/nightly-qemu.yml, .github/workflows/nightly-mutation.yml, .github/workflows/release.yml</files>
  <read_first>
    docs/HARNESS.md (§1.1 — layout confirms `.github/workflows/` location),
    docs/HARNESS.md (§1.3 — test layers: pre-commit, CLI unit, bats Docker matrix, QEMU, mutation),
    docs/HARNESS.md (§8 — success-criteria drive what each workflow exercises),
    .github/workflows/deploy.yml (existing v0.1.0 website workflow — DO NOT MODIFY; read only to confirm it stays untouched and uses `on: push: master`)
  </read_first>
  <action>
Write four GH Actions workflow files. Each must parse as valid YAML (`python3 -c "import yaml; yaml.safe_load(open(<path>))"` exits 0). Each must be authored so it **passes on an empty-plugin commit** (no missing-file failures, no required step that fails because plugin/cli/ has no real CLI yet, no bats tests yet, no release tags yet).

The key pattern for "pass on empty plugin": each workflow's jobs either (a) run tools that are no-ops on an empty plugin (pre-commit passes because Plan 01 stubs are valid; biome has no `plugin/cli/src/*.ts` to complain about yet), or (b) guard expensive steps with `if:` conditions that skip when the relevant source is missing.

**A. `.github/workflows/test.yml`** — PR CI. Runs on `push` to any branch and `pull_request` targeting `master`. Path-filtered so it does not fire on website-only changes.
```yaml
name: test
on:
  push:
    branches-ignore: [master]
    paths-ignore:
      - 'index.html'
      - 'assets/**'
      - 'CNAME'
      - 'sitemap.xml'
      - 'robots.txt'
  pull_request:
    branches: [master]
    paths-ignore:
      - 'index.html'
      - 'assets/**'
      - 'CNAME'
      - 'sitemap.xml'
      - 'robots.txt'

jobs:
  pre-commit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install pre-commit
        run: pip install pre-commit==4.0.1
      - name: Run pre-commit
        run: pre-commit run --all-files --show-diff-on-failure

  cli-unit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Skip if plugin/cli has no source yet
        id: guard
        run: |
          if compgen -G "plugin/cli/test/*.test.*" > /dev/null || compgen -G "plugin/cli/test/**/*.test.*" > /dev/null; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_tests=false" >> "$GITHUB_OUTPUT"
            echo "No CLI tests yet — skipping (Phase 1 skeleton)"
          fi
      - name: Install deps
        if: steps.guard.outputs.has_tests == 'true'
        working-directory: plugin/cli
        run: npm install --no-audit --no-fund
      - name: Run tests
        if: steps.guard.outputs.has_tests == 'true'
        working-directory: plugin/cli
        run: npm test

  bats-docker:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        ubuntu: ['22.04', '24.04']
    steps:
      - uses: actions/checkout@v4
      - name: Skip if no bats suite yet
        id: guard
        run: |
          if compgen -G "tests/bats/*.bats" > /dev/null; then
            echo "has_bats=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_bats=false" >> "$GITHUB_OUTPUT"
            echo "No bats suite yet — skipping (lands in Phase 2)"
          fi
      - name: Run Docker bats matrix
        if: steps.guard.outputs.has_bats == 'true'
        run: bash tests/docker/run.sh ubuntu-${{ matrix.ubuntu }}
```

**B. `.github/workflows/nightly-qemu.yml`** — QEMU release-gate suite. Skeleton: scheduled + manual dispatch; guards on whether `tests/qemu/boot.sh` exists.
```yaml
name: nightly-qemu
on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch: {}

jobs:
  qemu:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Skip if QEMU harness not yet implemented
        id: guard
        run: |
          if [[ -x tests/qemu/boot.sh ]]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "QEMU harness arrives in Phase 6 — skipping"
          fi
      - name: Run QEMU suite
        if: steps.guard.outputs.ready == 'true'
        run: bash tests/qemu/boot.sh
```

**C. `.github/workflows/nightly-mutation.yml`** — stryker + bash-mutator, advisory only.
```yaml
name: nightly-mutation
on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  stryker:
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Skip if CLI has no source yet
        id: guard
        run: |
          if compgen -G "plugin/cli/src/*.ts" > /dev/null; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "plugin/cli/src is empty — skipping stryker (advisory)"
          fi
      - name: Install CLI deps
        if: steps.guard.outputs.ready == 'true'
        working-directory: plugin/cli
        run: npm install --no-audit --no-fund
      - name: Run stryker
        if: steps.guard.outputs.ready == 'true'
        working-directory: plugin/cli
        run: npx stryker run || echo "::warning::stryker below threshold (advisory in v0.3.0)"

  bash-mutator:
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Run bash mutator
        run: bash tests/mutation/bash-mutator.sh || echo "::warning::bash-mutator below target (advisory in v0.3.0)"
```

**D. `.github/workflows/release.yml`** — tag → tarball + .deb + sha256 → GitHub Release.
```yaml
name: release
on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag (e.g. v0.3.0)
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Resolve tag
        id: tag
        run: |
          if [[ -n "${{ inputs.tag }}" ]]; then
            echo "value=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=${GITHUB_REF##*/}" >> "$GITHUB_OUTPUT"
          fi
      - name: Build release tarball
        id: build
        run: |
          if [[ -x scripts/build-release.sh ]]; then
            bash scripts/build-release.sh "${{ steps.tag.outputs.value }}"
          else
            echo "scripts/build-release.sh not present yet — Phase 6 lands it. Emitting an empty placeholder so the workflow passes on an empty-plugin tag."
            mkdir -p dist
            echo "placeholder" > dist/agentlinux-${{ steps.tag.outputs.value }}.tar.gz
            sha256sum dist/agentlinux-${{ steps.tag.outputs.value }}.tar.gz > dist/agentlinux-${{ steps.tag.outputs.value }}.tar.gz.sha256
          fi
      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/agentlinux-*.tar.gz
            dist/agentlinux-*.tar.gz.sha256
```

All four files must pass `python3 -c "import yaml; yaml.safe_load(open(...))"`.
  </action>
  <verify>
    <automated>test -f .github/workflows/test.yml && test -f .github/workflows/nightly-qemu.yml && test -f .github/workflows/nightly-mutation.yml && test -f .github/workflows/release.yml && python3 -c "import yaml,sys; [yaml.safe_load(open(f)) for f in ['.github/workflows/test.yml','.github/workflows/nightly-qemu.yml','.github/workflows/nightly-mutation.yml','.github/workflows/release.yml']]; print('all four parse')" && grep -q "^name: test$" .github/workflows/test.yml && grep -q "^name: nightly-qemu$" .github/workflows/nightly-qemu.yml && grep -q "^name: nightly-mutation$" .github/workflows/nightly-mutation.yml && grep -q "^name: release$" .github/workflows/release.yml && grep -q "continue-on-error: true" .github/workflows/nightly-mutation.yml</automated>
  </verify>
  <acceptance_criteria>
    - All four workflow files exist at `.github/workflows/{test,nightly-qemu,nightly-mutation,release}.yml`
    - Each file parses as valid YAML (Python `yaml.safe_load` succeeds)
    - `grep -q "^name: test$" .github/workflows/test.yml` (exact `name:` field)
    - `grep -q "^name: nightly-qemu$" .github/workflows/nightly-qemu.yml`
    - `grep -q "^name: nightly-mutation$" .github/workflows/nightly-mutation.yml`
    - `grep -q "^name: release$" .github/workflows/release.yml`
    - `grep -q "continue-on-error: true" .github/workflows/nightly-mutation.yml` (mutation is advisory)
    - `grep -q "path.*ignore\|paths-ignore" .github/workflows/test.yml` (website changes do not trigger plugin CI)
    - Existing `.github/workflows/deploy.yml` (website) is still present and unmodified (`diff` against pre-task state returns empty; verify by checking file still exists with same mtime/size as before this task ran)
  </acceptance_criteria>
  <done>Four workflows exist, all parse as valid YAML, all authored to pass on an empty-plugin commit, mutation workflow is advisory (continue-on-error), legacy deploy.yml untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold mutation harness (stryker config + bash-mutator.sh)</name>
  <files>plugin/cli/stryker.config.json, tests/mutation/bash-mutator.sh, tests/mutation/README.md</files>
  <read_first>
    docs/HARNESS.md (§1.3 — mutation testing section specifies targets: 75% CLI, 60% bash; "advisory in v0.3.0; promotion to gate is v0.4 decision"),
    docs/HARNESS.md (§7 Phase A bullets for mutation scaffolding),
    plugin/cli/package.json (to confirm the CLI's expected structure under src/)
  </read_first>
  <action>
Three files.

**A. `plugin/cli/stryker.config.json`** — Stryker config targeting `plugin/cli/src/`. Threshold 75 advisory (does not fail CI).
```json
{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Advisory in v0.3.0. Promotion to gate is a v0.4 decision (see ADR-007 follow-up).",
  "packageManager": "npm",
  "reporters": ["html", "progress", "clear-text"],
  "testRunner": "command",
  "commandRunner": {
    "command": "npm test"
  },
  "mutate": ["src/**/*.ts", "!src/**/*.test.ts"],
  "coverageAnalysis": "perTest",
  "thresholds": { "high": 85, "low": 60, "break": 0 },
  "timeoutMS": 30000,
  "concurrency": 2
}
```

Note `thresholds.break: 0` — stryker never fails the job; warnings are emitted via the workflow's `continue-on-error: true` layer.

**B. `tests/mutation/bash-mutator.sh`** — minimal in-house bash mutator. Executable. Target 60% advisory. Must run (even if it does nothing useful yet — Phase 2+ will flesh it out). Exact body:
```bash
#!/usr/bin/env bash
# tests/mutation/bash-mutator.sh
# In-house mutation tester for plugin/bin, plugin/lib, plugin/provisioner.
# Advisory in v0.3.0 — promotion to release gate is a v0.4 decision (see ADR-007 follow-up).
#
# Mutations implemented:
#   1. Negation flip: `!` inserted/removed before tested conditions
#   2. Comparison-operator swap: `==` ↔ `!=`, `-eq` ↔ `-ne`
#   3. `set -e` removal
#   4. sudoers mode bit flip (0440 ↔ 0644)
#   5. `as_user <u> <cmd>` → direct `<cmd>`
#
# For each mutant, run the bats suite under Docker (if present). Report
# kill-rate. A kill-rate below 60 % emits a warning but does NOT exit non-zero.

set -euo pipefail

TARGETS=()
for d in plugin/bin plugin/lib plugin/provisioner; do
  if [[ -d "$d" ]]; then
    while IFS= read -r f; do TARGETS+=("$f"); done < <(find "$d" -type f \( -name "*.sh" -o -perm -u+x \) 2>/dev/null)
  fi
done

if [[ ${#TARGETS[@]} -eq 0 ]]; then
  echo "bash-mutator: no bash targets yet (Phase 1 skeleton) — skipping"
  exit 0
fi

if ! command -v bats >/dev/null 2>&1 && [[ ! -x tests/docker/run.sh ]]; then
  echo "bash-mutator: bats + tests/docker/run.sh not yet present — cannot score mutants (skipping, advisory)"
  exit 0
fi

echo "bash-mutator: ${#TARGETS[@]} target(s); full mutant scoring lands alongside Phase 2 bats suite."
exit 0
```

Make it executable: `chmod +x tests/mutation/bash-mutator.sh`.

**C. `tests/mutation/README.md`** — 20–40 line explainer:
- What mutation testing is and why (half-sentence each).
- That v0.3.0 uses stryker for Node and this script for bash.
- That mutation is ADVISORY in v0.3.0 — regressions open a follow-up issue; promotion to release gate is a v0.4 decision (cite ADR-007 and the ADR follow-up in v0.4).
- Targets: 75% Node, 60% bash (per HARNESS.md §1.3).
- How to run locally: `cd plugin/cli && npx stryker run` and `bash tests/mutation/bash-mutator.sh`.
  </action>
  <verify>
    <automated>test -f plugin/cli/stryker.config.json && node -e "JSON.parse(require('fs').readFileSync('plugin/cli/stryker.config.json','utf8'))" && grep -q '"break": 0' plugin/cli/stryker.config.json && test -x tests/mutation/bash-mutator.sh && bash tests/mutation/bash-mutator.sh && test -f tests/mutation/README.md && grep -qi "advisory" tests/mutation/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f plugin/cli/stryker.config.json` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('plugin/cli/stryker.config.json','utf8'))"` exits 0 (valid JSON)
    - `grep -q '"break": 0' plugin/cli/stryker.config.json` (stryker never fails the job on low score)
    - `grep -q '"mutate"' plugin/cli/stryker.config.json` and the mutate entry targets `src/**/*.ts`
    - `test -x tests/mutation/bash-mutator.sh` (executable)
    - `bash tests/mutation/bash-mutator.sh` exits 0 on an empty plugin (prints "skipping")
    - `test -f tests/mutation/README.md` and `grep -qi "advisory" tests/mutation/README.md`
    - `grep -qEi "75|Node|stryker" tests/mutation/README.md` (documents Node target)
    - `grep -qEi "60|bash" tests/mutation/README.md` (documents bash target)
  </acceptance_criteria>
  <done>Stryker config and bash-mutator stub exist, both runnable on empty plugin, both explicitly advisory (stryker `break: 0`; mutator always exits 0 on skeleton); README explains v0.3.0 status.</done>
</task>

</tasks>

<verification>
- `pre-commit run --all-files` (after `pre-commit install`) must pass once Plan 01's skeleton is in place. Actual install + run lands in Plan 05 harness tests; this plan only has to produce the config.
- `python3 -c "import yaml; [yaml.safe_load(open(f)) for f in ['.pre-commit-config.yaml','.github/workflows/test.yml','.github/workflows/nightly-qemu.yml','.github/workflows/nightly-mutation.yml','.github/workflows/release.yml']]"` exits 0.
- Mutation workflow has `continue-on-error: true` and stryker config has `break: 0`.
</verification>

<success_criteria>
- HRN-02 satisfied — pre-commit wires shellcheck, shfmt, biome, catalog JSON Schema validation.
- HRN-08 satisfied — four workflows exist in `.github/workflows/` and each parses as valid YAML and is authored to pass on an empty-plugin commit.
- TST-06 satisfied (scaffolded) — stryker config + bash-mutator script both runnable; both advisory (non-blocking).
- Legacy `.github/workflows/deploy.yml` untouched; website deploy workflow still works.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions runner → GitHub Release assets | release.yml pushes tarballs + sha256 to public GitHub Releases |
| External contributor PR → repo CI | PR code runs under `pull_request` trigger; must not leak secrets |
| Schedule trigger → nightly runs | nightly-qemu/mutation run with repo contents; no inbound untrusted payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Release tarball integrity | mitigate | `release.yml` emits a sibling `.sha256` alongside every tarball; ADR-006 locks this pattern; `packaging/curl-installer/install.sh` verifies the hash before exec (Phase 6 work). |
| T-02-02 | Elevation of Privilege | `pull_request` workflow | mitigate | `nightly-mutation.yml` declares `permissions: contents: read` (least-privilege); `test.yml` runs pre-commit only, no write operations. |
| T-02-03 | Elevation of Privilege | `release.yml` GitHub-token scope | mitigate | `permissions: contents: write` is the minimum required to publish a Release; no `actions: write`, `packages: write`, or `id-token: write` declared. |
| T-02-04 | Information Disclosure | stryker / bash-mutator logs | accept | Mutation-test output contains only source code and test names already public in the repo; no secrets consumed. |
| T-02-05 | Denial of Service | nightly cron jobs | mitigate | Both nightly workflows use `continue-on-error: true` so a single broken run cannot block the main branch; path filters on `test.yml` prevent noisy website-only PRs from burning CI minutes. |
| T-02-06 | Tampering | `catalog-schema-validate` local hook | mitigate | Validator runs with `language: system` against a fixed script committed to the repo; it reads files from-disk without eval, and the Phase 1 stub exits 0 cleanly when no catalog exists (no bypass surface). |
</threat_model>

<output>
After completion, create `.planning/phases/01-harness-setup/01-02-SUMMARY.md` describing the five files created, the "pass on empty plugin" guards added to each workflow, and any version pins that may drift (pre-commit hook revs, stryker major version, actions/checkout major version).
</output>
