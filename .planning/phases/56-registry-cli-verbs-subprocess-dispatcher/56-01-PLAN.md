---
phase: 56-registry-cli-verbs-subprocess-dispatcher
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - rust/crates/agentlinux/Cargo.toml
  - rust/crates/agentlinux/src/cli.rs
  - rust/crates/agentlinux/src/recipe_env.rs
  - rust/crates/agentlinux/src/dispatcher.rs
  - rust/crates/agentlinux/src/main.rs
  - tests/docker/run.sh
autonomous: true
requirements: [VERB-01, VERB-02, VERB-03, GATE-01, GATE-05]
must_haves:
  truths:
    - "`agentlinux <verb> [flags]` parses via clap with the exact Commander surface — 6 subcommands, 23 flags, 4 positionals, program-level `--version`, and `install --version <semver>` shadowing the program `-V` (CLI-03) — never colliding."
    - "The dispatcher runs argv as the target user (invoker==target → direct; else `sudo -u <user> -H -E -- argv`), tees stdout+stderr live while capturing, never throws on non-zero exit, honors a timeout that escalates SIGTERM→(2000ms)→SIGKILL, and maps exits timeout=124 / signal-kill=1 / ENOENT=1 / clean=code — all 6 dispatcher-stream.test.ts cases green."
    - "The 6 `AGENTLINUX_*` recipe env names live in exactly one Rust source (`RecipeEnv`); a rename is a compile error; `Command::envs` sets them so the ~25 unchanged Bash recipes read `${AGENTLINUX_*}` from the inherited process env."
    - "`tests/docker/run.sh <distro> [bats-file]` re-points the `agentlinux` symlink at the staged Rust musl bin (flag-gated) and runs a single bats file, failing loudly (not silently falling back to TS) when the Rust bin is expected but absent."
  artifacts:
    - rust/crates/agentlinux/src/cli.rs
    - rust/crates/agentlinux/src/recipe_env.rs
    - rust/crates/agentlinux/src/dispatcher.rs
  key_links:
    - "The dispatcher (Task 3) consumes `RecipeEnv::into_env_pairs()` (Task 2) to build the child env — VERB-02 dispatch + VERB-03 typed source meet here."
    - "`main.rs` routes clap-parsed subcommands (Task 1) to the Wave-1/2 verb adapters (Plans 02/03); the existing `reuse-decision` subcommand is preserved."
    - "run.sh symlink-override (Task 4) is the GATE-01 mechanic every later plan's bats verification depends on — without it the CLI bats hit the TS bundle, not the Rust bin."
---

<objective>
Wave 0 — scaffold the CLI shell and front-load the #1 risk (the subprocess
dispatcher) BEFORE any verb logic. Deliver four things, each with its own
automated gate:

1. **The clap CLI skeleton** (`cli.rs`) — the 6 subcommands + 23 flags + 4
   positionals matching the Commander surface (`plugin/cli/src/index.ts:20-108`),
   including the `install --version <semver>` shadow of the program-level `-V`
   (CLI-03), with a `cli_parse` unit test corpus asserting the parse tree.
2. **The typed `RecipeEnv`** (`recipe_env.rs`) — the ONE source of the 6
   `AGENTLINUX_*` names + the install-user + canonical-PATH assembly
   (`runner.ts:30-120`), so a rename is a compile error (VERB-03).
3. **The dispatcher** (`dispatcher.rs`) — buffered + streaming `as_user` with
   the invoker==target short-circuit, thread-per-pipe tee, SIGTERM→SIGKILL
   escalation, and the exact exit-code map, GREEN on all 6
   `dispatcher-stream.test.ts` parity cases (VERB-02). **This is the risk
   center — it is gated first.**
4. **The bats staging override** (`tests/docker/run.sh`) — re-point the
   `agentlinux` symlink at the staged Rust bin, flag-gated, per-file
   (Docker OOM), failing loudly on a missing Rust bin (GATE-01/GATE-05).

Purpose: unblock Wave 1/2 verb adapters (they call the dispatcher + read the
CLI structs) and establish the byte-compatible-bats-on-Rust harness the whole
phase verifies against.
Output: `cli.rs`, `recipe_env.rs`, `dispatcher.rs`, a `main.rs` that routes
clap subcommands, the two new bin deps (`clap`, `nix`), and a parameterized
`run.sh`. The pure `agentlinux-core` crate is untouched (all I/O in the bin).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-CONTEXT.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-VALIDATION.md
</context>

<user_setup>
None. All new dependencies (`clap`, `nix`) are public crates cleared by the
Package Legitimacy Gate (verdict OK — see below); no credentials, no dashboard
config, no human action required.
</user_setup>

<package_legitimacy>
Legitimacy gate run at plan time (`gsd-tools query package-legitimacy check
--ecosystem crates clap nix libc wait-timeout`): ALL verdict `OK` (source-backed,
high-download, non-deprecated, no postinstall). Resolved current versions via
`cargo search`: `clap = 4.6.4`, `nix = 0.31.3`, `wait-timeout = 0.2.1`. No
`[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human checkpoint required.
Pin `clap` (feature `derive`) and `nix` (feature `signal`) in Task 3; TTY
detection uses stable `std::io::IsTerminal` (no crate).
</package_legitimacy>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `rust/crates/agentlinux/src/cli.rs` — clap derive structs: the `Cli` root, a `Command` enum with the 6 verb variants, per-verb flag/positional fields; program `--version`; the `install --version` shadow config.
- `rust/crates/agentlinux/src/recipe_env.rs` — `RecipeEnv` (the 6 `AGENTLINUX_*` names, ONE source) + `into_env_pairs()` + `resolve_install_user()` + the canonical PATH/HOME/NPM_CONFIG_PREFIX/LANG assembly.
- `rust/crates/agentlinux/src/dispatcher.rs` — `DispatchResult` + `as_user()` (buffered + streaming) + `dispatch_recipe()`; the 6-case parity test module.
- `rust/crates/agentlinux/src/main.rs` — clap parse → subcommand dispatch (stubs for verbs land in Plans 02/03); `reuse-decision` preserved.
- `rust/crates/agentlinux/Cargo.toml` — `clap`/`nix` deps.
- `tests/docker/run.sh` — optional per-file bats arg + Rust-bin-as-`agentlinux` symlink override, flag-gated.
Downstream: Plan 02 (`list`/`adopt`/`pin`) reads the `cli.rs` structs; Plan 02/03 verb adapters call `dispatcher::dispatch_recipe`; every plan's bats verification uses the `run.sh` override.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: clap CLI skeleton — 6 subcommands + 23 flags + 4 positionals matching Commander</name>
  <files>rust/crates/agentlinux/src/cli.rs, rust/crates/agentlinux/src/main.rs, rust/crates/agentlinux/Cargo.toml</files>
  <read_first>
    - plugin/cli/src/index.ts:20-108 (READ IN FULL — the Commander bootstrap: `.name("agentlinux")`, `.version(VERSION, "-V, --version")`, `enablePositionalOptions()`, the preAction guard hook, and every `.command(...).option(...)` for the 6 verbs; this is the exact surface to mirror)
    - rust/crates/agentlinux/src/main.rs (READ IN FULL — the existing plain-argv `match` on `reuse-decision`; you REPLACE the dispatch with a clap parse but PRESERVE the `reuse-decision` path as a hidden/retained subcommand or a pre-clap shortcut)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md §"Arg Parsing — clap vs. plain-argv" (the counted surface: 23 flags / 4 positionals / 6 subcommands; the `install --version` shadow via `#[command(disable_version_flag)]`; Open Q1 RESOLVED at plan time — no bats asserts an exact `--help` body, only `--version` prints the version number, so clap default help is safe)
    - tests/bats/40-registry-cli.bats:97-100 (CLI-01: `agentlinux --version` prints the version — clap `.version()` covers it) and :350-359 (CLI-03: `agentlinux install --include-test --version 9.9.9 test-dummy` — the `--version` shadow MUST parse `9.9.9` as the install arg, not print the program version)
  </read_first>
  <behavior>
    - `list --include-test --by-category --descriptions --json` parses → List{include_test:true, by_category:true, descriptions:true, json:true}
    - `install test-dummy --force --version 9.9.9 --include-test --yes --dry-run` parses → Install{name:"test-dummy", force:true, version:Some("9.9.9"), include_test:true, yes:true, dry_run:true} (the `--version 9.9.9` is the SUBCOMMAND arg, NOT the program version flag — CLI-03)
    - `adopt` with no name parses → Adopt{name:None, all:false, ...}; `adopt foo --all` → Adopt{name:Some("foo"), all:true}
    - `remove test-dummy --force` → Remove{name:"test-dummy", force:true}
    - `upgrade --reset-all-curated --respect-overrides --all-latest --check-upstream --json` → Upgrade{ all five flags true }
    - `pin test-dummy=latest` → Pin{spec:"test-dummy=latest"}
    - `--version` (program level, no subcommand) → prints the crate version and exits 0 (clap `.version()`); an unknown verb / bad flag → clap error to stderr, exit 64 (mapped in main.rs, mirroring Commander's EX_USAGE)
  </behavior>
  <action>
    Add `clap = { version = "4.6", features = ["derive"] }` to
    `rust/crates/agentlinux/Cargo.toml` `[dependencies]` (version resolved at plan
    time via `cargo search`; use the `4.6` caret so patch updates flow).

    Create `cli.rs` with the clap derive tree. A root `#[derive(Parser)] struct Cli`
    carrying `#[command(subcommand)] command: Command` and a program-level version
    (`#[command(name = "agentlinux", version)]` — the version string comes from the
    crate `CARGO_PKG_VERSION`, which the bats CLI-01 test asserts against
    `package.json`; confirm the crate version matches). A `#[derive(Subcommand)] enum
    Command` with the six variants `List`, `Install`, `Adopt`, `Remove`, `Upgrade`,
    `Pin`, each an inline struct carrying EXACTLY the flags/positionals from
    index.ts:20-108: `List{include_test, by_category, descriptions, json}` (all
    `#[arg(long)]` bools); `Install{name: String (positional), force, version:
    Option<String>, include_test, yes, dry_run}`; `Adopt{name: Option<String>
    (optional positional), all, include_test, json}`; `Remove{name: String
    (positional), force}`; `Upgrade{reset_all_curated, respect_overrides, all_latest,
    check_upstream, json}`; `Pin{spec: String (positional)}`. Match each long-flag
    NAME to the Commander string exactly (`--reset-all-curated`, `--by-category`,
    etc. — clap kebab-cases the field by default, but assert with a `cli_parse` test).

    Reproduce the `install --version` shadow (Commander's `enablePositionalOptions()`,
    index.ts:31): put `#[command(disable_version_flag = true)]` on the `Install`
    subcommand struct so its `version: Option<String>` field owns `--version` locally,
    while the ROOT `Cli` keeps the global `--version` flag. Verify
    `agentlinux install foo --version 2.1.7` yields `version = Some("2.1.7")` and
    `agentlinux --version` prints the crate version — this is the CLI-03 keystone.

    Rewire `main.rs`: replace the plain-argv `match` with `Cli::parse()` (or
    `try_parse` → map clap errors to `ExitCode::from(64)` for EX_USAGE parity, and let
    `--version`/`--help` exit 0 as clap does). Route each `Command` variant to a stub
    that (for now) prints a `todo!`-style marker or delegates to the not-yet-written
    verb fns — the Wave-1/2 plans fill these in. **PRESERVE the existing
    `reuse-decision` subcommand** the provisioner shim calls (`plugin/lib/reuse/
    agents.sh` / `13-reuse.bats`): keep it as a retained `Command::ReuseDecision{id:
    Option<String>}` variant (hidden with `#[command(hide = true)]`) wired to the
    existing `cmd_reuse_decision`, OR short-circuit it before `Cli::parse()`. Do NOT
    break the Phase-53 reuse path — 13-reuse.bats must stay green.

    Add a `#[cfg(test)] mod cli_parse` module asserting every `<behavior>` row via
    `Cli::try_parse_from(["agentlinux", ...])` and pattern-matching the parsed
    `Command`. Include a NEGATIVE row: an unknown verb `Cli::try_parse_from([...,
    "frobnicate"])` returns `Err`.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux cli_parse 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux cli_parse` passes, covering all `<behavior>` rows incl. the `install --version 9.9.9` shadow and the unknown-verb Err.
    - `grep -n 'enum Command' rust/crates/agentlinux/src/cli.rs` and `grep -cE 'List|Install|Adopt|Remove|Upgrade|Pin' rust/crates/agentlinux/src/cli.rs` confirm all six variants present.
    - `grep -n 'disable_version_flag' rust/crates/agentlinux/src/cli.rs` returns a match (the `--version` shadow config).
    - `cd rust && cargo test -p agentlinux --no-run` succeeds (bin still compiles; `reuse-decision` path retained).
    - `agentlinux --version` prints the crate version (matches package.json per CLI-01); the flag surface matches index.ts:20-108 (6 verbs, 23 flags, 4 positionals).
  </acceptance_criteria>
  <done>
    `cli.rs` exposes the clap `Cli`/`Command` tree matching the Commander surface byte-for-byte on flag names + positionals; the `install --version` shadow parses; `main.rs` routes via clap while preserving `reuse-decision`; the `cli_parse` corpus is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecipeEnv — the single typed source of the 6 AGENTLINUX_* names + install-user + PATH assembly (VERB-03)</name>
  <files>rust/crates/agentlinux/src/recipe_env.rs, rust/crates/agentlinux/src/main.rs</files>
  <read_first>
    - plugin/cli/src/runner.ts:30-120 (READ IN FULL — `resolveInstallUser` [$AGENTLINUX_USER > /etc/agentlinux.env `AGENTLINUX_USER=` line > "agent", POSIX charset `^[a-z][a-z0-9_-]*$`], the `RecipeEnv` interface [the 6 names], the `AGENT_PATH` canonical literal, and `dispatchRecipe`'s env-map assembly — PATH/HOME/NPM_CONFIG_PREFIX/LANG/LC_ALL + extraEnv, and the `PRESERVE_PATHS = (preserve_paths ?? []).join(":")` rule)
    - plugin/cli/test/runner.test.ts (READ IN FULL — the parity oracle: the 6-var assembly [:66-70], the canonical PATH literal [:80-88], extraEnv append+override [:91-107], the configured-install-user derivation [`AGENTLINUX_USER=claude` → `/home/claude` env; malformed → fall back to `agent`, :158-196])
    - plugin/catalog/agents/gsd/install.sh:7-8 (recipes read `${AGENTLINUX_*}` plain from env — proves NO manifest file is needed; `Command::envs` on the child satisfies them)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md §"VERB-03 — the Env-Var Contract" (the 6 names + value sources table; the "single typed Rust source" recommendation; Pitfall 3 [sudo -E drops PATH → set PATH explicitly])
  </read_first>
  <behavior>
    - RecipeEnv{ pinned_version:"2.1.7", catalog_dir:"/opt/.../catalog", agent_home:"/home/agent", source_kind:"npm", install_log:"/var/log/agentlinux-install.log", preserve_paths:"" }.into_env_pairs() yields exactly 6 pairs keyed `AGENTLINUX_PINNED_VERSION`/`_CATALOG_DIR`/`_AGENT_HOME`/`_SOURCE_KIND`/`_INSTALL_LOG`/`_PRESERVE_PATHS` with those values
    - preserve_paths join: entry preserve_paths ["a",".config/x"] → "a:.config/x"; [] → "" (empty string, key still present)
    - resolve_install_user: env AGENTLINUX_USER="claude" (valid charset) → "claude"; unset + /etc/agentlinux.env line `AGENTLINUX_USER=claude` → "claude"; malformed "Bad User!" → "agent" (fallback); absent everywhere → "agent"
    - the canonical PATH for user "agent" == "/home/agent/.npm-global/bin:/home/agent/.local/bin:/usr/local/bin:/usr/bin:/bin" (byte-identical to runner.ts AGENT_PATH); HOME="/home/agent"; NPM_CONFIG_PREFIX="/home/agent/.npm-global"; LANG=LC_ALL="C.UTF-8"
  </behavior>
  <action>
    Create `recipe_env.rs` as the ONE place the 6 `AGENTLINUX_*` key strings live.
    Define `pub struct RecipeEnv { pub pinned_version: String, pub catalog_dir:
    String, pub agent_home: String, pub source_kind: String, pub install_log: String,
    pub preserve_paths: String }` and `impl RecipeEnv { pub fn into_env_pairs(self) ->
    [(&'static str, String); 6] }` returning the 6 `("AGENTLINUX_…", value)` pairs —
    the key literals appear in exactly this one function (a rename is a compile error,
    satisfying VERB-03 "a rename cannot silently desync"). Do NOT add a manifest file
    for recipes to `source`; recipes read the inherited env (proven by
    gsd/install.sh:7).

    Add `pub fn resolve_install_user() -> String` porting runner.ts:30-43: read
    `$AGENTLINUX_USER`; if empty/unset, read `/etc/agentlinux.env` and match the
    `^AGENTLINUX_USER=(.*)$` line (multiline); validate the result against the POSIX
    charset `^[a-z][a-z0-9_-]*$`; on any failure/mismatch fall back to `"agent"`. This
    is I/O (std::fs read of the env file) — it lives HERE in the bin, NOT in the pure
    core. Honor the `AGENTLINUX_USER` env override (bats seam, 23-install-user.bats).

    Add a helper (e.g. `pub fn full_child_env(recipe: RecipeEnv, user: &str, extra:
    &[(String,String)]) -> Vec<(String,String)>`) that assembles the FULL child
    environment the dispatcher sets: the 6 pairs from `into_env_pairs()`, PLUS `PATH`
    (built from the user's home: `{home}/.npm-global/bin:{home}/.local/bin:/usr/local/
    bin:/usr/bin:/bin` — byte-identical to runner.ts:114 for `agent`), `HOME`,
    `NPM_CONFIG_PREFIX = {home}/.npm-global`, `LANG=C.UTF-8`, `LC_ALL=C.UTF-8`, then
    `extra` (extraEnv) appended so later keys override earlier (mirror the TS spread
    order runner.ts:110-119). Set PATH EXPLICITLY (Pitfall 3: `sudo -E` alone drops
    PATH to secure_path).

    Add a `#[cfg(test)] mod recipe_env` module porting the runner.test.ts parity rows
    in `<behavior>`: the 6-pair assembly, the preserve_paths join (incl. empty), the
    install-user resolution (valid env / env-file line / malformed→agent / absent→
    agent), and the canonical PATH literal for `agent`. Use a temp env-file + env-var
    guards for the resolve_install_user rows. Keep this in the bin (the crate under
    test is `agentlinux`, not the pure core).

    Wire `main.rs` to `mod recipe_env;` (module registration only; the dispatcher in
    Task 3 consumes it).
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux recipe_env 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux recipe_env` passes, covering the 6-pair assembly, preserve_paths join (incl. empty string), install-user resolution (all 4 rows), and the canonical PATH literal.
    - `grep -c 'AGENTLINUX_' rust/crates/agentlinux/src/recipe_env.rs` shows the 6 key strings appear (filter comments: `grep -v '^\s*//'`); they exist in exactly one function (`into_env_pairs`).
    - `grep -n 'fn resolve_install_user' rust/crates/agentlinux/src/recipe_env.rs` returns a match; charset validation present (`^[a-z][a-z0-9_-]*$` regex or equivalent).
    - The PATH literal for `agent` is byte-identical to runner.ts AGENT_PATH (runner.test.ts:80-88); PRESERVE_PATHS empty string when no preserve_paths (runner.ts behavior).
  </acceptance_criteria>
  <done>
    `recipe_env.rs` centralizes the 6 `AGENTLINUX_*` names in one typed source (rename = compile error), resolves + validates the install user, and assembles the full child env with an explicit canonical PATH; the runner.test.ts parity rows are green (VERB-03).
  </done>
</task>

<task type="auto">
  <name>Task 3: The subprocess dispatcher — buffered + streaming as_user, SIGTERM→SIGKILL, exit-map; 6 dispatcher-stream.test.ts cases green (VERB-02)</name>
  <files>rust/crates/agentlinux/src/dispatcher.rs, rust/crates/agentlinux/src/main.rs, rust/crates/agentlinux/Cargo.toml</files>
  <read_first>
    - plugin/cli/src/state/dispatcher.ts (READ IN FULL — the `asUser` contract: invoker==target short-circuit [:65-72], buffered path [return the shape never throw, :81-107], `spawnTee` [pipe both, tee live + accumulate, :116-170], timeout→SIGTERM→(2000ms)→SIGKILL [:135-141], exit map `timedOut?124:(code ?? (signal?1:0))` [:167], ENOENT→exitCode 1 [:159-166])
    - plugin/cli/test/dispatcher-stream.test.ts (READ IN FULL — the 6 parity cases: :44 tee+capture, :64 non-zero-no-throw [exit 7], :77 buffered captures [streamed unset], :84 sudo-branch on unknown user, :104 ENOENT→1, :117 timeout→SIGTERM→124 [`bash -c "sleep 5"` timeout:300 → exit 124])
    - plugin/cli/src/runner.ts:96-120 (`dispatchRecipe` — how the verb layer calls the dispatcher: user + `["bash", recipePath]` argv + the env map + stream flag; this is the `dispatch_recipe` wrapper you build)
    - plugin/cli/src/upgrade/npm_ls.ts:52-119 (Open Q2: the BUFFERED path is called with `timeout: 30_000` for npm — the Rust buffered path MUST honor a timeout too, not only the streaming path)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md §"VERB-02 — the Dispatcher" + §"Common Pitfalls" (Pitfall 1 SIGKILL-only; Pitfall 2 two-pipe deadlock → thread-per-pipe; Pitfall 5 buffered-never-throw) + §"Code Examples" (buffered/short-circuit/timeout skeletons)
  </read_first>
  <action>
    Add `nix = { version = "0.31", features = ["signal"] }` to
    `rust/crates/agentlinux/Cargo.toml` (version resolved at plan time; the `signal`
    feature gives `nix::sys::signal::kill`). Optionally add `wait-timeout = "0.2"` for
    the buffered-path timeout (Open Q2), or hand-roll a `try_wait` poll loop — pick one
    and note the choice.

    Create `dispatcher.rs`. Define `pub struct DispatchResult { pub exit_code: i32,
    pub stdout: String, pub stderr: String, pub streamed: bool }`. Implement `pub fn
    as_user(user: &str, argv: &[String], env: &[(String,String)], stream: bool,
    timeout_ms: Option<u64>) -> DispatchResult`:

    - **Invoker==target short-circuit** (dispatcher.ts:65-72): resolve the invoker
      name (`nix::unistd::User::from_uid(getuid())` or `$USER`). If `invoker == user`,
      run `argv` directly; else prepend `["sudo","-u",user,"-H","-E","--"]`. The `--`
      terminator is load-bearing (no arg reparse).
    - **Buffered path** (stream=false): `Command::new(cmd).args(rest).env_clear()
      .envs(env).stdin(Stdio::null())`. If `timeout_ms` is Some, bound it (wait-timeout
      or try_wait loop; on expiry SIGTERM→2000ms→SIGKILL, exit_code=124). On success →
      `DispatchResult{exit_code: status.code().unwrap_or(1), stdout, stderr, streamed:
      false}`. On spawn error (ENOENT) → `{exit_code:1, stdout:"", stderr: err msg,
      streamed:false}`. **Never return `Err`/panic on a non-zero child exit** (Pitfall
      5): a non-zero exit is a valid `DispatchResult`, because callers (npm ls) decide
      fatality.
    - **Streaming path** (stream=true): spawn with `stdin(Stdio::null()).stdout(piped)
      .stderr(piped)`. Spawn TWO reader threads (Pitfall 2 — one per pipe) that read
      chunks, write through to the parent's stdout/stderr (live tee), and accumulate
      into shared `String`s (channel or `Arc<Mutex<String>>`). Run a timeout watchdog:
      on expiry set `timed_out=true`, `nix::sys::signal::kill(Pid::from_raw(child.id()
      as i32), SIGTERM)`, wait up to 2000ms, then `SIGKILL` if still alive. Join reader
      threads. Map exit: `timed_out → 124`; else `status.code()` → that; killed-by-
      signal-no-code → 1. Spawn error (ENOENT) → `{exit_code:1, stderr: accumulated +
      err msg, streamed:true}`. Always `streamed: true` on this path.

    Add `pub fn dispatch_recipe(user: &str, recipe_path: &str, env: &[(String,String)],
    stream: bool) -> DispatchResult` mirroring runner.ts:96-120: argv `["bash",
    recipe_path]`, delegating to `as_user`. (The verb layer in Plans 02/03 builds the
    env via `recipe_env::full_child_env` and calls this.)

    Add a `#[cfg(test)] mod dispatcher` module reproducing ALL 6
    dispatcher-stream.test.ts cases as Rust `#[test]`s (name them so
    `cargo test dispatcher` selects them): (1) streaming tees+captures `out-line`/
    `err-line`, exit 0, streamed true; (2) `bash -c "echo hi; exit 7"` → exit_code 7,
    streamed true, stdout has `hi`, NO panic; (3) buffered captures, streamed false;
    (4) sudo-branch on invoker!=target (use a bogus user → non-zero exit, streamed
    true, returned shape not a panic); (5) `/no/such/binary` → exit_code 1, streamed
    true; (6) `bash -c "sleep 5"` with timeout 300ms → exit_code 124, streamed true.
    Add a 7th escalation test (VALIDATION.md Manual-Only row): a `trap '' TERM; sleep
    5` child with a short timeout → still terminates (SIGKILL escalation) within the
    grace window and maps to 124. Keep runtimes short (sub-second timeouts) so the
    suite stays fast.

    Wire `main.rs` to `mod dispatcher;`. Keep `agentlinux-core` PURE — no `std::process`
    added to the pure crate.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux dispatcher 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux dispatcher` passes ALL 6 dispatcher-stream.test.ts parity cases + the SIGKILL-escalation case (7 tests), each within a sub-second timeout.
    - `grep -n 'fn as_user' rust/crates/agentlinux/src/dispatcher.rs` and `grep -n 'fn dispatch_recipe' ...` each return a match.
    - The buffered path returns a `DispatchResult` on a non-zero child exit (never `Err`/panic): the exit-7 test asserts `exit_code == 7` (Pitfall 5).
    - Timeout maps to exit_code 124 (GNU convention); ENOENT maps to exit_code 1; both via the streaming path with `streamed: true`.
    - `nix` signal escalation is used (SIGTERM then SIGKILL, not `Child::kill` which is SIGKILL-only — Pitfall 1): `grep -n 'SIGTERM' rust/crates/agentlinux/src/dispatcher.rs` returns a match.
    - `grep -rn 'std::process' rust/crates/agentlinux-core/src/` prints nothing (the pure core stays free of process I/O).
  </acceptance_criteria>
  <done>
    `dispatcher.rs` reproduces the `asUser` contract (short-circuit, buffered-never-throw, streaming thread-per-pipe tee, SIGTERM→2000ms→SIGKILL, exit-map 124/1/0/code) with all 6 parity cases + the escalation case green; `dispatch_recipe` wraps it for the verb layer; the pure core is untouched (VERB-02).
  </done>
</task>

<task type="auto">
  <name>Task 4: Bats staging override — run.sh per-file + Rust-bin-as-agentlinux symlink, flag-gated, fail-loud (GATE-01/GATE-05)</name>
  <files>tests/docker/run.sh</files>
  <read_first>
    - tests/docker/run.sh (READ IN FULL — the current single-`TARGET` positional [:42], the Rust-bin host-build + `docker cp` staging at `/home/agent/.local/bin/agentlinux` [~:210-250], the `AGENTLINUX_RUST_BIN` export for the reuse shim [~:250-256], and the `bats tests/bats/` whole-directory invocation [:256])
    - plugin/provisioner/50-registry-cli.sh:118-129 (the provisioner symlink `~agent/.npm-global/bin/agentlinux -> $CLI_STAGE_DIR/dist/index.js` — the TS bundle target you OVERRIDE post-install in the harness; the provisioner itself is NOT changed this phase — that's Phase 57/58)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md §"Acceptance Oracle" (the staging strategy: re-point the `agentlinux` symlink at the staged Rust musl bin AFTER the installer runs, flag-gated for GATE-05; per-file to dodge Docker OOM; exit-127 risk → fail loudly if the Rust bin is expected but absent — do NOT silently fall back to TS and report false-green)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-VALIDATION.md §"CLI bats on Rust build" (the target command shape: `AGENTLINUX_RUST_BIN=... ./tests/docker/run.sh ubuntu-24.04 40-registry-cli`)
  </read_first>
  <action>
    Extend `run.sh` with two capabilities, both behind a flag/env so master's TS path
    is unaffected (GATE-05 parallel-track):

    1. **Optional per-file bats arg.** Accept an OPTIONAL second positional
       `BATS_FILE` (a bare basename like `40-registry-cli` or `40-registry-cli.bats`).
       When present, run `bats tests/bats/<file>` instead of the whole `tests/bats/`
       directory (Docker OOM — MEMORY: full suite OOMs ~test 131 in this VM). When
       absent, preserve today's behavior (whole dir). Keep the existing target
       validation (`ubuntu-22.04|ubuntu-24.04|ubuntu-26.04|almalinux-9`) unchanged;
       update the usage() block to document the new optional arg.

    2. **Rust-bin-as-`agentlinux` symlink override.** Gate on an env flag (e.g.
       `AGENTLINUX_STAGE_RUST_CLI=1`, or reuse/extend the existing
       `AGENTLINUX_RUST_BIN` presence). When enabled AND the Rust musl bin was staged
       successfully into the container, re-point the CLI symlink AFTER the installer
       runs: `docker exec "$CID" ln -sfn <staged-rust-bin> ~agent/.npm-global/bin/
       agentlinux` (the same path the provisioner set at 50-registry-cli.sh:124, but
       now pointing at the Rust bin instead of `dist/index.js`), then
       `chown -h agent:agent`. This is a TEST-HARNESS override only — do NOT edit
       `plugin/provisioner/50-registry-cli.sh` (the provisioner keeps symlinking the
       TS bundle until Phase 57/58).

    3. **Fail loud on missing Rust bin (exit-127 guard).** When the Rust-CLI override
       is REQUESTED but the Rust bin failed to build/stage, ABORT with a clear non-zero
       error (`echo "ERROR: Rust CLI staging requested but musl bin absent — refusing
       to run bats against the TS bundle and report false-green" >&2; exit 1`) rather
       than leaving a dangling symlink (exit 127) or silently falling back to the TS
       bundle. On master (flag unset) the existing non-fatal fallback is preserved.

    Do NOT change which tests run by default on master. This task adds capabilities; it
    must not regress the existing `tests/docker/run.sh ubuntu-24.04` (no second arg, no
    flag) behavior. Follow the installer bash conventions (agentlinux-installer skill:
    `set -euo pipefail` already set; quote expansions; `docker exec` error propagation).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash -n tests/docker/run.sh && grep -q 'ln -sfn' tests/docker/run.sh && grep -qiE 'refusing|false-green' tests/docker/run.sh && echo "STAGING-OVERRIDE+FAIL-LOUD-PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/docker/run.sh` passes (valid syntax) and the script still accepts the existing single-arg form (`tests/docker/run.sh ubuntu-24.04`) without regression.
    - An optional second positional selects a single bats file: `grep` confirms a `BATS_FILE`/per-file branch and a `bats tests/bats/<file>` invocation exists.
    - The Rust-CLI symlink override is present and flag-gated: `grep -q 'ln -sfn' tests/docker/run.sh` matches and it targets `~agent/.npm-global/bin/agentlinux`; the provisioner file `plugin/provisioner/50-registry-cli.sh` is NOT modified (`git diff --name-only` shows only `tests/docker/run.sh`).
    - Fail-loud guard present: when the override is requested but the Rust bin is absent, the script exits non-zero rather than dangling-symlink (exit 127) or false-green (`grep -qiE 'refusing|false-green'`).
  </acceptance_criteria>
  <done>
    `tests/docker/run.sh` accepts an optional per-file bats arg AND (flag-gated) re-points the `agentlinux` symlink at the staged Rust musl bin, failing loudly on a missing Rust bin; master's default TS path is unchanged; the provisioner is untouched (GATE-01/GATE-05 parallel-track).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user argv → clap parse → dispatcher | User-controlled verb/flags/positionals (`<name>`, `<spec>`, `--version`) enter the CLI; must never reach a shell string (argv arrays only) and must be re-validated before crossing into `sudo -u`. |
| invoker → `sudo -u <user> -H -E -- argv` | The dispatcher elevates to another user; the `--` terminator + POSIX-charset-validated user name bound the elevation surface. |
| `/etc/agentlinux.env` `AGENTLINUX_USER=` → `resolve_install_user` | A tampered env file could inject a malformed user into the `sudo -u` argv; charset re-validation → fall back to `agent`. |
| recipe env (6 `AGENTLINUX_*` + PATH) → child bash recipe | The dispatcher sets the child env explicitly; PATH must be the canonical literal (not `sudo -E`'s secure_path) or recipes hit EACCES. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Elevation | `dispatcher::as_user` (`sudo -u` argv) | high | mitigate | argv arrays never a shell string (`Command::new("sudo").args([...])`, mirrors TS `execFile`/`spawn` not `exec`); the `--` terminator ends sudo option parsing so user args can't be reparsed (dispatcher.ts:6,72). Task 3 asserts the argv shape. |
| T-56-02 | Elevation | `recipe_env::resolve_install_user` | high | mitigate | Install-user string re-validated against `^[a-z][a-z0-9_-]*$` before it reaches `sudo -u`; malformed → fall back to `agent` (runner.ts:20,41; runner.test.ts:188). Task 2 covers the malformed→agent row. |
| T-56-03 | Denial of Service | `dispatcher` streaming tee | medium | mitigate | Two-pipe deadlock avoided by thread-per-pipe (Pitfall 2); a runaway recipe is bounded by the timeout → SIGTERM→(2000ms)→SIGKILL escalation (Task 3 escalation test). |
| T-56-04 | Tampering | `recipe_env` child env (PATH) | medium | mitigate | PATH set EXPLICITLY to the canonical literal via `env_clear()` + explicit set (not `sudo -E`, which drops PATH to secure_path — Pitfall 3), preventing a hijacked `npm` from `/usr/bin`. Task 2 asserts the byte-identical PATH. |
| T-56-05 | Spoofing | run.sh Rust-bin staging (false-green) | medium | mitigate | Fail-loud guard: a requested-but-absent Rust bin ABORTS rather than silently running bats against the TS bundle and reporting a false-green Rust pass (Task 4). |
| T-56-SC | Tampering | crates.io installs (`clap`, `nix`) | high | mitigate | Package Legitimacy Gate run at plan time — all verdict `OK` (source-backed, high-download, non-deprecated, no postinstall); versions pinned (`clap 4.6`, `nix 0.31`). No `[ASSUMED]`/`[SUS]`/`[SLOP]` → no blocking-human checkpoint needed. |
</threat_model>

<verification>
- `cd rust && cargo test -p agentlinux cli_parse` green — the 6-verb/23-flag/4-positional surface + the `install --version` shadow + unknown-verb Err (VERB-01 arg layer).
- `cd rust && cargo test -p agentlinux recipe_env` green — the 6-name typed source + install-user resolution + canonical PATH (VERB-03).
- `cd rust && cargo test -p agentlinux dispatcher` green — all 6 dispatcher-stream.test.ts parity cases + SIGKILL escalation (VERB-02, the risk center).
- `cd rust && cargo test --workspace && cargo clippy -p agentlinux --all-targets -- -D warnings` green (no regression; bin lints clean).
- `bash -n tests/docker/run.sh` valid; the symlink-override + per-file + fail-loud present; the provisioner untouched (GATE-01/GATE-05).
- `grep -rn 'std::process' rust/crates/agentlinux-core/src/` prints nothing (pure core stays pure).
</verification>

<success_criteria>
- The clap CLI shell parses the exact Commander surface (6 verbs, 23 flags, 4 positionals, `install --version` shadow); `reuse-decision` preserved.
- `RecipeEnv` centralizes the 6 `AGENTLINUX_*` names in one source (rename = compile error); install-user + canonical PATH assembled (VERB-03).
- The dispatcher reproduces `asUser` byte-for-byte on all 6 parity cases + the escalation case (VERB-02, front-loaded as the #1 risk).
- `tests/docker/run.sh` stages the Rust bin AS the `agentlinux` command (flag-gated, per-file, fail-loud), unblocking every later plan's bats verification.
- The pure core is untouched; all new I/O lives in the bin.
</success_criteria>

<output>
Create `.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-01-SUMMARY.md` when done
</output>
