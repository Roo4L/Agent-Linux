---
phase: 17-changes-delivery-and-release-candidate
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - docs/releases/v0.3.4-rc1-VALIDATION.md
autonomous: false
requirements: [DEL-01b, DEL-04-merge, DEL-02b, DEL-03, DEL-04-validate]
user_setup:
  - service: github
    why: "PR merge is the single human-reserved click (CONTEXT Q2); rc tag push is pre-authorized (CONTEXT Q3); brownfield-VM validation is the genuine v0.3.4 acceptance (CONTEXT Q4)"
must_haves:
  truths:
    - "Branch worktree-aware-install is pushed to origin and a PR to master is open with a v0.3.4 summary + test-evidence checklist"
    - "Maintainer has merged the PR to master (HUMAN checkpoint — Claude does NOT merge)"
    - "Tag v0.3.4-rc1 is pushed on the merged master commit and release.yml has published the rc"
    - "The published v0.3.4-rc1 GitHub Release carries the tarball AND its sibling .sha256"
    - "docs/releases/v0.3.4-rc1-VALIDATION.md exists with the ordered brownfield-VM runbook"
    - "Maintainer has validated the rc on his brownfield VM and returned feedback (HUMAN checkpoint)"
  artifacts:
    - path: "docs/releases/v0.3.4-rc1-VALIDATION.md"
      provides: "Step-by-step maintainer validation runbook (snapshot → curl-install → dry-run → bail → real install → AGT-02 → rollback → feedback)"
      min_lines: 60
  key_links:
    - from: "git tag v0.3.4-rc1 (pushed)"
      to: "release.yml publish job"
      via: "on: push: tags: 'v*.*.*'"
      pattern: "v0.3.4-rc1"
    - from: "docs/releases/v0.3.4-rc1-VALIDATION.md"
      to: "curl-installer sha256 verification"
      via: "official curl-pipe-bash path (packaging/curl-installer/install.sh verifies .sha256)"
      pattern: "sha256"
---

<objective>
Take the green, version-locked branch from Plan 01 all the way to a published, maintainer-validated release candidate: push the branch and open the PR, pause for the maintainer to merge it, push the pre-authorized `v0.3.4-rc1` tag (firing release.yml), confirm the rc published with its `.sha256`, write the brownfield-VM validation runbook, and pause for the maintainer's real-VM acceptance feedback.

Purpose: This is the irreversible / outward-facing chain on the live public repo (github.com/Roo4L/Agent-Linux). The orchestrator supervises every `autonomous: false` task directly. Two HARD STOPS are human-reserved: (1) the maintainer merges the PR; (2) the maintainer validates the rc on his real brownfield VM — the genuine v0.3.4 acceptance.

Output: An open PR, a published v0.3.4-rc1 GitHub Release (tarball + .sha256), a committed validation runbook, and captured maintainer feedback feeding the Plan 03 decision gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified facts. Use directly. -->

Remote / auth (verified 2026-05-30):
  origin = git@github.com:Roo4L/Agent-Linux.git (SSH push identity = Roo4L).
  gh CLI authed as Roo4L (repo + admin:public_key + write:packages scopes).
  Branch `worktree-aware-install` NOT yet pushed to origin.

Release pipeline (.github/workflows/release.yml):
  Trigger: `on: push: tags: 'v*.*.*'`. Pushing v0.3.4-rc1 FIRES it.
  Gate sequence (needs:): resolve → gate-1-precommit → gate-2-docker (22.04/24.04/26.04)
    → gate-3-qemu (22.04/24.04/26.04) → gate-4-pinned-combo → build → publish.
  publish runs ONLY on real tag push (softprops/action-gh-release@v2.6.2),
  uploads: agentlinux-<TAG>.tar.gz, agentlinux-<TAG>.tar.gz.sha256, catalog-<TAG>.json, VERSION (+ optional .deb).
  scripts/build-release.sh produces the tarball + sibling .sha256 (CLAUDE.md hard rule).

Existing tags: …v0.3.2, v0.3.2-rc1, v0.3.2-rc2 (last shipped v0.3.2). No v0.3.3/v0.3.4 yet.

Runbook verified invocations (CONTEXT code_context — use VERBATIM):
  - curl-pipe-bash install path mirrors tests/docker/dogfood.sh: export AGENTLINUX_VERSION=v0.3.4-rc1
    to dodge the AL-31 unpinned-resolution 404 against a just-published rc, then
    `… | sudo bash -s -- <flags>` (curl-installer forwards flags via `exec "$exe" "$@"`).
  - packaging/curl-installer/install.sh downloads <tarball>.sha256 and runs
    `sha256sum -c` BEFORE executing (CLAUDE.md rule satisfied by using the official path).
  - `sudo agentlinux-install --dry-run` → full detection + decision pass, exit 0, ZERO mutation
    (preview semantic, mirrors `terraform plan`). NOTE: `--dry-run --yes` is contradictory (exit 64) —
    the dry-run step MUST NOT pass --yes.
  - Plain `sudo agentlinux-install </dev/null` (no --yes, non-TTY) → fail-safe: prints the structured
    `[BAIL] component=<n> reason=<r> hint=<h>` block and exits 65 (EX_DATAERR) BEFORE mutating anything.
  - Real `sudo agentlinux-install --yes` applies remediations.
  - AGT-02 zero-EACCES check: `sudo -u agent -H bash --login -c 'claude update'` then
    `claude --version`; assert zero EACCES/permission-denied lines + version monotonicity (sort -V).
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1 (DEL-01b — supervised): Push branch + open PR to master</name>
  <files>(remote: github.com/Roo4L/Agent-Linux — no local file writes)</files>
  <read_first>
    - .planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md §Merge Path (Q2)
    - The Plan 01 SUMMARY (.planning/phases/17-*/17-01-SUMMARY.md) for the test-evidence to cite
  </read_first>
  <action>
    OUTWARD-FACING action on the live public repo — orchestrator-supervised (autonomous: false).
      1. Push the integrated branch: `git push -u origin worktree-aware-install`.
      2. Open a PR to master via gh:
         `gh pr create --base master --head worktree-aware-install \
            --title "v0.3.4 Aware Installation Process — release candidate" \
            --body-file <bodyfile>`
         The PR body MUST summarize: the v0.3.4 aware-install work (Phases 12–16: Detection →
         Reuse → Remediate → Pre-flight UX → Docs + Brownfield Gate), the rc plan (cut v0.3.4-rc1
         after merge → release.yml → maintainer brownfield-VM validation), and a test-evidence
         checklist (Docker bats 22.04 + 24.04 green; CLI pnpm test green; 3-way version lock for
         v0.3.4-rc1 validated via build-release --dry-run; AGT-02 zero-EACCES brownfield gate GREEN
         per docs/audits/v0.3.4/AGT-02-brownfield-acceptance.md).
    SECURITY: NO secrets in the PR body, title, or any commit message (tokens, keys, internal URLs).
    The PR diff is the maintainer's review surface — that review + the green release gates are the
    control against shipping unreviewed content.
    DO NOT merge the PR. Merging is the maintainer's reserved click (next task).
  </action>
  <acceptance_criteria>
    - `git ls-remote --heads origin worktree-aware-install` returns a non-empty line (branch pushed)
    - `gh pr view worktree-aware-install --json url,state -q .state` returns `OPEN`
    - `gh pr view worktree-aware-install --json baseRefName -q .baseRefName` returns `master`
    - `gh pr view worktree-aware-install --json body -q .body` contains a test-evidence checklist (e.g. the substring `Docker bats`)
  </acceptance_criteria>
  <verify>
    <automated>git ls-remote --heads origin worktree-aware-install | grep -q worktree-aware-install && [ "$(gh pr view worktree-aware-install --json state -q .state)" = "OPEN" ] && [ "$(gh pr view worktree-aware-install --json baseRefName -q .baseRefName)" = "master" ]</automated>
  </verify>
  <done>The branch is pushed to origin and an OPEN PR to master exists with a v0.3.4 summary + test-evidence checklist. PR is NOT merged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (DEL-04 — HUMAN CHECKPOINT): Maintainer merges the PR</name>
  <what-built>An OPEN PR to master summarizing the v0.3.4 aware-install work, with green test evidence. CI (test.yml / pr-preview.yml) runs on the integrated result.</what-built>
  <how-to-verify>
    HARD STOP — this step is human-reserved (CONTEXT Q2). The maintainer:
      1. Reviews the full PR diff on github.com/Roo4L/Agent-Linux.
      2. Confirms CI checks on the PR are green.
      3. Clicks "Merge" to merge the PR into master.
    Claude/the orchestrator MUST NOT merge the PR programmatically. Wait for the maintainer.
    Resume condition (orchestrator confirms before proceeding):
      `gh pr view worktree-aware-install --json state -q .state` returns `MERGED`.
  </how-to-verify>
  <resume-signal>Maintainer reports the PR is merged (verify: `gh pr view worktree-aware-install --json state -q .state` == `MERGED`). Type "merged" or report issues.</resume-signal>
</task>

<task type="auto" gate="blocking">
  <name>Task 3 (DEL-02b — supervised, irreversible): Cut + push v0.3.4-rc1 tag, watch release</name>
  <files>(remote: github.com/Roo4L/Agent-Linux — tag + GitHub Release)</files>
  <read_first>
    - .planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md §Version + RC Tag (Q3)
    - .github/workflows/release.yml (gate sequence + publish job + uploaded assets)
  </read_first>
  <action>
    ONLY after Task 2 confirms the PR is MERGED. Orchestrator-supervised (autonomous: false,
    irreversible/public) — but NO further human approval needed (the tag push is pre-authorized
    per CONTEXT Q3).
      1. Fast-forward local master to the merged origin/master:
         `git fetch origin && git checkout master && git merge --ff-only origin/master`.
      2. Re-confirm the 3-way lock on the merged commit before tagging:
         `bash scripts/build-release.sh v0.3.4-rc1 --dry-run` (must exit 0, report version=0.3.4 matches).
      3. Create + push the rc tag on the merged master commit:
         `git tag v0.3.4-rc1 && git push origin v0.3.4-rc1`.
         This FIRES release.yml (`on: push: tags: 'v*.*.*'`).
      4. Watch the release run to completion:
         `gh run watch $(gh run list --workflow=release.yml --branch=v0.3.4-rc1 --limit=1 --json databaseId -q '.[0].databaseId')`
         (or poll `gh run list --workflow=release.yml`). All gates (precommit → docker → qemu →
         pinned-combo → build → publish) must go green.
      5. Confirm the rc published WITH its sibling .sha256:
         `gh release view v0.3.4-rc1 --json assets -q '.assets[].name'` must list BOTH
         `agentlinux-v0.3.4-rc1.tar.gz` AND `agentlinux-v0.3.4-rc1.tar.gz.sha256`.
    SECURITY: a wrong 3-way lock would ship a mis-versioned tag — step 2's --dry-run is the gate
    BEFORE the push. The tag push triggers a PUBLIC release; the merged PR review + the green
    gates are the control against shipping unreviewed content.
  </action>
  <acceptance_criteria>
    - `git ls-remote --tags origin v0.3.4-rc1` returns a non-empty line (tag pushed)
    - `gh run list --workflow=release.yml --branch=v0.3.4-rc1 --json conclusion -q '.[0].conclusion'` returns `success`
    - `gh release view v0.3.4-rc1 --json assets -q '.assets[].name'` lists `agentlinux-v0.3.4-rc1.tar.gz`
    - `gh release view v0.3.4-rc1 --json assets -q '.assets[].name'` lists `agentlinux-v0.3.4-rc1.tar.gz.sha256` (sibling .sha256 — CLAUDE.md rule)
  </acceptance_criteria>
  <verify>
    <automated>git ls-remote --tags origin v0.3.4-rc1 | grep -q v0.3.4-rc1 && gh release view v0.3.4-rc1 --json assets -q '.assets[].name' | grep -qx 'agentlinux-v0.3.4-rc1.tar.gz' && gh release view v0.3.4-rc1 --json assets -q '.assets[].name' | grep -qx 'agentlinux-v0.3.4-rc1.tar.gz.sha256'</automated>
  </verify>
  <done>v0.3.4-rc1 is tagged on the merged master commit, release.yml succeeded, and the published GitHub Release carries both the tarball and its sibling .sha256.</done>
</task>

<task type="auto">
  <name>Task 4 (DEL-03): Write the maintainer brownfield-VM validation runbook</name>
  <files>docs/releases/v0.3.4-rc1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md §Specifics (runbook ordering) + §code_context (verified invocations)
    - tests/docker/dogfood.sh (the curl-pipe-bash pattern + AGENTLINUX_VERSION export to mirror)
    - packaging/curl-installer/install.sh (confirms the official path verifies .sha256)
  </read_first>
  <action>
    Create docs/releases/v0.3.4-rc1-VALIDATION.md — a step-by-step runbook the maintainer follows
    on his REAL brownfield VM, in this exact order, using the EXACT verified invocations from
    CONTEXT code_context:
      (a) Take a VM snapshot (rollback point) BEFORE anything.
      (b) Install the rc via the official curl-pipe-bash path against the published v0.3.4-rc1 tag,
          mirroring tests/docker/dogfood.sh — export `AGENTLINUX_VERSION=v0.3.4-rc1` (AL-31:
          unpinned curl-bash 404s against a fresh rc) so it resolves the pinned tag; note that the
          official curl-installer verifies the sibling .sha256 before executing (CLAUDE.md rule).
      (c) `sudo agentlinux-install --dry-run` — NON-mutating preview: read the detection report +
          any flagged remediate component. Exit 0, ZERO mutation. (Do NOT add --yes — `--dry-run --yes`
          is contradictory and exits 64.)
      (d) Plain `sudo agentlinux-install </dev/null` (no --yes, non-TTY) — fail-safe: shows the
          structured `[BAIL] component=<n> reason=<r> hint=<h>` refusal and exits 65 (EX_DATAERR)
          BEFORE mutating anything.
      (e) The real `sudo agentlinux-install --yes` install (applies remediations).
      (f) AGT-02 zero-EACCES check + version monotonicity:
          `sudo -u agent -H bash --login -c 'claude update'` then
          `sudo -u agent -H bash --login -c 'claude --version'` — assert zero EACCES/permission-denied
          lines and that the post-update version is >= the pre-update version (sort -V).
      (g) Rollback via the snapshot from (a) if anything looks wrong.
      (h) How to report feedback back (so it can feed the Plan 03 decision gate).
    Use literal copy-pasteable command blocks. SECURITY: instruct the maintainer to use the OFFICIAL
    curl-installer (which `sha256sum -c`-verifies the sibling .sha256) — do not suggest fetching the
    raw tarball without verification. No secrets in the doc.
    Run the CLAUDE.md review loop (technical-writer + fact-checker for docs) before declaring done.
  </action>
  <acceptance_criteria>
    - `test -f docs/releases/v0.3.4-rc1-VALIDATION.md` (file exists)
    - File contains `--dry-run` (step c) AND `--yes` (step e) AND `claude update` (step f)
    - File contains the literal `[BAIL]` token (step d structured refusal) AND `exit 65` (or `65`) reference
    - File contains `AGENTLINUX_VERSION=v0.3.4-rc1` (AL-31 pinned-resolution mirror of dogfood.sh)
    - File references `sha256` verification of the rc (the CLAUDE.md sibling-.sha256 rule via the official installer)
    - File references taking a VM `snapshot` (step a) and rollback (step g)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/releases/v0.3.4-rc1-VALIDATION.md && grep -q -- '--dry-run' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -q -- '--yes' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -q 'claude update' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -qF '[BAIL]' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -q 'AGENTLINUX_VERSION=v0.3.4-rc1' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -qi 'sha256' docs/releases/v0.3.4-rc1-VALIDATION.md && grep -qi 'snapshot' docs/releases/v0.3.4-rc1-VALIDATION.md</automated>
  </verify>
  <done>docs/releases/v0.3.4-rc1-VALIDATION.md exists with the ordered runbook (snapshot → pinned curl-install w/ .sha256 verify → --dry-run preview → no-yes [BAIL] exit 65 → --yes install → AGT-02 zero-EACCES + monotonicity → rollback → feedback), using the verified invocations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (DEL-04 — HUMAN CHECKPOINT): Maintainer validates the rc on his brownfield VM</name>
  <what-built>A published v0.3.4-rc1 GitHub Release (tarball + .sha256) and a committed brownfield-VM validation runbook (docs/releases/v0.3.4-rc1-VALIDATION.md).</what-built>
  <how-to-verify>
    HARD STOP — this is the genuine v0.3.4 acceptance (CONTEXT Q4). The maintainer follows
    docs/releases/v0.3.4-rc1-VALIDATION.md on his REAL brownfield VM, in order:
      1. Snapshot the VM.
      2. Install the rc via the official curl-pipe-bash path (AGENTLINUX_VERSION=v0.3.4-rc1; .sha256 verified).
      3. `sudo agentlinux-install --dry-run` — confirm non-mutating detection preview (exit 0).
      4. Plain `sudo agentlinux-install </dev/null` — confirm the [BAIL] structured refusal + exit 65.
      5. `sudo agentlinux-install --yes` — the real install.
      6. `claude update` AGT-02 zero-EACCES check + version monotonicity.
      7. Roll back via snapshot if anything looks wrong.
      8. Report feedback (positive → promote in Plan 03; issues → gap-closure in Plan 03).
    Wait for the maintainer's feedback before proceeding to the decision gate.
  </how-to-verify>
  <resume-signal>Maintainer returns brownfield-VM validation feedback. Type "validated: positive" or "validated: issues — <summary>" so Plan 03 can branch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch → public GitHub repo | Push/PR/tag expose content on github.com/Roo4L/Agent-Linux |
| rc tag push → public release pipeline | Pushing v0.3.4-rc1 triggers a PUBLIC build + GitHub Release |
| published tarball → maintainer VM | The runbook's install path executes downloaded code on a real host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | rc tag version lock | mitigate | Task 3 re-runs `build-release.sh v0.3.4-rc1 --dry-run` (3-way lock) on the merged commit BEFORE `git push origin v0.3.4-rc1` — a wrong version fails before the irreversible push. |
| T-17-05 | Tampering | runbook install path on the VM | mitigate | The runbook (Task 4) instructs the OFFICIAL curl-installer (packaging/curl-installer/install.sh), which downloads `<tarball>.sha256` and runs `sha256sum -c` before executing — CLAUDE.md sibling-.sha256 rule. Acceptance criteria assert the .sha256 reference is present. |
| T-17-06 | Information disclosure | PR body / release notes / commit & tag messages | mitigate | Task 1 explicitly forbids secrets in PR body/title/commits; Task 3 forbids secrets in tag message; release.yml uses `generate_release_notes` from merged commits (already reviewed). No token/key strings introduced. |
| T-17-07 | Elevation of privilege | public rc release of unreviewed content | mitigate | The rc only ships AFTER the maintainer's PR merge (human review) AND the 6-gate release.yml (precommit→docker→qemu→pinned-combo→build→publish) — both controls must pass before publish. |
| T-17-08 | Repudiation | who pushed the tag | accept | Push identity is Roo4L (the maintainer's own GitHub identity, pre-authorized per CONTEXT Q3); the tag is attributable. Low risk. |
</threat_model>

<verification>
- Branch pushed; OPEN PR to master with test-evidence checklist (Task 1).
- PR merged by the maintainer (Task 2 — human).
- v0.3.4-rc1 tag pushed on merged master; release.yml success; Release carries tarball + .sha256 (Task 3).
- Runbook committed with all ordered steps + verified invocations + .sha256 + AGENTLINUX_VERSION pin (Task 4).
- Maintainer brownfield-VM validation feedback captured (Task 5 — human).
</verification>

<success_criteria>
The branch is pushed and PR'd, the maintainer has merged it, the pre-authorized v0.3.4-rc1 tag is pushed and release.yml has published the rc with its sibling .sha256, the brownfield-VM validation runbook is committed, and the maintainer's real-VM acceptance feedback is captured — ready for the promote-or-iterate decision in Plan 03. Claude never merged the PR and never bypassed either human checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/17-changes-delivery-and-release-candidate/17-02-SUMMARY.md` (record the PR URL, the published Release URL, and the maintainer feedback verbatim for Plan 03).
</output>
