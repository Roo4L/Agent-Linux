---
phase: 17-changes-delivery-and-release-candidate
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md
autonomous: false
requirements: [DEL-05]
user_setup:
  - service: jira
    why: "On promote-to-final: transition AL-41/44/55/56/57 → Done and ASK the user before closing AL-38 (anchor memory)"
must_haves:
  truths:
    - "The maintainer's brownfield-VM feedback (from Plan 02 Task 5) is classified as positive or issues-found"
    - "Both decision branches are DESCRIBED (not executed) so the human picks the path"
    - "Positive branch describes: promote rc→final (push v0.3.4 tag on the SAME merged commit, bump nothing) → GSD milestone lifecycle (audit→complete→cleanup) → Jira (transition subtasks; ask before closing AL-38)"
    - "Negative branch describes: capture feedback as gap-closure plans INSIDE Phase 17 → re-cut rc2 (do NOT close the milestone)"
  artifacts:
    - path: ".planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md"
      provides: "The promote-or-iterate decision record + both described branches awaiting human selection"
      min_lines: 40
  key_links:
    - from: "maintainer feedback (Plan 02 Task 5)"
      to: "promote (v0.3.4 final) OR iterate (rc2 gap-closure)"
      via: "human decision gate"
      pattern: "promote|iterate"
---

<objective>
Convert the maintainer's brownfield-VM feedback into a clear go/no-go decision gate: DESCRIBE (not execute) both the promote-to-final path and the iterate-on-issues path, then let the human choose. This plan deliberately stops at the decision — it does not push the final v0.3.4 tag or close the milestone autonomously.

Purpose: v0.3.4 final and milestone close are gated on real maintainer acceptance (CONTEXT Q4). The decision (and AL-38 close) is the user's, not the planner's or executor's. This plan records the two branches and pauses.

Output: A committed decision record (17-DECISION.md) laying out both paths with exact follow-up steps, and a human-decision checkpoint that routes to either promotion or rc2 iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md
@.planning/phases/17-changes-delivery-and-release-candidate/17-02-SUMMARY.md

<interfaces>
<!-- Verified facts. Use directly. -->

Promote semantics (CONTEXT §Close Timing Q4): on positive feedback, promote rc→final by
pushing the `v0.3.4` tag on the SAME merged master commit the rc was cut from — bump NOTHING
(package.json/catalog.json already read 0.3.4; build-release.sh strips no suffix for a plain
v0.3.4, so the 3-way lock already holds). release.yml fires again on the v0.3.4 tag and
publishes the final.

Milestone lifecycle runs ONLY AFTER promote (CONTEXT Q4): GSD audit → complete → cleanup.
Jira (anchor memory + CONTEXT §Specifics): transition the 5 phase subtasks
AL-41/AL-44/AL-55/AL-56/AL-57 → Done; ASK THE USER before closing the AL-38 anchor.

Iterate semantics (CONTEXT §Q4): on issues, capture feedback as gap-closure plans INSIDE
Phase 17 (e.g. via `/gsd-plan-phase 17 --gaps`), fix, re-cut rc2 (v0.3.4-rc2). Do NOT close
the milestone on negative feedback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (DEL-05): Write the promote-or-iterate decision record (describe both branches)</name>
  <files>.planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md</files>
  <read_first>
    - .planning/phases/17-changes-delivery-and-release-candidate/17-02-SUMMARY.md (the captured maintainer feedback)
    - .planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md §Close Timing + Feedback Gate (Q4)
  </read_first>
  <action>
    Create .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md. Restate the
    maintainer's verbatim feedback (from 17-02-SUMMARY.md) and classify it as POSITIVE or
    ISSUES-FOUND. Then DESCRIBE both branches as concrete, ordered follow-up steps the human can
    pick from (do NOT execute either — no tag push, no milestone close, no Jira transition here):

    POSITIVE → promote rc → final v0.3.4:
      1. Bump NOTHING (package.json/catalog.json already at 0.3.4).
      2. `git tag v0.3.4 <merged-commit>` (same commit the rc was cut from) and
         `git push origin v0.3.4` → release.yml publishes the final (tarball + .sha256).
      3. Confirm `gh release view v0.3.4` shows the tarball + sibling .sha256.
      4. GSD milestone lifecycle: audit → complete → cleanup (per CONTEXT Q4).
      5. Jira: transition AL-41/AL-44/AL-55/AL-56/AL-57 → Done; ASK THE USER before closing AL-38
         (anchor memory — never auto-close AL-38).

    ISSUES-FOUND → iterate (do NOT close the milestone):
      1. Capture each issue as a discrete, testable gap with its acceptance criterion.
      2. Spin gap-closure plans INSIDE Phase 17 (e.g. `/gsd-plan-phase 17 --gaps`).
      3. After fixes land + suite green, re-cut rc2: push `v0.3.4-rc2` (same flow as rc1).
      4. Re-run the Plan 02 Task 5 brownfield-VM validation checkpoint against rc2.

    End the doc with an explicit "SELECTED BRANCH: <pending human decision>" line. This task is a
    DESCRIPTION artifact only.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md`
    - File contains a `promote` branch describing `git push origin v0.3.4` on the same merged commit (bump nothing)
    - File contains the literal Jira subtask IDs `AL-41` and `AL-38`, and instructs to ASK before closing AL-38
    - File contains an `iterate`/`gap-closure` branch describing re-cutting `v0.3.4-rc2` and NOT closing the milestone
    - File contains the milestone-lifecycle phrase (e.g. `audit` and `cleanup`) gated on the positive branch
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md && grep -q 'git push origin v0.3.4' .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md && grep -qF 'AL-38' .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md && grep -q 'v0.3.4-rc2' .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md && grep -qi 'gap-closure\|gap closure' .planning/phases/17-changes-delivery-and-release-candidate/17-DECISION.md</automated>
  </verify>
  <done>17-DECISION.md exists, restates+classifies the feedback, and describes both the promote (v0.3.4 final + milestone lifecycle + Jira, ask-before-AL-38) and iterate (gap-closure plans + rc2, no close) branches without executing either.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (DEL-05 — HUMAN DECISION GATE): Promote or iterate</name>
  <decision>Promote v0.3.4-rc1 → final v0.3.4, OR iterate (capture issues → rc2)?</decision>
  <context>
    The maintainer validated the rc on his real brownfield VM (Plan 02 Task 5 — the genuine v0.3.4
    acceptance). This is the human go/no-go. Per CONTEXT Q4, milestone close (and AL-38 close) only
    happen on promotion; negative feedback never closes the milestone. The planner/executor does NOT
    decide this — the human does. 17-DECISION.md describes both paths.
  </context>
  <options>
    <option id="promote">
      <name>Promote rc → final v0.3.4</name>
      <pros>Feedback positive; ships v0.3.4. Triggers milestone lifecycle (audit→complete→cleanup) + Jira subtask close (ask before AL-38).</pros>
      <cons>Final, public release of v0.3.4 on the same merged commit.</cons>
    </option>
    <option id="iterate">
      <name>Iterate — capture issues, re-cut rc2</name>
      <pros>Feedback surfaced issues; fix them as gap-closure plans inside Phase 17, re-cut v0.3.4-rc2, re-validate. Milestone stays open.</pros>
      <cons>Another rc cycle before final ship.</cons>
    </option>
  </options>
  <resume-signal>Select: promote (then run the promote branch from 17-DECISION.md, asking before closing AL-38) or iterate (then `/gsd-plan-phase 17 --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer feedback → ship/iterate decision | A misread of feedback could promote a flawed rc to final |
| promote path → public final release | Pushing v0.3.4 publishes the final to the public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Elevation of privilege | promote-to-final without true acceptance | mitigate | Promotion is a `checkpoint:decision` human gate (Task 2) — the executor cannot promote autonomously. 17-DECISION.md surfaces the verbatim feedback + classification for the human to confirm. |
| T-17-10 | Tampering | final v0.3.4 release content | accept | The final v0.3.4 tag is pushed on the SAME already-reviewed, already-released merged commit as rc1 (bump nothing) — no new unreviewed content enters; the rc1 PR review + green gates already covered it. |
| T-17-11 | Repudiation | premature milestone/AL-38 close | mitigate | CONTEXT Q4 + anchor memory: milestone lifecycle runs only on the promote branch, and AL-38 close explicitly ASKS the user first — encoded in 17-DECISION.md acceptance criteria. |
</threat_model>

<verification>
- 17-DECISION.md exists, classifies the feedback, and describes both branches (promote vs iterate) with exact steps.
- The decision is a human checkpoint:decision gate — neither branch is executed in this plan.
- Promote branch encodes: same-commit v0.3.4 tag, milestone lifecycle, Jira transitions, ask-before-AL-38.
- Iterate branch encodes: gap-closure plans inside Phase 17, rc2, no milestone close.
</verification>

<success_criteria>
The maintainer's feedback is converted into a committed decision record describing both the promote-to-final path (v0.3.4 tag on the same commit → milestone lifecycle → Jira, ask-before-AL-38) and the iterate path (gap-closure plans → rc2, milestone stays open), and the human is presented an explicit decision gate. Nothing is promoted or closed autonomously.
</success_criteria>

<output>
After completion, create `.planning/phases/17-changes-delivery-and-release-candidate/17-03-SUMMARY.md` (record which branch the human selected and what follow-up was triggered).
</output>
