---
phase: 17-changes-delivery-and-release-candidate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/cli/package.json
  - plugin/catalog/catalog.json
autonomous: true
requirements: [DEL-02a, DEL-01]
must_haves:
  truths:
    - "plugin/cli/package.json .version == 0.3.4 and plugin/catalog/catalog.json .version == 0.3.4 (lockstep)"
    - "scripts/build-release.sh v0.3.4-rc1 --dry-run exits 0 (3-way version lock matches)"
    - "origin/master (7 commits ahead, incl AL-51) is merged INTO worktree-aware-install with no conflict markers"
    - "Full test suite is green: Docker bats (22.04 + 24.04) + plugin/cli pnpm test"
    - "scripts/check-version-lockstep.sh passes (pre-commit version-lockstep hook clean)"
  artifacts:
    - path: "plugin/cli/package.json"
      provides: "Canonical version SoT bumped to 0.3.4"
      contains: '"version": "0.3.4"'
    - path: "plugin/catalog/catalog.json"
      provides: "Catalog version bumped to 0.3.4 in lockstep"
      contains: '"version": "0.3.4"'
  key_links:
    - from: "plugin/cli/package.json .version"
      to: "plugin/catalog/catalog.json .version"
      via: "scripts/check-version-lockstep.sh (pre-commit hook)"
      pattern: '"version": "0.3.4"'
    - from: "tag v0.3.4-rc1"
      to: "package.json/catalog.json base version 0.3.4"
      via: "scripts/build-release.sh --dry-run 3-way lock (BASE_VERSION=${VERSION%%-*})"
      pattern: "matches"
---

<objective>
Bump AgentLinux from 0.3.2 to 0.3.4 in lockstep across the two canonical version files, integrate the 7 diverged `origin/master` commits (incl. AL-51) INTO the worktree branch via a merge commit, and get the full test suite green — so the branch is conflict-free, version-locked, and ready to push as a PR.

Purpose: This is the safe, autonomous local prep that MUST be complete and green before any outward-facing git/remote operation (push, PR, tag). It de-risks the irreversible steps in Plan 02 by proving the version lock holds and the integrated tree passes CI locally.

Output: A version-bumped (0.3.4), master-integrated, fully-green worktree branch. No remote operations in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified facts from the codebase. Executor should use these directly — no exploration needed. -->

Version coupling (verified 2026-05-30): ONLY two live `.version` strings exist —
  plugin/cli/package.json  (the canonical SoT under AL-29) and
  plugin/catalog/catalog.json.
All other `0.3.2` occurrences in the repo are HISTORICAL COMMENTS referencing past
regressions (e.g. `v0.3.2-rc1` dogfood notes in plugin/bin/agentlinux-install,
packaging/curl-installer/install.sh, scripts/build-release.sh). DO NOT edit comments.

The installer derives version dynamically — DO NOT hardcode anywhere:
  - plugin/bin/agentlinux-install sed-extracts AGENTLINUX_VERSION from package.json at install time.
  - plugin/cli/src/version.ts reads package.json at module load (VERSION export).
  - bats tests read it via `jq -r .version /opt/agentlinux-src/plugin/cli/package.json`
    (10-installer.bats, 40-registry-cli.bats, 50-agents.bats — AL-29 SoT consolidation).
  => A lockstep bump of the two .version fields propagates to runtime paths, the CLI,
     and the bats matrix automatically. No staged-catalog-path edits needed.

Version-lockstep gate: scripts/check-version-lockstep.sh (pre-commit hook id
`check-version-lockstep`) asserts package.json.version == catalog.json.version using
coreutils sed (no jq). Drift => exit 1 with a precise diagnostic. Bump BOTH together.

3-way release lock: scripts/build-release.sh strips the rc suffix
(`BASE_VERSION=${VERSION%%-*}`), so tag `v0.3.4-rc1` requires base `0.3.4` in both files.
`--dry-run` validates tag-shape + 3-way lock, prints the planned artifact set, exits 0
WITHOUT building. A drift => exit 1 (version mismatch) or exit 64 (bad tag shape).

Git state (verified): branch `worktree-aware-install` @ 27ab393; divergence vs
origin/master = 7 (master-only) / 83 (branch-only); merge-base 77043fa.
origin = git@github.com:Roo4L/Agent-Linux.git. CONTEXT.md mandates merge-commit
integration (merge origin/master INTO the branch) — NOT a rebase of the 83 commits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lockstep version bump 0.3.2 → 0.3.4 + verify 3-way lock</name>
  <files>plugin/cli/package.json, plugin/catalog/catalog.json</files>
  <read_first>
    - plugin/cli/package.json (the .version field, currently "0.3.2")
    - plugin/catalog/catalog.json (the .version field, currently "0.3.2")
    - scripts/check-version-lockstep.sh (the gate this bump must satisfy)
    - scripts/build-release.sh §3 three-way-lock (the --dry-run gate)
  </read_first>
  <action>
    Bump BOTH version fields from 0.3.2 to 0.3.4 (there was never a 0.3.3 — milestone
    numbering jumped per CONTEXT.md):
      1. In plugin/cli/package.json, set the top-level `"version"` field to `"0.3.4"`.
         Use a surgical edit of the `"version": "0.3.2"` line ONLY — do not reformat the file.
      2. In plugin/catalog/catalog.json, set the top-level `"version"` field to `"0.3.4"`.
         Same surgical edit; preserve byte formatting (the release catalog snapshot is a
         byte-for-byte `cp`, so whitespace must not churn).
    DO NOT touch any other `0.3.2` occurrence — they are historical comments (see
    <interfaces>). DO NOT hardcode the version into bats tests, the installer, or version.ts;
    those derive it dynamically.
    After the edit, confirm no NEW hardcoded 0.3.2-vs-0.3.4 coupling slipped in by
    re-grepping tests/bats, plugin (excluding node_modules), packaging, scripts and
    confirming the only `.version` lines now read 0.3.4 and remaining 0.3.2 hits are comments.
  </action>
  <acceptance_criteria>
    - `jq -r .version plugin/cli/package.json` outputs exactly `0.3.4`
    - `jq -r .version plugin/catalog/catalog.json` outputs exactly `0.3.4`
    - `bash scripts/check-version-lockstep.sh` exits 0 (no stdout on success)
    - `bash scripts/build-release.sh v0.3.4-rc1 --dry-run` exits 0 AND its stdout contains
      `version=0.3.4` and both `(matches)` lines (CLI + catalog)
    - `grep -rn '"version": "0\.3\.2"' plugin/cli/package.json plugin/catalog/catalog.json`
      returns NOTHING (no stale 0.3.2 .version remains)
  </acceptance_criteria>
  <verify>
    <automated>jq -r .version plugin/cli/package.json | grep -qx 0.3.4 && jq -r .version plugin/catalog/catalog.json | grep -qx 0.3.4 && bash scripts/check-version-lockstep.sh && bash scripts/build-release.sh v0.3.4-rc1 --dry-run | grep -q 'version=0.3.4'</automated>
  </verify>
  <done>Both .version fields read 0.3.4, lockstep hook passes, and the build-release --dry-run reports the 3-way lock matches for tag v0.3.4-rc1.</done>
</task>

<task type="auto">
  <name>Task 2: Merge-integrate origin/master into the branch + full suite green</name>
  <files>(git working tree — merge commit; any conflict-resolved files from the 7 master commits incl AL-51)</files>
  <read_first>
    - .planning/phases/17-changes-delivery-and-release-candidate/17-CONTEXT.md §Merge Path (Q2)
    - The output of `git log --oneline origin/master ^HEAD` (the 7 master-only commits, incl AL-51)
    - CLAUDE.md §Commands (the canonical test invocations)
  </read_first>
  <action>
    Integrate the 7 diverged master commits INTO the worktree branch via a MERGE COMMIT
    (CONTEXT Q2 — NOT a rebase of the 83 branch commits; merge-commit integration has lower
    conflict risk). Concretely:
      1. `git fetch origin` to refresh origin/master.
      2. Stage and commit any in-flight planning artifacts first so the tree is clean before
         merge: the version bump from Task 1 (plugin/cli/package.json, plugin/catalog/catalog.json)
         plus the Phase 17 planning files (.planning/phases/17-*, .planning/ROADMAP.md,
         .planning/STATE.md, this PLAN). Use a `chore(17): bump to 0.3.4 + phase-17 plan`-style
         commit. (Working tree must be clean for the merge to be reviewable.)
      3. `git merge origin/master` (default merge commit). If conflicts arise, resolve them
         honoring the project rules in CLAUDE.md (never `sudo npm install -g`; behavior tests in
         tests/bats are the spec; every release tarball ships a sibling .sha256). For version
         conflicts in package.json/catalog.json, KEEP 0.3.4 (our bump wins; master is still 0.3.2).
         Commit the merge.
      4. Run the FULL suite and get it green:
           - `./tests/docker/run.sh ubuntu-24.04`
           - `./tests/docker/run.sh ubuntu-22.04`
           - `cd plugin/cli && pnpm test`
         If AL-51 (or any integrated master commit) introduced a fixture/path that interacts with
         the version bump, reconcile so the bats matrix + CLI unit tests stay green. Do NOT pin
         implementation choices — fix the test wiring, keep behavior intact.
      5. Commit hygiene check: confirm no `<<<<<<<`/`>>>>>>>`/`=======` conflict markers remain in
         tracked files and no stray debug/WIP commits are in the merge.
    Run the CLAUDE.md review feedback loop on any files you had to touch during conflict
    resolution before declaring done.
  </action>
  <acceptance_criteria>
    - `git merge-base --is-ancestor origin/master HEAD` exits 0 (origin/master is now an ancestor — integration done)
    - `git rev-list --left-right --count origin/master...HEAD` shows `0` on the left (zero master-only commits remain)
    - `grep -rIl '^<<<<<<< \|^>>>>>>> \|^=======$' $(git ls-files) 2>/dev/null` returns NOTHING (no conflict markers in tracked files)
    - `./tests/docker/run.sh ubuntu-24.04` exits 0 (bats matrix green)
    - `./tests/docker/run.sh ubuntu-22.04` exits 0 (bats matrix green)
    - `cd plugin/cli && pnpm test` exits 0 (CLI unit tests green)
    - `jq -r .version plugin/cli/package.json` still outputs `0.3.4` after the merge (our bump survived any conflict)
  </acceptance_criteria>
  <verify>
    <automated>git merge-base --is-ancestor origin/master HEAD && [ "$(git rev-list --count origin/master ^HEAD)" = "0" ] && jq -r .version plugin/cli/package.json | grep -qx 0.3.4 && ./tests/docker/run.sh ubuntu-24.04 && ./tests/docker/run.sh ubuntu-22.04 && ( cd plugin/cli && pnpm test )</automated>
  </verify>
  <done>origin/master is fully merged into the branch with zero conflict markers, the version bump survived, and Docker bats (22.04 + 24.04) plus CLI unit tests are all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version files → release tag | A wrong version in package.json/catalog.json would let a mis-versioned tag ship (the 3-way lock is the control) |
| origin/master → local branch | Integrating upstream commits could introduce unreviewed content into what becomes the PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | plugin/cli/package.json + catalog.json version lock | mitigate | Task 1 runs `scripts/build-release.sh v0.3.4-rc1 --dry-run` (3-way lock) AND `check-version-lockstep.sh` before any tag is ever cut in Plan 02 — a drifted version fails loudly here, never reaches the push. |
| T-17-02 | Tampering | origin/master merge result | mitigate | Merge-commit integration + full Docker bats (22.04+24.04) + CLI unit suite must be green; conflict-marker grep gate ensures no half-resolved files land. The downstream PR review (Plan 02) is the second control. |
| T-17-03 | Information disclosure | merge commit / bumped files | accept | No secrets touched; only `.version` strings + a routine upstream merge. Low-value, fully public-bound content. |
</threat_model>

<verification>
- `jq -r .version` on both files == 0.3.4; lockstep hook + build-release --dry-run green for v0.3.4-rc1.
- origin/master is an ancestor of HEAD (merge done), zero conflict markers, full Docker + CLI suite green.
- No new hardcoded 0.3.2 coupling introduced; remaining 0.3.2 strings are comments only.
</verification>

<success_criteria>
The worktree branch is version-bumped to 0.3.4 (lockstep), the 7 master commits (incl AL-51) are merge-integrated, the 3-way release lock validates for v0.3.4-rc1, and the full test suite (Docker bats 22.04 + 24.04 + CLI pnpm test) is green — leaving the branch conflict-free and ready to push as a PR in Plan 02. No remote operations performed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-changes-delivery-and-release-candidate/17-01-SUMMARY.md`
</output>
