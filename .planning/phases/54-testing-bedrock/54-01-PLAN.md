---
phase: 54-testing-bedrock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/crates/agentlinux-core/Cargo.toml
  - rust/crates/agentlinux-core/src/classify.rs
  - rust/crates/agentlinux-core/src/semver_shim.rs
  - rust/crates/agentlinux-core/src/divergence.rs
  - rust/crates/agentlinux-core/src/reuse.rs
  - docs/audits/v0.4.0/TEST-04-node-semver-parity.md
  - .gitignore
autonomous: true
requirements: [TEST-01, TEST-04, GATE-01, GATE-05]
tags: [rust, proptest, semver, property-testing, testing-bedrock]

must_haves:
  truths:
    - "classify provably returns a Status and never panics for any generated (entry, sentinel, installed) input (P1)"
    - "For any non-drift input where the sentinel is sticky, classify yields Synced or PinnedOverride and nothing else (P2)"
    - "resolve_latest_for either returns a published version that satisfies the constraint, or a typed DivergenceError — never an Ok that violates the range, never a panic (P3)"
    - "normalize_range is idempotent, parse_lenient never panics on loose inputs, and any max_satisfying result is in the input list and satisfies the range (P4)"
    - "The Rust semver_shim produces the same satisfies/maxSatisfying/valid verdicts as node-semver on every range shape the live catalog uses (TEST-04 golden)"
    - "A discovered proptest counterexample is replayed on every future run (proptest-regressions committed)"
  artifacts:
    - "rust/crates/agentlinux-core/Cargo.toml with proptest 1.11 + serde_json dev-deps AND schemars 1.2.2 dep (staged here so Plan 02 does not touch Cargo.toml)"
    - "proptest invariant modules P1-P4 inside classify.rs / semver_shim.rs / divergence.rs plus a reuse totality property"
    - "TEST-04 golden test module in semver_shim.rs seeded from the TS corpora + a catalog-driven case reading plugin/catalog/catalog.json"
    - "docs/audits/v0.4.0/TEST-04-node-semver-parity.md audit doc covering every catalog range/prerelease case"
    - "rust/crates/agentlinux-core/proptest-regressions/ committed to git"
  key_links:
    - "Every version comparison in the property + golden tests routes through semver_shim (never semver:: directly) so parity stays isolated"
    - "The catalog-driven golden case reads the real plugin/catalog/catalog.json version fields so a future catalog edit that introduces an unhandled range fails the test"
---

<objective>
Stand up the property-test machinery (TEST-01) and the node-semver parity audit + golden test (TEST-04) on the Phase-53 pure core (rust/crates/agentlinux-core), plus stage all Cargo.toml dependency additions this phase needs so downstream plans touch no shared manifest.

Purpose: Make the testing rigor that justified the Rust rewrite operational — invariants over generated inputs (not just the 44 example unit tests) and a maintained parity artifact that catches a future catalog range the shim mis-handles.

Output: proptest modules P1-P4, a TEST-04 golden test + audit doc, a committed proptest-regressions dir, and the fully-populated agentlinux-core/Cargo.toml (proptest + serde_json dev-deps, schemars dep).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-testing-bedrock/54-RESEARCH.md
@.planning/phases/54-testing-bedrock/54-VALIDATION.md

# The pure-core source the property + golden tests attach to
@rust/crates/agentlinux-core/src/classify.rs
@rust/crates/agentlinux-core/src/semver_shim.rs
@rust/crates/agentlinux-core/src/divergence.rs
@rust/crates/agentlinux-core/src/reuse.rs
@rust/crates/agentlinux-core/Cargo.toml

# The node-semver oracle corpora reused verbatim for the golden test
@plugin/cli/test/divergence.test.ts
@plugin/cli/test/classify.test.ts
@plugin/catalog/catalog.json
</context>

<artifacts_this_phase_produces>
This plan (54-01) produces:
- rust/crates/agentlinux-core/Cargo.toml — [dev-dependencies] proptest = "1.11", serde_json = "1"; [dependencies] schemars = { version = "1.2.2", features = ["derive"] } (schemars is staged HERE so Plan 02 schema-gen does not re-edit the shared manifest; the derive itself lands in Plan 02, and an unused dep does not fail the build).
- proptest invariant modules P1 (classify totality/determinism), P2 (sticky invariant), P3 (resolve_latest_for postcondition), P4 (semver_shim parse/normalize/max_satisfying totality + idempotence), plus a reuse::agent_decision totality property.
- docs/audits/v0.4.0/TEST-04-node-semver-parity.md — the parity audit doc.
- TEST-04 golden test module (in semver_shim.rs #[cfg(test)]) + a catalog-driven golden case.
- rust/crates/agentlinux-core/proptest-regressions/ — committed counterexample replay dir.
- .gitignore — ignore rust/**/mutants.out* scratch (staged here so Plan 03 does not touch .gitignore); DO NOT ignore proptest-regressions/.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test deps + proptest generators and the four invariant properties (TEST-01)</name>
  <files>rust/crates/agentlinux-core/Cargo.toml, rust/crates/agentlinux-core/src/classify.rs, rust/crates/agentlinux-core/src/semver_shim.rs, rust/crates/agentlinux-core/src/divergence.rs, rust/crates/agentlinux-core/src/reuse.rs, .gitignore</files>
  <read_first>
    - rust/crates/agentlinux-core/Cargo.toml (current deps: semver =1.0.28, serde, thiserror — add to these, do not replace)
    - rust/crates/agentlinux-core/src/classify.rs (branch order at lines 29-55; P1/P2 target; classify is #[must_use] and total via unwrap_or(false))
    - rust/crates/agentlinux-core/src/semver_shim.rs (parse_lenient, normalize_range, max_satisfying; the existing #[cfg(test)] mod tests block that P4 extends)
    - rust/crates/agentlinux-core/src/divergence.rs (resolve_latest_for lines 76-112; the three DivergenceError variants; P3 target)
    - rust/crates/agentlinux-core/src/reuse.rs (agent_decision; Decision enum; totality property target)
    - 54-RESEARCH.md section "proptest Invariants (TEST-01)" — the generator strategies (version_str, loose_version_str, range_str) and the P1-P4 property table; Pitfall 5 (P2 non-drift precondition)
  </read_first>
  <behavior>
    - P1 (classify total and deterministic): for any generated (entry, optional sentinel, optional installed), classify returns some Status without panicking, and two calls on the same input return the same Status.
    - P2 (sticky invariant): for any generated input constrained so sentinel.version equals installed (the non-drift subspace) and sentinel.sticky is true, classify returns Synced or PinnedOverride and never OverrideAhead/OverrideBehind/DriftUndeclared/NotInstalled. Encode the precondition with prop_assume or generate the two versions equal (per Pitfall 5 — sticky is only reached at branch 4 after the drift check).
    - P3 (resolve_latest_for postcondition): for any generated (entry with version_constraint drawn from range_str, published_versions list), the result is either Ok(v) where v is a member of published_versions AND max_satisfying(&[v], constraint) confirms v satisfies the range, OR an Err of one of the three DivergenceError variants; never a panic and never an Ok that fails the satisfies re-check.
    - P4 (semver_shim totality): (a) normalize_range is idempotent — normalize_range of normalize_range of x equals normalize_range of x; (b) parse_lenient on loose_version_str is Ok or a typed Err, never a panic; (c) max_satisfying on (vs, range_str) is Ok or typed Err, never a panic, and any returned version is a member of vs and satisfies the range.
    - reuse totality: for any (id, status, detected_path, canonical, gsd_system_path) strings, agent_decision returns some Decision without panicking (the fn is branch-total).
  </behavior>
  <action>
    Extend agentlinux-core/Cargo.toml: add a [dev-dependencies] section with proptest = "1.11" and serde_json = "1"; add to [dependencies] schemars = { version = "1.2.2", features = ["derive"] } (staged now so Plan 02 schema-gen touches no shared manifest — the JsonSchema derive lands in Plan 02, and an unused dep does not fail the build). Keep semver = "=1.0.28" pinned exactly — do NOT bump it (the shim reproduces that version divergences per TEST-04).

    Add hand-written proptest strategy generators (dtolnay semver ships no Arbitrary/proptest support per RESEARCH). Model them on the RESEARCH "proptest Invariants" section — a full-version generator, a loose-version generator (adds v-prefix + two-part partial shapes the shim must accept), and a range generator (caret, tilde, star, and space-separated compound comparators). Place each property in a new #[cfg(test)] mod proptests next to the fn it exercises: P1/P2 in classify.rs, P3 in divergence.rs, P4 in semver_shim.rs (extending the existing test module), and the reuse totality property in reuse.rs. Write invariants the 44 unit tests cannot express (totality, determinism, idempotence, output-satisfies-or-typed-error) — do NOT re-list concrete example rows (anti-pattern per RESEARCH). For P2 encode the non-drift precondition (generate sentinel.version equal to installed, or prop_assume) so the property scopes to where sticky is actually reached. Keep PROPTEST_CASES at the default 256. Add a rust/**/mutants.out entry to .gitignore (staged here for Plan 03); do NOT gitignore proptest-regressions.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" 2>/dev/null; cargo test -p agentlinux-core 2>&1 | grep -E 'test result: ok' && cargo clippy -p agentlinux-core --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core` passes with the new proptest modules (existing 44 unit tests still green; report shows `test result: ok`).
    - `cargo clippy -p agentlinux-core --all-targets -- -D warnings` is clean.
    - `rust/crates/agentlinux-core/proptest-regressions/` exists after the first proptest run and is NOT gitignored; `.gitignore` contains a mutants.out entry (grep for `mutants.out`) and no `proptest-regressions` entry.
    - `grep -E 'proptest|serde_json|schemars' rust/crates/agentlinux-core/Cargo.toml` shows all three added.
  </acceptance_criteria>
  <done>proptest generators + P1-P4 invariant properties + a reuse totality property are green under cargo test -p agentlinux-core; Cargo.toml carries proptest/serde_json dev-deps and the schemars dep; clippy is clean; proptest-regressions is committed and mutants.out is ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: node-semver parity audit doc + golden test (TEST-04)</name>
  <files>docs/audits/v0.4.0/TEST-04-node-semver-parity.md, rust/crates/agentlinux-core/src/semver_shim.rs</files>
  <read_first>
    - 54-RESEARCH.md section "node-semver Parity Audit + Golden Test (TEST-04)" — the required audit-doc sections (case-class table) and the golden-test shape, incl. the catalog-driven case and the known scope boundary (no OR-ranges / no hyphen ranges)
    - plugin/cli/test/divergence.test.ts (the versions array and maxSatisfying verdicts — the recorded node-semver oracle)
    - plugin/cli/test/classify.test.ts (the six-state rows already transitively proven in classify.rs)
    - plugin/catalog/catalog.json (real version fields: the sole version_constraint is "^2.1"; compatibility_window uses >=X <Y compound + open-ended >=YYYY.M.D GA-date pins; pinned_version is exact semver)
    - rust/crates/agentlinux-core/src/semver_shim.rs (the fns under audit: normalize_range, parse_lenient, eq, gt, max_satisfying)
  </read_first>
  <behavior>
    - Golden case (corpus): with versions ["1.0.0","1.1.0","1.2.0","2.0.0","2.1.0"], max_satisfying yields Some("1.2.0") for "^1.0", Some("1.1.0") for "~1.1", Some("2.1.0") for "*", and None for "^9.0" — identical to the recorded node-semver verdicts in divergence.test.ts.
    - Golden case (catalog-driven): loading plugin/catalog/catalog.json, every pinned_version parse_lenient-parses without error; every version_constraint and every compatibility_window normalize_range then VersionReq-parses without error (a future catalog edit introducing an unhandled range fails this test). This test reads a file — keep it #[cfg(test)].
    - Golden case (loose shapes): parse_lenient accepts "v1.0.0" (equals "1.0.0"), "2.1" (equals "2.1.0"), and the GA-date pins "2026.2.17"/"2025.5.1"/"2026.2.4" parse as ordinary 3-part semver.
  </behavior>
  <action>
    Write docs/audits/v0.4.0/TEST-04-node-semver-parity.md formalizing the parity: a case-class table (caret, compound-space, compound-open-ended, exact pin, GA-date pin, v-prefix, partial, prerelease-vs-non-pre range, zero-match, empty list, valid()) each with the node-semver oracle verdict, the dtolnay semver=1.0.28 raw behavior, and how the shim reconciles it. State the KNOWN SCOPE BOUNDARY explicitly: the catalog uses no OR-ranges and no hyphen ranges, so those node-semver features are documented as untested/unsupported — a future catalog adding them must extend TEST-04. Note node-semver stays uninstalled by design; the oracle is the committed TS corpora, with a one-off node -e require semver invocation in a throwaway container as the documented tiebreaker if a verdict is ever disputed.

    Add a golden test module in semver_shim.rs (#[cfg(test)]): (1) seed the corpus table verbatim from divergence.test.ts:133 verdicts; (2) add a catalog-driven case that reads plugin/catalog/catalog.json (via a CARGO_MANIFEST_DIR-relative path + serde_json), iterates every entry, and asserts each pinned_version parse_lenient-parses and each version_constraint/compatibility_window normalize_range then parses; (3) assert the loose shapes (v-prefix, two-part partial, GA-date pins). Route every comparison through semver_shim (never semver:: directly). Name the golden test fns so a `cargo test -p agentlinux-core parity` filter selects them.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" 2>/dev/null; cargo test -p agentlinux-core parity 2>&1 | grep -E 'test result: ok' && test -f ../docs/audits/v0.4.0/TEST-04-node-semver-parity.md && grep -v '^#' ../docs/audits/v0.4.0/TEST-04-node-semver-parity.md | grep -ciE 'hyphen|OR-range|scope boundary'</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core parity` passes (report shows `test result: ok`); the corpus, catalog-driven, and loose-shape golden fns all run under the `parity` filter.
    - `docs/audits/v0.4.0/TEST-04-node-semver-parity.md` exists and its body (comments stripped) mentions the scope boundary (hyphen ranges / OR-ranges documented as untested).
    - The catalog-driven case reads the real plugin/catalog/catalog.json (grep the test source for `catalog.json`).
    - Full crate suite still green: `cargo test -p agentlinux-core` shows `test result: ok`.
  </acceptance_criteria>
  <done>The TEST-04 audit doc enumerates every catalog range/prerelease case with node-semver oracle verdicts and the documented scope boundary; the golden test (corpus + catalog-driven + loose shapes) is green and would fail if a future catalog edit introduced an unhandled range.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test corpus -> pure fns | Generated/loose version strings cross into classify/semver_shim; the property tests assert no untrusted string can panic (T-53-01 becomes machine-checked). No production trust boundary is added — this plan is test-infra only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-01 | Denial of Service | semver_shim on malformed input | low | mitigate | P4 proves parse_lenient / normalize_range / max_satisfying never panic on generated loose inputs (the no-panic contract, threat T-53-01, is now property-checked, not just unit-checked). |
| T-54-02 | Tampering | proptest-regressions replay dir | low | mitigate | Commit proptest-regressions so a discovered counterexample is replayed forever; a silent deletion would re-open the gap — the committed dir + CI test run guard it. |
| T-54-SC | Tampering | cargo dev-dep installs (proptest, serde_json) | low | accept | Both are ecosystem-canonical crates re-confirmed OK in RESEARCH Package Legitimacy Audit (no [ASSUMED]/[SUS]); pinned via Cargo.lock. No blocking checkpoint required. |
</threat_model>

<verification>
- `cd rust && cargo test -p agentlinux-core` — all unit + proptest + golden tests green (`test result: ok`); the pre-existing 44 unit tests must not regress (GATE-01: no regression).
- `cargo clippy -p agentlinux-core --all-targets -- -D warnings` and `cargo fmt --all --check` clean.
- `docs/audits/v0.4.0/TEST-04-node-semver-parity.md` present with the scope-boundary statement.
- `rust/crates/agentlinux-core/proptest-regressions/` committed; `.gitignore` ignores mutants.out scratch but not proptest-regressions.
- No change to any shipped Bash / catalog / CLI behavior — this plan is pure-core test machinery only (GATE-05: nothing here can block a master hotfix; all Rust, behind the rust-job guard extended in Plan 03).
</verification>

<success_criteria>
- TEST-01: proptest properties P1-P4 (+ reuse totality) are green and assert invariants the example unit tests cannot express.
- TEST-04: the audit doc + golden test lock node-semver parity on every catalog range/prerelease case; the catalog-driven case guards against a future catalog edit introducing an unhandled range.
- Cargo.toml carries proptest + serde_json dev-deps and the schemars dep (staged for Plan 02); semver stays pinned at =1.0.28.
- GATE-01: the existing 44 unit tests + shipped ajv fixtures remain green (no regression, no newly-skipped test).
</success_criteria>

<output>
Create `.planning/phases/54-testing-bedrock/54-01-SUMMARY.md` when done.
</output>
