---
phase: 58-distribution-musl-tarball-sole-channel
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - scripts/build-release.sh
  - rust/crates/agentlinux/Cargo.toml
  - packaging/deb/postinst.sh
  - packaging/deb/.gitkeep
  - .github/workflows/release.yml
  - tests/harness/00-layout.bats
  - tests/qemu/boot.sh
  - docs/HARNESS.md
  - AGENTS.md
  - docs/decisions/006-curl-pipe-bash-plus-deb.md
requirements: [DIST-01, DIST-02, GATE-01, GATE-05]
must_haves:
  truths:
    - "`scripts/build-release.sh vX.Y.Z` builds the static musl bin (`cargo build --release --target x86_64-unknown-linux-musl`) and assembles the tarball payload `plugin/bin/agentlinux` (the bin) + `plugin/catalog/` + the ~25 Bash recipes — NO pnpm build, NO `dist/index.js`/`node_modules` in the payload — retaining the reproducible tar recipe (`--sort=name --owner=0 --group=0 --numeric-owner --mtime=@$SOURCE_DATE_EPOCH --pax-option=...delete=atime,delete=ctime | gzip -n`) and the GNU `sha256sum` sidecar (DIST-01)."
    - "Two back-to-back `build-release.sh vX.Y.Z` runs on the same HEAD produce a BYTE-IDENTICAL `.sha256` — the compiled musl bin is made reproducible via a `[profile.release] strip = true` + `-C link-arg=-Wl,--build-id=none` / `--remap-path-prefix` (or equivalent) so no build-host path or build-id leaks non-determinism into the bin (Pitfall 6)."
    - "The DIST-02 fpm/.deb path is fully removed: `packaging/deb/` deleted; the `## 11. Optional .deb` fpm block + `--no-deb` flag parse + `NO_DEB_FLAG`/`SKIP_DEB`/`DEB_SUFFIX`/`DRY_DEB_LINE` references removed from `build-release.sh`; the release.yml `Install fpm` step + `dist/agentlinux_*.deb` publish glob removed; the HRN-01 `packaging/deb` `@test` deleted; `SKIP_DEB=1 ... --no-deb` dropped from `boot.sh`; the `.deb` doc lines updated; ADR-006 flagged (not deleted)."
    - "The three-way version lock keeps reading `plugin/cli/package.json` + `plugin/catalog/catalog.json` (both retained as the parity oracle until Phase 59) AND additionally asserts `rust/crates/agentlinux/Cargo.toml` version parity — so a musl bin whose `CARGO_PKG_VERSION` diverges from the staged paths fails the build loudly (Pitfall 3, Open Q3 option a)."
    - "`tests/harness/run.sh` (HRN-01 layout) is GREEN after the `packaging/deb` `@test` is deleted; `grep -rIn` for fpm/`.deb`/`dpkg` across `scripts/ packaging/ .github/ tests/harness/ tests/qemu/` returns ZERO live references (doc-history lines in ADR-006 excepted, marked as superseded)."
  artifacts:
    - scripts/build-release.sh
    - rust/crates/agentlinux/Cargo.toml
  key_links:
    - "`build-release.sh` produces `dist/agentlinux-<tag>.tar.gz` whose payload `plugin/bin/agentlinux` IS the musl bin the Wave-2 installer execs and the Wave-2 `registry_cli.rs` stages — the tarball layout (`plugin/` prefix + bin at `plugin/bin/agentlinux`) is the contract `install.sh`'s `exe=` path (Wave 2) depends on."
    - "The `[profile.release]` strip/remap in `rust/crates/agentlinux/Cargo.toml` is the reproducibility control that keeps the tarball `.sha256` stable — the DIST-01 two-build byte-identity invariant."
    - "ADR-006's channel-(1) curl-pipe-bash + the `.sha256`-before-exec consequence SURVIVE + are reinforced; only channel-(2) `.deb` is removed. Wave 2 consumes the tarball this plan produces; Wave 3 does the flag fold + rollback lever."
---

<objective>
Wave 1 — swap the release PRODUCER + delete the fpm/.deb path. Rewire
`scripts/build-release.sh` to build the static x86_64-musl `agentlinux` bin and
ship it (plus the catalog + the ~25 Bash recipes) in the reproducible tarball,
DROP the pnpm/TS-bundle build, and DELETE every fpm/.deb reference across the
repo (the DIST-02 15-item inventory). This wave has LOW coupling to the staging
swap (Wave 2) — it only changes what the tarball CONTAINS and what CI publishes,
not how the installer hands off.

Purpose: produce a reproducible musl tarball + `.sha256` as the sole channel
(DIST-01) and remove the dead fpm path (DIST-02), with the reproducible-tar
recipe + sha256 sidecar retained verbatim.
Output: a rewired `build-release.sh` (musl payload, no fpm branch), a reproducible
`[profile.release]` in the crate Cargo.toml, a deleted `packaging/deb/`, a
deb-free `release.yml`/`boot.sh`/harness layout test, updated docs, and ADR-006
flagged for the tarball-only channel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-VALIDATION.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `scripts/build-release.sh` — the producer swap: `cargo build --release --target x86_64-unknown-linux-musl` replaces the pnpm build; tarball payload is `plugin/bin/agentlinux` (bin) + `plugin/catalog/` + recipes; the reproducible-tar recipe + sha256 sidecar are retained; the fpm/.deb branch + `--no-deb`/`SKIP_DEB` surface deleted; the version lock gains a Cargo.toml parity assertion.
- `rust/crates/agentlinux/Cargo.toml` — a `[profile.release]` (or `[profile.release-musl]`) with `strip = true` + build-id/path-remap flags for a reproducible bin.
- DELETED: `packaging/deb/postinst.sh`, `packaging/deb/.gitkeep` (the whole dir).
- `.github/workflows/release.yml` — `Install fpm` step + `dist/agentlinux_*.deb` glob + `.deb` comments removed.
- `tests/harness/00-layout.bats` — the HRN-01 `packaging/deb` `@test` deleted.
- `tests/qemu/boot.sh` — `SKIP_DEB=1 ... --no-deb` dropped from the build-release call.
- `docs/HARNESS.md`, `AGENTS.md` — `.deb`/fpm lines updated to tarball-only.
- `docs/decisions/006-curl-pipe-bash-plus-deb.md` — flagged "Superseded-in-part by Phase 58 (DIST-02): tarball-only".
Downstream: Wave 2 (`58-02`) execs + stages the `plugin/bin/agentlinux` bin this tarball ships; Wave 3 (`58-03`) folds the flags + adds the rollback lever.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: build-release.sh — musl-bin producer swap (drop pnpm, ship plugin/bin/agentlinux) + Cargo reproducible release profile + Cargo.toml version-parity assertion; two builds → identical sha256</name>
  <files>scripts/build-release.sh, rust/crates/agentlinux/Cargo.toml</files>
  <read_first>
    - scripts/build-release.sh (READ IN FULL — the tag+regex validation [:74-112]; the three-way version lock [:128-150] reading `plugin/cli/package.json` + `plugin/catalog/catalog.json`; the `--dry-run` short-circuit [:160-177] incl. the `DRY_DEB_LINE` [:161-164]; the pnpm CLI build to REMOVE [:207-225]; SOURCE_DATE_EPOCH pin [:238]; the reproducible tar recipe to RETAIN [:269-278] incl. the two pnpm `--exclude` lines [:274-275] now dead; the sha256 sidecar [:287-290]; the catalog snapshot `cp` [:300-301] + self-verify [:310-316] — KEEP; the VERSION sentinel [:329]; the fpm `## 11. Optional .deb` block to DELETE [:331-356]; the DEB_SUFFIX in the final summary [:336,:355,:361])
    - rust/crates/agentlinux/Cargo.toml (the `version = "0.3.6"` anchor [:9] the parity assertion reads; note there is NO `[profile.release]` yet — you are ADDING one)
    - rust/rust-toolchain.toml (channel 1.97.1 + `targets = ["x86_64-unknown-linux-musl"]` already pinned — reproducibility precondition A2 satisfied; the build just invokes the pinned target)
    - tests/docker/run.sh §host_build_musl [:251-275] (the working `cargo build --release --target x86_64-unknown-linux-musl` invocation shape + the bin path `rust/target/x86_64-unknown-linux-musl/release/agentlinux` — mirror the inline-build-always discipline, no stale-bin early-return)
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md §"Pattern 1" (determinism checklist + the compiled-bin strip requirement), §"Pitfall 6" (build-host-path non-determinism), §"Pitfall 3" (version lock spans package.json/catalog.json/Cargo.toml), §"Open Questions" Q1 (payload layout: keep `plugin/` prefix, bin at `plugin/bin/agentlinux`) + Q3 (version lock option a)
  </read_first>
  <action>
    Rewire `build-release.sh` so the tarball payload is the static musl bin, not
    the pnpm TS bundle. Preserve the reproducible-tar recipe + sha256 sidecar +
    catalog snapshot machinery verbatim — this is a producer swap, not a rewrite.

    1. Version lock (Open Q3 option a): after the existing `CLI_V`/`CAT_V` reads
       and the two mismatch gates ([:141-150]), ADD a third gate that reads the
       `version` field from `rust/crates/agentlinux/Cargo.toml` (grep the
       `^version = "X.Y.Z"` line under `[package]`) and fails the build (exit 1)
       if it != `BASE_VERSION`. Message parallels the existing two. Cite the
       intent: "Cargo.toml version parity — the shipped musl bin's
       CARGO_PKG_VERSION must match the tag (DIST-01/Pitfall 3)".

    2. Replace the pnpm CLI build block ([:207-225], the `cd plugin/cli` +
       corepack/pnpm install/build/prune subshell) with a musl bin build:
       `cargo build --release --target x86_64-unknown-linux-musl` run from
       `rust/` (mirror run.sh:host_build_musl — always build, no stale-bin
       early-return). Fail the build loudly if the resulting
       `rust/target/x86_64-unknown-linux-musl/release/agentlinux` is absent or
       not statically linked. Do NOT invoke pnpm/npm anywhere (DIST-01 no-Node
       producer path).

    3. Assemble the payload under a staging dir so the tarball keeps the
       `plugin/` prefix (Open Q1): stage the musl bin at
       `plugin/bin/agentlinux` (0755), the catalog at `plugin/catalog/`, and the
       ~25 Bash recipes (`plugin/catalog/agents/*/install.sh`) — copy from the
       repo `plugin/` tree, substituting the compiled bin for the CLI bundle.
       The tarball must NOT contain `plugin/cli/dist/` or `plugin/cli/node_modules/`
       (the TS bundle stops SHIPPING; `plugin/cli/` stays in the repo as the
       oracle, it is just excluded from the payload).

    4. Point the tar recipe ([:269-278]) at the staged payload root instead of
       the repo `plugin/`. Keep every determinism flag byte-for-byte
       (`--sort=name --owner=0 --group=0 --numeric-owner --mtime=@$SOURCE_DATE_EPOCH
       --pax-option=exthdr.name=%d/PaxHeaders/%f,delete=atime,delete=ctime | gzip -n`).
       Remove the two now-dead pnpm `--exclude` lines ([:274-275]).

    5. Reproducible bin (Pitfall 6): add a `[profile.release]` to
       `rust/crates/agentlinux/Cargo.toml` (or the workspace `rust/Cargo.toml`)
       with `strip = true`, and set link/rustflags to zero out the build-id and
       remap the build path (e.g. `-C link-arg=-Wl,--build-id=none` and
       `--remap-path-prefix=$PWD=.`), passed via a `RUSTFLAGS`/`CARGO_ENCODED_RUSTFLAGS`
       the build step exports, OR the equivalent `[profile.release]` keys. The
       goal is: two builds on different `$PWD` → identical bin bytes → identical
       tarball `.sha256`.

    6. Delete the fpm/.deb branch ([:331-356]) entirely; delete the `--no-deb`
       flag case + `NO_DEB_FLAG` ([:88-105]); delete `SKIP_DEB`/`DEB_SUFFIX`/
       `DRY_DEB_LINE` references ([:59,:69,:161-164,:173,:336,:355,:361]); drop
       the `.deb` lines from the usage/header ([:9,:14,:38-39,:53,:59]). Keep
       `--dry-run`. The final summary line drops `${DEB_SUFFIX}`.

    Do NOT touch `install.sh`, `registry_cli.rs`, or any bats in this task —
    those are the Wave-2 coupled swap. This task changes only the producer.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash -n scripts/build-release.sh && TAG=v0.3.6 && bash scripts/build-release.sh "$TAG" >/tmp/b1.log 2>&1 && S1=$(cut -d' ' -f1 dist/agentlinux-${TAG}.tar.gz.sha256) && bash scripts/build-release.sh "$TAG" >/tmp/b2.log 2>&1 && S2=$(cut -d' ' -f1 dist/agentlinux-${TAG}.tar.gz.sha256) && echo "sha1=$S1" && echo "sha2=$S2" && [ "$S1" = "$S2" ] && echo "REPRODUCIBLE-OK" && tar -tzf dist/agentlinux-${TAG}.tar.gz | grep -qx 'plugin/bin/agentlinux' && echo "PAYLOAD-BIN-OK" && ! tar -tzf dist/agentlinux-${TAG}.tar.gz | grep -q 'plugin/cli/dist/index.js' && echo "NO-TS-BUNDLE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Two back-to-back `build-release.sh v0.3.6` runs produce a BYTE-IDENTICAL `.sha256` (the DIST-01 reproducibility invariant) — the verify prints `REPRODUCIBLE-OK`.
    - The tarball payload contains `plugin/bin/agentlinux` (the musl bin, `PAYLOAD-BIN-OK`) and does NOT contain `plugin/cli/dist/index.js` (`NO-TS-BUNDLE-OK`) — the TS bundle stopped shipping.
    - `readelf -d rust/target/x86_64-unknown-linux-musl/release/agentlinux` reports no NEEDED dynamic libs (static) — or `ldd` reports "not a dynamic executable" (RUST-01 carried forward).
    - The producer invokes NO pnpm/npm: `grep -cE 'pnpm|npm ' scripts/build-release.sh` (filter comments `grep -v '^\s*#'`) is 0.
    - The version lock asserts Cargo.toml parity: `grep -c 'Cargo.toml' scripts/build-release.sh` > 0 and a mismatched `Cargo.toml` version fails the build (spot-check by a throwaway bump if practical, else assert the gate is present).
    - `rust/crates/agentlinux/Cargo.toml` (or `rust/Cargo.toml`) has a `[profile.release]` with `strip = true` — `grep -A3 '\[profile.release\]' rust/*/Cargo.toml rust/Cargo.toml 2>/dev/null | grep -q 'strip'`.
    - `cargo test --workspace` still passes (the profile/rustflags change must not break units); `cargo fmt --all --check` clean.
  </acceptance_criteria>
  <done>
    `build-release.sh vX.Y.Z` builds the static musl bin and ships it as
    `plugin/bin/agentlinux` in a reproducible tarball (two builds → identical
    `.sha256`), with NO pnpm build and NO TS bundle in the payload, a Cargo.toml
    version-parity gate, and a reproducible `[profile.release]`. The producer
    swap is done; the installer handoff + staging swap are Wave 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: DIST-02 deletion sweep — delete packaging/deb, deb-free release.yml/boot.sh/HRN-01 layout test, update docs, flag ADR-006; harness green + zero live fpm/.deb refs</name>
  <files>packaging/deb/postinst.sh, packaging/deb/.gitkeep, .github/workflows/release.yml, tests/harness/00-layout.bats, tests/qemu/boot.sh, docs/HARNESS.md, AGENTS.md, docs/decisions/006-curl-pipe-bash-plus-deb.md</files>
  <read_first>
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md §"The DIST-02 Deletion Inventory" (the 15-item table with every `file:line` — this task closes items 1,2,9,10,11,12,13,14,15; item 3-8 are build-release.sh, closed in Task 1)
    - packaging/deb/postinst.sh (READ — the dpkg→installer bridge being deleted; whole file)
    - .github/workflows/release.yml (the `Install fpm (optional .deb — ADR-006, Pitfall 6)` step [:275-283]; the `dist/agentlinux_*.deb` publish glob [:356] + the `.deb` comments [:17,:258-260,:325]; the build step at [:288-290] which no longer needs `setup-node` for the musl build — but leave node setup if other steps need it, do NOT expand scope)
    - tests/harness/00-layout.bats [:45-46] (`HRN-01: packaging/deb directory exists` — the whole `@test` to delete)
    - tests/qemu/boot.sh [:481-488] (the `SKIP_DEB=1 bash ... build-release.sh "$TAG" --no-deb` call + the surrounding comment about fpm being optional)
    - docs/HARNESS.md [:58,:90,:200-201,:260] (the `.deb`/fpm lines) + AGENTS.md [:27] ("optional fpm .deb wrapper")
    - docs/decisions/006-curl-pipe-bash-plus-deb.md (READ IN FULL — the two-channel decision [:16-29]; you ADD a superseded-in-part note, do NOT delete the ADR — it is the historical record; channel-(1) + the mandatory-`.sha256` consequence SURVIVE)
  </read_first>
  <action>
    Close the DIST-02 deletion inventory (items not owned by Task 1):

    - `git rm` `packaging/deb/postinst.sh` and `packaging/deb/.gitkeep`; the
      `packaging/deb/` directory disappears (item 1,2).
    - `release.yml`: delete the `Install fpm (optional .deb …)` step
      ([:275-283]); delete the `dist/agentlinux_*.deb` line from the publish
      `files:` glob ([:356]); update the `.deb` comments ([:17,:258-260,:325])
      to reflect tarball-only (item 9,10). Leave `setup-node@v4` alone unless it
      is DEMONSTRABLY unused now — do not expand scope; the musl build is
      cargo-driven but node may still be needed by other jobs.
    - `tests/harness/00-layout.bats`: delete the entire
      `@test "HRN-01: packaging/deb directory exists"` block ([:45-46]) (item 11).
      This is a HARNESS meta-test, not a behavior bats — deleting it is the
      correct response to the directory being removed (the layout contract no
      longer enumerates `packaging/deb`).
    - `tests/qemu/boot.sh`: change the build-release call ([:488]) from
      `SKIP_DEB=1 bash "${REPO_ROOT}/scripts/build-release.sh" "$TAG" --no-deb`
      to `bash "${REPO_ROOT}/scripts/build-release.sh" "$TAG"` and drop the
      now-stale fpm-optional comment ([:481-482]) (item 12). QEMU itself is
      Phase-59-gated, but the source edit belongs here to keep the flag surface
      consistent (an unknown `--no-deb` would exit 64 once Task 1 removes the
      parse).
    - `docs/HARNESS.md` ([:58,:90,:200-201,:260]) + `AGENTS.md` ([:27]): update
      the `.deb`/fpm mentions to "reproducible musl tarball + `.sha256` (sole
      channel)" (item 13,14).
    - `docs/decisions/006-curl-pipe-bash-plus-deb.md`: add a status note near
      the top — "Superseded-in-part by Phase 58 (DIST-02, v0.4.0): channel (2)
      the optional fpm `.deb` is REMOVED; the sole channel is the reproducible
      musl tarball + `.sha256`. Channel (1) curl-pipe-bash + the mandatory
      `.sha256`-before-exec consequence SURVIVE and are reinforced." Do NOT
      delete the ADR (item 15).

    Comment-text discipline: the harness `@test` deletion is a positive removal,
    not a negative grep. The zero-live-refs check in <verify> greps CODE dirs
    (`scripts/ packaging/ .github/ tests/`) and excludes the ADR doc-history — no
    literal in an `<action>` body will trip a commit-time gate here.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && test ! -e packaging/deb && echo "DEB-DIR-GONE" && ! grep -rIn -E 'fpm|--no-deb|SKIP_DEB|dpkg-deb|agentlinux_[^ ]*\.deb' scripts/ packaging/ .github/ tests/ 2>/dev/null && echo "ZERO-LIVE-DEB-REFS" && grep -q 'Superseded-in-part by Phase 58' docs/decisions/006-curl-pipe-bash-plus-deb.md && echo "ADR-FLAGGED" && bash tests/harness/run.sh 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `packaging/deb/` no longer exists (`DEB-DIR-GONE`).
    - `grep -rIn` for fpm/`--no-deb`/`SKIP_DEB`/`dpkg-deb`/`agentlinux_*.deb` across `scripts/ packaging/ .github/ tests/` returns ZERO matches (`ZERO-LIVE-DEB-REFS`) — the code surface is deb-free.
    - `tests/harness/run.sh` is GREEN (HRN-01 no longer asserts `packaging/deb`; no red).
    - ADR-006 carries the "Superseded-in-part by Phase 58" note (`ADR-FLAGGED`) and is NOT deleted; channel-(1) + the `.sha256` consequence text survive.
    - `docs/HARNESS.md` + `AGENTS.md` no longer describe an optional `.deb` channel (manual grep confirms the lines now say tarball-only).
    - The `release.yml` publish glob no longer lists `dist/agentlinux_*.deb`; the `Install fpm` step is gone.
  </acceptance_criteria>
  <done>
    Every fpm/.deb reference across the code surface is deleted (packaging/deb,
    the release.yml fpm step + deb glob, the HRN-01 layout `@test`, the boot.sh
    `--no-deb`/`SKIP_DEB`, the doc lines); `tests/harness/run.sh` is green;
    ADR-006 is flagged tarball-only (not deleted). DIST-02 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → the shipped musl bin | Reproducibility is the auditor's ability to prove the bin matches source; a non-reproducible bin (build-host path / build-id leak) breaks that proof. The strip/remap profile + the two-build byte-identity check bound this. |
| release build → `dist/*.tar.gz` + `.sha256` | The sha256 sidecar is the user-facing integrity anchor; the sidecar MUST round-trip (`sha256sum -c`). Unchanged from master — reinforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-01 | Repudiation | the compiled musl bin (reproducibility) | medium | mitigate | `[profile.release] strip = true` + build-id/path-remap so two builds on different `$PWD` yield identical bytes → identical tarball `.sha256`; Task 1 asserts the two-build byte-identity. Shifts the reproducibility risk class from pnpm-bookkeeping-timestamps (going away) to compiler-embedded build-host paths (defused here — Pitfall 6). |
| T-58-02 | Tampering | `dist/*.tar.gz.sha256` sidecar | high | mitigate | GNU `sha256sum` sidecar retained verbatim (build-release.sh sha256 step); the release.yml verify gate `sha256sum -c` round-trip SURVIVES (release.yml:294-306). The sha256-before-exec USER gate is Wave 2's install.sh (unchanged there). |
| T-58-03 | Tampering | version drift (tag vs staged paths) | medium | mitigate | The three-way lock gains a `Cargo.toml` parity assertion so a musl bin whose CARGO_PKG_VERSION diverges from the tarball's staged version paths fails the build loudly (Pitfall 3). |
| T-58-SC | Tampering | cargo/crates (no new crates) | low | accept | No new dependencies — this phase removes a packaging path + swaps a build target; the musl target + tar/gzip/sha256sum/curl are pre-existing + CI-green (58-RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Two `build-release.sh v0.3.6` runs → byte-identical `.sha256` (reproducibility); payload has `plugin/bin/agentlinux`, no `plugin/cli/dist/index.js`; bin is static.
- Producer invokes no pnpm/npm; version lock asserts Cargo.toml parity; `[profile.release] strip = true` present.
- `packaging/deb/` deleted; zero live fpm/.deb refs across `scripts/ packaging/ .github/ tests/`; `tests/harness/run.sh` green; ADR-006 flagged (not deleted).
- `cargo test --workspace` green; `cargo fmt --all --check` clean; `agentlinux-core` untouched.
- master's Bash+TS distribution is unchanged on `master` (this branch only — GATE-05); `plugin/cli/` retained in-repo as the parity oracle.
</verification>

<success_criteria>
- `build-release.sh` produces a reproducible x86_64-musl static tarball + `.sha256` as the sole channel, with the TS bundle no longer shipped (DIST-01 producer half).
- The fpm/.deb path is fully removed across code + docs; ADR-006 flagged; harness green (DIST-02).
- The reproducible-tar recipe + sha256 sidecar are retained verbatim; the compiled bin is made reproducible via strip/remap.
- master shippable; `agentlinux-core` pure + untouched; `plugin/cli/` retained as the oracle (GATE-05).
</success_criteria>

<output>
Create `.planning/phases/58-distribution-musl-tarball-sole-channel/58-01-SUMMARY.md` when done
</output>
