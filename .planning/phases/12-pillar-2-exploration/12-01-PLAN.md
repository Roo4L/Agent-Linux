---
phase: 12-pillar-2-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/exploration/PILLAR-2-NOTES.md
  - .planning/phases/12-pillar-2-exploration/12-AUDIT.md
autonomous: true
requirements: [EXPL-01]
must_haves:
  truths:
    - "docs/exploration/PILLAR-2-NOTES.md exists at exactly that path"
    - "File body is between 2 KB (2048 bytes) and 12 KB (12288 bytes)"
    - "File ends with a `## Decision summary` section (literal heading) — Phase 14's grep anchor"
    - "Decision summary names the pillar, lists ≥2 table-stakes, ≥1 differentiator (or `(none — intentional)`), ≥2 non-goals, Today / Direction content seed"
    - "Decision summary contains the literal string `next-milestone`"
    - "Body cites ≥5 distinct named references matching `grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\\^k|time-to-productive|SWE-bench|Helicone|Langfuse)'`"
    - "Direction subsection uses forward-looking voice (subject = 'we' / 'our roadmap' / explicit milestone tag) — never 'AgentLinux + present-tense verb' for unshipped behaviour"
    - "Phase-close audit `12-AUDIT.md` exists, cites file path + Decision summary line range + full grep transcripts, emits GREEN gate"
  artifacts:
    - path: "docs/exploration/PILLAR-2-NOTES.md"
      provides: "Pillar 2 exploration verdict + Decision summary section authoritative for Phase 14"
      contains: "## Decision summary"
    - path: ".planning/phases/12-pillar-2-exploration/12-AUDIT.md"
      provides: "Phase-close audit with grep transcripts + GREEN gate verdict"
      contains: "GREEN"
  key_links:
    - from: "docs/exploration/PILLAR-2-NOTES.md"
      to: ".planning/phases/12-pillar-2-exploration/12-CONTEXT.md"
      via: "decisions block lifted into doc body (T-1/T-2, D-1/D-2/D-3, NG-1..NG-4, Today/Direction)"
      pattern: "(T-1|T-2|D-1|D-2|D-3|NG-1|NG-2|NG-3|NG-4|next-milestone)"
    - from: ".planning/phases/12-pillar-2-exploration/12-AUDIT.md"
      to: "docs/exploration/PILLAR-2-NOTES.md"
      via: "grep transcripts + line-range citation of Decision summary"
      pattern: "PILLAR-2-NOTES.md"
---

<objective>
Author the canonical pillar 2 exploration verdict (`docs/exploration/PILLAR-2-NOTES.md`) and the
phase-close audit (`12-AUDIT.md`) for the EXPL-01 gate.

The exploration doc must lift the locked decisions from `12-CONTEXT.md` into a published verdict
that Phase 14 can grep-anchor and lift verbatim into `docs/STRATEGY.md` Pillar 2 — without
re-deciding pillar substance. The audit must cite file path + line range of the Decision summary
section + full grep transcripts and emit GREEN.

Purpose: Decide and publish what AgentLinux's pillar 2 actually commits to so the strategy doc
authoring phase (14) does not re-open these decisions and the website refresh phase (15) has a
stable pillar-2 card source.

Output: Two new files —
- `docs/exploration/PILLAR-2-NOTES.md` (≥2 KB, ≤12 KB, ends with `## Decision summary`)
- `.planning/phases/12-pillar-2-exploration/12-AUDIT.md` (cites EXPL-01 evidence, GREEN gate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-pillar-2-exploration/12-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@docs/STABILITY-MODEL.md

<voice_rule>
**Voice rule (PITFALLS.md, applied to the Direction subsection of the Decision summary):**

- Every sentence about an UNSHIPPED behaviour MUST have grammatical subject = "we" / "our
  roadmap" / explicit milestone tag (e.g. "the v0.6+ Benchmarks milestone", "in the next
  milestone"). NEVER subject = "AgentLinux" + present-tense verb for unshipped behaviour.
- Sentences about SHIPPED behaviour (Today subsection) may use "AgentLinux <verb>" delivered-fact
  voice with citations to v0.3.0 ADRs / bats tests / release artifacts.
- Forbidden patterns in the Direction block: "AgentLinux holds...", "AgentLinux provides...",
  "AgentLinux benchmarks...", "AgentLinux measures...", "AgentLinux protects...".
- Required patterns in the Direction block: "we will hold...", "our roadmap commits to...",
  "the v0.6+ milestone ships...", "in the next milestone we...".

This is the structural defense against aspirational drift — the single most dangerous v0.3.3
pattern. Phase 14's STRAT-11 grep gate runs against `docs/STRATEGY.md`, but Phase 14 lifts this
doc verbatim, so applying the rule here prevents drift from propagating downstream.
</voice_rule>

<grep_tokens>
**EXPL-01 success-criterion 2 — distinct named-reference grep tokens (≥5 required):**

The body of `PILLAR-2-NOTES.md` MUST contain literal text matching at least 5 distinct tokens
from this set:

```
terminal-bench
Multi-Docker-Eval
tau-bench
pass^k
time-to-productive
SWE-bench
Helicone
Langfuse
```

The CONTEXT.md hard-reframe handles this elegantly: each token is cited in a "Considered and
rejected — agent-focused benchmarks" subsection of the body, where the doc honestly documents
that these were the research raw material and explains why each was rejected as pillar-2
substance ("AgentLinux is infrastructure, not an agent product"). This satisfies the gate (≥5
distinct hits, easily) while honoring Pitfall #13's rejected-alternatives discipline.

Note: the regex literal is `tau-bench` (Greek τ rendered as `tau` per the success criterion's
ASCII regex), and `pass^k` is the literal caret-k notation from Sierra τ-bench.
</grep_tokens>

<doc_structure>
**Recommended structure for `docs/exploration/PILLAR-2-NOTES.md` (Claude's discretion on prose
voice + section ordering, but the section anchors below are NOT discretionary):**

```markdown
# Pillar 2 Exploration — Stability + Time-to-Productive

> Phase 12 verdict. Phase 14 lifts the `## Decision summary` section verbatim into
> `docs/STRATEGY.md` Pillar 2.

## Framing

[1–2 paragraphs. The hard-reframe paragraph: AgentLinux is *infrastructure*, not an agent
product. We provision the environment in which agents run. We do not run, score, or compare
agents. Therefore agent-focused benchmarks measure the wrong thing for our pillar.]

## What pillar 2 commits to

[Brief intro to T-1, T-2, D-1, D-2, D-3 as the load-bearing pillar substance. Forward-pointer to
the Decision summary at the bottom for the canonical list.]

### Table-stakes (already shipped)

[T-1 — AGT-02 zero-EACCES self-update. Cite the AGT-02 bats test + claude-code/install.sh
recipe. Delivered-fact voice OK here — this is shipped.]

[T-2 — ADR-011 stability model. Cite `docs/STABILITY-MODEL.md` + ADR-011 + TST-08 4-gate release
pipeline + the curated combo tested in QEMU on Ubuntu 22.04 + 24.04. Delivered-fact voice OK.]

### Differentiators (forward-looking — voice rule applies)

[D-1 — Compat-guarded default version set. Forward-looking voice ("we will hold...", "our
roadmap commits to..."). Phrase the falsifiable position: when an upstream package update breaks
the curated combo, the default version set holds at last-known-good; we monitor for the upstream
fix, verify compatibility on the new combo via CI, and roll forward only when a verified-
compatible combo emerges.]

[D-2 — Preset framework: `bare` / `must-haves` / `optimum`. Forward-looking voice. Name RTK as
the canonical `optimum` example. Note the `bare` preset preserves the ADR-003 no-default-agents
invariant. v0.3.3 commits to the *concept*; specific contents lock when the framework ships.]

[D-3 — Profile framework: orthogonal to presets. Forward-looking voice. Name `web-development`
(playwright-cli + browser deps) as the canonical example. Composable: `agentlinux install
--preset optimum --profile web-development`. v0.3.3 commits to the *shape*; specific profile
list locks when the framework ships.]

## Considered and rejected — agent-focused benchmarks

[The hard-reframe rejection rationale, applied to each named research-raw-material item. THIS
SUBSECTION IS WHERE THE GREP TOKENS LIVE — at minimum 5 distinct tokens from the EXPL-01 set
must appear in literal form here. Recommended approach: dedicate one paragraph or one bullet
per suite, naming the suite literally and giving its rejection reason.]

- **terminal-bench** — measures agents operating real CLI environments. Closest analog to
  "agent runs on top of AgentLinux", but still measures the agent, not the infrastructure.
  Considered as a possible measurement layer for `time-to-productive`; rejected as pillar
  substance because the credible measurable claims AgentLinux can make are not benchmark scores.
- **Multi-Docker-Eval** — env-build efficiency reporting (token + wall-clock + image size).
  Methodologically the closest precedent to "what AgentLinux would even measure if it measured
  anything" — but AgentLinux is not a build product; we provision agent-user environments, not
  Docker images.
- **tau-bench / `pass^k`** — Sierra Research's reliability-first metric (vs `pass@k`). Cited as
  landscape because `pass^k` is the right metric *if* a stability-pillar product ever publishes
  reliability scores. v0.3.3 commits to no such publication; v0.6+ may revisit.
- **SWE-bench Verified / Live / Aider polyglot** — the field's reference benchmarks. Cited as
  the dominant landscape AgentLinux is *not* trying to climb. Per the time-to-productive vs
  SWE-bench-Verified honesty rule from research, AgentLinux does not claim to change Claude's
  SWE-bench Verified score; we change time-to-productive.
- **Helicone / Langfuse** — agent observability vendors. Remain opt-in catalog entries (per
  ADR-003 no-default-agents); not pillar commitments. AgentLinux does not bundle, brand, or
  compete on observability.

[The literal tokens to ensure are present: `terminal-bench`, `Multi-Docker-Eval`, `tau-bench`,
`pass^k`, `time-to-productive`, `SWE-bench`, `Helicone`, `Langfuse`. The body above hits all 8;
the gate requires ≥5 distinct.]

## Decision summary

**Pillar name:** Stability + time-to-productive.

**Priority tag:** `next-milestone` (locked per user direction at milestone-open 2026-05-09;
reaffirmed in this exploration 2026-05-10).

**Table-stakes (already shipped, delivered-fact voice):**

- AGT-02 zero-EACCES self-update: the curated `claude` self-updates against the live Anthropic
  CDN with zero EACCES and zero sudo prompts. Release-gated via TST-08; AGT-02 bats test is the
  evidence.
- ADR-011 stability model: `pinned_version` per catalog agent, curated combo, TST-08 4-gate
  release pipeline (pre-commit → docker matrix → QEMU matrix → pinned-combo).

**Differentiators (forward-looking, voice rule applies):**

- D-1 — Compat-guarded default version set. We hold the default version set at last-known-good
  when upstream breaks a curated combo, and our roadmap commits to rolling forward only after a
  verified-compatible upstream fix lands in CI.
- D-2 — Preset framework. Our roadmap ships three install-time presets (`bare` / `must-haves` /
  `optimum`); RTK is the canonical `optimum` example; `bare` preserves the ADR-003
  no-default-agents invariant.
- D-3 — Profile framework. Our roadmap ships orthogonal use-case profiles
  (e.g. `web-development` = playwright-cli + browser deps), composable with presets.

**Explicit non-goals (≥2):**

- NG-1 — Not running, scoring, or comparing agents. Agent-focused benchmarks measure the wrong
  thing for our pillar; AgentLinux is infrastructure.
- NG-2 — Not maintaining backports, forks, or downstream patches of upstream packages. We pin,
  hold, and wait for upstream fixes. We do not carry patches downstream.
- NG-3 — Not publishing per-model performance scores.
- NG-4 — Not becoming an agent observability product. Helicone / Langfuse remain opt-in catalog
  entries, not pillar commitments.

**Today / Direction content seed:**

- **Today (v0.3.0 reality, delivered-fact voice):** AGT-02 zero-EACCES self-update release-gate
  green; ADR-011 `pinned_version` + TST-08 4-gate release pipeline; curated combo (claude-code +
  gsd + playwright + agentlinux-cli) tested in QEMU on Ubuntu 22.04 + 24.04 every release. No
  preset framework yet; no profile framework yet; no formal compat-guarded update flow beyond
  TST-08 manual gate.
- **Direction (next-milestone, forward-looking voice):** Our roadmap commits to a preset
  framework (`bare` / `must-haves` / `optimum`), a profile framework (orthogonal use-case
  bundles), and a compat-guarded update flow (we hold the default set on upstream breakage and
  roll forward only after a CI-verified fix). The `next-milestone` tag is reaffirmed.
```

**Section ordering and prose voice are Claude's discretion, BUT the following anchors are
NOT discretionary:**

- Title `# Pillar 2 Exploration — Stability + Time-to-Productive` (or equivalent H1).
- A trailing `## Decision summary` section (literal heading, Phase 14's grep anchor).
- The Decision summary section MUST contain: the literal text `next-milestone`; ≥2 table-stakes
  bullets; ≥1 differentiator bullet (or the literal text `(none — intentional)`); ≥2 non-goal
  bullets; a Today / Direction content seed (or two clearly named subsections).
- Body must contain ≥5 distinct grep tokens from the EXPL-01 set.
- File body size between 2 KB (2048 bytes) and 12 KB (12288 bytes).
</doc_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/exploration/PILLAR-2-NOTES.md from CONTEXT.md decisions</name>
  <files>docs/exploration/PILLAR-2-NOTES.md</files>
  <read_first>
    - .planning/phases/12-pillar-2-exploration/12-CONTEXT.md (the entire file — `<decisions>` block is the substance to lift; `<specifics>` block carries the RTK + web-development + compat-guarded-update phrasing; `<deferred>` block tells you what NOT to commit to)
    - .planning/research/SUMMARY.md §0 (scope correction) and §4 (pillar-2 raw material) for grep-token landscape
    - .planning/research/PITFALLS.md (voice rule + Pitfall #13 rejected-alternatives discipline)
    - docs/STABILITY-MODEL.md (T-2 citation target — read so the in-doc cross-link is accurate)
  </read_first>
  <action>
    Author `docs/exploration/PILLAR-2-NOTES.md` per the structure in the plan's `<doc_structure>`
    block. Lift CONTEXT.md's locked decisions verbatim where they are content (T-1/T-2 table-
    stakes, D-1/D-2/D-3 differentiators, NG-1/NG-2/NG-3/NG-4 non-goals, Today/Direction seeds,
    `next-milestone` priority tag). Apply the voice rule (`<voice_rule>`) rigorously to the
    Direction subsection of the Decision summary — every claim about unshipped behaviour gets
    subject "we" / "our roadmap" / explicit milestone tag, never "AgentLinux + present-tense
    verb".

    Specifically (per CONTEXT.md `<decisions>`):
    - **Framing:** Open with the hard-reframe — AgentLinux is infrastructure, not an agent
      product. The benchmark suites stay in the body as **considered and rejected** raw material
      (per CONTEXT.md `<decisions>` "Pillar Framing — Hard Reframe vs Research SUMMARY §4").
    - **Table-stakes (T-1, T-2):** Lift from CONTEXT.md `<decisions>` "Table-Stakes Commitments"
      section. T-1 cites AGT-02 bats test + `plugin/catalog/agents/claude-code/install.sh`
      recipe; T-2 cites `docs/STABILITY-MODEL.md` + ADR-011 + TST-08.
    - **Differentiators (D-1, D-2, D-3):** Lift from CONTEXT.md `<decisions>` "Differentiator
      Commitments" section. D-2 names RTK as canonical `optimum` example. D-3 names
      `web-development` (playwright-cli + browser deps) as canonical profile example. D-1
      phrasing per CONTEXT.md `<specifics>`: "AgentLinux holds the default version set at last-
      known-good when upstream breaks a curated combo, and rolls forward only after a
      verified-compatible upstream fix lands in CI" — REWRITE to forward-looking voice for the
      Direction block (subject = "we" / "our roadmap"); the phrase "AgentLinux holds..." is
      acceptable in the framing prose if it describes the *position* rather than a shipped
      behaviour, but the safer path is "we will hold...".
    - **Non-goals (NG-1, NG-2, NG-3, NG-4):** Lift from CONTEXT.md `<decisions>` "Explicit
      Non-Goals" section. NG-3 is a stronger restatement of NG-1's specific case — keep both
      (CONTEXT.md leaves both in deliberately).
    - **Today / Direction:** Lift from CONTEXT.md `<decisions>` "Today / Direction Content
      Seeds" section. Today block uses delivered-fact voice (citations to AGT-02, TST-08,
      ADR-011, QEMU/Docker matrix). Direction block uses forward-looking voice. The literal
      string `next-milestone` MUST appear in the Decision summary.
    - **Considered and rejected (grep-token subsection):** Cite each of {terminal-bench,
      Multi-Docker-Eval, tau-bench, pass^k, time-to-productive, SWE-bench, Helicone, Langfuse}
      with a one-line rejection rationale per the EXPL-01 grep-gate citation strategy in
      CONTEXT.md `<decisions>`. Body must hit ≥5 distinct tokens — recommend hitting all 8 for
      safety margin.
    - **Decision summary anchor:** Trailing section heading is exactly `## Decision summary`
      (Phase 14's grep anchor — exact match required).

    File-size constraint: aim for 4–8 KB body (substantive but not unbounded). Hard bounds are
    2 KB ≤ size ≤ 12 KB.

    Forbidden content (per CONTEXT.md `<deferred>` and the scope_reduction_prohibition rule):
    - Do NOT lock specific `must-haves` contents beyond illustrative Claude Code + Codex naming.
    - Do NOT lock specific `optimum` contents beyond RTK as canonical example.
    - Do NOT lock specific profile contents beyond `web-development` as canonical example.
    - Do NOT decide pillar 3 substance (Phase 13 owns that).
    - Do NOT use phrases like "v1", "v2", "simplified version", "static for now", "placeholder",
      "future enhancement", "basic version", "minimal implementation" anywhere in the doc.
    - Do NOT introduce content that contradicts CONTEXT.md's locked decisions.

    Per CONTEXT.md `<decisions>` "Claude's Discretion": section ordering, prose voice, and
    whether the "Considered and rejected" subsection cites suites as paragraphs or as a list
    are at Claude's discretion as long as the grep gate hits ≥5 distinct tokens. Optional T-3
    (default-set compat verification as additional table-stakes) is recommended OMITTED — D-1
    carries the position more cleanly as a differentiator (per CONTEXT.md).
  </action>
  <verify>
    <automated>test -f docs/exploration/PILLAR-2-NOTES.md &amp;&amp; bytes=$(wc -c &lt; docs/exploration/PILLAR-2-NOTES.md) &amp;&amp; [ "$bytes" -ge 2048 ] &amp;&amp; [ "$bytes" -le 12288 ] &amp;&amp; echo "size OK ($bytes bytes)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/exploration/PILLAR-2-NOTES.md` returns 0 (file exists)
    - `wc -c docs/exploration/PILLAR-2-NOTES.md` returns a value in `[2048, 12288]` (between 2 KB and 12 KB)
    - `grep -c '^## Decision summary$' docs/exploration/PILLAR-2-NOTES.md` returns exactly `1`
    - `tail -1 docs/exploration/PILLAR-2-NOTES.md` is non-empty (file ends cleanly, not with a stray `## Decision summary` heading and no body)
    - `grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' docs/exploration/PILLAR-2-NOTES.md | grep -oE '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' | sort -u | wc -l` returns a value `≥ 5` (≥5 distinct grep tokens)
    - In the section starting at the `## Decision summary` heading and continuing to end-of-file: `grep -F 'next-milestone'` returns `≥ 1` hit (the literal `next-milestone` string is present in the Decision summary section)
    - In that same Decision summary section: at least 2 lines name table-stakes commitments (lines containing `T-1` or `AGT-02` or `T-2` or `ADR-011`); at least 1 line names a differentiator (lines containing `D-1` or `D-2` or `D-3` — or the literal text `(none — intentional)` if Claude lands there, though CONTEXT.md commits to D-1/D-2/D-3); at least 2 lines name explicit non-goals (lines containing `NG-1`, `NG-2`, `NG-3`, or `NG-4`)
    - Voice-rule check: `grep -nE 'AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-2-NOTES.md` returns zero matches in any line that is part of the Direction subsection of the Decision summary (lines after a `Direction` marker and before EOF or next `##` heading). Manual scan acceptable; mechanical check is a strong filter even if it over-reports for non-Direction lines.
    - The forbidden vocabulary check returns zero hits: `grep -nE '\b(v1|v2|simplified version|static for now|hardcoded for now|placeholder|future enhancement|basic version|minimal implementation)\b' docs/exploration/PILLAR-2-NOTES.md` returns 0 matches (this regex is approximate; the human review at audit time confirms no scope-reduction language slipped in).
  </acceptance_criteria>
  <done>
    File `docs/exploration/PILLAR-2-NOTES.md` exists, body is 2–12 KB, ends with `## Decision
    summary` section that names the pillar + ≥2 table-stakes + ≥1 differentiator + ≥2 non-goals
    + Today/Direction seed + literal `next-milestone`. Body cites ≥5 distinct EXPL-01 grep
    tokens. Direction subsection uses forward-looking voice. No scope-reduction vocabulary
    leaked in. Ready for grep-gate verification in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all EXPL-01 grep gates locally and capture transcripts</name>
  <files>docs/exploration/PILLAR-2-NOTES.md</files>
  <read_first>
    - docs/exploration/PILLAR-2-NOTES.md (the file just authored — read it end-to-end to find the line range of the `## Decision summary` section for the audit's line-range citation in Task 3)
    - .planning/REQUIREMENTS.md (EXPL-01 success criteria 1–5 — the gates Task 2 verifies)
    - .planning/ROADMAP.md (Phase 12 "Success Criteria" block — same five criteria, in the form Phase-close audit will quote)
  </read_first>
  <action>
    Run each EXPL-01 gate command literally and capture both the command and its output. If any
    gate fails, return to Task 1, edit the doc to fix the failure, and re-run. Do NOT proceed to
    Task 3 until every gate passes. Capture the following five gate transcripts (these become
    Task 3's audit evidence):

    1. **Gate 1 — file exists at exact path + size in [2 KB, 12 KB]:**
       ```
       test -f docs/exploration/PILLAR-2-NOTES.md &amp;&amp; echo "EXISTS"
       wc -c docs/exploration/PILLAR-2-NOTES.md
       ```
       Pass: `EXISTS` printed AND `wc -c` value in `[2048, 12288]`.

    2. **Gate 2 — ≥5 distinct named-reference hits:**
       ```
       grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' docs/exploration/PILLAR-2-NOTES.md \
         | grep -oE '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' \
         | sort -u
       ```
       Pass: output is ≥5 distinct lines.

    3. **Gate 3 — `## Decision summary` section is the trailing anchor and contains the required substance:**
       ```
       grep -n '^## Decision summary$' docs/exploration/PILLAR-2-NOTES.md
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-2-NOTES.md
       ```
       Pass: `grep -n` returns exactly 1 match; the awk slice contains the pillar name + ≥2
       table-stakes lines + ≥1 differentiator line + ≥2 non-goal lines + Today/Direction seed.
       Capture the line number from `grep -n` and the line number of the last non-empty line
       (`awk 'NF{ln=NR}END{print ln}' docs/exploration/PILLAR-2-NOTES.md`) — these form the
       Decision-summary line range cited in the audit.

    4. **Gate 4 — literal `next-milestone` in Decision summary:**
       ```
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-2-NOTES.md \
         | grep -F 'next-milestone'
       ```
       Pass: ≥1 hit.

    5. **Gate 5 — voice-rule sanity check on Direction subsection (advisory, hard gate is in
       Phase 14 STRAT-11):**
       ```
       grep -nE 'AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-2-NOTES.md
       ```
       Read each match; if any match falls inside the Direction subsection of Decision summary,
       return to Task 1 and rewrite to forward-looking voice. Matches outside the Direction
       subsection (e.g. in framing prose describing the *position*) are advisory only — but
       prefer rewriting to the safer forward-looking form because Phase 14 lifts this verbatim.

    Save the verbatim outputs of all five gates to a temporary buffer (the bash session) — Task
    3 quotes them into `12-AUDIT.md`. Do NOT write the audit yet; Task 3 owns that file.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; test -f docs/exploration/PILLAR-2-NOTES.md &amp;&amp; bytes=$(wc -c &lt; docs/exploration/PILLAR-2-NOTES.md) &amp;&amp; [ "$bytes" -ge 2048 ] &amp;&amp; [ "$bytes" -le 12288 ] &amp;&amp; hits=$(grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' docs/exploration/PILLAR-2-NOTES.md | grep -oE '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' | sort -u | wc -l) &amp;&amp; [ "$hits" -ge 5 ] &amp;&amp; [ "$(grep -c '^## Decision summary$' docs/exploration/PILLAR-2-NOTES.md)" = "1" ] &amp;&amp; awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-2-NOTES.md | grep -qF 'next-milestone' &amp;&amp; echo "ALL GATES GREEN ($hits distinct tokens, $bytes bytes)"</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 passes: file exists; size in `[2048, 12288]` bytes
    - Gate 2 passes: `≥ 5` distinct named-reference tokens after `sort -u`
    - Gate 3 passes: exactly 1 `## Decision summary` heading; section contains pillar name + ≥2 table-stakes + ≥1 differentiator + ≥2 non-goals + Today/Direction seed
    - Gate 4 passes: literal `next-milestone` appears at least once in the Decision summary section
    - Gate 5 reviewed: any `AgentLinux <verb>` matches in the Direction subsection were rewritten to forward-looking voice; remaining matches (if any) are in framing/Today/considered-and-rejected prose where delivered-fact voice is correct
    - Single combined `<automated>` command (above) returns `ALL GATES GREEN (...)` with exit code 0
    - The Decision summary line range is identified: start line = output of `grep -n '^## Decision summary$'`; end line = last non-empty line in file (output of `awk 'NF{ln=NR}END{print ln}'`). Both numbers captured for Task 3.
  </acceptance_criteria>
  <done>
    All five EXPL-01 gates pass locally. The `<automated>` one-liner emits `ALL GATES GREEN`.
    Decision summary line range identified. Verbatim grep transcripts captured for the audit.
    Ready for audit authoring in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author phase-close audit `12-AUDIT.md` with grep transcripts and GREEN gate verdict</name>
  <files>.planning/phases/12-pillar-2-exploration/12-AUDIT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (EXPL-01 success criterion 5 — what the audit must contain)
    - .planning/ROADMAP.md Phase 12 "Success Criteria" block #5 (audit must cite file path + line range of Decision summary + grep transcript; gate emits GREEN)
    - docs/exploration/PILLAR-2-NOTES.md (the doc just authored — needed for line-range citation)
    - .planning/phases/ (look for any existing AUDIT.md from prior milestones to match the in-repo audit template, e.g. v0.4.0 phase audits — fall back to the format below if none found)
  </read_first>
  <action>
    Author `.planning/phases/12-pillar-2-exploration/12-AUDIT.md` as the phase-close audit doc.
    The audit closes EXPL-01 with cited evidence per success-criterion 5: file path + line range
    of Decision summary + verbatim grep transcript. Gate verdict: GREEN.

    Required structure for the audit (same shape as v0.3.0 / v0.4.0 phase-close audit
    convention — file path + line range + grep transcripts as the evidence form for
    documentation-only requirements):

    ```markdown
    # Phase 12 Audit — Pillar 2 Exploration

    **Phase:** 12 — Pillar 2 Exploration
    **Closed:** [today's date in ISO 8601 — read from `date -I` or use 2026-05-09]
    **Requirement:** EXPL-01
    **Gate verdict:** GREEN

    ## Requirement EXPL-01 — evidence

    **Success criterion 1 — file exists at exact path; body in [2 KB, 12 KB]:**
    - File: `docs/exploration/PILLAR-2-NOTES.md`
    - `test -f docs/exploration/PILLAR-2-NOTES.md` → EXISTS
    - `wc -c docs/exploration/PILLAR-2-NOTES.md` → [exact byte count from Task 2]
    - Within bounds: 2048 ≤ [byte count] ≤ 12288. PASS.

    **Success criterion 2 — ≥5 distinct named-reference hits:**

    Command:
    ```
    grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' docs/exploration/PILLAR-2-NOTES.md \
      | grep -oE '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' \
      | sort -u
    ```

    Output (verbatim from Task 2 capture):
    ```
    [paste verbatim output — list of distinct tokens, one per line, sorted]
    ```

    Distinct hit count: [N] (≥5 required). PASS.

    **Success criterion 3 — `## Decision summary` section heading + required substance:**
    - File: `docs/exploration/PILLAR-2-NOTES.md`
    - Decision summary section line range: lines [start_line]–[end_line] (start = grep -n output; end = last non-empty line)
    - Section names the pillar: [pillar name from doc, e.g. "Stability + time-to-productive"]
    - Table-stakes count: [≥2]; differentiators count: [≥1]; non-goals count: [≥2]; Today/Direction content seed: present
    - PASS.

    **Success criterion 4 — literal `next-milestone` in Decision summary:**

    Command:
    ```
    awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-2-NOTES.md \
      | grep -F 'next-milestone'
    ```

    Output (verbatim):
    ```
    [paste verbatim — at least one line containing `next-milestone`]
    ```

    PASS.

    **Success criterion 5 — phase-close audit (this file) cites the above:**
    - This audit cites file path: `docs/exploration/PILLAR-2-NOTES.md`. ✓
    - This audit cites Decision summary line range: [start_line]–[end_line]. ✓
    - This audit contains the verbatim grep transcripts above. ✓
    - Gate verdict: GREEN. ✓

    ## Voice-rule advisory check (Phase 14 hard gate STRAT-11 will run on STRATEGY.md)

    Command:
    ```
    grep -nE 'AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-2-NOTES.md
    ```

    Output (verbatim from Task 2 capture):
    ```
    [paste verbatim — likely empty, or a small number of matches in delivered-fact voice in
    framing/Today prose where delivered-fact voice is correct]
    ```

    Notes: [Document each match if any: which subsection it's in (framing / Today /
    considered-and-rejected / Direction). Direction subsection matches would have been rewritten
    in Task 2 — none should remain.]

    ## Phase-close gate

    All five EXPL-01 success criteria pass with cited evidence. **Gate: GREEN.**

    Phase 12 complete. Phase 13 (Pillar 3 Candidate Exploration) may proceed.

    ## References

    - `docs/exploration/PILLAR-2-NOTES.md` — the doc this audit closes.
    - `.planning/REQUIREMENTS.md` — EXPL-01 acceptance criteria.
    - `.planning/ROADMAP.md` — Phase 12 success criteria.
    - `.planning/phases/12-pillar-2-exploration/12-CONTEXT.md` — locked decisions lifted into the doc.
    - Jira: [AL-7 — Project agenda redefinition](https://copiedwonder.atlassian.net/browse/AL-7).
    ```

    Substitute the bracketed placeholders with the verbatim outputs captured in Task 2 and the
    line numbers identified there. The audit is the phase-close evidence artifact and must be
    self-contained — a future visitor running the same greps against any future commit gets the
    same answers (the grep is the spec).

    Once the audit is written, run a final sanity check that the grep transcripts in the audit
    match what re-running the same commands today would produce (defensive — catches any drift
    introduced between Task 2 capture and audit authoring).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; test -f .planning/phases/12-pillar-2-exploration/12-AUDIT.md &amp;&amp; grep -q 'GREEN' .planning/phases/12-pillar-2-exploration/12-AUDIT.md &amp;&amp; grep -q 'docs/exploration/PILLAR-2-NOTES.md' .planning/phases/12-pillar-2-exploration/12-AUDIT.md &amp;&amp; grep -qE 'EXPL-01' .planning/phases/12-pillar-2-exploration/12-AUDIT.md &amp;&amp; grep -qE 'Decision summary' .planning/phases/12-pillar-2-exploration/12-AUDIT.md &amp;&amp; echo "AUDIT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/12-pillar-2-exploration/12-AUDIT.md` returns 0 (file exists)
    - `grep -F 'GREEN' .planning/phases/12-pillar-2-exploration/12-AUDIT.md` returns ≥1 hit (gate verdict recorded)
    - `grep -F 'docs/exploration/PILLAR-2-NOTES.md' .planning/phases/12-pillar-2-exploration/12-AUDIT.md` returns ≥1 hit (file path cited)
    - `grep -F 'EXPL-01' .planning/phases/12-pillar-2-exploration/12-AUDIT.md` returns ≥1 hit (requirement ID cited)
    - `grep -F 'Decision summary' .planning/phases/12-pillar-2-exploration/12-AUDIT.md` returns ≥1 hit (line-range citation present)
    - Audit body contains the verbatim grep transcripts for success criteria 2 and 4 (manual scan: each transcript appears as a fenced code block with the command preceding it)
    - Audit body cites the Decision summary section by line range (e.g. "lines 87–145") — the line numbers match the actual file contents at audit-write time (re-run `grep -n '^## Decision summary$' docs/exploration/PILLAR-2-NOTES.md` and `awk 'NF{ln=NR}END{print ln}' docs/exploration/PILLAR-2-NOTES.md` to confirm)
    - Final sanity re-run: re-running the gate-2 and gate-4 commands today reproduces the audit's quoted output verbatim (no drift)
  </acceptance_criteria>
  <done>
    `.planning/phases/12-pillar-2-exploration/12-AUDIT.md` exists, cites EXPL-01 + file path +
    Decision summary line range + verbatim grep transcripts for all five success criteria, emits
    GREEN gate. Phase 12 closed; Phase 13 unblocked.
  </done>
</task>

</tasks>

<verification>
**Phase-level verification (run after all 3 tasks complete):**

1. Both target files exist:
   ```
   test -f docs/exploration/PILLAR-2-NOTES.md
   test -f .planning/phases/12-pillar-2-exploration/12-AUDIT.md
   ```

2. EXPL-01 gates all pass (the canonical command set):
   ```
   # Gate 1: existence + size
   wc -c docs/exploration/PILLAR-2-NOTES.md  # expect [2048, 12288]

   # Gate 2: ≥5 distinct named-reference hits
   grep -E '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' \
     docs/exploration/PILLAR-2-NOTES.md \
     | grep -oE '(terminal-bench|Multi-Docker-Eval|tau-bench|pass\^k|time-to-productive|SWE-bench|Helicone|Langfuse)' \
     | sort -u | wc -l  # expect ≥5

   # Gate 3: exactly 1 Decision summary heading
   grep -c '^## Decision summary$' docs/exploration/PILLAR-2-NOTES.md  # expect 1

   # Gate 4: literal `next-milestone` in Decision summary
   awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-2-NOTES.md \
     | grep -cF 'next-milestone'  # expect ≥1

   # Gate 5: audit cites all of the above
   grep -E '(GREEN|EXPL-01|PILLAR-2-NOTES.md|Decision summary)' \
     .planning/phases/12-pillar-2-exploration/12-AUDIT.md  # expect ≥4 distinct hits
   ```

3. Voice-rule advisory check (informational; Phase 14 owns the hard gate):
   ```
   grep -nE 'AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' \
     docs/exploration/PILLAR-2-NOTES.md
   ```
   Any matches in the Direction subsection of the Decision summary fail the phase. Matches in
   delivered-fact voice (Today subsection, framing prose describing the position, considered-
   and-rejected subsection) are acceptable.

4. CONTEXT.md fidelity spot-check:
   ```
   # Locked decisions T-1, T-2, D-1, D-2, D-3, NG-1..NG-4 all referenced
   grep -E '(T-1|T-2|D-1|D-2|D-3|NG-1|NG-2|NG-3|NG-4)' docs/exploration/PILLAR-2-NOTES.md
   # RTK named (D-2 canonical example)
   grep -F 'RTK' docs/exploration/PILLAR-2-NOTES.md  # expect ≥1
   # web-development named (D-3 canonical example)
   grep -F 'web-development' docs/exploration/PILLAR-2-NOTES.md  # expect ≥1
   ```
</verification>

<success_criteria>
1. `docs/exploration/PILLAR-2-NOTES.md` exists at exactly that path; body is between 2 KB and 12 KB.
2. Body cites ≥5 distinct EXPL-01 grep tokens (terminal-bench / Multi-Docker-Eval / tau-bench / pass^k / time-to-productive / SWE-bench / Helicone / Langfuse).
3. File ends with a `## Decision summary` section (literal heading) that names the pillar, lists ≥2 table-stakes, ≥1 differentiator, ≥2 non-goals, Today/Direction seed.
4. Decision summary contains literal `next-milestone` priority tag.
5. Direction subsection uses forward-looking voice (subject = "we" / "our roadmap" / explicit milestone tag).
6. `.planning/phases/12-pillar-2-exploration/12-AUDIT.md` exists and cites file path + Decision summary line range + verbatim grep transcripts; emits GREEN.
7. CONTEXT.md fidelity: T-1/T-2 (table-stakes), D-1/D-2/D-3 (differentiators including RTK + web-development examples), NG-1/NG-2/NG-3/NG-4 (non-goals), `next-milestone` priority all present.
8. No scope-reduction vocabulary leaked into the doc (`v1`, `v2`, `simplified version`, `placeholder`, etc. — zero hits).
</success_criteria>

<output>
After completion, create `.planning/phases/12-pillar-2-exploration/12-01-SUMMARY.md` per the
GSD execute-plan convention. The SUMMARY records what was built, the file paths created, the
grep evidence for the EXPL-01 gate, and any decisions Claude made under the
"Claude's Discretion" portions of CONTEXT.md (section ordering, prose voice, whether
considered-and-rejected was paragraphs vs list, whether T-3 was included or omitted per
recommendation).

Phase 12 closes when 12-AUDIT.md emits GREEN; Phase 13 unblocked.
</output>
