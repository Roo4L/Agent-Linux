---
phase: 12-detection-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - plugin/lib/detect/nodejs.sh
  - plugin/lib/detect/npm_prefix.sh
  - plugin/lib/detect/agents.sh
  - tests/bats/15-detection.bats
autonomous: true
requirements:
  - DET-02
  - DET-03
  - DET-04
tags:
  - bash
  - detection
  - nodejs
  - npm
  - catalog

must_haves:
  truths:
    - "DET-02 detect::nodejs_probe enumerates Node.js installs across the 8 covered sources (NodeSource APT, distro APT, manual /usr/local/bin/node, nvm, fnm, volta, mise, asdf-node, pnpm-managed) using canonical-path file existence — never sourcing a manager's shell init"
    - "DET-02 each enumerated entry carries source, path, version, install_user_can_write_prefix bool — matching RESEARCH.md §Pattern 2 shape verbatim"
    - "DET-02 NodeSource and distro APT installs are distinguished by the dpkg-query Version-string suffix (`-1nodesource` vs not)"
    - "DET-02 a /usr/local/bin/node symlinked into a manager prefix does NOT double-count (readlink -f resolved)"
    - "DET-03 detect::npm_prefix_probe surfaces user_prefix (per-user override from ~/.npmrc) AND system_prefix (npm builtin default) AND effective_prefix (resolved) as separate fields"
    - "DET-03 the npm probe runs via as_user_login (NOT plain as_user) so a user-shell NPM_CONFIG_PREFIX export is observed correctly per RESEARCH.md §Pitfall 7"
    - "DET-03 effective_prefix ownership + mode + install-user-writability captured via stat + as_user test -w"
    - "DET-04 detect::agents_probe classifies each catalog agent (claude-code/claude, gsd/get-shit-done-cc, playwright-cli/playwright-cli) as healthy/broken/absent"
    - "DET-04 the binary-name → catalog-id mapping matches plugin/catalog/catalog.json verbatim (NOT REQUIREMENTS.md original prose, which Plan 12-01 amended)"
    - "DET-04 detect::agent_status reader returns one of {healthy, broken, absent} for every catalog agent ID"
    - "All three detector probe functions run via as_user (or as_user_login for npm) — never directly from root context, so PATH visibility matches the install user's view"
    - "tests/bats/15-detection.bats has DET-02:, DET-03:, DET-04: @tests asserting JSON structure on the post-installer fixture host"
  artifacts:
    - path: "plugin/lib/detect/nodejs.sh"
      provides: "DET-02 8-source Node.js detection (canonical-path file existence + dpkg-query for system installs)"
      min_lines: 90
      contains: "dpkg-query"
    - path: "plugin/lib/detect/npm_prefix.sh"
      provides: "DET-03 three-value npm prefix probe (user / system / effective) + ownership + writability"
      min_lines: 50
      contains: "--no-userconfig"
    - path: "plugin/lib/detect/agents.sh"
      provides: "DET-04 catalog agent classifier (binary path + version + ownership + health probe + status)"
      min_lines: 50
      contains: "playwright-cli"
    - path: "tests/bats/15-detection.bats"
      provides: "DET-02:, DET-03:, DET-04: @tests appended to the Plan 12-01 file"
  key_links:
    - from: "plugin/lib/detect/nodejs.sh"
      to: "dpkg-query -W -f='${Version}\\n' nodejs"
      via: "NodeSource vs distro APT discrimination by '-1nodesource' suffix in version string"
      pattern: "-1nodesource"
    - from: "plugin/lib/detect/nodejs.sh"
      to: "$HOME/.nvm/versions/node + $HOME/.local/share/{fnm,mise,pnpm} + $HOME/.volta/tools/image/node + $HOME/.asdf/installs/nodejs"
      via: "find -maxdepth N -name node -type f (canonical-path file existence)"
      pattern: "find.*-name node"
    - from: "plugin/lib/detect/npm_prefix.sh"
      to: "as_user_login agent npm config get prefix"
      via: "login-shell invocation observes NPM_CONFIG_PREFIX user-shell exports per RESEARCH.md §Pitfall 7"
      pattern: "as_user_login"
    - from: "plugin/lib/detect/agents.sh"
      to: "as_user $user command -v $binary"
      via: "binary-on-PATH lookup AS the install user, not as root (RESEARCH.md §Pitfall 4)"
      pattern: "as_user.*command -v"
    - from: "plugin/lib/detect/agents.sh"
      to: "plugin/catalog/catalog.json (claude-code → claude, gsd → get-shit-done-cc, playwright-cli → playwright-cli)"
      via: "DETECT_AGENT_BINARIES associative array with the verbatim mapping"
      pattern: "DETECT_AGENT_BINARIES"
---

<objective>
Fill the three detector stubs Plan 12-01 placeholdered: DET-02 (8-source
Node.js discovery), DET-03 (three-value npm prefix probe), DET-04 (catalog
agents classified healthy/broken/absent). Append the corresponding bats @tests
to the existing tests/bats/15-detection.bats so the green/red signal expands
without losing Plan 12-01's coverage.

Purpose: The Phase 13 → Phase 14 brownfield-aware install needs accurate
classification of Node.js installs (REUSE-02), npm prefix ownership
(REMEDIATE-01), and catalog agent health (REUSE-03 + REMEDIATE-04). This plan
ships the detector bodies that those phases consume.

Output: A `--report-only --report-format=json` invocation now emits real (not
stubbed) `nodejs[]`, `npm_prefix.{user,system,effective}_prefix`, and
`agents[]` fields. The DET-02, DET-03, DET-04 @tests in 15-detection.bats are
green against the post-install fixture host that the Docker harness provides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-detection-layer/12-CONTEXT.md
@.planning/phases/12-detection-layer/12-RESEARCH.md
@.planning/phases/12-detection-layer/12-VALIDATION.md
@.planning/phases/12-detection-layer/12-01-PLAN.md
@CLAUDE.md
@plugin/lib/log.sh
@plugin/lib/as_user.sh
@plugin/lib/detect.sh
@plugin/lib/detect/render.sh
@plugin/lib/detect/user.sh
@plugin/lib/detect/sudoers.sh
@plugin/lib/detect/nodejs.sh
@plugin/lib/detect/npm_prefix.sh
@plugin/lib/detect/agents.sh
@plugin/catalog/catalog.json
@plugin/catalog/agents/claude-code/install.sh
@plugin/catalog/agents/gsd/install.sh
@plugin/catalog/agents/playwright-cli/install.sh
@plugin/provisioner/30-nodejs.sh
@tests/bats/15-detection.bats
@tests/bats/helpers/assertions.bash
@tests/bats/helpers/detection.bash
@tests/docker/run.sh
@.claude/skills/agentlinux-installer/SKILL.md
@.claude/skills/behavior-test-contract/SKILL.md

<interfaces>
<!-- Plan 12-01 contracts that this plan implements against. -->

Detector function signatures (Plan 12-01 stubs these; Plan 12-02 fills bodies):
```bash
detect::nodejs_probe     <user> <home> <fragment_path>      # writes {nodejs: [...]} to fragment_path
detect::npm_prefix_probe <user> <fragment_path>             # writes {npm_prefix: {...}} to fragment_path
detect::agents_probe     <user> <fragment_path>             # writes {agents: [...]} to fragment_path
```

Reader function signatures (Phase 13 contract; Plan 12-02 must keep these symbols):
```bash
detect::nodejs_satisfies_pin                                # exit 0 if any Node 22 LTS is installed
detect::nodejs_prefix_writable                              # exit 0 if install user can write to ANY enumerated prefix
detect::npm_prefix_path                                     # prints DETECT_NPM_PREFIX_PATH (effective)
detect::npm_prefix_writable_by_install_user                 # exit 0 if user can write effective prefix
detect::agent_status <id>                                   # prints healthy|broken|absent (one of three)
```

JSON fragment shapes (LOCKED — RESEARCH.md §7 sample, 12-01 stubs):
```json
{"nodejs": [
  {"source": "<id>", "path": "<bin_path>", "version": "<ver>",
   "install_user_can_write_prefix": <bool>, "prefix_root": "<dir>"}
]}

{"npm_prefix": {
  "npm_present": <bool>,
  "user_prefix": "<from ~/.npmrc, may equal /usr if absent>",
  "system_prefix": "<npm builtin default>",
  "effective_prefix": "<resolved>",
  "effective_owner": "<user:group>",
  "effective_mode": "<octal>",
  "install_user_writable": <bool>,
  "prefix_declarations": <int — count of ^prefix= in ~/.npmrc>
}}

{"agents": [
  {"id": "<catalog-id>", "binary": "<binary-name>", "path": "<resolved-path-or-empty>",
   "version": "<probed-version-or-empty>", "owner": "<user:group-or-empty>",
   "status": "healthy|broken|absent"}
]}
```

Catalog binary mapping (LOCKED — read from plugin/catalog/catalog.json + plugin/catalog/agents/*/install.sh):
| catalog id      | binary on PATH       | version probe              | health probe (--help exit) | install.sh path                                |
|-----------------|----------------------|----------------------------|----------------------------|------------------------------------------------|
| claude-code     | claude               | `claude --version`         | `claude --help`            | ~/.local/bin/claude (native)                   |
| gsd             | get-shit-done-cc     | `get-shit-done-cc --help` (banner-grep) | `get-shit-done-cc --help` | ~/.npm-global/bin/get-shit-done-cc |
| playwright-cli  | playwright-cli       | `playwright-cli --version` | `playwright-cli --help`    | ~/.npm-global/bin/playwright-cli               |

`test-dummy` (catalog id) is `test_only: true` in catalog.json — DO NOT include
in the agents array (see RESEARCH.md §Pattern 4).

NodeSource version-string discriminator (VERIFIED in dev env per RESEARCH.md §2):
- NodeSource: `nodejs 22.22.2-1nodesource1` — ends in `-1nodesource<N>`.
- Distro APT: e.g. `nodejs 12.22.9~dfsg-1ubuntu3.6` — does NOT contain `-1nodesource`.

Manager canonical paths (LOCKED — RESEARCH.md §Pattern 2 table):
| Source       | Probe root (relative to install-user $HOME) | maxdepth |
|--------------|---------------------------------------------|----------|
| nvm          | .nvm/versions/node                           | 3        |
| fnm          | .local/share/fnm/node-versions               | 4        |
| volta        | .volta/tools/image/node                      | 4        |
| mise         | .local/share/mise/installs/node              | 4        |
| asdf-node    | .asdf/installs/nodejs                        | 4        |
| pnpm-managed | .local/share/pnpm/nodejs                     | 4        |

Forbidden in DET-02 probes (Q4 contract; T-12-04):
- `source $HOME/.nvm/nvm.sh`
- `eval "$(fnm env)"`
- `eval "$(mise activate bash)"`
- Any direct invocation of a manager CLI (`nvm ls`, `fnm ls`, `mise ls`, etc.)

Forbidden in DET-03 probes:
- Reading `~/.npmrc` from root context (always probe via as_user / as_user_login).
- `which npm` (use `command -v` per CLAUDE.md skill convention).

Forbidden in DET-04 probes:
- `command -v <binary>` directly from root (RESEARCH §Pitfall 4).
- Hardcoding REQUIREMENTS.md prose names (`playwright`); use the catalog mapping above.

Plan 12-01 helpers consumed by this plan's bats tests:
- tests/bats/helpers/detection.bash::snapshot_paths (already present; Plan 12-03's read-only @test consumer)
- tests/bats/helpers/assertions.bash::assert_exit_zero, __fail (existing — used by Plan 12-01 @tests)
- tests/bats/15-detection.bats:LOG=/var/log/agentlinux-install.log + INSTALLER=/opt/agentlinux-src/plugin/bin/agentlinux-install (set by Plan 12-01 in the file header — re-use these globals; do NOT re-declare)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill DET-02 detect::nodejs_probe — 8-source Node.js discovery via canonical-path file existence</name>
  <files>plugin/lib/detect/nodejs.sh, plugin/lib/detect/render.sh</files>
  <read_first>
    - plugin/lib/detect/nodejs.sh (Plan 12-01 stub; replace the stub body, keep the source-once guard + log.sh dependency check + the detect::nodejs_satisfies_pin / detect::nodejs_prefix_writable reader symbols intact)
    - plugin/lib/detect/render.sh (the DET-02 placeholder section — replace `__det_field DET-02 nodejs.section_status stub` with full per-entry rendering)
    - plugin/lib/as_user.sh (as_user signature; required for invoking the per-manager `node --version` probes as the install user)
    - plugin/provisioner/30-nodejs.sh:50-56 (NodeSource dual-gate idiom: BOTH nodesource.sources AND nodesource.list — DET-02 must mirror this)
    - .planning/phases/12-detection-layer/12-RESEARCH.md §Pattern 2 (full implementation skeleton — copy verbatim modulo the local code style), §Pitfall 1 (forbidden: source nvm.sh / eval fnm env), §Pitfall 5 (readlink -f resolution to avoid manual+manager double-count), §Pitfall 10 (NodeSource dual-gate)
    - .planning/phases/12-detection-layer/12-RESEARCH.md §Pattern 2 table (8 sources × canonical paths × maxdepth)
  </read_first>
  <behavior>
    - detect::nodejs_probe enumerates NodeSource (when dpkg version contains `-1nodesource` AND the dual-gated apt source file exists)
    - detect::nodejs_probe enumerates distro APT (dpkg nodejs without `-1nodesource`)
    - detect::nodejs_probe enumerates a manual /usr/local/bin/node real file (readlink -f equals self) — does NOT double-count when the symlink points into a manager prefix
    - detect::nodejs_probe enumerates each of nvm, fnm, volta, mise, asdf-node, pnpm-managed when their canonical roots exist under install user $HOME
    - detect::nodejs_probe outputs a JSON fragment {nodejs: [...]} where each entry has 5 fields: source, path, version, install_user_can_write_prefix (bool), prefix_root
    - detect::nodejs_satisfies_pin returns exit 0 when any enumerated entry's version string contains the major `22.` (Node 22 LTS pin)
    - detect::nodejs_prefix_writable returns exit 0 when ANY enumerated entry has install_user_can_write_prefix=true
    - render.sh DET-02 section emits one [DET-02] nodejs.${i}.{source,path,version,install_user_can_write_prefix} marker line per enumerated entry; emits absent + count=0 when count is 0
  </behavior>
  <action>
    Replace the stub body of plugin/lib/detect/nodejs.sh with the full 8-source
    implementation from RESEARCH §Pattern 2.

    **detect::nodejs_probe(user, home, fragment_path):**
    1. Initialize `entries=()` accumulator.
    2. **NodeSource APT** — `dpkg-query -W -f='${Version}\n' nodejs 2>/dev/null || true`;
       if version contains `-1nodesource` AND (`/etc/apt/sources.list.d/nodesource.sources`
       OR `/etc/apt/sources.list.d/nodesource.list` exists per Pitfall 10 dual-gate),
       append entry with source=`nodesource`, path=`/usr/bin/node`, version=that string,
       prefix_root=`/usr`.
    3. **Distro APT** — same dpkg-query string but does NOT contain `-1nodesource` →
       append entry with source=`distro_apt`.
    4. **Manual /usr/local/bin/node** — `[[ -f /usr/local/bin/node ]]` AND
       `readlink -f /usr/local/bin/node` returns `/usr/local/bin/node` (real file,
       not a symlink chain into a manager — Pitfall 5). Run `as_user "$user" /usr/local/bin/node --version`
       for the version. Append source=`manual`, prefix_root=`/usr/local`.
    5. **Per-user managers** (loop over the 6 names): for each manager root + maxdepth
       per the LOCKED table in `<interfaces>`, call a helper
       `__det_nodejs_manager <name> <root> <maxdepth> <user> entries`. Helper does:
       `[[ -d "$root" ]] || return 0`; `find "$root" -maxdepth "$maxdepth" -name node -type f 2>/dev/null`;
       per binary, run `as_user "$user" "$bin" --version` and append entry with
       source=`<name>`, prefix_root=`$(dirname "$(dirname "$bin")")`.
    6. **Helper: __det_nodejs_entry(source, bin, version, user, prefix_root)** —
       per-entry JSON construction via `jq -n --arg ... --argjson writable "$bool"
       '{source: $source, path: $path, version: $version,
       install_user_can_write_prefix: $writable, prefix_root: $prefix_root}'`.
       The writable bool is computed via `as_user "$user" test -w "$prefix_root" && writable=true || writable=false`.
       USE `--argjson writable` (not --arg) — `--argjson` parses the value as a JSON
       boolean instead of a quoted string.
    7. Final fragment: `printf '%s\n' "${entries[@]}" | jq -s '{nodejs: .}' > "$fragment_path"`
       (or `jq -n '{nodejs: []}' > "$fragment_path"` when entries is empty).
    8. Export `DETECT_NODEJS_COUNT=${#entries[@]}` and `DETECT_NODEJS_SECTION_STATUS=present` (or `absent` when 0).
       Also export per-index DETECT_NODEJS_${i}_{SOURCE,PATH,VERSION,WRITABLE,PREFIX_ROOT}
       so render.sh + Plan 12-03's render_json can read them by name.

    **Reader functions (replace the Plan 12-01 stub `return 1` bodies):**
    - `detect::nodejs_satisfies_pin` — exit 0 if any DETECT_NODEJS_${i}_VERSION contains
      `22.` as the leading major (parse the version string; accept `v22.`, `22.`).
      Walk indices 0..DETECT_NODEJS_COUNT-1; bash regex `=~ '^v?22\.'` is sufficient.
    - `detect::nodejs_prefix_writable` — exit 0 if ANY DETECT_NODEJS_${i}_WRITABLE
      equals `true`.

    **Renderer wiring (plugin/lib/detect/render.sh):** Replace the
    DET-02 placeholder line with full per-entry rendering — one
    `[DET-02] nodejs.<i>.source=...` group per entry, plus a header line:
    ```bash
    __det_section "DET-02" "Node.js Installations"
    if [[ "${DETECT_NODEJS_COUNT:-0}" -gt 0 ]]; then
      printf '%s present (%s source(s))\n' "$(__det_glyph ok)" "$DETECT_NODEJS_COUNT"
      local i=0
      for ((i = 0; i < DETECT_NODEJS_COUNT; i++)); do
        local s_var="DETECT_NODEJS_${i}_SOURCE"
        local p_var="DETECT_NODEJS_${i}_PATH"
        local v_var="DETECT_NODEJS_${i}_VERSION"
        local w_var="DETECT_NODEJS_${i}_WRITABLE"
        __det_field DET-02 "nodejs.${i}.source" "${!s_var:-}"
        __det_field DET-02 "nodejs.${i}.path" "${!p_var:-}"
        __det_field DET-02 "nodejs.${i}.version" "${!v_var:-}"
        __det_field DET-02 "nodejs.${i}.install_user_can_write_prefix" "${!w_var:-false}"
      done
    else
      printf '%s absent\n' "$(__det_glyph absent)"
      __det_field DET-02 nodejs.count 0
    fi
    ```

    Strict mode handling: per RESEARCH.md `set -euo pipefail` is inherited from the
    entrypoint. The dpkg-query / find calls that may have empty results MUST use
    `|| true` (mirror plugin/provisioner/30-nodejs.sh:209-style) so a 0-count
    install (greenfield container) does not trip set -e.

    Forbidden patterns (T-12-04 + Pitfall 1):
    - NO `source ~/.nvm/nvm.sh`, `eval "$(fnm env)"`, `eval "$(mise activate bash)"`.
    - NO direct manager CLI invocations (`nvm ls`, `fnm ls`, etc.).
    - NO write to /etc, /home, /usr/local, /opt.

    shellcheck --severity=warning + shfmt -i 2 -ci -bn must be clean after the edit.
  </action>
  <verify>
    <automated>shellcheck --severity=warning --shell=bash --external-sources plugin/lib/detect/nodejs.sh plugin/lib/detect/render.sh && shfmt -i 2 -ci -bn -d plugin/lib/detect/nodejs.sh plugin/lib/detect/render.sh && grep -F 'dpkg-query' plugin/lib/detect/nodejs.sh && grep -F '-1nodesource' plugin/lib/detect/nodejs.sh && grep -E '\-name node' plugin/lib/detect/nodejs.sh && grep -E 'nodesource\.(sources|list)' plugin/lib/detect/nodejs.sh && grep -F 'readlink -f' plugin/lib/detect/nodejs.sh && grep -F 'as_user' plugin/lib/detect/nodejs.sh && ! grep -E 'source.*\.nvm|eval.*fnm env|eval.*mise activate|nvm ls|fnm ls|mise ls' plugin/lib/detect/nodejs.sh</automated>
  </verify>
  <acceptance_criteria>
    - plugin/lib/detect/nodejs.sh contains literal substrings: `dpkg-query`, `-1nodesource`, `-name node`, `nodesource.sources`, `nodesource.list`, `readlink -f`, `as_user`
    - plugin/lib/detect/nodejs.sh defines a helper named `__det_nodejs_entry` and a manager-loop helper named `__det_nodejs_manager`
    - plugin/lib/detect/nodejs.sh probes for ALL 6 per-user manager roots: `.nvm/versions/node`, `.local/share/fnm/node-versions`, `.volta/tools/image/node`, `.local/share/mise/installs/node`, `.asdf/installs/nodejs`, `.local/share/pnpm/nodejs` (one grep per path)
    - plugin/lib/detect/nodejs.sh contains ZERO occurrences of: `source ~/.nvm`, `eval "$(fnm`, `eval "$(mise`, `nvm ls`, `fnm ls`, `mise ls`
    - detect::nodejs_satisfies_pin and detect::nodejs_prefix_writable reader functions exist and are NOT stubs (their bodies don't unconditionally `return 1` / `return 0` — they consult DETECT_NODEJS_* state)
    - shellcheck --severity=warning passes (zero warnings) on both modified files
    - shfmt -i 2 -ci -bn -d reports zero diff on both modified files
    - render.sh DET-02 section iterates DETECT_NODEJS_COUNT entries and emits per-index `nodejs.${i}.{source,path,version,install_user_can_write_prefix}` markers (verified by grep on render.sh body)
  </acceptance_criteria>
  <done>
    DET-02 enumerates Node.js across 8 sources without sourcing any manager hook. The Phase 13 contract reader detect::nodejs_satisfies_pin returns the right answer on a host that has a Node 22 LTS install (NodeSource or any manager-installed v22). The renderer emits the full per-entry structure that bats DET-02: @tests in Task 4 will assert against.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill DET-03 detect::npm_prefix_probe — three-value (user / system / effective) prefix discovery</name>
  <files>plugin/lib/detect/npm_prefix.sh, plugin/lib/detect/render.sh</files>
  <read_first>
    - plugin/lib/detect/npm_prefix.sh (Plan 12-01 stub — replace the body, keep the source-once guard + Phase 13 reader symbols)
    - plugin/lib/detect/render.sh (the DET-03 placeholder section — replace `__det_field DET-03 npm.section_status stub` with the full field set)
    - plugin/lib/as_user.sh (as_user_login uses sudo -i — login shell, sources profile; THIS plan uses as_user_login for npm probes per RESEARCH §Pitfall 7 + Open Question 2 recommendation)
    - .planning/phases/12-detection-layer/12-RESEARCH.md §Pattern 3 (full implementation skeleton — copy verbatim), §Pitfall 2 (location semantics — distinguishing user_prefix=/usr-because-empty from user_prefix=/usr-explicitly), §Pitfall 6 (count `^prefix=` declarations in ~/.npmrc), §Pitfall 7 (use as_user_login for the npm probe so user-shell NPM_CONFIG_PREFIX is observed)
    - plugin/provisioner/30-nodejs.sh:75-99 (the v0.3.0 ~agent/.npm-global setup — DET-03 reads what this provisioner writes)
  </read_first>
  <behavior>
    - detect::npm_prefix_probe early-bails with npm_present=false + null fields when as_user_login command -v npm fails
    - When npm is present, the JSON fragment carries DISTINCT user_prefix, system_prefix, and effective_prefix values (system_prefix is npm builtin via --no-userconfig + cleared NPM_CONFIG_PREFIX env)
    - The npm probe runs via as_user_login (NOT plain as_user) so user-shell NPM_CONFIG_PREFIX exports propagate
    - effective_prefix ownership (user:group), mode (octal), and install_user_writable bool are captured via stat + as_user test -w
    - prefix_declarations counts the literal `^prefix=` lines in the install user's ~/.npmrc (Pitfall 6 disambiguator)
    - detect::npm_prefix_path reader prints DETECT_NPM_PREFIX_PATH (the effective prefix string)
    - detect::npm_prefix_writable_by_install_user reader returns exit 0 only when DETECT_NPM_PREFIX_USER_WRITABLE=true
    - render.sh DET-03 section emits one [DET-03] npm.<field>=value line for each of the 7 captured fields when present, or a single absent line when npm is not installed
  </behavior>
  <action>
    Replace the stub body of plugin/lib/detect/npm_prefix.sh with the full
    three-value implementation per RESEARCH §Pattern 3. Use **as_user_login**
    (not as_user) for the npm config probes — Pitfall 7 + RESEARCH §Open
    Question 2's locked recommendation.

    **detect::npm_prefix_probe(user, fragment_path):**
    1. Early bail when npm is absent — the bash entrypoint may have invoked
       --report-only on a host that hasn't run 30-nodejs.sh yet:
       ```bash
       if ! as_user_login "$user" command -v npm >/dev/null 2>&1; then
         jq -n --arg user "$user" \
           '{npm_prefix: {npm_present: false, user_prefix: null, system_prefix: null, effective_prefix: null, effective_owner: null, effective_mode: null, install_user_writable: false, prefix_declarations: 0}}' \
           > "$fragment_path"
         export DETECT_NPM_PREFIX_PATH=""
         export DETECT_NPM_PREFIX_USER_WRITABLE=false
         export DETECT_NPM_PREFIX_SECTION_STATUS=absent
         return 0
       fi
       ```
    2. **user_prefix** — `as_user_login "$user" npm config get prefix --location=user 2>/dev/null | tr -d '[:space:]'`
       (per Pitfall 2: returns `/usr` if ~/.npmrc has no `prefix=` line; that's
       different from a user explicitly setting prefix=/usr — the
       prefix_declarations counter below disambiguates).
    3. **system_prefix** — npm builtin default. Use `--no-userconfig` to skip
       ~/.npmrc + clear NPM_CONFIG_PREFIX from env (which sudo's -E may have
       carried in):
       `as_user_login "$user" env NPM_CONFIG_PREFIX= npm config get prefix --no-userconfig 2>/dev/null | tr -d '[:space:]'`.
    4. **effective_prefix** — `as_user_login "$user" npm config get prefix 2>/dev/null | tr -d '[:space:]'`.
       This is the value the install user's interactive shell would see (because
       as_user_login sources their .profile / .bashrc — Pitfall 7 mitigation).
    5. **Ownership + mode + writability on effective_prefix** — when `[[ -d "$effective_prefix" ]]`:
       owner=`stat -c '%U:%G' "$effective_prefix"`, mode=`stat -c '%a' "$effective_prefix"`,
       user_writable via `as_user "$user" test -w "$effective_prefix"`. When the
       dir doesn't exist, set owner=`absent`, mode=``, user_writable=false.
    6. **prefix_declarations** — count `^prefix=` lines in ~/.npmrc per Pitfall 6.
       Reads the user's ~/.npmrc as root (root can read; we just don't write).
       Discover the home dir from `getent passwd "$user"` (column 6 split on `:`).
       `local home; home=$(getent passwd "$user" | cut -d: -f6)`.
       `local count=0`; if `[[ -f "$home/.npmrc" ]]` then `count=$(grep -cE '^prefix=' "$home/.npmrc" || true)`.
    7. **JSON fragment** via jq -n --arg / --argjson, exact shape from `<interfaces>`.
       Set DETECT_NPM_PREFIX_SECTION_STATUS=present.
    8. **Exports the renderer + Phase 13 readers consume:**
       - DETECT_NPM_PREFIX_PATH=$effective_prefix
       - DETECT_NPM_PREFIX_USER_WRITABLE=$user_writable (true/false string)
       - DETECT_NPM_PREFIX_USER_VALUE=$user_prefix
       - DETECT_NPM_PREFIX_SYSTEM_VALUE=$system_prefix
       - DETECT_NPM_PREFIX_EFFECTIVE_OWNER=$owner
       - DETECT_NPM_PREFIX_EFFECTIVE_MODE=$mode
       - DETECT_NPM_PREFIX_DECLARATIONS=$count

    **Reader functions (replace Plan 12-01 stubs):**
    - `detect::npm_prefix_path` — already correct (`printf '%s' "${DETECT_NPM_PREFIX_PATH:-}"`).
    - `detect::npm_prefix_writable_by_install_user` — already correct
      (`[[ "${DETECT_NPM_PREFIX_USER_WRITABLE:-false}" == "true" ]]`). Body
      was correct in Plan 12-01 stub; verify the export in step 8 still feeds it.

    **Renderer wiring (plugin/lib/detect/render.sh):** Replace the DET-03
    placeholder. When DETECT_NPM_PREFIX_SECTION_STATUS=absent, print a single
    "absent — npm not installed" line. When present, emit one
    `[DET-03] npm.<field>=value` line per:
    user_prefix, system_prefix, effective_prefix, effective_owner, effective_mode,
    install_user_writable, prefix_declarations.

    Forbidden patterns:
    - Reading ~/.npmrc via as_user_login `cat` — root can stat + grep -c directly,
      no need to round-trip through sudo for a read. (Reading is fine; writing
      would be a T-12-04 violation.)
    - `which npm` — use `command -v` per CLAUDE.md skill.
    - `npm install -g jq` if jq missing — Plan 12-01's ensure_jq covers this.

    shellcheck + shfmt clean after the edit.
  </action>
  <verify>
    <automated>shellcheck --severity=warning --shell=bash --external-sources plugin/lib/detect/npm_prefix.sh plugin/lib/detect/render.sh && shfmt -i 2 -ci -bn -d plugin/lib/detect/npm_prefix.sh plugin/lib/detect/render.sh && grep -F -- '--no-userconfig' plugin/lib/detect/npm_prefix.sh && grep -F 'as_user_login' plugin/lib/detect/npm_prefix.sh && grep -F -- '--location=user' plugin/lib/detect/npm_prefix.sh && grep -F 'prefix_declarations' plugin/lib/detect/npm_prefix.sh && grep -E "stat -c '%U:%G'" plugin/lib/detect/npm_prefix.sh && ! grep -F 'which npm' plugin/lib/detect/npm_prefix.sh && ! grep -E 'npm install' plugin/lib/detect/npm_prefix.sh</automated>
  </verify>
  <acceptance_criteria>
    - plugin/lib/detect/npm_prefix.sh contains literal substrings: `--no-userconfig`, `as_user_login`, `--location=user`, `prefix_declarations`, `stat -c '%U:%G'`, `getent passwd`
    - plugin/lib/detect/npm_prefix.sh contains ZERO occurrences of `which npm` and `npm install`
    - The npm probe uses `as_user_login` (not bare `as_user`) for every `npm config get` invocation (verified by grep: every line containing `npm config get` also contains `as_user_login` on the same line OR a few lines before it in the same statement)
    - detect::npm_prefix_path and detect::npm_prefix_writable_by_install_user reader functions exist and consult the DETECT_NPM_PREFIX_* exports (NOT stubbed)
    - shellcheck --severity=warning passes (zero warnings) on both modified files
    - shfmt -i 2 -ci -bn -d reports zero diff on both modified files
    - The JSON fragment writer uses jq -n with --arg / --argjson — never string interpolation into a printf JSON template
    - render.sh DET-03 section emits per-field `npm.<field>=value` markers for all 7 fields when DETECT_NPM_PREFIX_SECTION_STATUS=present (verified by grep on render.sh body for `npm.user_prefix`, `npm.system_prefix`, `npm.effective_prefix`, `npm.effective_owner`, `npm.effective_mode`, `npm.install_user_writable`, `npm.prefix_declarations`)
  </acceptance_criteria>
  <done>
    DET-03 surfaces the three distinct npm prefix values plus ownership + writability + declaration count per RESEARCH §Pattern 3. The Phase 13 contract readers detect::npm_prefix_path / detect::npm_prefix_writable_by_install_user return real (non-stub) values. The renderer emits per-field `[DET-03] npm.<field>=value` lines that the bats DET-03: @tests in Task 4 will grep for.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill DET-04 detect::agents_probe — catalog agent classifier (healthy/broken/absent)</name>
  <files>plugin/lib/detect/agents.sh, plugin/lib/detect/render.sh</files>
  <read_first>
    - plugin/lib/detect/agents.sh (Plan 12-01 stub — replace the body, keep source-once guard + detect::agent_status reader symbol)
    - plugin/lib/detect/render.sh (the DET-04 placeholder section — replace `__det_field DET-04 agents.section_status stub` with full per-agent rendering)
    - plugin/lib/as_user.sh (as_user signature; required for invoking `command -v <binary>` and `<binary> --version` / `<binary> --help` AS the install user — Pitfall 4 PATH visibility)
    - plugin/catalog/catalog.json (authoritative agent IDs + post_install_verify + npm_package_name; `test_only: true` agents are skipped)
    - plugin/catalog/agents/claude-code/install.sh (binary lands at ~/.local/bin/claude — native installer)
    - plugin/catalog/agents/gsd/install.sh (binary lands at ~/.npm-global/bin/get-shit-done-cc; no --version flag — banner-grep on --help)
    - plugin/catalog/agents/playwright-cli/install.sh (binary lands at ~/.npm-global/bin/playwright-cli)
    - .planning/phases/12-detection-layer/12-RESEARCH.md §Pattern 4 (full implementation skeleton — copy verbatim), §Code Examples §4 (DETECT_AGENT_BINARIES + per-agent version-flag map), §Pitfall 3 (binary-name catalog discrepancy: REQUIREMENTS.md prose vs catalog.json verbatim), §Pitfall 4 (PATH visibility — must probe AS the install user)
  </read_first>
  <behavior>
    - detect::agents_probe iterates the LOCKED catalog binary mapping (claude-code → claude, gsd → get-shit-done-cc, playwright-cli → playwright-cli) verbatim from catalog.json
    - test-dummy is NOT included in the agents array (catalog.json `test_only: true` filter)
    - For each agent, `as_user "$user" command -v <binary>` is invoked (Pitfall 4 — never plain `command -v` as root)
    - When command -v returns empty: status=absent, version="", path="", owner=""
    - When binary present: version is captured via the agent's documented probe (claude --version, get-shit-done-cc --help with banner extract, playwright-cli --version), owner via `stat -c '%U:%G' <bin_path>`, health via `as_user "$user" <binary> --help` exit 0
    - status=healthy when binary present + version probe parses something + health probe exits 0
    - status=broken when binary present BUT (version probe yields empty OR health probe non-zero exit)
    - detect::agent_status <id> reader returns one of `healthy`, `broken`, or `absent` (and ONLY one of those three strings) for every catalog agent ID
    - render.sh DET-04 section emits one [DET-04] agents.<id>.{path,version,owner,status} marker line per agent when DETECT_AGENTS_SECTION_STATUS=present
    - Probed binary stdout is NEVER eval'd, NEVER source'd, NEVER passed unquoted into a shell — only into jq via --arg
  </behavior>
  <action>
    Replace the stub body of plugin/lib/detect/agents.sh with the full
    classification implementation per RESEARCH §Pattern 4 + §Code Examples §4.

    **Top-of-file: source-once guard + log.sh dependency guard (preserve Plan 12-01 stub's guards verbatim).**

    **DETECT_AGENT_BINARIES associative array** — verbatim from RESEARCH:
    ```bash
    declare -A DETECT_AGENT_BINARIES=(
      [claude-code]=claude
      [gsd]=get-shit-done-cc
      [playwright-cli]=playwright-cli
    )
    ```
    Iteration order: keep the same three insertion-order keys that catalog.json
    lists (test-dummy excluded — its `test_only: true` flag means "not a real
    agent"). For determinism in renderer output, iterate via an explicit ordered
    list `local ids=(claude-code gsd playwright-cli)` rather than the bash
    associative array's iteration order (which is hash-bucket order on bash 5).

    **__det_agent_version_probe(id, user, binary) — emits the version string:**
    ```bash
    __det_agent_version_probe() {
      local id=$1 user=$2 binary=$3
      case "$id" in
        claude-code)
          # claude --version — semver line ("1.2.3" / "1.2.3 (something)")
          as_user "$user" "$binary" --version 2>/dev/null | head -1 | tr -d '\r' \
            | grep -Eo '[0-9]+\.[0-9]+\.[0-9]+(-[a-z0-9.-]+)?' | head -1
          ;;
        gsd)
          # get-shit-done-cc has no --version flag; the --help banner mentions GSD.
          # Per RESEARCH §Code Examples §4 + §Pattern 4: head -1 of --help output,
          # quoted into a single line. The classifier downstream treats anything
          # parseable as healthy; jq --arg quotes the bytes safely.
          as_user "$user" "$binary" --help 2>/dev/null | head -1 | tr -d '\r' | tr -d '\n'
          ;;
        playwright-cli)
          as_user "$user" "$binary" --version 2>/dev/null | head -1 | tr -d '\r' \
            | grep -Eo '[0-9]+\.[0-9]+\.[0-9]+(-[a-z0-9.-]+)?' | head -1
          ;;
      esac
    }
    ```
    NOTE: the version-extracting regex (`[0-9]+\.[0-9]+\.[0-9]+`) is the threat
    mitigation for T-12-02 — anything that doesn't match the regex returns empty,
    which downstream classifier interprets as broken (not as a malicious string
    written into the report verbatim). The gsd path is more permissive (banner
    grep) because gsd has no --version flag; jq --arg still quotes the bytes
    safely.

    **detect::agents_probe(user, fragment_path):**
    ```bash
    detect::agents_probe() {
      local user=$1 fragment_path=$2
      local entries=()
      local ids=(claude-code gsd playwright-cli)
      local id binary bin_path ver health_rc owner status

      for id in "${ids[@]}"; do
        binary=${DETECT_AGENT_BINARIES[$id]}
        bin_path=$(as_user "$user" command -v "$binary" 2>/dev/null || true)

        if [[ -z "$bin_path" ]]; then
          status=absent
          ver=""
          owner=""
        else
          ver=$(__det_agent_version_probe "$id" "$user" "$binary")
          owner=$(stat -c '%U:%G' "$bin_path" 2>/dev/null || echo "unknown")
          if as_user "$user" "$binary" --help >/dev/null 2>&1; then
            health_rc=0
          else
            health_rc=$?
          fi
          if [[ "$health_rc" -eq 0 && -n "$ver" ]]; then
            status=healthy
          else
            status=broken
          fi
        fi

        entries+=("$(jq -n \
          --arg id "$id" \
          --arg binary "$binary" \
          --arg path "$bin_path" \
          --arg version "$ver" \
          --arg owner "$owner" \
          --arg status "$status" \
          '{id: $id, binary: $binary, path: $path, version: $version, owner: $owner, status: $status}')")

        # Per-agent reader exports (Phase 13 consumes via detect::agent_status).
        local upper=${id^^}
        upper=${upper//-/_}
        export "DETECT_AGENT_${upper}_STATUS"="$status"
        export "DETECT_AGENT_${upper}_PATH"="$bin_path"
        export "DETECT_AGENT_${upper}_VERSION"="$ver"
        export "DETECT_AGENT_${upper}_OWNER"="$owner"
      done

      printf '%s\n' "${entries[@]}" | jq -s '{agents: .}' > "$fragment_path"
      export DETECT_AGENTS_SECTION_STATUS=present
      export DETECT_AGENTS_COUNT=${#entries[@]}
    }
    ```

    **detect::agent_status reader (replaces Plan 12-01 stub):**
    ```bash
    detect::agent_status() {
      local id=$1
      local upper=${id^^}
      upper=${upper//-/_}
      local var="DETECT_AGENT_${upper}_STATUS"
      printf '%s' "${!var:-absent}"
    }
    ```

    **Renderer wiring (plugin/lib/detect/render.sh):** Replace the DET-04
    placeholder. Iterate `claude-code gsd playwright-cli` and emit one block
    per agent:
    ```bash
    __det_section "DET-04" "Catalog Agents"
    if [[ "${DETECT_AGENTS_SECTION_STATUS:-stub}" == "present" ]]; then
      printf '%s present (%s agent(s))\n' "$(__det_glyph ok)" "${DETECT_AGENTS_COUNT:-0}"
      local id upper
      for id in claude-code gsd playwright-cli; do
        upper=${id^^}
        upper=${upper//-/_}
        local s_var="DETECT_AGENT_${upper}_STATUS"
        local p_var="DETECT_AGENT_${upper}_PATH"
        local v_var="DETECT_AGENT_${upper}_VERSION"
        local o_var="DETECT_AGENT_${upper}_OWNER"
        __det_field DET-04 "agents.${id}.status" "${!s_var:-absent}"
        __det_field DET-04 "agents.${id}.path" "${!p_var:-}"
        __det_field DET-04 "agents.${id}.version" "${!v_var:-}"
        __det_field DET-04 "agents.${id}.owner" "${!o_var:-}"
      done
    else
      printf '%s stub\n' "$(__det_glyph absent)"
      __det_field DET-04 agents.section_status "${DETECT_AGENTS_SECTION_STATUS:-stub}"
    fi
    ```

    Forbidden patterns (T-12-02 + Pitfall 4):
    - NO `eval` anywhere in the file (probed binary stdout into eval is the threat).
    - NO direct `command -v <binary>` from root context — every probe goes through `as_user "$user"`.
    - NO hardcoded `playwright` (without `-cli`) — that's REQUIREMENTS.md prose, not catalog truth.
    - NO `npm install` / `apt-get install` — detection is read-only.

    shellcheck + shfmt clean after the edit.
  </action>
  <verify>
    <automated>shellcheck --severity=warning --shell=bash --external-sources plugin/lib/detect/agents.sh plugin/lib/detect/render.sh && shfmt -i 2 -ci -bn -d plugin/lib/detect/agents.sh plugin/lib/detect/render.sh && grep -F 'DETECT_AGENT_BINARIES' plugin/lib/detect/agents.sh && grep -F 'as_user' plugin/lib/detect/agents.sh && grep -F 'command -v' plugin/lib/detect/agents.sh && grep -F 'playwright-cli' plugin/lib/detect/agents.sh && grep -F 'get-shit-done-cc' plugin/lib/detect/agents.sh && grep -E '\[claude-code\]=claude' plugin/lib/detect/agents.sh && ! grep -E '\beval\b' plugin/lib/detect/agents.sh && ! grep -E 'npm install|apt-get install' plugin/lib/detect/agents.sh</automated>
  </verify>
  <acceptance_criteria>
    - plugin/lib/detect/agents.sh contains literal substrings: `DETECT_AGENT_BINARIES`, `as_user`, `command -v`, `playwright-cli`, `get-shit-done-cc`, `[claude-code]=claude`
    - plugin/lib/detect/agents.sh contains ZERO occurrences of `eval`, `npm install`, `apt-get install`
    - The agent iteration list contains exactly three IDs in this order: claude-code, gsd, playwright-cli (test-dummy explicitly excluded)
    - detect::agent_status returns ONLY one of `healthy`, `broken`, or `absent` (verified by grep that the function body's printf path can produce only those three strings — i.e. no other status string is exported into DETECT_AGENT_*_STATUS)
    - All `command -v <binary>` invocations go through `as_user "$user" command -v <binary>` (Pitfall 4 — verified by grep)
    - shellcheck --severity=warning passes (zero warnings) on both modified files
    - shfmt -i 2 -ci -bn -d reports zero diff on both modified files
    - The JSON fragment writer uses jq -n with --arg exclusively for probed-binary stdout (T-12-02 mitigation)
    - render.sh DET-04 section emits per-agent `agents.<id>.{status,path,version,owner}` markers when DETECT_AGENTS_SECTION_STATUS=present (verified by grep on render.sh body)
  </acceptance_criteria>
  <done>
    DET-04 classifies each catalog agent as healthy / broken / absent per the LOCKED binary mapping (catalog.json verbatim, NOT REQUIREMENTS.md prose). The Phase 13 contract reader detect::agent_status returns the right answer for every catalog ID. The renderer emits per-agent `[DET-04] agents.<id>.<field>=value` lines that the bats DET-04: @tests in Task 4 will grep for.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Append DET-02 / DET-03 / DET-04 @tests to tests/bats/15-detection.bats</name>
  <files>tests/bats/15-detection.bats</files>
  <read_first>
    - tests/bats/15-detection.bats (Plan 12-01 created the file with DET-01:, DET-05:, DET-06: @tests + LOG/INSTALLER globals + load 'helpers/assertions' + load 'helpers/detection' — append to the END of the file; do NOT re-declare the globals; do NOT touch the existing @tests)
    - tests/bats/helpers/assertions.bash (assert_exit_zero, __fail signature)
    - tests/bats/helpers/detection.bash (snapshot_paths helper — Plan 12-01 already loaded via `load 'helpers/detection'`)
    - tests/bats/22-agent-sudo.bats (existing pattern: @test name = REQ-ID, __fail four-line diagnostic, jq -e for JSON assertions)
    - .planning/phases/12-detection-layer/12-RESEARCH.md §8 (full bats fixture template — DET-02:/DET-03:/DET-04: @test bodies)
    - .planning/phases/12-detection-layer/12-VALIDATION.md "Per-Task Verification Map" rows 12-02-01..03 (REQ-ID linkage)
    - .claude/skills/behavior-test-contract/SKILL.md (REQ-ID-in-name + REQ-ID-in-comment rule)
  </read_first>
  <behavior>
    - At least 9 new @tests are appended (3 each for DET-02, DET-03, DET-04 minimum) — every @test name starts with `DET-NN: ` and includes a `# REQ: DET-NN` comment line per the behavior-test-contract SKILL
    - DET-02 @tests assert: nodejs probe enumerates NodeSource install on the post-installer fixture host; nodejs probe enumerates a per-user manager install when the fixture creates one (e.g. `~/.nvm/versions/node/v20.10.0/bin/node`); a /usr/local/bin/node symlinked to a manager prefix does NOT double-count (readlink-f resolution)
    - DET-03 @tests assert: npm prefix surfaces the three values (user_prefix, system_prefix, effective_prefix) as DISTINCT JSON fields; the npm probe runs via as_user_login (verified indirectly — when fixture sets NPM_CONFIG_PREFIX in the install user's ~/.profile, effective_prefix reflects that value, not /usr); ownership + writability captured for the effective prefix
    - DET-04 @tests assert: claude classified healthy when present + --version exits 0; get-shit-done-cc classified healthy when --help banner is parseable; playwright-cli classified absent when binary missing from install user's PATH; classifier returns broken when binary present but health probe non-zero
    - bats parse exits 0 for the file; new @test count adds at least 9 to the pre-existing 7 from Plan 12-01
  </behavior>
  <action>
    Append the following @tests to the END of tests/bats/15-detection.bats. Do
    NOT modify the file header (load lines, LOG, INSTALLER) — Plan 12-01 set
    those. Do NOT touch the existing DET-01:/DET-05:/DET-06: @tests. Append a
    blank line first, then this block:

    ```bash

    # ---- Plan 12-02 appends: DET-02, DET-03, DET-04 @tests ----
    # Per .planning/phases/12-detection-layer/12-VALIDATION.md rows 12-02-01..03.
    # Each @test name starts with the REQ-ID (behavior-test-contract SKILL).

    @test "DET-02: nodejs probe enumerates NodeSource install on post-installer host" {
      # REQ: DET-02
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-02"
      # Post-installer host has Phase 4's NodeSource install. Expect at least one entry
      # whose source is `nodesource` and whose version contains `-1nodesource`.
      printf '%s' "$output" | jq -e '.components.nodejs // .nodejs | map(select(.source == "nodesource")) | length >= 1' >/dev/null \
        || __fail "DET-02" "at least one nodejs[] entry with source=nodesource on post-installer host" "$output" "$LOG"
    }

    @test "DET-02: nodejs probe enumerates nvm install when fixture sets ~/.nvm" {
      # REQ: DET-02
      # Fixture: drop a fake node binary at the canonical nvm path under the install
      # user's home; detection should pick it up via canonical-path file existence
      # WITHOUT sourcing ~/.nvm/nvm.sh.
      local nvm_root=/home/agent/.nvm/versions/node/v20.10.0/bin
      sudo -u agent mkdir -p "$nvm_root"
      cat > /tmp/_fake_node.sh <<'EOF'
    #!/bin/bash
    [[ "$1" == "--version" ]] && { echo "v20.10.0"; exit 0; }
    exit 0
    EOF
      sudo install -m 0755 -o agent -g agent /tmp/_fake_node.sh "$nvm_root/node"
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-02"
      printf '%s' "$output" | jq -e '.components.nodejs // .nodejs | map(select(.source == "nvm")) | length >= 1' >/dev/null \
        || __fail "DET-02" "at least one nodejs[] entry with source=nvm when fixture creates ~/.nvm" "$output" "$LOG"
      # Cleanup so subsequent @tests in this run see a clean fixture.
      sudo rm -f "$nvm_root/node"
      sudo rmdir -p --ignore-fail-on-non-empty "$nvm_root" 2>/dev/null || true
    }

    @test "DET-02: a /usr/local/bin/node symlinked to nvm does not double-count" {
      # REQ: DET-02
      # Fixture: create the nvm install AND a /usr/local/bin/node symlink to it.
      # The probe must skip the manual entry (readlink -f resolves into the manager dir)
      # but still emit the nvm entry — exactly one nvm entry, zero manual entries.
      local nvm_root=/home/agent/.nvm/versions/node/v20.10.0/bin
      sudo -u agent mkdir -p "$nvm_root"
      cat > /tmp/_fake_node.sh <<'EOF'
    #!/bin/bash
    [[ "$1" == "--version" ]] && { echo "v20.10.0"; exit 0; }
    exit 0
    EOF
      sudo install -m 0755 -o agent -g agent /tmp/_fake_node.sh "$nvm_root/node"
      sudo ln -sf "$nvm_root/node" /usr/local/bin/node
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-02"
      local manual_count nvm_count
      manual_count=$(printf '%s' "$output" | jq -r '.components.nodejs // .nodejs | map(select(.source == "manual")) | length')
      nvm_count=$(printf '%s' "$output" | jq -r '.components.nodejs // .nodejs | map(select(.source == "nvm")) | length')
      [[ "$manual_count" == "0" ]] \
        || __fail "DET-02" "manual entry suppressed when /usr/local/bin/node is a symlink into a manager dir (got manual=$manual_count)" "$output" "$LOG"
      [[ "$nvm_count" -ge 1 ]] \
        || __fail "DET-02" "nvm entry still emitted alongside the symlink (got nvm=$nvm_count)" "$output" "$LOG"
      sudo rm -f /usr/local/bin/node
      sudo rm -f "$nvm_root/node"
      sudo rmdir -p --ignore-fail-on-non-empty "$nvm_root" 2>/dev/null || true
    }

    @test "DET-03: npm prefix surfaces user / system / effective values as separate JSON fields" {
      # REQ: DET-03
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-03"
      printf '%s' "$output" | jq -e '
        (.components.npm_prefix // .npm_prefix) as $np
        | $np.npm_present == true
        and ($np | has("user_prefix") and has("system_prefix") and has("effective_prefix"))
        and ($np.effective_prefix | type == "string" and length > 0)
      ' >/dev/null \
        || __fail "DET-03" "user_prefix + system_prefix + effective_prefix all present and effective_prefix non-empty" "$output" "$LOG"
    }

    @test "DET-03: npm prefix probe runs via as_user_login (NPM_CONFIG_PREFIX user-shell export observed)" {
      # REQ: DET-03
      # Fixture: write a .profile snippet that exports NPM_CONFIG_PREFIX to a
      # known sentinel path. as_user_login (sudo -i) sources the profile;
      # bare as_user (sudo -E without -i) would NOT source it. If the probe is
      # implemented correctly with as_user_login, effective_prefix reflects
      # the sentinel value.
      local sentinel=/tmp/det03-npm-prefix-sentinel
      sudo -u agent mkdir -p "$sentinel"
      sudo -u agent bash -c 'echo "export NPM_CONFIG_PREFIX=/tmp/det03-npm-prefix-sentinel" >> ~/.profile'
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-03"
      local effective
      effective=$(printf '%s' "$output" | jq -r '.components.npm_prefix.effective_prefix // .npm_prefix.effective_prefix')
      [[ "$effective" == "/tmp/det03-npm-prefix-sentinel" ]] \
        || __fail "DET-03" "effective_prefix reflects user-shell NPM_CONFIG_PREFIX export (got '$effective'); confirms as_user_login was used" "$output" "$LOG"
      # Cleanup so subsequent @tests see a clean profile.
      sudo -u agent sed -i '/NPM_CONFIG_PREFIX=\/tmp\/det03-npm-prefix-sentinel/d' /home/agent/.profile
      sudo rm -rf "$sentinel"
    }

    @test "DET-03: effective prefix ownership + writability captured for the install user" {
      # REQ: DET-03
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-03"
      printf '%s' "$output" | jq -e '
        (.components.npm_prefix // .npm_prefix) as $np
        | $np.npm_present == true
        and ($np.effective_owner | type == "string" and test("^[a-z_][a-z0-9_-]*:[a-z_][a-z0-9_-]*$"))
        and (($np.install_user_writable == true) or ($np.install_user_writable == false))
        and (($np.prefix_declarations | type) == "number")
      ' >/dev/null \
        || __fail "DET-03" "effective_owner is user:group, install_user_writable is bool, prefix_declarations is number" "$output" "$LOG"
    }

    @test "DET-04: claude classified healthy when present and --version exits 0" {
      # REQ: DET-04
      # Post-installer host: claude-code may or may not be installed in this CI
      # base (catalog opt-in). Test asserts the classifier reaches one of the
      # three valid statuses and, IF claude is present, does not classify it absent.
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-04"
      printf '%s' "$output" | jq -e '
        (.components.agents // .agents)
        | map(select(.id == "claude-code"))
        | length == 1
        and .[0].status == ("healthy" // "broken" // "absent")
        and (.[0].status == "healthy" or .[0].status == "broken" or .[0].status == "absent")
      ' >/dev/null \
        || __fail "DET-04" "claude-code agent entry present with status in {healthy, broken, absent}" "$output" "$LOG"
    }

    @test "DET-04: get-shit-done-cc classified healthy when --help banner parseable" {
      # REQ: DET-04
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-04"
      # Same shape assertion as claude-code: entry exists with one of the three valid statuses.
      printf '%s' "$output" | jq -e '
        (.components.agents // .agents)
        | map(select(.id == "gsd"))
        | length == 1
        and (.[0].status == "healthy" or .[0].status == "broken" or .[0].status == "absent")
      ' >/dev/null \
        || __fail "DET-04" "gsd agent entry present with status in {healthy, broken, absent}" "$output" "$LOG"
    }

    @test "DET-04: playwright-cli classified absent when binary missing from install user PATH" {
      # REQ: DET-04
      # Greenfield CI base: playwright-cli is not pre-installed. Assert absent.
      # If a future CI base pre-installs it, this @test becomes a check that the
      # classifier returns one of the three valid statuses.
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-04"
      local status
      status=$(printf '%s' "$output" | jq -r '(.components.agents // .agents) | map(select(.id == "playwright-cli")) | .[0].status')
      [[ "$status" == "absent" || "$status" == "healthy" || "$status" == "broken" ]] \
        || __fail "DET-04" "playwright-cli status in {healthy, broken, absent} (got '$status')" "$output" "$LOG"
    }

    @test "DET-04: classifier returns broken when binary present but --help non-zero" {
      # REQ: DET-04
      # Fixture: drop a fake claude binary on the install user's PATH that exits 0 on
      # --version (parses as "1.0.0") but exits 1 on --help. Classifier must return broken.
      sudo -u agent mkdir -p /home/agent/.local/bin
      cat > /tmp/_fake_claude.sh <<'EOF'
    #!/bin/bash
    [[ "$1" == "--version" ]] && { echo "1.0.0"; exit 0; }
    [[ "$1" == "--help" ]] && exit 1
    exit 1
    EOF
      sudo install -m 0755 -o agent -g agent /tmp/_fake_claude.sh /home/agent/.local/bin/claude
      run bash "$INSTALLER" --report-only --report-format=json
      assert_exit_zero "DET-04"
      local status
      status=$(printf '%s' "$output" | jq -r '(.components.agents // .agents) | map(select(.id == "claude-code")) | .[0].status')
      [[ "$status" == "broken" ]] \
        || __fail "DET-04" "claude-code classified broken when --help exits non-zero (got '$status')" "$output" "$LOG"
      sudo rm -f /home/agent/.local/bin/claude
    }
    ```

    Notes for the executor:
    - The `.components.agents // .agents` jq pattern handles both shapes: Plan 12-02
      writes `{nodejs: [...], npm_prefix: {...}, agents: [...]}` (top-level keys);
      Plan 12-03 will wrap them under `.components`. The `//` alternative makes
      these @tests robust across both states so Plan 12-03's renderer change
      doesn't retroactively break them.
    - Cleanup blocks at the end of each fixture-mutating @test undo the fixture so
      sequential bats execution sees a clean baseline.
    - All @tests use the LOG and INSTALLER globals already declared by Plan 12-01.
    - Every @test name begins with `DET-NN: ` AND every @test body has a
      `# REQ: DET-NN` comment per behavior-test-contract SKILL.
    - bats parse must exit 0 — verified in <verify> below.
  </action>
  <verify>
    <automated>bats --count tests/bats/15-detection.bats >/dev/null && bats --count tests/bats/15-detection.bats | awk '{ if ($1 < 16) { print "FAIL: expected at least 16 @tests (Plan 01 added 7 + Plan 02 adds 9 = 16); got " $1; exit 1 } }' && grep -c '^@test "DET-02:' tests/bats/15-detection.bats | awk '{ if ($1 < 3) exit 1 }' && grep -c '^@test "DET-03:' tests/bats/15-detection.bats | awk '{ if ($1 < 3) exit 1 }' && grep -c '^@test "DET-04:' tests/bats/15-detection.bats | awk '{ if ($1 < 4) exit 1 }' && grep -c '# REQ: DET-' tests/bats/15-detection.bats | awk '{ if ($1 < 9) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `bats --count tests/bats/15-detection.bats` returns at least 16 (7 from Plan 12-01 + 9 added here)
    - At least 3 @test lines beginning with `@test "DET-02: ` exist
    - At least 3 @test lines beginning with `@test "DET-03: ` exist
    - At least 4 @test lines beginning with `@test "DET-04: ` exist
    - At least 9 occurrences of `# REQ: DET-` comments (one per appended @test)
    - The DET-01: / DET-05: / DET-06: @tests from Plan 12-01 are unchanged (verified by `git diff --stat tests/bats/15-detection.bats` showing only additions, no deletions on those lines)
    - The file's header (load lines, LOG, INSTALLER globals) is unchanged — only the new block is appended
    - bats parse exits 0 (no syntax errors)
    - jq is invoked with `-e` for boolean assertions (matches Plan 12-01's existing convention)
  </acceptance_criteria>
  <done>
    The DET-02 / DET-03 / DET-04 @tests are appended to tests/bats/15-detection.bats and reference the REQ-IDs in both the @test name and a `# REQ:` comment. The file is bats-parseable and the count threshold (≥16) matches what Plan 12-03 Task 5 expects when it asserts the read-only invariant against the snapshot scope.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run bats fixture inside Docker on ubuntu-24.04 — assert DET-02/03/04 @tests pass and v0.3.0 baseline preserved</name>
  <files>(none — execution-only verification step)</files>
  <read_first>
    - tests/docker/run.sh (full pipeline: builds image, copies sources to /opt/agentlinux-src, invokes agentlinux-install once, then runs `bats tests/bats/`)
    - tests/bats/15-detection.bats (post-Task-4 — should now have ≥16 @tests)
    - tests/docker/Dockerfile.ubuntu-24.04 (jq pre-installed; the host is post-installer state by the time bats runs)
    - .planning/STATE.md (v0.3.0 baseline = 66/66; Plan 12-01 added 7 → 73; Plan 12-02 adds 9 → 82 expected total)
  </read_first>
  <action>
    Execute the Docker harness end-to-end on ubuntu-24.04 to validate Plan 12-02's
    work in the same fixture-host environment that Plan 12-01 was validated in.

    **Pass 1 — Phase 12 + greenfield baseline (ubuntu-24.04):**
    ```bash
    ./tests/docker/run.sh ubuntu-24.04
    ```
    Expected outcome: bats reports `82 tests, 0 failures` (66 v0.3.0 baseline + 7
    from Plan 12-01 + 9 from Plan 12-02). If the count is higher (e.g. some
    DET-* @tests get split into more cases during execution), that's fine as
    long as the failure count is 0.

    **Pass 2 — repeat on ubuntu-22.04** (matches CONTEXT Q3: read-only @test
    runs on both Docker images, but the broader Phase 12 @tests should also be
    green on the older image):
    ```bash
    ./tests/docker/run.sh ubuntu-22.04
    ```
    Same expected outcome shape (count may differ by 1-2 if a fixture is image-specific;
    failure count must be 0).

    **Pass 3 — re-run for idempotency / no-flake confirmation on ubuntu-24.04:**
    ```bash
    ./tests/docker/run.sh ubuntu-24.04
    ```
    Same outcome. If a transient AGT-02 CDN flake fires (per STATE.md Plan 05-02
    precedent), retry once; do NOT retry on detection-layer failures.

    Do NOT skip the v0.3.0 baseline check. The greenfield invariant
    (run_provisioners untouched on the no-flag path, no regression on 66/66)
    is one of the must_haves.truths for this plan.

    **Failure-mode triage:**
    - If a DET-02 @test fails: most likely Task 1's nodejs.sh probe doesn't
      enumerate the fixture's NodeSource install OR doesn't honor the readlink-f
      symlink-resolution rule. Check the actual JSON via
      `bash plugin/bin/agentlinux-install --report-only --report-format=json | jq .`
      inside the container.
    - If a DET-03 @test fails: most likely Task 2's npm_prefix.sh uses bare
      as_user instead of as_user_login (the NPM_CONFIG_PREFIX user-shell export
      @test is the diagnostic). Verify with grep.
    - If a DET-04 @test fails: most likely Task 3's agents.sh has the wrong
      binary mapping OR exits with a status string outside {healthy, broken, absent}.
      Verify with `jq -r '.components.agents // .agents | .[].status'`.
    - If a baseline @test (e.g. AGT-02, INST-02) fails: detection-layer changes
      should NOT touch any provisioner. If they did, revert and fix; do NOT
      loosen the @test to make it pass.

    **NEVER loosen an @test to make it pass.** If a Phase 12 @test has a wrong
    expectation (e.g. fixture state assumption), surface that as a defect and
    fix the @test in a follow-up commit; do not silently weaken the assertion.
  </action>
  <verify>
    <automated>./tests/docker/run.sh ubuntu-24.04 2>&amp;1 | tee /tmp/12-02-bats.log | tail -5 | grep -E '0 failures'</automated>
  </verify>
  <acceptance_criteria>
    - `./tests/docker/run.sh ubuntu-24.04` exits 0 on the FIRST run (no transient retries needed for detection-layer @tests)
    - Total bats count is at least 82 (66 v0.3.0 baseline + 7 from Plan 12-01 + 9 from Plan 12-02)
    - Zero failures across the entire run (bats tail line: `XX tests, 0 failures`)
    - `./tests/docker/run.sh ubuntu-22.04` exits 0 with the same shape (failures = 0)
    - `/var/log/agentlinux-install.log` inside the container shows the original "agentlinux-install complete" banner from the harness's pre-bats install (proves greenfield path untouched)
    - The 9 new Plan 12-02 @test results in the bats output read `ok` (not `ok # SKIP` and not `not ok`)
    - If a re-run is needed (rare), it is for an unrelated flake (e.g. AGT-02 CDN), NOT for any DET-02 / DET-03 / DET-04 @test
  </acceptance_criteria>
  <done>
    Phase 12 Plan 12-02 is end-to-end green on ubuntu-24.04 + ubuntu-22.04: detection layer detector bodies are filled, DET-02 + DET-03 + DET-04 @tests pass against the orchestrator on the post-installer fixture host, and the v0.3.0 greenfield baseline (66/66) is preserved because Plan 12-02 adds NO provisioner edits — only new code under plugin/lib/detect/ + appended bats @tests. Plan 12-03 can build on a known-green substrate (≥82 tests, 0 failures).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer (root) → install user (agent) | Detection probes that need to resolve as the install user (`npm config get prefix` via as_user_login, `command -v <agent>` via as_user, `<binary> --version` via as_user) cross from root context into agent context. Output of those calls is UNTRUSTED and must never be eval'd or unquoted-expanded. |
| install user PATH → catalog binary version-probe | DET-04 invokes `as_user agent <binary> --version` and `as_user agent <binary> --help` for each catalog agent. The binary resolved from the install user's PATH may be malicious (user added /tmp to PATH; planted a fake `claude`). Captured stdout flows into the JSON report. |
| install user ~/.npmrc + ~/.profile → DET-03 npm probe | as_user_login sources the install user's profile (~/.profile / ~/.bashrc) before invoking npm. Profile content can set NPM_CONFIG_PREFIX to anything; that path appears verbatim in effective_prefix. |
| install user $HOME → DET-02 nvm/fnm/volta/mise/asdf/pnpm probes | find runs as root over the install user's $HOME. find walks the user's directory tree but only with -maxdepth N -name node -type f (read-only); the user could place arbitrary files but cannot trick find into writing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | DET-04 detect::agents_probe captures `<binary> --version` stdout AND `<binary> --help` first line; DET-02 captures `<bin> --version` for nvm/fnm/volta/mise/asdf/pnpm Nodes; DET-03 captures `npm config get prefix` stdout. The captured strings are stored in DETECT_* exports and (Plan 12-03) passed into jq for JSON construction. A user who has placed a malicious binary on the install user's PATH could emit a string with shell metacharacters or with embedded ANSI escape sequences trying to break the renderer or inject into the JSON. | mitigate | (a) Probed strings are NEVER `eval`d, NEVER source'd, NEVER passed to a shell unquoted (Task 3 acceptance: `grep -E '\beval\b' plugin/lib/detect/agents.sh` returns 0 matches; same for nodejs.sh and npm_prefix.sh). (b) Captured strings are stored as bash variables and passed to jq via `--arg` exclusively (Task 3 action mandates `--arg`; Plan 12-03 owns the top-level jq construction and inherits the `--arg` discipline). (c) Version regex extracts only `[0-9]+\.[0-9]+\.[0-9]+(-[a-z0-9.-]+)?` for semver-shaped agents (claude, playwright-cli) — anything else is treated as empty (which the classifier maps to broken), so a malicious binary cannot mass-mark other agents broken or inject arbitrary bytes through the version field. The gsd path is more permissive (banner head -1) because gsd has no --version flag; jq --arg still quotes the bytes safely so JSON shape is preserved even with adversarial input. |
| T-12-03 | Tampering / Information Disclosure | DET-04 runs `as_user agent command -v <binary>` to find each catalog binary on the install user's PATH. The install user's PATH may include /tmp or world-writable directories (the user explicitly added them). A binary at /tmp/claude that the install user can run would resolve as the catalog's claude install. | accept | This is the install user's own configuration; AgentLinux does not police it. detect::agents_probe correctly REPORTS what's on the install user's PATH because that is what Phase 13's REUSE decisions need to know about. The classification is `healthy` only if the binary's `--version` matches catalog's expected output; a malicious /tmp/claude that doesn't match catalog's --version regex is classified `broken`, which causes Phase 14 (REMEDIATE) to surface a warning. Plan 12-02 cites this threat; mitigation verification belongs to Phase 14 (when REMEDIATE consumes the broken classification). |
| T-12-05 | Tampering | DET-03 as_user_login sources the install user's ~/.profile / ~/.bashrc. A profile with an `export NPM_CONFIG_PREFIX=/some/path` line is REFLECTED VERBATIM in effective_prefix. A malicious profile could set NPM_CONFIG_PREFIX to /etc/cron.d (no write permission, but the report would mis-classify writability). | mitigate | (a) The install_user_writable bool is computed via `as_user "$user" test -w "$effective_prefix"` (Task 2 step 5). If the user lacks write to /etc/cron.d, the bool is false — Phase 14 will not attempt to reuse it. (b) The path appears in the JSON report as a string; downstream consumers (Phase 13/14) MUST NOT execute or write to a path read from the report without re-validating ownership. Plan 12-02 documents this contract in the npm_prefix.sh comments; verification belongs to Phase 13. (c) Per Plan 12-01's threat model T-12-04 (read-only invariant), detection itself never writes anywhere outside /run/agentlinux-detect.json — even if NPM_CONFIG_PREFIX were /etc, detection would not touch it. |
| T-12-01 | N/A | Sudoers detection probe — owned by Plan 12-01 detect::sudoers_probe (read-only stat + sha256). Cited here for completeness; Plan 12-02 does not modify sudoers.sh. | (mitigated_in: 12-01) | See 12-01-PLAN.md threat register. |
| T-12-04 | N/A | Read-only invariant violation — owned by Plan 12-03's snapshot @test (full /etc /home /usr/local/bin /opt scope). Plan 12-02 must not introduce any write to those paths in nodejs.sh / npm_prefix.sh / agents.sh; Plan 12-03's @test catches a regression. Acceptance criteria in Tasks 1-3 each forbid `npm install` / `apt-get install` / write to /etc /home /usr/local — verified by grep. | (mitigated_in: 12-03) | See 12-03-PLAN.md threat register. |
</threat_model>

<verification>
  <run_after_each_task>
    shellcheck --severity=warning + shfmt -i 2 -ci -bn on the modified plugin/lib/detect/*.sh file; bats parse on tests/bats/15-detection.bats (only Task 4 modifies the bats file; Tasks 1-3 modify only plugin/lib/detect/ files)
  </run_after_each_task>
  <run_after_plan>
    ./tests/docker/run.sh ubuntu-24.04 — every @test in tests/bats/15-detection.bats must pass green (DET-01 + DET-05 + DET-06 from Plan 01 + DET-02 + DET-03 + DET-04 from Plan 02 = at least 16 @tests, of which the 9 new ones from Plan 02 are the focus); also ./tests/docker/run.sh ubuntu-22.04 with same shape
  </run_after_plan>
  <regression_guard>
    ./tests/docker/run.sh ubuntu-24.04 — full bats matrix stays at the 66/66 v0.3.0 baseline plus the new Phase 12 @tests (Plan 12-02 must NOT modify any provisioner; only adds new code under plugin/lib/detect/ + appends to one bats file). End-to-end count: ≥82 tests, 0 failures.
  </regression_guard>
</verification>

<success_criteria>
- [ ] plugin/lib/detect/nodejs.sh probes 8 sources per RESEARCH §Pattern 2 — verified by grep of the 6 manager paths + dpkg-query + readlink -f, AND by a bats @test on a fixture that creates an nvm install
- [ ] plugin/lib/detect/npm_prefix.sh emits the 3-value shape (user_prefix, system_prefix, effective_prefix) — verified by `jq -e '... has("user_prefix") and has("system_prefix") and has("effective_prefix")'` in the bats DET-03 @test
- [ ] plugin/lib/detect/agents.sh classifies each catalog agent (claude, get-shit-done-cc, playwright-cli) as healthy / broken / absent — verified by bats @tests covering present + absent + broken fixture states
- [ ] tests/bats/15-detection.bats has at least 9 new @tests citing DET-02:, DET-03:, DET-04: in their names AND `# REQ: DET-NN` comments in their bodies
- [ ] No `eval` anywhere in plugin/lib/detect/{nodejs,npm_prefix,agents}.sh (T-12-02 mitigation)
- [ ] All `command -v <binary>` invocations in agents.sh go through `as_user agent ...` (PATH visibility per RESEARCH §Pitfall 4)
- [ ] npm probe in npm_prefix.sh specifically uses `as_user_login` (not bare as_user) for every npm config get invocation (RESEARCH §Pitfall 7; verified indirectly by the DET-03 NPM_CONFIG_PREFIX-sentinel @test)
- [ ] Renderer (plugin/lib/detect/render.sh) emits per-entry / per-field [DET-NN] markers for DET-02, DET-03, DET-04 — no longer the Plan 12-01 placeholder lines
- [ ] Phase 13 contract reader functions (detect::nodejs_satisfies_pin, detect::nodejs_prefix_writable, detect::npm_prefix_path, detect::npm_prefix_writable_by_install_user, detect::agent_status) all return real (non-stub) values
- [ ] shellcheck --severity=warning + shfmt -i 2 -ci -bn pass on every modified plugin/lib/detect/*.sh file (zero warnings, zero diffs)
- [ ] full Docker matrix (`./tests/docker/run.sh ubuntu-24.04` + `./tests/docker/run.sh ubuntu-22.04`) exits 0 with ≥82 tests and 0 failures on each image
- [ ] No edits to any plugin/provisioner/*.sh or plugin/bin/agentlinux-install (regression guard — only Plan 12-01 owns those edits in this phase)
</success_criteria>

<output>
  <files_created>
    - (none — all 4 files in files_modified are EDITED, not created; Plan 12-01 created the stubs)
  </files_created>
  <files_modified>
    - plugin/lib/detect/nodejs.sh (DET-02 fill: 8-source enumeration body replaces the stub)
    - plugin/lib/detect/npm_prefix.sh (DET-03 fill: three-value probe body replaces the stub)
    - plugin/lib/detect/agents.sh (DET-04 fill: classifier body replaces the stub)
    - plugin/lib/detect/render.sh (DET-02/DET-03/DET-04 sections replace the placeholder `__det_field DET-NN section.status stub` lines)
    - tests/bats/15-detection.bats (append DET-02 / DET-03 / DET-04 @tests; do NOT modify Plan 12-01's @tests or globals)
  </files_modified>
  <commit_message>feat(12): detect node + npm prefix + catalog agents (DET-02, DET-03, DET-04 + bats @tests)</commit_message>
  <summary_path>
    After completion, create `.planning/phases/12-detection-layer/12-02-SUMMARY.md`
    following the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
    Cite every modified file path, the shellcheck + shfmt + bats results from
    Task 5, the new vs prior bats count delta (Plan 01 → Plan 02), and confirm
    no provisioner edits (regression guard for the v0.3.0 baseline).
  </summary_path>
</output>
