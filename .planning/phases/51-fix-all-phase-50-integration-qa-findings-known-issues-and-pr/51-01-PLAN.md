---
phase: 51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/catalog/agents/firecrawl-mcp/install.sh
  - plugin/catalog/agents/github-mcp/install.sh
  - plugin/catalog/lib/mcp-register.sh
  - plugin/catalog/catalog.json
  - tests/bats/60-catalog-github-mcp.bats
  - tests/bats/62-catalog-firecrawl-mcp.bats
  - tests/bats/71-phase51-hosted-mcp.bats
  - docs/internals/catalog.md
  - docs/internals/registry-cli.md
autonomous: true
requirements: [MCP-03, MCP-07, OPS-01, TST-08]
must_haves:
  truths:
    - "Hosted MCP registration still fans out to all five supported clients and removes symmetrically."
    - "Firecrawl's catalog and user guidance describe the observed OAuth-first endpoint and runtime-only API-key URL fallback truthfully."
    - "The OpenCode GitHub failure is distinguished from an AgentLinux registration failure: the protected-resource metadata points at GitHub's OAuth server, which does not provide DCR."
    - "No credential, OAuth token, authorization header, or API key is written to catalog files, tests, reports, or client configuration by AgentLinux."
  artifacts:
    - path: "tests/bats/71-phase51-hosted-mcp.bats"
      provides: "Offline regression for metadata, five-client fan-out, explicit OAuth diagnostics, and no-secret invariants"
    - path: "plugin/catalog/agents/firecrawl-mcp/install.sh"
      provides: "Truthful Firecrawl authentication guidance"
  key_links:
    - from: "plugin/catalog/lib/mcp-register.sh"
      to: "plugin/catalog/agents/github-mcp/install.sh"
      via: "shared bare remote-http registration"
      pattern: "al_mcp_register_http"
    - from: "tests/bats/71-phase51-hosted-mcp.bats"
      to: "plugin/catalog/catalog.json"
      via: "catalog-derived URLs and metadata"
      pattern: "endpoint_url"

<threat_model>
- Credential leakage: API keys and OAuth tokens are accepted only through the
  disposable runtime environment; tests inspect that persistent AgentLinux
  config contains no credential-shaped fields.
- OAuth redirect/token state: OpenCode debug/auth commands run against a clean
  temporary HOME and the plan records metadata and redirect outcomes without
  copying auth state into the repository.
- Registration integrity: preserve the existing marker/JSON atomic writes and
  all five-client fan-out; a client-specific diagnostic must not become a
  client-specific credential injection.
- External incompatibility: if GitHub's authorization server still lacks DCR,
  classify the boundary explicitly and retain a safe bare registration rather
  than claiming a repaired OAuth flow.
</threat_model>

<objective>
Resolve the Phase 50 Firecrawl and OpenCode/GitHub hosted-MCP findings at the
correct seam: repair any AgentLinux contract or guidance defect, prove the
actual OAuth metadata/root cause, and keep ADR-017's thin-installer boundary.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-CONTEXT.md
@.planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-RESEARCH.md
@.planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-PATTERNS.md
@.planning/phases/50-integration-qa/50-EVIDENCE.md
@docs/decisions/017-mcp-thin-installer-in-client-auth.md
@plugin/catalog/lib/mcp-register.sh
@plugin/catalog/catalog.json
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the hosted-MCP auth contract and OpenCode root-cause seam</name>
  <read_first>
    - .planning/phases/50-integration-qa/50-EVIDENCE.md
    - plugin/catalog/lib/mcp-register.sh
    - plugin/catalog/agents/github-mcp/install.sh
    - plugin/catalog/agents/firecrawl-mcp/install.sh
    - docs/decisions/017-mcp-thin-installer-in-client-auth.md
    - https://docs.firecrawl.dev/developer-guides/mcp-setup-guides/oauth
    - https://opencode.ai/docs/mcp-servers/#oauth
  </read_first>
  <action>
    Reproduce the two documented Firecrawl paths in a disposable user HOME: use
    a runtime-only API-key URL for the smallest read-only scrape and then use the
    bare endpoint through the client's OAuth discovery/consent path. Separately
    run OpenCode's documented `mcp debug`, `mcp auth`, and `auth list` flow for
    github-mcp in a clean HOME, capturing HTTP status, protected-resource
    metadata, authorization-server discovery, DCR response, redirect URI, and
    persisted-state path with all tokens and key-bearing URLs redacted. Verify
    the GitHub metadata currently identifies `https://github.com/login/oauth` and
    whether that server offers registration; do not infer an OpenCode bug from a
    remote server's missing DCR endpoint.

    Store only a redacted diagnostic transcript and a finding disposition under
    the Phase 51 planning directory. If the server remains non-DCR, keep the
    existing bare registration and document the external compatibility boundary;
    do not introduce a baked GitHub client secret or silently replace the
    five-client fan-out with a single-client workaround.
  </action>
  <acceptance_criteria>
    - A redacted diagnostic records both Firecrawl auth attempts and the
      OpenCode/GitHub metadata → DCR → redirect chain without any secret value.
    - The transcript identifies whether the failure is in AgentLinux, OpenCode,
      or the remote authorization server and records the exact remaining block.
    - `git grep -nE '(fc-[A-Za-z0-9]|ghp_[A-Za-z0-9]|github_pat_|Bearer |Authorization:)' -- .planning/phases/51*` finds no credential.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-OAUTH-DIAGNOSTICS.md && ! rg -n 'ghp_[A-Za-z0-9]|github_pat_|Bearer [A-Za-z0-9]|fc-[A-Za-z0-9]+' .planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Make Firecrawl and OpenCode guidance/metadata truthful and regression-covered</name>
  <read_first>
    - plugin/catalog/catalog.json
    - plugin/catalog/agents/firecrawl-mcp/install.sh
    - plugin/catalog/agents/github-mcp/install.sh
    - plugin/catalog/lib/mcp-register.sh
    - tests/bats/60-catalog-github-mcp.bats
    - tests/bats/62-catalog-firecrawl-mcp.bats
    - .planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-OAUTH-DIAGNOSTICS.md
  </read_first>
  <action>
    Update the Firecrawl catalog description, requires_secret/auth wording, and
    install output to say OAuth is the bare-endpoint path and the API-key URL is
    an explicit user-supplied fallback for clients that cannot complete OAuth.
    Keep the URL key out of all AgentLinux-written config and preserve the
    endpoint URL itself. Update OpenCode/GitHub install guidance with the exact
    `opencode mcp debug`, `opencode mcp auth`, and `opencode auth list` commands,
    explain the DCR compatibility boundary found in Task 1, and avoid promising
    a successful GitHub OAuth read when the remote server cannot register a
    client. If a newer OpenCode pin is required by the diagnostic, pin the exact
    verified version and update its version-lock test; do not use latest.

    Extend the existing GitHub and Firecrawl Bats contracts and add
    71-phase51-hosted-mcp.bats for offline assertions: the five target adapters
    remain present, URLs remain catalog-derived, OpenCode remains type remote,
    no auth/header/token field is emitted, auth guidance is present, and remove
    leaves no marker/config residue. Tests must not attempt live OAuth or embed a
    runtime key; the live read is recorded only in the redacted QA artifact.
  </action>
  <acceptance_criteria>
    - Catalog validation passes and Firecrawl no longer claims unrestricted
      keyless scraping when the bare endpoint requires OAuth.
    - `bash -n` passes every changed shell recipe and the targeted MCP Bats
      tests pass in the Docker RC harness.
    - `rg -n 'codex|gemini|opencode|qwen|claude' plugin/catalog/lib/mcp-register.sh`
      still shows all five adapters and no adapter is removed as a workaround.
    - Static tests prove no credential-shaped field is serialized by either
      installer and uninstall remains idempotent.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/catalog/agents/firecrawl-mcp/install.sh plugin/catalog/agents/github-mcp/install.sh plugin/catalog/lib/mcp-register.sh && jq empty plugin/catalog/catalog.json && rg -n 'oauth|mcp debug|mcp auth|auth list|five|credential' plugin/catalog/agents/firecrawl-mcp/install.sh plugin/catalog/agents/github-mcp/install.sh tests/bats/71-phase51-hosted-mcp.bats</automated>
  </verify>
</task>

</tasks>

<verification>
  <must_pass>bash -n all changed MCP shell recipes and jq validation of catalog.json.</must_pass>
  <must_pass>Targeted MCP Bats prove five-client fan-out, no credentials, and symmetric removal.</must_pass>
  <must_pass>51-OAUTH-DIAGNOSTICS.md records the live Firecrawl/OpenCode result without secrets.</must_pass>
</verification>

<success_criteria>
  - Firecrawl authentication guidance matches the reproduced OAuth/API-key behavior.
  - OpenCode/GitHub's DCR incompatibility is either repaired by a tested compatible path or recorded as an external blocker with no unsafe workaround.
  - The shared MCP fan-out and ADR-017 boundary remain intact.
</success_criteria>
