---
phase: 51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr
plan: 03
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - plugin/catalog/agents/gsd/install.sh
  - plugin/catalog/agents/gsd/uninstall.sh
  - plugin/catalog/agents/gsd/preserve_paths.json
  - plugin/catalog/catalog.json
  - plugin/cli/src/detect.ts
  - plugin/lib/detect/agents.sh
  - plugin/lib/reuse/agents.sh
  - plugin/lib/remediate/nodejs.sh
  - tests/bats/50-agents.bats
  - tests/bats/56-catalog-skill-wiring.bats
  - tests/bats/71-phase51-hosted-mcp.bats
  - tests/bats/73-phase51-gsd-codex.bats
  - plugin/cli/test/install.test.ts
  - plugin/cli/test/loader.test.ts
  - plugin/cli/test/npm_ls.test.ts
  - docs/internals/gsd.md
  - docs/internals/catalog.md
autonomous: true
requirements: [WIRE-01, AGT-07, OPS-01, TST-08]
must_haves:
  truths:
    - "The existing gsd catalog identity installs the exact official @opengsd/gsd-core pin and its package-native command resolves agent-owned."
    - "Open GSD installs Codex integration without invalid hooks TOML; codex remains launchable and a real GSD command is discoverable."
    - "The GSD install/remove path is symmetric across every runtime Open GSD supports, and unsupported/sunset runtime flags are documented rather than silently attempted."
    - "Detection, reuse, upgrade, uninstall, and tests agree on the new package name and canonical command without a /usr/local shim."
  artifacts:
    - path: "tests/bats/73-phase51-gsd-codex.bats"
      provides: "OpenGSD exact-pin, Codex config-load, cross-runtime wiring, and symmetric removal regression"
  key_links:
    - from: "plugin/catalog/catalog.json"
      to: "plugin/catalog/agents/gsd/install.sh"
      via: "npm package and pin injected through catalog dispatch"
      pattern: "@opengsd/gsd-core"
    - from: "plugin/catalog/agents/gsd/install.sh"
      to: "plugin/cli/src/detect.ts"
      via: "canonical gsd-core binary path"
      pattern: "gsd-core"

<threat_model>
- Package identity: pin `@opengsd/gsd-core` exactly and version-check the
  package-native command; do not use `@latest` or a compatibility alias shim.
- Codex config integrity: run OpenGSD in a disposable Codex HOME, validate TOML
  and launch Codex before/after wiring; preserve unrelated user TOML entries and
  never overwrite auth state.
- Cross-agent cleanup: invoke upstream uninstall where supported, then sweep
  only GSD-owned markers/paths; preserve user workflow data from preserve_paths.
- Runtime support: treat OpenGSD's documented runtime roster as authoritative;
  unsupported Gemini behavior is explicit and does not justify skipping Codex.
- Ownership: npm remains the agent user's global prefix; no sudo npm command or
  wrapper under `/usr/local/bin` is allowed.
</threat_model>

<objective>
Replace the incompatible legacy GSD package with official Open GSD and make its
Codex integration a tested, safe, user-visible part of `agentlinux install gsd`.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-CONTEXT.md
@.planning/phases/51-fix-all-phase-50-integration-qa-findings-known-issues-and-pr/51-RESEARCH.md
@https://github.com/open-gsd/gsd-core
@plugin/catalog/agents/gsd/install.sh
@plugin/catalog/agents/gsd/uninstall.sh
@plugin/catalog/agents/codex/install.sh
@tests/bats/56-catalog-skill-wiring.bats
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate catalog identity and GSD lifecycle to Open GSD 1.7.0</name>
  <read_first>
    - plugin/catalog/catalog.json
    - plugin/catalog/agents/gsd/install.sh
    - plugin/catalog/agents/gsd/uninstall.sh
    - plugin/catalog/agents/gsd/preserve_paths.json
    - plugin/cli/src/detect.ts
    - plugin/lib/detect/agents.sh
    - plugin/lib/reuse/agents.sh
    - plugin/lib/remediate/nodejs.sh
    - plugin/cli/test/loader.test.ts
    - plugin/cli/test/npm_ls.test.ts
    - Open GSD package metadata and installer at the exact selected release
  </read_first>
  <action>
    Change only the gsd catalog entry's package identity/pin/homepage,
    preserving id `gsd`, catalog recipe paths, license, and user-facing install
    command. Use `@opengsd/gsd-core@1.7.0` (or document a more recent exact
    stable pin only if verification selects it) and update compatibility windows,
    post-install verification, canonical detection/reuse maps, migration cleanup,
    and test fixtures to the package-native `gsd-core` command. Do not add a
    `gsd` or `/usr/local/bin` wrapper shim.

    Rewrite the recipe to run the upstream installer with explicit supported
    runtime flags for Claude, OpenCode, Codex, and Qwen, using its native
    Codex hooks/config writer. Handle OpenGSD's removed Gemini selector as an
    explicit N/A message and clean up any legacy GSD surfaces from previous
    installs without deleting user-owned workflow directories. Keep the npm
    operation agent-owned and make repeated install/remove converge.
  </action>
  <acceptance_criteria>
    - `npm_package_name` is exactly `@opengsd/gsd-core`, the pin is exact, and
      `gsd-core --help`/the package's version signal matches it.
    - All canonical maps, detection probes, reuse/remediation, upgrade tests,
      and catalog tests agree on `/home/agent/.npm-global/bin/gsd-core`.
    - No changed recipe contains `sudo npm install -g`, `npm install ... latest`,
      or a `/usr/local/bin` shim.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.agents[] | select(.id=="gsd" and .npm_package_name=="@opengsd/gsd-core" and .pinned_version=="1.7.0")' plugin/catalog/catalog.json && rg -n 'gsd-core|@opengsd/gsd-core' plugin/catalog/agents/gsd plugin/cli/src/detect.ts plugin/lib/detect/agents.sh plugin/lib/reuse/agents.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add mandatory Codex wiring and exact runtime regression coverage</name>
  <read_first>
    - plugin/catalog/agents/gsd/install.sh
    - plugin/catalog/agents/gsd/uninstall.sh
    - plugin/catalog/agents/codex/install.sh
    - tests/bats/56-catalog-skill-wiring.bats
    - tests/bats/50-agents.bats
    - plugin/cli/test/install.test.ts
    - docs/internals/gsd.md
  </read_first>
  <action>
    Replace the old Codex-excluded assertions with tests that install Codex,
    install GSD, parse `~/.codex/config.toml` using the repository's available
    TOML validation path, run `codex --version` and one minimal non-authenticated
    GSD/Codex discoverability check, and assert a GSD-owned Codex skill/hook is
    present. Seed unrelated Codex config/auth-shaped user content and prove it
    survives install and remove. Exercise provider-first and Codex-first order,
    reinstall, and remove-one-keep-sibling; assert no `[[hooks]]` legacy shape,
    no invalid `HooksToml` error, and no credential values in the resulting
    config.

    Create 73-phase51-gsd-codex.bats and update 50/56 plus TypeScript fixtures
    for the new command/version. Run the corrected Gemini observation invocation
    twice in the disposable QA environment and record F-007 as closed
    unconfirmed; do not add retry logic to the recipe.
  </action>
  <acceptance_criteria>
    - Codex launches after GSD install and the Codex integration is present;
      no test or recipe skips Codex.
    - Install/remove is symmetric for every supported OpenGSD runtime and leaves
      unrelated config plus preserved workflow data intact.
    - The regression suite explicitly distinguishes a valid OpenGSD Codex
      configuration from the old invalid array-of-tables failure.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Codex|codex|gsd-core|invalid type|HooksToml|provider-first|remove' tests/bats/73-phase51-gsd-codex.bats tests/bats/56-catalog-skill-wiring.bats && ! rg -n 'codex excluded|codex skipped|--codex.*omit|--codex.*skip' plugin/catalog/agents/gsd tests/bats/73-phase51-gsd-codex.bats</automated>
  </verify>
</task>

</tasks>

<verification>
  <must_pass>CLI unit tests, catalog schema validation, and GSD/Codex Bats pass with the exact OpenGSD pin.</must_pass>
  <must_pass>Codex launches before and after GSD wiring in a fresh user HOME, with no legacy invalid TOML and no credential leakage.</must_pass>
  <must_pass>Gemini F-007 is closed as unconfirmed only in the QA report; no speculative production retry behavior exists.</must_pass>
</verification>

<success_criteria>
  - `agentlinux install gsd` installs official Open GSD and wires Codex safely.
  - Detection, upgrade, uninstall, docs, and tests all describe the same package-native identity.
  - Legacy GSD/Codex incompatibility is absent from the product path and covered by regression tests.
</success_criteria>
