---
phase: 55-pure-logic-core-parity
plan: 03
type: execute
wave: 3
depends_on: ["55-01"]
files_modified:
  - rust/crates/agentlinux-core/src/classify.rs
  - rust/crates/agentlinux-core/src/divergence.rs
  - rust/crates/agentlinux-core/src/types.rs
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, GATE-01, GATE-05]
must_haves:
  truths:
    - "classify returns verdicts identical to TS classify across the full classify.test.ts six-state corpus (CORE-01) — re-asserted, plus any missing corpus rows imported."
    - "decide_version returns the same {version, source, sticky} as TS decideVersion across all 5 classify.test.ts:96-121 rows (folded into this phase, closing the classify.test.ts corpus)."
    - "computeDivergence + resolveLatestFor parity is re-asserted against the divergence corpus incl. the zero-match + empty-list error paths (CORE-02)."
  artifacts:
    - rust/crates/agentlinux-core/src/classify.rs
    - rust/crates/agentlinux-core/src/divergence.rs
    - rust/crates/agentlinux-core/src/types.rs
  key_links:
    - "decide_version reads CatalogEntry.pinned_version + Sentinel.sticky/version/source and returns VersionDecision (added to types.rs) — the pure companion to classify."
    - "divergence.rs resolve_latest_for already routes through semver_shim::max_satisfying — CORE-02 re-assert confirms coverage; no new port."
---

<objective>
Close CORE-01/CORE-02 and fold `decideVersion` into the pure core:

1. **CORE-01 re-assert + `decideVersion` fold-in:** `classify.rs` already ports all six
   classify states (Phase 53). This plan re-asserts that against the full
   classify.test.ts corpus and PORTS `decideVersion` (deferred at classify.rs:61) as
   `decide_version` with its 5-row golden table — a ~15-LOC pure add that closes the
   classify.test.ts corpus completely (orchestrator decision: it belongs with the pure
   core, not Phase 56).
2. **CORE-02 re-assert:** confirm `divergence.rs` (`compute_divergence` + `resolve_latest_for`)
   already covers the TS `divergence.test.ts` branches including the zero-match and empty-list
   error paths; import any missing corpus row.

Purpose: complete the classify.test.ts golden corpus and formally re-assert CORE-01/02 so
the pure decision core is byte-for-byte with TS end to end.
Output: `decide_version` in `classify.rs` (+ `VersionDecision` in `types.rs`) with a 5-row
golden; re-asserted classify/divergence corpora.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-pure-logic-core-parity/55-RESEARCH.md
@.planning/phases/55-pure-logic-core-parity/55-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `classify::decide_version(entry, version_override: Option<&str>, existing_sentinel: Option<&Sentinel>) -> VersionDecision` — folds in the deferred decideVersion (classify.rs:61).
- `types::VersionDecision { version, source, sticky }` — mirror of TS `VersionDecision` (types.ts:91-95).
- Re-asserted classify.test.ts + divergence.test.ts golden coverage (CORE-01/CORE-02) — importing any previously-missing corpus row.
Downstream: Phase 56 install/upgrade CLI verbs consume `decide_version` (which version the recipe installs) alongside `classify`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-assert classify.test.ts coverage + import any missing corpus row</name>
  <files>rust/crates/agentlinux-core/src/classify.rs</files>
  <read_first>
    - rust/crates/agentlinux-core/src/classify.rs (READ IN FULL — the existing six-state port + the `tests` module already covering not-installed×2 / synced / drift / override-ahead / override-behind / pinned-override; the note at classify.rs:60-62 that decideVersion is deferred)
    - plugin/cli/test/classify.test.ts (READ IN FULL — the classify() suite rows AND the decideVersion suite at lines 96-121; confirm every classify() assertion has a matching Rust `#[test]` row)
    - plugin/cli/src/version/classify.ts:15-27 (the six-state branch order — the parity source)
  </read_first>
  <action>
    Compare the `classify()` assertions in classify.test.ts against the existing `#[test]`
    rows in classify.rs. The Phase-53 port already covers all six states; if classify.test.ts
    contains a classify() row NOT represented in the Rust `tests` module (e.g. a distinct input
    combination), import it verbatim as a new `#[test]` so the classify() corpus is fully
    mirrored. Do NOT change the `classify` function logic — this is a coverage re-assertion; the
    branch order is already byte-for-byte with classify.ts:15-27. Update the module doc-comment
    at classify.rs:60-62 to note decideVersion is now ported (Task 2 in this plan) rather than
    deferred.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core classify 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core classify` passes; every `classify()` row in classify.test.ts has a corresponding green Rust `#[test]`.
    - The `classify` function body is unchanged (re-assertion only): the six-state branch order still matches classify.ts:15-27.
    - Matches TS corpus: not-installed / synced / drift-undeclared / override-ahead / override-behind / pinned-override all covered (CORE-01).
  </acceptance_criteria>
  <done>
    The classify.test.ts `classify()` corpus is fully mirrored in classify.rs with green tests; CORE-01 re-asserted; no logic change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port decide_version with the 5-row classify.test.ts golden + VersionDecision type</name>
  <files>rust/crates/agentlinux-core/src/classify.rs, rust/crates/agentlinux-core/src/types.rs</files>
  <read_first>
    - plugin/cli/src/version/classify.ts:29-50 (READ IN FULL — `decideVersion`: override flag wins → source='override' sticky=false; else sticky sentinel preserved → inherit source, sticky=true; else catalog pin → source='curated' sticky=false)
    - plugin/cli/test/classify.test.ts:96-121 (the 5 decideVersion golden rows — this IS the parity oracle; port each verbatim)
    - plugin/cli/src/types.ts:91-95 (`VersionDecision { version, source, sticky }` — the return shape to mirror)
    - rust/crates/agentlinux-core/src/types.rs (the `Sentinel` struct with `version`/`source`/`sticky`; add `VersionDecision` here mirroring the `Serialize` derive convention of `DivergenceReport`)
    - rust/crates/agentlinux-core/src/classify.rs (the `#[test]` fixtures `base_entry()`/`sentinel()` to reuse for the decide_version golden module)
    - .planning/phases/55-pure-logic-core-parity/55-RESEARCH.md §"Open Questions" #1 (the orchestrator decision to fold decideVersion into THIS phase; it is pure, ~15 LOC, closes the classify.test.ts corpus)
  </read_first>
  <behavior>
    - decide_version(entry, Some("2.0.0"), None) → { version:"2.0.0", source:"override", sticky:false } (override wins)
    - decide_version(entry, Some("2.0.0"), Some(sentinel("1.1.0","pinned",true))) → { "2.0.0", "override", false } (override wins even over sticky)
    - decide_version(entry, None, Some(sentinel("1.1.0","pinned",true))) → { "1.1.0", "pinned", true } (sticky preserved, source inherited)
    - decide_version(entry{pinned:"1.0.0"}, None, Some(sentinel("0.9.0","curated",false))) → { "1.0.0", "curated", false } (non-sticky → catalog pin)
    - decide_version(entry{pinned:"1.0.0"}, None, None) → { "1.0.0", "curated", false } (no sentinel → catalog pin)
  </behavior>
  <action>
    Add `VersionDecision { version: String, source: String, sticky: bool }` to `types.rs`
    (mirror types.ts:91-95; derive `Debug, Clone, PartialEq, Eq, Serialize` to match the
    `DivergenceReport` convention and to allow `assert_eq!` in the golden test). Port
    `decideVersion` as `pub fn decide_version(entry: &CatalogEntry, version_override:
    Option<&str>, existing_sentinel: Option<&Sentinel>) -> VersionDecision` in `classify.rs`,
    branch order verbatim from classify.ts:39-49: if `version_override` is Some → `{ version:
    override.to_string(), source: "override", sticky: false }`; else if the sentinel is Some
    AND `sentinel.sticky` → `{ version: sentinel.version.clone(), source:
    sentinel.source.clone(), sticky: true }` (inherit the sentinel's source, NOT hardcoded);
    else → `{ version: entry.pinned_version.clone(), source: "curated", sticky: false }`. This
    is pure (no I/O, no semver) — a straight field dispatch.

    Add a golden `#[test]` module porting all 5 classify.test.ts:96-121 decideVersion rows
    verbatim (the `<behavior>` rows), each `assert_eq!`-ing the full `VersionDecision`. Reuse
    the existing `base_entry()`/`sentinel()` fixtures. No proptest is required for this trivial
    dispatch, but a totality note is fine.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core decide 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core decide` passes, covering all 5 classify.test.ts:96-121 golden rows.
    - `grep -n 'fn decide_version' rust/crates/agentlinux-core/src/classify.rs` + `grep -n 'struct VersionDecision' rust/crates/agentlinux-core/src/types.rs` each return a match.
    - The sticky branch INHERITS the sentinel source (not hardcoded): the row `decide_version(entry, None, sentinel("1.1.0","pinned",true))` yields source "pinned" (classify.test.ts:107-109).
    - Matches TS corpus: override wins even over sticky (classify.test.ts:102-105); no-sentinel + no-override → catalog pin "1.0.0"/"curated"/false (classify.test.ts:117-119).
  </acceptance_criteria>
  <done>
    `decide_version` matches TS `decideVersion` byte-for-byte across all 5 golden rows; `VersionDecision` mirrors the TS shape; the classify.test.ts corpus is fully closed (CORE-01 complete).
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-assert CORE-02 divergence/resolveLatestFor coverage</name>
  <files>rust/crates/agentlinux-core/src/divergence.rs</files>
  <read_first>
    - rust/crates/agentlinux-core/src/divergence.rs (READ IN FULL — `compute_divergence`, `resolve_latest_for`, `DivergenceError`, and the existing ~14 `#[test]` rows covering the six states + highest-match/constraint/no-match-throw/empty-throw)
    - plugin/cli/test/divergence.test.ts (READ IN FULL — the computeDivergence six-state rows + the resolveLatestFor branches at lines ~132-156 incl. the zero-match error path and the empty-list error path; confirm each TS assertion has a matching Rust `#[test]`)
    - plugin/cli/src/version/divergence.ts (the parity source for compute_divergence + resolveLatestFor)
  </read_first>
  <action>
    Compare the `computeDivergence` + `resolveLatestFor` assertions in divergence.test.ts
    against the existing `#[test]` rows in divergence.rs. Research confirms the Phase-53 port
    already covers all branches incl. both throw paths (zero-match + empty-list); if a TS
    assertion is NOT represented (e.g. a specific version-list or constraint combination),
    import it verbatim as a new `#[test]`. Do NOT change `compute_divergence`/`resolve_latest_for`
    logic — this is a re-assertion. Confirm `resolve_latest_for` still routes through
    `semver_shim::max_satisfying` (never `semver::` directly). If coverage is already complete,
    add a short doc-comment in the test module noting the divergence.test.ts corpus is fully
    mirrored (CORE-02 re-asserted) so the traceability is explicit.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core divergence 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core divergence` passes; every `computeDivergence` + `resolveLatestFor` row in divergence.test.ts has a corresponding green Rust `#[test]`, including the zero-match and empty-list error paths.
    - The `compute_divergence`/`resolve_latest_for` bodies are unchanged (re-assertion only).
    - No direct `semver::`: `grep -n 'semver::' rust/crates/agentlinux-core/src/divergence.rs | grep -v '^\s*//'` prints nothing (routes through semver_shim::max_satisfying).
    - Matches TS corpus: the zero-match error path and empty-list error path both covered (CORE-02).
  </acceptance_criteria>
  <done>
    The divergence.test.ts corpus (six states + resolveLatestFor branches + both error paths) is fully mirrored with green tests; CORE-02 re-asserted; no logic change; all version ops via semver_shim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog pin / sentinel / installed version → classify+decide_version+divergence | Concrete version strings enter the pure deciders; all comparisons route through `semver_shim` (total, no panic — T-53-01). No new external input surface this plan (re-assert + a pure field-dispatch add). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-06 | Tampering | `decide_version` | low | mitigate | Pure field dispatch with no semver/parse; the override/sticky/curated branches are exhaustively pinned by the 5-row golden, so a divergence from the TS install-version decision (which version the recipe installs) trips the corpus. |
| T-55-07 | Denial of Service | `resolve_latest_for` (re-assert) | low | accept | Already covered by divergence.rs's existing typed zero-match/empty-list error paths (Phase 53) + semver_shim totality; re-assertion adds coverage only, no new surface. |
</threat_model>

<verification>
- `cd rust && cargo test -p agentlinux-core classify decide divergence` green (CORE-01 full corpus incl. decide_version, CORE-02 re-assert).
- `cd rust && cargo test --workspace` green (no regression across the crate).
- `decide_version` + `VersionDecision` exist; classify/divergence logic bodies unchanged; no direct `semver::` outside the shim.
</verification>

<success_criteria>
- CORE-01 complete: full classify.test.ts corpus (six states + the 5 decideVersion rows) mirrored byte-for-byte.
- CORE-02 re-asserted: computeDivergence + resolveLatestFor corpus (incl. zero-match + empty-list) mirrored.
- `decide_version` folded into the pure core (orchestrator decision); the crate stays PURE.
</success_criteria>

<output>
Create `.planning/phases/55-pure-logic-core-parity/55-03-SUMMARY.md` when done
</output>
