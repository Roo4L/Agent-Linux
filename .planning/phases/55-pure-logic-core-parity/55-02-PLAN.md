---
phase: 55-pure-logic-core-parity
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - rust/crates/agentlinux-core/src/category.rs
  - rust/crates/agentlinux-core/src/pin_spec.rs
  - rust/crates/agentlinux-core/src/detect_gates.rs
  - rust/crates/agentlinux-core/src/lib.rs
autonomous: true
requirements: [CORE-03, GATE-01, GATE-05]
must_haves:
  truths:
    - "derive_category(entry) returns the same Category as TS deriveCategory for every row of category.test.ts — first-match-wins, ORDER-preserving precedence, source_kind=mcp fallback, Other floor."
    - "parse_pin_spec(spec) returns the same ParsedPin/PinTarget as TS parsePinSpec, and the two distinct error messages (Usage vs InvalidTarget) match the substrings pin.test.ts greps for."
    - "The pure detect-gate deciders (is_canonical_agent_path / reuse gate / remediate gate / presence gate) return the same verdicts as TS across the adopt/list-presence corpus, WITHOUT any host statSync or cache read."
  artifacts:
    - rust/crates/agentlinux-core/src/category.rs
    - rust/crates/agentlinux-core/src/pin_spec.rs
    - rust/crates/agentlinux-core/src/detect_gates.rs
  key_links:
    - "parse_pin_spec calls semver_shim::valid (from Plan 01); the detect-gate deciders call semver_shim::satisfies (from Plan 01)."
    - "All three read shared types from types.rs (CatalogEntry.tags/source_kind, DetectedAgent, CategoryKey/Category — from Plan 01); registered in lib.rs."
---

<objective>
Port the three NEW pure deciders from TS to `agentlinux-core` with byte-for-byte
golden-corpus parity — this is the substance of CORE-03:

1. `category.rs` — `deriveCategory` (category.ts) → `derive_category`, corpus =
   `category.test.ts` verbatim.
2. `pin_spec.rs` — `parsePinSpec` (pin.ts:52-74) → `parse_pin_spec` + `PinTarget` enum +
   `PinSpecError`, corpus = `pin.test.ts:117-159` verbatim.
3. `detect_gates.rs` — the PURE decision cores of `isCanonicalAgentPath` / `tryReuse` /
   `tryRemediate` / `detectPresence` (detect.ts), corpus = the deterministic pre-statSync
   branches from `adopt.test.ts` + `list-presence.test.ts`.

Each ported function carries its verbatim TS test table as the Rust `#[test]` golden
module (the parity oracle) plus a totality/determinism proptest. Every version op routes
through `semver_shim` (`valid`/`satisfies` from Plan 01) — never `semver::` directly. The
cache read + the `statSync` re-validation are OUT of scope (Phase-56 adapter seam).

Purpose: make the highest-value decision logic mutation- and property-testable.
Output: three new pure modules registered in `lib.rs`, each with a green golden corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-pure-logic-core-parity/55-RESEARCH.md
@.planning/phases/55-pure-logic-core-parity/55-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `category::derive_category(entry: &CatalogEntry) -> Category` + `TAG_PRECEDENCE` (ORDERED slice) + CATEGORIES table.
- `pin_spec::parse_pin_spec(spec: &str) -> Result<ParsedPin, PinSpecError>` with `PinTarget` enum (`Curated`/`Latest`/`Version(String)`) + two `PinSpecError` variants (`Usage`, `InvalidTarget`).
- `detect_gates`: `is_canonical_agent_path`, `is_at_managed_path`, and the pure `reuse` / `remediate` / `presence` deciders over `(&CatalogEntry, &DetectedAgent, canonical_map, agent_home)` returning `Option<ReuseCandidate>` / `Option<RemediateHit>` / `Option<PresenceHit>`.
Downstream: Phase 56 wires the cache-read adapter + `statSync` around these pure deciders; Phase 56 CLI verbs (pinCmd/adoptCmd/listCmd) consume `parse_pin_spec` + the gates.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port derive_category with the verbatim category.test.ts golden corpus</name>
  <files>rust/crates/agentlinux-core/src/category.rs, rust/crates/agentlinux-core/src/lib.rs</files>
  <read_first>
    - plugin/cli/src/catalog/category.ts (READ IN FULL — the `CATEGORIES` table with labels+orders, the ORDERED `TAG_PRECEDENCE` array, and `deriveCategory`: loop TAG_PRECEDENCE in order returning the first tag present, then source_kind=="mcp" fallback, then Other)
    - plugin/cli/test/category.test.ts:27-72 (the 7 golden assertions — this IS the parity oracle; port each row verbatim)
    - rust/crates/agentlinux-core/src/types.rs (the `CategoryKey`/`Category` types + `CatalogEntry.tags`/`source_kind` added in Plan 01)
    - rust/crates/agentlinux-core/src/reuse.rs (the module-doc + `#[test]`/`proptests` module conventions to mirror)
    - .planning/phases/55-pure-logic-core-parity/55-RESEARCH.md Pitfall 3 (precedence is first-match-wins, ORDER-SENSITIVE — NOT a HashMap) + §"Code Examples" (the ordered-slice port)
  </read_first>
  <behavior>
    - entry(tags=["agent","coding-agent"]) → key "coding-agent" (coding-agent beats bare agent — Pitfall 3)
    - entry(tags=["agent","anthropic"]) → "coding-agent" (bare agent still maps to coding-agent via the last precedence row)
    - entry(tags=["assistant","daemon"]) → "assistant", label "AI assistants"
    - entry(tags=["mcp","docs"], source_kind="mcp") → "mcp"; entry(tags=["docs"], source_kind="mcp") → "mcp" (source_kind fallback)
    - entry(tags=["token","workflow","devops"]) → "workflow" (workflow/token PRECEDE devops — Pitfall 3); entry(tags=["workflow","cost","token"]) → "workflow"
    - entry(tags=["devops","git","github"]) → "devops"; entry(tags=["devops","security","scanner"]) → "devops"
    - entry(tags=["browser","automation","agent-skill"]) → "browser" (browser beats bare agent)
    - entry(tags=["unknowable"]) → "other"; entry(tags=[]) → "other" (never dropped — the Other floor)
  </behavior>
  <action>
    Create `rust/crates/agentlinux-core/src/category.rs` and register it in `lib.rs`
    (`pub mod category;`). Port `deriveCategory` as `pub fn derive_category(entry:
    &CatalogEntry) -> Category`. Port `TAG_PRECEDENCE` as an ORDERED
    `&[(&str, CategoryKey)]` slice iterated IN ORDER (the exact 9 rows from category.ts:44-54,
    same order) — do NOT use a `HashMap` (Pitfall 3: ordering is load-bearing; `workflow`/
    `token` MUST precede `devops` and `coding-agent` MUST precede bare `agent`, so rtk
    ["token","workflow","devops"] resolves `workflow`). Loop the slice returning the first tag
    present in `entry.tags`; then the `source_kind == "mcp"` fallback (compare
    `entry.source_kind.as_deref() == Some("mcp")`); then the `Other` floor so no entry is ever
    dropped. Port the `CATEGORIES` table (labels + display orders from category.ts:30-38) as a
    `category_for(CategoryKey) -> Category` constructor so the returned `Category` carries the
    exact label + order.

    Add a golden `#[test]` module porting every assertion from category.test.ts:27-72 verbatim
    (the 7 `<behavior>` rows), each asserting `derive_category(entry).key` (and label where TS
    asserts it). Add a proptest: `derive_category` is total + deterministic (any CatalogEntry
    yields exactly one Category, two calls agree — the `Other` floor guarantees totality) and
    the precedence-monotonicity property (if a higher-precedence tag is present the result is
    that category regardless of lower tags — the property form of Pitfall 3). Keep the crate
    PURE.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core category 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core category` passes, covering all 7 category.test.ts golden rows + the totality/precedence proptests.
    - `grep -n 'fn derive_category' rust/crates/agentlinux-core/src/category.rs` returns a match; `grep -n 'pub mod category' rust/crates/agentlinux-core/src/lib.rs` returns a match.
    - TAG_PRECEDENCE is an ordered slice, not a map: `grep -n 'HashMap\|BTreeMap' rust/crates/agentlinux-core/src/category.rs` prints nothing.
    - Matches TS corpus: rtk ["token","workflow","devops"] → "workflow" (category.test.ts:46-52); ["agent","coding-agent"] → "coding-agent" (category.test.ts:29).
  </acceptance_criteria>
  <done>
    `derive_category` matches TS `deriveCategory` byte-for-byte across the category.test.ts corpus; precedence is order-preserving (not a map); totality + precedence proptests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port parse_pin_spec + PinTarget with the pin.test.ts golden corpus + 2 error messages</name>
  <files>rust/crates/agentlinux-core/src/pin_spec.rs, rust/crates/agentlinux-core/src/lib.rs</files>
  <read_first>
    - plugin/cli/src/commands/pin.ts:44-74 (READ IN FULL — the `PinTarget` discriminated union carrying `name`, the `parsePinSpec` parser: `eq <= 0` guard, `slice(0,eq)`/`slice(eq+1)`, curated/latest literals, `semver.valid(tgt)` for the version case, and the TWO exact throw messages)
    - plugin/cli/test/pin.test.ts:117-159 (the 8 golden parse cases — this IS the parity oracle; port each verbatim, including which error each bad spec throws)
    - rust/crates/agentlinux-core/src/semver_shim.rs (the `valid` fn added in Plan 01 — call it, do NOT call `semver::` directly)
    - rust/crates/agentlinux-core/src/divergence.rs (the `thiserror` `DivergenceError` pattern to mirror for `PinSpecError`)
    - .planning/phases/55-pure-logic-core-parity/55-RESEARCH.md Pitfall 1 (the `eq <= 0` collapse: `"=curated"` with eq===0 is the empty-name case → Usage message) + Pitfall 2 (the TWO distinct error messages + the subtle `"foo="` empty-target → InvalidTarget) + §"Pattern 2" (discriminated union → Rust enum)
  </read_first>
  <behavior>
    - parse_pin_spec("foo=curated") → Ok(ParsedPin { name:"foo", target: Curated })
    - parse_pin_spec("foo=latest") → Ok(ParsedPin { name:"foo", target: Latest })
    - parse_pin_spec("foo=2.1.7") → Ok(ParsedPin { name:"foo", target: Version("2.1.7") })
    - parse_pin_spec("foo=2.1.7-beta.1") → Ok(..., Version("2.1.7-beta.1")) (prerelease accepted via semver_shim::valid)
    - parse_pin_spec("foo=bogus") → Err(InvalidTarget) whose message contains "invalid target" AND lists curated/latest/semver (pin.test.ts:143-144)
    - parse_pin_spec("no-equals") → Err(Usage) whose message contains "<name>=<target>" (pin.test.ts:148)
    - parse_pin_spec("=curated") → Err(Usage) — empty name, eq===0 (Pitfall 1; pin.test.ts:152, contains "<name>=<target>")
    - parse_pin_spec("foo=") → Err(InvalidTarget) — empty target is not curated/latest/valid-semver (Pitfall 2; pin.test.ts:155-158, contains "invalid target")
  </behavior>
  <action>
    Create `rust/crates/agentlinux-core/src/pin_spec.rs` and register it in `lib.rs`
    (`pub mod pin_spec;`). Define `PinTarget` as a Rust enum with variants `Curated`,
    `Latest`, `Version(String)` (Pattern 2). The TS `PinTarget` ALSO carries `name`
    (`spec.slice(0, eq)`) — keep it: return `struct ParsedPin { name: String, target:
    PinTarget }` from the parser. Define `PinSpecError` (via `thiserror`, mirroring
    `DivergenceError`) with exactly TWO variants: `Usage { spec }` and `InvalidTarget { spec,
    target }`. Their `#[error(...)]` strings MUST contain the exact substrings the corpus greps
    for: the Usage message contains `<name>=<target>` (and mirrors pin.ts:57-59's usage help
    listing curated/latest/semver); the InvalidTarget message contains `invalid target` and
    lists `curated`, `latest`, `semver` (mirrors pin.ts:71-73). Copy the message wording from
    pin.ts so it stays byte-identical.

    Port `parse_pin_spec(spec: &str) -> Result<ParsedPin, PinSpecError>`: find the first `=`
    via `spec.find('=')`; guard `matches!(idx, None | Some(0))` → `PinSpecError::Usage`
    (Pitfall 1 — this catches BOTH "no `=`" AND "empty name" `"=curated"`, exactly the TS
    `eq <= 0`). Split `name = &spec[..idx]`, `tgt = &spec[idx+1..]`. If `tgt == "curated"` →
    `Curated`; if `tgt == "latest"` → `Latest`; else if `semver_shim::valid(tgt).is_some()` →
    `Version(tgt.to_string())`; else → `PinSpecError::InvalidTarget` (Pitfall 2 — the empty
    target `"foo="` lands here, NOT Usage, because `""` is not curated/latest/valid-semver).
    Call `semver_shim::valid` (from Plan 01) — do NOT call `semver::` directly. NOTE: the
    version case keeps the RAW `tgt` string as the version (matching TS `{ ... version: tgt }`
    at pin.ts:69, which stores `tgt`, not the normalized form) — `valid` is used only as the
    accept/reject predicate here.

    Add a golden `#[test]` module porting all 8 pin.test.ts:117-159 cases verbatim, asserting
    the parsed value for the Ok cases and, for the Err cases, that the rendered error message
    CONTAINS the substring the corpus greps for (`/invalid target/`, `/curated.*latest.*semver/`,
    `/<name>=<target>/`) — assert on `err.to_string().contains(...)` so a message drift trips
    the test. Add a proptest: `parse_pin_spec` is TOTAL — for any `&str` it returns `Ok` or a
    `PinSpecError`, never panics (mirrors reuse.rs totality proptest). Keep the crate PURE.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core pin_spec 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core pin_spec` passes, covering all 8 pin.test.ts:117-159 golden rows (incl. both error-message assertions) + the totality proptest.
    - `grep -n 'fn parse_pin_spec' rust/crates/agentlinux-core/src/pin_spec.rs` + `grep -n 'enum PinTarget' ...` + `grep -n 'enum PinSpecError' ...` each return a match; `grep -n 'pub mod pin_spec' rust/crates/agentlinux-core/src/lib.rs` returns a match.
    - Two distinct error variants: the `"=curated"` and `"no-equals"` rows yield a message containing `<name>=<target>`; the `"foo=bogus"` and `"foo="` rows yield a message containing `invalid target` (Pitfalls 1+2).
    - No direct `semver::` use: `grep -n 'semver::' rust/crates/agentlinux-core/src/pin_spec.rs | grep -v '^\s*//'` prints nothing (routes through `semver_shim::valid`).
  </acceptance_criteria>
  <done>
    `parse_pin_spec` matches TS `parsePinSpec` byte-for-byte across the corpus, with the two distinct error messages (Usage vs InvalidTarget) and the `eq <= 0` / empty-target edge cases correct; totality proptest green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the pure detect-gate deciders (reuse/remediate/presence + isCanonicalAgentPath)</name>
  <files>rust/crates/agentlinux-core/src/detect_gates.rs, rust/crates/agentlinux-core/src/lib.rs</files>
  <read_first>
    - plugin/cli/src/detect.ts:32-275 (READ IN FULL — `isCanonicalAgentPath`, `managedBinDir`/`isManagedPath`/`isAtManagedPath` (the source_kind managed-dir heuristic using agentHome), `tryReuse` (pre-statSync gates), `tryRemediate` (broken vs path-mismatch), `detectPresence` (the canonical + adoptable overlay). Note the pure/IO seam: the cache read + statSync are OUT of scope)
    - plugin/cli/test/adopt.test.ts:26-70,194-235 (the tryReuse/remediate corpus fixtures: out-of-window skip, path-mismatch, GSD_SYSTEM_PATH, the four catalog entries with compatibility_window)
    - plugin/cli/test/list-presence.test.ts:24-182 (the detectPresence corpus: healthy-at-canonical → present+adoptable; broken → not present; healthy at non-canonical path → present but not-canonical migration candidate)
    - rust/crates/agentlinux-core/src/semver_shim.rs (`satisfies` + `valid` from Plan 01 — call these, not `semver::`)
    - rust/crates/agentlinux-core/src/types.rs (`DetectedAgent`, `CatalogEntry.source_kind`/`compatibility_window` from Plan 01)
    - rust/crates/agentlinux-core/src/reuse.rs (the DISTINCT provisioner reuse decision — do NOT conflate; §"Reuse Overlap Clarity" in RESEARCH)
    - .planning/phases/55-pure-logic-core-parity/55-RESEARCH.md Pitfall 4 (`!!compatibility_window` JS-falsiness: empty string `""` is NOT adoptable — gate on `.as_deref().is_some_and(|w| !w.is_empty())`) + Pitfall 5 (`tryReuse` uses `isAtManagedPath` / any managed tool; `tryRemediate` is canonical-gated / only ids WITH a canonical entry — DIFFERENT predicates/sources) + Pitfall 6 (`valid` returns the CLEAN version) + Open Q2 (return a slim `ReuseCandidate` = path+version, NO statSync — statSync is the Phase-56 adapter)
  </read_first>
  <behavior>
    - is_canonical_agent_path(entry, path, canonical) → true when path==canonical OR (entry.id=="gsd" AND path==GSD_SYSTEM_PATH passed in) (detect.ts:32-38)
    - reuse gate: entry.compatibility_window empty/None → None (Pitfall 4); detected.status != "healthy" → None; not at managed path → None; version fails valid → None; version not in window → None; else Some(ReuseCandidate { path, version: <clean> }) — NO statSync (Open Q2). GSD at GSD_SYSTEM_PATH in-window → Some; gsd out-of-window (e.g. 1.36.0 vs ">=1.37.0 <2.0.0") → None (adopt.test.ts:194-203)
    - remediate gate (canonical-gated: id must have a canonical entry, else None): status=="broken" → Some(RemediateHit{reason:"broken", detected_version: None}); status=="healthy" AND NOT is_canonical_agent_path → Some(RemediateHit{reason:"path-mismatch", detected_version: valid(version)}); healthy at canonical → None (detect.ts:202-224). claude at /home/agent/.npm-global/bin/claude with canonical native path → path-mismatch (adopt.test.ts:206-227)
    - presence gate: source_kind=="mcp" → None; detected None or status!="healthy" → None (broken → None, list-presence.test.ts:122-131); else PresenceHit{ version: valid(version), path, canonical: is_at_managed_path(...), adoptable: canonical AND window-nonempty AND version-in-window }. healthy at canonical in-window → present + canonical + adoptable (list-presence.test.ts:92-101); healthy at non-canonical path → present + NOT canonical (migration candidate, list-presence.test.ts:135-149)
  </behavior>
  <action>
    Create `rust/crates/agentlinux-core/src/detect_gates.rs` and register it in `lib.rs`
    (`pub mod detect_gates;`). This module ports the PURE decision cores only — the cache read
    (`readCachedAgentById`/`readDetectedAgent`/`detectCachePath`) and the `tryReuse` host
    `statSync` re-validation are OUT of scope (Phase-56 adapter). Do NOT add `std::fs`.

    Port `is_canonical_agent_path(entry: &CatalogEntry, path: &str, canonical: &str,
    gsd_system_path: &str) -> bool` = `path == canonical || (entry.id == "gsd" && path ==
    gsd_system_path)`. The `GSD_SYSTEM_PATH` constant + the `CANONICAL_PATHS` map stay
    DUPLICATED (Phase-57 consolidation) — pass them IN as parameters here (do NOT hardcode a
    map inside a decider; mirror `reuse::agent_decision` taking `canonical`/`gsd_system_path`).
    Port `managed_bin_dir`/`is_managed_path`/`is_at_managed_path` as pure helpers taking the
    resolved `agent_home: &str` as a parameter (TS reads `AGENTLINUX_AGENT_HOME` — that env read
    stays in the Phase-56 adapter; the pure fn receives the resolved home string). `managed_bin_dir`
    returns `~/.npm-global/bin` for npm, `~/.local/bin` for binary/script, None for mcp/other
    (detect.ts:51-61).

    Port the three gate deciders as pure functions over `(&CatalogEntry, &DetectedAgent,
    canonical: Option<&str>, gsd_system_path, agent_home)`:
      - reuse gate → `Option<ReuseCandidate>` where `ReuseCandidate { path: String, version:
        String }` (Open Q2 — slim: path + CLEAN version, NO statSync/ReuseHit; the Phase-56
        adapter statSyncs + builds the final ReuseHit). Gates, in TS order: window nonempty
        (Pitfall 4 — treat `""` as absent via `.as_deref().is_some_and(|w| !w.is_empty())`),
        status=="healthy", `is_at_managed_path`, `semver_shim::valid(version)` yields Some
        (use the clean version — Pitfall 6), `semver_shim::satisfies(clean, window)`.
      - remediate gate → `Option<RemediateHit>` (`RemediateHit { reason: &'static str /* or an
        enum */, detected_path, canonical_path, detected_version: Option<String> }`). CANONICAL-
        GATED (Pitfall 5): if `canonical` is None → None. Then: status=="broken" → broken hit
        (detected_version None); status=="healthy" && !is_canonical_agent_path → path-mismatch hit
        (detected_version = `semver_shim::valid(version)`); else None.
      - presence gate → `Option<PresenceHit>` (`PresenceHit { version: Option<String>, path:
        String, canonical: bool, adoptable: bool }`). source_kind=="mcp" → None;
        status!="healthy" → None; else `canonical = is_at_managed_path(...)`, `version =
        semver_shim::valid(version)`, `adoptable = canonical && window-nonempty (Pitfall 4,
        `!!` truthiness) && version.is_some() && satisfies(version, window)`.

    KEEP `detect_gates` DISTINCT from `reuse.rs`: `reuse::agent_decision` is the provisioner's
    3-way Reuse/Remediate/Create dispatch that does NOT evaluate semver; these gates evaluate
    the compatibility_window via `satisfies` (RESEARCH §"Reuse Overlap Clarity"). Do not
    re-port or conflate.

    Add a golden `#[test]` module encoding the pre-statSync deterministic rows from
    adopt.test.ts + list-presence.test.ts as `(entry, detected) → verdict` cases: the
    in-window reuse hit, the out-of-window skip (gsd 1.36.0 vs ">=1.37.0 <2.0.0"), the
    path-mismatch remediate (claude at npm-global path), the broken remediate, the
    healthy-at-canonical presence (present+adoptable), the broken → not-present, the
    non-canonical → present-not-canonical, and the `compatibility_window: ""` → not-adoptable
    (Pitfall 4) row. Add one proptest per decider: totality (any `(entry, detected)` → an
    `Option`, never panics). Keep the crate PURE.
  </action>
  <verify>
    <automated>cd rust && . "$HOME/.cargo/env" && cargo test -p agentlinux-core detect_gates 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd rust && cargo test -p agentlinux-core detect_gates` passes, covering the reuse/remediate/presence golden rows (incl. the out-of-window skip, path-mismatch, broken, non-canonical-present, and the `compatibility_window:""` non-adoptable row) + the totality proptests.
    - `grep -n 'fn is_canonical_agent_path\|struct ReuseCandidate\|fn.*remediate\|fn.*presence' rust/crates/agentlinux-core/src/detect_gates.rs` returns matches; `grep -n 'pub mod detect_gates' rust/crates/agentlinux-core/src/lib.rs` returns a match.
    - No I/O leaked into the pure core: `grep -n 'std::fs\|statSync\|read_to_string\|std::env' rust/crates/agentlinux-core/src/detect_gates.rs` prints nothing.
    - No direct `semver::`: `grep -n 'semver::' rust/crates/agentlinux-core/src/detect_gates.rs | grep -v '^\s*//'` prints nothing (routes through semver_shim).
    - Matches TS corpus: gsd@GSD_SYSTEM_PATH in-window is a reuse hit; gsd 1.36.0 out-of-window is skipped (adopt.test.ts:194-203); claude@npm-global path is a path-mismatch remediate (adopt.test.ts:206-227); `compatibility_window:""` is NOT adoptable (Pitfall 4).
  </acceptance_criteria>
  <done>
    The pure detect-gate deciders match TS `tryReuse`/`tryRemediate`/`detectPresence`/`isCanonicalAgentPath` verdicts across the corpus, with NO cache read / statSync in the core, the canonical map passed in as params, and the `compatibility_window` JS-falsiness handled; distinct from `reuse.rs`; totality proptests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detect cache record → pure gate deciders | Untrusted `DetectedAgent` (status/path/version) + user pin spec cross into the pure deciders; all parse via `semver_shim` without panic (T-53-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-03 | Tampering | `parse_pin_spec` | medium | mitigate | The parser rejects malformed/partial/range targets via `semver_shim::valid` (InvalidTarget error) so only a valid version point flows onward — the pin-safety invariant pin.ts:29-32 relies on; totality proptest covers adversarial specs. |
| T-55-04 | Elevation of Privilege | detect-gate reuse decider | medium | mitigate | The reuse candidate is only produced for a healthy, at-managed-path, in-window agent (window-nonempty gate + `satisfies`), so an out-of-window or mislocated brownfield binary is never surfaced as adoptable; the statSync existence re-check remains a Phase-56 adapter gate. |
| T-55-05 | Denial of Service | detect-gate deciders | low | mitigate | All deciders route version/range ops through `semver_shim` (total, malformed→false/None); totality proptests assert no panic on adversarial `DetectedAgent` input. |
</threat_model>

<verification>
- `cd rust && cargo test -p agentlinux-core category pin_spec detect_gates` green (all three golden corpora + proptests).
- Each new module is registered in `lib.rs`; the crate stays PURE (no std::fs/env/process; no direct `semver::`).
- The three modules touch disjoint new files + a single shared `lib.rs` mod-registration edit.
</verification>

<success_criteria>
- `derive_category`, `parse_pin_spec`, and the three detect-gate deciders match their TS counterparts byte-for-byte across the ported golden corpora (CORE-03).
- The six research pitfalls (ordered precedence, two error messages, `!!compatibility_window`, strict `valid`, tryReuse-vs-tryRemediate sources, clean-version normalization) are honored and named in the tasks.
- The crate stays PURE; cache read + statSync remain the Phase-56 seam.
</success_criteria>

<output>
Create `.planning/phases/55-pure-logic-core-parity/55-02-SUMMARY.md` when done
</output>
