---
phase: 57-provisioner-port-logic-consolidation
plan: 06
type: execute
wave: 5
depends_on: ["57-05"]
autonomous: true
files_modified:
  - rust/crates/agentlinux/src/provision/registry_cli.rs
  - rust/crates/agentlinux/src/provision/probe.rs
  - rust/crates/agentlinux/src/cmd/provision.rs
  - rust/crates/agentlinux/src/provision/mod.rs
  - plugin/lib/reuse/agents.sh
  - plugin/lib/remediate.sh
  - plugin/lib/prompt.sh
requirements: [PROV-01, PROV-02, PROV-03, GATE-01, GATE-05]
must_haves:
  truths:
    - "`provision/registry_cli.rs` stages `/opt/agentlinux/cli/<ver>/{dist,node_modules,package.json}` + `/opt/agentlinux/catalog/<ver>/` + `/opt/agentlinux/state/installed.d/` (empty — CAT-02) + the `~/.npm-global/bin/agentlinux` symlink → dist/index.js, verified executable as the install user — keeping the TS-bundle staging observable-identical to master (Q1: defer the musl-binary swap to Phase 58)."
    - "The entrypoint OWNS the full detect→decide→act flow: it probes user/nodejs/npm-prefix/agents/sudoers into the detect cache, calls the ALREADY-PORTED pure gates (`reuse_gate`/`remediate_gate`/`agent_decision`) to build the `Resolutions` map (replacing the Wave-1..4 seeds), gates state-overwrites behind `--yes`, flushes bails (exit 65) BEFORE any mutation, then runs the ordered steps + `agentlinux adopt --all`."
    - "PROV-02: the entrypoint iterates the Rust `canonical_path` map's ids directly (no per-id `agentlinux reuse-decision` shell-out), and the Bash `REUSE_AGENT_CANONICAL_PATHS` + `REUSE_GSD_SYSTEM_PATH` map + the `reuse::agent_decision` Rust-else-bash shim + `remediate.sh`/`prompt.sh` iterators are DELETED — the PROV-02 grep gate (57-01) now exits ZERO."
    - "`provision --purge` (the 7-step teardown) + `--dry-run` + `--report-only` parity LAND in-phase (Q3), so the `10-installer.bats` / `23-install-user.bats` flag-path @tests are green on the Rust provisioner."
    - "The full provisioner-surface bats (10-installer, 23-install-user, 22-agent-sudo, 20-agent-user, 30-runtime non-QEMU, 50-agents non-gated, 13-reuse, 14-remediate, 15-detection, 18-{distro-detect,pkg-dispatch,detect-el9}) are GREEN on the Rust provisioner across the 4-distro matrix per-file, with the systemd/cron/QEMU remainder enumerated as Phase-59 — NO regression / NO newly-skipped (GATE-01)."
  artifacts:
    - rust/crates/agentlinux/src/provision/registry_cli.rs
    - .planning/phases/57-provisioner-port-logic-consolidation/57-06-SUMMARY.md
  key_links:
    - "`cmd/provision.rs` swaps the Wave-1..4 seeded `Resolutions` for the real ported-gate computation here — the detect probes feed `agentlinux-core` gates, closing the decide-then-act loop."
    - "Deleting the Bash map turns off the last PROV-02 duplication; the 57-01 grep gate flips from red→green, and `13-reuse`/`14-remediate` must stay green via the entrypoint's direct iteration (no shell-out shim)."
    - "This wave runs ALL provisioner bats end-to-end (the 57-01 run.sh seam) — catching cross-step shared-state gaps the per-step waves missed — and consolidates the Phase-59 QEMU-only remainder."
---

<objective>
Wave 5 — the phase closeout. Deliver the last step + the decision wiring + the
PROV-02 deletion + the teardown/preview parity + the full-surface bats
verification:

1. **`provision/registry_cli.rs`** — the `50-registry-cli.sh` port (stage the TS
   bundle + catalog + state dir + symlink, verified as the install user). Q1:
   keep TS-bundle staging (defer the musl swap to Phase 58).
2. **The detect→decide→act wiring + PROV-02 deletion** — the entrypoint owns the
   per-agent iteration via the ported gates, and the Bash `CANONICAL_PATHS`/
   `GSD_SYSTEM_PATH` map + shim + iterators are DELETED (the 57-01 grep gate
   flips green). Plus `--purge`/`--dry-run`/`--report-only` parity (Q3).
3. **Full-surface bats closeout** — every provisioner bats file green on the Rust
   provisioner across the 4-distro matrix per-file, GATE-01, with the Phase-59
   QEMU remainder enumerated.

Purpose: close PROV-01/02/03 + GATE-01/05 — the Rust provisioner leaves
identical observable state, the last Bash duplication is gone, and the full bats
surface is green.
Output: `registry_cli.rs`, the wired entrypoint, the deleted Bash map/shim/
iterators, and the 4-distro per-file green record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-provisioner-port-logic-consolidation/57-RESEARCH.md
@.planning/phases/57-provisioner-port-logic-consolidation/57-VALIDATION.md
@.planning/phases/57-provisioner-port-logic-consolidation/57-01-PLAN.md
@.planning/phases/57-provisioner-port-logic-consolidation/57-02-PLAN.md
@.planning/phases/57-provisioner-port-logic-consolidation/57-05-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `rust/crates/agentlinux/src/provision/registry_cli.rs` — the `50-registry-cli.sh` port (TS-bundle staging, symlink, state dir, as-user verify).
- `rust/crates/agentlinux/src/provision/probe.rs` — the thin detect readers (user/nodejs/npm-prefix/agents/sudoers) that feed the pure gates (reuse `cache.rs` from Phase 56 for the detect-cache read).
- `rust/crates/agentlinux/src/cmd/provision.rs` — the real detect→decide→act wiring (replace the Wave-1..4 `Resolutions` seeds); `--purge`/`--dry-run`/`--report-only` parity; the per-agent iteration over the Rust `canonical_path` ids; `adopt --all` post-provision.
- DELETIONS in `plugin/lib/reuse/agents.sh` (the `REUSE_AGENT_CANONICAL_PATHS` + `REUSE_GSD_SYSTEM_PATH` map + the `reuse::agent_decision` shim + `_agent_decision_bash` fallback), `plugin/lib/remediate.sh` (the `${!REUSE_AGENT_CANONICAL_PATHS[@]}` iterator at :286-296), `plugin/lib/prompt.sh` (the same iterator at :110-114).
- `.planning/phases/57-provisioner-port-logic-consolidation/57-06-SUMMARY.md` — the per-step→bats green matrix + the Phase-59 deferral + the PROV-01/02/03 + GATE-01/05 sign-off.
Downstream: `/gsd-verify-work` consumes the green-bats + the deferral record; Phase 58 (DIST-01) swaps the TS-bundle staging for the musl binary.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: registry_cli.rs (TS-bundle staging) + detect→decide→act wiring + PROV-02 Bash-map deletion + --purge/--dry-run parity</name>
  <files>rust/crates/agentlinux/src/provision/registry_cli.rs, rust/crates/agentlinux/src/provision/probe.rs, rust/crates/agentlinux/src/cmd/provision.rs, rust/crates/agentlinux/src/provision/mod.rs, plugin/lib/reuse/agents.sh, plugin/lib/remediate.sh, plugin/lib/prompt.sh</files>
  <read_first>
    - plugin/provisioner/50-registry-cli.sh (READ IN FULL — the stage layout `/opt/agentlinux/cli/<ver>/{dist,node_modules,package.json}` + `/opt/agentlinux/catalog/<ver>/` + `/opt/agentlinux/state/installed.d/` empty [CAT-02] + the `~/.npm-global/bin/agentlinux → dist/index.js` symlink [:124] + the `as_user test -x` verify [:137-140]; the malformed-tarball sanity checks; Q1: keep symlinking the TS bundle — the run.sh harness re-points it at the Rust bin under the flag)
    - plugin/bin/agentlinux-install (READ :452-613 the FULL main() order to reproduce: parse_args → validate user → require_root → user_adoptable gate → --purge branch → detect_distro → ensure_jq → detect::run_once → --report-only short-circuit → collect_all_decisions → --dry-run early-return → flush_bails_or_continue → run_provisioners → run_agent_adoption; AND :363-450 `run_purge` [the 7 ordered steps]; AND :341-346 `run_agent_adoption`)
    - plugin/lib/remediate.sh:264-299 (READ — `collect_all_decisions` builds RESOLUTIONS[{user,node,npm-prefix,sudoers,agents.<id>}] from DETECT_* via the gates + the `${!REUSE_AGENT_CANONICAL_PATHS[@]}` iterator at :286-296 you DELETE) + plugin/lib/prompt.sh:105-114 (the same iterator you DELETE) + plugin/lib/reuse/agents.sh:38-108 (the map + the shim + the _agent_decision_bash fallback you DELETE)
    - rust/crates/agentlinux/src/main.rs:35-49 (the Rust `canonical_path(id)` map + `GSD_SYSTEM_PATH` const — the SINGLE source the entrypoint iterates: ids claude-code/gsd/playwright-cli) + rust/crates/agentlinux-core/src/detect_gates.rs (the ported `reuse_gate`/`remediate_gate`/`presence_gate`) + rust/crates/agentlinux-core/src/reuse.rs (`agent_decision`) + rust/crates/agentlinux/src/cache.rs (the Phase-56 detect-cache read `read_cache_agents`/`read_detected_agent`)
    - .planning/phases/57-provisioner-port-logic-consolidation/57-RESEARCH.md §"PROV-02 Map Consolidation" (the three Bash sites + two consumers + the zero-Bash-consumer target) + §"The Detect / Remediate / Reuse Wiring" (the probe→cache→gate→Resolutions→remediate-I/O flow) + §"Runtime State Inventory" (the `--purge` parity requirement) + §"Each Provisioner Step → 50-registry-cli.sh" (Q1: keep TS-bundle staging)
    - scripts/check-no-bash-canonical-map.sh (from 57-01 — the gate this task must flip to green by deleting the Bash map)
  </read_first>
  <action>
    Four moves in one task (they are interdependent — the deletion is only safe
    once the entrypoint owns the iteration):

    **A. `provision/registry_cli.rs`** — `pub fn run(ctx) -> io::Result<()>`
    porting `50-registry-cli.sh`: the malformed-tarball sanity checks, stage the
    CLI bundle (dist + node_modules + package.json) under
    `/opt/agentlinux/cli/<AGENTLINUX_VERSION>/`, stage the catalog under
    `/opt/agentlinux/catalog/<ver>/`, create `/opt/agentlinux/state/installed.d/`
    EMPTY (CAT-02) owned by the install user, `ensure_dir ~/.npm-global/bin`, then
    `ln -sfn <dist/index.js> ~/.npm-global/bin/agentlinux` + `chown -h <user>:<user>`,
    then verify `test -x` AS the install user via the Phase-56 dispatcher
    (`as_user`). Q1: keep symlinking the TS bundle's `dist/index.js` — do NOT
    stage the musl binary (Phase 58 owns that). Register `pub mod registry_cli;`
    + `pub mod probe;` in `provision/mod.rs`; slot `registry_cli::run` last in the
    step vec (replace the Wave-5 marker).

    **B. `provision/probe.rs` + the detect→decide→act wiring in `cmd/provision.rs`.**
    Add thin readers (user exists/home/uid; node version + NodeSource repo
    presence via `pkg::nodesource_repo_paths`; npm-prefix writable by the install
    user; per-agent status+path; sudoers line present) that write the detect-cache
    JSON (reuse `cache.rs` shape). In `cmd/provision.rs` REPLACE the Wave-1..4
    `Resolutions` seeds with the real computation: run the probes → call the
    ported gates (`agentlinux_core::detect_gates::{reuse_gate,remediate_gate}` +
    `reuse::agent_decision`) with the probed inputs → build the full `Resolutions`
    map INCLUDING the per-agent entries by iterating the Rust `canonical_path` ids
    (claude-code, gsd, playwright-cli) DIRECTLY (no per-id `agentlinux
    reuse-decision` shell-out — the entrypoint calls the pure gate in-process).
    Gate state-overwriting remediations behind `--yes`; accumulate bails and exit
    65 BEFORE any step runs (`flush_bails_or_continue` equivalent). After the
    steps, run `agentlinux adopt --all` as the install user via the dispatcher
    (best-effort — a failure must not fail the install).

    **C. `--purge`/`--dry-run`/`--report-only` parity (Q3).** Port `run_purge`
    (the 7 ordered steps: per-agent uninstall.sh via the catalog snapshot keyed by
    sentinel id [NOT sentinel JSON], `rm -rf /opt/agentlinux/`, rm the PATH
    artefacts + sudoers drop-in, rm NodeSource repo files via
    `nodesource_repo_paths`, optional `pkg_remove nodejs` on `--remove-nodejs`,
    `userdel -r` the charset-validated user, rm the install log LAST). Every rm
    target is a LITERAL absolute path; only the validated user home feeds
    `userdel -r` (never `rm -rf $VAR`). Wire `--report-only` (emit the detection
    report, exit 0) + `--dry-run` (collect decisions, print the report, exit 0, ZERO
    mutation) into `cmd/provision.rs`.

    **D. DELETE the Bash duplication (PROV-02).** Now that the entrypoint owns the
    iteration, delete from `plugin/lib/reuse/agents.sh` the
    `REUSE_AGENT_CANONICAL_PATHS` map + `REUSE_GSD_SYSTEM_PATH` const + the
    `reuse::agent_decision` Rust-else-bash shim + `reuse::_agent_decision_bash`
    fallback; delete the `${!REUSE_AGENT_CANONICAL_PATHS[@]}` iterators in
    `remediate.sh:286-296` and `prompt.sh:110-114`. Confirm no bats @test greps
    `REUSE_AGENT_CANONICAL_PATHS` directly (RESEARCH A2 verified only the two libs
    consume it — re-grep to confirm). The 57-01 grep gate
    (`scripts/check-no-bash-canonical-map.sh`) must now exit ZERO.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash scripts/check-no-bash-canonical-map.sh && echo "PROV-02-GATE-GREEN" && ! grep -rn 'REUSE_AGENT_CANONICAL_PATHS\|_agent_decision_bash' plugin/lib/ | grep -v '^[^:]*:[0-9]*:[[:space:]]*#' && for d in ubuntu-24.04 almalinux-9; do echo "== $d =="; AGENTLINUX_PROVISION_RUST=1 ./tests/docker/run.sh $d 10-installer 2>&1 | tail -8; AGENTLINUX_PROVISION_RUST=1 ./tests/docker/run.sh $d 13-reuse 2>&1 | tail -6; done</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-bash-canonical-map.sh` now exits ZERO (`PROV-02-GATE-GREEN`) — no live Bash `REUSE_AGENT_CANONICAL_PATHS`/`REUSE_GSD_SYSTEM_PATH` definition survives; the shim + `_agent_decision_bash` + the two iterators are deleted (the grep for them, filtered of comments, is empty).
    - `10-installer.bats` (entrypoint flags/usage/exit-codes incl. `--purge`/`--dry-run`/`--report-only`) + `13-reuse.bats` (the reuse decision, now driven by the in-process gate not the shim) are GREEN on the Rust provisioner on BOTH ubuntu-24.04 AND almalinux-9.
    - `registry_cli.rs` stages the TS bundle (Q1 — symlink → `dist/index.js`, NOT the musl binary) + the empty `installed.d/` (CAT-02) + verifies the symlink `test -x` as the install user.
    - The entrypoint iterates the Rust `canonical_path` ids DIRECTLY, calling the ported gate in-process (no per-agent shell-out that spawns a fresh `agentlinux` subprocess). Assert positively: `grep -qE 'canonical_path|reuse_gate|agent_decision' rust/crates/agentlinux/src/cmd/provision.rs` matches (the in-process gate call). The per-agent loop must NOT build a `Command::new` that re-invokes the bin's own decision verb — confirm by reading the loop, not by a bare literal negative-grep.
    <!-- planner-discipline-allow: reuse-decision -->
    - `--purge` targets are LITERAL absolute paths; only the charset-validated user home feeds `userdel -r` (no `rm -rf $VAR`) — asserted by reading the ported `run_purge`.
    - `cargo fmt --all --check` clean; the deletions touch only `plugin/lib/{reuse/agents.sh,remediate.sh,prompt.sh}` + the Rust additions under `rust/`.
  </acceptance_criteria>
  <done>
    `registry_cli.rs` stages the TS bundle (Q1) + symlink + empty state dir; the entrypoint owns the detect→decide→act flow via the ported gates iterating the Rust map directly; the Bash `CANONICAL_PATHS`/`GSD_SYSTEM_PATH` map + shim + iterators are DELETED (PROV-02 grep gate green); `--purge`/`--dry-run`/`--report-only` parity lands (Q3); `10-installer` + `13-reuse` green on the apt/dnf pair.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full provisioner-surface bats green on the Rust build across the 4-distro matrix + PROV-01/02/03 + GATE-01/05 sign-off</name>
  <files>rust/crates/agentlinux/src/provision/registry_cli.rs, rust/crates/agentlinux/src/provision/probe.rs, rust/crates/agentlinux/src/cmd/provision.rs</files>
  <read_first>
    - tests/bats/23-install-user.bats (READ IN FULL — the alt-user install [AL-50] + the INST-02-under-flag item this phase resolves; run WHOLE against the Rust provisioner) + tests/bats/14-remediate.bats + tests/bats/15-detection.bats (READ IN FULL — the remediate decisions/actions + the detect probes/report; enumerate the cases needing real host state vs Docker-runnable) + tests/bats/50-agents.bats (READ — the non-gated AGT cases vs the network/systemd-gated `skip` set → Phase 59)
    - tests/bats/18-distro-detect.bats + tests/bats/18-pkg-dispatch.bats + tests/bats/18-detect-el9.bats (READ — these UNIT-SOURCE the Bash libs; Open Q2: decide per-file shim-vs-rewrite. Recommendation: since Wave 5 deletes the reuse map but NOT distro_detect.sh/pkg.sh [those Bash libs stay for the Bash entrypoint on master], the 18-* @tests keep unit-sourcing the Bash libs unchanged — they are NOT orphaned by this phase. Confirm no 18-* @test sources the deleted reuse map)
    - .planning/phases/57-provisioner-port-logic-consolidation/57-RESEARCH.md §"Acceptance Oracle + Staging" (the bats file map + Docker-now vs QEMU/Phase-59 split: systemd_user+cron modes of 30/50, AGT-02 self-update, full 22/24/26+EL9 QEMU → Phase 59) + §"Assumptions Log" (A2 map-deletion safety, A3 flag-path parity, A5 log-transcript fidelity) + 57-VALIDATION.md §"Manual-Only Verifications" + §"Validation Sign-Off"
    - .planning/phases/57-provisioner-port-logic-consolidation/57-VALIDATION.md (READ IN FULL — set `nyquist_compliant: true` + `wave_0_complete: true` here; the sampling-continuity + per-step-bats-green checklist to sign off)
  </read_first>
  <action>
    Run each provisioner bats file WHOLE against the staged Rust provisioner
    (`AGENTLINUX_PROVISION_RUST=1`), per-file (Docker OOM — NEVER the full
    `tests/bats/` dir in this VM), on the 4-distro matrix (ubuntu-22.04,
    ubuntu-24.04, ubuntu-26.04, almalinux-9 — at minimum ubuntu-24.04 +
    almalinux-9 as the apt/dnf floor, and the ubuntu-22.04/26.04 spot-checks for
    the distro-specific cases). For each failure, triage:
    - **Genuine cross-step parity gap** (a byte mismatch, wrong exit, or a
      shared-state/ordering interaction only visible when a whole file runs — e.g.
      a `provision` run that leaves a sentinel a later detect case reads, or a
      `--purge` teardown a later install case depends on) → fix the corresponding
      `provision/*.rs`.
    - **Network/systemd-gated `skip`** (30-runtime systemd_user+cron, 50-agents
      real-install/systemd, AGT-02 self-update) → leave skipped, RECORD as Phase-59.
    Do NOT edit the bats files (they are the spec). Do NOT weaken a gate. Do NOT
    silently fall back to the Bash provisioner (the 57-01 fail-loud guard prevents
    a false-green).

    Continue until the Docker-runnable provisioner surface is GREEN on the Rust
    provisioner with NO newly-skipped test vs. the Bash provisioner: 10-installer,
    13-reuse, 14-remediate (Docker subset), 15-detection, 18-{distro-detect,
    pkg-dispatch,detect-el9}, 20-agent-user, 22-agent-sudo, 23-install-user,
    30-runtime (interactive/ssh/sudo_u/sudo_u_i modes), 50-agents (non-gated).
    Confirm the Bash provisioner (master) is NOT regressed (`git diff --name-only`
    shows the deletions in `plugin/lib/` + the Rust additions, no other plugin/
    churn; the flag-unset run.sh path still runs the Bash entrypoint).

    Produce `57-06-SUMMARY.md`: (a) the per-step→bats-file green matrix
    (agent_user→20; sudoers→22; nodejs→30 RT-01/RT-04; path_wiring→30 RT-02+BHV
    Docker modes; registry_cli→10/50; the wiring→13/14/15; distro/pkg→18-*); (b)
    the consolidated Phase-59-deferred set (systemd_user+cron modes of 30/50,
    AGT-02 self-update, full 22/24/26+EL9 QEMU) — enumerated file + @test + gate
    reason; (c) the PROV-01/02/03 + GATE-01/05 sign-off (PROV-01: observable state
    identical across the Docker modes; PROV-02: Bash map deleted, grep gate green,
    single Rust source; PROV-03: distro-detect + apt/dnf parity green on both
    families). Set `57-VALIDATION.md` `nyquist_compliant: true` + `wave_0_complete:
    true`.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && for f in 15-detection 18-distro-detect 18-pkg-dispatch 23-install-user 50-agents; do for d in ubuntu-24.04 almalinux-9; do echo "== $f @ $d =="; AGENTLINUX_PROVISION_RUST=1 ./tests/docker/run.sh $d $f 2>&1 | tail -6; done; done && AGENTLINUX_PROVISION_RUST=1 ./tests/docker/run.sh ubuntu-26.04 18-pkg-dispatch 2>&1 | tail -6 && AGENTLINUX_PROVISION_RUST=1 ./tests/docker/run.sh almalinux-9 18-detect-el9 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The Docker-runnable provisioner surface (10/13/14-subset/15/18-*/20/22/23/30-Docker-modes/50-non-gated) is GREEN on the Rust provisioner across ubuntu-24.04 + almalinux-9 (+ ubuntu-22.04/26.04 spot-checks for distro-specific cases), per-file, with NO red and NO newly-skipped vs. the Bash provisioner (GATE-01).
    - The bats files are UNCHANGED (`git diff --name-only tests/bats/` empty); no gate weakened.
    - Every systemd/cron/network-gated `skip` is enumerated in `57-06-SUMMARY.md` (file + @test + gate reason) as the Phase-59 QEMU remainder — no gated case claimed as a Rust pass.
    - `57-06-SUMMARY.md` records the per-step→bats matrix + the PROV-01/02/03 + GATE-01/05 sign-off; `57-VALIDATION.md` `nyquist_compliant: true` + `wave_0_complete: true` set.
    - The Bash provisioner (master) is not regressed: only `plugin/lib/{reuse/agents.sh,remediate.sh,prompt.sh}` deletions + `rust/` additions changed; the flag-unset run.sh path still runs the Bash entrypoint (GATE-05).
    - `cd rust && cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check` clean; `grep -rn 'std::process\|std::fs\|std::env' rust/crates/agentlinux-core/src/` prints nothing (pure core stayed pure).
  </acceptance_criteria>
  <done>
    The full Docker-runnable provisioner-surface bats is green on the Rust provisioner across the apt/dnf matrix (+ distro spot-checks) with no regression / no newly-skipped; the Phase-59 QEMU remainder is enumerated; PROV-01/02/03 + GATE-01/05 are signed off; VALIDATION.md is flipped to nyquist-compliant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detect probes → pure gates → Resolutions | Untrusted host state (npm prefix perms, node version, sudoers line) feeds the decision; the pure gates + the `--yes` mutation gate + the bail-before-mutate split bound what a hostile host state can trigger. |
| sentinel JSON → `--purge` per-agent uninstall | A tampered sentinel must not pick which scripts run as agent; recipe paths derive from the catalog snapshot keyed by sentinel ID, not the sentinel JSON. |
| Rust `canonical_path` map → per-agent iteration | After the Bash deletion, the Rust map is the SOLE per-agent enumerator; a drift would silently drop an agent — the grep gate + the 13-reuse/14-remediate bats bound this. |
| flag-unset run.sh → Bash entrypoint (rollback) | master keeps the Bash provisioner authoritative; a red Rust provisioner phase cannot block a master hotfix (GATE-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-15 | Tampering | `--purge` sentinel → uninstall.sh | high | mitigate | Recipe paths derive from the catalog snapshot keyed by sentinel id (NOT sentinel JSON), so a tampered sentinel cannot pick scripts to run as agent; every rm target is a literal absolute path; only the charset-validated user home feeds `userdel -r`. Task 1 asserts the literal-path + no-`rm -rf $VAR` shape. |
| T-57-16 | Spoofing | full-surface false-green | high | mitigate | Per-file bats against the STAGED Rust provisioner with the 57-01 fail-loud guard (a missing bin aborts, never falls back to Bash); no gate weakened; the gated set is enumerated, not silently passed. Task 2 asserts unchanged bats + only-expected-churn. |
| T-57-17 | Tampering | PROV-02 map desync post-deletion | medium | mitigate | The Rust `canonical_path` map is the sole per-agent enumerator; the 57-01 grep gate fails if any live Bash map survives, and 13-reuse/14-remediate stay green via the in-process gate — a drift would red the bats. Task 1 flips the gate green + asserts the bats. |
| T-57-18 | Elevation | Elevation via `provision` non-root | high | mitigate | The entrypoint's `require_root` guard (57-02) still fronts the whole flow; combined with the `--yes` mutation gate + bail-before-mutate, a non-root or non-consenting invoker cannot trigger a state overwrite. |
| T-57-SC | Tampering | crates.io installs (no new crates) | low | accept | No new dependencies this plan; the TS-bundle staging is Q1-deferred to Phase 58 for the musl swap. |
</threat_model>

<verification>
- `scripts/check-no-bash-canonical-map.sh` exits ZERO (PROV-02 map deleted, single Rust source); no live Bash map/shim/iterator survives.
- `10-installer` + `13-reuse` + `14-remediate` + `15-detection` + `18-*` + `20`/`22`/`23`/`30`-Docker-modes/`50`-non-gated GREEN on the Rust provisioner across the apt/dnf matrix (+ distro spot-checks) — no regression / no newly-skipped (GATE-01).
- `--purge`/`--dry-run`/`--report-only` parity ported (Q3); `registry_cli.rs` stages the TS bundle (Q1).
- The Phase-59 QEMU remainder (systemd/cron modes, AGT-02, full matrix) enumerated; `57-VALIDATION.md` flipped to `nyquist_compliant: true`.
- `cargo test --workspace && cargo clippy … -- -D warnings && cargo fmt --all --check` clean; the pure core stayed pure; master's Bash path unchanged (GATE-05).
</verification>

<success_criteria>
- `registry_cli.rs` stages the TS bundle + symlink + empty state dir observable-identical to master (Q1); the entrypoint owns the detect→decide→act flow via the ported gates.
- PROV-02: the Bash `CANONICAL_PATHS`/`GSD_SYSTEM_PATH` map + shim + iterators are deleted, single Rust source, grep gate green.
- `--purge`/`--dry-run`/`--report-only` parity lands (Q3); the full Docker-runnable provisioner-surface bats is green on the Rust provisioner across the apt/dnf matrix (GATE-01), the Phase-59 remainder enumerated.
- PROV-01/02/03 + GATE-01/05 signed off; the pure core stayed pure; master stays shippable (GATE-05).
</success_criteria>

<output>
Create `.planning/phases/57-provisioner-port-logic-consolidation/57-06-SUMMARY.md` when done
</output>
