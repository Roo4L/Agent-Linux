---
phase: 28-rtk
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - tests/bats/57-catalog-binary.bats
  - docs/internals/catalog.md
autonomous: true
requirements: [ENABLE-01, WORK-02, OPS-01]
must_haves:
  truths:
    - "A bats @test drives the full rtk lifecycle: install → checksum-verified fetch → rtk --version 0.42.4 → binary under ~/.local/bin (no /usr/local shim, no EACCES) → symmetric remove leaves no residue → idempotent re-remove"
    - "A bats @test proves the opt-in contract: install does NOT mutate ~/.claude; after manual `rtk init -g` the hook exists; remove reverts it (no orphan hook)"
    - "A bats @test proves verify-before-extract: a tampered/mismatched checksum aborts with non-zero exit and does NOT install/replace the binary"
    - "An OPS-01 smoke @test runs a real offline rtk operation as the agent user and gets sensible output"
    - "docs/internals/catalog.md documents the prebuilt-binary (binary) source_kind + rtk"
  artifacts:
    - path: "tests/bats/57-catalog-binary.bats"
      provides: "ENABLE-01 + WORK-02 + OPS-01 lifecycle, opt-in, negative-checksum, idempotent-remove tests"
      contains: "ENABLE-01"
    - path: "docs/internals/catalog.md"
      provides: "binary source_kind + shared prebuilt-binary helper + rtk documentation"
      contains: "binary"
  key_links:
    - from: "tests/bats/57-catalog-binary.bats"
      to: "the provisioned catalog pin"
      via: "jq -r ... select(.id==\"rtk\") .pinned_version (NEVER hardcoded)"
      pattern: "jq -r.*pinned_version"
    - from: "@test names"
      to: "requirement IDs"
      via: "every @test prefixed with ENABLE-01 / WORK-02 / OPS-01"
      pattern: "@test \"(ENABLE-01|WORK-02|OPS-01)"
---

<objective>
Write the TST-07/OPS-01 gate for Phase 28: a bats file that drives rtk's full prebuilt-binary lifecycle on a provisioned host, proving ENABLE-01 (checksum-verified fetch → ~/.local/bin, no root/shim/EACCES, symmetric residue-free remove, verify-before-extract abort) and WORK-02 (pin 0.42.4, opt-in hook reverted on remove), plus an OPS-01 real-operation smoke. Then document the new `binary` source_kind in docs/internals/catalog.md (dev-docs-auditor scope).

Purpose: success criterion #5 requires ≥1 green bats @test covering binary fetch → checksum → version → optional-hook → remove; the REQUIREMENTS.md contract additionally requires an OPS-01 operational smoke (install-clean is necessary but not sufficient). This plan is the phase's executable proof.
Output: tests/bats/57-catalog-binary.bats (green on the Docker matrix) + a catalog.md section on the prebuilt-binary kind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-rtk/28-RESEARCH.md
@.claude/skills/behavior-test-contract/SKILL.md
@tests/bats/53-catalog-npm-cluster.bats
@plugin/catalog/agents/rtk/install.sh
@plugin/catalog/agents/rtk/uninstall.sh

<interfaces>
<!-- bats harness conventions (from 53-catalog-npm-cluster.bats + behavior-test-contract). -->
- `load 'helpers/invoke_modes'` + `load 'helpers/assertions'`
- LOG=/var/log/agentlinux-install.log
- PKG_VERSION=$(jq -r .version /opt/agentlinux-src/plugin/cli/package.json)
  CATALOG=/opt/agentlinux/catalog/${PKG_VERSION}/catalog.json
- setup_file(): re-run `/opt/agentlinux-src/plugin/bin/agentlinux-install` if the
  `~/.npm-global/bin/agentlinux` symlink is missing (INST-04 --purge recovery)
- Available assertions: assert_exit_zero, assert_no_eacces, assert_path_has,
  assert_user_prefix_in_home; failures use `__fail <req> <expected> <actual> <log>`
  (four-line TST-04 diagnostics)
- Run installs as the agent user via a login shell:
  `run sudo -u agent -H bash --login -c "agentlinux install rtk"`
- Pins read from the provisioned CATALOG via jq — NEVER hardcoded
- Single-file run: `./tests/docker/run.sh ubuntu-24.04` (or pass a filter for 57-*.bats)

<!-- rtk runtime facts (RESEARCH.md). -->
binary → ~/.local/bin/rtk ; `rtk --version` → "rtk 0.42.4"
`rtk init -g` writes ~/.claude/{RTK.md, CLAUDE.md @ref, settings.json hook} + ~/.config/rtk
`rtk init --uninstall -g --auto-patch` reverts ~/.claude/* ; remove also drops ~/.config/rtk + ~/.local/share/rtk + settings.json.bak
OPS-01 offline op (no credential — Appendix C): a token-optimized op such as `rtk ls <dir>`
  (token-optimized listing) or `rtk gain`; confirm the exact subcommand from `rtk --help`.
Negative checksum: keep the ONE real network download for the happy path; do the
  tamper check on a LOCAL copy (corrupt the staged tarball / point at a wrong
  asset name) and assert non-zero exit + a "verification failed"-class message +
  the binary NOT replaced — so the test stays green offline in Docker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 57-catalog-binary.bats — ENABLE-01 + WORK-02 lifecycle, opt-in, negative-checksum, idempotent</name>
  <read_first>
    - tests/bats/53-catalog-npm-cluster.bats (full file — the lifecycle driver shape, jq-pin, __fail, login-shell, setup_file/teardown_file recovery)
    - tests/bats/helpers/assertions.bash (assert_exit_zero / assert_no_eacces / __fail signatures)
    - .claude/skills/behavior-test-contract/SKILL.md (TST-07, req-ID-prefixed @test names, six invocation modes, no-EACCES, no /usr/local shim)
    - .planning/phases/28-rtk/28-RESEARCH.md (§Validation Architecture lines 450-483: Phase Requirements → Test Map + Wave 0 Gaps)
    - plugin/catalog/agents/rtk/uninstall.sh (what residue remove must clear)
  </read_first>
  <action>
    Create tests/bats/57-catalog-binary.bats modeled on 53-catalog-npm-cluster.bats:
    header comment naming ENABLE-01 / WORK-02 / OPS-01; `load 'helpers/invoke_modes'`
    + `load 'helpers/assertions'`; LOG + jq-derived PKG_VERSION + CATALOG; a
    `setup_file()` that recovers the agentlinux symlink (mirror 53) and scrubs
    `~/.local/bin/rtk ~/.config/rtk ~/.local/share/rtk ~/.claude/RTK.md
    ~/.claude/settings.json.bak` before tests; a `teardown_file()` that runs
    `agentlinux remove --force rtk` + scrubs the same paths. Derive the pin:
    `pinned=$(jq -r '.agents[]|select(.id=="rtk")|.pinned_version' "$CATALOG")` and
    guard it non-empty/non-null with __fail (so `grep -F -- ""` can't match
    anything). Write these @test cases (each name prefixed with its req ID):

    1. `@test "ENABLE-01: rtk install fetches+checksum-verifies a binary into
       ~/.local/bin (no root/shim/EACCES) and removes symmetrically"` — install
       as agent (assert_exit_zero + assert_no_eacces); `command -v rtk` resolves
       under `/home/agent/.local/bin/*` (NOT /usr/local — __fail otherwise);
       `rtk --version` contains "$pinned"; then `agentlinux remove --force rtk`,
       assert binary gone from PATH AND `test ! -e ~/.config/rtk` AND
       `test ! -e ~/.local/share/rtk` (no residue); a SECOND remove exits 0
       (idempotent).
    2. `@test "WORK-02: rtk install does NOT mutate ~/.claude; opt-in rtk init -g
       wires the hook; remove reverts it (no orphan)"` — install, then assert
       ~/.claude has NO rtk hook yet (e.g. `grep -L 'rtk hook' ~/.claude/settings.json`
       or settings.json/RTK.md absent); run `rtk init -g --auto-patch` as agent,
       assert the hook/RTK.md now exists; `agentlinux remove --force rtk`, assert
       `~/.claude/RTK.md` gone, no `rtk hook` entry survives, and
       `~/.claude/settings.json.bak` is gone. Guard each step; do not leave a
       broken settings.json behind for downstream files.
    3. `@test "ENABLE-01: a mismatched checksum aborts BEFORE extract (binary not
       installed/replaced)"` — the negative test. Keep network use to the happy
       path: stage a deliberately-corrupt tarball or a checksums.txt with a wrong
       hash in a tmpdir and invoke the helper's verify path (source
       `/opt/agentlinux/catalog/${PKG_VERSION}/lib/prebuilt-binary.sh` and call
       `al_pb_fetch_and_verify` against a local file:// or pre-staged tmp), assert
       NON-zero exit and that no `rtk` was written to the test dest. Document in a
       comment that this exercises the verify-before-extract security gate.
  </action>
  <acceptance_criteria>
    - `grep -Ec '@test "(ENABLE-01|WORK-02)' tests/bats/57-catalog-binary.bats` ≥ 3
    - `grep -q 'jq -r' tests/bats/57-catalog-binary.bats && ! grep -q '0.42.4' tests/bats/57-catalog-binary.bats` (pin from catalog, never hardcoded)
    - `grep -q '/home/agent/.local/bin' tests/bats/57-catalog-binary.bats` (asserts the agent-owned install path / no /usr/local shim)
    - `grep -q 'assert_no_eacces' tests/bats/57-catalog-binary.bats`
    - `grep -q '.config/rtk' tests/bats/57-catalog-binary.bats && grep -q '.local/share/rtk' tests/bats/57-catalog-binary.bats` (residue checks)
    - `grep -q 'rtk init' tests/bats/57-catalog-binary.bats` (opt-in hook test) and a negative-checksum @test exists (`grep -q 'mismatched checksum' tests/bats/57-catalog-binary.bats`)
    - `bats --count tests/bats/57-catalog-binary.bats` parses (≥3) and `shellcheck -s bash tests/bats/57-catalog-binary.bats` has no blocking errors
    - `./tests/docker/run.sh ubuntu-24.04` runs 57-catalog-binary.bats green (full lifecycle on a provisioned host)
  </acceptance_criteria>
  <verify>
    <automated>bats --count tests/bats/57-catalog-binary.bats && ./tests/docker/run.sh ubuntu-24.04 2>&1 | tail -40</automated>
  </verify>
  <done>57-catalog-binary.bats has ≥3 req-ID-prefixed @tests (install→checksum→version→remove→idempotent; opt-in hook revert; negative-checksum abort) that pass on Ubuntu 24.04 Docker; pins are jq-derived; no /usr/local shim, no EACCES, no residue.</done>
</task>

<task type="auto">
  <name>Task 2: OPS-01 smoke @test + docs/internals/catalog.md prebuilt-binary section</name>
  <read_first>
    - tests/bats/57-catalog-binary.bats (the Task 1 file this extends)
    - docs/internals/catalog.md (current structure — where to add the binary source_kind section)
    - .planning/REQUIREMENTS.md (OPS-01 contract lines ~32-37 + Appendix C rtk = no credential)
    - .planning/phases/28-rtk/28-RESEARCH.md (§Assumptions Log A2 — OPS-01 op options)
  </read_first>
  <action>
    (a) Append an OPS-01 smoke @test to tests/bats/57-catalog-binary.bats:
    `@test "OPS-01: rtk performs a real offline operation as the agent user"` —
    install rtk, create a tmp dir/file as the agent, run a real offline rtk op
    (confirm the subcommand from `rtk --help`; prefer `rtk ls "$tmpdir"`
    token-optimized listing, fallback `rtk gain`), assert_exit_zero and that the
    output is non-empty / mentions the seeded file. No credential (Appendix C).
    Record in the @test comment that this is the OPS-01 phase-close smoke.

    (b) Update docs/internals/catalog.md: add a short section documenting the new
    `binary` source_kind — what it does (fetch a pinned GitHub release, verify
    its checksum BEFORE extract, install an agent-owned binary to ~/.local/bin,
    no root/shim), the shared `plugin/catalog/lib/prebuilt-binary.sh` helper
    (al_pb_install + verify-before-extract), and rtk as the first consumer
    (pin 0.42.4, opt-in `rtk init`, symmetric residue-free remove). Match the
    doc's existing voice/structure; keep it product-perspective (why it exists).
    Avoid internal-only vocabulary that a public reader can't resolve
    (external-audience-auditor / dev-docs-auditor scope).
  </action>
  <acceptance_criteria>
    - `grep -q '@test "OPS-01' tests/bats/57-catalog-binary.bats`
    - The OPS-01 @test runs a real rtk subcommand (not just `--version`): `grep -Eq 'rtk (ls|gain|proxy)' tests/bats/57-catalog-binary.bats`
    - `grep -qi 'binary' docs/internals/catalog.md && grep -qi 'prebuilt' docs/internals/catalog.md && grep -q 'rtk' docs/internals/catalog.md`
    - `grep -q 'prebuilt-binary.sh' docs/internals/catalog.md` (helper documented)
    - `./tests/docker/run.sh ubuntu-24.04` runs the OPS-01 @test green
    - `pre-commit run --files tests/bats/57-catalog-binary.bats docs/internals/catalog.md` clean
  </acceptance_criteria>
  <verify>
    <automated>bats --count tests/bats/57-catalog-binary.bats && ./tests/docker/run.sh ubuntu-24.04 2>&1 | tail -40</automated>
  </verify>
  <done>An OPS-01 smoke @test exercises a real offline rtk operation and passes; docs/internals/catalog.md documents the binary source_kind + the shared helper + rtk; pre-commit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → verify path | The negative-checksum test feeds a deliberately-tampered artifact across the helper's verification boundary to prove it aborts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12 | Tampering | verify-before-extract regression | mitigate | The negative-checksum @test asserts a mismatched checksum aborts non-zero and installs nothing — guards against a future recipe/helper edit that extracts before verifying |
| T-28-13 | Repudiation | hardcoded pin masking drift | mitigate | The @tests derive the pin from the provisioned catalog via jq (never hardcoded) and guard it non-empty, so a missing/renamed catalog fails loudly rather than silently passing |
| T-28-14 | Tampering | broken settings.json leaking to downstream test files | accept | teardown_file scrubs rtk's ~/.claude residue; the opt-in test never deletes a user-owned settings.json — only rtk-owned RTK.md/.bak/hook entries (V4 boundary respected) |
</threat_model>

<verification>
- `bats --count tests/bats/57-catalog-binary.bats` ≥ 4 (lifecycle + opt-in + negative + OPS-01)
- `./tests/docker/run.sh ubuntu-24.04` green for 57-catalog-binary.bats (the TST-07/OPS-01 gate)
- All @test names prefixed with ENABLE-01 / WORK-02 / OPS-01; pins jq-derived; no /usr/local shim, no EACCES, no residue
- docs/internals/catalog.md documents the binary source_kind + shared helper + rtk
- `pre-commit run --files tests/bats/57-catalog-binary.bats docs/internals/catalog.md` clean
- Record the OPS-01 smoke as run-and-passed in 28-04-SUMMARY.md (phase-close gate)
</verification>

<success_criteria>
- ≥1 green bats @test covers binary fetch → checksum → version (0.42.4) → optional-hook → symmetric remove (success criterion #5 / TST-07)
- The opt-in contract is proven: install doesn't mutate ~/.claude; manual `rtk init -g` wires the hook; remove reverts it with no orphan
- Verify-before-extract is proven by a negative-checksum abort test
- OPS-01 smoke runs a real offline rtk operation as the agent user and passes
- docs/internals/catalog.md documents the prebuilt-binary source_kind
</success_criteria>

<output>
After completion, create `.planning/phases/28-rtk/28-04-SUMMARY.md` (record the OPS-01 smoke result — phase-close gate).
</output>
