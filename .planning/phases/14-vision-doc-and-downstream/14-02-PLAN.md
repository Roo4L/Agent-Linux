---
phase: 14-vision-doc-and-downstream
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - README.md
  - CONTRIBUTING.md
  - .planning/PROJECT.md
  - docs/STABILITY-MODEL.md
  - .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md
autonomous: false
requirements:
  - VIS-08
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "A future visitor reading any of {README.md, CONTRIBUTING.md, .planning/PROJECT.md, docs/STABILITY-MODEL.md, docs/VISION.md} sees the same coherent two-pillar story"
    - "README.md ## About names the two pillars and links to docs/VISION.md; ## Links carries a Vision: row"
    - "CONTRIBUTING.md carries a 'Why this project exists' paragraph that links to docs/VISION.md and names which pillars accept contributions today"
    - ".planning/PROJECT.md Core Value + Current Milestone sections cross-reference docs/VISION.md; three-pillar text replaced with two-pillar; Out-of-Scope reflects EXPL-01/02-surfaced non-goals"
    - "docs/STABILITY-MODEL.md Related section gains a back-link to docs/VISION.md Pillar 2"
    - "14-AUDIT.md cites every VIS-01..VIS-09 + DOC-01..DOC-05 with file paths / line ranges / commit hashes / grep transcripts; DOC-05 records N/A close with EXPL-02 Verdict-line cite; gate emits GREEN"
    - "VIS-07 voice-rule grep still returns zero matches against docs/VISION.md after Plan 14-02 closes (no Plan-14-02 edit touches docs/VISION.md, so this is invariant — verified at audit time)"
  artifacts:
    - path: ".planning/phases/14-vision-doc-and-downstream/14-AUDIT.md"
      provides: "Phase-close audit covering VIS-01..VIS-09 + DOC-01..DOC-05; gate emits GREEN"
      contains: "## VIS-07 — Voice-rule hard gate"
    - path: "README.md"
      provides: "About section names the two pillars + Vision link; Links section carries Vision row"
      contains: "docs/VISION.md"
    - path: "CONTRIBUTING.md"
      provides: "'Why this project exists' paragraph linking to docs/VISION.md"
      contains: "docs/VISION.md"
    - path: ".planning/PROJECT.md"
      provides: "Core Value + Current Milestone cross-reference VISION.md; two-pillar framing replaces three-pillar"
      contains: "docs/VISION.md"
    - path: "docs/STABILITY-MODEL.md"
      provides: "Related section back-link to docs/VISION.md Pillar 2"
      contains: "docs/VISION.md#pillar-2"
  key_links:
    - from: "README.md"
      to: "docs/VISION.md"
      via: "Markdown link in ## About paragraph and ## Links list"
      pattern: "docs/VISION\\.md"
    - from: "CONTRIBUTING.md"
      to: "docs/VISION.md"
      via: "Markdown link in 'Why this project exists' paragraph"
      pattern: "docs/VISION\\.md"
    - from: ".planning/PROJECT.md"
      to: "docs/VISION.md"
      via: "Markdown links in Core Value + Current Milestone sections"
      pattern: "docs/VISION\\.md"
    - from: "docs/STABILITY-MODEL.md"
      to: "docs/VISION.md"
      via: "Markdown link to docs/VISION.md#pillar-2 in Related section"
      pattern: "docs/VISION\\.md"
    - from: ".planning/phases/14-vision-doc-and-downstream/14-AUDIT.md"
      to: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      via: "DOC-05 N/A close cites EXPL-02 Verdict line verbatim"
      pattern: "PILLAR-3-CANDIDATE-NOTES\\.md"
---

<objective>
Propagate the docs/VISION.md framing to the four downstream documentation surfaces (README.md, CONTRIBUTING.md, .planning/PROJECT.md, docs/STABILITY-MODEL.md), close DOC-05 as N/A with a cited rationale, and author the phase-close audit file `14-AUDIT.md` that consolidates every VIS-01..VIS-09 + DOC-01..DOC-05 evidence transcript and emits the Phase 14 GREEN gate.

Purpose: A future visitor (external contributor, AI agent reading the repo, product-leadership reviewer) reading any of the surface files sees the same coherent two-pillar story without contradictions. The audit file is the load-bearing close artifact — it lifts the Plan 14-01 transcripts verbatim, captures the Plan 14-02 DOC-XX evidence, and records the DOC-05 N/A close per Phase 13 verdict (b).

Output:
- `README.md`, `CONTRIBUTING.md`, `.planning/PROJECT.md`, `docs/STABILITY-MODEL.md` — surgical back-pointer edits per DOC-01..DOC-04.
- `.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md` — phase-close audit, gate GREEN.
- Single commit landing the five files (one commit, per CLAUDE.md's prefer-new-commits rule).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md
@.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md
@docs/VISION.md
@docs/decisions/015-agenda-redefinition.md
@docs/exploration/PILLAR-2-NOTES.md
@docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
@README.md
@CONTRIBUTING.md
@docs/STABILITY-MODEL.md
@CLAUDE.md

<voice_rule>
The voice rule applies to every line of new text authored across the four
downstream surface edits AND the audit file. The blocked pattern (case-
sensitive against capitalized-line-start) for `AgentLinux` as subject:
`provides`, `offers`, `ensures`, `protects`, `defends`, `benchmarks`,
`measures`, `hardens`, `isolates`, `detects`, `prevents`. Forward-looking
claims MUST use "we" / "our roadmap" / "v0.3.3" / "v0.6+" as subject.

Particularly load-bearing in this plan:
- The PROJECT.md `**Goal:**` rewrite (line 17 of current file) replaces
  "three-pillar product" framing with two-pillar framing. The Pillar 2
  description in the existing line 21 makes forward-looking benchmark
  claims ("extended with measurable benchmarks vs vanilla setups: token
  consumption, throughput/speed, task success rate") that MUST be cut or
  rewritten to "we" voice — those benchmarks are out of scope for v0.3.3
  per REQUIREMENTS.md and the framing has changed (Pillar 2 is Stability,
  not benchmarks).
- The Pillar 3 description (existing line 22) is removed entirely — Phase
  13 verdict (b) means there is no Pillar 3.
- README.md About section currently uses delivered-fact voice describing
  the EACCES / recursive-shim story (line 147+); the DOC-01 edit ADDS a
  pillar-naming sentence + Vision link WITHOUT rewriting the existing
  paragraph. Keep the existing copy.

Defensive practice: run the same VIS-07 voice-rule grep against each
edited file after the edit lands (audit file Task 6 captures these as
DOC-0N "no voice-rule regression" transcripts).
</voice_rule>

<interfaces>
<!-- Exact edit anchors and replacement text the executor uses verbatim. -->

README.md — current shape (verified 2026-05-16):
- `## Links` lives at line 136. Existing entries are bulleted: Source + issues,
  Releases, Architecture decisions (`docs/decisions/`), Test harness spec
  (`docs/HARNESS.md`), Stability model (`docs/STABILITY-MODEL.md`), Landing
  page.
- `## About` lives at line 145. Existing first paragraph (lines 147-156) is
  the EACCES / recursive-shim narrative; DO NOT rewrite — append/prepend
  one sentence + add the Vision link.

CONTRIBUTING.md — current shape (verified 2026-05-16):
- H1 + intro paragraph at lines 1-4. The "Why this project exists" paragraph
  is NEW (does not yet exist); insert it BETWEEN the intro paragraph (line 4)
  and the `## Quick start` heading (line 6) so it reads as the orienting
  context for the rest of the file.

.planning/PROJECT.md — current shape (verified 2026-05-16):
- `## Core Value` lives at line 9. Body is one paragraph (line 11) describing
  the dropped-in agent + zero-EACCES value prop.
- `## Current Milestone: v0.3.3 Agenda Redefinition` lives at line 13. Body
  spans lines 15-30 and CURRENTLY uses three-pillar framing (lines 17, 19-22,
  25, 28). MUST be rewritten to two-pillar framing per the EXPL-01 + EXPL-02
  verdicts. The "three pillars (per AL-7)" header at line 19 + Pillar 1/2/3
  bullets at lines 20-22 + the three-pillar sentence at line 17 + benchmark
  framing at line 21 + security framing at line 22 ALL need replacement.
- `### Out of Scope` lives at line 100. The v0.3.3 entries (lines 102-107)
  currently mention benchmarks (pillar 2) and security-hardening (pillar 3)
  framings that are stale post-Phase-13. Update the framing.
- "Previous Milestone" section (line 32+) and "Earlier Milestones" (line 38+)
  describe shipped v0.4.0 / v0.3.0 / v0.2.0 reality — DO NOT TOUCH (delivered-
  fact voice; orthogonal to the framing reframe).
- "Active (v0.3.3 — Agenda Redefinition)" section at line 89 references the
  pre-reframe `STRAT-XX` / `MILE-XX` categories (line 93-96). REQUIREMENTS.md
  superseded those (STRAT-* → VIS-* + STRATR-*). The exact text needs an
  update to reflect the post-reframe categories (VIS / STRATR / SITE / DOC).
- Line 145 in the Context section: "AgentLinux v0.3.0" is delivered-fact
  voice; preserve.
- Lines 178-180 in Key Decisions table: the "Agenda redefinition (v0.3.3)"
  / "Strategy doc as the v0.3.3 keystone" / "Defer pillar 2 + pillar 3
  implementation" rows still say "three pillars" / "pillar 3". Either
  update the row text in-place or append a 2026-05-16 reframe row that
  records the two-pillar split. Recommended: append a new row with the
  reframe entry rather than rewriting the original (audit trail of how
  the decision evolved).
- Line 191 "What This Is still accurate?" guidance — preserve verbatim.

docs/STABILITY-MODEL.md — current shape (verified 2026-05-16):
- `## Related` lives at line 117. Existing entries (lines 119-124) link to
  ADR-011, ADR-006, and README.md. Add ONE new entry pointing to
  `docs/VISION.md#pillar-2` (the GitHub-rendered anchor for `### Pillar 2 —
  Stability`).

docs/VISION.md anchor format (for the STABILITY-MODEL.md DOC-04 edit):
- GitHub renders `### Pillar 2 — Stability` as anchor `#pillar-2--stability`
  (em-dash dropped, double-dash between word boundaries). However a more
  durable convention is to use the short anchor `#pillar-2` which GitHub
  also generates for `### Pillar 2` even when there is suffix text — verify
  by visiting the rendered page if uncertain. Recommended: `docs/VISION.md`
  WITHOUT a fragment if anchor stability is a concern; the link text in
  prose can name "Pillar 2 — Stability" explicitly. The acceptance for
  DOC-04 is "back-link to VISION.md Pillar 2" — naming Pillar 2 in the
  link TEXT satisfies the requirement even if the URL fragment is omitted.

ADR-015 path (already committed by Plan 14-01):
- `docs/decisions/015-agenda-redefinition.md` — Plan 14-02 may add a
  README.md `## Links` row pointing to it, BUT the DOC-01 acceptance only
  requires a Vision link; adding ADR-015 to the Links section is optional
  (Claude's discretion per 14-CONTEXT.md).

Verification commands (run inside Task 6 audit file authoring):
- VIS-08 inbound count: `grep -lE 'docs/VISION\.md' README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md`
- VIS-07 voice-rule invariant on edited files:
  `for f in README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md docs/VISION.md; do echo "=== $f ==="; grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' "$f"; done ; echo "exit=$?"`
  (Note: on no-matches across all files, grep returns 1 for the LAST file; the
  intent is "no matches across the set." Capture each file's output separately;
  the audit's PASS shape is "every echoed section is empty.")
- DOC-01..04 line ranges: `grep -nE 'docs/VISION\.md' <file>` returns the
  line(s) where the back-pointer was added.

EXPL-02 Verdict line cite for DOC-05 N/A close:
- File: `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`
- Line: 17 (verbatim text: "**Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.")
- Verify with: `sed -n '17p' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`
  (or `awk 'NR==17' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md DOC-01 + VIS-08 back-pointer (About + Links sections)</name>
  <files>README.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/README.md (lines 130-156 — current ## Links and ## About sections; lines below 156 are EOF / mostly empty)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (lines 24-46 — `## The two pillars` section; the source-of-truth for pillar names referenced in the new sentence)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (`### DOC-01..DOC-04 — back-pointers to VISION.md` for the locked edit shape)
  </read_first>
  <action>
    Two surgical edits to `README.md`, both ADDITIVE — do NOT rewrite the existing About paragraph (the EACCES / recursive-shim narrative at lines 147-156 stays delivered-fact and accurate; the pillars reframe is *additional* framing context, not a replacement).

    **Edit 1 — `## Links` section (DOC-01 link row).** Currently at lines 136-143. Insert a new bulleted entry between the existing "Architecture decisions" row (line 140) and the "Test harness spec" row (line 141). The exact line to add:

    ```
    - **Vision:** [docs/VISION.md](docs/VISION.md)
    ```

    Final ordering in `## Links` after the edit:
    - Source + issues
    - Releases
    - Architecture decisions
    - **Vision** (new — DOC-01)
    - Test harness spec
    - Stability model (user-facing)
    - Landing page

    **Edit 2 — `## About` section (pillar-naming sentence).** Currently the About paragraph runs from line 147 ("AgentLinux exists because...") through line 156 (continuation). Add ONE new paragraph IMMEDIATELY AFTER the existing About paragraph (after the line ending "...downstream of that one"). The new paragraph names the two pillars and links to VISION.md. Exact text to add:

    ```

    AgentLinux is framed around two pillars: **Time-to-productive** (the
    assembly the user gets on install — agent user, runtime, permissions,
    catalog — without learning to assemble it themselves) and **Stability**
    (the curated toolchain holds compatible across upstream churn). See
    [docs/VISION.md](docs/VISION.md) for the full framing.
    ```

    Voice-rule check on this paragraph:
    - "AgentLinux is framed" — `is framed` is past-participle copula, not in the blocked verb list. PASSES.
    - "the user gets on install" — "the user" is the grammatical subject. PASSES.
    - "the curated toolchain holds compatible" — "toolchain" is the subject. PASSES.

    Do NOT replace the existing About paragraph. The new pillar paragraph is ADDITIONAL framing context that sits underneath the existing motivating-bug narrative.

    Per CLAUDE.md, use a heredoc-style Markdown edit via the Edit tool (NOT a `cat > README.md` overwrite). Preserve every other line of the file byte-for-byte.
  </action>
  <verify>
    <automated>grep -cE '^- \*\*Vision:\*\* \[docs/VISION\.md\]\(docs/VISION\.md\)$' README.md | grep -qE '^1$' &amp;&amp; grep -cE 'AgentLinux is framed around two pillars' README.md | grep -qE '^1$' &amp;&amp; grep -nE 'docs/VISION\.md' README.md | wc -l | xargs -I N test N -ge 2 &amp;&amp; ! grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Links` contains the line `- **Vision:** [docs/VISION.md](docs/VISION.md)` exactly once.
    - `## About` contains the pillar-naming paragraph starting `AgentLinux is framed around two pillars:` with `**Time-to-productive**` and `**Stability**` and a `[docs/VISION.md](docs/VISION.md)` link inside that paragraph.
    - Existing About paragraph (EACCES / recursive-shim narrative at lines 147-156 in pre-edit state) is preserved byte-for-byte.
    - Voice-rule grep returns zero matches against README.md.
    - At least 2 occurrences of `docs/VISION.md` in README.md (one in Links, at least one in About).
  </acceptance_criteria>
  <done>DOC-01 closed: README.md About section names two pillars + links to VISION.md; Links section carries a Vision row.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md DOC-02 + VIS-08 back-pointer ("Why this project exists" paragraph)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/CONTRIBUTING.md (lines 1-40 — H1, intro paragraph, and Quick start section to confirm insertion point)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (lines 24-46 — pillars; for accurate "which pillar accepts contributions today" claim)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (`### DOC-01..DOC-04 — back-pointers to VISION.md`: locked DOC-02 shape is "one short paragraph linking to VISION.md; names which pillars accept contributions today: Pillar 1 yes, Pillar 2 early-stage")
  </read_first>
  <action>
    Insert a new section `## Why this project exists` between the intro paragraph (ends line 4) and the `## Quick start` heading (currently line 6). Exact text to insert:

    ```

    ## Why this project exists

    AgentLinux is framed around two pillars: **Time-to-productive** (the
    work that gets a user from `curl | bash` to a first useful agent run on
    a fresh box) and **Stability** (the curated toolchain holds compatible
    across upstream churn). See [docs/VISION.md](docs/VISION.md) for the
    full framing. Pillar 1 is what v0.3.0 already shipped — contributions
    landing in the agent-user / runtime / catalog surface area are welcome
    today. Pillar 2 is early-stage — the supply-chain monitoring + curated
    catalog admission sub-concern (Phase 13 verdict, folded into Pillar 2)
    locks at v0.3.3, but the mechanism work lands later (v0.6+). Pillar-2
    contributions are welcome with the heads-up that the framing locked in
    v0.3.3 and the implementation primitives ship in a later milestone.
    ```

    Then ensure a blank line precedes `## Quick start` (it already does in the current file, but the insertion above ends with `[some text].` — keep the existing blank line before `## Quick start`).

    Voice-rule check on this paragraph:
    - "AgentLinux is framed" — `is framed` (copula + past participle). PASSES.
    - "Pillar 1 is what v0.3.0 already shipped" — delivered-fact, "Pillar 1" subject. PASSES.
    - "Pillar 2 is early-stage" — copula. PASSES.
    - "the mechanism work lands later" — "work" subject; "lands" not in blocked list. PASSES.
    - "Pillar-2 contributions are welcome" — "contributions" subject. PASSES.

    Per CLAUDE.md, use the Edit tool with a single targeted insertion at the right anchor (between line 4 ending with "...how we accept changes." and the `## Quick start` heading).
  </action>
  <verify>
    <automated>grep -cE '^## Why this project exists$' CONTRIBUTING.md | grep -qE '^1$' &amp;&amp; grep -cE '\[docs/VISION\.md\]\(docs/VISION\.md\)' CONTRIBUTING.md | grep -qE '^1$' &amp;&amp; grep -qE 'Time-to-productive' CONTRIBUTING.md &amp;&amp; grep -qE 'Stability' CONTRIBUTING.md &amp;&amp; grep -qE 'Pillar 1 is what v0\.3\.0' CONTRIBUTING.md &amp;&amp; grep -qE 'Pillar 2 is early-stage' CONTRIBUTING.md &amp;&amp; ! grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Why this project exists` H2 heading appears exactly once.
    - The paragraph under it links to `[docs/VISION.md](docs/VISION.md)` exactly once.
    - Both pillar names appear: `Time-to-productive` and `Stability`.
    - The "which pillar accepts contributions today" framing is present: `Pillar 1` named with shipped reality + `Pillar 2` named with early-stage status (matches 14-CONTEXT.md DOC-02 lock).
    - Voice-rule grep returns zero matches against CONTRIBUTING.md.
    - The `## Quick start` section that follows is unchanged byte-for-byte from its prior state.
  </acceptance_criteria>
  <done>DOC-02 closed: CONTRIBUTING.md carries a "Why this project exists" paragraph linking to VISION.md with per-pillar contribution status.</done>
</task>

<task type="auto">
  <name>Task 3: .planning/PROJECT.md DOC-03 + VIS-08 back-pointer (three-pillar → two-pillar rewrite + Core Value link + Current Milestone link + Out-of-Scope refresh)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/PROJECT.md (read end-to-end — this is the largest edit in Plan 14-02 and touches multiple sections: Core Value at line 9, Current Milestone at lines 13-30, Active requirements at lines 89-96, Out-of-Scope at lines 100-107, Context milestone summary at line 143, Key Decisions table at lines 178-180)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (the canonical pillar definitions referenced by the rewritten Current Milestone section)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/exploration/PILLAR-2-NOTES.md (lines 1-100 — Phase 12 verdict, source for Pillar 2 framing in PROJECT.md)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/exploration/PILLAR-3-CANDIDATE-NOTES.md (lines 1-30 — Verdict (b), source for why pillar 3 is removed)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/REQUIREMENTS.md (lines 22-110 — current requirement categories VIS/STRATR/DOC/SITE, for the Active section refresh)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (DOC-03 locked shape)
  </read_first>
  <action>
    Six surgical edits to `.planning/PROJECT.md`. Apply each with the Edit tool against the specific anchor line(s) — do NOT overwrite the file wholesale (the milestone history sections for v0.4.0 / v0.3.0 / v0.2.0 are delivered-fact and accurate; preserve them byte-for-byte).

    **Edit 1 — `## Core Value` (line 9-11): append a VISION.md cross-reference sentence.**

    Find the existing Core Value paragraph (line 11, single paragraph ending "...built right the first time."). Append (on a new line, single trailing newline before next section header) one sentence:

    ```

    See [docs/VISION.md](../docs/VISION.md) for the framing this Core Value seeds.
    ```

    (Note: `.planning/PROJECT.md` is at `.planning/PROJECT.md`; the relative path to `docs/VISION.md` from `.planning/` is `../docs/VISION.md`.)

    **Edit 2 — `## Current Milestone: v0.3.3 Agenda Redefinition` (lines 13-30): three-pillar → two-pillar rewrite.**

    Replace the existing Goal + pillars block + Target features bullets. The CURRENT text at lines 15-30 is:

    ```
    **Anchor:** Jira epic [AL-7 — Project agenda redefinition](https://...).

    **Goal:** Broaden AgentLinux's mission from a single-pillar product (...) to a three-pillar product, capture the new framing in a canonical product-strategy document, and propagate the framing to the public landing page at agentlinux.org.

    **The three pillars (per AL-7):**
    1. **Separated, correctly-owned agent environment** — the existing v0.3.0 core: dedicated agent user, per-user npm prefix, no-EACCES self-update. Foundational; not changing in v0.3.3.
    2. **Stability + best-tested setup for agents** — pinned curated combinations (ADR-011 is the seed) extended with **measurable benchmarks** vs vanilla setups: token consumption, throughput/speed, task success rate.
    3. **Security hardening** — defenses against (a) supply-chain attacks on the agent toolchain (npm registry compromise, recipe tampering, transitive deps) and (b) prompt/tool-injection attacks against the agent itself (OWASP LLM Top 10 territory, Anthropic tool-use safety guidance).

    **Target features:**
    - Canonical **product-strategy document** in the repo (likely `docs/strategy/`) — vision, target users, jobs-to-be-done, the three pillars + their non-goals, positioning vs alternatives, candidate roadmap themes for v0.6+. Authored from a PM-framework template chosen via parallel research.
    - **Decision provenance** — ADR documenting the framing choice, discarded alternatives, and the AL-7 connection.
    - **Roadmap themes for v0.6+** surfaced as a forward-looking appendix in the strategy doc — not committed phases (those happen at the next `/gsd-new-milestone`). Likely candidates: a Benchmarks/Stability milestone and a Security Hardening milestone.
    - **Website refresh** at agentlinux.org reflecting the three-pillar framing — scope locked at phase-discuss time after the strategy doc lands.

    **Verification posture:** v0.3.3 is a planning/strategy milestone. Most evidence is documents (strategy doc + ADR + audit reports per phase), not bats @tests. The website-refresh phase produces real product code (HTML/CSS/JS); the bats / Docker / QEMU harness from v0.3.0 stays green throughout (a regression there blocks the milestone) but no new bats are required.
    ```

    Replace with this (anchor + new goal + two-pillar list + target features + verification posture):

    ```
    **Anchor:** Jira epic [AL-7 — Project agenda redefinition](https://copiedwonder.atlassian.net/browse/AL-7).

    **Goal:** Broaden AgentLinux's framing from a single-pillar product ("separated, correctly-owned agent environment" — v0.3.0 core) to a two-pillar product whose pillar names are *optimization values*, capture the new framing in a canonical vision document plus a separate strategy/roadmap document, and propagate it to every downstream documentation surface and to the public landing page at agentlinux.org. See [../docs/VISION.md](../docs/VISION.md) for the canonical framing.

    **The two pillars (locked by Phase 13 verdict (b)):**
    1. **Time-to-productive** — the assembly the user gets on install: agent user, runtime, permissions, curated catalog. Where a stock `sudo npm install -g claude` hands the user a chain of small decisions, AgentLinux ships one working environment. Foundational; not changing in v0.3.3.
    2. **Stability** — the curated toolchain holds compatible across upstream churn. The default version set holds at the last-known-good combination; the bump waits for verified compatibility. The supply-chain monitoring + curated catalog admission sub-concern folded into this pillar in Phase 13 verdict (b) — the mechanism work lands in a later milestone (v0.6+ Security Hardening theme).

    Phase 13 declined a separate Pillar 3 (security). Phase-13 verdict line: "(b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3." (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17).

    **Target features:**
    - Canonical **vision document** at [`../docs/VISION.md`](../docs/VISION.md) — mission, the two pillars as optimization values, vision-level guiding principles, vision-level non-goals.
    - Canonical **strategy/roadmap document** at `docs/STRATEGY.md` (Phase 15) — execution principles (voice rule, behavior-tests-as-spec, evidence-cite discipline, curated-combo testing), themes for v0.6+ (Security Hardening, preset/profile framework, compat-guarded update flow), near-term focus.
    - **Decision provenance** — ADR-015 at `docs/decisions/015-agenda-redefinition.md` records the AL-7 framing decision (two pillars + vision/strategy split) with three considered-and-rejected alternatives and back-links to AL-7 + VISION.md.
    - **Downstream surface updates** — README About + Links, CONTRIBUTING "Why this project exists", PROJECT.md (this file), STABILITY-MODEL.md Related. Locked under Phase 14 DOC-01..DOC-04.
    - **Website refresh** at agentlinux.org (Phase 16) reflecting the two-pillar framing.

    **Verification posture:** v0.3.3 is a framing milestone. Most evidence is documentation artefacts (`docs/VISION.md`, `docs/STRATEGY.md`, ADR-015, per-phase `<phase-NN>-AUDIT.md` files), not bats @tests. The voice-rule grep gate (VIS-07 on VISION.md, STRATR-06 on STRATEGY.md, SITE-06 on rendered HTML) is the load-bearing structural defence — every claim about an unshipped behaviour MUST appear in a sentence whose grammatical subject is "we" / "our roadmap" / an explicit milestone identifier. The bats / Docker / QEMU harness from v0.3.0 stays green throughout (a regression there blocks the milestone) but no new bats are required.
    ```

    **Edit 3 — `### Active (v0.3.3 — Agenda Redefinition)` (lines 89-98): refresh requirement categories from pre-reframe STRAT-XX / MILE-XX to post-reframe VIS / STRATR / DOC / SITE.**

    Current text at lines 89-96:

    ```
    ### Active (v0.3.3 — Agenda Redefinition)

    Requirements for this milestone are defined in `.planning/REQUIREMENTS.md` after the parallel-research pass and the requirement-scoping conversation. Categories will likely span:

    - **STRAT-XX**: Product-strategy document content (vision, target users, JTBD, three pillars, positioning, roadmap themes appendix)
    - **ADR-XX**: Decision provenance for the framing
    - **SITE-XX**: Website refresh deliverables (scope locked at phase-discuss time after the strategy doc lands)
    - **MILE-XX**: Forward-looking roadmap-theme commitments seeded from the strategy doc

    Refer to `REQUIREMENTS.md` for the locked list once defined.
    ```

    Replace with:

    ```
    ### Active (v0.3.3 — Agenda Redefinition)

    Requirements for this milestone are defined in [`.planning/REQUIREMENTS.md`](REQUIREMENTS.md) and grouped (post-2026-05-16 vision/strategy reframe) as:

    - **EXPL-XX** (Phase 12 + 13): Pillar exploration verdicts — `docs/exploration/PILLAR-2-NOTES.md`, `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`. Completed 2026-05-10.
    - **VIS-XX** (Phase 14): `docs/VISION.md` content + voice-rule grep gate (VIS-07) + ADR-015 (VIS-09).
    - **DOC-XX** (Phase 14): Downstream surface back-pointers to VISION.md (README, CONTRIBUTING, PROJECT.md, STABILITY-MODEL.md). DOC-05 closed N/A (Phase 13 verdict (b)).
    - **STRATR-XX** (Phase 15): `docs/STRATEGY.md` content + voice-rule grep gate (STRATR-06).
    - **SITE-XX** (Phase 16): Website refresh at agentlinux.org reflecting the two-pillar framing + voice-rule grep gate (SITE-06) on rendered HTML.

    Refer to `REQUIREMENTS.md` for the per-requirement acceptance language.
    ```

    **Edit 4 — `### Out of Scope` (lines 100-107): refresh v0.3.3 entries.**

    Current text at lines 102-107:

    ```
    **v0.3.3 out of scope:**
    - *Implementing* benchmarks (pillar 2) — v0.3.3 surfaces benchmarks as a roadmap theme; the actual harness/dataset/scoring lands in a v0.6+ milestone
    - *Implementing* security-hardening countermeasures (pillar 3) — v0.3.3 surfaces threat models + roadmap themes; supply-chain and prompt-injection mitigations land in a v0.6+ milestone
    - New distro targets, new catalog agents, new installer features — pillar 1 stays at its v0.3.0 surface for this milestone
    - A full website redesign — the website-refresh phase decides scope at phase-discuss time, after the strategy doc; default expectation is content/IA changes, not a new visual design
    - Renaming, restructuring, or moving the strategy doc after Phase A lands — the doc location locks at Phase A close so the website refresh can link to a stable URL
    ```

    Replace with:

    ```
    **v0.3.3 out of scope:**
    - *Implementing* preset/profile framework or compat-guarded update mechanism (Pillar 2 forward differentiators) — v0.3.3 surfaces them in the strategy doc; the actual implementation lands in a v0.6+ milestone.
    - *Implementing* security-hardening countermeasures (the Phase 13 opportunistic theme) — supply-chain and prompt-injection mitigations land in a v0.6+ milestone.
    - New distro targets, new catalog agents, new installer features — Pillar 1 stays at its v0.3.0 surface for this milestone.
    - A full website redesign — the website-refresh phase keeps the existing dark JetBrains-Mono aesthetic + crab mascot.
    - Renaming, restructuring, or moving the vision or strategy docs after Phase 14 / Phase 15 land — locations lock at `docs/VISION.md` and `docs/STRATEGY.md`.
    - *Resolving* the ADR-012 NOPASSWD tension. The vision doc and ADR-015 *document* the tension; the *resolution* belongs to the v0.6+ Security Hardening milestone.
    - Authoring a Code of Conduct, SECURITY.md, or full issue / PR templates — track separately as a community-platform milestone.
    ```

    (This is lifted from `.planning/REQUIREMENTS.md` lines 117-124 — the source of truth for the post-reframe v0.3.3 out-of-scope list.)

    **Edit 5 — `## Context` section milestone summary at line 143: update "three pillars" → "two pillars" framing.**

    Current text at line 143:

    ```
    **Agenda Redefinition (v0.3.3, current):** Anchored on Jira epic AL-7. Broadens AgentLinux's mission from a single-pillar product to three pillars (separated agent environment + stability/benchmarks + security hardening), captures the new framing in a canonical product-strategy document plus an ADR, and propagates the framing to the public landing page. Forward-looking — pillar 2 (benchmarks) and pillar 3 (security) describe direction and seed v0.6+ milestones; v0.3.3 ships the framing, not the implementation.
    ```

    Replace with:

    ```
    **Agenda Redefinition (v0.3.3, current):** Anchored on Jira epic AL-7. Broadens AgentLinux's framing from a single-pillar product to two pillars (Time-to-productive + Stability) — Phase 13 verdict (b) declined a separate security pillar; the supply-chain monitoring sub-concern folds into Pillar 2. Captures the framing in `docs/VISION.md` (Phase 14), `docs/STRATEGY.md` (Phase 15), ADR-015, and the post-Phase-16 website refresh. Forward-looking — Pillar 2's mechanism work (preset/profile framework + compat-guarded update flow + Security Hardening) seeds v0.6+ milestones; v0.3.3 ships the framing, not the implementation.
    ```

    **Edit 6 — `## Key Decisions` table at lines 178-180: append a 2026-05-16 reframe row recording the two-pillar split.**

    Do NOT rewrite the existing rows at lines 178-180 (audit trail). Append ONE new row at the end of the table (after line 180, before any non-table content). Exact row:

    ```
    | **2026-05-16 reframe — two pillars + vision/strategy split** (ADR-015) | Phase 13 verdict (b) declined a separate security pillar (no honest already-shipped table-stakes; aspirational drift risk per Pitfall #6); Phase 14 user reframe split vision/strategy/roadmap into separate documents because they serve different audiences (product leadership vs contributors + AI agents). Three considered-and-rejected alternatives recorded in ADR-015. | — Active (Phase 14 lands the vision doc + ADR-015; Phase 15 lands the strategy doc; Phase 16 renumbered from old Phase 15) |
    ```

    Finally, update the file's last line (line 200):

    Current:
    ```
    *Last updated: 2026-05-09 — v0.4.0 (Open-Source Release) shipped; v0.3.3 (Agenda Redefinition, AL-7) milestone planned.*
    ```

    Replace with:

    ```
    *Last updated: 2026-05-16 — v0.4.0 (Open-Source Release) shipped; v0.3.3 (Agenda Redefinition, AL-7) reframed to two-pillar vision-only Phase 14 + strategy-doc Phase 15 + website-refresh Phase 16. ADR-015 records the framing decision.*
    ```

    Voice-rule check across the rewritten text:
    - "AgentLinux ships one working environment" — `ships` not in blocked list. PASSES.
    - "The supply-chain monitoring + curated catalog admission sub-concern folded into this pillar" — past-tense, "sub-concern" subject. PASSES.
    - "the curated toolchain holds compatible" — "toolchain" subject. PASSES.
    - "we ship one working environment" — wait, the actual text is "AgentLinux ships one working environment." Verify "ships" is not in the blocked list: blocked = (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents). "ships" PASSES.
    - "AgentLinux + present-tense verb" appears in delivered-fact form only (`ships`, `is framed`); the blocked verbs are all absent.

    Per CLAUDE.md, apply each edit with a separate Edit tool call (six total) targeting the precise anchor lines. Verify byte-stability of the un-touched milestone history sections after all six edits by spot-checking a few preserved lines (e.g. line 32 v0.4.0 milestone heading, line 156 Earlier Milestones heading area).
  </action>
  <verify>
    <automated>bash -c "grep -c 'docs/VISION.md' .planning/PROJECT.md | awk '{exit !(\$1 >= 3)}' && ! grep -q 'The three pillars (per AL-7)' .planning/PROJECT.md && ! grep -q 'measurable benchmarks' .planning/PROJECT.md && grep -q 'The two pillars (locked by Phase 13 verdict (b))' .planning/PROJECT.md && grep -q '2026-05-16 reframe' .planning/PROJECT.md && grep -q 'Last updated: 2026-05-16' .planning/PROJECT.md && grep -q 'EXPL-XX' .planning/PROJECT.md && grep -q 'VIS-XX' .planning/PROJECT.md && grep -q 'STRATR-XX' .planning/PROJECT.md && ! grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' .planning/PROJECT.md"</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 occurrences of `docs/VISION.md` in `.planning/PROJECT.md` (Core Value link, Current Milestone Goal link + Vision target-features bullet link).
    - String `The three pillars (per AL-7)` is GONE.
    - String `measurable benchmarks` is GONE (was in the stale Pillar 2 description).
    - String `The two pillars (locked by Phase 13 verdict (b))` is present.
    - A new Key Decisions row containing `2026-05-16 reframe` is present.
    - `Last updated: 2026-05-16` is present (replacing the prior `2026-05-09` line).
    - Active section lists `EXPL-XX`, `VIS-XX`, `DOC-XX`, `STRATR-XX`, `SITE-XX` as the post-reframe requirement categories.
    - Voice-rule grep returns zero matches against `.planning/PROJECT.md`.
    - Untouched delivered-fact sections (`## Previous Milestone: v0.4.0`, `### v0.3.0 AgentLinux Plugin`, `### v0.2.0 First Distro Image`, `## Requirements ## Validated` list, `## Constraints`, `## Evolution`) are byte-stable.
  </acceptance_criteria>
  <done>DOC-03 closed: .planning/PROJECT.md Core Value + Current Milestone + Active + Out-of-Scope + Context milestone summary + Key Decisions table reflect the two-pillar reframe; cross-references to VISION.md in place; three-pillar text gone.</done>
</task>

<task type="auto">
  <name>Task 4: docs/STABILITY-MODEL.md DOC-04 + VIS-08 back-pointer (Related section)</name>
  <files>docs/STABILITY-MODEL.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/STABILITY-MODEL.md (lines 117-124 — Related section)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (lines 39-46 — `### Pillar 2 — Stability` for accurate cite + anchor naming)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (DOC-04 locked shape: "Related section back-link to VISION.md Pillar 2")
  </read_first>
  <action>
    Single surgical insertion. Currently the `## Related` section at line 117 has three bulleted entries:
    1. ADR-011 link (lines 119-121)
    2. ADR-006 link (lines 122-123)
    3. README.md link (line 124)

    Append ONE new bullet as the LAST item of the Related list (after the README.md bullet at line 124). The placement is end-of-list because the README.md bullet is the easiest unique anchor to target without disturbing the multi-line ADR continuations.

    Exact new bullet:

    ```
    - [docs/VISION.md — Pillar 2: Stability](VISION.md)
      — the canonical framing this stability model implements. Pillar 2 names what the optimization target is; this doc names the mechanism (the `pinned_version` contract + the explicit reconciliation verbs).
    ```

    Voice-rule check:
    - "the canonical framing this stability model implements" — "framing" subject; "implements" not in blocked list. PASSES.
    - "Pillar 2 names what the optimization target is" — "Pillar 2" subject; "names" not in blocked list. PASSES.
    - "this doc names the mechanism" — "doc" subject. PASSES.

    Final Related list ordering after the edit:
    1. ADR-011 link (existing)
    2. ADR-006 link (existing)
    3. README.md link (existing)
    4. VISION.md Pillar 2 link (new — DOC-04)
  </action>
  <verify>
    <automated>bash -c "grep -q '\[docs/VISION.md — Pillar 2: Stability\](VISION.md)' docs/STABILITY-MODEL.md && grep -c 'VISION.md' docs/STABILITY-MODEL.md | awk '{exit !(\$1 >= 1)}' && ! grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STABILITY-MODEL.md"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/STABILITY-MODEL.md` `## Related` section contains the new bullet `- [docs/VISION.md — Pillar 2: Stability](VISION.md)` exactly once.
    - The bullet sits inside the `## Related` section (verify by reading lines 117-EOF).
    - Voice-rule grep returns zero matches against `docs/STABILITY-MODEL.md`.
    - Existing Related entries (ADR-011, ADR-006, README.md) are preserved byte-for-byte.
    - Earlier sections of the file (`## Why pin at all (the trade-off)` and prior) are byte-stable.
  </acceptance_criteria>
  <done>DOC-04 closed: docs/STABILITY-MODEL.md Related section carries a back-link to docs/VISION.md Pillar 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Reviewer pass on downstream surface edits (technical-writer + fact-checker + ai-deslop)</name>
  <what-built>
    Four downstream documentation surfaces propagated with the docs/VISION.md framing:
    - `README.md` — `## About` paragraph names the two pillars + Vision link; `## Links` carries a Vision row (DOC-01).
    - `CONTRIBUTING.md` — new `## Why this project exists` paragraph linking to VISION.md with per-pillar contribution status (DOC-02).
    - `.planning/PROJECT.md` — three-pillar framing replaced with two-pillar across Core Value, Current Milestone, Active, Out-of-Scope, Context milestone summary, Key Decisions table, and footer date (DOC-03).
    - `docs/STABILITY-MODEL.md` — Related section back-link to VISION.md Pillar 2 (DOC-04).
  </what-built>
  <how-to-verify>
    Per CLAUDE.md §Review Loop, downstream-surface edits trigger `technical-writer` + `fact-checker` + `ai-deslop`. Invoke the three reviewers in parallel against each of the four changed files, collect findings classified CRITICAL/MEDIUM/LOW:

    1. **technical-writer** — verify the new prose reads at the same register as the surrounding existing prose; no sudden marketing voice; no register breaks. Particular attention to the PROJECT.md rewrite, which touches multiple sections.
    2. **fact-checker** — verify every factual claim:
       - DOC-01 README About paragraph: two pillar names exact match against `docs/VISION.md` lines 29 + 39 (`Time-to-productive`, `Stability`).
       - DOC-02 CONTRIBUTING.md "Pillar 1 is what v0.3.0 already shipped" claim — verify against `.planning/PROJECT.md ## Previous Milestone: v0.4.0` section and `### v0.3.0 AgentLinux Plugin` section that v0.3.0 indeed shipped the agent-user / runtime / catalog surface.
       - DOC-03 PROJECT.md Phase-13 verdict cite at line "(b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3." — verify against `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17.
       - DOC-03 PROJECT.md Phase 15 / Phase 16 numbering — verify against `.planning/ROADMAP.md` lines 38-39.
       - DOC-04 STABILITY-MODEL.md "ADR-011 is the pillar-2 seed" framing — verify against `docs/decisions/011-stability-first-version-pinning.md` substance.
    3. **ai-deslop** — pattern check for slop indicators (em-dashes used as stylistic crutches, hollow phrases like "robust and scalable" or "best-in-class," repetition of stock phrases, AI-cadence rhythm). The new prose should sound like a human wrote it specifically for this repository.

    Then verify by hand the consolidated voice-rule grep on every edited file:

    ```
    for f in README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md ; do
      echo "=== $f ==="
      grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' "$f"
    done
    ```

    Every echoed section MUST be empty. Capture this transcript for Task 6's audit.

    Also verify VIS-07 still passes on `docs/VISION.md` (invariant — Plan 14-02 does NOT touch VISION.md, so this is a defensive sanity check):

    ```
    grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/VISION.md ; echo "exit=$?"
    ```

    MUST emit `exit=1` (no matches).

    Apply CRITICAL findings inline; surface MEDIUM/LOW to the user for triage. If all three reviewers return clean (zero CRITICAL) and voice-rule grep stays clean, proceed to Task 6 (audit authoring).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <files>(reviewer-pass checkpoint; no file edits unless CRITICAL findings require inline patches to README.md, CONTRIBUTING.md, .planning/PROJECT.md, or docs/STABILITY-MODEL.md)</files>
  <action>
    Invoke three project subagents in parallel via the Task tool, each scoped to the four Plan-14-02-edited files (`README.md`, `CONTRIBUTING.md`, `.planning/PROJECT.md`, `docs/STABILITY-MODEL.md`):

    1. `technical-writer` — register match against surrounding existing prose; no sudden marketing voice; PROJECT.md rewrite holds register across all six edit anchors.
    2. `fact-checker` — verify every factual claim:
       - DOC-01 README About paragraph: two pillar names exact match against `docs/VISION.md` lines 29 + 39 (`Time-to-productive`, `Stability`).
       - DOC-02 CONTRIBUTING.md "Pillar 1 is what v0.3.0 already shipped" claim — verify against `.planning/PROJECT.md ## Previous Milestone: v0.4.0` section + `### v0.3.0 AgentLinux Plugin`.
       - DOC-03 PROJECT.md Phase-13 verdict cite — `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17.
       - DOC-03 PROJECT.md Phase 15 / Phase 16 numbering — `.planning/ROADMAP.md` lines 38-39.
       - DOC-04 STABILITY-MODEL.md "ADR-011 is the pillar-2 seed" framing — `docs/decisions/011-stability-first-version-pinning.md`.
    3. `ai-deslop` — slop indicators (em-dashes as crutches, hollow phrases, AI-cadence rhythm). Per CLAUDE.md §Review Loop, ai-deslop applies to docs (skipped only for ADRs + research summaries — these are README / CONTRIBUTING / PROJECT.md / STABILITY-MODEL.md, not ADRs).

    Collect findings classified CRITICAL/MEDIUM/LOW. Apply CRITICAL findings inline against the relevant file(s). Surface MEDIUM/LOW to the user for triage; document disposition in the resume message.

    Then run the consolidated voice-rule grep across every edited file:

    ```
    for f in README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md ; do
      echo "=== $f ==="
      grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' "$f"
    done
    ```

    Every echoed section MUST be empty. Capture the full transcript for Task 6 (audit authoring).

    Also re-run VIS-07 against `docs/VISION.md` as a defensive sanity check (Plan 14-02 does NOT touch VISION.md, so VIS-07 should remain GREEN by invariant):

    ```
    grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/VISION.md ; echo "exit=$?"
    ```

    Output MUST be `exit=1` (no matches).

    Stop here for the user. Resume signal: "approved" or a description of remaining issues to address.
  </action>
  <verify>
    <automated>bash -c "for f in README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md docs/VISION.md ; do grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' \"\$f\" && exit 1 ; done ; exit 0"</automated>
  </verify>
  <done>Reviewer pass returned zero CRITICAL across the four files; voice-rule grep returns zero matches across all five files (four edited + docs/VISION.md invariant); MEDIUM/LOW disposition documented; user approved or issues addressed inline.</done>
  </task>


<task type="auto">
  <name>Task 6: Author 14-AUDIT.md (consolidates VIS-01..VIS-09 + DOC-01..DOC-05; gate emits GREEN)</name>
  <files>.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md (Plan 14-01 transcripts for VIS-01..VIS-07 — lift verbatim)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/decisions/015-agenda-redefinition.md (for VIS-09 evidence)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/exploration/PILLAR-3-CANDIDATE-NOTES.md (line 17 — EXPL-02 Verdict line for DOC-05 N/A close cite)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/REQUIREMENTS.md (every VIS-XX + DOC-XX acceptance string — quote the acceptance language under each audit section header)
    - The four files edited by Plan 14-02 Tasks 1-4: README.md, CONTRIBUTING.md, .planning/PROJECT.md, docs/STABILITY-MODEL.md (for line-range cites of the added back-pointers)
    - git log for commit-hash evidence: run `git log --oneline --follow docs/VISION.md` and `git log --oneline -- docs/decisions/015-agenda-redefinition.md`
    - Any prior `<phase-NN>-AUDIT.md` in the repo (e.g. `.planning/phases/12-pillar-2-exploration/12-AUDIT.md` if present, recent commit `e51e1ce docs(12-01): phase-close audit 12-AUDIT.md (EXPL-01 GREEN)`) — match the project's audit file shape exactly
  </read_first>
  <action>
    Author `.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md` as the consolidated phase-close audit. The audit lifts Plan 14-01 evidence transcripts verbatim and adds Plan 14-02 DOC-XX evidence — every requirement closes with a file path / line range / commit hash / grep transcript before the gate emits GREEN.

    File structure (use exactly these top-level headers, in order):

    1. `# Phase 14 Audit — Vision Doc + ADR-015 + Downstream Surface Updates` (H1) with a blockquote at the top: `> Phase: 14-vision-doc-and-downstream`, `> Authored: <today's date YYYY-MM-DD>`, `> Gate: GREEN`.

    2. `## Summary` — 1-2 paragraphs naming what Phase 14 landed (VISION.md as canonical vision; ADR-015 as framing decision; back-pointer edits in the four downstream surfaces; DOC-05 N/A per Phase 13 verdict (b); VIS-07 hard gate PASS). Then list the two plans + their commit hashes (run `git log` to capture). Then a "Total requirements closed: 14 (9 VIS + 5 DOC, of which DOC-05 is N/A)" line.

    3. `## VIS-01 — docs/VISION.md exists; size <= 6144 bytes` — quote acceptance from REQUIREMENTS.md verbatim; then "Lifted verbatim from `14-01-EVIDENCE.md` § VIS-01:" followed by a fenced code block copying the exact transcript from 14-01-EVIDENCE.md; then `Verdict: PASS.`.

    4. `## VIS-02 — Spine` — same pattern; lift the two grep transcripts from 14-01-EVIDENCE.md verbatim; verdict line.

    5. `## VIS-03 — Exactly 2 pillars` — same pattern; lift verbatim.

    6. `## VIS-04 — Guiding principles 4–6` — same pattern; lift verbatim.

    7. `## VIS-05 — What we're explicitly not >= 4` — same pattern; lift verbatim.

    8. `## VIS-06 — Last reviewed header` — same pattern; lift verbatim.

    9. `## VIS-07 — Voice-rule hard gate (HARD GATE)` — acceptance language quoted verbatim from REQUIREMENTS.md; the verbatim grep command in a fenced block; the verbatim `exit=1` output in a fenced block; `Verdict: PASS (HARD GATE GREEN).` Then a one-line note: "Plan 14-02 additionally re-ran the voice-rule grep against every Plan-14-02-edited file as a defensive check; transcripts captured under DOC-01..DOC-04 below."

    10. `## VIS-08 — Inbound cross-link map populated` — quote acceptance; run `grep -lE 'docs/VISION\.md' README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md` and embed verbatim stdout (expect 4 file paths). Then a bulleted per-file line-range list:
       - `README.md` — About paragraph at lines X-Y, Links row at line Z.
       - `CONTRIBUTING.md` — Why this project exists paragraph at lines X-Y.
       - `.planning/PROJECT.md` — Core Value link at line X, Current Milestone Goal link at line Y, Vision target-features bullet at line Z.
       - `docs/STABILITY-MODEL.md` — Related back-link at line X.
       Each line-range derived from a `grep -n 'docs/VISION\.md' <file>` invocation; embed the grep outputs verbatim. `Verdict: PASS.`

    11. `## VIS-09 — ADR-015 authored` — quote acceptance; embed verbatim: `git log --oneline -- docs/decisions/015-agenda-redefinition.md | head -1`; `grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md` (expect 5); `grep -cE '^### Alternative [0-9]' docs/decisions/015-agenda-redefinition.md` (expect 3); back-link checks (`grep -c 'AL-7'`, `grep -c 'VISION.md'`). `Verdict: PASS.`

    12. `## DOC-01 — README.md About + Links` — quote acceptance from REQUIREMENTS.md; embed verbatim: line-range grep for the new About paragraph (`grep -nE 'AgentLinux is framed around two pillars' README.md`); line-range grep for the new Links row; voice-rule grep on README.md with `exit=$?` (expect `exit=1`); commit hash reference (the Plan 14-02 commit). `Verdict: PASS.`

    13. `## DOC-02 — CONTRIBUTING.md "Why this project exists"` — quote acceptance; embed verbatim: `grep -nE '^## Why this project exists$' CONTRIBUTING.md` (line number); per-pillar contribution status grep transcripts; voice-rule grep on CONTRIBUTING.md (expect `exit=1`); commit hash. `Verdict: PASS.`

    14. `## DOC-03 — .planning/PROJECT.md Core Value + Current Milestone + Out-of-Scope refresh` — quote acceptance; embed verbatim:
       - `grep -c 'docs/VISION\.md' .planning/PROJECT.md` (expect >= 3)
       - `grep -c 'The three pillars (per AL-7)' .planning/PROJECT.md` (expect 0)
       - `grep -c 'measurable benchmarks' .planning/PROJECT.md` (expect 0)
       - `grep -n 'The two pillars (locked by Phase 13 verdict (b))' .planning/PROJECT.md` (expect line number)
       - `grep -n 'EXPL-XX\|VIS-XX\|STRATR-XX\|DOC-XX\|SITE-XX' .planning/PROJECT.md`
       - `grep -n '2026-05-16 reframe' .planning/PROJECT.md`
       - `grep -n 'Last updated: 2026-05-16' .planning/PROJECT.md`
       - voice-rule grep on `.planning/PROJECT.md` (expect `exit=1`)
       - commit hash
       `Verdict: PASS.`

    15. `## DOC-04 — docs/STABILITY-MODEL.md Related back-link` — quote acceptance; embed verbatim: line-range grep (`grep -nE '\[docs/VISION\.md — Pillar 2: Stability\]' docs/STABILITY-MODEL.md`); voice-rule grep (expect `exit=1`); commit hash. `Verdict: PASS.`

    16. `## DOC-05 — Closed N/A (Phase 13 verdict (b))` — quote the ORIGINAL acceptance from REQUIREMENTS.md (the closed-N/A version with the rationale already pre-locked); then "Disposition: N/A" paragraph naming the Phase 13 verdict; then a verbatim cite of the EXPL-02 Verdict line in a fenced block (use `sed -n '17p' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` and embed exact stdout); then a one-line note that the ADR-012 NOPASSWD tension is recorded inside Pillar 2 of VISION.md per ADR-015 Consequences. `Verdict: N/A (closed per Phase 13 verdict (b)).`

    17. `## Aggregate gate status` — a Markdown table with columns `Requirement | Verdict`; 14 rows (VIS-01..VIS-09 + DOC-01..DOC-05). Below the table: `**Phase 14 gate: GREEN.**` on its own line.

    18. `### Reviewer notes (Claude's discretion — 14-CONTEXT.md \`### Claude's Discretion\`)` — a small block summarizing the Plan 14-01 reviewer pass on ADR-015 (technical-writer + fact-checker — CRITICAL count + 1-line summary of MEDIUM/LOW disposition) and the Plan 14-02 reviewer pass on downstream edits (technical-writer + fact-checker + ai-deslop — CRITICAL count + 1-line summary). Note that VISION.md reviewer pass already ran 2026-05-16 per 14-CONTEXT.md and was not re-run.

    All `<placeholder>` markers in the structure above MUST be filled with actual values by the executor — the audit is consumed by a future reader who needs verbatim transcripts, not templated stubs.

    Hard-gate failure handling: if VIS-07 returns ANY non-empty grep result (zero matches / empty output is the PASS shape), the audit MUST emit `Phase 14 gate: RED — VIS-07 HARD-GATE FAIL` and the executor STOPS the commit (Task 7) instead of proceeding. Surface to the user for direction.

    Soft-gate failure handling (any VIS-08/DOC-01..DOC-04 with missing line-range cite or missing back-pointer): record `Verdict: FAIL` for that requirement, set the aggregate gate to RED, surface to the user before committing.

    Commit-hash strategy: the Plan 14-02 commit doesn't exist yet at audit-authoring time. Two options:
    (a) Author the audit referencing "the Plan 14-02 commit (this commit, in flight)" for the DOC-XX rows; or
    (b) Stage everything, run `git diff --cached --name-only` to confirm the set, commit, then amend the audit with the actual hash, then commit again.

    Recommended: option (a). The single commit message in Task 7 references the audit AND the four DOC files together; the audit's commit-hash field can read "(this audit + the four DOC edits ship together in the Plan 14-02 commit)" without an explicit hash. Plan 14-01 commit hashes (for VIS-01..VIS-09 references) ARE known and should be filled in.
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md && grep -cE '^## VIS-0[1-9]|^## DOC-0[1-5]' .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md | awk '{exit !(\$1 == 14)}' && grep -q 'Phase 14 gate: GREEN' .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md && grep -q 'exit=1' .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md && grep -q 'N/A (closed per Phase 13 verdict (b))' .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md && grep -q 'Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.' .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md` exists.
    - 14 requirement sections present: VIS-01..VIS-09 (9 sections) + DOC-01..DOC-05 (5 sections).
    - VIS-07 section contains the verbatim voice-rule grep command + `exit=1` output (the empty-grep PASS signature).
    - DOC-05 section is marked N/A with `Verdict: N/A (closed per Phase 13 verdict (b))` and quotes the EXPL-02 Verdict line verbatim from `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17.
    - VIS-08 section enumerates per-file line ranges of the back-pointers (one line per edited file).
    - Each DOC-01..DOC-04 section captures: line-range grep transcript + voice-rule grep transcript + commit hash reference.
    - Final aggregate table lists 13 PASS + 1 N/A; final `Phase 14 gate: GREEN.` line present.
    - No literal `<placeholder>` markers, `<verbatim>` markers, or `<today's date>` markers remain in the file — every angle-bracket-template-stub has been filled with actual transcript output.
  </acceptance_criteria>
  <done>14-AUDIT.md authored; phase-close gate emits GREEN; 13 PASS + 1 N/A verdict.</done>
</task>

<task type="auto">
  <name>Task 7: Commit Plan 14-02 deliverables (4 DOC edits + audit file)</name>
  <files>(git operation; no file edits)</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/CLAUDE.md (commit message conventions; no auto-push)
    - Output of `git status` (verify staged set is exactly the five Plan-14-02 files)
    - Output of `git diff --cached --stat` (sanity check on edit sizes — PROJECT.md should be the largest diff)
  </read_first>
  <action>
    Stage and commit the five Plan-14-02 deliverables in a single commit.

    Step 1 — stage:
    ```
    git add README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md
    ```

    Step 2 — sanity-check the staged set has exactly five files and no unrelated drift:
    ```
    git diff --cached --name-only | sort
    ```

    The expected sorted set is exactly:
    - `.planning/PROJECT.md`
    - `.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md`
    - `CONTRIBUTING.md`
    - `README.md`
    - `docs/STABILITY-MODEL.md`

    If `git diff --cached --name-only | sort` shows ANY other file, STOP — surface to the user to determine whether the extra file is in-scope or an accidental stage. Do NOT commit a polluted set.

    Step 3 — commit. Use a HEREDOC commit message in delivered-fact voice (no AgentLinux + blocked verbs):

    ```
    git commit -m "$(cat <<'EOF'
    docs(14-02): propagate vision framing + 14-AUDIT.md GREEN

    Downstream surface back-pointers to docs/VISION.md (Phase 14 reframe,
    two-pillar landing per Phase 13 verdict (b)):

    - README.md ## About paragraph names the two pillars + Vision link;
      ## Links carries Vision row (DOC-01).
    - CONTRIBUTING.md ## Why this project exists paragraph with per-pillar
      contribution status (DOC-02).
    - .planning/PROJECT.md three-pillar framing replaced with two-pillar
      across Core Value, Current Milestone, Active categories, Out-of-Scope,
      Context milestone summary, Key Decisions reframe row, footer date
      (DOC-03).
    - docs/STABILITY-MODEL.md Related section back-link to VISION.md Pillar 2
      (DOC-04).
    - DOC-05 closed N/A in 14-AUDIT.md per Phase 13 verdict (b) — pillar 3
      did not survive; no ADR-012 forward-reference edit.

    14-AUDIT.md consolidates VIS-01..VIS-09 (lifted from 14-01-EVIDENCE.md)
    + DOC-01..DOC-05; gate emits GREEN. VIS-07 voice-rule hard gate passes
    (exit=1 on docs/VISION.md).

    Drives: v0.3.3 VIS-08, DOC-01..DOC-05.
    Closes Phase 14.
    EOF
    )"
    ```

    Step 4 — verify HEAD is clean for the five files:
    ```
    git status --porcelain README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md
    ```
    Output MUST be empty.

    Do NOT push. Per CLAUDE.md, the project does not auto-push from agent commits; the milestone-close PR is the publication point.

    Step 5 — session tracking. Per CLAUDE.md §Session Tracking, this phase produced concrete deliverables (ADR-015 + the vision-doc verification + propagation). Invoke `session-tracker` skill to log the Phase 14 close on Jira AL-14 (or under AL-38 / AL-41 per the v0.3.4 anchor memory in MEMORY.md). The skill picks the right subtask.
  </action>
  <verify>
    <automated>bash -c "git log -1 --format='%s' | grep -qE '^docs\(14-02\): propagate vision framing' && git diff HEAD~1 --name-only | sort > /tmp/14-02-staged.txt && printf '.planning/PROJECT.md\n.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md\nCONTRIBUTING.md\nREADME.md\ndocs/STABILITY-MODEL.md\n' | sort > /tmp/14-02-expected.txt && diff /tmp/14-02-staged.txt /tmp/14-02-expected.txt && test -z \"\$(git status --porcelain README.md CONTRIBUTING.md .planning/PROJECT.md docs/STABILITY-MODEL.md .planning/phases/14-vision-doc-and-downstream/14-AUDIT.md)\""</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject begins `docs(14-02): propagate vision framing`.
    - `git diff HEAD~1 --name-only` returns exactly the 5 file paths listed above (in any order).
    - All 5 Plan-14-02 files report clean in `git status --porcelain`.
    - No `git push` invocation happened.
    - session-tracker skill invoked to log Phase 14 close on Jira (AL-14 or AL-41).
  </acceptance_criteria>
  <done>Phase 14 closes with a 5-file commit; 14-AUDIT.md GREEN; gate fires; downstream surfaces propagate the two-pillar framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | The four downstream surfaces (README, CONTRIBUTING, PROJECT.md, STABILITY-MODEL.md) are the surfaces external visitors (GitHub browsers, AI agents reading the repo, product-leadership reviewers) trust as authoritative. A contradiction between any two of them (e.g. README still saying "three pillars" while VISION.md says "two pillars") creates a coherence break the voice rule alone cannot catch. |
| Plan 14-02 → milestone gate | The 14-AUDIT.md file is the load-bearing close artifact. A FAIL silently recorded as PASS lets the phase emit GREEN against a broken back-pointer or a regressed voice-rule. The gate runs unattended in execute-plan — the audit's verbatim transcript discipline is the only check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Spoofing | Voice rule regression on edited files | mitigate | Task 5 (reviewer pass) runs the voice-rule grep on every Plan-14-02-edited file and captures the transcript; Task 6 (audit) captures the same transcripts as DOC-0N evidence; the automated verify on Tasks 1/2/3/4 each enforce the rule per-file inline. |
| T-14-02-02 | Tampering | PROJECT.md preserved sections drift | mitigate | Task 3 `<action>` calls out six exact edit anchors; everything else MUST stay byte-stable. Acceptance criteria for Task 3 explicitly name sections that MUST stay byte-stable (Previous Milestone v0.4.0, v0.3.0 / v0.2.0 history, Validated requirements list, Constraints, Evolution). Reviewer pass in Task 5 verifies. |
| T-14-02-03 | Repudiation | DOC-05 N/A close vs Phase 13 verdict | mitigate | Task 6 audit copies the EXPL-02 Verdict line verbatim (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17) so a future reader can verify the N/A close without re-reading Phase 13. The line text is grep-checkable. |
| T-14-02-04 | Information disclosure | Stale benchmark / security framing | mitigate | Task 3 removes the "measurable benchmarks" Pillar 2 text + the entire Pillar 3 security entry from PROJECT.md. Acceptance criteria explicitly grep for absence of those strings. |
| T-14-02-05 | Denial of service | Phase-close gate | accept | The phase-close gate (14-AUDIT.md) is informational, not blocking on any external runtime. No DoS surface. |
| T-14-02-06 | Elevation of privilege | Plan touches no executable surface | accept | Plan modifies only Markdown files (README, CONTRIBUTING, .planning/PROJECT.md, docs/STABILITY-MODEL.md, audit file). No CI / installer / catalog / runtime code is touched. |
| T-14-02-07 | Tampering | Audit-file `<placeholder>` leakage | mitigate | Task 6 acceptance criteria explicitly states "no `<placeholder>` markers remain in the file." A template-stub left in the audit would render the gate verdict untrustworthy. |
</threat_model>

<verification>
Phase-level checks for Plan 14-02 + consolidated phase close:
- DOC-01..DOC-04 closed: README, CONTRIBUTING, PROJECT.md, STABILITY-MODEL.md each carry the prescribed back-pointer; line-range cites captured in 14-AUDIT.md.
- DOC-05 closed N/A with verbatim cite of EXPL-02 Verdict line.
- VIS-08 closed: inbound back-pointer count is 4 (one per edited file).
- 14-AUDIT.md exists; 14 requirement sections; aggregate gate GREEN.
- Voice-rule grep returns zero matches against all five files in scope (4 edited + docs/VISION.md unchanged from Plan 14-01).
- Git: HEAD commit subject begins `docs(14-02):`; 5 files in the diff; clean working tree on all 5.
</verification>

<success_criteria>
- DOC-01..DOC-04 acceptance criteria each PASS individually (per-task verify automation).
- DOC-05 recorded N/A in 14-AUDIT.md with cited rationale.
- VIS-08 acceptance: 4 downstream surfaces carry back-pointers to docs/VISION.md.
- 14-AUDIT.md emits GREEN with 13 PASS + 1 N/A verdicts.
- VIS-07 voice-rule hard gate stays GREEN against docs/VISION.md (invariant; Plan 14-02 does not touch VISION.md).
- Voice rule passes on all four Plan-14-02-edited files (defensive practice; not a formal gate but enforced via per-task verify automation).
- Reviewer pass (technical-writer + fact-checker + ai-deslop on downstream edits) returns zero CRITICAL.
- Single commit lands the five files with subject `docs(14-02): propagate vision framing + 14-AUDIT.md GREEN`.
- session-tracker skill invoked to log Phase 14 close on Jira AL-14 / AL-41.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vision-doc-and-downstream/14-02-SUMMARY.md` capturing:
- The Plan 14-02 commit hash.
- 14-AUDIT.md gate verdict (GREEN expected) + the requirement-by-requirement verdict table.
- Reviewer-pass outcomes for the downstream edits (CRITICAL count + MEDIUM/LOW disposition summary).
- Phase 14 close confirmation: VIS-01..VIS-09 + DOC-01..DOC-04 closed PASS; DOC-05 closed N/A.
- Pointer forward to Phase 15 (Strategy + Roadmap Doc) as the next phase.
- session-tracker invocation outcome (Jira ticket URL where Phase 14 close was logged).
</output>
