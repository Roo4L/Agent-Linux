---
phase: 14-vision-doc-and-downstream
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md
  - docs/decisions/015-agenda-redefinition.md
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - VIS-07
  - VIS-09

must_haves:
  truths:
    - "docs/VISION.md exists at the committed path with file size <= 6144 bytes"
    - "docs/VISION.md spine matches VIS-02 (Mission, Positioning subsection, two pillars, Guiding principles, What we're explicitly not)"
    - "docs/VISION.md contains exactly two ### Pillar headings (VIS-03 / Phase 13 verdict (b))"
    - "docs/VISION.md contains a `> Last reviewed: 2026-05-` blockquote in the top 5 lines"
    - "VIS-07 voice-rule grep gate returns zero matches against docs/VISION.md"
    - "docs/decisions/015-agenda-redefinition.md exists with Status / Context / Decision / Considered alternatives / Consequences sections"
    - "ADR-015 lists at least 3 considered-and-rejected alternatives, includes back-links to AL-7 and docs/VISION.md, and cites EXPL-01 + EXPL-02 verdicts"
  artifacts:
    - path: ".planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md"
      provides: "Pre-audit verification transcripts (wc -c, grep matches, head outputs) for VIS-01..VIS-07; consumed verbatim by 14-AUDIT.md in Plan 14-02"
      contains: "VIS-01"
    - path: "docs/decisions/015-agenda-redefinition.md"
      provides: "ADR recording the AL-7 framing decision (two pillars + vision-only doc separated from strategy)"
      contains: "## Considered alternatives"
  key_links:
    - from: "docs/decisions/015-agenda-redefinition.md"
      to: "docs/VISION.md"
      via: "back-link in Decision and References sections"
      pattern: "docs/VISION\\.md"
    - from: "docs/decisions/015-agenda-redefinition.md"
      to: "Jira AL-7"
      via: "back-link in References section"
      pattern: "AL-7"
    - from: ".planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md"
      to: "docs/VISION.md"
      via: "captured grep + wc transcripts cite docs/VISION.md by path"
      pattern: "docs/VISION\\.md"
---

<objective>
Verify the already-committed `docs/VISION.md` (commit `864d64c` 2026-05-16, post technical-writer + fact-checker polish) against VIS-01..VIS-07 and capture every grep / wc / head transcript into a phase-01 evidence file. Then author ADR-015 (`docs/decisions/015-agenda-redefinition.md`) recording the AL-7 framing decision — two pillars (Phase 13 verdict (b)) and a vision-only document separated from strategy/roadmap (user reframe 2026-05-16). VIS-08 (downstream back-pointer fan-out), DOC-01..DOC-04 (downstream doc edits), DOC-05 (N/A close), and the consolidated `14-AUDIT.md` phase-close gate all belong to Plan 14-02.

Purpose: Lock the vision artifacts (VISION.md as already-shipped spec; ADR-015 as the framing decision record) inside the Phase 14 commit window so downstream propagation in Plan 14-02 can back-point to them with stable paths.

Output:
- `.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md` — captured grep / wc / head transcripts for VIS-01..VIS-07; consumed verbatim by Plan 14-02's `14-AUDIT.md`.
- `docs/decisions/015-agenda-redefinition.md` — ADR-015, status Accepted, dated 2026-05-16.
- Commit landing both files (after the reviewer pass on the ADR draft passes clean).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md
@docs/VISION.md
@docs/exploration/PILLAR-2-NOTES.md
@docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
@docs/decisions/000-template.md
@docs/decisions/014-secret-remediation-noop.md
@CLAUDE.md

<voice_rule>
The voice rule (PITFALLS.md, enforced by VIS-07 hard gate) applies to every
line of new text authored in this plan, including ADR-015 prose. The rule:
every claim about an unshipped behavior MUST appear in a sentence whose
grammatical subject is "we" / "our roadmap" / an explicit milestone
identifier — NEVER `AgentLinux + present-tense verb` for an unshipped
capability. The blocked verb list for `AgentLinux` as subject (case-sensitive
since the regex `^[^a-z]*` permits lines starting with capitals/whitespace):
`provides`, `offers`, `ensures`, `protects`, `defends`, `benchmarks`,
`measures`, `hardens`, `isolates`, `detects`, `prevents`. The committed
docs/VISION.md already passes the gate; new ADR-015 text MUST author within
the same rule so the audit gate (run in Plan 14-02) stays GREEN.

Bypass note: claims about behavior already SHIPPED (e.g. ADR-004 + AGT-02
zero-EACCES self-update, ADR-011 stability model, AL-7 framing decision
itself) may use delivered-fact voice; the regex only blocks the forward-
looking verbs above. When in doubt, rewrite with "we" / "our roadmap" /
"v0.3.3" / "v0.6+" as the grammatical subject.
</voice_rule>

<interfaces>
<!-- Key reference paths and exact strings the executor must use. -->

docs/VISION.md (already committed; verification target — do NOT re-author):
- Spine headings: `## Mission`, `### Positioning`, `## The two pillars`,
  `### Pillar 1 — Time-to-productive`, `### Pillar 2 — Stability`,
  `## Guiding principles`, `## What we're explicitly not`.
- Top-of-file `> Last reviewed: 2026-05-16` blockquote.
- Currently 4500 bytes (well under 6144 ceiling).
- Commit: `864d64c` (docs(14): draft docs/VISION.md (Phase 14 reframe — vision-only)).

docs/decisions/014-secret-remediation-noop.md (voice reference for ADR-015):
- H1: `# 014: Secret remediation for v0.4.0 — no rotation required`
- Frontmatter style: `**Status:** Accepted`, `**Date:** YYYY-MM-DD`,
  optional `**Drives:** ...`, optional `**Companion to:** ...`.
- Spine: `## Context`, `## Decision`, `## Consequences`, `## References`.
- Tone: factual, no aspirational verbs.

docs/decisions/000-template.md (skeletal ADR template):
- H1: `# NNN: [Title]`
- Frontmatter: `**Status:** ...`, `**Date:** YYYY-MM-DD`
- Spine: `## Context`, `## Decision`, `## Consequences`.

Locked ADR-015 substance (verbatim from 14-CONTEXT.md `### ADR-015 — agenda redefinition (VIS-09)`):
- Title: `015: Agenda redefinition — two pillars, vision-only doc`.
- Status: Accepted. Date: 2026-05-16.
- Drives: VIS-09 (Phase 14).
- Considered alternatives (≥3): (1) stay single-pillar; (2) ship vision +
  strategy + roadmap + trade-offs in one `docs/STRATEGY.md` per original
  Phase 14 plan; (3) pivot security-first to a Pillar 3.
- Consequences MUST mention: Phase 15 strategy-doc insertion; Phase 15 →
  Phase 16 renumber of website-refresh; DOC-05 closes N/A; ADR-012 NOPASSWD
  tension recorded inside Pillar 2 of VISION.md as a known limitation
  rather than via an ADR-012 file edit.
- Back-links to AL-7 (https://copiedwonder.atlassian.net/browse/AL-7) and
  to `docs/VISION.md`.
- Grep gate (VIS-09 acceptance): `grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md` returns at least 5.

Verification commands the executor must run verbatim (capture stdout exactly):
- VIS-01: `wc -c docs/VISION.md`
- VIS-02: `grep -nE '^## (Mission|The two pillars|Guiding principles|What we'\''re explicitly not)' docs/VISION.md`
- VIS-02: `grep -nE '^### Positioning' docs/VISION.md`
- VIS-03: `grep -cE '^### Pillar [0-9]+' docs/VISION.md`
- VIS-03: `grep -nE '^### Pillar [0-9]+' docs/VISION.md`
- VIS-03 (negative): `grep -nE '^#### (Today|Direction)' docs/VISION.md`
- VIS-04 (count principles): `awk '/^## Guiding principles/{flag=1;next} /^## /{flag=0} flag && /^### /{c++} END{print c}' docs/VISION.md`
- VIS-05 (non-goal bullets): `awk '/^## What we'\''re explicitly not/{flag=1;next} /^## /{flag=0} flag && /^- /{c++} END{print c}' docs/VISION.md`
- VIS-06: `head -5 docs/VISION.md | grep -E '^> Last reviewed: 2026-05'`
- VIS-07 (voice-rule grep gate — MUST return zero matches; exit code 1):
  `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/VISION.md ; echo "exit=$?"`

Git commit hash command (for the evidence file):
`git log -1 --format='%H %s' docs/VISION.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify docs/VISION.md against VIS-01..VIS-07; capture transcripts to 14-01-EVIDENCE.md</name>
  <files>.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (the verification target — read end-to-end so the executor knows the file's exact shape before scripting greps against it)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/REQUIREMENTS.md (VIS-01..VIS-07 acceptance language — quoted verbatim into the evidence file headers)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (locked decisions on size ceiling, spine, voice rule)
  </read_first>
  <action>
    Create `.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md` as a pre-audit transcript file. The file is consumed verbatim by Plan 14-02's `14-AUDIT.md` — its purpose is to capture every command + output once, so the audit cites it rather than re-running commands.

    Run each verification command in the `<interfaces>` block verbatim from the worktree root. For each command, capture the EXACT stdout (and stderr if non-empty) and embed it in a fenced code block under the requirement header. Do NOT paraphrase outputs — the audit gate requires verbatim transcripts.

    File structure (use exactly these headers and order):

    ```markdown
    # Phase 14 Plan 01 — Pre-Audit Evidence

    > Captured: <today's date YYYY-MM-DD>
    > Consumed by: `14-AUDIT.md` (authored in Plan 14-02).
    > Target commit for docs/VISION.md: `864d64c` (or its descendants in the Phase 14 commit window).

    ## VIS-01 — docs/VISION.md exists; size <= 6144 bytes

    **Acceptance:** `wc -c docs/VISION.md` returns a byte count <= 6144.

    Command:
    ```
    wc -c docs/VISION.md
    ```

    Output:
    ```
    <verbatim stdout>
    ```

    Verdict: PASS (size <X> <= 6144) | FAIL.

    Commit context:
    ```
    <verbatim `git log -1 --format='%H %s' docs/VISION.md` stdout>
    ```

    ## VIS-02 — Spine: Mission / Positioning / two pillars / Guiding principles / What we're explicitly not

    **Acceptance:** `grep -nE '^## (Mission|The two pillars|Guiding principles|What we'\''re explicitly not)' docs/VISION.md` returns >= 4 matches in that order; `grep -nE '^### Positioning' docs/VISION.md` returns >= 1 match.

    Command 1: <verbatim>
    Output 1: <verbatim>
    Command 2: <verbatim>
    Output 2: <verbatim>

    Verdict: PASS (4 H2 spine matches + 1 Positioning subsection) | FAIL.

    ## VIS-03 — Exactly 2 pillars; no Today/Direction subsections

    **Acceptance:** `grep -cE '^### Pillar [0-9]+' docs/VISION.md` returns exactly 2; `grep -nE '^#### (Today|Direction)' docs/VISION.md` returns zero matches.

    Command 1 (count): <verbatim>
    Output 1: <verbatim>
    Command 2 (list): <verbatim>
    Output 2: <verbatim>
    Command 3 (negative): <verbatim>
    Output 3: <verbatim — expect empty>

    Verdict: PASS | FAIL.

    ## VIS-04 — Guiding principles: 4–6 `###` entries

    **Acceptance:** awk-counted `###` entries under `## Guiding principles` is in [4..6]. Principles are vision-level identity claims, NOT execution rules.

    Command: <verbatim awk command>
    Output: <verbatim integer>

    Verdict: PASS (count <N>, in [4..6]) | FAIL.

    ## VIS-05 — What we're explicitly not: >= 4 bullet items

    **Acceptance:** awk-counted top-level bullets under `## What we're explicitly not` is >= 4.

    Command: <verbatim>
    Output: <verbatim integer>

    Verdict: PASS (count <N>, >= 4) | FAIL.

    ## VIS-06 — `> Last reviewed: 2026-05-...` in top 5 lines

    **Acceptance:** `head -5 docs/VISION.md | grep -E '^> Last reviewed: 2026-05'` returns exactly 1 match.

    Command: <verbatim>
    Output: <verbatim>

    Verdict: PASS | FAIL.

    ## VIS-07 — Voice-rule hard gate (HARD GATE; zero matches required)

    **Acceptance:** The grep returns zero matches; the grep command + empty output get committed verbatim to `14-AUDIT.md` (Plan 14-02 lifts this block).

    Command:
    ```
    grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/VISION.md ; echo "exit=$?"
    ```

    Output:
    ```
    exit=1
    ```
    (Empty grep output + `exit=1` is the PASS shape. Any other shape is a HARD-GATE FAIL — STOP and surface to the user instead of continuing.)

    Verdict: PASS | FAIL.

    ## Aggregate gate status

    VIS-01: PASS/FAIL
    VIS-02: PASS/FAIL
    VIS-03: PASS/FAIL
    VIS-04: PASS/FAIL
    VIS-05: PASS/FAIL
    VIS-06: PASS/FAIL
    VIS-07: PASS/FAIL (HARD GATE)

    Plan 01 verdict: <PASS if all 7 PASS; FAIL otherwise; HARD-GATE FAIL if VIS-07 fails>.
    ```

    Hard-gate failure handling: if VIS-07 returns ANY match, STOP this plan immediately. Do NOT attempt to patch docs/VISION.md silently — surface the matching line(s) to the user. Per 14-CONTEXT.md `<specifics>`: "if the planner finds a gap, the plan can patch VISION.md as part of closing the gap — but the user-validated structure stays." Any patch needed beyond the polish already applied 2026-05-16 requires user confirmation.

    Soft-gate failure handling (VIS-01..VIS-06): if any of VIS-01..VIS-06 fails, record the FAIL verbatim in the evidence file, then STOP and surface to the user for direction. Do NOT silently re-author the vision doc.

    Per CLAUDE.md, all paths in commands are repo-root-relative (the worktree at `/home/agent/agent-linux/.claude/worktrees/agenda`). The executor runs from the worktree root.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md &amp;&amp; grep -cE '^## VIS-0[1-7]' .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md | grep -qE '^7$' &amp;&amp; grep -qE '^Plan 01 verdict: PASS' .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md &amp;&amp; grep -qE '^exit=1$' .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md` exists.
    - File contains seven `## VIS-0N` section headers (VIS-01 through VIS-07).
    - VIS-07 section contains the literal voice-rule grep command + an `exit=1` line (the empty-grep PASS signature).
    - Aggregate verdict line reads `Plan 01 verdict: PASS`.
    - No claim in the evidence file is paraphrased — every output block is verbatim stdout from the executed command.
    - If ANY VIS-0N fails (especially VIS-07), the executor STOPPED here and surfaced to the user instead of proceeding to Task 2.
  </acceptance_criteria>
  <done>VIS-01..VIS-07 verified PASS against committed docs/VISION.md; transcripts captured verbatim for downstream consumption by 14-AUDIT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/decisions/015-agenda-redefinition.md (ADR-015, VIS-09)</name>
  <files>docs/decisions/015-agenda-redefinition.md</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/decisions/000-template.md (skeletal ADR shape)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/decisions/014-secret-remediation-noop.md (voice + frontmatter reference — extended spine with Drives/Companion-to/References)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/phases/14-vision-doc-and-downstream/14-CONTEXT.md (locked ADR-015 substance: alternatives list, consequences list, back-links)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/VISION.md (the artifact this ADR records the decision behind; ADR back-links to it)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/exploration/PILLAR-2-NOTES.md (lines 1-100 — Phase 12 verdict; cited in ADR-015 Decision section)
    - /home/agent/agent-linux/.claude/worktrees/agenda/docs/exploration/PILLAR-3-CANDIDATE-NOTES.md (lines 1-30 — Phase 13 Verdict section; cited in ADR-015 Decision section as the pillar-count lock)
    - /home/agent/agent-linux/.claude/worktrees/agenda/.planning/ROADMAP.md (lines 60-80 — Phase 14 spec; for Consequences section accuracy on Phase 15 / Phase 16 renumber)
  </read_first>
  <action>
    Author `docs/decisions/015-agenda-redefinition.md` as the ADR recording the AL-7 framing decision. Follow the spine and voice of `docs/decisions/014-secret-remediation-noop.md`. Substance is locked by 14-CONTEXT.md `### ADR-015 — agenda redefinition (VIS-09)` — author within that envelope.

    Exact frontmatter (lines 1-6):

    ```markdown
    # 015: Agenda redefinition — two pillars, vision-only doc

    **Status:** Accepted
    **Date:** 2026-05-16
    **Drives:** v0.3.3 VIS-09 (Phase 14)
    **Companion to:** docs/VISION.md, docs/exploration/PILLAR-2-NOTES.md, docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Required sections (the spine — must each appear as a `## ` heading; the VIS-09 grep gate counts exactly these five):

    ## Context
    One paragraph framing AL-7 ("Project agenda redefinition") and three subordinate paragraphs:
    1. Why the original single-pillar framing ("separated, correctly-owned agent environment" — v0.3.0 core) was getting in the way: too narrow to position against agent-environment competitors; did not give external contributors a defensible product story; collapsed the stability + supply-chain story into installer-plumbing detail.
    2. Why the original three-pillar bundle (separated env + stability/benchmarks + security hardening, per AL-7 2026-05-09) was getting in the way once we explored it: Phase 13 (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` § Verdict, line 17) found no honest already-shipped table-stakes for security as a pillar — every commitment was forward-looking and would force aspirational drift (Pitfall #6). Phase 12 (`docs/exploration/PILLAR-2-NOTES.md` § Decision summary) consolidated stability + time-to-productive into a single pillar named by its optimization values, not by historical engineering vocabulary.
    3. Why the original combined vision + strategy + roadmap + framework-trade-offs document (per the 2026-05-09 ROADMAP) was getting in the way: vision-level identity claims and execution-level rules + roadmap themes serve different audiences and benefit from doc-level separation; mixing them invited drift on both axes; the framework-shape trade-offs (Sourcegraph template vs Lean Canvas vs BMC vs PR-FAQ) added ceremony for no reader value.

    ## Decision
    Two paragraphs. First paragraph names the decision:

    - **Two pillars, named by optimization value.** Pillar 1 — Time-to-productive. Pillar 2 — Stability. Locked by Phase 13 verdict (b) (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`: "Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.") and Phase 12 Decision summary (`docs/exploration/PILLAR-2-NOTES.md`).
    - **Vision-only document at `docs/VISION.md`, separated from strategy/roadmap.** The strategy/roadmap content (execution principles, themes for v0.6+, near-term focus, current state) moves to `docs/STRATEGY.md`, authored in Phase 15. Locked by user reframe 2026-05-16 during `/gsd-discuss-phase`.

    Second paragraph references the artifacts:
    `docs/VISION.md` is the canonical "what we want to be" reference; `docs/STRATEGY.md` (Phase 15, forthcoming) will be the canonical "how we get there" companion. The vision doc carries the Pillar 2 sub-concern (active supply-chain monitoring + curated catalog admission) from Phase 13 verdict (b), and records the ADR-012 NOPASSWD tension inside Pillar 2's section as a known limitation rather than via an ADR-012 file edit (which closes DOC-05 as N/A in `14-AUDIT.md`).

    ## Considered alternatives
    Exactly three subsections, each as a `### `-level heading + a "Rejected: ..." line + 2-4 sentences of rationale. Order them by Claude's discretion (per 14-CONTEXT.md `### Claude's Discretion`); recommended order = chronology of how each got rejected:

    ### Alternative 1 — Stay single-pillar
    Rejected: AL-7 explicitly called for broadening. Continuing to ship as "separated, correctly-owned agent environment" alone left no room for the stability story (curated catalog, compat-guarded version pinning, ADR-011) that the v0.3.0 reality already supported. Single-pillar framing was the starting point AL-7 set out to leave behind.

    ### Alternative 2 — Ship vision + strategy + roadmap + framework trade-offs in one `docs/STRATEGY.md` (original Phase 14 plan)
    Rejected: user reframe 2026-05-16 mid-`/gsd-discuss-phase`. Vision-level identity claims and execution-level rules serve different audiences (product leadership reading "what is AgentLinux" vs contributors and AI agents reading "how do we operate"); mixing them invited drift on both axes. The framework-shape trade-offs (Sourcegraph template vs Lean Canvas vs Business Model Canvas vs PR-FAQ vs OKRs) added ceremony for no reader value. Splitting the deliverable into VISION.md (Phase 14) and STRATEGY.md (Phase 15) costs one extra commit window but produces a sharper artifact at each path.

    ### Alternative 3 — Pivot security-first to a Pillar 3
    Rejected: Phase 13 verdict (b) (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` § Verdict, line 17). No honest already-shipped table-stakes for security as a pillar — every candidate commitment (capability-scoped sudoers, cosign-signed catalog, npm provenance, bubblewrap sandbox, iptables egress allowlist) was forward-looking and would have forced aspirational drift per Pitfall #6 ("voice-rule discipline catches future-tense leaking into present-tense product claims"). The one honest forward commitment (active supply-chain monitoring + curated catalog admission) folds into Pillar 2 as a sub-concern; the rest stays as a v0.6+ `opportunistic` theme in the forthcoming `docs/STRATEGY.md`.

    ## Consequences
    Bulleted list (use `- ` items, not numbered):

    - **Phase 15 strategy/roadmap document inserted** at `docs/STRATEGY.md` (`.planning/phases/15-strategy-roadmap-doc/`). Hosts the execution principles + themes for v0.6+ + near-term focus content cut from Phase 14. Lands after Phase 14 closes so it can cite VISION.md as upstream "what."
    - **Phase 15 → Phase 16 renumber.** The previously-numbered Phase 15 (website refresh at agentlinux.org) becomes Phase 16. Phase directories renamed in the same ROADMAP rewrite 2026-05-16.
    - **DOC-05 closes N/A.** Phase 13 verdict (b) means there is no Pillar 3, so the original DOC-05 acceptance ("`docs/decisions/012-agent-user-full-sudo.md` gains a forward-reference to Pillar 3") is not applicable. The Phase 14 audit (`14-AUDIT.md`, Plan 14-02) records DOC-05 as N/A with a one-line rationale and cites EXPL-02's `## Verdict` line.
    - **ADR-012 NOPASSWD tension recorded inside Pillar 2 of VISION.md**, not via an ADR-012 file edit. The unresolved trade-off (passwordless sudo for the agent user vs the future Pillar 2 supply-chain monitoring sub-concern) lives as a known limitation in the vision doc until a Security Hardening milestone resolves it.
    - **Voice rule (PITFALLS.md) becomes a hard gate** at VIS-07 (Phase 14, on VISION.md), STRATR-06 (Phase 15, on STRATEGY.md), and SITE-06 (Phase 16, on rendered HTML). The grep is the spec; the command + output get committed verbatim to each phase's AUDIT file.
    - **Cadence binding deferred.** The `/gsd-complete-milestone` template amendment that would update the VISION.md `> Last reviewed:` header (and a future STRATEGY.md equivalent) on every milestone close is flagged for the v0.3.3 retrospective, not in-milestone. Pitfall #12 / #23 mitigation.

    ## References

    - [AL-7 — Project agenda redefinition](https://copiedwonder.atlassian.net/browse/AL-7) — Jira epic anchoring the milestone.
    - [docs/VISION.md](../VISION.md) — the canonical vision document this ADR records the framing decision behind.
    - [docs/exploration/PILLAR-2-NOTES.md](../exploration/PILLAR-2-NOTES.md) — Phase 12 verdict (EXPL-01), cited in Decision section above.
    - [docs/exploration/PILLAR-3-CANDIDATE-NOTES.md](../exploration/PILLAR-3-CANDIDATE-NOTES.md) — Phase 13 verdict (b) (EXPL-02), cited as the pillar-count lock.
    - [.planning/REQUIREMENTS.md](../../.planning/REQUIREMENTS.md) §"Superseded Items (2026-05-16 reframe)" — the requirement-ID delta from the reframe (STRAT-* → VIS-* + STRATR-*).

    Voice rule enforcement: review every sentence before writing. Any unshipped behavior MUST use "we" / "our roadmap" / "v0.3.3" / "v0.6+" as the grammatical subject — never `AgentLinux + present-tense verb` from the blocked list. The committed VISION.md already passes the gate (verified in Task 1); ADR-015 MUST author within the same rule so the audit gate stays GREEN. After authoring, run the same voice-rule grep against `docs/decisions/015-agenda-redefinition.md` (optional sanity check; not a formal gate — the formal gate is only on VISION.md / STRATEGY.md / index.html, not ADRs — but a defensive practice given how often ADR-015's prose names the product).

    Do NOT reintroduce the narrow problem list (EACCES, recursive shim, self-update breakage) into ADR-015's Context section. Per 14-CONTEXT.md `<specifics>`: the user pushed back on that framing during smart-discuss. The single-pillar framing's weakness is positional ("too narrow against agent-environment competitors"), not anecdotal.
  </action>
  <verify>
    <automated>test -f docs/decisions/015-agenda-redefinition.md &amp;&amp; grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md | grep -qE '^[5-9]$' &amp;&amp; grep -qE '^# 015: Agenda redefinition' docs/decisions/015-agenda-redefinition.md &amp;&amp; grep -qE '^\*\*Status:\*\* Accepted$' docs/decisions/015-agenda-redefinition.md &amp;&amp; grep -qE '^\*\*Date:\*\* 2026-05-16$' docs/decisions/015-agenda-redefinition.md &amp;&amp; grep -cE '^### Alternative [0-9]' docs/decisions/015-agenda-redefinition.md | grep -qE '^[3-9]$' &amp;&amp; grep -qE 'AL-7' docs/decisions/015-agenda-redefinition.md &amp;&amp; grep -qE 'VISION\.md' docs/decisions/015-agenda-redefinition.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/decisions/015-agenda-redefinition.md` exists.
    - H1: `# 015: Agenda redefinition — two pillars, vision-only doc`.
    - Frontmatter: `**Status:** Accepted`, `**Date:** 2026-05-16`, plus `**Drives:**` and `**Companion to:**` lines.
    - VIS-09 spine grep returns >= 5 H2 headings: `## Status` is actually a frontmatter style, NOT an H2 — re-check: `## Context`, `## Decision`, `## Considered alternatives`, `## Consequences`, `## References`. The VIS-09 grep is `grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md` and requires >= 5. If `Status` is in frontmatter (not an H2), the grep counts 4 H2s + the frontmatter `**Status:**` line does NOT match `^## `. Mitigation: add `## References` so 4 H2s + we still need >= 5. RESOLUTION: the VIS-09 grep regex `^## (Status|...)` is a documentation typo from REQUIREMENTS.md — Status is conventionally a frontmatter field, not an H2. The acceptance language allows >= 5 in the alternation; achieving 4 of the named H2s + 1 References gets us to 5 only if `## References` matches the alternation, which it does NOT. The executor MUST add an additional H2 to satisfy the regex literally: include `## Status` as a one-line H2 *in addition* to the `**Status:**` frontmatter line, repeating the value (e.g. `## Status\n\nAccepted (2026-05-16).`). This satisfies the literal regex while preserving ADR-014's frontmatter style.
    - Considered alternatives: at least 3 `### Alternative N — ...` subsections.
    - Back-links present: at least one `AL-7` mention and at least one `VISION.md` link.
    - Voice rule: a manual scan finds no `AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b` line. (Not a formal gate; defensive practice.)
    - Narrow problem list (EACCES / recursive shim / self-update) does NOT appear in Context — per 14-CONTEXT.md user pushback.
  </acceptance_criteria>
  <done>ADR-015 authored per locked substance; VIS-09 acceptance grep returns >= 5; back-links to AL-7 + VISION.md + exploration notes present; no aspirational drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reviewer pass on ADR-015 (technical-writer + fact-checker)</name>
  <what-built>
    `docs/decisions/015-agenda-redefinition.md` authored from locked substance. ADR-015 records the AL-7 framing decision: two pillars (Phase 13 verdict (b)) and a vision-only document separated from strategy/roadmap (user reframe 2026-05-16).
  </what-built>
  <how-to-verify>
    Per CLAUDE.md §Review Loop, docs trigger `technical-writer` + `fact-checker` reviewers. ADRs skip `ai-deslop` per the project guideline. The executor invokes the two reviewers in parallel against `docs/decisions/015-agenda-redefinition.md` and surfaces:

    1. **technical-writer** findings, classified CRITICAL/MEDIUM/LOW. Apply CRITICAL findings without asking; surface MEDIUM/LOW to the user for triage.
    2. **fact-checker** findings on every factual claim. Verify especially: (a) Phase 13 verdict (b) cite (`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17 says "Verdict: (b) Fold into Pillar 2 as sub-concern"); (b) Phase 12 Decision summary cite; (c) the date 2026-05-16 matches the user reframe + `> Last reviewed:` blockquote; (d) ROADMAP claim that Phase 15 / Phase 16 renumber happened 2026-05-16 — verify against `.planning/ROADMAP.md` line 17.

    Then verify by hand:
    - `grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md` returns 5 (literal regex match of the VIS-09 acceptance grep).
    - `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/decisions/015-agenda-redefinition.md` returns zero matches (defensive practice; voice rule applied even though not formally gated on ADRs).
    - ADR-015 does NOT contain the narrow problem list (EACCES / recursive shim / Claude Code self-update breakage); the Context section's single-pillar critique is positional, not anecdotal.

    Apply CRITICAL findings inline; surface MEDIUM/LOW to the user for triage. If both reviewers return clean (zero CRITICAL), proceed to Task 4.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <files>(reviewer-pass checkpoint; no file edits unless CRITICAL findings require inline patches to docs/decisions/015-agenda-redefinition.md)</files>
  <action>
    Invoke two project subagents in parallel via the Task tool, each scoped to `docs/decisions/015-agenda-redefinition.md` only:

    1. `technical-writer` — register match, prose register, voice rule, ADR voice consistent with `docs/decisions/014-secret-remediation-noop.md`.
    2. `fact-checker` — verify every factual claim against the cited sources. Particular attention to:
       - Phase 13 verdict (b) cite: `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17 reads `**Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.` Verify the ADR-015 cite matches.
       - Phase 12 Decision summary cite: `docs/exploration/PILLAR-2-NOTES.md` `## Decision summary` (line 75+).
       - Date 2026-05-16 matches the user reframe + the `> Last reviewed:` blockquote in `docs/VISION.md`.
       - Phase 15 / Phase 16 renumber claim: verify against `.planning/ROADMAP.md` line 17 (the locked-2026-05-16 reframe note).
       - ADR-012 reference: verify the file path `docs/decisions/012-agent-user-full-sudo.md` exists and the NOPASSWD framing matches that ADR's substance.

    Skip `ai-deslop` per CLAUDE.md §Review Loop ("Docs → technical-writer, fact-checker, ai-deslop (skip for ADRs and research summaries)") — ADR-015 IS an ADR.

    Collect findings classified CRITICAL/MEDIUM/LOW. Apply CRITICAL findings inline against `docs/decisions/015-agenda-redefinition.md` (edit the file before this task completes). Surface MEDIUM/LOW to the user for triage and document the disposition (applied vs deferred) in the resume message.

    Then run two by-hand sanity checks:
    - `grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md` — MUST return 5 (literal VIS-09 acceptance grep).
    - `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/decisions/015-agenda-redefinition.md` — MUST return zero matches (defensive voice-rule application on ADRs; not a formal gate).

    Stop here for the user. Resume signal: "approved" or a description of remaining issues to address.
  </action>
  <verify>
    <automated>bash -c "grep -cE '^## (Status|Context|Decision|Considered alternatives|Consequences)' docs/decisions/015-agenda-redefinition.md | awk '{exit !(\$1 == 5)}' && ! grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/decisions/015-agenda-redefinition.md"</automated>
  </verify>
  <done>Reviewer pass returned zero CRITICAL; ADR-015 spine grep returns 5; voice-rule grep returns zero matches; MEDIUM/LOW disposition documented; user approved or issues addressed inline.</done>
  </task>

<task type="auto">
  <name>Task 4: Commit Plan 14-01 deliverables (ADR-015 + evidence file)</name>
  <files>(git operation; no file edits)</files>
  <read_first>
    - /home/agent/agent-linux/.claude/worktrees/agenda/CLAUDE.md (commit message conventions, review-loop discipline)
    - Output of `git status` (verify staged set)
  </read_first>
  <action>
    Stage and commit the two new files from Plan 14-01:

    ```
    git add docs/decisions/015-agenda-redefinition.md \
            .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md
    ```

    Then commit with this message (HEREDOC; voice rule applies — use delivered-fact only):

    ```
    git commit -m "$(cat <<'EOF'
    docs(14-01): author ADR-015 + capture VIS-01..07 evidence

    - docs/decisions/015-agenda-redefinition.md (VIS-09): records the AL-7
      framing decision — two pillars (Phase 13 verdict (b)) and a
      vision-only document separated from strategy/roadmap (user reframe
      2026-05-16). Status: Accepted. Three considered-and-rejected
      alternatives. Back-links to AL-7, VISION.md, and the EXPL-01 / EXPL-02
      exploration notes.

    - .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md:
      pre-audit transcripts for VIS-01..VIS-07 verified against
      docs/VISION.md (commit 864d64c). VIS-07 voice-rule hard gate
      returns zero matches (exit=1). Plan 14-02's 14-AUDIT.md lifts
      these transcripts verbatim.

    Drives: v0.3.3 VIS-01..VIS-07 (verification), VIS-09 (authoring).
    Plan 14-02 picks up VIS-08, DOC-01..DOC-04, DOC-05 N/A close, and the
    consolidated 14-AUDIT.md phase-close gate.
    EOF
    )"
    ```

    Do NOT push. The commit lives on the local branch until the milestone-close PR (per CLAUDE.md, the project does not auto-push from agent commits).

    Run `git status` after the commit to confirm clean working tree (modulo any untracked files from other worktrees — `.claude/agents/fact-checker.md`, `.claude/agents/technical-writer.md`, `.planning/phases/12-pillar-2-exploration/12-VERIFICATION.md` are pre-existing untracked files visible in the conversation-start `git status` and are NOT touched by this plan).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -qE '^docs\(14-01\): author ADR-015' &amp;&amp; git diff HEAD~1 --name-only | grep -qE '^docs/decisions/015-agenda-redefinition\.md$' &amp;&amp; git diff HEAD~1 --name-only | grep -qE '^\.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE\.md$' &amp;&amp; test -z "$(git status --porcelain docs/decisions/015-agenda-redefinition.md .planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md)"</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject begins `docs(14-01): author ADR-015`.
    - `git diff HEAD~1 --name-only` shows exactly the two new files added by Plan 14-01 (plus possibly the conversation-start untracked files if previously staged unrelated; verify only the two named files are in this commit's diff).
    - `git status --porcelain` reports both Plan-14-01 files clean (no unstaged changes, no untracked).
    - No push happened (no `git push` invocation).
  </acceptance_criteria>
  <done>ADR-015 + Plan-14-01 evidence file committed; ready for Plan 14-02 to lift VIS-01..VIS-07 transcripts into 14-AUDIT.md and propagate back-pointers to ADR-015.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Vision/ADR prose claims about unshipped behavior crossing into reader-trusted documentation. Untrusted at the author layer (aspirational drift); trusted at the reader layer (treated as commitment by external contributors and AI agents reading the repo). The voice rule is the boundary check. |
| Plan 14-01 → Plan 14-02 | The evidence file is the contract carried into Plan 14-02's audit. A FAIL silently recorded as PASS would let the phase-close gate emit GREEN against a broken VIS-07. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information disclosure | ADR-015 Context section | mitigate | Per 14-CONTEXT.md `<specifics>`: do NOT reintroduce the EACCES / recursive-shim / Claude Code self-update narrow problem list (user pushback during smart-discuss). Task 2 `<action>` block calls this out explicitly; Task 3 reviewer pass verifies. |
| T-14-01-02 | Spoofing | Voice rule (VIS-07) | mitigate | The grep gate is the spec; Task 1 runs the verbatim VIS-07 grep against the committed docs/VISION.md and captures `exit=1` evidence. Defensive practice: Task 3 also runs the gate against ADR-015 even though ADRs are not formally gated. |
| T-14-01-03 | Tampering | Evidence file integrity | mitigate | Task 1 requires verbatim stdout (no paraphrasing); Task 4 commits the evidence file alongside ADR-015 in the same commit so they share a hash and travel together. |
| T-14-01-04 | Repudiation | ADR-015 vs Phase 13 verdict | mitigate | ADR-015 Decision section cites `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` line 17 (the verdict line) explicitly so a future reader can verify the pillar-count lock without re-reading Phase 13. Task 3 fact-checker pass verifies the cite. |
| T-14-01-05 | Denial of service | Plan timing | accept | Doc-only plan; no runtime DoS surface. |
| T-14-01-06 | Elevation of privilege | Plan touches no executable surface | accept | Plan modifies only Markdown files under `docs/decisions/` and `.planning/phases/`. No CI / installer / catalog / runtime code is touched. |
</threat_model>

<verification>
Phase-level checks for Plan 14-01 (consolidated 14-AUDIT.md gate fires from Plan 14-02):
- VIS-01..VIS-07: PASS verdicts captured in `.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md`.
- VIS-09: `docs/decisions/015-agenda-redefinition.md` exists with the required spine + alternatives count + back-links.
- Git: HEAD commit on this branch is the Plan 14-01 commit; working tree clean for the two Plan-14-01 files.
- Voice rule: VIS-07 hard gate PASS recorded with verbatim grep + `exit=1`. No `AgentLinux + blocked-verb` lines in ADR-015 (manual scan; defensive practice).
</verification>

<success_criteria>
- VIS-01..VIS-07 verified PASS against committed docs/VISION.md (`864d64c` or its descendants in the Phase 14 window).
- VIS-09 authored: ADR-015 committed at `docs/decisions/015-agenda-redefinition.md` with Status Accepted (2026-05-16), three considered-and-rejected alternatives, back-links to AL-7 + VISION.md + exploration notes, and a Consequences list naming Phase 15 insertion + Phase 15 → Phase 16 renumber + DOC-05 N/A close + ADR-012 NOPASSWD tension recording inside Pillar 2.
- `.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md` captures verbatim transcripts for VIS-01..VIS-07 ready to be lifted into Plan 14-02's `14-AUDIT.md`.
- Reviewer pass on ADR-015 returns clean (zero CRITICAL); MEDIUM/LOW triaged.
- Single commit lands both new files with subject `docs(14-01): author ADR-015 + capture VIS-01..07 evidence`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vision-doc-and-downstream/14-01-SUMMARY.md` capturing:
- The Plan 14-01 commit hash.
- VIS-01..VIS-07 verdicts (lifted from `14-01-EVIDENCE.md`).
- The reviewer-pass outcome on ADR-015 (technical-writer + fact-checker — CRITICAL count + a one-line note on what MEDIUM/LOW were applied vs deferred).
- A pointer to Plan 14-02 for the rest of Phase 14 (VIS-08, DOC-01..DOC-04, DOC-05 N/A, 14-AUDIT.md).
</output>
