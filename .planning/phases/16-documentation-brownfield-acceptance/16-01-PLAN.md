---
phase: 16-documentation-brownfield-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/MIGRATION.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
tags: [docs, readme, migration, brownfield, ux-walkthrough, dry-run, four-states]

must_haves:
  truths:
    - "README.md gains a second-level `## Brownfield install (existing user / Node.js / agents)` section placed AFTER the existing `## Install` section and BEFORE the `## Uninstall` / `## Stability model` sections (D-16-01 — mid-README placement so brownfield users find their guidance on first scroll)"
    - "README.md `## Install` section gains a one-liner pointer link with the literal text 'If you already have an `agent` user, Node.js, or any of these agents installed, see [Brownfield install](#brownfield-install).' immediately AFTER the canonical curl-install code blocks and BEFORE the SHA256-verify paragraph (D-16-06 — link wiring; link uses GitHub-rendered anchor that matches the H2 slug)"
    - "README.md brownfield section enumerates the four detection states (Reuse / Create / Remediate / Bail) in a tight definition block + shows a worked `agentlinux install --dry-run` console transcript on a host that already has Claude Code installed under root + shows the corresponding `agentlinux install --yes` console transcript + closes with a link to `docs/MIGRATION.md` for per-scenario walkthroughs (DOC-01)"
    - "README.md brownfield section documents the three documented exit codes (0 success, 64 EX_USAGE, 65 EX_DATAERR) inline as a one-paragraph blurb — Phase 14 UX-05 contract surfaced where users will read it"
    - "README.md brownfield section transcripts are illustrative — annotated with `(transcript trimmed for clarity)` markers so users do not paste version strings verbatim if upstream releases bump pinned versions (T-16-01-04 mitigation: scenarios pin to BEHAVIOR not version strings)"
    - "docs/MIGRATION.md is a new file with a top-level `# Migrating to AgentLinux on a brownfield host` heading + an opening paragraph explaining the four-state taxonomy + a short ToC linking to the four scenario sub-sections (DOC-02)"
    - "docs/MIGRATION.md contains EXACTLY four H2 scenario sections in difficulty order — `## Scenario B — NodeSource Node.js is already correct (Reuse happy path)`, `## Scenario A — Manual useradd `agent` (Reuse-with-warning if sudoers drifted)`, `## Scenario C — Claude Code installed under root (Remediate-04 reinstall under agent)`, `## Scenario D — Playwright with a broken chromium cache (Remediate-04 reinstall + cache rebuild)` (D-16-07 — all four are mandatory; D-16-02 — full transcripts per scenario)"
    - "Each `docs/MIGRATION.md` scenario section has FIVE labelled sub-blocks per locked D-16-02 schema: **Setup** (one-liner concrete shell commands the user would have run to get into this state — `apt list --installed | grep nodejs`, `id agent`, etc.), **Pre-flight report** (copy-pasteable `agentlinux install --dry-run` output block, ~10-20 lines), **Decision tree** (1-2 sentence narrative pointing at the right flag), **Non-interactive command** (exact `agentlinux install --yes ...` invocation, single code block), **Resulting host state** (bullet list: what was created, reused, or remediated; what was preserved)"
    - "Each scenario section is ~150-250 words (D-16-02 — full transcripts, not summary tables; concrete commands the user can paste, no hand-waving)"
    - "README.md brownfield section transcripts and `docs/MIGRATION.md` Scenario C transcript reference the same canonical worked example (root-installed Claude Code → REMEDIATE-04 reinstall under agent) — single source of truth; README is the executive summary, MIGRATION.md is the full walkthrough (D-16-06 implication: scannable README, deep MIGRATION.md)"
    - "README.md brownfield section links to `docs/MIGRATION.md` by relative path (not anchor) so link integrity does not depend on heading-slug stability — T-16-01-03 mitigation against anchor rot"
    - "docs/MIGRATION.md links BACK to `README.md#brownfield-install` once in the opening paragraph so the docs cross-reference is bidirectional"
    - "Greenfield invariant: README.md `## Install` section's primary `curl | sudo bash` flow remains the FIRST install instruction on the page (untouched verbatim except for the new one-liner pointer); a user who wants greenfield gets greenfield on first scroll (D-16-06 mid-README placement does not bury the happy path)"
    - "No new bats @tests in this plan (pure docs); Plan 16-02 owns the brownfield-AGT-02 gate that exercises the contract DOC-01 and DOC-02 describe"
  artifacts:
    - path: "README.md"
      provides: "New `## Brownfield install` H2 section after `## Install` and before `## Uninstall`; one-liner pointer link inserted in `## Install`; four-state taxonomy + dry-run transcript + --yes transcript + exit-codes blurb + link to MIGRATION.md"
      contains: "Brownfield install"
      min_lines: 195
    - path: "docs/MIGRATION.md"
      provides: "New focused brownfield migration guide. Top-level heading + opening paragraph + ToC + four H2 scenario sections (B → A → C → D in difficulty order; spec letters preserved in anchors) each with five labelled sub-blocks (Setup, Pre-flight report, Decision tree, Non-interactive command, Resulting host state)"
      contains: "Scenario C — Claude Code installed under root"
      min_lines: 250
  key_links:
    - from: "README.md `## Install` section"
      to: "README.md `## Brownfield install` section (relative anchor `#brownfield-install`)"
      via: "One-liner pointer text 'If you already have an `agent` user, Node.js, or any of these agents installed, see [Brownfield install](#brownfield-install).'"
      pattern: "see \\[Brownfield install\\]\\(#brownfield-install\\)"
    - from: "README.md `## Brownfield install` section"
      to: "docs/MIGRATION.md"
      via: "Relative-path link `[per-scenario walkthroughs](docs/MIGRATION.md)`"
      pattern: "\\[per-scenario walkthroughs\\]\\(docs/MIGRATION\\.md\\)"
    - from: "docs/MIGRATION.md opening paragraph"
      to: "README.md#brownfield-install"
      via: "Cross-reference link `[the README's Brownfield install section](../README.md#brownfield-install)`"
      pattern: "README\\.md#brownfield-install"
    - from: "docs/MIGRATION.md Scenario C section"
      to: "REMEDIATE-04 contract (Phase 14)"
      via: "Decision-tree narrative cites the REMEDIATE-04 PATH-MISMATCH branch + `--yes` flag for non-interactive remediation"
      pattern: "REMEDIATE-04"
---

<objective>
Land DOC-01 (README "Brownfield install" section) and DOC-02 (`docs/MIGRATION.md` with four worked scenarios) per locked D-16-01..D-16-07 + D-16-09. Both deliverables are pure documentation — no code changes, no new bats tests. Plan 16-02 owns the brownfield-AGT-02 acceptance gate that proves the contract these docs describe.

**Why this plan ships first (Wave 1):** the milestone-close audit Plan 16-02 writes cites DOC-01 + DOC-02 as the evidence artifacts for Phase 16's two requirements. The docs must exist before the audit references them.

**Single source of truth for the worked example:** README.md gets a tight executive-summary transcript of Scenario C (Claude Code under root → REMEDIATE-04 reinstall under agent). `docs/MIGRATION.md` Scenario C is the deep walkthrough of the same example. Pick Scenario C for the README because it exercises REMEDIATE-04 (the most novel v0.3.4 feature) — operators reading the README understand the value proposition immediately.

**Scenario ordering in MIGRATION.md** is thematic (simplest → hardest) but **spec letters are preserved in section anchors** so a user grepping for "scenario c" finds Scenario C regardless of file order:
  1. **Scenario B** — NodeSource Node.js already correct (REUSE-02 happy path)
  2. **Scenario A** — Manual useradd agent (REUSE-01; may trigger REMEDIATE-03 if sudoers drifted)
  3. **Scenario C** — Claude Code under root (REMEDIATE-04 PATH-MISMATCH happy path)
  4. **Scenario D** — Playwright with broken chromium cache (REMEDIATE-04 broken-status path)

**T-16-01-04 mitigation (scenario drift):** every transcript carries an inline `(illustrative — version strings may differ from your release)` note. The contract documented is the BEHAVIOR (`reuse`, `remediate`, exit codes), not the version strings.

Purpose: A user landing on README.md with a partially-populated host (manual `agent` user, existing Node.js, root-installed Claude Code, broken Playwright) discovers AgentLinux's brownfield contract on first scroll and follows a worked transcript to the right outcome. A user in incident-recovery mode (their unattended install hit a Bail) opens MIGRATION.md and finds their exact scenario with a pasteable flag set.

Output: Two new/extended documentation artifacts (README.md +1 section + 1 pointer line; docs/MIGRATION.md new file). No code changes. No test deltas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-brownfield-acceptance/16-CONTEXT.md
@.planning/phases/15-preflight-ux/15-AUDIT.md
@.planning/phases/14-remediate-consent-flag-exit-codes/14-AUDIT.md
@README.md
@docs/HARNESS.md
@.claude/skills/review/SKILL.md

<interfaces>
<!-- Contracts referenced by the docs (no code interfaces created here; doc IS the interface). -->

From plugin/bin/agentlinux-install (Phase 12-15 contract — surfaced in docs):
```
agentlinux install [--user=NAME] [--dry-run] [--yes] [--report-format=text|json]

Exit codes:
  0   — success
  64  — EX_USAGE (bad flags, contradictory options)
  65  — EX_DATAERR (incompatible host state surfaced by detection)
  1   — runtime failure during Create / Remediate path

Four detection states (per component):
  Reuse      — component matches AgentLinux's contract; provisioner short-circuits
  Create     — component absent; provisioner runs as in greenfield
  Remediate  — component present with a fixable defect; runs with --yes (non-TTY) or per-action TTY prompt
  Bail       — component present with irreconcilable defect; exit 65 with remediation hint
```

From `agentlinux install --dry-run` (canonical Scenario C report shape — derived from Phase 12-15 implementation):
```console
$ agentlinux install --dry-run
[DET-01] user=agent uid=1001 shell=/bin/bash home=/home/agent writable=true
[DET-02] nodejs=v22.x source=nodesource prefix=/usr/lib/node_modules user_writable=false
[DET-04] claude-code status=broken path=/usr/local/bin/claude owner=root (PATH-MISMATCH: canonical is ~/.local/bin/claude)
[DET-04] gsd status=absent
[DET-04] playwright-cli status=absent
[DET-05] sudoers=/etc/sudoers.d/agentlinux present mode=0440 sha256=ok

pre-flight resolution:
  user        agent          Reuse      (existing user matches contract)
  nodejs      v22.x          Reuse      (NodeSource, correct major)
  npm-prefix  /usr/lib/...   Remediate  (REMEDIATE-01 — root-owned; --yes rebases to ~agent/.npm-global)
  sudoers     ok             Reuse
  claude-code broken         Remediate  (REMEDIATE-04 — reinstall under agent)

exit code: 0 (dry-run — no state changed)
```

From `tests/bats/14-remediate.bats` + `tests/bats/helpers/brownfield.bash::setup_brownfield_broken_claude_code` (Plan 14-03 — the canonical PATH-MISMATCH fixture the README + Scenario C transcripts mirror).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README link target → file/anchor existence | README anchor links can rot if heading slugs change; relative-path links to MIGRATION.md are more stable than anchor links |
| Transcript version strings → upstream package bumps | Scenarios that hardcode versions (claude-code@2.1.98) go stale as upstream releases; mitigation: behavior is the contract, versions are illustrative |
| MIGRATION.md scenario flag sets → CLI contract drift | If the `--yes` flag surface evolves (Phase 17+), scenarios become stale; mitigation: link scenarios to REQ-IDs not implementations |
| Scenario fixture commands → actual host state | Operator-pasted setup commands could leave the host in a different shape than the scenario; mitigation: scenarios document EXACTLY ONE fixture per section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-03 | I (Information Disclosure) | README link target rot — anchor `#brownfield-install` could drift if H2 heading slug changes | mitigate | README → MIGRATION.md uses **relative-path link** (`docs/MIGRATION.md`) not anchor; the `#brownfield-install` anchor is internal to README and auto-generated from the H2 slug — Markdown renderers (GitHub, GitLab, npm-render) all use the same slugify algorithm. Bats verify-step in Plan 16-02 runs a literal `grep -F "[Brownfield install](#brownfield-install)"` to catch link rot at commit time. |
| T-16-01-04 | I (Information Disclosure) | MIGRATION.md scenario commands becoming stale as catalog evolves (claude-code version bumps, gsd renames) | mitigate | Every transcript carries inline `(illustrative — version strings may differ)` note; scenarios cite REQ-IDs (REUSE-01, REMEDIATE-04) and FLAG NAMES (`--yes`, `--dry-run`), not version literals; the BEHAVIOR is the contract. Bats verify-step in Plan 16-02 spot-checks the presence of the illustrative note in each scenario. |
| T-16-01-07 | D (Denial of Service) | Operator copy-pastes a scenario's setup commands and damages their host (e.g., `rm -rf /home/agent`) | accept | Scenarios use ONLY non-destructive setup commands (`apt list --installed`, `id agent`, fixture references). Destructive commands (`userdel -r`, `rm -rf`) appear ONLY in inline scenario context with `# UNDO this state — destructive` warnings. Operator-side audit is the operator's responsibility. |
| T-16-01-08 | T (Tampering) | A README PR could remove the brownfield section anchor without updating the `## Install` pointer | mitigate | Plan 16-02 audit step adds a literal `grep -F "[Brownfield install](#brownfield-install)" README.md` + `grep -F "## Brownfield install" README.md` pair to its acceptance criteria; both must succeed for milestone close. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: README.md — add `## Brownfield install` section + pointer link from `## Install`</name>
  <read_first>
    - README.md (current 156-line greenfield-focused landing page; the `## Install` section is lines 15-39, the `## Verify` / `## Uninstall` / `## Stability model` sections follow — Brownfield section inserts BETWEEN `## Install` and `## Uninstall`)
    - .planning/phases/16-documentation-brownfield-acceptance/16-CONTEXT.md (D-16-01 placement, D-16-02 transcript shape, D-16-06 link wiring)
    - .planning/phases/15-preflight-ux/15-AUDIT.md (UX-01..04 evidence — provides the canonical dry-run/TTY/non-TTY behavior the README documents)
    - .planning/phases/14-remediate-consent-flag-exit-codes/14-AUDIT.md (REMEDIATE-04 + UX-03 + UX-05 — exit codes blurb sources here)
    - tests/bats/helpers/brownfield.bash::setup_brownfield_broken_claude_code (the canonical Scenario C fixture; README transcript should reflect this shape)
  </read_first>
  <action>
    1. **Open `README.md`.** It is 156 lines today and ends with the `## About` section. The greenfield install lives at `## Install` (line 15). The brownfield section inserts as a NEW H2 between `## Install` (lines 15-39) and `## Uninstall` (lines 54-65). The `## Verify` section (lines 41-52) stays where it is — it serves both greenfield and brownfield users.

    2. **Modify the `## Install` section** — INSERT a one-liner pointer immediately after the THIRD curl-install code block (the `AGENTLINUX_VERSION=...` example block, currently lines 32-34) and BEFORE the SHA256-verify paragraph (currently lines 36-39). The inserted line is exactly:

       ```markdown
       If you already have an `agent` user, Node.js, or any of these agents installed, see [Brownfield install](#brownfield-install).
       ```

       That is the literal text — the link uses GitHub's auto-slug (`brownfield-install`) which matches what the H2 will produce. Plan 16-02's audit step will `grep -F "[Brownfield install](#brownfield-install)" README.md` to verify.

    3. **Add the new section** — INSERT this block between `## Install` (after the SHA256-verify paragraph) and `## Verify` (existing H2). Place the new section header BEFORE `## Verify` so the flow reads: Install → Brownfield install → Verify → Uninstall → Stability model:

       ```markdown
       ## Brownfield install (existing user / Node.js / agents)

       If your host already has an `agent` user, a Node.js install, or any of the
       catalog agents (claude-code, gsd, playwright), AgentLinux detects them up
       front and decides on a per-component basis. There are four possible
       states:

       - **Reuse** — the existing component matches AgentLinux's contract; the
         provisioner or recipe short-circuits without writing.
       - **Create** — the component is absent; the greenfield path runs as in
         a fresh-host install.
       - **Remediate** — the component is present but has a fixable defect
         (wrong ownership on the npm prefix, drifted sudoers, broken catalog
         agent). The installer either prompts you per action in TTY mode or
         requires `--yes` in non-interactive mode (`apt install -y`-style).
       - **Bail** — the component has an irreconcilable defect (e.g. the
         existing `agent` user has the wrong login shell). The installer
         exits with code `65` and a remediation hint.

       **Preview first — zero changes:**

       ```console
       $ agentlinux install --dry-run
       [DET-01] user=agent uid=1001 shell=/bin/bash home=/home/agent writable=true
       [DET-02] nodejs=v22.x source=nodesource user_writable=false
       [DET-04] claude-code status=broken path=/usr/local/bin/claude owner=root
       [DET-04] gsd status=absent
       [DET-04] playwright-cli status=absent
       [DET-05] sudoers=present sha256=ok

       pre-flight resolution:
         user        agent       Reuse      (existing user matches contract)
         nodejs      v22.x       Reuse      (NodeSource, correct major)
         npm-prefix  /usr/lib/   Remediate  (REMEDIATE-01 — root-owned; rebases to ~agent/.npm-global)
         sudoers     ok          Reuse
         claude-code broken      Remediate  (REMEDIATE-04 — reinstall under agent)

       exit code: 0 (dry-run — no state changed)
       ```
       *(illustrative — version strings and component layout may differ on your host)*

       **Apply in non-interactive mode** (`--yes` opts in to every required
       remediation in one shot — there are no per-action flags):

       ```console
       $ agentlinux install --yes
       [REMEDIATE-01] rebasing npm-global to /home/agent/.npm-global
       [REMEDIATE-04] reinstalling claude-code under agent (preserving ~/.claude/)
       [INSTALL] complete
       ```

       **In a terminal** (`stdin` is a TTY), the installer asks `Proceed with
       this remediation? [Y/n]` per state-overwriting action; declining one
       skips it (the component stays as-is, flagged
       `reused — declined remediation` in `agentlinux list`) and continues
       with the others.

       **Exit codes:**

       - `0` — success
       - `64` (`EX_USAGE`) — bad command-line flags or contradictory options
         (e.g. `--dry-run --yes`)
       - `65` (`EX_DATAERR`) — incompatible host state surfaced by detection
         that `--yes` cannot resolve (e.g. existing `agent` user has shell
         `/bin/sh`); the bail message names the conflicting attribute and
         suggests `--user=NAME` or manual remediation
       - `1` — runtime failure during the Create / Remediate path

       For per-scenario walkthroughs (manual useradd, NodeSource already
       correct, root-installed Claude Code, broken Playwright), see
       [per-scenario walkthroughs](docs/MIGRATION.md).
       ```

       NOTES on the embedded code blocks above:
       - The inner triple-backticks within the outer literal block need to be
         written as literal triple-backticks in the README (no escape needed —
         markdown nesting works because the OUTER block in this PLAN is
         indented, but the README is just markdown). When editing README.md
         directly, write the section as plain markdown (no escapes).
       - The trailing `*(illustrative — version strings ...)*` italic line is
         intentional — T-16-01-04 mitigation: the scenarios document BEHAVIOR
         and FLAG NAMES, not version literals.
       - The bullet on REMEDIATE actions explicitly states `--yes` is the
         consent surface in non-TTY mode (UX-03 contract) and TTY uses
         per-action prompts (UX-02 contract).

    4. **Save README.md.** Run `grep -F "## Brownfield install" README.md` and `grep -F "[Brownfield install](#brownfield-install)" README.md` to verify both insertions landed; both must return at least one match.

    5. **Verify the `## Install` section's primary `curl | sudo bash` line is UNCHANGED** — `grep -F "curl -fsSL https://agentlinux.org/install.sh | sudo bash" README.md` must still return exactly one match (the original greenfield install). The brownfield section is ADDITIVE — it does not modify any greenfield instruction.

    6. **Verify section ordering is `Install` → `Brownfield install` → `Verify` → `Uninstall` → `Stability model`** by running `grep -n "^## " README.md` and confirming the line-number order of the headings.

    7. **Run `pre-commit run --files README.md` (if pre-commit hooks include markdown linting; if not, skip)** to catch any obvious linting fails before commit.

    8. **Commit atomic**: `gsd-sdk query commit "docs(README): add Brownfield install section + pointer link (DOC-01)" README.md`
  </action>
  <verify>
    <automated>grep -cF "## Brownfield install (existing user / Node.js / agents)" README.md</automated>
    <automated>grep -cF "[Brownfield install](#brownfield-install)" README.md</automated>
    <automated>grep -cF "REMEDIATE-04 — reinstall under agent" README.md</automated>
    <automated>grep -cF "(illustrative — version strings" README.md</automated>
    <automated>grep -cF "[per-scenario walkthroughs](docs/MIGRATION.md)" README.md</automated>
    <automated>grep -cF "curl -fsSL https://agentlinux.org/install.sh | sudo bash" README.md</automated>
    <automated>grep -cF "EX_USAGE" README.md</automated>
    <automated>grep -cF "EX_DATAERR" README.md</automated>
    <automated>awk '/^## /' README.md | head -5</automated>
    All seven grep counts return >= 1. The awk output prints the FIRST 5 H2 headings in file order, which MUST be: `## Install`, `## Brownfield install (existing user / Node.js / agents)`, `## Verify`, `## Uninstall`, `## Stability model`.
  </verify>
  <done>
    - README.md `## Install` section gains a one-liner pointer link AFTER the existing curl-install code blocks (line not removed; SHA256 paragraph preserved).
    - README.md gains a new `## Brownfield install (existing user / Node.js / agents)` H2 section between `## Install` and `## Verify`.
    - Section enumerates the four states (Reuse / Create / Remediate / Bail) with one-sentence definitions.
    - Section shows a worked `agentlinux install --dry-run` console transcript (Scenario C — Claude Code under root).
    - Section shows a worked `agentlinux install --yes` console transcript with `[REMEDIATE-01]` + `[REMEDIATE-04]` markers.
    - Section documents the four exit codes (0/64/65/1) with mnemonics.
    - Section ends with link to `docs/MIGRATION.md` for per-scenario walkthroughs (relative path, not anchor).
    - Transcripts carry the literal `(illustrative — version strings may differ)` note for T-16-01-04 mitigation.
    - Greenfield `curl | sudo bash` flow remains the FIRST install instruction (untouched).
    - One atomic commit on README.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/MIGRATION.md — create new file with four worked scenarios (B → A → C → D)</name>
  <read_first>
    - docs/HARNESS.md (existing structured-walkthrough doc style; MIGRATION.md mirrors the section-with-sub-blocks shape but is operator-facing not maintainer-facing)
    - README.md (the new `## Brownfield install` section from Task 1 — MIGRATION.md cross-references it; Scenario C transcript is the EXPANDED version of the README's executive summary)
    - .planning/phases/16-documentation-brownfield-acceptance/16-CONTEXT.md (D-16-02 — five labelled sub-blocks per scenario; D-16-07 — all four scenarios mandatory in spec letters)
    - .planning/phases/13-reuse-wiring/13-AUDIT.md (or 13-02-SUMMARY.md) — REUSE-01 / REUSE-02 contract details for Scenarios A + B
    - .planning/phases/14-remediate-consent-flag-exit-codes/14-AUDIT.md — REMEDIATE-03 (sudoers drift), REMEDIATE-04 (broken catalog agent) contracts for Scenarios A + C + D
    - tests/bats/helpers/brownfield.bash (`setup_brownfield_host`, `setup_brownfield_broken_claude_code`, `_brownfield_baseline` — the exact pre-existing-host shapes the MIGRATION.md scenarios mirror; these helpers are the source of truth for "what does Setup look like")
  </read_first>
  <action>
    1. **Create `docs/MIGRATION.md`** with the following exact structure (write the literal markdown as shown — the outer fenced block here is just for visibility, drop the OUTER fences when writing the file):

       ```markdown
       # Migrating to AgentLinux on a brownfield host

       AgentLinux's `agentlinux install` detects every component it owns
       (install user, Node.js, npm global prefix, sudoers drop-in, catalog
       agents) before mutating anything and decides per component: **Reuse**,
       **Create**, **Remediate**, or **Bail**. The four states are exhaustive
       and exclusive; the design philosophy is documented in
       [the README's Brownfield install section](../README.md#brownfield-install).

       This document walks four representative pre-existing-host scenarios
       end to end. Each scenario shows the host state you start from, the
       pre-flight report `agentlinux install --dry-run` produces, the
       decision tree, the exact non-interactive command, and the resulting
       host state. Scenarios are ordered by difficulty (simplest to hardest);
       the spec-locked letters (a-d per
       [REQUIREMENTS.md DOC-02](../.planning/REQUIREMENTS.md)) are preserved
       in section anchors.

       > **Note** — every transcript below is illustrative. Pinned version
       > strings (`claude-code@2.1.98`, `node v22.x`) may differ from your
       > release; what is contractual is the **detection state**
       > (`Reuse` / `Create` / `Remediate` / `Bail`), the **flag surface**
       > (`--dry-run`, `--yes`, `--user=NAME`), and the **exit codes**
       > (`0` / `64` / `65` / `1`).

       ## Table of contents

       1. [Scenario B — NodeSource Node.js already correct](#scenario-b--nodesource-nodejs-already-correct-reuse-happy-path) — REUSE-02 happy path
       2. [Scenario A — Manual useradd `agent`](#scenario-a--manual-useradd-agent-reuse-with-warning-if-sudoers-drifted) — REUSE-01 happy path (with REMEDIATE-03 if sudoers drifted)
       3. [Scenario C — Claude Code installed under root](#scenario-c--claude-code-installed-under-root-remediate-04-reinstall-under-agent) — REMEDIATE-04 PATH-MISMATCH
       4. [Scenario D — Playwright with a broken chromium cache](#scenario-d--playwright-with-a-broken-chromium-cache-remediate-04-reinstall--cache-rebuild) — REMEDIATE-04 broken-status

       ---

       ## Scenario B — NodeSource Node.js already correct (Reuse happy path)

       **Setup.** Your host already has Node.js 22.x installed via NodeSource
       (the same source AgentLinux would use), and the global npm prefix is
       writable by whichever user will install agents:

       ```console
       $ apt list --installed 2>/dev/null | grep -E '^nodejs/'
       nodejs/jammy,now 22.11.0-1nodesource1 amd64 [installed]
       $ id agent
       uid=1001(agent) gid=1001(agent) groups=1001(agent)
       $ stat -c '%U:%G %a' "$(sudo -u agent -H npm config get prefix)/lib"
       agent:agent 0755
       ```

       **Pre-flight report:**

       ```console
       # agentlinux install --dry-run
       [DET-01] user=agent shell=/bin/bash writable=true
       [DET-02] nodejs=v22.11.0 source=nodesource user_writable=true
       [DET-05] sudoers=present sha256=ok

       pre-flight resolution:
         user      agent       Reuse  (existing user matches contract)
         nodejs    v22.11.0    Reuse  (REUSE-02 — NodeSource, correct major, writable prefix)
         sudoers   ok          Reuse
         catalog   absent      Create

       exit code: 0 (dry-run — no state changed)
       ```

       **Decision tree.** Every detected component is `Reuse`; only catalog
       agents need to be installed (`Create`). No remediation flags needed.

       **Non-interactive command:**

       ```bash
       agentlinux install
       ```

       **Resulting host state.**

       - Pre-existing NodeSource Node.js: preserved, untouched.
       - Pre-existing `agent` user: preserved, untouched.
       - npm global prefix: preserved at its existing path (writable by agent).
       - `/etc/sudoers.d/agentlinux` drop-in: preserved if present, installed
         if absent (additive — no `--yes` needed).
       - Catalog agents: installed fresh under the existing agent ownership.

       ---

       ## Scenario A — Manual useradd `agent` (Reuse-with-warning if sudoers drifted)

       **Setup.** You manually created an `agent` user months ago for an
       unrelated tool; the user has the right shell (`/bin/bash`) and a
       writable home, but no AgentLinux sudoers drop-in (or one with a
       narrower NOPASSWD scope than ADR-012's `agent ALL=(ALL) NOPASSWD: ALL`):

       ```console
       # useradd -m -s /bin/bash agent      # earlier manual setup
       # echo 'agent ALL=(ALL) NOPASSWD: /usr/bin/apt-get' > /etc/sudoers.d/local-agent-apt
       $ id agent && [ -f /etc/sudoers.d/agentlinux ] || echo "no agentlinux sudoers"
       uid=1001(agent) ...
       no agentlinux sudoers
       ```

       **Pre-flight report:**

       ```console
       # agentlinux install --dry-run
       [DET-01] user=agent shell=/bin/bash writable=true
       [DET-05] sudoers=absent (canonical /etc/sudoers.d/agentlinux not present)

       pre-flight resolution:
         user      agent     Reuse      (REUSE-01 — existing user matches contract)
         nodejs    absent    Create
         sudoers   absent    Remediate  (REMEDIATE-03 — additive install, no --yes needed)
         catalog   absent    Create

       exit code: 0 (dry-run — no state changed)
       ```

       **Decision tree.** The agent user is `Reuse`. The canonical
       AgentLinux sudoers drop-in is absent — REMEDIATE-03's additive branch
       (missing-file install) does NOT require `--yes` because it never
       overwrites pre-existing state. If your host had a DIFFERENT canonical
       sudoers file with drifted content, REMEDIATE-03 would be a drift
       overwrite and require `--yes`.

       **Non-interactive command:**

       ```bash
       agentlinux install                 # additive REMEDIATE-03 only
       # — or, if your sudoers drifted (not missing-but-different):
       agentlinux install --yes
       ```

       **Resulting host state.**

       - Pre-existing manual `agent` user: preserved, untouched.
       - `/etc/sudoers.d/agentlinux` drop-in: installed canonically
         (mode `0440`, `agent ALL=(ALL) NOPASSWD: ALL` per ADR-012).
       - Pre-existing `local-agent-apt` (or any other sudoers file):
         preserved alongside — AgentLinux owns only its own drop-in.
       - Node.js + catalog agents: installed fresh.

       ---

       ## Scenario C — Claude Code installed under root (REMEDIATE-04 reinstall under agent)

       **Setup.** Your host already has Claude Code installed at
       `/usr/local/bin/claude` (root-owned) — installed via
       `sudo npm install -g @anthropic-ai/claude-code` or the standalone
       installer running as root. This is the canonical bug class AgentLinux
       exists to fix: a root-owned `claude` shim shadows the agent-owned
       canonical path, so `claude update` hits `EACCES`:

       ```console
       $ which claude
       /usr/local/bin/claude
       $ stat -c '%U:%G' /usr/local/bin/claude
       root:root
       $ sudo -u agent -H claude update
       npm error code EACCES
       npm error syscall mkdir
       npm error path /usr/local/lib/node_modules
       ```

       **Pre-flight report:**

       ```console
       # agentlinux install --dry-run
       [DET-04] claude-code status=broken path=/usr/local/bin/claude owner=root
                 (PATH-MISMATCH — canonical is ~agent/.local/bin/claude)

       pre-flight resolution:
         user        agent       Reuse
         nodejs      v22.x       Reuse
         sudoers     ok          Reuse
         claude-code broken      Remediate  (REMEDIATE-04 — reinstall under agent, preserve ~/.claude/)

       exit code: 0 (dry-run — no state changed)
       ```

       **Decision tree.** REMEDIATE-04 is a state-overwriting remediation
       (it runs the catalog `uninstall.sh` then `install.sh`). In TTY mode
       you will be prompted `Proceed with this remediation? [Y/n]`. In
       non-interactive mode (cron, CI, `curl | sudo bash`) you must pass
       `--yes`; without it the installer bails with exit `65` and a hint
       message naming `claude-code` as the component that needs remediation.

       **Non-interactive command:**

       ```bash
       agentlinux install --yes
       ```

       **Resulting host state.**

       - `/usr/local/bin/claude` (root-owned): removed by the catalog
         `uninstall.sh`.
       - `~agent/.local/bin/claude` (agent-owned, canonical path): installed
         via the official Anthropic native installer at the catalog-pinned
         version.
       - `~agent/.claude/` user data: **preserved** across uninstall →
         install (catalog `preserve_paths.json` per Phase 14 CAT-04).
       - `claude update` from the agent user now succeeds with zero `EACCES`.

       ---

       ## Scenario D — Playwright with a broken chromium cache (REMEDIATE-04 reinstall + cache rebuild)

       **Setup.** Playwright is installed globally for the agent user
       (or a previous broken installer left a partial chromium cache),
       but the chromium binary download is corrupted or missing — `playwright
       --version` reports the package version but `playwright chromium ...`
       fails:

       ```console
       $ sudo -u agent -H playwright --version
       Version 1.59.1
       $ ls -la ~agent/.cache/ms-playwright/
       chromium-1217  (incomplete: missing chrome-linux/chrome binary)
       $ sudo -u agent -H playwright chromium --version
       browserType.launch: Executable doesn't exist at /home/agent/.cache/ms-playwright/chromium-1217/chrome-linux/chrome
       ```

       **Pre-flight report:**

       ```console
       # agentlinux install --dry-run
       [DET-04] playwright-cli status=broken path=/home/agent/.npm-global/bin/playwright-cli
                 (health probe failed — chromium cache incomplete)

       pre-flight resolution:
         user           agent       Reuse
         nodejs         v22.x       Reuse
         sudoers        ok          Reuse
         playwright-cli broken      Remediate  (REMEDIATE-04 — reinstall + rebuild chromium cache)

       exit code: 0 (dry-run — no state changed)
       ```

       **Decision tree.** REMEDIATE-04 again — same contract as Scenario C.
       The catalog `uninstall.sh` removes the broken binary and (per the
       Phase 14 CAT-04 amendment for playwright-cli) the broken chromium
       cache; `install.sh` runs `npm install -g playwright@<pin>` followed
       by `npx --yes playwright install --with-deps chromium` which
       re-downloads the chromium binary from Playwright's CDN. Needs
       `--yes` in non-interactive mode.

       **Non-interactive command:**

       ```bash
       agentlinux install --yes
       ```

       **Resulting host state.**

       - Broken `~agent/.cache/ms-playwright/chromium-*`: removed by
         catalog `uninstall.sh`.
       - Fresh `~agent/.cache/ms-playwright/chromium-<id>`: re-downloaded
         and verified by `npx --yes playwright install --with-deps chromium`.
       - `playwright-cli` binary at `~agent/.npm-global/bin/playwright-cli`:
         re-installed at the catalog-pinned version.
       - Other catalog agents: unchanged.

       ---

       *Last updated: 2026-05-26 — AgentLinux v0.3.4 close-out.*
       ```

       NOTE on the anchor slugs above: GitHub-flavored markdown auto-generates
       anchors from H2 text by lowercasing, replacing spaces with hyphens, and
       dropping punctuation. The four ToC anchor links are:
       - `#scenario-b--nodesource-nodejs-already-correct-reuse-happy-path`
       - `#scenario-a--manual-useradd-agent-reuse-with-warning-if-sudoers-drifted`
       - `#scenario-c--claude-code-installed-under-root-remediate-04-reinstall-under-agent`
       - `#scenario-d--playwright-with-a-broken-chromium-cache-remediate-04-reinstall--cache-rebuild`

       (GitHub's slugify drops dashes in `remediate-04` from `REMEDIATE-04`; the double `--` represents the original ` — ` em-dash that became two hyphens after punctuation stripping; the trailing slug ends correctly.)

    2. **Verify file creation.** `wc -l docs/MIGRATION.md` should report >= 250 lines (substantive scenarios, ~150-250 words each → ~250-350 lines total including code blocks).

    3. **Verify all four scenarios exist** by grepping for the H2 headings:
       - `grep -cF "## Scenario B — NodeSource" docs/MIGRATION.md` → 1
       - `grep -cF "## Scenario A — Manual useradd" docs/MIGRATION.md` → 1
       - `grep -cF "## Scenario C — Claude Code installed under root" docs/MIGRATION.md` → 1
       - `grep -cF "## Scenario D — Playwright with a broken chromium cache" docs/MIGRATION.md` → 1

    4. **Verify each scenario has all five labelled sub-blocks** by grepping for the section labels (each label appears exactly 4 times — once per scenario):
       - `grep -c "^\*\*Setup\.\*\*" docs/MIGRATION.md` → 4
       - `grep -c "^\*\*Pre-flight report:\*\*" docs/MIGRATION.md` → 4
       - `grep -c "^\*\*Decision tree\.\*\*" docs/MIGRATION.md` → 4
       - `grep -c "^\*\*Non-interactive command:\*\*" docs/MIGRATION.md` → 4
       - `grep -c "^\*\*Resulting host state\.\*\*" docs/MIGRATION.md` → 4

    5. **Verify cross-link back to README** — `grep -cF "../README.md#brownfield-install" docs/MIGRATION.md` returns >= 1.

    6. **Verify illustrative-version-strings note (T-16-01-04 mitigation)** — `grep -cF "illustrative" docs/MIGRATION.md` returns >= 1.

    7. **Run pre-commit** on the new file: `pre-commit run --files docs/MIGRATION.md` (skip silently if hook is not registered for markdown).

    8. **Commit atomic**: `gsd-sdk query commit "docs(migration): four worked brownfield scenarios (DOC-02)" docs/MIGRATION.md`
  </action>
  <verify>
    <automated>test -f docs/MIGRATION.md</automated>
    <automated>wc -l docs/MIGRATION.md | awk '{print ($1 >= 250) ? "OK" : "FAIL"}'</automated>
    <automated>grep -cF "# Migrating to AgentLinux on a brownfield host" docs/MIGRATION.md</automated>
    <automated>grep -cF "## Scenario B — NodeSource" docs/MIGRATION.md</automated>
    <automated>grep -cF "## Scenario A — Manual useradd" docs/MIGRATION.md</automated>
    <automated>grep -cF "## Scenario C — Claude Code installed under root" docs/MIGRATION.md</automated>
    <automated>grep -cF "## Scenario D — Playwright with a broken chromium cache" docs/MIGRATION.md</automated>
    <automated>grep -c "^\*\*Setup\.\*\*" docs/MIGRATION.md</automated>
    <automated>grep -c "^\*\*Pre-flight report:\*\*" docs/MIGRATION.md</automated>
    <automated>grep -c "^\*\*Decision tree\.\*\*" docs/MIGRATION.md</automated>
    <automated>grep -c "^\*\*Non-interactive command:\*\*" docs/MIGRATION.md</automated>
    <automated>grep -c "^\*\*Resulting host state\.\*\*" docs/MIGRATION.md</automated>
    <automated>grep -cF "REMEDIATE-04" docs/MIGRATION.md</automated>
    <automated>grep -cF "REUSE-01" docs/MIGRATION.md</automated>
    <automated>grep -cF "REUSE-02" docs/MIGRATION.md</automated>
    <automated>grep -cF "REMEDIATE-03" docs/MIGRATION.md</automated>
    <automated>grep -cF "../README.md#brownfield-install" docs/MIGRATION.md</automated>
    <automated>grep -cF "illustrative" docs/MIGRATION.md</automated>
    File exists; >= 250 lines; top-level heading present; all four H2 scenario sections present; each of the five sub-block labels appears 4 times; cross-link to README present; illustrative-version-strings note present; all four REQ-ID citations (REUSE-01, REUSE-02, REMEDIATE-03, REMEDIATE-04) appear at least once.
  </verify>
  <done>
    - `docs/MIGRATION.md` exists as a new file with top-level heading + opening paragraph (cross-references README's Brownfield install section) + ToC + four H2 scenario sections in difficulty order.
    - Each scenario has all five labelled sub-blocks (Setup, Pre-flight report, Decision tree, Non-interactive command, Resulting host state).
    - Scenarios reference REQ-IDs (REUSE-01, REUSE-02, REMEDIATE-03, REMEDIATE-04) and flag names (`--yes`, `--dry-run`, `--user=NAME`), not version literals (T-16-01-04 mitigation).
    - Opening paragraph contains an illustrative-version-strings note.
    - Bidirectional cross-link: README → MIGRATION.md (relative path); MIGRATION.md → README (anchor).
    - One atomic commit on docs/MIGRATION.md.
  </done>
</task>

</tasks>

<verification>
Plan 16-01 verification:

1. README.md has `## Brownfield install (existing user / Node.js / agents)` H2 between `## Install` and `## Verify`.
2. README.md `## Install` section has the one-liner pointer link `[Brownfield install](#brownfield-install)`.
3. README.md brownfield section contains: four-state taxonomy, dry-run transcript, --yes transcript, exit-codes blurb, link to docs/MIGRATION.md.
4. Greenfield `curl -fsSL ... | sudo bash` install command unchanged.
5. docs/MIGRATION.md exists with top-level heading, opening paragraph with README cross-link, ToC, four H2 scenario sections (B, A, C, D in difficulty order with spec letters preserved).
6. Each scenario has all five labelled sub-blocks (Setup, Pre-flight report, Decision tree, Non-interactive command, Resulting host state).
7. Every transcript carries the illustrative-version-strings note (T-16-01-04).
8. README → MIGRATION.md uses relative-path link, not anchor (T-16-01-03).
9. No code changes; no bats test deltas; no plugin/ or tests/ files touched.
10. Two atomic commits (one per task), both on documentation files only.
</verification>

<success_criteria>
- DOC-01 deliverable: README.md `## Brownfield install` section exists and is linked from `## Install`.
- DOC-02 deliverable: `docs/MIGRATION.md` exists with four worked scenarios in difficulty order.
- All scenarios cite REQ-IDs and flag names, not version literals.
- Both deliverables ready to be cited as evidence artifacts in Plan 16-02's `16-AUDIT.md`.
- No regressions: greenfield install instruction preserved verbatim; no code files touched; no test deltas.
- Review-loop: dispatch `technical-writer` + `fact-checker` reviewers on both files per CLAUDE.md §Review Loop before declaring done.
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-brownfield-acceptance/16-01-SUMMARY.md` documenting:

- README.md insertion point (between `## Install` and `## Verify`) + the pointer-link insertion in `## Install`.
- docs/MIGRATION.md skeleton (top-level heading, opening paragraph, ToC, four scenarios B → A → C → D).
- Each scenario's five labelled sub-blocks + word counts (~150-250 each per D-16-02).
- T-16-01-03, T-16-01-04, T-16-01-07, T-16-01-08 mitigation evidence.
- Review-loop findings + dispositions (technical-writer + fact-checker).
- File modifications (README.md +~70 lines; docs/MIGRATION.md new ~280 lines).
- Two atomic commits + verify chain.
</output>
