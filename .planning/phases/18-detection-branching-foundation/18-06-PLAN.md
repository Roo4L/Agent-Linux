---
phase: 18-detection-branching-foundation
plan: 06
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - docs/decisions/017-distro-family-bucket.md
autonomous: true
requirements: [EL-01, EL-02]
must_haves:
  truths:
    - "ADR-017 records the AGENTLINUX_DISTRO_FAMILY ∈ {debian, rhel} bucket + single pkg.sh-branch decision"
    - "It documents the rejected alternatives (inline per-site `case`; AppStream `dnf module install nodejs:22`; `localectl set-locale`) with rationale"
    - "It follows the existing ADR heading structure (Status / Date / Context / Decision / Consequences)"
  artifacts:
    - path: "docs/decisions/017-distro-family-bucket.md"
      provides: "Architecture Decision Record for the distro-family abstraction"
      contains: "AGENTLINUX_DISTRO_FAMILY"
      min_lines: 30
  key_links:
    - from: "docs/decisions/017-distro-family-bucket.md"
      to: "plugin/lib/pkg.sh + plugin/lib/distro_detect.sh"
      via: "documents the abstraction those files implement"
      pattern: "pkg.sh"
---

<objective>
Record ADR-017: the decision to recognize AlmaLinux 9 via a single `AGENTLINUX_DISTRO_FAMILY`
bucket exported from `distro_detect.sh` and to consolidate every apt↔dnf branch into one
`lib/pkg.sh` dispatch layer — rather than scattering `if [[ $FAMILY == rhel ]]` across 13 call
sites. Capture the rejected alternatives and their rationale so a future EL-family expansion
inherits the reasoning.

Purpose: ADR-017 is the planned decision artifact for this phase (STATE.md + RESEARCH note it);
it is the design rationale future maintainers read before extending the family bucket.
Output: docs/decisions/017-distro-family-bucket.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-detection-branching-foundation/18-RESEARCH.md
@.planning/phases/18-detection-branching-foundation/18-PATTERNS.md

<interfaces>
<!-- Existing ADR structure to mirror (docs/decisions/005-system-nodejs-over-version-managers.md):
     # NNN: Title / **Status:** Accepted / **Date:** YYYY-MM-DD / ## Context / ## Decision / ## Consequences
     Directly-referenced precedents: ADR-005 (NodeSource curl-pipe), ADR-012 (sudoers drop-in). -->

Rejected alternatives to document (18-RESEARCH.md "Alternatives Considered" + "Anti-Patterns"):
  - Inline `if [[ $FAMILY == rhel ]]` per call site → 13× duplicated drift across 5 files (Anti-Pattern 2)
  - AppStream `dnf module install nodejs:22` → stream availability drifts across 9.x minors; diverges from NodeSource-everywhere (RT-01)
  - `localectl set-locale` → needs systemd-localed/D-Bus, absent in Docker; write /etc/locale.conf directly
  - Branch on `ID_LIKE` → silently admits Rocky/RHEL/CentOS/Fedora (out of scope); match `ID` exactly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-017 distro-family-bucket</name>
  <files>docs/decisions/017-distro-family-bucket.md</files>
  <read_first>
    - docs/decisions/005-system-nodejs-over-version-managers.md (full — heading structure to mirror)
    - docs/decisions/012-agent-user-full-sudo.md (heading + cross-ref style for a longer ADR)
    - 18-RESEARCH.md "Alternatives Considered", "Anti-Patterns to Avoid", "Architectural Responsibility Map"
  </read_first>
  <action>
    Create docs/decisions/017-distro-family-bucket.md mirroring the ADR-005/012 structure:
    title `# 017: Distro-family bucket (AGENTLINUX_DISTRO_FAMILY) + single pkg.sh dispatch`,
    `**Status:** Accepted`, `**Date:** 2026-06-28`. Sections:
    - Context: v0.3.5 ports the Ubuntu-only installer to AlmaLinux 9; the apt/dpkg/locale coupling is
      13 hardcoded sites across 5 files; the behavior contract asserts outcomes not package managers.
    - Decision: `distro_detect.sh` exports `AGENTLINUX_DISTRO_FAMILY ∈ {debian, rhel}` (ID-exact match,
      almalinux 9.x only); every package/locale/NodeSource operation routes through `lib/pkg.sh` verbs
      that branch ONCE on the family; the debian arm preserves Ubuntu behavior byte-for-byte; contract
      JSON field names (e.g. can_sudo_apt) are preserved while their probes generalize.
    - Consequences: one auditable branch point; future EL-family expansion is a small follow-on (but no
      family-wide claim is made now — AlmaLinux 9 ONLY); the bats contract survives unchanged; documents
      the rejected alternatives (inline per-site case; AppStream `dnf module install nodejs:22`;
      `localectl set-locale`; `ID_LIKE` matching) with the rationale from the <interfaces> block.
      Cross-reference ADR-005 (NodeSource) and ADR-012 (sudoers drop-in).
    ADRs skip ai-deslop review per CLAUDE.md; technical-writer + fact-checker apply.
  </action>
  <acceptance_criteria>
    - File docs/decisions/017-distro-family-bucket.md exists
    - `grep -q 'AGENTLINUX_DISTRO_FAMILY' docs/decisions/017-distro-family-bucket.md` succeeds
    - `grep -q '## Decision' docs/decisions/017-distro-family-bucket.md` AND `grep -q '## Consequences' docs/decisions/017-distro-family-bucket.md` AND `grep -q '## Context' docs/decisions/017-distro-family-bucket.md`
    - `grep -Eq 'localectl|module install|ID_LIKE' docs/decisions/017-distro-family-bucket.md` (rejected alternatives present)
    - `grep -q '\*\*Status:\*\* Accepted' docs/decisions/017-distro-family-bucket.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/decisions/017-distro-family-bucket.md && grep -q AGENTLINUX_DISTRO_FAMILY docs/decisions/017-distro-family-bucket.md && grep -q '## Consequences' docs/decisions/017-distro-family-bucket.md</automated>
  </verify>
  <done>ADR-017 records the family-bucket + single-pkg.sh-branch decision and its rejected alternatives in the standard ADR format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (documentation) | ADR-017 is a decision record; it introduces no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-20 | Information disclosure | ADR leaking internal-only vocabulary | accept | docs/decisions/ is exempt from external-audience-auditor per CLAUDE.md; this is an internal design record, no secrets |
</threat_model>

<verification>
- ADR file exists with the standard Status/Context/Decision/Consequences structure.
- Rejected alternatives and their rationale documented.
- Cross-references to ADR-005 and ADR-012 present.
</verification>

<success_criteria>
docs/decisions/017-distro-family-bucket.md records the AGENTLINUX_DISTRO_FAMILY + single-pkg.sh-branch
decision, the rejected alternatives, and the AlmaLinux-9-ONLY scope boundary, in the project's ADR format.
</success_criteria>

<output>
After completion, create `.planning/phases/18-detection-branching-foundation/18-06-SUMMARY.md`
</output>
