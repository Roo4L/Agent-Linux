---
phase: 15-strategy-roadmap-doc
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md
  - docs/STRATEGY.md
autonomous: false
requirements:
  - STRATR-01
  - STRATR-02
  - STRATR-03
  - STRATR-04
  - STRATR-05
  - STRATR-06

must_haves:
  truths:
    - "A reviewer pass (technical-writer + fact-checker, in parallel) has reviewed docs/STRATEGY.md and any actionable feedback has been triaged + applied before the phase-close audit emits GREEN"
    - "Any reviewer-applied edits preserve the STRATR-02 spine grep gate (≥ 5 matches), STRATR-03 theme count (= 4), STRATR-04 execution-principle count (5-7), STRATR-05 Last reviewed blockquote, and STRATR-06 voice-rule HARD GATE (zero matches)"
    - "If the reviewer pass mutates docs/STRATEGY.md, the STRATR-01 size gate (≤ 8192 bytes) still holds AND a fresh STRATR-06 voice-rule grep runs clean on the post-review file"
    - "15-AUDIT.md exists at the canonical phase-close path and consolidates every STRATR-01..06 evidence transcript with file paths / line ranges / commit hashes / grep transcripts"
    - "15-AUDIT.md lifts the STRATR-06 voice-rule HARD-GATE grep command + empty output verbatim from 15-01-EVIDENCE.md (or re-captured post-review if Plan 15-02 mutated docs/STRATEGY.md)"
    - "15-AUDIT.md emits the Phase 15 GREEN gate at the bottom with all 6 STRATR-XX requirements marked PASS"
    - "15-AUDIT.md cites the Plan 15-01 commit hash and (if any) the Plan 15-02 review-loop commit hash"
  artifacts:
    - path: ".planning/phases/15-strategy-roadmap-doc/15-AUDIT.md"
      provides: "Phase 15 phase-close audit consolidating STRATR-01..06 evidence + reviewer-pass record + GREEN gate emission"
      contains: "Gate: GREEN"
  key_links:
    - from: ".planning/phases/15-strategy-roadmap-doc/15-AUDIT.md"
      to: ".planning/phases/15-strategy-roadmap-doc/15-01-EVIDENCE.md"
      via: "Verbatim lift of STRATR-01..06 transcript blocks (mirrors 14-AUDIT.md → 14-01-EVIDENCE.md pattern in Plan 14-02)"
      pattern: "15-01-EVIDENCE\\.md"
    - from: ".planning/phases/15-strategy-roadmap-doc/15-AUDIT.md"
      to: "docs/STRATEGY.md"
      via: "Audit cites docs/STRATEGY.md by path in every STRATR-XX section's command + output blocks"
      pattern: "docs/STRATEGY\\.md"
---

<objective>
Run the reviewer pass on `docs/STRATEGY.md` (technical-writer + fact-checker, spawned in parallel per the Phase 14 / `docs/VISION.md` precedent), triage feedback into apply / skip buckets, apply the actionable comments without regressing any STRATR-XX gate, then author the consolidated phase-close audit `.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` that lifts the Plan-15-01 transcripts verbatim and emits the Phase 15 GREEN gate.

Purpose: Plan 15-01 landed the spec amendment + the doc draft + the captured evidence transcripts. Plan 15-02 is the reviewer-and-close pair — same shape as Phase 14 / Plan 14-02 (which combined the downstream surface edits with the consolidated 14-AUDIT.md gate close). The reviewer pass MUST execute on the post-Plan-15-01 docs/STRATEGY.md so any wording smells, fact-checking issues, or aspirational-drift near-misses get caught BEFORE the gate emits GREEN. Per CLAUDE.md `## Review Loop`: Markdown docs (excluding ADRs + research summaries) take `technical-writer` + `fact-checker` + `ai-deslop`; for the strategy doc the discuss-phase locked technical-writer + fact-checker only (mirroring the docs/VISION.md reviewer-pass pattern committed `f95a4ee` 2026-05-16). ai-deslop is optional for this plan (Claude's discretion under reviewer-loop discipline).

Output:
- (Optional) revised `docs/STRATEGY.md` if reviewer feedback warrants an edit — landed in a separate commit before the audit close. STRATR-XX gates must re-pass on the revised file.
- `.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` — phase-close audit with STRATR-01..06 evidence (lifted verbatim from 15-01-EVIDENCE.md, OR re-captured if Plan 15-02 edited docs/STRATEGY.md), reviewer-pass record, and the Phase 15 GREEN gate emission line.
- Single commit landing 15-AUDIT.md (and any reviewer-applied edits to docs/STRATEGY.md if they happened, in a SEPARATE earlier commit so the audit commit lands clean — mirrors Plan 14-02's commit pattern).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-strategy-roadmap-doc/15-CONTEXT.md
@.planning/phases/15-strategy-roadmap-doc/15-01-PLAN.md
@.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md
@.planning/phases/14-vision-doc-and-downstream/14-01-EVIDENCE.md
@docs/STRATEGY.md
@docs/VISION.md
@CLAUDE.md

<voice_rule>
Reviewer-applied edits to docs/STRATEGY.md MUST preserve the STRATR-06
voice-rule HARD GATE (zero matches on the blocked AgentLinux + verb regex).
If a reviewer suggests rewording that introduces a blocked construction
(e.g. "AgentLinux protects users from supply-chain drift"), REJECT or
REWRITE — never accept the edit verbatim. Use "we" / "our roadmap" /
explicit milestone identifier as grammatical subject for any forward-
looking claim.

Re-run the STRATR-06 grep AFTER applying reviewer edits AND BEFORE writing
15-AUDIT.md. If the post-review grep produces ANY match, the plan is in
HARD-GATE FAIL state — STOP, revert the offending reviewer edit, re-test,
re-loop.

15-AUDIT.md itself is also subject to the voice rule (the audit is
authored Markdown). Apply delivered-fact voice for shipped behaviours
recorded in evidence transcripts (e.g. "the v0.3.0 plugin ships",
"STRATR-06 grep returns zero matches"). Forward-looking text inside the
audit ("the v0.3.4 milestone will land brownfield installation") MUST use
"we" / "our roadmap" / "v0.3.4" / "AL-38" voice.
</voice_rule>

<interfaces>
<!-- Reviewer-pass invocation pattern + 15-AUDIT.md skeleton the executor uses. -->

Reviewer pass — invocation pattern (mirrors Phase 14 / Plan 14-01 reviewer-pass committed `f95a4ee` 2026-05-16):

Spawn two subagents in PARALLEL via the Task tool, each with `docs/STRATEGY.md` as the review target:

1. `technical-writer` subagent:
   ```
   prompt: |
     Review docs/STRATEGY.md for technical-writing concerns. Focus on:
     - Clarity (each section's claim is decipherable on first read)
     - Concision (no padding; the doc is intentionally short; 8 KB ceiling)
     - Voice consistency (delivered-fact vs forward-looking; the doc carries
       a voice-rule grep gate so aspirational drift is a hard fail)
     - Section transitions (the 5-section spine should read as a coherent
       argument: diagnosis → bets → state → plan → principles, per Rumelt-
       style strategy doc design)
     - Cross-reference density (light-touch inline; bulk lives in `## Related`)
     - Adjacency to VISION.md (companion-doc framing; do not duplicate
       VISION.md's mission/pillars content)

     Return free-form feedback. NOT a rewrite; surface concerns + propose
     specific edits where useful. Do not introduce new claims — the
     substance is locked by 15-CONTEXT.md and CANNOT be changed by review.
   ```

2. `fact-checker` subagent:
   ```
   prompt: |
     Fact-check docs/STRATEGY.md against the canonical refs in
     `.planning/phases/15-strategy-roadmap-doc/15-CONTEXT.md`. Verify:
     - Every ADR / Jira / decision-summary citation resolves to a real file
       or URL (ADR-001, ADR-002, ADR-004, ADR-011, ADR-015, AL-7, AL-38,
       AGT-02, TST-07, TST-08, HARNESS.md §4)
     - The "first usable release = v0.3.4 + AlmaLinux" definition matches
       15-CONTEXT.md `<decisions>` § "First usable AgentLinux release for
       myself"
     - The 4 themes (Security Hardening / Preset-Profile / Broader catalog
       / Public engagement) match STRATR-03 + 15-CONTEXT.md theme list +
       the locked sequencing rationale (Public engagement gated on
       catalog critical mass)
     - The 5 mandated execution principles match STRATR-04 + 15-CONTEXT.md
       § "Execution principles" — voice rule + behavior-tests-as-spec
       (ADR-002) + evidence-cite (TST-07) + curated-combo testing (TST-08)
       + no `sudo npm install -g` (ADR-004) are all present with correct
       citations
     - The "v0.3.0 plugin ships against Ubuntu 22.04 / 24.04 / 26.04 with
       AGT-02 zero-EACCES self-update" sentence matches PROJECT.md /
       STATE.md / ADR-011 / AGT-02 reality (delivered fact, not claim)
     - The voice-rule regex listed in `## Execution principles` matches
       VIS-07 / STRATR-06 / SITE-06 verbatim

     Return free-form feedback. Flag any factually wrong claim, broken
     ADR citation, mis-cited Jira link, or contradiction with VISION.md /
     STABILITY-MODEL.md.
   ```

Both subagents return free-form text (per `.claude/agents/` review-loop convention — see CLAUDE.md `## Review Loop` and ADR-010). The triage step (Task 1 below) reads both returns and decides per-comment: apply / skip / counter.

Per CLAUDE.md: "Main agent owns triage: fix what's valid, skip what's noise, iterate until the remaining comments are not actionable." For this plan: max 1 review-loop iteration. If a second iteration would be needed, escalate to the user instead of looping silently.

15-AUDIT.md skeleton (lifts 14-AUDIT.md precedent shape verbatim):

```markdown
# Phase 15 Audit — Strategy + Roadmap Doc

> Phase: 15-strategy-roadmap-doc
> Authored: 2026-05-19
> Gate: GREEN

## Summary

Phase 15 landed the canonical product strategy/roadmap document at
`docs/STRATEGY.md` — the "how we get there" companion to `docs/VISION.md`'s
"what we want to be" — and amended REQUIREMENTS.md STRATR-02 in the same
commit window to mandate the new Rumelt-style 5-section spine (mid-discuss
research-driven reframe locked 2026-05-19; precedent: Phase 14 / Plan 14-02
STRAT-* → VIS-* + STRATR-* amendment). The STRATR-06 voice-rule HARD GATE
passes (zero matches on `docs/STRATEGY.md`). Reviewer pass (technical-writer
+ fact-checker, parallel) returned [N] comments; [M] applied as edits in a
separate pre-audit commit, [K] declined as not actionable.

Plan commits:

- **Plan 15-01 (REQUIREMENTS.md amendment + docs/STRATEGY.md authoring + 15-01-EVIDENCE.md):**
  - `{hash}` — `docs(15-01): land docs/STRATEGY.md + amend REQUIREMENTS.md STRATR-02`
- **Plan 15-02 (reviewer pass + this audit):**
  - (optional, only if reviewer edits applied) `{hash}` — `docs(15-02): apply reviewer-pass feedback to docs/STRATEGY.md`
  - (this commit) — `docs(15-02): phase-close audit 15-AUDIT.md (Phase 15 GREEN)`

Total requirements closed: 6 (STRATR-01..06).

## STRATR-01 — docs/STRATEGY.md exists; size <= 8192 bytes

**Acceptance (from REQUIREMENTS.md):** docs/STRATEGY.md exists at the canonical repo path, sibling to VISION.md / STABILITY-MODEL.md / HARNESS.md. File size ≤ 8 KB on first cut. `wc -c docs/STRATEGY.md` ≤ 8192.

Lifted verbatim from `15-01-EVIDENCE.md` § STRATR-01:

Command:
```
wc -c docs/STRATEGY.md
```

Output:
```
{N} docs/STRATEGY.md
```

Commit context:
```
{Plan 15-01 hash + subject line}
```

Verdict: PASS (size {N} <= 8192).

## STRATR-02 — Rumelt-style 5-section spine (amended 2026-05-19)

**Acceptance (from REQUIREMENTS.md, amended in same commit window):** The doc's spine reflects Rumelt-style strategy structure in order: `## What we're solving` → `## Our bets` → `## Where we are now` → `## What's next` (with `### Near-term` + `### Themes for v0.6+` subsections) → `## Execution principles`. `grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md` returns ≥ 5 matches in prescribed order; `grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md` returns ≥ 2 matches.

Lifted verbatim from `15-01-EVIDENCE.md` § STRATR-02:

[Both commands + outputs verbatim.]

Amendment trail:
- REQUIREMENTS.md STRATR-02 was amended in the Plan 15-01 commit window to replace the original 4-section spine (`Where we are now` / `What we're working on next` / `Themes for` / `Execution principles` ≥ 4 matches) with the new 5-section Rumelt-style spine. Amendment grep:
  ```
  grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving" .planning/REQUIREMENTS.md
  ```
  Output:
  ```
  {line}: ...
  ```
- The 2026-05-19 amendment is recorded in `## Superseded Items (2026-05-19 Phase 15 spine reframe)` block in REQUIREMENTS.md (sibling to the existing 2026-05-16 Phase 14 reframe block). Grep:
  ```
  grep -nE '## Superseded Items \(2026-05-19' .planning/REQUIREMENTS.md
  ```
  Output:
  ```
  {line}: ## Superseded Items (2026-05-19 Phase 15 spine reframe)
  ```

Verdict: PASS (5 H2 spine matches in prescribed order + 2 H3 subsection matches; REQUIREMENTS.md amendment landed; Superseded-items audit trail recorded).

## STRATR-03 — `### Themes for v0.6+` lists exactly 4 themes

[Lifted from 15-01-EVIDENCE.md § STRATR-03 — awk count 4; sequencing-rationale count >= 4; Public engagement critical-mass grep >= 1.]

Verdict: PASS.

## STRATR-04 — `## Execution principles` lists 5-7 entries

[Lifted from 15-01-EVIDENCE.md § STRATR-04 — awk count in [5..7]; bullet format `**Name** — ` ... `(citation)`.]

Verdict: PASS.

## STRATR-05 — `> Last reviewed: 2026-05-19` in top 5 lines

[Lifted from 15-01-EVIDENCE.md § STRATR-05.]

Verdict: PASS.

## STRATR-06 — Voice-rule hard gate (HARD GATE; zero matches required)

**Acceptance (from REQUIREMENTS.md):** The voice-rule grep gate passes on STRATEGY.md. `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md` MUST return zero matches anywhere in the doc. Acceptance evidence: the grep command + its empty output committed verbatim to this audit. Hard gate.

Lifted verbatim from `15-01-EVIDENCE.md` § STRATR-06 (or re-captured below if Plan 15-02 mutated docs/STRATEGY.md):

Command:
```
grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"
```

Output:
```
exit=1
```
(Empty grep output + `exit=1` is the PASS shape. Any other shape is a HARD-GATE FAIL.)

Verdict: PASS (HARD GATE).

## Reviewer pass record

**Reviewers:** technical-writer + fact-checker (spawned in parallel; per CLAUDE.md `## Review Loop` Markdown docs convention; ai-deslop intentionally skipped per Claude's discretion — strategy-doc voice is intentionally on-brand technical prose, not the AI-deslop target).

**Iterations:** 1.

**Comments returned:**
- technical-writer: [N] free-form comments
- fact-checker: [M] free-form comments

**Triage:**

| Comment | Source | Disposition | Action |
|---------|--------|-------------|--------|
| {summary} | technical-writer | applied | Edit at line {L} in docs/STRATEGY.md; preserved STRATR-XX gates |
| {summary} | fact-checker | skipped | Not actionable — {reason} |
| ... | ... | ... | ... |

**Post-review re-verify (if edits applied):**
- STRATR-01 size: `wc -c docs/STRATEGY.md` returns `{N} <= 8192`.
- STRATR-02 spine: 5 H2 matches in order; 2 H3 subsection matches.
- STRATR-03 themes: 4 themes; sequencing rationales present; critical-mass gating preserved.
- STRATR-04 execution principles: count in [5..7].
- STRATR-05 Last reviewed header: present.
- STRATR-06 voice-rule grep: zero matches (HARD GATE; re-run + empty output captured).

**Reviewer commit (if applied):** `{hash}` — `docs(15-02): apply reviewer-pass feedback to docs/STRATEGY.md`.

## Aggregate gate status

| Requirement | Verdict | Evidence source |
|-------------|---------|----------------|
| STRATR-01 (size ≤ 8 KB) | PASS | 15-01-EVIDENCE.md § STRATR-01 |
| STRATR-02 (5-section spine + subsections; amendment landed) | PASS | 15-01-EVIDENCE.md § STRATR-02 + REQUIREMENTS.md amendment grep |
| STRATR-03 (4 themes + sequencing rationales + critical-mass gating) | PASS | 15-01-EVIDENCE.md § STRATR-03 |
| STRATR-04 (5-7 execution principles with citations) | PASS | 15-01-EVIDENCE.md § STRATR-04 |
| STRATR-05 (Last reviewed blockquote) | PASS | 15-01-EVIDENCE.md § STRATR-05 |
| STRATR-06 (voice-rule HARD GATE) | PASS | 15-01-EVIDENCE.md § STRATR-06 (or re-captured post-review) |

**Phase 15 gate: GREEN.**

All 6 STRATR-XX requirements close PASS. The strategy doc is the canonical
strategy/roadmap reference for v0.3.3 onward; Phase 16 (website refresh) can
now consume `docs/STRATEGY.md` as a stable URL target for the SITE-04
comparison-block reframe + SITE-07 footer link.
```
</interfaces>

</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Spawn reviewer pass on docs/STRATEGY.md (technical-writer + fact-checker, parallel)</name>
  <files>docs/STRATEGY.md</files>
  <read_first>
    - `docs/STRATEGY.md` (the just-landed-by-Plan-15-01 review target)
    - `.planning/phases/15-strategy-roadmap-doc/15-CONTEXT.md` (locked decisions reviewers must NOT contradict)
    - `.planning/REQUIREMENTS.md` STRATR-01..06 (the gates the post-review file must still pass)
    - `CLAUDE.md` `## Review Loop` section (reviewer-by-file-type matrix; docs row = technical-writer + fact-checker + ai-deslop; ai-deslop optional per Claude's discretion for strategy doc)
  </read_first>
  <what-built>
Plan 15-01 landed docs/STRATEGY.md with the locked 5-section Rumelt-style spine, 4 themes, and 5-7 execution principles; STRATR-06 voice-rule grep passes clean. Plan 15-02 now runs the reviewer pass to catch wording smells, fact-check ADR/Jira/decision-summary citations, and verify the doc reads as a coherent strategy argument before the phase-close gate emits GREEN.
  </what-built>
  <how-to-verify>
Spawn the reviewer pass via the Task tool, two subagents IN PARALLEL (single message with two Task tool calls — per CLAUDE.md `## Review Loop` and the Phase 14 / docs/VISION.md `f95a4ee` reviewer-pass precedent):

1. **technical-writer subagent.** Prompt per `<interfaces>` § "Reviewer pass — invocation pattern" item 1 (clarity / concision / voice consistency / section transitions / cross-reference density / VISION.md adjacency).

2. **fact-checker subagent.** Prompt per `<interfaces>` § "Reviewer pass — invocation pattern" item 2 (ADR/Jira/decision-summary citation resolution; first-usable-release definition match; theme + execution-principle list match; AGT-02 / Ubuntu version reality; voice-rule regex verbatim).

Per CLAUDE.md: ai-deslop is OPTIONAL for this plan (Claude's discretion — strategy-doc voice is intentionally on-brand technical prose, not the AI-deslop pattern target). Skip unless the reviewer-pass returns clearly suggest AI-deslop signals (then spawn ai-deslop and treat as a 1-iteration loop with the same triage).

Both subagents return free-form comment text. Read both returns end-to-end (single pass — no re-reads per the planner critical-rules) and produce a triage table:

| Comment | Source | Disposition | Reasoning |
|---------|--------|-------------|-----------|
| {summary one-liner} | technical-writer / fact-checker | apply / skip | Why apply (specific gate impact) OR why skip (not actionable, contradicts locked decision, false positive) |

Per CLAUDE.md `## Review Loop`: main agent owns triage. Fix what's valid, skip what's noise. Reviewer feedback CANNOT change the locked spine (STRATR-02), the locked theme count (STRATR-03 = 4 themes), the locked execution-principle citations (STRATR-04), or any 15-CONTEXT.md `<decisions>` content. Reviewer feedback CAN improve wording, fix typos, suggest sentence reordering, flag broken ADR/Jira links, surface voice-rule near-misses, and propose section-transition tweaks.

Save the triage table as a transient note (the audit lifts it in Task 3). If ANY comment is applied as an edit, proceed to Task 2 (apply + re-verify). If ALL comments are skipped (no actionable feedback), skip Task 2 and go straight to Task 3.

**Hard gate:** if reviewer feedback would require a SECOND review iteration (revised doc + re-review needed), STOP and surface to the user — per the planner discuss-phase locked decision: max 1 review iteration in this plan.
  </how-to-verify>
  <action>
Spawn the reviewer pass via the Task tool: two Task tool calls IN PARALLEL in a single message — one with `subagent_type=technical-writer` (prompt per `<interfaces>` § "Reviewer pass — invocation pattern" item 1) and one with `subagent_type=fact-checker` (prompt per `<interfaces>` § "Reviewer pass — invocation pattern" item 2). Both target `docs/STRATEGY.md` as the review file.

After both subagents return free-form comment text, produce the triage table per the `<how-to-verify>` block — read both returns end-to-end in a single pass, classify each comment as apply / skip with a one-line rationale. Per CLAUDE.md `## Review Loop`: locked decisions in 15-CONTEXT.md `<decisions>` are non-negotiable; reviewer feedback that contradicts a locked decision is automatically skipped.

If ANY comment is applied: hand off to Task 2 (apply + re-verify). If ALL comments are skipped: hand off straight to Task 3 (audit close); record the "no edits applied — all comments triaged as not actionable" reasoning summary for the audit's `## Reviewer pass record` section.

This is a checkpoint:human-action task because the spawning + triage decision is interactive: the user reviews the triage table before edits land. Resume signal: type "approved" after triage table is captured + apply/skip decisions are recorded.
  </action>
  <verify>
    <automated>echo "checkpoint:human-action — verification is the captured triage table itself; no automated check"</automated>
  </verify>
  <done>
Reviewer pass invoked (technical-writer + fact-checker, parallel); both subagent returns triaged into apply / skip buckets with per-comment rationale; user approved the triage table.
  </done>
  <resume-signal>Type "approved" after triage table is captured + apply/skip decisions are recorded</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply triaged reviewer edits to docs/STRATEGY.md + re-verify STRATR-XX gates</name>
  <files>docs/STRATEGY.md</files>
  <read_first>
    - `docs/STRATEGY.md` (current state after Plan 15-01 commit)
    - Task 1 triage table (from the human-action checkpoint; held in the orchestrator's conversation context — NOT persisted to disk before this Task runs)
    - `.planning/phases/15-strategy-roadmap-doc/15-01-EVIDENCE.md` (locked STRATR-01..06 verdict shapes; the post-Task-2 re-verify must match these PASS shapes)
    - `.planning/phases/15-strategy-roadmap-doc/15-CONTEXT.md` (locked decisions — reviewer-applied edits MUST honor every `<decisions>` § locked decision; non-negotiable)
  </read_first>
  <action>
**SKIP this task entirely if Task 1 triage decided ALL reviewer comments are skipped (no edits to apply).** Proceed straight to Task 3.

Otherwise:

For each comment marked "apply" in the Task 1 triage table, edit `docs/STRATEGY.md` using the Edit tool (NEVER overwrite the whole file; use surgical Edit calls with exact old_string / new_string per comment). Constraints:

1. Preserve the locked 5-section spine. Edits MUST NOT add, remove, rename, or reorder any of the 5 H2 sections or the 2 H3 subsections under `## What's next`.

2. Preserve the locked theme count (4) + sequencing rationale lines. Edits MUST NOT collapse two themes, add a fifth theme, or remove a `**Sequencing rationale:**` line.

3. Preserve the Public engagement theme #4 critical-mass gating language. Edits MAY tighten the wording but MUST retain an explicit reference to theme #3 (Broader catalog) reaching critical mass as the sequencing gate.

4. Preserve every execution principle's parenthetical citation (e.g., `(ADR-002)`, `(ADR-004)`, `(ADR-011, TST-08, STABILITY-MODEL.md)`). Reviewer can suggest tightening the gloss; cannot drop the citation.

5. Preserve the `> Last reviewed: 2026-05-19` blockquote as first non-blank line after H1 (STRATR-05).

6. Preserve the `## Related` block at the bottom with at least 8 cross-reference entries.

7. **HARD CONSTRAINT — voice rule.** No reviewer edit can introduce a blocked `AgentLinux + verb` construction. Re-run the STRATR-06 grep after EACH edit batch:
   ```
   grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md
   ```
   If ANY match, revert the offending edit, rewrite with "we" / "our roadmap" / explicit milestone identifier as grammatical subject, re-test.

8. **Size constraint.** After all edits, re-run `wc -c docs/STRATEGY.md`. Must still be ≤ 8192. If over: drop the optional 6th/7th execution principle first, then tighten prose. If still over: escalate.

After all triaged edits apply cleanly, run the full STRATR-XX re-verify suite (same commands as 15-01-EVIDENCE.md):

```bash
wc -c docs/STRATEGY.md
grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md
grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md
awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md
grep -c '\*\*Sequencing rationale:\*\*' docs/STRATEGY.md
awk '/^#### Public engagement/{flag=1;next} /^#### /{flag=0} /^### /{flag=0} flag && /critical mass/{c++} END{print c}' docs/STRATEGY.md
awk '/^## Execution principles/{flag=1;next} /^## /{flag=0} flag && /^- \*\*/{c++} END{print c}' docs/STRATEGY.md
head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05-19'
grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"
```

Capture the re-verified outputs (held in conversation context — the audit in Task 3 will either lift these OR lift the originals from 15-01-EVIDENCE.md depending on whether Task 2 mutated the doc). If ANY gate fails, STOP — revert the offending reviewer edit, re-test, iterate. Max 1 review-loop iteration per the Task 1 escalation rule.

After re-verify clean, commit the reviewer-applied edits in a SEPARATE commit (before the audit commit lands in Task 3):

```
docs(15-02): apply reviewer-pass feedback to docs/STRATEGY.md

Phase 15 Plan 02: applied [N] of [M] reviewer comments from the
technical-writer + fact-checker parallel pass. Skipped [K] as not
actionable. All STRATR-01..06 gates re-pass on the revised doc;
STRATR-06 voice-rule HARD GATE re-runs clean (zero matches).

Comments applied:
- {one-line summary}
- {one-line summary}
...

Comments skipped (rationale):
- {one-line summary} — {reason}
...

Co-Authored-By: Claude <noreply@anthropic.com>
```

(Per CLAUDE.md prefer-new-commits rule; co-author trailer; explicit subject area in parens; mirrors the Phase 14 docs/VISION.md reviewer-pass commit `f95a4ee` shape.)
  </action>
  <verify>
    <automated>test -f docs/STRATEGY.md && wc -c docs/STRATEGY.md | awk '{print ($1 <= 8192 ? "PASS" : "FAIL"), $1}' && grep -nE '^## (What we'"'"'re solving|Our bets|Where we are now|What'"'"'s next|Execution principles)' docs/STRATEGY.md | wc -l && awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md && grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "voice-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - If Task 1 triage applied >= 1 reviewer edit: a new commit exists with message starting `docs(15-02): apply reviewer-pass feedback`.
    - If Task 1 triage applied 0 edits: this task is skipped; git log shows no `docs(15-02): apply reviewer-pass feedback` commit (only the Plan 15-01 commit + the upcoming audit commit from Task 3).
    - `wc -c docs/STRATEGY.md` returns a byte count <= 8192.
    - `grep -nE '^## (What we'"'"'re solving|Our bets|Where we are now|What'"'"'s next|Execution principles)' docs/STRATEGY.md` still returns exactly 5 lines in prescribed order.
    - `awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md` still returns exactly 4.
    - `awk '/^## Execution principles/{flag=1;next} /^## /{flag=0} flag && /^- \*\*/{c++} END{print c}' docs/STRATEGY.md` still returns a value in [5..7].
    - `head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05-19'` still returns 1 match.
    - `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md` still returns ZERO matches (HARD GATE preserved).
    - `awk '/^#### Public engagement/{flag=1;next} /^#### /{flag=0} /^### /{flag=0} flag && /critical mass/{c++} END{print c}' docs/STRATEGY.md` still returns >= 1 (critical-mass gating preserved).
  </acceptance_criteria>
  <done>
Reviewer-pass edits applied (or skipped if no actionable feedback). All STRATR-01..06 gates re-pass on the revised file. If edits applied, separate reviewer commit landed before the audit commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 15-AUDIT.md (consolidated phase-close audit; emit Phase 15 GREEN gate)</name>
  <files>.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md</files>
  <read_first>
    - `.planning/phases/15-strategy-roadmap-doc/15-01-EVIDENCE.md` (Plan 15-01's pre-audit transcripts — lifted verbatim into the audit per the 14-AUDIT.md → 14-01-EVIDENCE.md precedent)
    - `.planning/phases/15-strategy-roadmap-doc/15-01-PLAN.md` (commit-hash reference)
    - `.planning/phases/14-vision-doc-and-downstream/14-AUDIT.md` (audit-pattern precedent; full shape — Summary block, per-requirement section, Aggregate gate status, GREEN emission line)
    - `docs/STRATEGY.md` (audit target; re-grepped post-review if Plan 15-02 Task 2 mutated it)
    - `.planning/REQUIREMENTS.md` (verify STRATR-02 amendment landed correctly; cite the amendment commit hash)
  </read_first>
  <action>
Use the Write tool to create `.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` from scratch. Lift the FULL skeleton from `<interfaces>` § "15-AUDIT.md skeleton" and fill in the bracketed `{...}` placeholders.

Specific authoring steps:

1. **Header.** `# Phase 15 Audit — Strategy + Roadmap Doc` + `> Phase: 15-strategy-roadmap-doc` + `> Authored: 2026-05-19` + `> Gate: GREEN` blockquotes.

2. **`## Summary` block.** One paragraph naming what shipped (docs/STRATEGY.md with locked 5-section Rumelt-style spine + REQUIREMENTS.md STRATR-02 amended in same commit window + STRATR-06 HARD GATE clean). Reviewer pass result (`[N]` comments returned, `[M]` applied, `[K]` skipped — fill from Task 1 triage). Plan commits list (Plan 15-01 hash + subject; Plan 15-02 reviewer hash if Task 2 produced one; this audit commit as the closing). Total requirements closed: 6.

3. **`## STRATR-01` through `## STRATR-06` sections.** For each:
   a. State the acceptance criterion (lift verbatim from `.planning/REQUIREMENTS.md` line for that ID, OR for STRATR-02 use the amended text since the amendment is in scope).
   b. State the lift-source: "Lifted verbatim from `15-01-EVIDENCE.md` § STRATR-NN" — UNLESS Plan 15-02 Task 2 mutated docs/STRATEGY.md, in which case the lift-source is the post-review re-capture (use the same exact transcript shape).
   c. Reproduce the verbatim command + verbatim output (triple-backtick code fences; exact stdout; no paraphrasing).
   d. Verdict line: `Verdict: PASS (<one-line reason>).` — append `(HARD GATE)` annotation for STRATR-06.

   For STRATR-02 specifically: BOTH grep commands (5-section spine + Near-term/Themes-for-v0.6+ subsections) + the REQUIREMENTS.md amendment grep + the Superseded-items grep all four blocks appear under this section. The amendment trail subsection is explicit — the audit names that the amendment landed in the Plan 15-01 commit window per the 2026-05-16 Phase 14 reframe precedent.

   For STRATR-06: capture the empty-grep + `exit=1` shape verbatim. Annotate "HARD GATE". This is the load-bearing structural defense for the milestone (per ROADMAP.md "Notes on verification" + 14-AUDIT.md precedent).

4. **`## Reviewer pass record` section.** Document the parallel technical-writer + fact-checker invocation, the iteration count (1), the comment counts per reviewer, and the triage table from Task 1. List applied edits with line-reference + STRATR-XX gate impact. List skipped comments with one-line rationale. If Task 2 ran (edits applied), include the reviewer commit hash; if Task 2 skipped (no actionable feedback), state "No edits applied — all comments triaged as not actionable" + reasoning summary.

5. **`## Aggregate gate status` block.** Table per STRATR-XX requirement (6 rows): Requirement / Verdict / Evidence source. All 6 rows verdict PASS. End with the `**Phase 15 gate: GREEN.**` line + the one-paragraph close ("All 6 STRATR-XX requirements close PASS. The strategy doc is the canonical strategy/roadmap reference for v0.3.3 onward; Phase 16 (website refresh) can now consume `docs/STRATEGY.md` as a stable URL target for the SITE-04 comparison-block reframe + SITE-07 footer link.").

CRITICAL: Plan 14-02's `14-AUDIT.md` is the precedent shape. Use the same conventions:
- Verbatim lift from EVIDENCE.md transcripts (not paraphrased).
- Triple-backtick code fences with exact stdout.
- Commit hashes in code fences as one-liners.
- `Verdict: PASS (...)` line per section.
- Aggregate gate status table at the bottom.
- `Gate: GREEN` blockquote at the top + `**Phase 15 gate: GREEN.**` emission line at the bottom (both required by 14-AUDIT.md precedent — the audit IS the gate).

After authoring, commit 15-AUDIT.md in a SINGLE commit (separate from the Task 2 reviewer commit if any):

```
docs(15-02): phase-close audit 15-AUDIT.md (Phase 15 GREEN)

Phase 15 Plan 02: authored 15-AUDIT.md consolidating STRATR-01..06
evidence (lifted verbatim from 15-01-EVIDENCE.md), reviewer-pass record
(technical-writer + fact-checker parallel; [N] comments returned, [M]
applied, [K] skipped), and the Phase 15 GREEN gate emission line. The
strategy doc + REQUIREMENTS.md STRATR-02 amendment + this audit together
close Phase 15.

Phase 15 gate: GREEN.

Co-Authored-By: Claude <noreply@anthropic.com>
```

(Per CLAUDE.md prefer-new-commits rule; co-author trailer; explicit subject area in parens; mirrors the Phase 14 Plan 14-02 audit-close commit shape.)
  </action>
  <verify>
    <automated>test -f .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md && grep -c '^## STRATR-0[1-6]' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md && grep -c 'Verdict: PASS' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md && grep -c 'Gate: GREEN' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md && grep -c 'HARD GATE' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md && grep -c 'Phase 15 gate: GREEN' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` exists.
    - `grep -c '^## STRATR-0[1-6]' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns exactly 6 (one section per STRATR-XX requirement).
    - `grep -c 'Verdict: PASS' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns at least 6 (one PASS verdict per STRATR-XX section; reviewer-pass record may add more if it states re-verify PASS).
    - `grep -c 'Gate: GREEN' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns at least 1 (top blockquote).
    - `grep -c 'HARD GATE' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns at least 1 (STRATR-06 section annotation).
    - `grep -c 'Phase 15 gate: GREEN' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns at least 1 (closing emission line).
    - `grep -c '15-01-EVIDENCE\.md' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns at least 6 (lifted-verbatim attribution per requirement, mirroring 14-AUDIT.md → 14-01-EVIDENCE.md attribution pattern).
    - `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` returns ZERO matches (the audit itself respects the voice rule per `<voice_rule>` note above).
    - Git log shows a commit with message starting `docs(15-02): phase-close audit 15-AUDIT.md (Phase 15 GREEN)` — `git log -1 --format="%s"` returns that subject line.
    - The audit file references the Plan 15-01 commit hash (which carried REQUIREMENTS.md + docs/STRATEGY.md + 15-01-EVIDENCE.md).
  </acceptance_criteria>
  <done>
15-AUDIT.md exists at the canonical phase-close path; all 6 STRATR-XX requirements close PASS with verbatim-lifted evidence transcripts; the Phase 15 gate emits GREEN. Final audit commit landed. The strategy doc + REQUIREMENTS.md STRATR-02 amendment + this audit together close Phase 15.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer subagent → main-agent triage | Reviewer subagents (technical-writer + fact-checker) return free-form text; main agent triages. Untrusted-input style threat — a reviewer could in principle suggest an edit that would regress a STRATR-XX gate. Mitigated by Task 2's re-verify suite. |
| main-agent → docs/STRATEGY.md (reviewer-applied edits) | Edits land via Edit tool with surgical old_string / new_string. Untrusted only in the sense that an over-eager apply could break a gate. Mitigated by the post-edit STRATR-XX re-verify in Task 2. |
| audit author → audit file (committed Markdown) | Audit is locally authored and committed to the public repo. Same trust profile as Plan 15-01's STRATEGY.md authoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | T (Tampering) | Reviewer subagent suggests an edit that introduces a STRATR-06 voice-rule violation | mitigate | Task 2 explicit hard constraint: re-run STRATR-06 grep after EACH edit batch; revert and rewrite on any match. The HARD GATE language in `<voice_rule>` is non-negotiable. |
| T-15-08 | T (Tampering) | Reviewer subagent suggests rewording that drops a `**Sequencing rationale:**` line or collapses two themes (regresses STRATR-03) | mitigate | Task 2 explicit constraints #1-7 forbid spine / theme / execution-principle / Last-reviewed / Related-block changes; only wording-level edits are permitted. Re-verify suite catches structural regressions before commit. |
| T-15-09 | T (Tampering) | Audit cites a wrong commit hash or paraphrases a transcript instead of lifting verbatim | mitigate | Task 3 explicit constraint: lift verbatim from 15-01-EVIDENCE.md; mirror 14-AUDIT.md precedent shape. Re-grep the same commands against the live files before committing as a final cross-check. |
| T-15-10 | I (Information disclosure) | Reviewer-pass triage table contains reviewer free-form text that includes a secret or PII | accept | Reviewers are scoped subagents reading docs/STRATEGY.md only. The strategy doc contains no secrets / PII. Triage table records summaries (no verbatim full reviewer dumps); no sensitive surface. |
| T-15-11 | R (Repudiation — "Was the reviewer pass actually run?") | Reviewer-pass record in 15-AUDIT.md `## Reviewer pass record` section | mitigate | The section explicitly names reviewer subagent types invoked + comment counts + triage table + per-comment disposition. If reviewer pass was not run, the audit cannot truthfully fill this section; the auditing convention enforces the record. |
| T-15-12 | E (Elevation of privilege) | n/a — doc-only + audit-only | accept | No code, no runtime surface, no privilege flows changed in Plan 15-02. |
</threat_model>

<verification>
Overall Plan 15-02 verification (run before declaring the plan complete):

1. **Reviewer pass executed.** Task 1 checkpoint resolved with a triage table in conversation context naming technical-writer + fact-checker as the spawned reviewer subagents (parallel; one Task tool call message with two parallel spawns). Iteration count = 1 (escalate if a second iteration would be needed).

2. **Reviewer-applied edits preserve STRATR-XX gates.** If Task 2 ran, the post-edit STRATR-XX re-verify suite passes clean. If Task 2 skipped (no actionable feedback), the original 15-01-EVIDENCE.md transcripts still reflect docs/STRATEGY.md state.

3. **STRATR-06 voice-rule HARD GATE is clean on the final docs/STRATEGY.md (post-review).** Re-run the grep at audit-authoring time:
   ```
   grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"
   ```
   Returns empty grep output + `exit=1`. Any other shape = HARD-GATE FAIL.

4. **15-AUDIT.md complete.** Contains: top blockquotes (Phase / Authored / Gate: GREEN), `## Summary`, `## STRATR-01` through `## STRATR-06` sections (each with lifted-verbatim evidence + Verdict: PASS), `## Reviewer pass record`, `## Aggregate gate status` table, and `**Phase 15 gate: GREEN.**` closing emission line.

5. **Audit lift fidelity.** Each STRATR-XX section in 15-AUDIT.md cites `15-01-EVIDENCE.md § STRATR-NN` (or the post-review re-capture commit if Plan 15-02 Task 2 mutated docs/STRATEGY.md) and reproduces the command + output in triple-backtick fences.

6. **Audit-itself voice rule.** Run the STRATR-06 grep against 15-AUDIT.md as well (not a formal gate, defensive practice per 14-AUDIT.md precedent):
   ```
   grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' .planning/phases/15-strategy-roadmap-doc/15-AUDIT.md ; echo "exit=$?"
   ```
   Returns empty grep output + `exit=1`. Any other shape = surface to user; rewrite the offending line.

7. **Phase 15 commit history is clean.** `git log --oneline | head -5` shows in order:
   - `docs(15-02): phase-close audit 15-AUDIT.md (Phase 15 GREEN)` — this audit commit.
   - (Optional, only if Task 2 ran) `docs(15-02): apply reviewer-pass feedback to docs/STRATEGY.md`.
   - `docs(15-01): land docs/STRATEGY.md + amend REQUIREMENTS.md STRATR-02`.
   - (Older Phase 14 commits below.)

8. **Downstream-ready hand-off.** docs/STRATEGY.md is a stable repo path that Phase 16 (website refresh) can link to for SITE-04 + SITE-07. ROADMAP.md Phase 15 entry checkboxes can flip from `[ ]` to `[x]` (handled by the gsd-execute-phase workflow or a separate ROADMAP update — not in this plan's scope).
</verification>

<success_criteria>
- [ ] Reviewer pass run — technical-writer + fact-checker spawned in parallel; comments triaged; apply / skip decisions recorded.
- [ ] If any reviewer comments were applied: docs/STRATEGY.md edited via Edit tool with surgical changes; all STRATR-01..06 gates re-pass on the revised file; separate reviewer commit landed with message starting `docs(15-02): apply reviewer-pass feedback`.
- [ ] If no reviewer comments were applied: Task 2 skipped; reasoning recorded in audit `## Reviewer pass record` section.
- [ ] 15-AUDIT.md exists at `.planning/phases/15-strategy-roadmap-doc/15-AUDIT.md` with the top blockquotes (`> Phase: 15-strategy-roadmap-doc`, `> Authored: 2026-05-19`, `> Gate: GREEN`) + `## Summary` block + `## STRATR-01..06` sections + `## Reviewer pass record` + `## Aggregate gate status` + `**Phase 15 gate: GREEN.**` emission line.
- [ ] All 6 STRATR-XX sections in 15-AUDIT.md lift their evidence transcripts verbatim from 15-01-EVIDENCE.md (or from re-captured post-review evidence if Task 2 ran).
- [ ] STRATR-06 section in 15-AUDIT.md is annotated HARD GATE and shows the empty-grep + exit=1 PASS shape.
- [ ] Aggregate gate status table marks STRATR-01..06 all PASS.
- [ ] Final audit commit landed with message starting `docs(15-02): phase-close audit 15-AUDIT.md (Phase 15 GREEN)`.
- [ ] STRATR-06 voice-rule grep against 15-AUDIT.md (defensive practice, not a formal gate) returns zero matches.
- [ ] Hand-off note for Phase 16: docs/STRATEGY.md at stable repo path, ready for SITE-04 / SITE-07 linkage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-strategy-roadmap-doc/15-02-SUMMARY.md` per `@.claude/get-shit-done/templates/summary.md` with:
- Per-task commit hash: Task 1 = no commit (reviewer-pass invocation only); Task 2 = optional reviewer-applied edits commit (or "no commit — all comments skipped"); Task 3 = the audit-close commit.
- Deviations section — note reviewer-pass comments applied vs skipped (with rationale per skipped comment); any voice-rule near-misses caught + corrected during reviewer triage; any STRATR-XX re-verify that almost regressed (with rationale for the corrective edit).
- Self-Check: PASSED with the 15-AUDIT.md path + the Phase 15 GREEN gate emission line reference.
- Hand-off note for downstream: docs/STRATEGY.md is the canonical strategy/roadmap reference for v0.3.3 onward; Phase 16 (website refresh) can now consume it as a stable URL target for SITE-04 (`#comparison` reframe) + SITE-07 (footer link) per ROADMAP.md.
</output>
