---
phase: 59-full-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - tests/bats/13-reuse.bats
  - .planning/research/SUMMARY.md
  - .claude/agents/security-engineer.md
requirements: [GATE-01]
must_haves:
  truths:
    - "The stale 13-reuse #29 assertion (`13-reuse.bats:552`, `.compatibility_window.type == \"string\"`) is fixed to accept the schemars-generated nullable union `[\"string\",\"null\"]` — the schema (schemars-generated from `Option<String>`, byte-locked by the drift-check, the TEST-03 source-of-truth) is authoritative and UNCHANGED; only the stale TEST expectation is updated. `13-reuse` goes 32/32 (was 31/32) on the Rust build."
    - "HRN-05 red is resolved by RESTORING the durable source `.planning/research/SUMMARY.md` (byte-identical to `docs/research/v0.3.0/SUMMARY.md`, the RHS of the `40-adrs-and-research.bats:88` byte-match). `.planning/research/` is a durable ALLOWED dir in the planning-clean gate (`check-planning-clean.sh:44`), so the restored source survives merge — this is the master-shippable fix, not a re-scope."
    - "HRN-06 red is resolved by adding a `sudoers mode 0440` review-checklist line to `.claude/agents/security-engineer.md` so `grep -qEi \"0440|sudoers\"` (`50-agents-and-skills.bats:71`) passes — a legitimate review-surface addition (the ADR-012 sudoers drop-in is `0440 root:root NOPASSWD`), not a test weakening."
    - "`bash tests/harness/run.sh` is fully GREEN after the HRN-05/HRN-06 fixes (no carried harness reds), and `13-reuse` is 32/32 on the Rust build across ubuntu-24.04 + almalinux-9 — the three known non-environment reds are cleared, so GATE-01 (no red / no newly-skipped) is genuinely met before the matrix is declared."
  artifacts:
    - tests/bats/13-reuse.bats
    - .planning/research/SUMMARY.md
    - .claude/agents/security-engineer.md
  key_links:
    - "The 13-reuse fix is spec-tracks-the-authoritative-schema: `schema_gen.rs:79` declares `compatibility_window: Option<String>`, schemars canonically serializes that to `type: [\"string\",\"null\"]`, and `schema_is_not_drifted` (schema_gen.rs:143-158) byte-locks the generated `schema.json` — so the schema CANNOT be hand-changed to satisfy `type==\"string\"` without failing the drift-check AND making the field required (breaking the test_only-entries-omit-it invariant at 13-reuse.bats:521-543). The only correct fix is the test expectation."
    - "HRN-05's byte-match invariant (`.planning/research/SUMMARY.md` == `docs/research/v0.3.0/SUMMARY.md`) is preserved, not dropped: the fix RE-CREATES the stripped LHS from the durable RHS. The planning-clean gate keeps `.planning/research/` (durable dir), so the restored file is not re-stripped on merge — restore beats re-scope here."
    - "All three reds are independent of the QEMU re-wire (Wave 2) and the coverage/AGT-02/declare work (Wave 3); Wave 1 must land FIRST because a red-free suite is the precondition for a meaningful full-matrix + a genuinely-green gate declaration."
---

<objective>
Wave 1 — resolve the three carried, pre-existing reds so the validation gate is
GENUINELY green (GATE-01: no red / no newly-skipped): fix the stale `13-reuse`
#29 assertion to accept the schemars nullable form (the schema is the TEST-03
source-of-truth — fix the TEST, never the schema); restore the durable
`.planning/research/SUMMARY.md` source HRN-05 byte-matches against; add the
`sudoers mode 0440` review line HRN-06 greps for in `security-engineer.md`.

Purpose: the gate cannot be declared green with 3 known reds standing. `13-reuse`
#29 gates the Docker/QEMU BEHAVIOR matrix (must fix); HRN-05/HRN-06 gate the
`tests/harness/run.sh` meta-suite (fix so the harness is green too). All three
are non-environment reds — the only ones blocking a truly-green gate.
Output: `13-reuse` 32/32 on the Rust build; `tests/harness/run.sh` fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-validation-gate/59-RESEARCH.md
@.planning/phases/59-full-validation-gate/59-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `tests/bats/13-reuse.bats` — the stale #29 `compatibility_window.type` assertion
  (line 552) updated to accept the schemars nullable union `["string","null"]`.
- `.planning/research/SUMMARY.md` — restored (durable, byte-identical to
  `docs/research/v0.3.0/SUMMARY.md`) so the HRN-05 byte-match has its LHS source.
- `.claude/agents/security-engineer.md` — a `sudoers mode 0440` review-checklist
  line added so HRN-06's `grep -qEi "0440|sudoers"` passes.
Downstream: Wave 2 (59-02) re-wires the QEMU gate at the Rust `provision` on a
red-free suite; Wave 3 (59-03) runs the coverage audit + AGT-02 + declares ready.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: fix the stale 13-reuse #29 assertion to accept the schemars nullable union (schema is TEST-03 SoT — fix the TEST, not the schema)</name>
  <files>tests/bats/13-reuse.bats</files>
  <read_first>
    - tests/bats/13-reuse.bats [:547-554] (the stale @test "REUSE-03: schema.json declares compatibility_window field"; the exact assertion at :552 — `jq -e '.["$defs"].agent.properties.compatibility_window.type == "string"'` — and its `__fail` diagnostic at :553; the surrounding tests :521-545 that ALREADY work with the nullable schema — do NOT touch them)
    - rust/crates/agentlinux-core/src/schema_gen.rs [:79] (`compatibility_window: Option<String>` — the reason schemars emits `["string","null"]`) + [:143-158] (`schema_is_not_drifted` — the drift-check that byte-locks the generated schema.json; proves the schema is the SoT and cannot be hand-edited)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"The Carried Reds — Red 1" (the root cause + the recommended fix form + Pitfall 3: changing `Option<String>`→`String` would make the field required, break test_only entries + 13-reuse.bats:521-543, and fail the drift-check — the fix is the TEST expectation)
    - plugin/catalog/schema.json (`jq '.["$defs"].agent.properties.compatibility_window'` — confirm the on-disk generated type is `["string","null"]` with `minLength:1`, so the corrected assertion matches reality)
  </read_first>
  <action>
    Update ONLY the assertion at `13-reuse.bats:552` (inside the @test
    "REUSE-03: schema.json declares compatibility_window field", :547-554) so it
    accepts the schemars-generated nullable union `["string","null"]` that the
    schema actually declares, instead of the stale bare `"string"`.

    This is a spec-tracks-the-authoritative-schema fix — DOCUMENT THAT in the
    test's comment: the schema.json `compatibility_window` type is GENERATED from
    schemars (`schema_gen.rs:79`, `Option<String>` produces the canonical nullable
    union) and BYTE-LOCKED by the `schema_is_not_drifted` drift-check
    (schema_gen.rs:143-158, enforced by the rust + cli-unit CI jobs). The schema
    is the TEST-03 source-of-truth; the bare scalar assertion predates the
    schemars generation (Phase-54-02 origin) and is stale. NEVER hand-edit
    schema.json or change the Rust type to satisfy the old assertion — that would
    make the field required, break the test_only-entries-MUST-NOT-carry-it
    invariant (13-reuse.bats:521-543), and fail the drift-check.

    Corrected assertion (accept EITHER the array-union form OR a bare scalar, so
    it stays robust if schemars' emission normalizes later) — a jq predicate that,
    given `.compatibility_window.type`, returns true when it is the array
    containing "string" OR the scalar "string":
      `jq -e '(.["$defs"].agent.properties.compatibility_window.type) as $t | ($t == ["string","null"]) or ($t == "string") or (($t | type=="array") and (any($t[]; . == "string")))'`
    Keep the existing four-line `__fail` diagnostic (:553) intact, updating only
    its expected-value string to describe the nullable form.

    Do NOT modify tests :521-545 (they already pass with the nullable schema —
    only the bare scalar check at :552 was wrong). Do NOT touch any Rust source,
    schema.json, or any other bats file.

    Comment-text discipline: no acceptance criterion below negative-greps a
    literal that this action writes into 13-reuse.bats, so no allowlist marker is
    needed. The verify greps the JSON `type` value at runtime (a schema artifact),
    not a source literal.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash -n tests/bats/13-reuse.bats && grep -qF '["string","null"]' tests/bats/13-reuse.bats && echo "NULLABLE-ACCEPTED" && for d in ubuntu-24.04 almalinux-9; do echo "===== $d 13-reuse ====="; ./tests/docker/run.sh "$d" 13-reuse 2>&1 | tail -8; done</automated>
  </verify>
  <acceptance_criteria>
    - The nullable union `["string","null"]` is accepted by the :552 assertion
      (`NULLABLE-ACCEPTED`); the stale bare-scalar equality check is gone.
    - `13-reuse` is 32/32 on the Rust build (default artifact, no override) on
      BOTH ubuntu-24.04 AND almalinux-9 — the pre-existing #29 red is cleared
      (was 31/32).
    - schema.json, `schema_gen.rs`, the Rust type, and 13-reuse tests :521-545 are
      UNTOUCHED (the fix is the stale test expectation only; the schema is the
      TEST-03 SoT).
    - The test comment documents WHY this is a spec-tracks-the-schema fix (schemars
      `Option<String>` produces the nullable union, drift-locked) — not a weakening.
  </acceptance_criteria>
  <done>
    The stale 13-reuse #29 assertion accepts the schemars nullable union; the
    schema is untouched (it is the authoritative TEST-03 SoT); `13-reuse` is 32/32
    on the Rust build on both distros. The only carried BEHAVIOR-matrix red is
    cleared.
  </done>
</task>

<task type="auto">
  <name>Task 2: resolve HRN-05 (restore the durable .planning/research/SUMMARY.md) + HRN-06 (add the sudoers-0440 review line to security-engineer.md); tests/harness/run.sh fully green</name>
  <files>.planning/research/SUMMARY.md, .claude/agents/security-engineer.md</files>
  <read_first>
    - tests/harness/40-adrs-and-research.bats [:87-93] (HRN-05: the byte-match @test at :87-89 — `diff -q .planning/research/SUMMARY.md docs/research/v0.3.0/SUMMARY.md` — the LHS `.planning/research/SUMMARY.md` is absent on this branch; and the v0.2.0 counterpart :91-93 which ALREADY passes because `.planning/milestones/v0.2.0-research/SUMMARY.md` exists — the pattern to mirror)
    - tests/harness/50-agents-and-skills.bats [:70-72] (HRN-06: `grep -qEi "0440|sudoers" .claude/agents/security-engineer.md` — the exact grep the fix must satisfy)
    - .claude/agents/security-engineer.md [:1-40] (the reviewer role's front-matter + the review-question structure the new sudoers line joins — the doc has NO 0440/sudoers mention today; the addition is a legitimate review-surface, per ADR-012's `0440 root:root NOPASSWD` sudoers drop-in the rewrite touches)
    - scripts/check-planning-clean.sh [:32-54] (the ALLOWED durable-dir list — `research` is at :44, so `.planning/research/` is KEPT on merge, NOT stripped; this is why RESTORE, not re-scope, is the master-shippable fix)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"Red 2" + §"Red 3" (the fix directions; NOTE: research Red 2 floated re-scope as an option, but the planner RESOLVES the open question in favor of RESTORE — `.planning/research/` is a durable ALLOWED dir, so the restored source survives merge and keeps the byte-match invariant intact) + .planning/phases/59-full-validation-gate/59-VALIDATION.md §"Wave 0 Requirements" (which specifies RESTORE for HRN-05)
  </read_first>
  <action>
    Resolve HRN-05 and HRN-06 so `tests/harness/run.sh` is fully green.

    HRN-05 (RESTORE the durable source):
    - Create `.planning/research/SUMMARY.md` as a BYTE-IDENTICAL copy of
      `docs/research/v0.3.0/SUMMARY.md` (the RHS of the :87-89 byte-match). Use a
      literal file copy (`cp docs/research/v0.3.0/SUMMARY.md .planning/research/SUMMARY.md`)
      so `diff -q` reports no difference. This RESTORES the byte-match invariant
      (docs mirror the planning research source); it does NOT weaken it.
    - This is the correct fix — NOT a test re-scope — because
      `check-planning-clean.sh:44` lists `research` as a durable ALLOWED
      `.planning/` dir: the restored file survives the planning-clean gate on
      merge (unlike `phases/`, which is stripped). Restoring the source keeps the
      Rust track master-shippable with the byte-match invariant honored.
    - Do NOT touch `40-adrs-and-research.bats` — the test is correct; it was
      missing its LHS source, which this restore provides.

    HRN-06 (add the sudoers-0440 review line to the DOC):
    - Add a review-checklist / review-question line to
      `.claude/agents/security-engineer.md` that mentions the sudoers drop-in
      mode `0440` (e.g. a bullet under the reviewer's privilege/permission
      surface: verify a sudoers drop-in is installed mode `0440 root:root` and
      scopes NOPASSWD narrowly — the ADR-012 `agent ALL=(ALL) NOPASSWD: ALL`
      drop-in the provisioner writes). This makes the reviewer role actually
      cover the sudoers surface the Rust provisioner touches, so
      `grep -qEi "0440|sudoers"` at 50-agents-and-skills.bats:71 passes.
    - Keep it a genuine review concern (1-3 lines), not a token string —
      HRN-06's intent is that the security reviewer covers the sudoers mode.

    Do NOT modify any `tests/harness/*.bats` file, any Rust source, or any bats
    behavior spec. This task edits exactly two files: the restored SUMMARY and the
    reviewer doc.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && test -f .planning/research/SUMMARY.md && diff -q .planning/research/SUMMARY.md docs/research/v0.3.0/SUMMARY.md && echo "HRN05-BYTE-MATCH-OK" && grep -qEi "0440|sudoers" .claude/agents/security-engineer.md && echo "HRN06-RUBRIC-OK" && echo "=== full harness suite ===" && bash tests/harness/run.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/SUMMARY.md` exists and byte-matches
      `docs/research/v0.3.0/SUMMARY.md` (`HRN05-BYTE-MATCH-OK`) — the HRN-05
      byte-match at 40-adrs-and-research.bats:87-89 passes.
    - `.claude/agents/security-engineer.md` mentions the sudoers mode `0440`
      (`HRN06-RUBRIC-OK`) — HRN-06 at 50-agents-and-skills.bats:71 passes; the
      addition is a genuine review-surface line, not a token string.
    - `bash tests/harness/run.sh` is FULLY GREEN (no carried harness reds) —
      HRN-05 and HRN-06 both resolved.
    - No `tests/harness/*.bats` file, Rust source, or bats behavior spec was
      modified — the fix is the restored source + the reviewer-doc line only.
  </acceptance_criteria>
  <done>
    HRN-05 is resolved by restoring the durable `.planning/research/SUMMARY.md`
    (byte-matches the docs copy; survives merge as a durable ALLOWED dir); HRN-06
    is resolved by the sudoers-0440 review line in security-engineer.md.
    `tests/harness/run.sh` is fully green — the last two carried non-behavior reds
    are cleared.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the generated schema (SoT) → the bats assertion | schema.json is schemars-generated + drift-locked; the test must track the schema, not vice-versa. Fixing the stale assertion (not the schema) keeps the SoT authoritative and avoids a false "make the field required" regression. |
| the durable planning source → the docs mirror | HRN-05's byte-match asserts the docs research SUMMARY mirrors the planning research source; restoring the durable LHS preserves that integrity invariant rather than deleting the check. |
| the reviewer role rubric → the sudoers surface | HRN-06 ensures the security reviewer covers the `0440 root:root NOPASSWD` sudoers drop-in the Rust provisioner writes — a real privilege-boundary review concern (ADR-012). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-01 | Tampering | a bats fix that WEAKENS a real invariant | high | mitigate | The 13-reuse fix is justified as spec-tracks-the-authoritative-schema (schemars `Option<String>` produces the nullable union; the schema is drift-locked, the TEST-03 SoT) — the assertion is corrected to match reality, and the schema/Rust type are untouched. The verify runs the REAL schema through the corrected jq predicate, so a truly-wrong type would still fail. |
| T-59-02 | Repudiation | a re-scoped HRN-05 dropping the mirror invariant | medium | mitigate | HRN-05 is fixed by RESTORING the durable source (keeping the byte-match), not by re-scoping/deleting the check; `check-planning-clean.sh:44` keeps `.planning/research/` so the restore is durable, not a merge-transient patch. |
| T-59-03 | Elevation of Privilege | sudoers drop-in mode drift going unreviewed | low | mitigate | HRN-06's rubric line makes the security reviewer cover the `0440 root:root NOPASSWD` sudoers mode — closing the review gap for the privilege surface the provisioner touches. |
| T-59-SC | Tampering | cargo/npm installs (none) | low | accept | This wave installs no packages — it edits one bats assertion, restores a docs-mirror file, and adds a reviewer-doc line. No package-legitimacy gate applies. |
</threat_model>

<verification>
- `13-reuse` is 32/32 on the Rust build on ubuntu-24.04 + almalinux-9 — the #29
  compatibility_window red is cleared by fixing the stale TEST (not the schema).
- `.planning/research/SUMMARY.md` restored (byte-matches the docs copy); HRN-05
  passes; the restore is durable (planning-clean gate keeps `research/`).
- `.claude/agents/security-engineer.md` mentions sudoers-0440; HRN-06 passes.
- `bash tests/harness/run.sh` is fully green — no carried harness reds remain.
- No schema, Rust source, or `tests/harness/*.bats` spec was changed — the only
  bats edit is the single stale-assertion fix in 13-reuse.bats (justified).
</verification>

<success_criteria>
- All three carried, non-environment reds are GREEN: `13-reuse` 32/32 on the Rust
  build (behavior matrix); HRN-05 + HRN-06 green in `tests/harness/run.sh`.
- The 13-reuse fix is documented as spec-tracks-the-authoritative-schema (the
  only bats spec edit in the phase, and it is a correction, not a weakening).
- HRN-05 is fixed by RESTORING the durable source (byte-match preserved); HRN-06
  by a genuine reviewer-doc line.
- GATE-01 (no red / no newly-skipped) is genuinely met — the precondition for a
  meaningful Wave-2 QEMU re-wire and a truly-green Wave-3 gate declaration.
</success_criteria>

<output>
Create `.planning/phases/59-full-validation-gate/59-01-SUMMARY.md` when done
</output>
