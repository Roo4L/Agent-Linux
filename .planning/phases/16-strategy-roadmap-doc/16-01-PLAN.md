---
phase: 15-strategy-roadmap-doc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/STRATEGY.md
  - .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md
autonomous: false
requirements:
  - STRATR-01
  - STRATR-02
  - STRATR-03
  - STRATR-04
  - STRATR-05
  - STRATR-06

must_haves:
  truths:
    - "REQUIREMENTS.md STRATR-02 is amended to mandate the 5-section Rumelt-style spine (`What we're solving` / `Our bets` / `Where we are now` / `What's next` / `Execution principles`) with the new grep gate requiring >= 5 matches"
    - "REQUIREMENTS.md gains a `## Superseded items` (or appends to the existing `## Superseded Items (2026-05-16 reframe)` block) note recording the STRATR-02 amendment per the 2026-05-16 Phase-14 reframe precedent"
    - "docs/STRATEGY.md exists at the committed repo path (sibling to VISION.md / STABILITY-MODEL.md / HARNESS.md) with file size <= 8192 bytes"
    - "docs/STRATEGY.md spine matches the locked 5-section list in order: `## What we're solving`, `## Our bets`, `## Where we are now`, `## What's next`, `## Execution principles`"
    - "docs/STRATEGY.md `## What's next` section contains exactly two `### Near-term` and `### Themes for v0.6+` subsections in that order"
    - "docs/STRATEGY.md `### Themes for v0.6+` lists exactly 4 themes (Security Hardening + Preset/Profile/Compat-guarded + Broader catalog + Public engagement) each with a `**Sequencing rationale:**` bold-label line"
    - "docs/STRATEGY.md `## Execution principles` lists 5-7 entries, each with `**Name** — ` prefix + one-line gloss + source citation"
    - "docs/STRATEGY.md contains a `> Last reviewed: 2026-05-19` blockquote as the first non-blank line after the H1"
    - "VIS-07 / STRATR-06 voice-rule grep returns zero matches against docs/STRATEGY.md (HARD GATE - any match fails the plan)"
    - "Public engagement theme (#4) sequencing rationale explicitly states it is gated on theme #3 (broader catalog) reaching critical mass"
    - "First usable release (per `## Where we are now`) is named as v0.3.4 Aware Installation (AL-38) + AlmaLinux support, referenced by Jira AL-38 key"
    - "16-01-EVIDENCE.md captures wc, grep, head, awk transcripts for STRATR-01..STRATR-06 plus the REQUIREMENTS.md amendment grep + Superseded-items entry, consumed verbatim by 16-AUDIT.md in Plan 16-02"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended STRATR-02 mandating the 5-section spine + new grep gate; appended Superseded-items entry recording the amendment"
      contains: "What we're solving|Our bets|Where we are now|What's next|Execution principles"
    - path: "docs/STRATEGY.md"
      provides: "Canonical strategy/roadmap document — 5-section spine, 4 themes, execution principles, voice-rule compliant"
      contains: "## What we're solving"
    - path: ".planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md"
      provides: "Pre-audit verification transcripts (wc -c, grep matches, awk counts, head output) for STRATR-01..STRATR-06 + REQUIREMENTS.md amendment; consumed verbatim by 16-AUDIT.md in Plan 16-02"
      contains: "STRATR-06"
  key_links:
    - from: "docs/STRATEGY.md"
      to: "docs/VISION.md"
      via: "Markdown link in `## What we're solving` (upstream `what we want to be`) and in `## Related` block at bottom of doc"
      pattern: "docs/VISION\\.md"
    - from: "docs/STRATEGY.md"
      to: "docs/decisions/016-agenda-redefinition.md"
      via: "Markdown link in `## Related` cross-reference block (sibling-doc shape per STABILITY-MODEL.md precedent)"
      pattern: "decisions/016-agenda-redefinition\\.md"
    - from: "docs/STRATEGY.md"
      to: "docs/decisions/002-behavior-contract-framing.md"
      via: "Citation inside `## Execution principles` — behavior-tests-as-spec principle ends with `(ADR-002)` parenthetical"
      pattern: "ADR-002|behavior-contract-framing"
    - from: "docs/STRATEGY.md"
      to: "docs/decisions/004-per-user-npm-prefix.md"
      via: "Citation inside `## Execution principles` — no `sudo npm install -g` principle ends with `(ADR-004)`"
      pattern: "ADR-004|per-user-npm-prefix"
    - from: "docs/STRATEGY.md"
      to: "docs/decisions/011-stability-first-version-pinning.md"
      via: "Citation inside `## Our bets` (curated combos bet) AND inside `## Execution principles` (curated-combo testing — TST-08)"
      pattern: "ADR-011|stability-first-version-pinning"
    - from: "docs/STRATEGY.md"
      to: "Jira AL-38"
      via: "Citation inside `## Where we are now` (current goal = first usable release) and `### Near-term` subsection"
      pattern: "AL-38"
    - from: ".planning/REQUIREMENTS.md"
      to: "docs/STRATEGY.md"
      via: "STRATR-02 spine grep references docs/STRATEGY.md as gate target"
      pattern: "docs/STRATEGY\\.md"
---

<objective>
Land the canonical product strategy/roadmap document at `docs/STRATEGY.md` — the "how we get there" companion to `docs/VISION.md`'s "what we want to be" — and amend REQUIREMENTS.md STRATR-02 in the same commit window to mandate the new Rumelt-style 5-section spine that replaces the original 4-section spine. Capture STRATR-01..06 evidence transcripts into a pre-audit `16-01-EVIDENCE.md` file that Plan 16-02 lifts verbatim into the consolidated `16-AUDIT.md` phase-close gate.

Purpose: The Phase 16 deliverable is one Markdown file at a stable path + an amended requirements spec + a captured evidence record. The reviewer pass + final consolidated audit + GREEN-gate emission belong to Plan 16-02 to keep the two concerns separated (authoring + spec amendment in Wave 1; reviewer feedback + close in Wave 2). The mid-discuss research-driven reframe — replacing the original `Where we are now` / `What we're working on next` / `Themes for` / `Execution principles` 4-section spine with the locked `What we're solving` / `Our bets` / `Where we are now` / `What's next` (with `### Near-term` + `### Themes for v0.6+` subsections) / `Execution principles` 5-section spine — IS the load-bearing change in this plan and rides in the same commit window as the doc authoring per the 2026-05-16 Phase-14 reframe precedent.

Output:
- `.planning/REQUIREMENTS.md` — amended STRATR-02 (new spine + new grep gate) + appended Superseded-items entry recording the amendment.
- `docs/STRATEGY.md` — single Markdown file, sibling to VISION.md, ≤ 8192 bytes, with locked spine + 4 themes + voice-rule compliant prose + `> Last reviewed: 2026-05-19` blockquote.
- `.planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` — captured wc, grep, head, awk transcripts for STRATR-01..06 + the REQUIREMENTS.md amendment grep; PASS verdict per requirement.
- Single commit landing all three files (one commit, per CLAUDE.md's prefer-new-commits rule and the 2026-05-16 Phase-14 precedent of "REQUIREMENTS.md amendment lands in the same window as the doc that amends it").
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-strategy-roadmap-doc/16-CONTEXT.md
@.planning/phases/15-vision-doc-and-downstream/15-CONTEXT.md
@.planning/phases/15-vision-doc-and-downstream/15-AUDIT.md
@docs/VISION.md
@docs/STABILITY-MODEL.md
@docs/HARNESS.md
@docs/decisions/002-behavior-contract-framing.md
@docs/decisions/004-per-user-npm-prefix.md
@docs/decisions/011-stability-first-version-pinning.md
@docs/decisions/016-agenda-redefinition.md
@docs/exploration/PILLAR-2-NOTES.md
@docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
@CLAUDE.md

<voice_rule>
The STRATR-06 voice-rule grep gate applies to every line of new text authored
in docs/STRATEGY.md. Same regex as VIS-07 on VISION.md (HARD GATE; zero
matches required). Command:

```
grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md
```

The blocked verbs (case-sensitive against capitalized-line-start) for
`AgentLinux` as grammatical subject: `provides`, `offers`, `ensures`,
`protects`, `defends`, `benchmarks`, `measures`, `hardens`, `isolates`,
`detects`, `prevents`. Forward-looking claims about unshipped behaviour
MUST use "we" / "our roadmap" / explicit milestone identifier ("v0.3.4",
"v0.6+") as grammatical subject.

Bypass note (delivered-fact voice is allowed): claims about behavior
already SHIPPED — the v0.2.0 → v0.3.0 plugin pivot, the v0.4.0 OSS flip,
the v0.3.0 AGT-02 zero-EACCES self-update behaviour, ADR-011 curated combos
— may use delivered-fact voice. The regex only blocks forward-looking
verbs above. When in doubt, rewrite with "we" / "our roadmap" / "v0.3.3"
/ "v0.3.4" / "v0.6+" / "AL-38" as grammatical subject.

Particularly load-bearing in this plan:
- `## What we're solving` enumerates the bug-class AgentLinux ELIMINATES
  (EACCES on `sudo npm install -g`, recursive-shim self-update breakage,
  dependency drift from un-curated `npm install -g`). Use delivered-fact
  voice ("we eliminate", "the v0.3.0 plugin eliminates") since these are
  shipped behaviours per AGT-02; do NOT use "AgentLinux prevents" /
  "AgentLinux defends" / "AgentLinux protects".
- `## Our bets` describes strategic CHOICES — past-tense + delivered-fact
  voice ("we picked", "we ship") OR forward-looking with "we" subject
  ("we will continue to"). Each bet ends with a one-line "why".
- `## Where we are now` opens with the current-goal sentence ("our current
  goal is to ship the first usable release for the maintainer = v0.3.4
  Aware Installation per AL-38 + AlmaLinux support") — "we" / "our
  roadmap" / "AL-38" voice. Delivered-fact for the just-landed sentence
  ("v0.3.0 ships ... v0.4.0 flipped to OSS").
- `### Themes for v0.6+` describes UNSHIPPED themes — every theme body
  MUST use "we" / "our roadmap" / "v0.6+" voice. NO `AgentLinux + present-
  tense verb` for theme bodies.
- `## Execution principles` describes RULES we author by — present-tense
  "we" voice ("We treat behavior tests as the spec ... ADR-002") OR
  imperative ("Behavior tests are the spec — no `sudo npm install -g`
  anywhere"). Citation parenthetical at the end of each bullet.

Defensive practice: run the STRATR-06 grep against docs/STRATEGY.md after
the file is written; capture command + empty output into 16-01-EVIDENCE.md.
Any non-empty grep output is a HARD-GATE FAIL — STOP, surface to user,
do NOT commit until rewritten.
</voice_rule>

<interfaces>
<!-- Exact REQUIREMENTS.md amendment text + STRATEGY.md spine + theme bodies the executor uses verbatim. -->

REQUIREMENTS.md STRATR-02 amendment (replaces existing STRATR-02 line at line 55):

OLD (current):
```
- [ ] **STRATR-02**: The doc's spine reflects strategy/roadmap content. At minimum: `## Where we are now` (current state of v0.3.0/v0.4.0 + recently shipped), `## What we're working on next` (near-term focus, milestone-level), `## Themes for v0.6+` (forward-looking themes with sequencing rationale), `## Execution principles` (the process-level principles cut from VISION.md). `grep -nE '^## (Where we are now|What we'\''re working on next|Themes for|Execution principles)' docs/STRATEGY.md` returns ≥ 4 matches.
```

NEW (replacement; amends in the Phase 16 commit window per 2026-05-16 Phase 15 reframe precedent):
```
- [ ] **STRATR-02**: The doc's spine reflects Rumelt-style strategy structure (diagnosis + guiding policy + state + plan + principles), in this order: `## What we're solving` (the diagnosis — narrow bug-class AgentLinux eliminates, grounded against VISION.md upstream), `## Our bets` (the guiding policy — 2-3 load-bearing strategic choices with one-line why each), `## Where we are now` (honest current state ≤ 1 paragraph; load-bearing current goal: ship first usable release per AL-38 + AlmaLinux), `## What's next` (fused near-term + v0.6+ section — `### Near-term` subsection + `### Themes for v0.6+` subsection), `## Execution principles` (process-level rules cut from VISION.md). `grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md` returns ≥ 5 matches in the prescribed order. Amendment 2026-05-19 (mid-discuss research-driven reframe): supersedes the original 4-section spine (`Where we are now` / `What we're working on next` / `Themes for` / `Execution principles`) which was diagnosed as "roadmap with execution principles bolted on" — missing the diagnosis + guiding-policy moves that distinguish strategy from roadmap per Rumelt, Cagan, Pichler.
```

REQUIREMENTS.md Superseded-items append (insert into existing `## Superseded Items (2026-05-16 reframe)` table at line 189-208 OR append a new sibling block `## Superseded Items (2026-05-19 Phase 16 spine reframe)`):

Recommended: append a NEW sibling block after the existing table (preserves audit trail of how each amendment landed):

```
## Superseded Items (2026-05-19 Phase 16 spine reframe)

The following requirement was amended in the Phase 16 commit window per the mid-discuss research-driven spine reframe (precedent: 2026-05-16 STRAT-* → VIS-* + STRATR-* reframe in Phase 15 / Plan 15-02 commit window).

| Old ID | Disposition |
|--------|-------------|
| STRATR-02 (4-section spine: `Where we are now` / `What we're working on next` / `Themes for` / `Execution principles`; ≥ 4 grep matches) | Amended in-place: STRATR-02 (5-section Rumelt-style spine: `What we're solving` / `Our bets` / `Where we are now` / `What's next` / `Execution principles`; ≥ 5 grep matches; `### Near-term` + `### Themes for v0.6+` as subsections under `## What's next`). Substance preserved (status content moves into `## Where we are now`; near-term content moves into `### Near-term` subsection; themes move into `### Themes for v0.6+` subsection); diagnosis + guiding-policy moves added (`## What we're solving` + `## Our bets`). |
```

docs/STRATEGY.md — locked spine and content per 16-CONTEXT.md `<decisions>`:

```markdown
# AgentLinux — Strategy

> Last reviewed: 2026-05-19

## What we're solving

[2-3 short paragraphs. Reference VISION.md upstream for the broad value-prop, then name the specific bug-class AgentLinux exists to eliminate. Voice: delivered-fact for shipped eliminations ("we eliminate", "the v0.3.0 plugin eliminates") — NOT "AgentLinux prevents". Bug-class items:
- EACCES errors when a user runs `sudo npm install -g <pkg>` on a stock distribution
- Recursive-shim breakage on `claude update` self-update flows where /usr/local/bin/ shims point to agent-owned binaries
- Dependency drift from un-curated `npm install -g` paths
- Inconsistent agent-friendly defaults that the user is then expected to maintain
Anchor: "VISION.md names what we want to be; this section names the bug-class we exist to eliminate so the bets that follow make sense."]

## Our bets

[2-3 load-bearing strategic choices with one-line why each. Use a bulleted list with bold label + one-paragraph body. Locked candidate set (pick 3-4):

- **Installable plugin over custom distro.** (Why: v0.2.0 was a custom Ubuntu spin; the v0.2.0 → v0.3.0 pivot 2026-04-18 traded the boot-image surface for a curl-pipe-bash plugin that meets users on their distribution. ADR-001 + VISION.md "We meet users on their distribution" principle.)
- **Behaviors as spec, not implementation.** (Why: the bats suite at tests/bats/ IS the contract — implementation can vary as long as BHV/RT/AGT/CLI/CAT/INST tests stay green. ADR-002.)
- **Curated combos over user-assembled stacks.** (Why: every release ships a `pinned_version` set tested end-to-end on the Docker + QEMU matrix; we hold the line at the last-known-good combination. ADR-011, STABILITY-MODEL.md.)
- **Infrastructure not agent product.** (Why: AgentLinux provisions the environment in which agents run; we do not build agents, prompt-injection guardrails, or model-layer defenses. VISION.md non-goal. The catalog stays narrow as a consequence.)

Voice: delivered-fact for past choices ("we picked", "we ship", "we hold"). Forward-looking with "we" subject where applicable. Each bet ends with the parenthetical citation.]

## Where we are now

[≤ 1 paragraph (max 3 short paragraphs). Content posture: NOT a product-capability status report — goal-flavored, not status-flavored. Open with the load-bearing current goal:

"Our current goal is to ship the first usable release of AgentLinux for the maintainer as canonical user. That means v0.3.4 Aware Installation Process ([AL-38](https://copiedwonder.atlassian.net/browse/AL-38)) — a brownfield-aware installer that detects existing agent user / Node.js / catalog packages, reuses what's compatible, remediates what's broken, with a consent gate for mutations — followed by AlmaLinux support, the first distro expansion past Ubuntu."

Then the just-landed sentence (delivered-fact voice; one to two sentences):

"The v0.3.0 plugin ships against Ubuntu 22.04 / 24.04 / 26.04 with the agent's zero-EACCES self-update behaviour locked by AGT-02; the v0.4.0 license flip made the project OSS-MIT on 2026-05-09. The v0.3.3 milestone — this strategy framing — closes once Phase 17's website refresh ships."

Voice: "our current goal" / "we" forward-looking; delivered-fact ("ships", "flipped", "closes") for landed items.]

## What's next

### Near-term

[Current-milestone tail + the next 1-2 milestones, ordered. Use a bulleted or numbered list with brief context per item. Locked items (in this order):

1. Finish v0.3.3 (this strategy doc + Phase 17 website refresh)
2. Ship v0.3.4 Aware Installation Process (AL-38 — brownfield-aware installer that adopts existing setups instead of refusing to touch them)
3. Add AlmaLinux support (first distro expansion past Ubuntu — Pillar 1 reach)
4. OSS funding application (parallel/meta — not blocking on engineering)

Voice: "we will" / "we ship" / explicit milestone identifier. Do NOT include "mailing-list announce" here — it is intentionally deferred to themes#4 (Public engagement) and gated on catalog critical mass.]

### Themes for v0.6+

[Exactly 4 themes (STRATR-03 ceiling). Each theme = one `### {Theme}` heading + body paragraph + a single line `**Sequencing rationale:** ...`. Themes (locked count + names):

#### Security Hardening

[Phase 14 (b) opportunistic theme. Body: capability-scoped sudoers replacing ADR-012 NOPASSWD ALL; cosign-signed catalog releases; npm provenance verification on install; bubblewrap-based per-recipe sandbox profile; iptables egress allowlist for catalog recipes. Source: docs/exploration/PILLAR-3-CANDIDATE-NOTES.md Decision summary.]
**Sequencing rationale:** Independent of the catalog-expansion / engagement track. Maturity gate: pick the first defense after the ADR-012 NOPASSWD scope is honestly understood in practice (post-AL-38 brownfield findings will surface which capabilities the agent actually needs).

#### Preset / profile framework + compat-guarded update flow

[Phase 13 differentiators. Body: `bare` / `must-haves` / `optimum` presets; `web-development`-style profiles; hold-and-wait-on-upstream-breakage policy; covers the catalog update pipeline. Source: docs/exploration/PILLAR-2-NOTES.md Decision summary.]
**Sequencing rationale:** Builds on the curated-combo + `pinned_version` foundation already in v0.3.0; the work is mechanism design + UX, not new product surface. Should land before broader catalog expansion so new agents adopt the preset/profile framework from day one.

#### Broader agentic-dev catalog

[Toward critical mass. Body: Cursor CLI, OpenAI Codex CLI, aider, Continue, Goose — each admitted via the catalog admission contract (behavior test, maintainer reputation, considered decision per VISION.md). The current 3-agent catalog (claude-code + gsd + playwright) is too small to be the basis of a public engagement push.]
**Sequencing rationale:** Gates theme #4 (Public engagement). The current release surface is too tiny to engage subscribers meaningfully — we build critical mass first.

#### Public engagement

[Mailing-list announce + feedback loop + community-platform basics. Body: low-overhead opt-in mailing list for release announcements; structured feedback collection (issue templates, contributor invite paths in CONTRIBUTING.md); community-platform basics if the catalog reaches critical mass.]
**Sequencing rationale:** Explicitly gated on theme #3 reaching critical mass. The current 3-agent release is too tiny to engage subscribers meaningfully; we need a broader catalog before coming up with any public announcement.

Voice: "we" / "our roadmap" / "v0.6+" for theme bodies. Each `**Sequencing rationale:**` line is bold-prefixed inline (NOT a nested `####` heading — flatter hierarchy, per 16-CONTEXT.md specifics).]

## Execution principles

[Bulleted list with `**Name** — ` prefix + one-line gloss + parenthetical citation. 5 entries mandated by STRATR-04 floor; up to 7 if room permits under the 8 KB ceiling. Locked entries:

- **Voice rule** — Never `AgentLinux + present-tense verb` for unshipped behaviour. Forward-looking claims use "we" / "our roadmap" / explicit milestone identifier as grammatical subject. Enforced by automated grep gate on every authored doc. (VIS-07, STRATR-06, SITE-06; PITFALLS.md aspirational drift.)
- **Behavior tests are the spec** — The bats suite at `tests/bats/` IS the contract; implementation can vary as long as BHV/RT/AGT/CLI/CAT/INST tests stay green. (ADR-002.)
- **Evidence-cite discipline** — Phase-close audits (`<phase-NN>-AUDIT.md`) cite file paths, line ranges, commit hashes, and grep transcripts per requirement. The audit IS the gate. (TST-07; Phase 15 / 15-AUDIT.md precedent.)
- **Curated-combo testing** — Every release ships a `pinned_version` set tested end-to-end by the 4-gate release pipeline (pre-commit → docker matrix → QEMU matrix → pinned-combo gate). A red combo cannot ship. (ADR-011, TST-08, STABILITY-MODEL.md.)
- **No `sudo npm install -g` anywhere** — All catalog installs run via `sudo -u agent -H npm install -g`. The whole point of the v0.3.0 plugin is to eliminate the EACCES / recursive-shim bug class; the installer enforces it. (ADR-004.)

Optional additions (left to author's discretion under the 8 KB ceiling):
- **Reviewer feedback loop** — Every substantive change to `plugin/` / `tests/` / `docs/` runs through the project-scoped review subagents (bash-engineer, qa-engineer, security-engineer, ai-deslop, etc.) before merge. (HARNESS.md §4, ADR-010.)
- **AI-agent collaboration pattern** — Coding agents operating in the repo own their own back-pressure (per-issue lifecycle, GSD reconciliation), and human review applies to AI-generated edits the same as human-generated ones. (agents/software-engineer/AGENTS.md.)

Voice: imperative or "we" subject; each bullet ends with the parenthetical source citation.]

## Related

- [docs/VISION.md](VISION.md) — the canonical "what we want to be" doc this strategy doc operationalizes.
- [ADR-016](decisions/016-agenda-redefinition.md) — the framing decision (two pillars + vision/strategy split, 2026-05-16).
- [ADR-002](decisions/002-behavior-contract-framing.md) — behavior tests are the spec.
- [ADR-004](decisions/004-per-user-npm-prefix.md) — per-user npm prefix (no `sudo npm install -g`).
- [ADR-011](decisions/011-stability-first-version-pinning.md) — stability-first version pinning.
- [docs/STABILITY-MODEL.md](STABILITY-MODEL.md) — the user companion to ADR-011; mechanizes the "curated combos" bet.
- [docs/HARNESS.md](HARNESS.md) — review feedback loop + reviewer-by-file-type matrix.
- [Jira AL-7](https://copiedwonder.atlassian.net/browse/AL-7) — v0.3.3 agenda redefinition epic.
- [Jira AL-38](https://copiedwonder.atlassian.net/browse/AL-38) — v0.3.4 Aware Installation Process; defines "first usable release."
```

Note on cross-reference density (Claude's discretion per 16-CONTEXT.md):
- Inline links inside section bodies stay light-touch — only ADR-002, ADR-004, ADR-011, ADR-016 + AL-38 get inline links in the relevant paragraph.
- The `## Related` block at the bottom carries the wider reference index (per STABILITY-MODEL.md `## Related` precedent — see lines 117-126).

Size budget (STRATR-01: ≤ 8192 bytes; target 6-7 KB per 16-CONTEXT.md):
- H1 + blockquote: ~80 bytes
- `## What we're solving`: ~700 bytes
- `## Our bets`: ~1200 bytes
- `## Where we are now`: ~700 bytes
- `## What's next` (Near-term + 4 themes): ~2500 bytes
- `## Execution principles`: ~1500 bytes
- `## Related`: ~700 bytes
- Total: ~7400 bytes (under 8 KB; room for ~700 bytes of additional prose under optional 6th/7th execution principle).

16-01-EVIDENCE.md skeleton (Plan 16-02's 16-AUDIT.md lifts blocks verbatim):

```markdown
# Phase 16 Plan 01 — Pre-Audit Evidence

> Captured: 2026-05-19
> Consumed by: `16-AUDIT.md` (authored in Plan 16-02).
> Target commit: this plan's single commit landing REQUIREMENTS.md amendment + docs/STRATEGY.md + this evidence file.

## STRATR-01 — docs/STRATEGY.md exists; size <= 8192 bytes

**Acceptance:** `wc -c docs/STRATEGY.md` returns a byte count <= 8192.

Command:
```
wc -c docs/STRATEGY.md
```

Output:
```
{N} docs/STRATEGY.md
```

Verdict: PASS (size {N} <= 8192).

## STRATR-02 — Spine: What we're solving / Our bets / Where we are now / What's next / Execution principles

**Acceptance (amended 2026-05-19):** `grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md` returns >= 5 matches in prescribed order; `grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md` returns >= 2 matches (Near-term + Themes-for-v0.6+ subsections under `## What's next`).

[Capture both grep commands + outputs; verdict.]

## STRATR-03 — `### Themes for v0.6+` lists exactly 4 themes

**Acceptance:** `awk` count of `^#### ` headings inside the `### Themes for v0.6+` subsection returns exactly 4. Each theme block ends with a `**Sequencing rationale:**` bold-label line. The Public engagement theme's sequencing rationale explicitly references the catalog critical-mass gate.

Command:
```
awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md
```

Output:
```
4
```

[Plus: grep `**Sequencing rationale:**` count >= 4; grep "critical mass" inside Public engagement section returns >= 1 match.]

## STRATR-04 — `## Execution principles` lists 5-7 entries

**Acceptance:** awk-counted top-level bullets under `## Execution principles` is in [5..7]. Each bullet uses `**Name** — ` prefix + one-line gloss + parenthetical citation.

[Capture awk count + sample grep transcripts.]

## STRATR-05 — `> Last reviewed: 2026-05-19` in top 5 lines

**Acceptance:** `head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05'` returns 1 match.

[Capture head + grep + output.]

## STRATR-06 — Voice-rule hard gate (HARD GATE; zero matches required)

**Acceptance:** `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md` returns zero matches. Command + empty output committed verbatim to 16-AUDIT.md.

Command:
```
grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"
```

Output:
```
exit=1
```
(Empty grep output + `exit=1` is the PASS shape. Any other shape is a HARD-GATE FAIL — STOP and surface to the user instead of continuing.)

Verdict: PASS.

## REQUIREMENTS.md amendment landing — STRATR-02 spine + Superseded-items entry

**Acceptance:** REQUIREMENTS.md STRATR-02 spine grep + Superseded-items entry both land in the same commit window as docs/STRATEGY.md.

Command 1:
```
grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving.*Our bets.*Where we are now.*What'\''s next.*Execution principles" .planning/REQUIREMENTS.md
```

Output 1:
```
{line}: ...
```

Command 2:
```
grep -nE '## Superseded Items \(2026-05-19' .planning/REQUIREMENTS.md
```

Output 2:
```
{line}: ## Superseded Items (2026-05-19 Phase 16 spine reframe)
```

Verdict: PASS (amendment landed; audit trail recorded).

## Aggregate gate status

STRATR-01: PASS
STRATR-02: PASS
STRATR-03: PASS
STRATR-04: PASS
STRATR-05: PASS
STRATR-06: PASS (HARD GATE)
REQUIREMENTS.md amendment: PASS

Plan 01 verdict: PASS
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend REQUIREMENTS.md STRATR-02 + append Superseded-items block</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (current state; STRATR-02 lives at line 55; existing `## Superseded Items (2026-05-16 reframe)` block lives at lines 189-208)
    - `.planning/phases/16-strategy-roadmap-doc/16-CONTEXT.md` (`<decisions>` § "STRATR-02 amendment (Phase 16 commit window)" — exact amendment text)
    - `.planning/phases/15-vision-doc-and-downstream/15-CONTEXT.md` (Phase 15 reframe precedent — how the 2026-05-16 STRAT-* → VIS-* + STRATR-* amendment landed)
  </read_first>
  <action>
Use the Edit tool. Two distinct edits in this file:

EDIT A — Replace existing STRATR-02 line at line 55 (`- [ ] **STRATR-02**:`) with the amended text from `<interfaces>` § "REQUIREMENTS.md STRATR-02 amendment". The old line currently mandates the 4-section spine (`Where we are now|What we're working on next|Themes for|Execution principles` ≥ 4 matches). The new line mandates the 5-section spine (`What we're solving|Our bets|Where we are now|What's next|Execution principles` ≥ 5 matches), names `### Near-term` + `### Themes for v0.6+` as subsections under `## What's next`, AND includes the 2026-05-19 amendment-history note ("Amendment 2026-05-19 (mid-discuss research-driven reframe): supersedes the original 4-section spine ... per Rumelt, Cagan, Pichler"). Copy the new line VERBATIM from the `<interfaces>` block — do not paraphrase.

EDIT B — Append a NEW sibling block AFTER the existing `## Superseded Items (2026-05-16 reframe)` table (which ends at line 208, just before `## Deferred Items` at line 209). Insert the block VERBATIM from `<interfaces>` § "REQUIREMENTS.md Superseded-items append" (the entire `## Superseded Items (2026-05-19 Phase 16 spine reframe)` block). The new block is a sibling to the existing 2026-05-16 block (NOT a row appended into the existing 2026-05-16 table) — this preserves the audit trail of how each amendment landed in its own commit window. The block names old STRATR-02 (4-section) as the superseded ID and amended STRATR-02 (5-section) as the disposition; substance preserved (status / near-term / themes / execution principles all retained), diagnosis + guiding-policy moves added.

Do NOT touch any other line in REQUIREMENTS.md (STRATR-01, STRATR-03..06, the existing 2026-05-16 Superseded table, traceability table, deferred-items section, etc.). The amendment is surgical.

Per CLAUDE.md prefer-new-commits rule: this edit lands in the same commit as Task 2 (STRATEGY.md authoring) + Task 3 (evidence file). Plan 15-02 / Phase 15 reframe precedent: REQUIREMENTS.md amendment + downstream doc edit + audit close all land in ONE commit at the end of the plan.
  </action>
  <verify>
    <automated>grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving" .planning/REQUIREMENTS.md && grep -nE '## Superseded Items \(2026-05-19' .planning/REQUIREMENTS.md && grep -c '## Superseded Items' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving" .planning/REQUIREMENTS.md` returns >= 1 match on the amended STRATR-02 line (one of the two distinctive strings present).
    - `grep -nE '## Superseded Items \(2026-05-19' .planning/REQUIREMENTS.md` returns exactly 1 match (the new sibling block).
    - `grep -c '## Superseded Items' .planning/REQUIREMENTS.md` returns exactly 2 (the existing 2026-05-16 block + the new 2026-05-19 block).
    - `grep -nE '^- \[ \] \*\*STRATR-' .planning/REQUIREMENTS.md` still returns 6 STRATR-XX entries (STRATR-01..06 — none accidentally deleted).
    - `grep -nE "What we'\''re working on next|Themes for v0\.6\+" .planning/REQUIREMENTS.md` against STRATR-02 line: the OLD spine pattern (`What we're working on next` ≥ 4 matches) is gone from STRATR-02 (may still appear in the Superseded-items table as documented; verify it does not appear in STRATR-02 itself).
  </acceptance_criteria>
  <done>
REQUIREMENTS.md STRATR-02 mandates the new 5-section spine + new grep gate; `## Superseded Items (2026-05-19 Phase 16 spine reframe)` block appended as a sibling to the existing 2026-05-16 block; STRATR-01 and STRATR-03..06 untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author docs/STRATEGY.md (5-section spine + 4 themes + voice-rule compliant prose)</name>
  <files>docs/STRATEGY.md</files>
  <read_first>
    - `docs/VISION.md` (upstream "what we want to be" — STRATEGY.md `## What we're solving` references; voice + structure precedent; `> Last reviewed:` blockquote convention; sibling-doc shape)
    - `docs/STABILITY-MODEL.md` (sibling-doc `## Related` block precedent at lines 117-126 — copy the cross-reference pattern; mechanizes the "curated combos" bet referenced in `## Our bets`)
    - `docs/HARNESS.md` (optional 6th Execution principle source — reviewer feedback loop §4)
    - `docs/decisions/016-agenda-redefinition.md` (ADR-016 — framing decision context; STRATEGY.md `## Related` cross-links)
    - `docs/decisions/002-behavior-contract-framing.md` (ADR-002 — behavior-tests-as-spec; cited in `## Our bets` + `## Execution principles`)
    - `docs/decisions/004-per-user-npm-prefix.md` (ADR-004 — no `sudo npm install -g`; cited in `## Execution principles`)
    - `docs/decisions/011-stability-first-version-pinning.md` (ADR-011 — curated combos / pinned_version contract; cited in `## Our bets` curated-combos bet + `## Execution principles` curated-combo testing)
    - `docs/exploration/PILLAR-2-NOTES.md` Decision summary (substance source for Theme #2 — Preset/Profile/Compat-guarded; lifted/paraphrased into theme body)
    - `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` Decision summary + Verdict (substance source for Theme #1 — Security Hardening; declined-defenses-eligible-for-v0.6+ content)
    - `.planning/phases/16-strategy-roadmap-doc/16-CONTEXT.md` (locked decisions — full spine, theme count, theme names, sequencing rationale wording, current-goal definition, "first usable release" = v0.3.4 + AlmaLinux)
    - `.planning/phases/15-vision-doc-and-downstream/15-AUDIT.md` (audit-pattern precedent + voice-rule grep transcript format)
  </read_first>
  <action>
Use the Write tool to create `docs/STRATEGY.md` from scratch. Use the FULL skeleton in `<interfaces>` § "docs/STRATEGY.md — locked spine and content" as the structural template — every H2 / H3 / H4 heading and bracketed content guide is non-negotiable per 16-CONTEXT.md decisions.

Fill in the bracketed `[...]` prose blocks with concrete content per the locked decisions:

1. H1 (`# AgentLinux — Strategy`) + blockquote (`> Last reviewed: 2026-05-19`) — copy verbatim from the skeleton; this is the first non-blank line after the H1 per STRATR-05.

2. `## What we're solving` — 2-3 short paragraphs. Reference VISION.md upstream as the "what we want to be" companion, then enumerate the four bug-class items (EACCES on `sudo npm install -g`, recursive-shim self-update breakage, dependency drift from un-curated installs, inconsistent agent-friendly defaults). Voice: delivered-fact ("we eliminate", "the v0.3.0 plugin eliminates") for shipped behaviours — NOT "AgentLinux prevents" / "AgentLinux defends" / "AgentLinux protects". This section does NOT duplicate VISION.md — VISION.md was purged of diagnosis content during Phase 15; this is the diagnosis that grounds the bets.

3. `## Our bets` — 3-4 bulleted bets, each with bold label + one-paragraph body + parenthetical citation. Use the locked candidate set from the skeleton (Installable plugin / Behaviors as spec / Curated combos / Infrastructure not agent product). Each bet ends with the parenthetical citation (ADR-001 / ADR-002 / ADR-011 / VISION.md). Voice: delivered-fact for past choices ("we picked", "we ship"); forward-looking with "we" subject where applicable.

4. `## Where we are now` — ≤ 3 short paragraphs. Goal-flavored, NOT status-report-flavored. Open with the current-goal sentence ("Our current goal is to ship the first usable release of AgentLinux for the maintainer as canonical user. That means v0.3.4 Aware Installation Process ([AL-38](https://copiedwonder.atlassian.net/browse/AL-38)) — a brownfield-aware installer..."). Then the just-landed sentence (delivered-fact voice: v0.3.0 plugin / Ubuntu 22-26 / AGT-02 zero-EACCES self-update; v0.4.0 OSS-MIT flip 2026-05-09; v0.3.3 framing closes once Phase 17 ships). Inline link to Jira AL-38 (full URL: `https://copiedwonder.atlassian.net/browse/AL-38`).

5. `## What's next` H2 header (no body content directly under it — only the two subsections). Then:
   a. `### Near-term` subsection — 4 ordered items in this order: (1) Finish v0.3.3 (this strategy doc + Phase 17 website refresh); (2) Ship v0.3.4 Aware Installation Process (AL-38); (3) Add AlmaLinux support (first distro expansion past Ubuntu); (4) OSS funding application (parallel/meta — not blocking on engineering). Each item one sentence of context. Voice: "we will" / "we ship" / explicit milestone identifier. CRITICAL: do NOT include "mailing-list announce" here — it is intentionally deferred to theme #4 per 16-CONTEXT.md `<decisions>` § "Forward-item sequencing".
   b. `### Themes for v0.6+` subsection — exactly 4 themes (STRATR-03 ceiling locked at 4). Each theme = one `#### {Theme}` heading + body paragraph + a single line `**Sequencing rationale:** ...`. Theme order: (1) Security Hardening (sources PILLAR-3-CANDIDATE-NOTES.md Decision summary — capability-scoped sudoers replacing ADR-012 NOPASSWD ALL, cosign-signed catalog releases, npm provenance verification, bubblewrap-based per-recipe sandbox, iptables egress allowlist); (2) Preset / profile framework + compat-guarded update flow (sources PILLAR-2-NOTES.md Decision summary — `bare` / `must-haves` / `optimum` presets, `web-development`-style profiles, hold-and-wait-on-upstream-breakage policy); (3) Broader agentic-dev catalog (Cursor CLI, OpenAI Codex CLI, aider, Continue, Goose); (4) Public engagement (mailing-list announce + feedback loop + community-platform basics). Sequencing rationale for theme #4 MUST explicitly state "gated on theme #3 reaching critical mass" — user-direction-locked language. Order of themes #1 and #2 is author's discretion per 16-CONTEXT.md `### Claude's Discretion`; themes #3 and #4 MUST connect explicitly via the rationale gating. Voice: "we" / "our roadmap" / "v0.6+" for theme bodies; ZERO `AgentLinux + present-tense verb` constructions.

6. `## Execution principles` — bulleted list with `**Name** — ` prefix + one-line gloss + parenthetical citation. 5 mandated entries: Voice rule (VIS-07/STRATR-06/SITE-06; PITFALLS.md aspirational drift); Behavior tests are the spec (ADR-002); Evidence-cite discipline (TST-07; Phase 15 / 15-AUDIT.md precedent); Curated-combo testing (ADR-011, TST-08, STABILITY-MODEL.md); No `sudo npm install -g` anywhere (ADR-004). Optional 6th principle if room under 8 KB ceiling: Reviewer feedback loop (HARNESS.md §4, ADR-010). Author's discretion on whether to include the 6th/7th — 16-CONTEXT.md `### Claude's Discretion` permits 5-7 floor/ceiling. Voice: imperative or "we" subject; parenthetical source citation at end of each bullet.

7. `## Related` — cross-reference block at the bottom; copy the STABILITY-MODEL.md pattern (lines 117-126). Include: docs/VISION.md, ADR-016, ADR-002, ADR-004, ADR-011, docs/STABILITY-MODEL.md, docs/HARNESS.md, Jira AL-7, Jira AL-38. Each entry one short line: bullet + Markdown link + em-dash + one-line description. Use relative paths for in-repo files (`VISION.md`, `decisions/016-agenda-redefinition.md`, etc.) — docs/STRATEGY.md lives in docs/.

Size budget: total file ≤ 8192 bytes (STRATR-01); target 6-7 KB per 16-CONTEXT.md. Run `wc -c docs/STRATEGY.md` after writing; if over 8192, trim the optional 6th/7th execution principle first, then tighten prose. If still over, escalate (do not silently cut a locked section).

Voice-rule discipline: after writing the file, run the STRATR-06 grep:
```
grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md
```
If the grep returns ANY match, the plan is in HARD-GATE FAIL state — STOP, surface to user, do not commit until the offending line is rewritten with "we" / "our roadmap" / explicit milestone identifier as grammatical subject. Capture the empty grep output (verbatim including the `exit=1` line) into 16-01-EVIDENCE.md § STRATR-06 in Task 3.
  </action>
  <verify>
    <automated>test -f docs/STRATEGY.md && wc -c docs/STRATEGY.md | awk '{print ($1 <= 8192 ? "PASS" : "FAIL"), $1}' && grep -nE '^## (What we'"'"'re solving|Our bets|Where we are now|What'"'"'s next|Execution principles)' docs/STRATEGY.md | wc -l && grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md | wc -l && grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "voice-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/STRATEGY.md` exists.
    - `wc -c docs/STRATEGY.md` returns a byte count <= 8192.
    - `grep -nE '^## (What we'"'"'re solving|Our bets|Where we are now|What'"'"'s next|Execution principles)' docs/STRATEGY.md` returns exactly 5 lines, in this order: `## What we're solving`, `## Our bets`, `## Where we are now`, `## What's next`, `## Execution principles`.
    - `grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md` returns exactly 2 lines: `### Near-term` then `### Themes for v0.6+`.
    - `awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md` returns exactly 4.
    - `grep -c '\*\*Sequencing rationale:\*\*' docs/STRATEGY.md` returns at least 4.
    - `awk '/^## Execution principles/{flag=1;next} /^## /{flag=0} flag && /^- \*\*/{c++} END{print c}' docs/STRATEGY.md` returns a value in [5..7].
    - `head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05-19'` returns 1 match.
    - `grep -c 'docs/VISION.md\|VISION.md' docs/STRATEGY.md` returns at least 2 (one inline in `## What we're solving`, one in `## Related`).
    - `grep -c 'AL-38' docs/STRATEGY.md` returns at least 2 (one in `## Where we are now` current-goal sentence, one in `### Near-term`).
    - `grep -E 'critical mass' docs/STRATEGY.md` returns at least 1 match inside the Public engagement theme section.
    - `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md` returns ZERO matches (HARD GATE per STRATR-06).
    - `grep -E '^## Related' docs/STRATEGY.md` returns 1 match.
  </acceptance_criteria>
  <done>
docs/STRATEGY.md authored at the canonical repo path with the locked 5-section spine, exactly 4 themes (with Public engagement theme #4 explicitly gated on theme #3 critical mass), 5-7 execution principles, the `> Last reviewed: 2026-05-19` blockquote, the `## Related` cross-reference block, and ZERO voice-rule grep matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 16-01-EVIDENCE.md with STRATR-01..06 transcripts + REQUIREMENTS.md amendment evidence</name>
  <files>.planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md</files>
  <read_first>
    - `docs/STRATEGY.md` (just-authored from Task 2; the file under evidence capture)
    - `.planning/REQUIREMENTS.md` (just-amended from Task 1; the amendment + Superseded-items entry are the second evidence target)
    - `.planning/phases/15-vision-doc-and-downstream/15-01-EVIDENCE.md` (precedent shape — section per requirement; Command/Output/Verdict triplet; aggregate gate status at bottom)
    - `.planning/phases/16-strategy-roadmap-doc/16-01-PLAN.md` § `<interfaces>` § "16-01-EVIDENCE.md skeleton" (this plan's own evidence-file template)
  </read_first>
  <action>
Use the Write tool to create `.planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` from scratch. Lift the skeleton from `<interfaces>` § "16-01-EVIDENCE.md skeleton" and fill in the bracketed `{...}` and `[...]` placeholders by RUNNING the commands shown in the skeleton against the just-authored docs/STRATEGY.md + .planning/REQUIREMENTS.md and pasting the verbatim output.

Run each verification command sequentially via Bash and capture exact stdout (no paraphrasing):

1. STRATR-01: `wc -c docs/STRATEGY.md` — capture byte count; verdict PASS if <= 8192.
2. STRATR-02a: `grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md` — capture all 5 matching lines.
3. STRATR-02b: `grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md` — capture both subsection lines.
4. STRATR-03a: `awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md` — capture count (must equal 4).
5. STRATR-03b: `grep -c '\*\*Sequencing rationale:\*\*' docs/STRATEGY.md` — capture count (must be >= 4).
6. STRATR-03c: `awk '/^#### Public engagement/{flag=1;next} /^#### /{flag=0} /^### /{flag=0} flag && /critical mass/{c++} END{print c}' docs/STRATEGY.md` — capture count (must be >= 1; proves Public engagement theme #4 sequencing rationale references critical-mass gating).
7. STRATR-04: `awk '/^## Execution principles/{flag=1;next} /^## /{flag=0} flag && /^- \*\*/{c++} END{print c}' docs/STRATEGY.md` — capture count (must be in [5..7]).
8. STRATR-05: `head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05-19'` — capture single matching line.
9. STRATR-06 (HARD GATE): `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"` — capture empty grep output + `exit=1`. Any other shape is a HARD-GATE FAIL — STOP and surface to user; do not commit until Task 2 is rewritten.
10. REQUIREMENTS.md amendment grep: `grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving" .planning/REQUIREMENTS.md` — capture match line(s) proving the amendment landed.
11. REQUIREMENTS.md Superseded-items grep: `grep -nE '## Superseded Items \(2026-05-19' .planning/REQUIREMENTS.md` — capture the new sibling block heading line.
12. REQUIREMENTS.md Superseded-items block count: `grep -c '## Superseded Items' .planning/REQUIREMENTS.md` — capture count (must equal 2: the existing 2026-05-16 block + the new 2026-05-19 block).

For each requirement, format as:

```
## STRATR-NN — <one-line acceptance summary>

**Acceptance:** <verbatim from REQUIREMENTS.md or the 16-CONTEXT.md decision text>

Command:
```
<exact command>
```

Output:
```
<verbatim stdout, line-by-line>
```

Verdict: PASS (<one-line reason>).
```

End the file with an `## Aggregate gate status` block listing each requirement + verdict, plus an overall "Plan 01 verdict: PASS" line. This mirrors the 15-01-EVIDENCE.md precedent.

CRITICAL: 16-AUDIT.md (Plan 16-02) lifts these blocks VERBATIM — the formatting matters. Use triple-backtick code fences, exact command echo, exact stdout. Do NOT paraphrase.

After writing, run the same commands one more time and diff against the captured outputs to confirm no drift (the file should reflect the exact state at the moment of capture).

Once 16-01-EVIDENCE.md is complete with all PASS verdicts, this Task closes; the executor commits all three files (REQUIREMENTS.md + docs/STRATEGY.md + 16-01-EVIDENCE.md) in a SINGLE commit with message:

```
docs(15-01): land docs/STRATEGY.md + amend REQUIREMENTS.md STRATR-02

Phase 16 Plan 01: author docs/STRATEGY.md with the locked 5-section Rumelt-
style spine (What we're solving / Our bets / Where we are now / What's next
/ Execution principles), 4 themes (Security Hardening + Preset/Profile +
Broader catalog + Public engagement gated on critical mass), 5-7 execution
principles. Amend REQUIREMENTS.md STRATR-02 to mandate the new spine (>= 5
grep matches replaces old >= 4) and append the 2026-05-19 Superseded Items
block per the 2026-05-16 Phase-14 reframe precedent. Captured STRATR-01..06
evidence transcripts (incl. STRATR-06 voice-rule HARD GATE empty grep) in
16-01-EVIDENCE.md; Plan 16-02 will lift verbatim into 16-AUDIT.md.

Co-Authored-By: Claude <noreply@anthropic.com>
```

(Per CLAUDE.md: prefer-new-commits rule; co-author trailer; explicit subject area in parens.)
  </action>
  <verify>
    <automated>test -f .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md && grep -cE '^## (STRATR-0[1-6]|Aggregate gate status|REQUIREMENTS\.md)' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md && grep -c 'Verdict: PASS' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md && grep -c 'HARD GATE' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` exists.
    - `grep -cE '^## (STRATR-0[1-6]|Aggregate gate status|REQUIREMENTS\.md)' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` returns at least 8 (STRATR-01..06 = 6 sections + REQUIREMENTS.md amendment section + Aggregate gate status).
    - `grep -c 'Verdict: PASS' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` returns at least 7 (one PASS verdict per requirement section + aggregate PASS).
    - `grep -c 'HARD GATE' .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` returns at least 1 (STRATR-06 voice-rule gate annotated as HARD GATE).
    - The STRATR-06 section contains an empty grep output line followed by `exit=1` (the locked PASS shape per 15-01-EVIDENCE.md precedent).
    - Aggregate gate status section lists STRATR-01..06 each with PASS verdict + REQUIREMENTS.md amendment with PASS verdict + overall "Plan 01 verdict: PASS".
    - Single commit landed all three files (REQUIREMENTS.md + docs/STRATEGY.md + 16-01-EVIDENCE.md) with the specified message; `git log -1 --name-only` shows all three paths.
  </acceptance_criteria>
  <done>
16-01-EVIDENCE.md captures verbatim transcripts for STRATR-01..06 + REQUIREMENTS.md amendment, all with PASS verdicts. STRATR-06 voice-rule HARD GATE shows the empty-grep + exit=1 shape. Single commit lands REQUIREMENTS.md + docs/STRATEGY.md + 16-01-EVIDENCE.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → repo (committed Markdown) | Documentation prose authored locally and committed to the public repo. No untrusted input. |
| repo → reader (rendered Markdown on GitHub/website) | Static Markdown rendered by GitHub's renderer; no JS, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I (Information disclosure) | docs/STRATEGY.md prose | accept | Strategy doc is intentionally public (sibling to VISION.md and STABILITY-MODEL.md, both already public on the public repo + agentlinux.org). Doc-only / OSS-flipped 2026-05-09; no secrets or PII exposed. |
| T-15-02 | T (Tampering — supply-chain) | docs/STRATEGY.md content drift | mitigate | Voice-rule grep gate (STRATR-06) + reviewer pass in Plan 16-02 + REQUIREMENTS.md amendment audit trail (Superseded-items block) make any silent edit visible at the next phase-close audit. The audit IS the gate. |
| T-15-03 | T (Tampering — REQUIREMENTS.md spec drift) | .planning/REQUIREMENTS.md STRATR-02 amendment | mitigate | The amendment lands in a SINGLE commit with STRATEGY.md + evidence file (atomicity guarantees the doc and the spec it satisfies move together); Superseded-items block preserves the audit trail of how the amendment evolved. |
| T-15-04 | R (Repudiation — "Where did this 5-section spine come from?") | 16-CONTEXT.md decisions section | accept | 16-CONTEXT.md `<decisions>` section locks the spine reframe with date (2026-05-19) + rationale (research-driven; Rumelt/Cagan/Pichler); 16-DISCUSSION-LOG.md captures the user-facing reasoning. Both files commit alongside the plan and stay in `.planning/` as audit trail. |
| T-15-05 | D (Denial of service — broken link rot) | Jira AL-7 / AL-38 URLs, Anthropic CDN refs (if any), VISION.md anchors | accept | Listed in 16-CONTEXT.md `## Deferred Ideas` as a known not-addressed-in-Phase-15 concern; doc is mutable so rot can be fixed in-place. No mitigation in Phase 16 scope. |
| T-15-06 | E (Elevation of privilege) | n/a — doc-only | accept | No code, no runtime surface, no privilege flows changed. Phase 16 is pure prose authoring. |
</threat_model>

<verification>
Overall Plan 16-01 verification (run before declaring the plan complete):

1. **STRATR-01 size gate.** `wc -c docs/STRATEGY.md` returns a byte count <= 8192. If over: tighten prose (optional 6th/7th execution principle is the first thing to drop) or escalate.

2. **STRATR-02 spine + subsections grep.** `grep -nE '^## (What we'\''re solving|Our bets|Where we are now|What'\''s next|Execution principles)' docs/STRATEGY.md` returns exactly 5 matches in the prescribed order. `grep -nE '^### (Near-term|Themes for v0\.6\+)' docs/STRATEGY.md` returns exactly 2.

3. **STRATR-03 theme count.** `awk '/^### Themes for v0\.6\+/{flag=1;next} /^## /{flag=0} /^### /{if (flag) flag=0} flag && /^#### /{c++} END{print c}' docs/STRATEGY.md` returns exactly 4. Each theme has a `**Sequencing rationale:**` bold-label line. Public engagement theme references critical-mass gating.

4. **STRATR-04 execution principles count.** `awk '/^## Execution principles/{flag=1;next} /^## /{flag=0} flag && /^- \*\*/{c++} END{print c}' docs/STRATEGY.md` returns a value in [5..7].

5. **STRATR-05 Last reviewed header.** `head -5 docs/STRATEGY.md | grep -E '^> Last reviewed: 2026-05-19'` returns 1 match.

6. **STRATR-06 voice-rule HARD GATE.** `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/STRATEGY.md ; echo "exit=$?"` returns empty grep output + `exit=1`. Any other shape STOPS the plan — no commit until rewritten.

7. **REQUIREMENTS.md amendment landed.** `grep -nE "STRATR-02.*Rumelt-style|What we'\''re solving" .planning/REQUIREMENTS.md` returns at least 1 match on the amended STRATR-02 line. `grep -c '## Superseded Items' .planning/REQUIREMENTS.md` returns exactly 2.

8. **Evidence file complete.** `.planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md` exists with STRATR-01..06 + REQUIREMENTS.md amendment sections, each with a Verdict: PASS line, plus an Aggregate gate status block ending in "Plan 01 verdict: PASS".

9. **Single-commit atomicity.** `git log -1 --name-only` shows all three modified files (.planning/REQUIREMENTS.md, docs/STRATEGY.md, .planning/phases/16-strategy-roadmap-doc/16-01-EVIDENCE.md) in one commit.
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md STRATR-02 mandates the 5-section Rumelt-style spine + new ≥ 5 grep gate.
- [ ] REQUIREMENTS.md `## Superseded Items (2026-05-19 Phase 16 spine reframe)` block appended as a sibling to the existing 2026-05-16 block.
- [ ] docs/STRATEGY.md exists at the committed repo path with file size ≤ 8192 bytes.
- [ ] docs/STRATEGY.md spine matches the locked 5-section list in order: `## What we're solving`, `## Our bets`, `## Where we are now`, `## What's next`, `## Execution principles`.
- [ ] docs/STRATEGY.md `## What's next` carries exactly `### Near-term` + `### Themes for v0.6+` subsections.
- [ ] docs/STRATEGY.md `### Themes for v0.6+` contains exactly 4 themes; theme #4 (Public engagement) sequencing rationale explicitly states it is gated on theme #3 (Broader catalog) reaching critical mass.
- [ ] docs/STRATEGY.md `## Execution principles` contains 5-7 entries; voice rule + behavior-tests-as-spec + evidence-cite + curated-combo testing + no `sudo npm install -g` are all present with source citations.
- [ ] docs/STRATEGY.md `> Last reviewed: 2026-05-19` blockquote present as first non-blank line after H1.
- [ ] docs/STRATEGY.md `## Related` block at bottom with at least 8 cross-reference entries (VISION.md, ADR-016, ADR-002, ADR-004, ADR-011, STABILITY-MODEL.md, HARNESS.md, AL-38).
- [ ] STRATR-06 voice-rule grep returns ZERO matches against docs/STRATEGY.md (HARD GATE PASS).
- [ ] 16-01-EVIDENCE.md captures verbatim transcripts for STRATR-01..06 + REQUIREMENTS.md amendment, each with Verdict: PASS, plus Aggregate gate status ending "Plan 01 verdict: PASS".
- [ ] Single commit lands REQUIREMENTS.md + docs/STRATEGY.md + 16-01-EVIDENCE.md with the specified message.
</success_criteria>

<output>
After completion, create `.planning/phases/16-strategy-roadmap-doc/16-01-SUMMARY.md` per `@.claude/get-shit-done/templates/summary.md` with:
- Per-task commit hash (single commit covers all three tasks; reference it for each).
- Deviations section — note any voice-rule near-misses that required rewriting, any size-budget pressure that required dropping the optional 6th/7th execution principle, any theme-order judgment calls (themes #1 vs #2 ordering — author's discretion per 16-CONTEXT.md).
- Self-Check: PASSED with the STRATR-01..06 transcript pointers (line references inside 16-01-EVIDENCE.md).
- Hand-off note for Plan 16-02: 16-01-EVIDENCE.md is ready for verbatim lift into 16-AUDIT.md; reviewer-pass target files = docs/STRATEGY.md only (REQUIREMENTS.md amendment is a spec edit, reviewed at audit time not at draft time per the 15-AUDIT.md precedent).
</output>
