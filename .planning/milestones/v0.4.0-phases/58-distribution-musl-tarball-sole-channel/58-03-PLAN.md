---
phase: 58-distribution-musl-tarball-sole-channel
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
autonomous: true
files_modified:
  - tests/docker/run.sh
  - tests/bats/61-no-node-prereq.bats
requirements: [GATE-05, GATE-01, DIST-01]
must_haves:
  truths:
    - "The forward harness flags `AGENTLINUX_STAGE_RUST_CLI` / `AGENTLINUX_PROVISION_RUST` are FOLDED INTO THE DEFAULT: with the musl bin now the shipped + staged artifact (Wave 2), `run.sh` runs the Rust provisioner + stages the Rust CLI by default (no override needed); the two forward-flag override blocks (run.sh:289-308 provision seam + run.sh:375-398 symlink-relocate seam) are removed or collapsed so the bats exercise Rust with NO flag — exactly GATE-01's intent for this phase."
    - "ONE inverse rollback lever `AGENTLINUX_LEGACY_TS=1` is added to `run.sh`: when set, it restores the Bash+TS distribution end-to-end (build+splice the TS bundle, exec the Bash `plugin/bin/agentlinux-install` entrypoint, stage `dist/index.js`) so a broken Phase-58 reverts to the shippable Bash+TS build (GATE-05). It fails LOUD if the TS bundle is absent (mirrors the run.sh:292-295/:377-379 fail-loud discipline) — never a false-green on the musl bin."
    - "A new negative-assertion bats `61-no-node-prereq.bats` proves DIST-01's central claim: on a host with NO node/npm/pnpm present at install-start, the musl `agentlinux provision` runs (reaches + executes) BEFORE `nodejs.rs` installs Node — the install log shows no `node`/`npm`/`pnpm` invocation before the bin runs, and the staged bin is statically linked (`ldd`/`readelf` — 'not a dynamic executable'). Mirrors the INST-05 negative-log-assertion pattern (10-installer.bats:142-171)."
    - "The full installer + registry bats closeout is GREEN on the Rust build (default artifact) on BOTH ubuntu-24.04 AND almalinux-9: `10-installer`, `60-curl-installer`, `61-no-node-prereq`, `23-install-user`, `40-registry-cli`, `13-reuse` (modulo the pre-existing #29) — no newly-red / no newly-skipped; and the `AGENTLINUX_LEGACY_TS=1` rollback smoke restores Bash+TS green (GATE-05)."
  artifacts:
    - tests/docker/run.sh
    - tests/bats/61-no-node-prereq.bats
  key_links:
    - "The flag fold depends on Wave 2 having made the musl bin the default staged artifact — once `registry_cli.rs` stages the bin by default, the `AGENTLINUX_STAGE_RUST_CLI` symlink-relocate override is redundant and the `AGENTLINUX_PROVISION_RUST` provision override becomes the default path."
    - "`AGENTLINUX_LEGACY_TS=1` is the GATE-05 per-phase rollback lever; it keeps the retained `plugin/cli/` oracle + the retained `plugin/bin/agentlinux-install` Bash entrypoint EXECUTABLE end-to-end so a Phase-58 regression is diagnosable by flipping one env var (and master's hotfix path stays reachable until the Phase-59 cutover)."
    - "`61-no-node-prereq.bats` is the DIST-01 'no Node prerequisite for the CLI/provisioner itself' proof; the recipes still get Node via `nodejs.rs` (the irreducible boundary — 50-agents.bats, unchanged)."
---

<objective>
Wave 3 — fold the forward harness flags into the DEFAULT (Rust IS the artifact
now), add the single `AGENTLINUX_LEGACY_TS=1` inverse rollback lever (GATE-05),
add the `61-no-node-prereq.bats` negative-assertion proving DIST-01's central
no-Node claim, and do the full installer-bats closeout on the Rust build across
both distros.

Purpose: make the bats exercise Rust with NO override (GATE-01), give a broken
Phase-58 a clean one-flag rollback to the shippable Bash+TS build (GATE-05), and
prove the CLI/provisioner needs no Node (DIST-01).
Output: a flag-folded `run.sh` with one inverse `AGENTLINUX_LEGACY_TS=1` lever,
a new `61-no-node-prereq.bats`, and a green full installer+registry closeout on
the Rust build on ubuntu-24.04 + almalinux-9.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-VALIDATION.md
@.planning/phases/58-distribution-musl-tarball-sole-channel/58-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `tests/docker/run.sh` — the forward flags (`AGENTLINUX_STAGE_RUST_CLI`/`AGENTLINUX_PROVISION_RUST`) folded into the default Rust path; ONE inverse `AGENTLINUX_LEGACY_TS=1` lever added (restores Bash+TS end-to-end, fail-loud on a missing bundle); the flag-docs block (run.sh:38-60) updated.
- `tests/bats/61-no-node-prereq.bats` — the DIST-01 no-Node negative-assertion (no node/npm/pnpm before the bin runs + static-link check), mirroring the INST-05 log-grep pattern.
Downstream: none in this phase; Phase 59 runs the full 4-distro Docker + QEMU release gate + AGT-02 self-update against the live CDN (deferred).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: fold the forward flags into the default + add the AGENTLINUX_LEGACY_TS=1 inverse rollback lever in run.sh (GATE-05); default bats run exercises Rust with no override</name>
  <files>tests/docker/run.sh</files>
  <read_first>
    - tests/docker/run.sh (READ IN FULL the flag surface — the flag-docs block [:38-60]; the `AGENTLINUX_PROVISION_RUST` provision seam [:289-308] with its fail-loud guard [:292-295]; the else-branch Bash entrypoint `docker exec … bash /opt/agentlinux-src/plugin/bin/agentlinux-install` [:309-311]; the RUST-03 reuse-path staging [:313-338]; the `AGENTLINUX_STAGE_RUST_CLI` symlink-relocate seam [:342-398] with its fail-loud guard [:377-379]; the `host_build_musl` def [:251-275] + `HOST_MUSL_BIN`; `RUST_PROVISION_BIN_IN_CONTAINER` [:249])
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md §"The Rollback Lever (GATE-05)" (fold the two forward flags into the default; add ONE inverse `AGENTLINUX_LEGACY_TS=1`; the rationale + the fail-loud-transfer requirement; the alternative-zero-new-flags is rejected — GATE-05 wants a LIVE per-phase lever) + §"The TS-source question" (`plugin/cli/` STAYS; the Bash entrypoint STAYS — both are the rollback substrate)
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-02-PLAN.md (Wave 2 made the musl bin the DEFAULT staged artifact via registry_cli.rs + install.sh — so the provision seam + the symlink-relocate override are now the default, not flag-gated)
  </read_first>
  <action>
    Invert the harness's default: Rust is the shipped artifact, so run.sh must
    run the Rust provisioner + stage the Rust CLI BY DEFAULT, with a single
    inverse lever restoring Bash+TS.

    Fold the forward flags:
    - The provision seam [:289-311]: make the Rust `provision` invocation the
      DEFAULT (drop the `if [[ -n ${AGENTLINUX_PROVISION_RUST:-} ]]` gate so the
      Rust `provision` runs unconditionally in the default path). Retain the
      fail-loud guard [:292-295] (a missing/broken musl bin ABORTS — never falls
      back silently). The Bash-entrypoint else-branch [:309-311] moves under the
      NEW inverse lever below.
    - The symlink-relocate seam [:342-398]: with Wave 2's `registry_cli.rs`
      staging the musl bin as the default `agentlinux` symlink, the
      `AGENTLINUX_STAGE_RUST_CLI` post-hoc relocate/override is redundant —
      remove or collapse that block. (If the RUST-03 reuse-path .local/bin
      staging [:313-338] is still needed for 13-reuse's reuse-decision path,
      RETAIN only that, and drop the flag-gated relocate that existed solely to
      un-shadow the canonical symlink under the old TS-default.)

    Add the ONE inverse lever `AGENTLINUX_LEGACY_TS=1`:
    - When set: restore the Bash+TS distribution end-to-end — build/splice the TS
      bundle (the existing pnpm/dist splice path the harness used pre-swap),
      exec the Bash `plugin/bin/agentlinux-install` entrypoint (the [:309-311]
      else-branch), and let `registry_cli`'s legacy TS staging apply (or stage
      `dist/index.js` via the harness as before). This is the GATE-05 rollback:
      a broken Phase-58 reverts to the shippable Bash+TS build with one env var.
    - Fail LOUD: if `AGENTLINUX_LEGACY_TS=1` is set but the TS bundle
      (`plugin/cli/dist/index.js` / `node_modules`) is absent, ABORT non-zero
      (mirror the run.sh:292-295/:377-379 discipline) — never false-green on the
      musl bin.
    - Update the flag-docs block [:38-60]: remove the two forward-flag entries,
      add the `AGENTLINUX_LEGACY_TS=1` entry describing the Bash+TS rollback +
      its fail-loud semantics.

    Keep `plugin/cli/` (the TS oracle) + `plugin/bin/agentlinux-install` (the
    Bash entrypoint) intact — they are the rollback substrate, deleted only at
    the Phase-59 cutover. `agentlinux-core` + the bats specs are NOT touched by
    this task (harness-only change).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash -n tests/docker/run.sh && grep -q 'AGENTLINUX_LEGACY_TS' tests/docker/run.sh && echo "LEGACY-LEVER-PRESENT" && ! grep -qE 'if \[\[ -n \$\{AGENTLINUX_PROVISION_RUST' tests/docker/run.sh && echo "FORWARD-FLAG-FOLDED" && echo "=== default Rust 10-installer ===" && ./tests/docker/run.sh ubuntu-24.04 10-installer 2>&1 | tail -6 && echo "=== rollback Bash+TS smoke ===" && AGENTLINUX_LEGACY_TS=1 ./tests/docker/run.sh ubuntu-24.04 10-installer 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTLINUX_LEGACY_TS` exists in run.sh (`LEGACY-LEVER-PRESENT`); the forward `AGENTLINUX_PROVISION_RUST` gate is folded (no `if [[ -n ${AGENTLINUX_PROVISION_RUST` conditional — `FORWARD-FLAG-FOLDED`).
    - Default run (no flag): `10-installer` is GREEN on the Rust build — the bats exercise Rust with NO override (GATE-01 intent).
    - Rollback run (`AGENTLINUX_LEGACY_TS=1`): `10-installer` is GREEN on the restored Bash+TS distribution (the Bash entrypoint execs, `dist/index.js` stages) — the GATE-05 per-phase rollback lever works end-to-end.
    - The rollback lever fails LOUD if the TS bundle is absent (spot-check: temporarily hide `plugin/cli/dist` → the run aborts non-zero, never false-green on the musl bin) — or the fail-loud guard is present in the added block.
    - The flag-docs block [:38-60] no longer lists the two forward flags and documents `AGENTLINUX_LEGACY_TS=1` + its fail-loud semantics.
    - `plugin/cli/` + `plugin/bin/agentlinux-install` are intact (the rollback substrate — not deleted this phase); `agentlinux-core` + bats specs untouched by this task.
  </acceptance_criteria>
  <done>
    The forward flags are folded into the default (bats exercise Rust with no
    override); one inverse `AGENTLINUX_LEGACY_TS=1` lever restores the Bash+TS
    distribution end-to-end (fail-loud on a missing bundle); default + rollback
    `10-installer` both green. GATE-05's live per-phase rollback lever is in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: 61-no-node-prereq.bats (DIST-01 no-Node negative-assertion) + full installer/registry bats closeout green on the Rust build on both distros</name>
  <files>tests/bats/61-no-node-prereq.bats</files>
  <read_first>
    - tests/bats/10-installer.bats [:142-171] (the INST-05 negative-log-assertion PATTERN to mirror — `grep`-the-transcript for a bad string, `__fail` on presence; the `$LOG`/transcript plumbing; the four-line `__fail` diagnostic contract)
    - tests/bats/helpers/assertions (the `__fail` + `assert_exit_zero` + `assert_no_eacces` helpers `61-no-node-prereq.bats` will `load`)
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-RESEARCH.md §"No-Node-Prereq Proof" (the 4-part proof: static-link RUST-01; the installer reaches+runs provision before Node exists; the recommended explicit assertion — a host with Node absent at install-start running `<bin> provision` + asserting green up to nodejs.rs installing Node; the log-negative-assertion mirrors INST-05) + §"Runtime State Inventory" (nodejs.rs is provisioner step 30 — runs INSIDE provision, so the bin executes before Node)
    - tests/docker/run.sh §host_build_musl [:251-275] + the provision seam (now default after Task 1) — the bin path + invocation the assertion runs against
    - .planning/phases/58-distribution-musl-tarball-sole-channel/58-VALIDATION.md §"Wave 0 Gaps" (the new negative-assertion bats: no node/npm before the musl bin runs — covers DIST-01's no-Node claim) + §"Per-Task Verification Map" row 58-01-02
  </read_first>
  <action>
    Create `tests/bats/61-no-node-prereq.bats` proving DIST-01's central claim —
    the CLI/provisioner needs no Node. Every `@test` name starts with `INST-08:`
    (or the next free INST-* id) so the TST-07 req-ID grep gate passes; failures
    route through `__fail` for the four-line TST-04 diagnostic.

    Assertions (mirror the INST-05 log-grep pattern, 10-installer.bats:142-171):
    1. Static-link proof: the staged `agentlinux` bin (resolve via `readlink`
       the `~/.npm-global/bin/agentlinux` symlink, or the
       `/opt/agentlinux/cli/<ver>/bin/agentlinux` path) reports "not a dynamic
       executable" under `ldd` (or `readelf -d` shows no NEEDED entries). This is
       the RUST-01 compile-time guarantee re-asserted at the staged artifact.
    2. No-Node-before-the-bin (the runtime proof): assert the install transcript
       (`/var/log/agentlinux-install.log` honoring `$AGENTLINUX_LOG`, the log.rs
       tee) shows the provisioner banner / a `provision`-step marker BEFORE any
       `node`/`npm`/`pnpm` invocation — i.e. the musl bin is already executing
       before `nodejs.rs` (step 30) installs Node. Concretely: negative-assert
       that no `node`/`npm`/`pnpm` command runs before the provisioner's first
       step marker in the log; positive-assert `nodejs.rs`'s Node install appears
       LATER (Node is provisioned FOR the recipes, correctly). If the transcript
       does not carry a pre-Node ordering marker, the minimal proof is: on a base
       image, the bin runs `provision` and reaches step 30 — assert the provision
       exits through nodejs.rs (Node present AFTER, absent-or-unused BEFORE).
    3. Recipes-still-get-Node (the irreducible boundary holds): a light assertion
       that `node --version` succeeds AFTER provision (nodejs.rs installed it for
       the recipes) — proving DIST-01 is scoped to "the CLI/provisioner itself",
       not the recipes. (Or defer this to the existing 50-agents.bats, which
       already proves the recipes get Node — reference it rather than duplicate.)

    Add `61-no-node-prereq` to whatever per-file bats manifest the harness reads
    (if run.sh enumerates files, ensure the new file is picked up). Keep the test
    Docker-runnable (no systemd/cron six-mode iteration — a plain provision +
    log-grep + ldd, all Docker-safe).

    Comment-text discipline: the negative-log-assertion greps for `node`/`npm`/
    `pnpm` in the TRANSCRIPT (a runtime artifact), not for a literal that an
    acceptance criterion negative-greps in a source file — no allowlist marker
    needed. Positive strings (`not a dynamic executable`, the provisioner banner)
    are presence-asserts.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bats --version >/dev/null 2>&1 && grep -c '^@test' tests/bats/61-no-node-prereq.bats && grep -q 'INST-0' tests/bats/61-no-node-prereq.bats && echo "REQ-ID-OK" && for d in ubuntu-24.04 almalinux-9; do echo "===== $d ====="; for f in 61-no-node-prereq 10-installer 60-curl-installer 23-install-user 40-registry-cli; do echo "--- $f ---"; ./tests/docker/run.sh $d $f 2>&1 | tail -5; done; done</automated>
  </verify>
  <acceptance_criteria>
    - `61-no-node-prereq.bats` exists, every `@test` carries an `INST-*` req-ID (`REQ-ID-OK`), and it is GREEN on the Rust build on BOTH ubuntu-24.04 AND almalinux-9.
    - The static-link assertion holds: the staged `agentlinux` bin is "not a dynamic executable" (`ldd`) / has no NEEDED dynamic libs (`readelf -d`).
    - The no-Node-before-the-bin assertion holds: no `node`/`npm`/`pnpm` runs before the musl bin's provision reaches `nodejs.rs` (the transcript log-grep is negative pre-Node, positive at step 30).
    - The full installer + registry closeout is GREEN on the Rust build (default artifact, no override) on both distros: `10-installer`, `60-curl-installer`, `61-no-node-prereq`, `23-install-user`, `40-registry-cli` — no newly-red / no newly-skipped (GATE-01).
    - The recipes-still-get-Node boundary is proven (in-file or by reference to 50-agents.bats) — DIST-01's no-Node claim is correctly scoped to the CLI/provisioner, not the recipes.
  </acceptance_criteria>
  <done>
    `61-no-node-prereq.bats` proves the static musl CLI/provisioner runs with no
    Node prerequisite (static-link + no-Node-before-the-bin + recipes-still-get-Node),
    green on the Rust build on both distros; the full installer+registry bats
    closeout is green with no newly-red/newly-skipped. DIST-01's central claim is
    asserted; GATE-01 holds for the phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness default path → the artifact under test | Folding the flags makes Rust the default; the fail-loud guard ensures a missing/broken musl bin ABORTS rather than silently testing the Bash path and reporting a false-green Rust pass. |
| rollback lever → the Bash+TS distribution | `AGENTLINUX_LEGACY_TS=1` restores the shippable Bash+TS build; it fails loud if the TS bundle is absent so a rollback attempt on a musl-only tree cannot false-green. |
| install-start (no Node) → `agentlinux provision` | The static bin reaches + runs `provision` before Node exists; `61-no-node-prereq` asserts this ordering — the DIST-01 chicken-and-egg-is-gone proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-07 | Repudiation | harness false-green on rollback/default | high | mitigate | Both the default Rust path and the `AGENTLINUX_LEGACY_TS=1` rollback carry the fail-loud discipline (abort non-zero on a missing bin/bundle) — the harness can never report a green against the wrong artifact (mirrors run.sh:292-295/:377-379). |
| T-58-08 | Spoofing | a dynamically-linked bin masquerading as static | medium | mitigate | `61-no-node-prereq` asserts `ldd` → "not a dynamic executable" on the STAGED bin — a regression that accidentally links libc (breaking the pre-Node run) is caught, not shipped. |
| T-58-SC | Tampering | cargo/crates (no new crates) | low | accept | No new dependencies — this wave is harness + a new bats file; the assertion tools (`ldd`/`readelf`/`grep`) are system coreutils/binutils, present on the base images. |
</threat_model>

<verification>
- The forward flags are folded into the default (bats exercise Rust with no override); `AGENTLINUX_LEGACY_TS=1` restores Bash+TS end-to-end (fail-loud on a missing bundle); default + rollback `10-installer` both green (GATE-05).
- `61-no-node-prereq.bats` proves static-link + no-Node-before-the-bin + recipes-still-get-Node; green on the Rust build on ubuntu-24.04 AND almalinux-9 (DIST-01).
- Full installer+registry closeout (`10/60/61/23/40`) green on the Rust build on both distros; no newly-red / newly-skipped (GATE-01).
- `plugin/cli/` + the Bash entrypoint retained as the rollback substrate; `agentlinux-core` pure (GATE-05).
- Phase-59-gated (NOT closed here): the 4-distro QEMU release gate + AGT-02 self-update against the live CDN.
</verification>

<success_criteria>
- The bats exercise the Rust musl bin as the DEFAULT artifact with no override (GATE-01); a single `AGENTLINUX_LEGACY_TS=1` lever gives a live per-phase rollback to the shippable Bash+TS build (GATE-05).
- The CLI/provisioner-needs-no-Node claim is proven by `61-no-node-prereq.bats` (static-link + pre-Node run); the recipes still get Node (the irreducible boundary) (DIST-01).
- The full installer + registry bats closeout is green on the Rust build on both distros with no newly-red/newly-skipped.
- `agentlinux-core` pure; master shippable; QEMU/AGT-02 deferred to Phase 59.
</success_criteria>

<output>
Create `.planning/phases/58-distribution-musl-tarball-sole-channel/58-03-SUMMARY.md` when done
</output>
