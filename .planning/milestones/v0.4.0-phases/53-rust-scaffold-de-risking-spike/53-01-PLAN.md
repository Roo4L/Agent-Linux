---
phase: 53-rust-scaffold-de-risking-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/Cargo.toml
  - rust/Cargo.lock
  - rust/rust-toolchain.toml
  - rust/.cargo/config.toml
  - rust/crates/agentlinux-core/Cargo.toml
  - rust/crates/agentlinux-core/src/lib.rs
  - rust/crates/agentlinux-core/src/types.rs
  - rust/crates/agentlinux-core/src/semver_shim.rs
  - rust/crates/agentlinux-core/src/classify.rs
  - rust/crates/agentlinux-core/src/divergence.rs
  - rust/crates/agentlinux/Cargo.toml
  - rust/crates/agentlinux/src/main.rs
  - .gitignore
autonomous: true
requirements: [RUST-01, RUST-03, GATE-05]
tags: [rust, cargo-workspace, musl, semver, port]

must_haves:
  truths:
    - "cargo build --release --target x86_64-unknown-linux-musl produces a single agentlinux binary that ldd reports as 'not a dynamic executable' (RUST-01)"
    - "agentlinux-core::classify returns the same six Status verdicts as the TS classify across the ported classify.test.ts corpus (RUST-03)"
    - "agentlinux-core::resolve_latest_for returns the same maxSatisfying result and typed zero-match error as the TS resolveLatestFor across the ported divergence.test.ts corpus (RUST-03)"
    - "the space-separated compound range '>=2.0.0 <3.0.0' matches 2.5.0 via the normalize_range shim, and 'v1.0.0' / '2.1' parse via the lenient parser (RUST-03 parity)"
    - "master stays untouched — all work lands on branch worktree-stack-revisiting; the rust/ track is additive (GATE-05)"
  artifacts:
    - "rust/Cargo.toml (workspace) + rust/Cargo.lock (committed) + rust/rust-toolchain.toml (channel 1.97.1) + rust/.cargo/config.toml (musl crt-static)"
    - "rust/crates/agentlinux-core/src/{lib,types,semver_shim,classify,divergence}.rs"
    - "rust/crates/agentlinux/src/main.rs (thin bin with hidden classify/divergence test subcommands)"
    - ".gitignore entry for rust/target/"
  key_links:
    - "semver_shim.rs is the ONLY module that calls the dtolnay semver crate — classify.rs and divergence.rs route every version/range string through it (normalize_range + parse_lenient) so the node-semver divergences never leak to callers"
    - "agentlinux-core is a pure crate (no std::process / std::fs / std::env) so Phase 54 cargo-mutants --package agentlinux-core can scope to it"
---

<objective>
Stand up the repo-root `rust/` cargo workspace and prove the first slice of the v0.4.0 Rust rewrite: a fully-static x86_64-musl `agentlinux` binary (RUST-01) plus the two pure-logic units — `classify` and `divergence` — ported into a separate `agentlinux-core` crate with verdicts identical to the TypeScript golden corpus (RUST-03, pure core).

Purpose: De-risk the toolchain (workspace + musl static link + pinned toolchain) and the single highest-correctness-risk item (node-semver → dtolnay `semver` parity) at small scale, so the bulk port in phases 54–59 is calibrated by working evidence, not estimate. The pure/adapter crate split is the structural reason the rewrite exists — it is what makes Phase 54's `proptest` + `cargo-mutants` gates reachable.

Output: A committed `rust/` workspace producing a static musl binary; `agentlinux-core` with `classify` + `divergence` + a `semver_shim` (`normalize_range` + `parse_lenient`); Rust `#[cfg(test)]` golden-corpus modules ported verbatim from `classify.test.ts` and `divergence.test.ts`; a thin `agentlinux` bin. This plan does NOT touch bash, bats, or CI — that is Plan 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-CONTEXT.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-VALIDATION.md

# Source-of-truth TS being ported (parity oracle):
@plugin/cli/src/version/classify.ts
@plugin/cli/src/upgrade/divergence.ts
@plugin/cli/src/types.ts
@plugin/cli/test/classify.test.ts
@plugin/cli/test/divergence.test.ts
</context>

## Artifacts this phase produces

> Full-phase artifact inventory (spans Plan 01 + Plan 02). Every symbol/file the spike creates.

**Crates & workspace files (Plan 01):**
- `rust/Cargo.toml` (workspace), `rust/Cargo.lock` (committed), `rust/rust-toolchain.toml` (channel 1.97.1), `rust/.cargo/config.toml` (musl crt-static)
- `rust/crates/agentlinux-core/Cargo.toml` (semver `=1.0.28`, serde, serde_json, thiserror)
- `rust/crates/agentlinux/Cargo.toml` (bin, depends on agentlinux-core)
- `.gitignore` entry: `rust/target/`

**agentlinux-core symbols (Plan 01 + Plan 02):**
- `types.rs`: `struct CatalogEntry`, `struct Sentinel`, `enum Status` (6 variants), `struct DivergenceReport`
- `semver_shim.rs`: `fn normalize_range`, `fn parse_lenient`, `fn eq`, `fn gt`, `fn max_satisfying`, `enum SemverError` (Plan 01)
- `classify.rs`: `fn classify` (Plan 01)
- `divergence.rs`: `fn compute_divergence`, `fn resolve_latest_for`, `enum DivergenceError` (Plan 01)
- `reuse.rs`: `fn agent_decision`, `enum Decision` (Plan 02)
- `lib.rs`: `pub mod` declarations for all of the above

**agentlinux bin symbols (Plan 01 + Plan 02):**
- `main.rs`: argv `match` dispatcher; `reuse-decision <id>` subcommand reading `DETECT_AGENT_<ID>_STATUS/_PATH` (Plan 02); canonical-path map mirror

**Bash / CI / metrics (Plan 02):**
- `plugin/lib/reuse/agents.sh`: shim body forwarding `reuse::agent_decision` → Rust bin (bash `REUSE_AGENT_CANONICAL_PATHS` map + `REUSE_GSD_SYSTEM_PATH` preserved)
- `.github/workflows/test.yml`: gated `rust` job + `rust/**` in the `changes` filter
- `.planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md`: agent-loop cost table + bulk-port verdict + GATE-05 rollback note

## Multi-Source Coverage Audit

> Every source item is COVERED by a plan. No MISSING items → no phase split required.

| Source | Item | Covered by | Notes |
|--------|------|-----------|-------|
| GOAL (ROADMAP Phase 53 SC-1) | static musl binary, `ldd`=not-dynamic | 53-01 Task 1 | RUST-01 |
| GOAL (SC-2) | CI build+clippy+fmt+test on every PR, rust job in Docker matrix | 53-02 Task 3 | RUST-02 (literal reading resolved) |
| GOAL (SC-3) | classify + divergence + 1 provisioner unit ported; bats green, no newly-skipped | 53-01 Tasks 2-3 + 53-02 Tasks 1-2 | RUST-03 + GATE-01 |
| GOAL (SC-4) | agent-loop metrics recorded | 53-02 Task 4 | RUST-03 |
| GOAL (SC-5) | parallel track, master shippable, per-phase rollback | 53-01 (branch isolation) + 53-02 Task 4 (rollback note) | GATE-05 |
| REQ | RUST-01 | 53-01 Task 1 | |
| REQ | RUST-02 | 53-02 Task 3 | |
| REQ | RUST-03 (classify/divergence pure) | 53-01 Tasks 2-3 | |
| REQ | RUST-03 (provisioner unit) | 53-02 Task 1 | |
| REQ | RUST-03 (metrics) | 53-02 Task 4 | |
| REQ | GATE-01 | 53-02 Task 2 (bats checkpoint) | |
| REQ | GATE-05 | 53-01 + 53-02 Task 4 | |
| RESEARCH | semver_shim (normalize_range + parse_lenient) | 53-01 Task 2 | HIGH-risk parity item |
| RESEARCH | pure/adapter crate split (mutation-enabler) | 53-01 Task 1 | Phase-54 readiness |
| RESEARCH | reuse::agent_decision single-call-site seam | 53-02 Task 1 | zero bats edits |
| RESEARCH | gated CI job mirroring cli-unit | 53-02 Task 3 | |
| RESEARCH | OOM-safe targeted bats runs | 53-02 Task 2 | checkpoint |
| CONTEXT | Claude's-discretion (infra, no locked D-NN decisions) | all tasks | CONTEXT.md has no `## Decisions` D-NN block — infrastructure-skip mode; recommended shape honored |

**Excluded (not gaps):** CONTEXT Deferred Ideas (proptest/cargo-mutants/schemars → Phase 54; full pure-core → 55; CLI verbs → 56; provisioner consolidation + CANONICAL_PATHS delete → 57; tarball → 58) are scoped to other phases per REQUIREMENTS traceability.

## Resolved decisions (from research open questions)

- **Layout:** Rust lives at repo-root `rust/`, NOT `plugin/rust/` (research §"Where the Rust code lives", A3). `plugin/` stays the clean "what ships in the tarball" boundary during the parallel-track migration; the CI `changes` filter gets one new `rust/**` entry (added in Plan 02).
- **classify/divergence are only INDIRECTLY bats-visible** (via `agentlinux list`/`upgrade`). Per research Open-Q2 + Pitfall 4, this plan validates them with **ported Rust unit tests reusing the TS test tables** as the golden corpus. It does NOT wire them behind live `list` output and does NOT rewrite any CLI verb — that scope is Phase 56.
- **CANONICAL_PATHS stays duplicated** (research Open-Q3) — no delete-from-TS/bash here; that consolidation is Phase 57. This plan does not touch `detect.ts` or `agents.sh`.
- **semver crate is pinned to the research-validated `1.0.28`** (`semver = "=1.0.28"`).

<tasks>

<task type="auto">
  <name>Task 1: Cargo workspace + static-musl build (RUST-01)</name>
  <files>rust/Cargo.toml, rust/rust-toolchain.toml, rust/.cargo/config.toml, rust/crates/agentlinux-core/Cargo.toml, rust/crates/agentlinux-core/src/lib.rs, rust/crates/agentlinux/Cargo.toml, rust/crates/agentlinux/src/main.rs, rust/Cargo.lock, .gitignore</files>
  <read_first>
    - .planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md (§"Recommended Project Structure", §"Static musl Build", §"Standard Stack" — the exact crate versions + the rust/.cargo/config.toml rustflags block)
    - .gitignore (repo root — add the rust/target/ ignore without disturbing existing entries)
  </read_first>
  <action>
    Create the repo-root `rust/` cargo workspace. `rust/Cargo.toml` is `[workspace]` with `members = ["crates/*"]` and `resolver = "2"`. `rust/rust-toolchain.toml` pins `channel = "1.97.1"` with components `rustfmt`, `clippy` and target `x86_64-unknown-linux-musl` so CI, local, and the agent loop share one compiler. `rust/.cargo/config.toml` sets, under `[target.x86_64-unknown-linux-musl]`, `rustflags = ["-C", "target-feature=+crt-static"]` and `linker = "musl-gcc"` (belt-and-braces; the ldd assertion is the real gate).

    Create two member crates. `crates/agentlinux-core` (lib): dependencies `semver = "=1.0.28"`, `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"`, `thiserror = "1"`. Its `src/lib.rs` declares `pub mod types; pub mod semver_shim; pub mod classify; pub mod divergence;` (module bodies land in Tasks 2–4 — for this task, create `types.rs`, `semver_shim.rs`, `classify.rs`, `divergence.rs` as empty-but-compiling stubs, or create them minimally so the workspace builds). `crates/agentlinux` (bin, package name `agentlinux`): depends on `agentlinux-core = { path = "../agentlinux-core" }`; `src/main.rs` is a thin entrypoint that dispatches on argv via a plain `match` (no `clap` this phase — deferred to Phase 56 per research §Alternatives). For this task `main.rs` only needs to compile and exit 0 on no args; the `reuse-decision` subcommand is added in Plan 02 and the hidden `classify`/`divergence` test subcommands are optional.

    The pure crate MUST NOT use `std::process`, `std::fs`, or `std::env` — keep it I/O-free so Phase 54's `cargo-mutants --package agentlinux-core` can scope to it (research §Pattern 1).

    Build the static binary: `cargo build --release --target x86_64-unknown-linux-musl -p agentlinux` (install `musl-tools` first if `musl-gcc` is absent, though it is present this session). Commit `rust/Cargo.lock`. Add `rust/target/` to `.gitignore`.

    Record iterations-to-green and any cargo-timeout / crate-hallucination incident for the `53-METRICS.md` table (populated in Plan 02) as you go.
  </action>
  <verify>
    <automated>cd rust && cargo build --release --target x86_64-unknown-linux-musl -p agentlinux && ldd target/x86_64-unknown-linux-musl/release/agentlinux 2>&1 | grep -q 'not a dynamic executable'</automated>
  </verify>
  <acceptance_criteria>
    - `rust/Cargo.toml` exists with `[workspace]` and `members = ["crates/*"]`
    - `rust/rust-toolchain.toml` contains `channel = "1.97.1"`
    - `rust/.cargo/config.toml` contains `target-feature=+crt-static` under the `x86_64-unknown-linux-musl` target
    - `rust/crates/agentlinux-core/Cargo.toml` pins `semver = "=1.0.28"`
    - `cargo build --release --target x86_64-unknown-linux-musl -p agentlinux` exits 0
    - `ldd target/x86_64-unknown-linux-musl/release/agentlinux` prints "not a dynamic executable"
    - `rust/Cargo.lock` is committed; `.gitignore` contains a `rust/target/` entry
    - `rust/crates/agentlinux-core/src/` contains no `std::process`, `std::fs`, or `std::env` usage (grep -rn to confirm)
  </acceptance_criteria>
  <done>The workspace builds a fully-static musl `agentlinux` binary (RUST-01) and the pure `agentlinux-core` crate compiles with the pinned semver dependency; Cargo.lock committed, target/ ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: semver_shim + types (the node-semver parity risk surface)</name>
  <files>rust/crates/agentlinux-core/src/semver_shim.rs, rust/crates/agentlinux-core/src/types.rs</files>
  <read_first>
    - .planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md (§"node-semver → Rust semver Parity" empirical table, §Pattern 2, Pitfall 1 + Pitfall 2)
    - plugin/cli/src/types.ts (CatalogEntry, Sentinel, Status, DivergenceReport shapes to mirror in Rust)
    - plugin/cli/src/version/classify.ts (which semver calls classify makes: semver.eq, semver.gt on two concrete versions)
    - plugin/cli/src/upgrade/divergence.ts (resolveLatestFor uses semver.maxSatisfying over version_constraint)
  </read_first>
  <behavior>
    - normalize_range(">=2.0.0 <3.0.0") converts the space-separated compound range to comma form so VersionReq::parse accepts it; the shim'd range matches 2.5.0 and rejects 3.0.0
    - normalize_range("^2.1") and normalize_range("*") pass through unchanged and still parse
    - parse_lenient("v1.0.0") strips the leading v and equals parse_lenient("1.0.0")
    - parse_lenient("2.1.0") parses; a partial like "2.1" is handled per the TS loose-accept behavior (either coerced to 2.1.0 or surfaced as a typed error — match what the ported classify/divergence corpora require, never a panic)
    - every parse/range entry point returns a typed error (thiserror enum) on malformed input — no unwrap/expect on untrusted version strings (T-53-01)
  </behavior>
  <action>
    Port the two Rust types the units consume into `types.rs`: a `CatalogEntry` struct (`#[derive(Debug, Clone, Deserialize)]`) mirroring `plugin/cli/src/types.ts` CatalogEntry (at minimum `id`, `pinned_version`, `version_constraint: Option<String>`, `npm_package_name: Option<String>`, `compatibility_window: Option<String>`), a `Sentinel` struct (`id`, `version`, `source`, `sticky`), and a `Status` enum with the six variants `NotInstalled`, `Synced`, `DriftUndeclared`, `OverrideAhead`, `OverrideBehind`, `PinnedOverride` (serde-renamed to the TS kebab strings: "not-installed", "synced", "drift-undeclared", "override-ahead", "override-behind", "pinned-override"). Model `CatalogEntry` as a plain `#[derive(Deserialize)]` struct so Phase 54's `#[derive(JsonSchema)]` bolts on without reshaping (research §Phase-54-readiness).

    Implement `semver_shim.rs` — the ONLY module that touches the dtolnay `semver` crate. Provide `pub fn normalize_range(node_range: &str) -> String` (splits on whitespace, joins with ", " so ">=2.0.0 <3.0.0" → ">=2.0.0, <3.0.0"; leaves "^2.1" and "*" untouched) and `pub fn parse_lenient(raw: &str) -> Result<semver::Version, SemverError>` (trims whitespace, strips a single leading 'v', then `semver::Version::parse`). Also expose `pub fn eq(a: &str, b: &str) -> Result<bool, SemverError>`, `pub fn gt(a: &str, b: &str) -> Result<bool, SemverError>`, and `pub fn max_satisfying<'a>(versions: &'a [String], node_range: &str) -> Result<Option<&'a str>, SemverError>` (parses via `VersionReq::parse(&normalize_range(...))`, picks the highest matching). Define a `SemverError` typed enum with `thiserror` — malformed version/range strings return a typed error, NEVER a panic (this is the untrusted-input threat T-53-01).

    Add `#[cfg(test)]` unit tests for the shim asserting the empirically-probed divergences from the research table: the compound-range comma case, the `v`-prefix strip, the caret/tilde/`*` maxSatisfying cases, and a malformed-input-returns-Err case (no panic).
  </action>
  <verify>
    <automated>cd rust && cargo test -p agentlinux-core semver_shim && cargo clippy -p agentlinux-core --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `rust/crates/agentlinux-core/src/semver_shim.rs` contains `pub fn normalize_range`, `pub fn parse_lenient`, `pub fn max_satisfying`, and a `thiserror`-derived `SemverError`
    - `rust/crates/agentlinux-core/src/types.rs` contains `pub struct CatalogEntry`, `pub struct Sentinel`, and a six-variant `pub enum Status` with serde kebab renames
    - `cargo test -p agentlinux-core semver_shim` passes, including a test asserting ">=2.0.0 <3.0.0" matches "2.5.0" and a test asserting parse_lenient("v1.0.0") == parse_lenient("1.0.0")
    - a test asserts malformed input returns Err (no panic) — grep the test file for an `is_err()` or `assert!(matches!(... Err` assertion
    - `cargo clippy -p agentlinux-core -- -D warnings` is clean
    - `semver_shim.rs` is the only file under agentlinux-core/src that references `semver::` (grep -rln 'semver::' shows only semver_shim.rs)
  </acceptance_criteria>
  <done>The semver compatibility shim isolates every node-semver → dtolnay divergence (compound-range comma, v-prefix, partial parse) behind typed-error functions, validated by unit tests reproducing the research parity table; the Rust type mirrors are in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port classify + divergence with TS golden corpus (RUST-03 pure core)</name>
  <files>rust/crates/agentlinux-core/src/classify.rs, rust/crates/agentlinux-core/src/divergence.rs</files>
  <read_first>
    - plugin/cli/src/version/classify.ts (the exact six-state decision order: not-installed → drift-undeclared → synced → pinned-override → override-ahead/behind)
    - plugin/cli/src/upgrade/divergence.ts (computeDivergence field mapping + resolveLatestFor zero-match/empty-list error paths)
    - plugin/cli/test/classify.test.ts (the six classify @tests — the golden corpus to port verbatim)
    - plugin/cli/test/divergence.test.ts (the resolveLatestFor table at line 132: versions=["1.0.0","1.1.0","1.2.0","2.0.0","2.1.0"], ^1.0→1.2.0, ~1.1→1.1.0, ^9.0→throw, []→throw; and the computeDivergence six-state suite)
    - rust/crates/agentlinux-core/src/semver_shim.rs (use eq/gt/max_satisfying/parse_lenient from Task 2)
  </read_first>
  <behavior>
    - classify: null sentinel OR null installed → NotInstalled; sentinel.version != installed → DriftUndeclared; installed == entry.pinned_version → Synced; sentinel.sticky → PinnedOverride; else installed > pinned → OverrideAhead / OverrideBehind — identical verdict to TS classify on every classify.test.ts row
    - compute_divergence: reifies classify result + the four version columns into a DivergenceReport with source "none" when sentinel absent, matching the divergence.test.ts computeDivergence suite (six states + latestVersion threading)
    - resolve_latest_for: max_satisfying over version_constraint (default "*"); zero matches → typed Err with the ${id}: no published version...satisfies message shape; empty versions list → typed Err with the no published versions message shape (NOT a panic)
  </behavior>
  <action>
    Port `classify.ts` into `classify.rs` as `pub fn classify(entry: &CatalogEntry, sentinel: Option<&Sentinel>, installed: Option<&str>) -> Status`, preserving the exact TS branch order (early NotInstalled on null sentinel/installed; then `!eq(sentinel.version, installed)` → DriftUndeclared; `eq(installed, pinned_version)` → Synced; `sentinel.sticky` → PinnedOverride; `gt(installed, pinned_version)` ? OverrideAhead : OverrideBehind). Use the Task-2 shim (`semver_shim::eq` / `::gt`) for every comparison — never call `semver::Version::parse` directly here.

    Port `divergence.ts` into `divergence.rs`: `pub fn compute_divergence(entry: &CatalogEntry, sentinel: Option<&Sentinel>, installed: Option<&str>, latest: Option<&str>) -> DivergenceReport` (add `DivergenceReport` to `types.rs` mirroring the TS shape: id, status, sentinel_version, installed_version, curated_version, latest_version, source, sticky), and `pub fn resolve_latest_for(entry: &CatalogEntry, published_versions: &[String]) -> Result<String, DivergenceError>` using `semver_shim::max_satisfying`. Empty list → typed Err (message mirroring "{id}: no published versions found"); zero-match → typed Err (message mirroring "{id}: no published version of {pkg} satisfies constraint {constraint}"). Define `DivergenceError` with `thiserror`.

    Port the TS test tables verbatim as `#[cfg(test)]` golden-corpus modules — the six classify states from `classify.test.ts:36-93` and the `resolveLatestFor` + `computeDivergence` tables from `divergence.test.ts` (the resolveLatestFor version array is at divergence.test.ts:133). Do NOT port the `queryGlobalNpm` / `queryNpmViewLatest` suites — those are impure npm-dispatcher tests (Phase 56 scope), not pure-core parity. Each Rust test asserts the same verdict the TS `assert.equal` / `assert.throws` asserts.

    Record iterations-to-green per unit (classify vs divergence) for the `53-METRICS.md` table populated in Plan 02.
  </action>
  <verify>
    <automated>cd rust && cargo test -p agentlinux-core classify && cargo test -p agentlinux-core divergence && cargo clippy -p agentlinux-core --all-targets -- -D warnings && cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - `rust/crates/agentlinux-core/src/classify.rs` contains `pub fn classify` returning the six-variant `Status`
    - `rust/crates/agentlinux-core/src/divergence.rs` contains `pub fn compute_divergence` and `pub fn resolve_latest_for` (the latter returning `Result<String, DivergenceError>`)
    - `cargo test -p agentlinux-core classify` passes all six ported classify states
    - `cargo test -p agentlinux-core divergence` passes the resolveLatestFor table (no-constraint→2.1.0, ^1.0→1.2.0, ~1.1→1.1.0, ^9.0→Err, []→Err) and the computeDivergence six-state suite
    - a divergence test asserts the zero-match case returns Err (not a panic) matching the TS assert.throws
    - `cargo clippy -p agentlinux-core -- -D warnings` clean and `cargo fmt --all -- --check` clean
    - grep confirms classify.rs / divergence.rs do NOT reference `semver::` directly (they route through semver_shim)
  </acceptance_criteria>
  <done>classify + divergence are ported into the pure core with verdicts identical to the TS golden corpus (including the zero-match typed-error path), routed through the semver shim, clippy-clean and fmt-clean (RUST-03 pure core).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version string → semver core | `installed` (from `<bin> --version` / `npm ls`) and catalog range/`compatibility_window` strings are untrusted text parsed by the semver crate |
| catalog/sentinel JSON → serde | CatalogEntry / Sentinel deserialized from on-disk JSON (well-formed by our own writers this phase, but treated as parse input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01 | Denial of Service | `semver_shim::parse_lenient` / `max_satisfying` / `classify` on malformed version input | medium | mitigate | Every parse/range entry point returns a typed `thiserror` error; no `unwrap`/`expect`/`panic` on untrusted version strings. Task-2 unit test asserts malformed input returns `Err` (no panic). |
| T-53-02 | Tampering | node-semver→dtolnay parity divergence silently flipping a verdict (compound-range space vs comma) | medium | mitigate | `normalize_range` shim + golden-corpus tests reproduce the empirically-probed divergences; classify/divergence never call `semver::` directly (grep-enforced in acceptance). |
| T-53-SC | Tampering | cargo crate installs (semver/serde/serde_json/thiserror) | low | accept | All four are crates.io-canonical (dtolnay/serde-team, billions of downloads) with an OK verdict in RESEARCH §"Package Legitimacy Audit"; no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries → no legitimacy checkpoint required. `semver` is version-pinned to `=1.0.28`. |

**AI-integration / assumption-delta / schema-gate detectors:** none fire — no external API/SDK integration, no singular→plural identity change (CANONICAL_PATHS consolidation is Phase 57, not here), no ORM/schema files touched (`schemars` schema-gen is Phase 54). Recorded non-fire per planner contribution.
</threat_model>

<verification>
- `cd rust && cargo build --release --target x86_64-unknown-linux-musl -p agentlinux && ldd <bin> | grep 'not a dynamic executable'` (RUST-01)
- `cd rust && cargo test --workspace` green (classify + divergence + semver_shim golden corpus, RUST-03)
- `cd rust && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check` clean
- Grep: `agentlinux-core/src` has no `std::process`/`std::fs`/`std::env`; only `semver_shim.rs` references `semver::`
- `master` untouched; work on branch `worktree-stack-revisiting` (GATE-05)
</verification>

<success_criteria>
- Static musl `agentlinux` binary builds; `ldd` = "not a dynamic executable" (RUST-01)
- `classify` + `divergence` ported to `agentlinux-core` with verdicts identical to the ported TS golden corpus including the zero-match typed error (RUST-03 pure core)
- `semver_shim` isolates the node-semver divergences (compound-range comma, v-prefix, lenient parse) behind typed-error functions
- Workspace is pure-core/thin-bin split so Phase 54 mutation/property gates can target `agentlinux-core`
- `Cargo.lock` committed, `rust/target/` gitignored, master untouched (GATE-05)
</success_criteria>

<output>
Create `.planning/phases/53-rust-scaffold-de-risking-spike/53-01-SUMMARY.md` when done. Record, for the METRICS table in Plan 02: iterations-to-green for the workspace/musl build, classify, and divergence; any cargo-compile-timeout or crate-hallucination incident encountered.
</output>
