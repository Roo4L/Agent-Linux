---
phase: 53-rust-scaffold-de-risking-spike
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - rust/crates/agentlinux-core/src/reuse.rs
  - rust/crates/agentlinux-core/src/lib.rs
  - rust/crates/agentlinux/src/main.rs
  - plugin/lib/reuse/agents.sh
  - .github/workflows/test.yml
  - .planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md
autonomous: false
requirements: [RUST-02, RUST-03, GATE-01, GATE-05]
tags: [rust, provisioner, bats, ci, reuse, metrics, musl]

must_haves:
  truths:
    - "agentlinux reuse-decision <id> reads DETECT_AGENT_<ID>_STATUS/PATH env vars and prints one of reuse|remediate|create, computed in agentlinux-core::reuse (RUST-03 provisioner)"
    - "tests/bats/13-reuse.bats REUSE-03 @tests pass with the Rust binary behind the ~3-line agents.sh shim and ZERO bats edits (GATE-01)"
    - "CI runs a gated rust job (build + clippy -D warnings + rustfmt --check + cargo test + static-musl ldd assertion) on every PR (RUST-02)"
    - "53-METRICS.md records iterations-to-green per unit + cargo-timeout + crate-hallucination incidents as the go/no-go evidence for the bulk port (RUST-03)"
    - "the rust job is gated so a red spike cannot block a master hotfix; master stays shippable, the rust track is additive and revertible (GATE-05)"
  artifacts:
    - "rust/crates/agentlinux-core/src/reuse.rs (pure agent_decision) + the reuse-decision subcommand in agentlinux/src/main.rs"
    - "plugin/lib/reuse/agents.sh shim body forwarding env → Rust bin (bash REUSE_AGENT_CANONICAL_PATHS map preserved)"
    - ".github/workflows/test.yml rust job + rust/** in the changes filter"
    - ".planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md (filled table + bulk-port verdict + GATE-05 rollback note)"
  key_links:
    - "agents.sh shim discovers the Rust binary via AGENTLINUX_RUST_BIN env override with a PATH fallback to `agentlinux`, so 13-reuse.bats points the shim at the built musl artifact without full provisioner staging"
    - "the bash REUSE_AGENT_CANONICAL_PATHS map is NOT deleted — remediate.sh:288 iterates its keys; deleting it is Phase 57 (the shim only replaces the function body)"
    - "the rust job mirrors the existing cli-unit guard shape (needs: changes; skip when code!=true or rust/Cargo.toml absent) so required-check contexts always report"
---

<objective>
Port the gnarly REUSE-03 provisioner unit — `reuse::agent_decision` — into the Rust binary behind the existing bats suite with zero bats edits, wire the gated Rust CI job into `.github/workflows/test.yml`, and capture the agent-loop metrics that decide go/no-go for the bulk port (phases 54–59).

Purpose: `reuse::agent_decision` is the exact sync-pain exemplar the stack-reconsideration decision quotes — bash stops at 2 of 3 predicates because semver-range satisfaction is non-trivial in bash, and it hand-maintains a `CANONICAL_PATHS` map that must stay byte-identical to `detect.ts`. Porting it proves Rust removes that bug class AND that the language-agnostic bats spec (ADR-002) validates a Rust build unchanged. The CI job establishes RUST-02; the metrics artifact is a first-class RUST-03 deliverable (the first per-language cost data on our model + our code).

Output: a pure `agentlinux-core::reuse::agent_decision`; a `reuse-decision` subcommand on the `agentlinux` bin; a ~3-line `agents.sh` shim (env-var-in / stdout-token-out contract preserved); the gated `rust` CI job + `rust/**` changes filter; a filled `53-METRICS.md` with the bulk-port verdict and the GATE-05 rollback note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-CONTEXT.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-VALIDATION.md
@.planning/phases/53-rust-scaffold-de-risking-spike/53-01-PLAN.md

# Source-of-truth being ported + the seam:
@plugin/lib/reuse/agents.sh
@tests/bats/13-reuse.bats
@.github/workflows/test.yml
</context>

## Resolved decisions (from research open questions)

- **RUST-02 "added to the Docker matrix" resolves to the LITERAL spike reading** (research Open-Q1, A6): add a **separate gated `rust` job to `test.yml`** that runs on every PR (build + clippy + rustfmt + cargo test + the static-musl `ldd` assertion). The per-distro in-container musl build is Phase 59's GATE-02 endgame — NOT this spike. State this in the plan and in `53-METRICS.md`.
- **The bash `REUSE_AGENT_CANONICAL_PATHS` map stays** (research Open-Q3). The shim replaces only the *body* of `reuse::agent_decision`; the `declare -gA REUSE_AGENT_CANONICAL_PATHS` block and `REUSE_GSD_SYSTEM_PATH` remain because `remediate.sh:288` iterates the map's keys (`for agent_id in "${!REUSE_AGENT_CANONICAL_PATHS[@]}"`). Deleting the duplication is Phase 57.
- **Docker OOM constraint** (research §"The bats-Integration Seam", MEMORY.md): the full Docker bats suite OOMs mid-run (~test 131) in this VM. The executor runs the ported-surface files targeted (`13-reuse.bats`, and smoke `14-remediate.bats`) and records exactly which files were exercised. Do NOT claim full-suite green from a partial local run — CI (fresh runners) asserts the full matrix.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port reuse::agent_decision to Rust + reuse-decision subcommand + agents.sh shim (RUST-03 provisioner, GATE-01)</name>
  <files>rust/crates/agentlinux-core/src/reuse.rs, rust/crates/agentlinux-core/src/lib.rs, rust/crates/agentlinux/src/main.rs, plugin/lib/reuse/agents.sh</files>
  <read_first>
    - plugin/lib/reuse/agents.sh (the full predicate logic: absent→create, unknown id→create, broken→remediate, path!=canonical→remediate except gsd@VERSION-path→reuse, else→reuse; the REUSE_AGENT_CANONICAL_PATHS map + REUSE_GSD_SYSTEM_PATH; the ${id^^//-/_} → DETECT_AGENT_<UPPER>_PATH env-var derivation)
    - tests/bats/13-reuse.bats (the REUSE-03 @tests at lines 429-518: how they export DETECT_AGENT_<ID>_STATUS/_PATH, call `reuse::agent_decision <id>`, and assert stdout == reuse|remediate|create; the __source_lib_chain_with_reuse helper at line 57)
    - plugin/lib/remediate.sh (the :288 call site iterating "${!REUSE_AGENT_CANONICAL_PATHS[@]}" — why the bash map must stay)
    - rust/crates/agentlinux-core/src/semver_shim.rs (from Plan 01 — available but predicate 3 / semver is NOT evaluated here; predicates 1+2 only, matching the bash contract)
  </read_first>
  <behavior>
    - agent_decision(id, status, detected_path, canonical, gsd_system_path): empty id → Create; status "absent" → Create; unknown id (no canonical) → Create; status "broken" → Remediate; healthy + detected_path != canonical → Remediate, EXCEPT id=="gsd" && detected_path==gsd_system_path → Reuse; healthy + path match → Reuse
    - `agentlinux reuse-decision <id>` reads DETECT_AGENT_<UPPER-with-hyphens-as-underscores>_STATUS and _PATH from the environment, resolves the canonical path from the map, prints exactly one lowercase token (reuse|remediate|create) with no trailing newline noise that would break the bats stdout==token assertion, exits 0
    - 13-reuse.bats REUSE-03 cases all pass unchanged: status=absent→create, healthy+canonical→reuse, healthy+wrong-path→remediate, gsd@VERSION-path→reuse, broken→remediate, unknown-id→create
  </behavior>
  <action>
    Add `reuse.rs` to `agentlinux-core` (declare `pub mod reuse;` in `lib.rs`). Implement the pure decision as `pub fn agent_decision(id: &str, status: &str, detected_path: Option<&str>, canonical: Option<&str>, gsd_system_path: &str) -> Decision` where `pub enum Decision { Reuse, Remediate, Create }` with a `Display`/`as_str` yielding the lowercase tokens "reuse"/"remediate"/"create". Preserve the bash predicate order exactly (research §Pattern 1 example): empty/absent/unknown-id → Create; broken → Remediate; healthy path-mismatch → Remediate except the gsd-at-VERSION-path → Reuse special case; else Reuse. Predicate 3 (semver compatibility_window) is NOT evaluated here — the bash contract stops at predicates 1+2 and the CLI layers predicate 3 on later; keep parity. Keep this crate pure (no env reads in `reuse.rs`).

    In `agentlinux/src/main.rs`, add a `reuse-decision <id>` subcommand (plain argv `match`, no clap). It does the I/O: derive `DETECT_AGENT_<UPPER>_STATUS` / `_PATH` var names from the id (uppercase, hyphens→underscores, matching bash `${id^^//-/_}`), read them from the environment (`std::env::var`), resolve the canonical path + gsd VERSION path from a map defined in the bin (mirroring the bash map values `claude-code`→/home/agent/.local/bin/claude, `gsd`→/home/agent/.npm-global/bin/gsd-core, `playwright-cli`→/home/agent/.npm-global/bin/playwright-cli; gsd VERSION path /home/agent/.claude/gsd-core/VERSION), call `agentlinux_core::reuse::agent_decision`, and `print!` the token with no trailing newline. The env/map reads live in the bin (adapter), the decision lives in core — the pure/adapter split.

    Replace ONLY the body of `reuse::agent_decision()` in `plugin/lib/reuse/agents.sh` with a shim that forwards to the Rust bin: resolve the binary as `${AGENTLINUX_RUST_BIN:-agentlinux}` (env override with PATH fallback), then `"$bin" reuse-decision "$id"`. Keep the `declare -gA REUSE_AGENT_CANONICAL_PATHS` map, `REUSE_GSD_SYSTEM_PATH`, the source-once guard, and the log.sh precondition check intact and unchanged (remediate.sh:288 iterates the map). Do NOT edit any bats file. Do NOT delete the duplicated map. The env-var-in / stdout-token-out contract is preserved so every 13-reuse.bats @test and the remediate.sh:288 call site work verbatim.

    Add `#[cfg(test)]` tests in `reuse.rs` covering the six REUSE-03 branches (absent→Create, unknown→Create, broken→Remediate, healthy+canonical→Reuse, healthy+wrong-path→Remediate, gsd@VERSION→Reuse).
  </action>
  <verify>
    <automated>cd rust && cargo test -p agentlinux-core reuse && cargo clippy --workspace --all-targets -- -D warnings && cargo build --release --target x86_64-unknown-linux-musl -p agentlinux && AGENTLINUX_RUST_BIN="$PWD/target/x86_64-unknown-linux-musl/release/agentlinux" bash -c 'DETECT_AGENT_CLAUDE_CODE_STATUS=healthy DETECT_AGENT_CLAUDE_CODE_PATH=/home/agent/.local/bin/claude "$AGENTLINUX_RUST_BIN" reuse-decision claude-code' | grep -qx reuse</automated>
  </verify>
  <acceptance_criteria>
    - `rust/crates/agentlinux-core/src/reuse.rs` contains `pub fn agent_decision` and `pub enum Decision { Reuse, Remediate, Create }`
    - `agentlinux-core/src/lib.rs` declares `pub mod reuse;`
    - `agentlinux reuse-decision claude-code` with `DETECT_AGENT_CLAUDE_CODE_STATUS=healthy` + `_PATH=/home/agent/.local/bin/claude` prints exactly `reuse`
    - `plugin/lib/reuse/agents.sh` `reuse::agent_decision` body forwards to `${AGENTLINUX_RUST_BIN:-agentlinux} reuse-decision "$id"`; the `declare -gA REUSE_AGENT_CANONICAL_PATHS` map and `REUSE_GSD_SYSTEM_PATH` are still present (grep confirms)
    - NO file under `tests/bats/` is modified by this task (`git diff --name-only tests/bats/` is empty)
    - `cargo test -p agentlinux-core reuse` passes all six REUSE-03 branch tests
    - `cargo clippy --workspace -- -D warnings` clean
  </acceptance_criteria>
  <done>reuse::agent_decision is computed in Rust and reachable through the unchanged bash/bats interface via a ~3-line shim; the bash CANONICAL_PATHS map is preserved for remediate.sh; the pure decision lives in agentlinux-core (RUST-03 provisioner).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: bats acceptance checkpoint — 13-reuse.bats green on the Rust build (GATE-01)</name>
  <files>tests/bats/13-reuse.bats, tests/bats/14-remediate.bats (read-only — asserted, never edited)</files>
  <what-built>The Rust `reuse-decision` unit is wired behind the `agents.sh` shim. This checkpoint runs the actual bats acceptance oracle (13-reuse.bats) against the Rust build inside a targeted Docker container — the GATE-01 evidence that the language-agnostic spec passes unchanged. It is human-gated because the full Docker bats suite OOMs in this VM (documented), so the run is a targeted per-file container the executor drives out-of-band, and a human confirms which files were exercised and that no test was newly skipped.</what-built>
  <action>Human-verified acceptance gate (not autonomous code). The executor builds the musl binary, starts a targeted Docker bats container for the ported surface, runs ONLY the ported-surface files (the full suite OOMs ~test 131 in this VM), and presents the results for human confirmation per how-to-verify. No source is edited in this task; the sole deliverable is the recorded pass/skip evidence.</action>
  <how-to-verify>
    1. Build the musl binary: `cd rust && cargo build --release --target x86_64-unknown-linux-musl -p agentlinux`.
    2. Start a targeted Docker bats container for the ported surface and run ONLY the ported-surface files (the full suite OOMs ~test 131 in this VM). Start the container per `tests/docker/run.sh`'s pattern, ensure `AGENTLINUX_RUST_BIN` points at the staged musl artifact (or the binary is on the agent PATH inside the container), then:
       `docker exec <cid> bash -c 'cd /opt/agentlinux-src && bats tests/bats/13-reuse.bats'`
       and smoke the downstream consumer: `... bats tests/bats/14-remediate.bats`.
    3. Confirm: all REUSE-03 @tests in 13-reuse.bats pass (reuse|remediate|create tokens correct), zero newly-skipped tests, and 14-remediate.bats is green.
    4. Record exactly which bats files were exercised (do NOT claim full-suite green from a partial local run — CI runs the full matrix on fresh runners).
  </how-to-verify>
  <verify>
    <human-check>13-reuse.bats REUSE-03 @tests all pass against the Rust build behind the shim; zero newly-skipped tests; 14-remediate.bats green; the list of exercised bats files is recorded.</human-check>
  </verify>
  <done>The ported-surface bats files pass on the Rust build with zero bats edits and zero newly-skipped tests, human-confirmed with the exercised-file list recorded (GATE-01).</done>
  <resume-signal>Type "approved" with the list of bats files run + their pass/skip counts, or describe the failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Gated rust CI job + rust/** changes filter (RUST-02)</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (the `changes` job filter block at lines 56-64; the `cli-unit` job guard shape at lines 89-127 to mirror; the `permissions: contents: read` least-privilege posture at line 24)
    - .planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md (§"Static musl Build in GitHub Actions CI" — the verbatim rust job YAML with dtolnay/rust-toolchain, Swatinem/rust-cache, the fmt/clippy/test/musl-ldd steps)
  </read_first>
  <action>
    Extend the `changes` job's `dorny/paths-filter` `code` filter (test.yml:56-64) with a `rust/**` glob so the rust job gates its steps the same way `cli-unit`/`bats-docker` do.

    Add a `rust` job (`needs: changes`, `runs-on: ubuntu-24.04`) mirroring the `cli-unit` guard-then-steps shape: a `guard` step that sets `ready=false` when `needs.changes.outputs.code != 'true'` OR `rust/Cargo.toml` is absent (so required-check contexts always report and a dev-only change reports green fast — the test.yml:37-40 matrix-skip trap note). When `ready == 'true'`: install the pinned toolchain via `dtolnay/rust-toolchain@stable` (respects `rust-toolchain.toml`) with components `clippy,rustfmt` and target `x86_64-unknown-linux-musl`; `Swatinem/rust-cache@v2` with `workspaces: rust`; then `working-directory: rust` run `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all`, and finally the static-musl step: `sudo apt-get install -y musl-tools`, `cargo build --release --target x86_64-unknown-linux-musl -p agentlinux`, and the `ldd <bin> | grep -q 'not a dynamic executable'` assertion (fail the job if dynamically linked). Keep the job's `permissions` least-privilege (inherit the workflow `contents: read`).

    This is the LITERAL RUST-02 reading (research Open-Q1 / A6): a separate gated rust job on every PR. The per-distro in-container musl build (building the binary inside each bats-docker arm) is Phase 59's GATE-02 endgame and is explicitly out of scope here.

    Validate the YAML parses (`python3 -c 'import yaml,sys; yaml.safe_load(open("...test.yml"))'` or the repo's pre-commit yaml hook). Do NOT run `act` if unavailable — YAML-validity + a structural grep for the required steps is the spike-level check.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/test.yml')); assert 'rust' in d['jobs'], 'rust job missing'; import subprocess; assert subprocess.run(['grep','-q','rust/\\*\\*','.github/workflows/test.yml']).returncode==0, 'rust/** filter missing'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` `changes` filter `code:` list includes a `rust/**` entry
    - `test.yml` has a `rust:` job with `needs: changes` and a guard step keying on `needs.changes.outputs.code` + `rust/Cargo.toml` presence
    - the rust job runs `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all`, and a static-musl build + `ldd ... 'not a dynamic executable'` assertion, all guarded by `steps.guard.outputs.ready == 'true'`
    - the YAML parses (yaml.safe_load succeeds) and the pre-commit yaml hook passes
  </acceptance_criteria>
  <done>CI builds, clippy-lints, rustfmt-checks, unit-tests, and static-musl-asserts the Rust binary on every PR via a gated job that never blocks a dev-only change or a master hotfix (RUST-02).</done>
</task>

<task type="auto">
  <name>Task 4: 53-METRICS.md — agent-loop cost + bulk-port verdict + GATE-05 rollback note (RUST-03, GATE-05)</name>
  <files>.planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md</files>
  <read_first>
    - .planning/phases/53-rust-scaffold-de-risking-spike/53-RESEARCH.md (§"Agent-Loop Metrics (RUST-03)" — the metrics table columns + the 53-METRICS.md skeleton; Pitfall 3 crate-hallucination + cargo-timeout definitions)
    - .planning/phases/53-rust-scaffold-de-risking-spike/53-01-SUMMARY.md (iterations-to-green recorded during Plan 01 for classify/divergence/musl build)
  </read_first>
  <action>
    Create `53-METRICS.md`. Fill the per-unit table with columns: Unit | Iters-to-green | Hallucinations | cargo-timeouts | Wall-clock | Tokens | Notes, with rows for `classify`, `divergence`, and `reuse-decision (+ bats)` — using the iterations-to-green figures recorded in the Plan 01 SUMMARY and this plan's Task 1/checkpoint. Count crate-hallucination incidents (a non-existent crate/API proposed in a Cargo.toml or `use`) and cargo-compile-timeout incidents (tool-timeouts on `cargo build`/`test`) observed across both plans. Record any parity surprise beyond the research's node-semver table (feeds Phase 54's TEST-04 audit).

    Add a `## Verdict for the bulk port (54–59)` section: go / go-with-caveats / re-estimate, justified by the measured iterations-to-green, hallucination rate, and timeout rate versus the stack-reconsideration §7 accepted-cost estimate. This is the go/no-go evidence — state it plainly.

    Add a `## RUST-02 "Docker matrix" decision` note recording the resolved literal reading (separate gated rust job on every PR; per-distro in-container musl build deferred to Phase 59 GATE-02).

    Add a `## GATE-05 rollback note` section: how to revert the spike without touching master — all work is on branch `worktree-stack-revisiting`; the Rust track is purely additive (a new `rust/` dir, a new gated CI job, and a shim in `agents.sh` whose fallback is the original bash logic path); reverting the `agents.sh` shim commit + dropping the `rust/` dir + removing the rust CI job restores the TS/bash-only build with no master impact. The gated rust job means a red spike never blocks a master hotfix.
  </action>
  <verify>
    <automated>test -f .planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md && grep -q 'Verdict for the bulk port' .planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md && grep -q 'GATE-05 rollback' .planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md && grep -Eq 'classify|divergence|reuse-decision' .planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/53-rust-scaffold-de-risking-spike/53-METRICS.md` exists with a per-unit table (rows: classify, divergence, reuse-decision) carrying iterations-to-green, hallucination, and cargo-timeout columns
    - a `## Verdict for the bulk port (54–59)` section states go / go-with-caveats / re-estimate with justification
    - a `## RUST-02 "Docker matrix" decision` note records the literal-reading decision
    - a `## GATE-05 rollback note` section describes reverting the spike without touching master
  </acceptance_criteria>
  <done>The agent-loop cost is captured as first-class go/no-go evidence for the bulk port, the RUST-02 Docker-matrix reading is documented, and the GATE-05 rollback path is recorded (RUST-03, GATE-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bats/provisioner env → `agentlinux reuse-decision` | `DETECT_AGENT_<ID>_STATUS/_PATH` env vars (set by the detector / bats) are read by the bin and gate the reuse→remediate→create verdict that `remediate.sh:288` acts on |
| shim → Rust binary discovery | `agents.sh` resolves the binary via `${AGENTLINUX_RUST_BIN:-agentlinux}`; a wrong/missing binary must fail closed, not silently mis-verdict |
| CI job → repo | the gated rust job runs on PRs under `contents: read` least-privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-03 | Spoofing / Elevation | injected/incorrect `DETECT_AGENT_*` env producing a wrong reuse verdict that mis-gates `remediate.sh` | medium | mitigate | The Rust unit preserves the exact bash predicate order (absent/unknown/broken/path-mismatch), validated by the six 13-reuse.bats REUSE-03 @tests running unchanged against the Rust build (GATE-01 checkpoint). A wrong verdict fails a bats assertion. |
| T-53-04 | Tampering | shim resolves the wrong `agentlinux` binary (PATH hijack) and returns a bogus token | low | mitigate | `AGENTLINUX_RUST_BIN` env override lets the caller pin an absolute path; bats points it at the built musl artifact. A missing/failing binary surfaces as a bats failure, not a silent pass. |
| T-53-05 | Denial of Service | a red rust CI job blocking a master hotfix | medium | mitigate | The rust job is gated (`needs: changes`; skips when `rust/Cargo.toml` absent or no code changed) and is additive; GATE-05 rollback note documents reverting the track. A red spike never blocks a `master` release. |
| T-53-SC | Tampering | no new package-manager installs this plan (musl-tools is an apt system package in CI, not an npm/pip/cargo dep) | low | accept | New crates were vetted in Plan 01's audit; this plan adds no new crate. `musl-tools` is a distro package installed in the CI runner only. No legitimacy checkpoint required. |

**AI-integration / assumption-delta / schema-gate detectors:** none fire — no external API/SDK integration, no singular→plural identity change (the CANONICAL_PATHS map is deliberately kept duplicated; consolidation is Phase 57), no ORM/schema files. Recorded non-fire per planner contribution.
</threat_model>

<verification>
- `cd rust && cargo test -p agentlinux-core reuse` green; `agentlinux reuse-decision claude-code` prints the correct token for each REUSE-03 case (RUST-03 provisioner)
- 13-reuse.bats REUSE-03 @tests green against the Rust build behind the shim, zero newly-skipped tests, 14-remediate.bats smoke green (GATE-01) — verified at the human checkpoint via targeted Docker containers
- `test.yml` has a gated `rust` job (fmt/clippy/test/musl-ldd) + `rust/**` in the changes filter; YAML parses (RUST-02)
- `git diff --name-only tests/bats/` is empty (zero bats edits)
- `53-METRICS.md` records per-unit iterations-to-green, the bulk-port verdict, the RUST-02 reading, and the GATE-05 rollback note (RUST-03, GATE-05)
- master untouched; rust track additive + revertible (GATE-05)
</verification>

<success_criteria>
- `reuse::agent_decision` computed in Rust, reachable through the unchanged bats/provisioner interface via a ~3-line shim, six REUSE-03 branches correct (RUST-03 provisioner)
- 13-reuse.bats green on the Rust build with zero bats edits and zero newly-skipped tests (GATE-01)
- Gated `rust` CI job (build + clippy -D warnings + rustfmt --check + cargo test + static-musl ldd) added to test.yml on every PR (RUST-02)
- `53-METRICS.md` captures the agent-loop cost, the bulk-port go/no-go verdict, and the GATE-05 rollback note (RUST-03, GATE-05)
- master stays shippable; the rust track is additive, gated, and per-phase revertible (GATE-05)
</success_criteria>

<output>
Create `.planning/phases/53-rust-scaffold-de-risking-spike/53-02-SUMMARY.md` when done. Note which bats files were exercised at the checkpoint (and their pass/skip counts) and confirm no `tests/bats/` file was modified.
</output>
