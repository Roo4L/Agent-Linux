---
phase: 59-full-validation-gate
plan: 03
type: execute
wave: 3
depends_on: ["59-01", "59-02"]
autonomous: true
files_modified:
  - .planning/phases/59-full-validation-gate/59-COVERAGE.md
  - .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md
requirements: [GATE-03, GATE-04, GATE-05]
must_haves:
  truths:
    - "The `behavior-coverage-auditor` subagent runs against `.planning/REQUIREMENTS.md` (v0.4.0 families RUST/TEST/CORE/VERB/PROV/DIST/GATE/ARCH/PERF) AND the legacy behavior families present in the bats `@test` names (BHV/RT/AGT/CLI/CAT/INST/HRN/TST/DOC), emits a coverage report to `.planning/phases/59-full-validation-gate/59-COVERAGE.md`, and its final line is `TST-07 gate: GREEN` with ZERO Uncovered — legacy HRN/DOC/TST classified as verified-elsewhere (tests/harness/, artifact checks) rather than uncovered (GATE-03)."
    - "AGT-02 (GATE-04 keystone) is validated on the RUST chain, NOT the Bash path: the check runs `tests/docker/run.sh <distro> 51-agt02-release-gate` so run.sh's default pre-provisions with the Rust musl `provision` (creating the `~agent/.npm-global/bin/agentlinux` symlink) BEFORE 51's setup_file runs — so setup_file's symlink-present branch is taken and the fallback `bash plugin/bin/agentlinux-install` (the isolated-invocation false-green trap at 51:37-39) is NOT fired. `claude update` exits 0 with zero EACCES on the Rust build; a real `claude update` smoke runs where the CDN is reachable."
    - "The isolated-invocation false-green trap is EXPLICITLY avoided + documented: a bare `bats 51-*.bats` (or `run.sh <t> 51` without the full provisioning) would let setup_file re-run the Bash entrypoint (51:38) and silently validate the Bash path. The GATE-04 check MUST go through run.sh's default (which pre-provisions with Rust) and assert the running `agentlinux` resolves to the musl bin — never the Bash entrypoint."
    - "The GATE-05 declaration is recorded in `.planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md`: the Rust track is DECLARED master-ready; the TS/Bash rollback substrate (`plugin/cli/`, `plugin/bin/agentlinux-install`, the retained Bash reuse map/shim/iterators) is RETAINED (NO cutover deletion this phase); the `AGENTLINUX_LEGACY_TS=1` rollback lever is demonstrated green (a per-file `AGENTLINUX_LEGACY_TS=1 run.sh <t> <file>` restores Bash+TS); master is shippable. The full Docker+QEMU+live-CDN matrix is documented as GREEN-IN-CI (the pipeline gate), NOT faked locally."
  artifacts:
    - .planning/phases/59-full-validation-gate/59-COVERAGE.md
    - .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md
  key_links:
    - "GATE-04's correctness hinges on the provisioning ORDER: run.sh's default path provisions with the Rust musl `provision` at the top of the run (creating the canonical `.npm-global/bin/agentlinux` symlink), so when 51-agt02-release-gate.bats's setup_file (51:37-39) checks for that symlink it is PRESENT and the Bash-entrypoint fallback does NOT fire — the whole chain (provisioner + `agentlinux install --force claude-code` + `claude update`) is the Rust build. This is why the check runs via run.sh, not a bare bats invocation."
    - "GATE-05 is a DECLARATION, not a deletion: PROV-02's 'retired at the Phase-59 entrypoint cutover' phrasing refers to the eventual entrypoint swap that Phase 59 makes POSSIBLE (by proving the gate green), NOT to a deletion inside this phase. CONTEXT + ROADMAP resolve the boundary: Phase 59 declares ready; the master-merge + TS/Bash deletion is the post-milestone cutover. This plan plans NO deletion tasks — only the declaration + the rollback-retained assertion."
    - "This wave depends on Wave 1 (red-free suite: 13-reuse 32/32 + harness green) AND Wave 2 (QEMU re-wired at Rust + Docker gates confirmed Rust): the coverage audit + AGT-02 + the master-ready declaration are only truthful on a red-free, correctly-wired gate."
---

<objective>
Wave 3 — close the gate: run the `behavior-coverage-auditor` (GATE-03, emit
`59-COVERAGE.md`, zero Uncovered); validate the AGT-02 keystone (GATE-04,
`claude update` zero-EACCES on the RUST chain via run.sh's default provisioning,
explicitly avoiding the isolated-invocation false-green trap); and record the
GATE-05 declaration (`59-GATE-DECLARATION.md`: Rust track master-ready, TS/Bash
rollback RETAINED, `AGENTLINUX_LEGACY_TS=1` lever demonstrated, master shippable).

Purpose: this is the FINAL wave of the FINAL phase — it drives the milestone to a
green gate. GATE-03 proves no behavior family lost evidence in the port; GATE-04
proves the keystone acceptance test (self-update without sudo, zero EACCES) on
the Rust build; GATE-05 declares the Rust track ready WITHOUT the cutover deletion.
Output: `59-COVERAGE.md` (TST-07 GREEN), a green AGT-02 on the Rust chain, and
`59-GATE-DECLARATION.md` (the master-ready declaration + the CI-gated matrix status).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-full-validation-gate/59-RESEARCH.md
@.planning/phases/59-full-validation-gate/59-VALIDATION.md
@.planning/phases/59-full-validation-gate/59-01-PLAN.md
@.planning/phases/59-full-validation-gate/59-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `.planning/phases/59-full-validation-gate/59-COVERAGE.md` — the
  `behavior-coverage-auditor` report (GATE-03): every requirement family
  Covered / Uncovered / Partial with file paths, ending `TST-07 gate: GREEN`.
- `.planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md` — the GATE-05
  declaration: Rust track master-ready; TS/Bash rollback retained (no deletion);
  `AGENTLINUX_LEGACY_TS=1` lever demonstrated; the full Docker+QEMU+live-CDN
  matrix status (green-in-CI / pipeline-gated, not faked).
Downstream: NONE — Phase 59 is the final phase. On a green gate, the milestone
proceeds to audit → complete → cleanup (post-milestone: the actual master-merge +
TS/Bash cutover deletion, which is OUT OF SCOPE here).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: run behavior-coverage-auditor (GATE-03) → emit 59-COVERAGE.md; zero Uncovered, TST-07 gate GREEN</name>
  <files>.planning/phases/59-full-validation-gate/59-COVERAGE.md</files>
  <read_first>
    - .claude/agents/behavior-coverage-auditor.md [:1-89] (the auditor's rubric: extract every `<FAMILY>-<NUMBER>` ID from REQUIREMENTS.md; for behavior/installer IDs grep `tests/bats/`, for harness/docs/ops IDs inspect the designated harness/artifact evidence; report Covered/Uncovered/Partial; emit `TST-07 gate: RED|GREEN`; do NOT force every ID into bats — classify non-bats IDs as "verified elsewhere — see <path>")
    - .planning/REQUIREMENTS.md [:29-114] (the v0.4.0 families RUST/TEST/CORE/VERB/PROV/DIST/GATE + ARCH/PERF v2 + the traceability table; GATE-02/03/04 are THIS phase)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"GATE-03 Coverage Audit" (the family→evidence map on the Rust build; the note that HRN/TST/DOC evidence lives OUTSIDE tests/bats/ — HRN-* in tests/harness/, DOC-* in artifact checks — and must be classified "verified elsewhere", NOT uncovered; and that the audit is EXPECTED green already since the rewrite was per-phase-gated — its job is to CONFIRM no family lost evidence in the port)
  </read_first>
  <action>
    Spawn the `behavior-coverage-auditor` subagent (it is read-only:
    Read/Grep/Glob/Bash) and capture its report to
    `.planning/phases/59-full-validation-gate/59-COVERAGE.md`.

    Feed it BOTH:
    - the v0.4.0 `.planning/REQUIREMENTS.md` families (RUST/TEST/CORE/VERB/PROV/
      DIST/GATE; ARCH/PERF are v2/out-of-scope — the auditor should mark them
      deferred, not uncovered), AND
    - the LEGACY behavior families whose IDs live in the bats `@test` NAMES rather
      than in REQUIREMENTS.md (BHV/RT/AGT/CLI/CAT/INST/HRN/TST/DOC) — note to the
      auditor that GATE-03 requires these retain evidence on the Rust build too.

    The auditor must classify non-bats evidence correctly (per its own rubric):
    - HRN-* → verified in `tests/harness/` (run by `tests/harness/run.sh`) — NOT
      uncovered.
    - DOC-* → verified by artifact/doc checks — NOT uncovered.
    - TST-* → partly harness scaffolding — classify per evidence, NOT silently
      omitted.

    GATE-03 pass condition: the report's final line is `TST-07 gate: GREEN` with
    ZERO Uncovered. If the auditor reports ANY Uncovered family (a family that
    genuinely lost evidence in the port), STOP and surface it — a test must be
    added (or the elsewhere-evidence documented) before the gate can close; do NOT
    hand-wave an Uncovered into "verified elsewhere" without a real path.

    Write the full auditor report verbatim to `59-COVERAGE.md` (grouped by family,
    table-shaped, with the `TST-07 gate: GREEN` summary line) so the coverage
    evidence is a durable phase artifact.

    This task installs no packages and touches no source/spec — it runs the
    read-only auditor and records its report.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && test -f .planning/phases/59-full-validation-gate/59-COVERAGE.md && grep -qE 'TST-07 gate:\s*GREEN' .planning/phases/59-full-validation-gate/59-COVERAGE.md && echo "TST07-GREEN" && ! grep -qiE '^\|\s*[A-Z]+-[0-9]+\s*\|\s*Uncovered' .planning/phases/59-full-validation-gate/59-COVERAGE.md && echo "ZERO-UNCOVERED" && grep -qiE 'RUST|VERB|PROV|DIST|GATE' .planning/phases/59-full-validation-gate/59-COVERAGE.md && echo "V040-FAMILIES-PRESENT" && grep -qiE 'BHV|INST|AGT|CLI' .planning/phases/59-full-validation-gate/59-COVERAGE.md && echo "LEGACY-FAMILIES-PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `59-COVERAGE.md` exists and ends with `TST-07 gate: GREEN` (`TST07-GREEN`)
      with ZERO Uncovered families (`ZERO-UNCOVERED`).
    - The report covers both the v0.4.0 families (`V040-FAMILIES-PRESENT`) and the
      legacy behavior families (`LEGACY-FAMILIES-PRESENT`); HRN/DOC/TST are
      classified "verified elsewhere" with a path, not omitted.
    - Any genuinely-Uncovered family was surfaced as a blocker (a test added or the
      elsewhere-evidence documented) — not hand-waved.
    - No source, spec, or workflow was modified — the auditor ran read-only and its
      report was recorded.
  </acceptance_criteria>
  <done>
    The behavior-coverage-auditor reports zero Uncovered on the Rust build across
    the v0.4.0 + legacy families; `59-COVERAGE.md` records the durable evidence and
    ends `TST-07 gate: GREEN`. GATE-03 is met.
  </done>
</task>

<task type="auto">
  <name>Task 2: validate the AGT-02 keystone (GATE-04) on the RUST chain via run.sh's default provisioning — avoid the isolated-invocation false-green trap; zero EACCES</name>
  <files>.planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md</files>
  <read_first>
    - tests/bats/51-agt02-release-gate.bats [:1-98] (the keystone: setup_file [:29-47] — the CRITICAL false-green trap at :37-39, which re-runs `bash plugin/bin/agentlinux-install` (the BASH entrypoint) ONLY IF the `~agent/.npm-global/bin/agentlinux` symlink is ABSENT; the @test [:53-98] — records before_version, runs `timeout 120s sudo -u agent -H bash --login -c 'claude update'`, asserts `assert_exit_zero` + `assert_no_eacces` + version monotonicity)
    - tests/docker/run.sh [:309-338] (the default Rust path: it provisions with the musl `provision` at the TOP of the run — creating the canonical `.npm-global/bin/agentlinux` symlink — BEFORE bats runs; this is WHY running 51 via run.sh's default makes setup_file's symlink-present branch fire, NOT the Bash-entrypoint fallback) + [:86-99] (the 2nd-positional per-file selection) + [:414-424] (AGENTLINUX_RUST_BIN export)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"GATE-04 — the keystone" (the full-suite-ordering vs isolated-invocation caveat; the mitigation: run the AGT-02 dev check via run.sh's default (pre-provisions with Rust) OR pre-stage the Rust provisioner + AGENTLINUX_RUST_BIN; verify the running agentlinux is the musl bin) + §"Pitfall 2" (the TS/Bash couplings in 51-*.bats — package.json + agentlinux-install — that false-green an ISOLATED run)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"Environment Availability" (the CDN `storage.googleapis.com` is HTTP-reachable this session; `claude`/`cargo`/`docker` present — the dev AGT-02 floor is runnable; the ENFORCING live-CDN gate is release.yml gate-2/gate-3 in CI)
  </read_first>
  <action>
    Validate AGT-02 (GATE-04) on the RUST chain, explicitly avoiding the
    isolated-invocation false-green trap the research flagged.

    Run the AGT-02 check via run.sh's DEFAULT path — `tests/docker/run.sh
    <distro> 51-agt02-release-gate` — NOT a bare `bats 51-*.bats`. run.sh's default
    provisions with the Rust musl `provision` at the top of the run, creating the
    canonical `~agent/.npm-global/bin/agentlinux` symlink; so when 51's setup_file
    (51:37-39) checks for that symlink it is PRESENT, the fallback
    `bash plugin/bin/agentlinux-install` (the Bash-entrypoint false-green trap)
    does NOT fire, and the whole chain (provisioner + `agentlinux install --force
    claude-code` + `claude update`) is the Rust build.

    Assert / document that the running provisioner IS the musl bin (defends the
    Pitfall-2 false-green): confirm run.sh's default Rust path ran (the run banner
    "run Rust provisioner (agentlinux provision) [default]", run.sh:310) and that
    the isolated-invocation fallback did NOT re-run the Bash entrypoint — i.e. the
    check went through the full default provisioning, not a bare bats invocation.

    The 51-agt02-release-gate.bats @test asserts: `claude update` exits 0,
    `assert_no_eacces` (zero EACCES / permission-denied — THE permission invariant
    the project exists to prove), and version monotonicity. Capture the result.

    Also run a targeted REAL `claude update` smoke where the CDN is reachable
    (`storage.googleapis.com` probed reachable) as the dev-floor keystone proof:
    `sudo -u agent -H bash --login -c 'claude update'` on the Rust-provisioned
    chain, confirming exit 0 + no EACCES. Record the transcript's EACCES-free
    status.

    Mark clearly (in the notes that feed 59-GATE-DECLARATION.md in Task 3) which
    part is DEV-RUNNABLE (the Docker per-file 51 run on the Rust build + the
    targeted `claude update` smoke) vs CI/QEMU-GATED (the ENFORCING live-CDN
    AGT-02 gate is release.yml gate-2 (Docker) + gate-3 (QEMU) against the live
    Anthropic CDN — proven by the pipeline on the Rust path after the Wave-2
    boot.sh re-wire, documented NOT faked). Do NOT claim a local full live-CDN
    QEMU pass.

    This task runs tests + a smoke; it modifies no source/spec. It appends its
    AGT-02 findings into `59-GATE-DECLARATION.md` (created/finalized in Task 3) or
    a scratch note Task 3 consumes — keep the artifact single-authored by Task 3.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && echo "== AGT-02 on the Rust chain via run.sh default (NOT bare bats — avoids the 51:37-39 Bash fallback) ==" && ./tests/docker/run.sh ubuntu-24.04 51-agt02-release-gate 2>&1 | tee /tmp/agt02-run.log | tail -12 && grep -qE 'run Rust provisioner \(agentlinux provision\) \[default\]' /tmp/agt02-run.log && echo "RUST-PROVISION-RAN" && grep -qiE 'AGT-02.*(ok|pass)|1\.\.[0-9]+' /tmp/agt02-run.log && echo "AGT02-RESULT-CAPTURED" && ! grep -qiE 'EACCES|permission denied' /tmp/agt02-run.log && echo "NO-EACCES-IN-RUN"</automated>
  </verify>
  <acceptance_criteria>
    - AGT-02 (`51-agt02-release-gate`) is validated via run.sh's DEFAULT Rust path
      (`RUST-PROVISION-RAN` — the "run Rust provisioner [default]" banner appears)
      — NOT a bare `bats 51-*.bats`, so the 51:37-39 Bash-entrypoint fallback did
      NOT fire (the isolated-invocation false-green trap is avoided).
    - The AGT-02 result is captured (`AGT02-RESULT-CAPTURED`) and the run shows no
      EACCES / permission-denied (`NO-EACCES-IN-RUN`) — the permission invariant
      holds on the Rust build.
    - A targeted real `claude update` smoke ran where the CDN is reachable
      (exit 0, no EACCES) as the dev-floor keystone proof; the ENFORCING live-CDN
      gate is documented as release.yml gate-2/gate-3 (CI), not faked locally.
    - The dev-runnable vs CI/QEMU-gated split is explicit in the findings fed to
      Task 3 (Docker per-file 51 + `claude update` smoke = dev; live-CDN QEMU =
      pipeline gate).
    - No source/spec/workflow was modified — this task runs tests + a smoke.
  </acceptance_criteria>
  <done>
    AGT-02 is green on the RUST chain (via run.sh's default provisioning, avoiding
    the isolated-invocation Bash false-green): `claude update` exits 0 with zero
    EACCES; the dev smoke confirms the keystone; the live-CDN QEMU gate is the
    documented CI pipeline gate. GATE-04's dev floor is met + correctly wired.
  </done>
</task>

<task type="auto">
  <name>Task 3: record the GATE-05 declaration (59-GATE-DECLARATION.md) — Rust track master-ready, TS/Bash rollback RETAINED (no cutover deletion), AGENTLINUX_LEGACY_TS=1 lever demonstrated, master shippable</name>
  <files>.planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md</files>
  <read_first>
    - tests/docker/run.sh [:297-308] (the `AGENTLINUX_LEGACY_TS=1` inverse lever — restores the Bash+TS distribution end-to-end, fail-loud if `dist/index.js` is absent; the demonstration target for GATE-05)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"The Cutover Boundary — Phase 59 declares, it does NOT delete (GATE-05)" (the reconciliation: PROV-02's 'retired at the Phase-59 entrypoint cutover' refers to the eventual swap Phase 59 makes POSSIBLE, NOT a deletion inside this phase; CONTEXT + ROADMAP resolve in favor of DECLARE-not-delete; plan NO deletion tasks — only the declaration + the rollback-retained assertion) + §"The Full-Matrix Reality" (how the phase asserts GATE-02 without faking a local full-matrix pass: the FULL Docker+QEMU+live-CDN matrix is GREEN-IN-CI / the pipeline gate, proven by the PR CI + nightly/release, documented not faked)
    - .planning/ROADMAP.md [:150-162] (the Phase 59 success criteria — the 5 truths the declaration must map to: GATE-02 Docker+QEMU, GATE-03 zero-uncovered, GATE-04 zero-EACCES, GATE-01 no-regression/no-newly-skipped, GATE-05 master shippable + Rust ready + TS/Bash retained)
    - .planning/phases/59-full-validation-gate/59-CONTEXT.md [:31-36, :119-124] (the deferred final cutover — deleting plugin/cli/ + the Bash entrypoint + the Bash reuse map is the POST-milestone step, NOT this phase)
    - .planning/phases/59-full-validation-gate/59-01-PLAN.md + 59-02-PLAN.md (the Wave 1 reds-resolved + Wave 2 QEMU-re-wire outcomes the declaration cites as evidence)
  </read_first>
  <action>
    Write `.planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md` — the
    milestone-closing declaration that the Rust track is master-ready. Structure
    it as a gate scorecard mapping each of the 5 ROADMAP Phase-59 success criteria
    to its evidence:

    - GATE-01 (no regression / no newly-skipped): the 3 carried reds resolved
      (Wave 1 — 13-reuse 32/32 on the Rust build; HRN-05 + HRN-06 green;
      `tests/harness/run.sh` green). Cite 59-01.
    - GATE-02 (full contract on Rust, Docker + QEMU): Docker gates run Rust by
      default (run.sh default, confirmed Wave 2); QEMU gate re-wired at the Rust
      `provision` with the provisioner-identity guard (Wave 2, boot.sh). The FULL
      matrix (all-tests Docker × 4 distros + full QEMU × 4 + live-CDN) is
      GREEN-IN-CI — the pipeline gate (test.yml bats-docker on the PR + nightly/
      release gate-2/gate-3), documented NOT faked locally (the dev VM OOMs on the
      full Docker suite + has no KVM). Cite 59-02 + the dev-runnable per-file floor.
    - GATE-03 (zero uncovered): the behavior-coverage-auditor report
      (59-COVERAGE.md), TST-07 gate GREEN. Cite Task 1.
    - GATE-04 (AGT-02 zero-EACCES on Rust vs live CDN): the AGT-02 keystone green
      on the Rust chain (Task 2, via run.sh's default provisioning, false-green
      trap avoided); the live-CDN enforcing gate is release.yml gate-2/gate-3 (CI).
      Cite Task 2.
    - GATE-05 (master shippable; Rust ready; TS/Bash retained): DECLARE the Rust
      track master-ready. DEMONSTRATE the `AGENTLINUX_LEGACY_TS=1` rollback lever
      restores Bash+TS (a per-file `AGENTLINUX_LEGACY_TS=1 run.sh <t> <file>`
      green). ASSERT the rollback substrate is RETAINED: `plugin/cli/` (TS),
      `plugin/bin/agentlinux-install` (Bash entrypoint), and the retained Bash
      reuse map/shim/iterators (`plugin/lib/reuse/agents.sh` + remediate/prompt
      iterators) are all still present — NO cutover deletion this phase.

    State the CUTOVER BOUNDARY explicitly: Phase 59 DECLARES the Rust track
    master-ready; the actual master-merge + TS/Bash deletion is the POST-milestone
    cutover (once the maintainer green-lights it). This phase plans + performs NO
    deletion.

    Include a "Dev-runnable vs CI/QEMU-gated" section restating the honest split:
    what was proven locally (per-file bats on Rust × 4 distros, coverage audit,
    AGT-02 dev smoke, LEGACY_TS rollback smoke) vs what is the pipeline gate (full
    Docker matrix, full QEMU matrix, live-CDN AGT-02) — no faked local full-matrix
    or QEMU pass.

    This task writes ONE declaration artifact; it deletes nothing and modifies no
    source/spec/workflow.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && test -f .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md && grep -qiE 'master-ready|ready to become master|master shippable' .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md && echo "MASTER-READY-DECLARED" && grep -qiE 'retain|rollback|NOT delete|no.{0,3}deletion|post-milestone cutover' .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md && echo "ROLLBACK-RETAINED-ASSERTED" && for f in GATE-01 GATE-02 GATE-03 GATE-04 GATE-05; do grep -q "$f" .planning/phases/59-full-validation-gate/59-GATE-DECLARATION.md || { echo "MISSING $f"; exit 1; }; done && echo "ALL-GATES-SCORED" && test -d plugin/cli && test -f plugin/bin/agentlinux-install && echo "TS-BASH-SUBSTRATE-PRESENT" && echo "== LEGACY_TS rollback lever demonstrated ==" && AGENTLINUX_LEGACY_TS=1 ./tests/docker/run.sh ubuntu-24.04 10-installer 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `59-GATE-DECLARATION.md` exists and DECLARES the Rust track master-ready
      (`MASTER-READY-DECLARED`) and asserts the TS/Bash rollback substrate is
      RETAINED / no cutover deletion (`ROLLBACK-RETAINED-ASSERTED`).
    - All five gates are scored with evidence (`ALL-GATES-SCORED`: GATE-01..05).
    - The TS/Bash substrate is physically present (`TS-BASH-SUBSTRATE-PRESENT`:
      `plugin/cli/` + `plugin/bin/agentlinux-install`) — NO deletion happened.
    - The `AGENTLINUX_LEGACY_TS=1` rollback lever is DEMONSTRATED green (Bash+TS
      `10-installer` passes under the flag) — GATE-05's rollback works.
    - The declaration states the CUTOVER BOUNDARY (Phase 59 declares; the
      master-merge + TS/Bash deletion is the post-milestone cutover) and the
      honest dev-runnable vs CI/QEMU-gated split (no faked local full-matrix/QEMU).
  </acceptance_criteria>
  <done>
    `59-GATE-DECLARATION.md` records the master-ready declaration with all five
    gates scored; the `AGENTLINUX_LEGACY_TS=1` rollback lever is demonstrated; the
    TS/Bash substrate is retained (no cutover deletion); the cutover boundary +
    the honest dev/CI split are documented. GATE-05 is met — the gate is CLOSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the coverage claim → the actual evidence | GATE-03's zero-uncovered claim must be backed by the auditor's real grep/Read over REQUIREMENTS.md + tests/; a hand-waved "verified elsewhere" without a path is a false coverage claim. |
| the AGT-02 check → the provisioner it exercises | GATE-04's zero-EACCES claim is only true if the chain is the RUST build; the isolated-invocation trap (51:37-39 Bash fallback) would silently validate the Bash path — run.sh's default provisioning + the identity confirmation prevents it. |
| the gate declaration → the actual repo state | GATE-05's "TS/Bash retained" claim must match the tree (`plugin/cli/` + the Bash entrypoint present); the declaration is verified against the physical substrate, not asserted on faith. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-07 | Repudiation | a false GATE-04 green on the Bash path (isolated-invocation trap) | high | mitigate | The AGT-02 check runs via run.sh's DEFAULT (which pre-provisions with the Rust musl `provision`, creating the symlink so 51's setup_file does NOT re-run the Bash entrypoint); the verify asserts the "run Rust provisioner [default]" banner + no EACCES — the keystone is proven on the Rust chain, not the Bash path. |
| T-59-08 | Tampering | a coverage report that omits a lost-evidence family | medium | mitigate | The auditor's rubric forbids silent omission (every non-bats ID is "verified elsewhere — see <path>" or "uncovered"); the verify asserts zero `Uncovered` rows + `TST-07 gate: GREEN`; a genuine Uncovered is surfaced as a blocker, not hand-waved. |
| T-59-09 | Elevation of Privilege / Integrity | an unintended cutover deletion breaking the rollback | high | mitigate | This plan plans NO deletion tasks; the verify asserts `plugin/cli/` + `plugin/bin/agentlinux-install` are PRESENT and demonstrates the `AGENTLINUX_LEGACY_TS=1` lever green — the GATE-05 rollback stays intact; the cutover is explicitly post-milestone. |
| T-59-SC | Tampering | cargo/npm installs (none) | low | accept | This wave runs the read-only auditor + bats tests + a `claude update` smoke + writes two planning docs — no package installs. |
</threat_model>

<verification>
- `behavior-coverage-auditor` reports zero Uncovered on the Rust build across the
  v0.4.0 + legacy families; `59-COVERAGE.md` ends `TST-07 gate: GREEN` (GATE-03).
- AGT-02 is green on the RUST chain via run.sh's default provisioning (the
  isolated-invocation false-green trap avoided — the Bash-entrypoint fallback does
  NOT fire); `claude update` exit 0 + zero EACCES; the live-CDN gate is the CI
  pipeline (GATE-04).
- `59-GATE-DECLARATION.md` declares the Rust track master-ready, scores all five
  gates, asserts the TS/Bash rollback substrate RETAINED (no deletion), and
  demonstrates the `AGENTLINUX_LEGACY_TS=1` lever (GATE-05).
- The full Docker+QEMU+live-CDN matrix is documented as GREEN-IN-CI / the pipeline
  gate — NOT faked locally (dev VM OOMs on the full Docker suite + has no KVM).
- The cutover boundary is explicit: Phase 59 declares ready; the master-merge +
  TS/Bash deletion is the post-milestone cutover (out of scope here).
</verification>

<success_criteria>
- GATE-03: `behavior-coverage-auditor` reports zero Uncovered (TST-07 GREEN);
  `59-COVERAGE.md` is the durable evidence.
- GATE-04: AGT-02 (`claude update` zero-EACCES) green on the RUST chain — the
  keystone acceptance test the whole project exists to pass, proven on the Rust
  build via run.sh's default provisioning (false-green trap avoided).
- GATE-05: the Rust track is DECLARED master-ready; the TS/Bash rollback substrate
  is retained (no cutover deletion); the `AGENTLINUX_LEGACY_TS=1` lever is
  demonstrated; master is shippable — recorded in `59-GATE-DECLARATION.md`.
- The gate is CLOSED honestly: the full matrix is CI/QEMU-gated (documented, not
  faked); the cutover is post-milestone. The milestone can proceed to audit →
  complete → cleanup.
</success_criteria>

<output>
Create `.planning/phases/59-full-validation-gate/59-03-SUMMARY.md` when done
</output>
