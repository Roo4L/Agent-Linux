---
phase: 59-full-validation-gate
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
autonomous: true
files_modified:
  - tests/qemu/boot.sh
requirements: [GATE-02]
must_haves:
  truths:
    - "`tests/qemu/boot.sh` is re-pointed at the Rust musl `provision`: the in-guest install invocation at `boot.sh:531` (`bash plugin/bin/agentlinux-install`, the legacy Bash/TS entrypoint) is replaced by invoking the STAGED MUSL BIN `plugin/bin/agentlinux provision --user agent --yes` (the tarball payload places the musl bin at `plugin/bin/agentlinux` per build-release.sh) — mirroring the Docker default at run.sh:337. The QEMU release gate now proves the RUST provisioner, not the Bash one."
    - "A concrete PROVISIONER-IDENTITY assertion is added so a future regression to the Bash path is caught mechanically (not just an exec swap): boot.sh asserts (before or at install-time, and fails the run non-zero on mismatch) that the artifact being run IS the static-musl bin — e.g. `readelf -l plugin/bin/agentlinux` shows NO PT_INTERP (statically linked) and/or `file`/`ldd` reports 'not a dynamic executable' / statically linked. A dynamically-linked or Bash-shebang'd `agentlinux` fails the guard — no false GATE-02 green on the Bash path (the #1 risk)."
    - "The `AGENTLINUX_LEGACY_TS=1` rollback branch is honored in boot.sh (mirroring run.sh:297-308): when set, boot.sh execs the Bash `plugin/bin/agentlinux-install` entrypoint (the retained GATE-05 rollback substrate) instead of the Rust `provision`; the default (no flag) is the Rust path. The Bash entrypoint is RETAINED, not deleted (GATE-05)."
    - "`test.yml` bats-docker + `release.yml` gate-2/gate-4 are CONFIRMED (asserted, not assumed) to run the Rust build by default — they call `bash tests/docker/run.sh <target>` with no override, and run.sh's default path is the Rust musl `provision` (Phase 58). No workflow edit is needed for the Docker gates; the QEMU wiring gap is the ONLY GATE-02 code change, and it lives entirely inside boot.sh (both `nightly-qemu.yml:124` and `release.yml:226` already call `boot.sh <target>` unchanged)."
  artifacts:
    - tests/qemu/boot.sh
  key_links:
    - "The tarball payload layout is the key that makes the boot.sh swap clean: build-release.sh places the static musl bin at `plugin/bin/agentlinux` in the release tarball, and boot.sh already extracts that tarball into `/opt/agentlinux-src` (boot.sh:527-529), so after extraction the musl bin is at `/opt/agentlinux-src/plugin/bin/agentlinux` — the swap invokes THAT (`plugin/bin/agentlinux provision --user agent --yes`), no separate scp/host-build/docker-cp needed (unlike run.sh's Docker path which docker-cp's a host-built bin)."
    - "The provisioner-identity assertion defends Risk #1 (a CI gate silently running the OLD Bash path = false GATE-02 green). It is the mechanical guard the research's Open Question #3 asked for: without it, a regression that reverts boot.sh:531 to the Bash entrypoint would pass a green QEMU gate while proving the wrong provisioner."
    - "Wave 2 depends on Wave 1: the QEMU matrix (and the Docker matrix it confirms) is only MEANINGFUL on a red-free suite — `13-reuse` #29 must already be 32/32 (Wave 1) before the full-matrix gate is declared, else the matrix carries a known red."
---

<objective>
Wave 2 — wire the QEMU gate to the Rust build (GATE-02's one real code change):
re-point `tests/qemu/boot.sh` from the legacy Bash entrypoint
(`bash plugin/bin/agentlinux-install`, boot.sh:531) to the STAGED MUSL BIN
(`plugin/bin/agentlinux provision --user agent --yes`) — mirroring the Docker
default (run.sh:337) — and add a concrete provisioner-IDENTITY assertion so the
running provisioner is proven to BE the musl bin (a regression to the Bash path
is caught mechanically). Honor the `AGENTLINUX_LEGACY_TS=1` rollback branch
(GATE-05). Confirm (assert, don't assume) the Docker gates already run Rust.

Purpose: close the #1 risk — the QEMU release/nightly gate currently runs the
BASH provisioner, so declaring GATE-02 on the current QEMU green is a FALSE pass.
This is the only GATE-02 wiring gap; the Docker matrix already defaults to Rust.
Output: a boot.sh that runs + asserts the Rust musl `provision` (default), with
the Bash rollback lever retained; the workflows unchanged (they call boot.sh).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-validation-gate/59-RESEARCH.md
@.planning/phases/59-full-validation-gate/59-VALIDATION.md
@.planning/phases/59-full-validation-gate/59-01-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- `tests/qemu/boot.sh` — the in-guest install invocation (currently
  `bash plugin/bin/agentlinux-install`, :531) re-pointed at the staged musl bin
  `plugin/bin/agentlinux provision --user agent --yes` (default, Rust); the
  `AGENTLINUX_LEGACY_TS=1` branch execs the Bash entrypoint (retained rollback);
  a provisioner-identity assertion (static-musl guard) added so a regression to
  the Bash path fails the run non-zero.
Downstream: Wave 3 (59-03) runs the coverage audit + validates AGT-02 on the Rust
chain + declares master-ready. The FULL QEMU matrix + live-CDN gate run in CI
(nightly-qemu.yml / release.yml gate-3, which call boot.sh unchanged) — documented
as the pipeline gate, NOT run locally (no KVM in the dev VM).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: re-point tests/qemu/boot.sh at the Rust musl `provision` (mirror run.sh:337) with the AGENTLINUX_LEGACY_TS=1 Bash rollback branch retained</name>
  <files>tests/qemu/boot.sh</files>
  <read_first>
    - tests/qemu/boot.sh [:476-532] (READ IN FULL the source-staging + install region: the build-release.sh tarball build [:483-494]; the scp of the release tarball + tests tarball into the guest [:513-515]; the REMOTE_INSTALL heredoc [:523-532] that `tar -xzf`'s the release tarball into `/opt/agentlinux-src` [:527-529] and runs `bash plugin/bin/agentlinux-install` [:531] — THIS is the swap point) + [:533-559] (the REMOTE_BATS heredoc that runs the full bats suite — unchanged)
    - tests/docker/run.sh [:309-338] (the Docker DEFAULT Rust path to MIRROR: build/stage the musl bin, then invoke `agentlinux provision --user agent --yes` as root [:337]; and the `AGENTLINUX_LEGACY_TS=1` rollback branch [:297-308] that execs `bash plugin/bin/agentlinux-install` with a fail-loud guard if the TS bundle is absent [:304-307])
    - scripts/build-release.sh [:10-13, :192-204] (the tarball PAYLOAD places the static musl bin at `plugin/bin/agentlinux` — so after boot.sh's `tar -xzf` into `/opt/agentlinux-src`, the musl bin is at `/opt/agentlinux-src/plugin/bin/agentlinux`; this is why the swap needs no separate build/scp of the bin)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"nightly-qemu.yml + tests/qemu/boot.sh — THE WIRING GAP" (the swap requirement + the note that the transport differs from Docker: the guest gets the bin via the release tarball, already scp'd) + §"Pitfall 1" (the false-GATE-02 risk + the "assert the running provisioner IS the musl bin" mitigation)
    - .planning/phases/58-.../58-03-PLAN.md [Task 1] (how run.sh folded the flags into the default + added the AGENTLINUX_LEGACY_TS=1 lever with fail-loud discipline — the pattern boot.sh mirrors)
  </read_first>
  <action>
    Re-point the in-guest install invocation inside the REMOTE_INSTALL heredoc
    (boot.sh:523-532) from the legacy Bash entrypoint to the staged Rust musl
    `provision`, mirroring the Docker default (run.sh:337). After the tarball is
    extracted into `/opt/agentlinux-src`, the static musl bin is at
    `/opt/agentlinux-src/plugin/bin/agentlinux` (build-release.sh payload) — so
    the guest already HAS the bin; no separate host-build/scp of the bin is
    needed (this is the transport difference from run.sh's docker-cp path).

    Default (Rust) path — replace `bash plugin/bin/agentlinux-install` (:531)
    with running the musl bin's `provision` verb as root inside the guest:
      `plugin/bin/agentlinux provision --user agent --yes`
    (root is the guest's remote user; the Rust `provision` uses require_root, not
    the CLI-05 guard — parity with run.sh:337). Pass `--user agent` explicitly for
    parity with the Bash entrypoint's target.

    PROVISIONER-IDENTITY assertion (defends Risk #1 — the #1 risk of a false
    GATE-02 green if the gate silently runs the Bash path): BEFORE (or as) the
    provision runs, assert in-guest that `plugin/bin/agentlinux` IS the static
    musl bin and FAIL the run non-zero on mismatch. Concretely, one or more of:
      - `readelf -l /opt/agentlinux-src/plugin/bin/agentlinux` shows no PT_INTERP
        (statically linked), OR
      - `file /opt/agentlinux-src/plugin/bin/agentlinux` reports an ELF
        "statically linked" executable (NOT an ASCII shell script — the Bash
        entrypoint is a shell script), OR
      - `ldd` reports "not a dynamic executable".
    On failure, print a loud diagnostic and `exit 1` from the REMOTE_INSTALL
    heredoc so the QEMU gate goes RED rather than proving the wrong provisioner.
    (`readelf`/`file` are present in the guest base images; if `readelf` is
    absent on a minimal image, fall back to `file` + a shebang check that the bin
    is NOT `#!`-prefixed.)

    AGENTLINUX_LEGACY_TS=1 rollback branch (GATE-05 — mirror run.sh:297-308):
    when the env var is set (forward it into the guest via the ssh SendEnv / the
    heredoc arg the way `$TAG` is passed), exec `bash plugin/bin/agentlinux-install`
    (the retained Bash entrypoint) INSTEAD of the Rust `provision`, so a broken
    Rust path can be rolled back to the shippable Bash+TS build for the QEMU gate
    too. The Bash entrypoint STAYS in the tree (GATE-05 rollback substrate — do
    NOT delete it). If forwarding the env var through the ssh hop is awkward, at
    minimum keep the Bash-entrypoint invocation present under the legacy branch so
    the rollback path is not lost — but prefer honoring the flag for lever parity
    with Docker.

    Keep the REMOTE_BATS heredoc (:533-559) and the rest of boot.sh UNCHANGED —
    the only edit is the install-invocation region + the identity assertion. Do
    NOT touch `nightly-qemu.yml` or `release.yml` (they call `boot.sh <target>`
    unchanged — the swap is entirely inside boot.sh). Do NOT edit any bats spec or
    Rust source.

    Comment-text discipline: no acceptance criterion below negative-greps a
    source literal this action writes into boot.sh; the verify greps boot.sh for
    the POSITIVE presence of `agentlinux provision` and the identity-assert
    tokens, and (informational) counts remaining `agentlinux-install` refs (the
    legacy-branch reference is expected to remain). No allowlist marker needed.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && bash -n tests/qemu/boot.sh && grep -q 'agentlinux provision --user agent --yes' tests/qemu/boot.sh && echo "RUST-PROVISION-WIRED" && grep -qE 'readelf|not a dynamic executable|statically linked' tests/qemu/boot.sh && echo "IDENTITY-ASSERT-PRESENT" && grep -q 'AGENTLINUX_LEGACY_TS' tests/qemu/boot.sh && echo "ROLLBACK-BRANCH-PRESENT" && test -f plugin/bin/agentlinux-install && echo "BASH-ENTRYPOINT-RETAINED" && echo "-- legacy-install refs remaining (expected >=1 under the rollback branch) --" && grep -c 'agentlinux-install' tests/qemu/boot.sh</automated>
  </verify>
  <acceptance_criteria>
    - `boot.sh` invokes `agentlinux provision --user agent --yes` (the staged musl
      bin) in the default path (`RUST-PROVISION-WIRED`); the legacy
      `bash plugin/bin/agentlinux-install` is now under the `AGENTLINUX_LEGACY_TS=1`
      rollback branch only.
    - A provisioner-IDENTITY assertion is present (`IDENTITY-ASSERT-PRESENT`) that
      fails the run non-zero if `plugin/bin/agentlinux` is NOT the static musl bin
      (readelf no-PT_INTERP / `file` statically-linked / `ldd` not-dynamic) — a
      regression to the Bash path cannot false-green the QEMU gate (Risk #1).
    - `AGENTLINUX_LEGACY_TS` is honored in boot.sh (`ROLLBACK-BRANCH-PRESENT`);
      `plugin/bin/agentlinux-install` is RETAINED in the tree
      (`BASH-ENTRYPOINT-RETAINED`) — GATE-05 rollback substrate not deleted.
    - `bash -n tests/qemu/boot.sh` parses clean; the REMOTE_BATS heredoc and the
      rest of boot.sh are unchanged; `nightly-qemu.yml` / `release.yml` are NOT
      edited (they call boot.sh unchanged).
    - The full in-guest QEMU run is CI/QEMU-GATED (no KVM in the dev VM) — the dev
      verification is the static-parse + the grep-asserts above + the identity
      guard; the actual QEMU-on-Rust green is proven by nightly-qemu / release
      gate-3 on the phase's pipeline (documented, not faked locally).
  </acceptance_criteria>
  <done>
    `tests/qemu/boot.sh` runs the Rust musl `provision` by default (mirroring the
    Docker default) with a static-musl provisioner-identity guard that fails the
    run on any regression to the Bash path; the `AGENTLINUX_LEGACY_TS=1` Bash
    rollback branch is honored and the Bash entrypoint retained (GATE-05). The #1
    risk — a false GATE-02 green on the Bash path — is closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: confirm (assert, don't assume) test.yml + release.yml Docker gates build/run the Rust build by default; document the wiring status; no workflow edit needed</name>
  <files>tests/qemu/boot.sh</files>
  <read_first>
    - .github/workflows/test.yml [:288-339] (the `bats-docker` job: matrix = 22.04/24.04/26.04/almalinux-9, `fail-fast: false`, runs `bash tests/docker/run.sh ${{ matrix.target }}` with NO override — so it exercises the Rust `provision` by default) + [:56-64] (the `changes` filter — Phase 59's PR touches `tests/`, so the full matrix runs; note it, do not change it)
    - .github/workflows/release.yml [:127-151] (gate-2-docker: `bash tests/docker/run.sh <target>` × 4 — Rust by default) + [:157-238] (gate-3-qemu: `bash tests/qemu/boot.sh <target>` — now Rust after Task 1's re-wire) + [:246-255] (gate-4-pinned-combo: `run.sh ubuntu-24.04` pinned combo — Rust by default)
    - .github/workflows/nightly-qemu.yml [:115-124] (`bash tests/qemu/boot.sh ${{ matrix.target }}` — now Rust after Task 1)
    - tests/docker/run.sh [:309-338] (the default Rust path — the reason the Docker gates run Rust with no override; confirmed in Phase 58)
    - .planning/phases/59-full-validation-gate/59-RESEARCH.md §"CI Gate Map" (the full map: Docker gates already Rust; QEMU was the only gap, now closed by Task 1)
  </read_first>
  <action>
    Confirm — by reading + grepping the workflows, NOT by assuming — that the
    Docker gates already build/run the Rust build by default, and record the
    wiring status as a documentation comment block in `tests/qemu/boot.sh` (near
    the re-wired install region) so the GATE-02 wiring is self-documenting for the
    executor and any future reader.

    Verify each of these holds (they should, from Phase 58 — this task ASSERTS it,
    closing the research's "confirm, don't assume" instruction):
    - `test.yml` bats-docker (:337-339) calls `bash tests/docker/run.sh <target>`
      with NO override → Rust default.
    - `release.yml` gate-2 (:127-151) + gate-4 (:246-255) call `run.sh <target>`
      with NO override → Rust default.
    - `release.yml` gate-3 (:157-238) + `nightly-qemu.yml` (:115-124) call
      `boot.sh <target>` — now the Rust path after Task 1.
    - No workflow file invokes `AGENTLINUX_LEGACY_TS=1` in a gate job (the rollback
      lever is a manual/diagnostic override, not a default gate).

    Add a short comment block in boot.sh (adjacent to Task 1's re-wire) that
    states: "GATE-02 wiring status — Docker gates (test.yml bats-docker,
    release.yml gate-2/gate-4) run the Rust musl provision by default via
    run.sh's default path (Phase 58); the QEMU gates (nightly-qemu.yml,
    release.yml gate-3) run the Rust musl provision via THIS script's re-wired
    install invocation (Phase 59). AGENTLINUX_LEGACY_TS=1 is the manual rollback
    lever, not a gate default." This makes the wiring auditable at the file the
    swap lives in. Do NOT edit any workflow YAML — no gap exists; the swap in
    Task 1 is the only code change.

    (If the confirmation reveals an ACTUAL gap — e.g. a gate job passing
    `AGENTLINUX_LEGACY_TS=1` or overriding run.sh in a way that runs the Bash
    path — STOP and surface it: that would be a real GATE-02 wiring gap needing a
    workflow edit + a new task. The research asserts no such gap exists, but this
    task's job is to PROVE it, not assume it.)
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && echo "== test.yml bats-docker runs run.sh with no LEGACY override ==" && grep -n 'run.sh' .github/workflows/test.yml && ! grep -qE 'AGENTLINUX_LEGACY_TS' .github/workflows/test.yml && echo "TESTYML-NO-LEGACY" && echo "== release.yml gates ==" && grep -n 'run.sh\|boot.sh' .github/workflows/release.yml && ! grep -qE 'AGENTLINUX_LEGACY_TS' .github/workflows/release.yml && echo "RELEASEYML-NO-LEGACY" && echo "== nightly-qemu calls boot.sh ==" && grep -n 'boot.sh' .github/workflows/nightly-qemu.yml && echo "== boot.sh carries the GATE-02 wiring-status comment ==" && grep -q 'GATE-02 wiring status' tests/qemu/boot.sh && echo "WIRING-STATUS-DOCUMENTED"</automated>
  </verify>
  <acceptance_criteria>
    - `test.yml` bats-docker calls `tests/docker/run.sh <target>` with no
      `AGENTLINUX_LEGACY_TS` override (`TESTYML-NO-LEGACY`) → Rust by default.
    - `release.yml` gate-2/gate-4 call `run.sh` (Rust default) and gate-3 calls
      `boot.sh` (Rust after Task 1); no gate job sets `AGENTLINUX_LEGACY_TS`
      (`RELEASEYML-NO-LEGACY`).
    - `nightly-qemu.yml` calls `boot.sh <target>` (Rust after Task 1).
    - The GATE-02 wiring status is documented as a comment in boot.sh
      (`WIRING-STATUS-DOCUMENTED`) — Docker=run.sh default, QEMU=this script,
      LEGACY_TS=manual lever.
    - No workflow YAML was edited (no gap found); if a real gap HAD been found, it
      was surfaced as a blocker rather than silently patched.
  </acceptance_criteria>
  <done>
    The Docker gates (test.yml, release.yml gate-2/gate-4) are PROVEN to run the
    Rust build by default; the QEMU gates run Rust via Task 1's boot.sh re-wire;
    no workflow edit is needed. The GATE-02 wiring is self-documenting in boot.sh.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the CI gate → the artifact under test | The QEMU gate must exercise the RUST provisioner, not the Bash entrypoint. The provisioner-identity assertion is the guard that a green QEMU gate proves the correct artifact — the integrity of the GATE-02 acceptance claim. |
| the guest install invocation → the staged bin | boot.sh runs whatever `plugin/bin/agentlinux` is in the extracted tarball; the identity guard ensures that is the static musl bin (not a Bash script or a dynamically-linked regression). |
| the rollback lever → the Bash+TS distribution | `AGENTLINUX_LEGACY_TS=1` restores the Bash entrypoint for the QEMU gate too (GATE-05); the Bash entrypoint is retained, not deleted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-04 | Tampering | a false GATE-02 green (QEMU silently runs the Bash path) | high | mitigate | boot.sh is re-pointed at the Rust `provision`; a provisioner-IDENTITY assertion (readelf no-PT_INTERP / `file` statically-linked / `ldd` not-dynamic) FAILS the run non-zero if the artifact is not the static musl bin — a regression to the Bash entrypoint cannot pass the gate (the #1 risk, closed). |
| T-59-05 | Repudiation | the wiring status being un-auditable | low | mitigate | The GATE-02 wiring status is documented in a boot.sh comment block (Docker=run.sh default, QEMU=this script, LEGACY_TS=manual lever) so the acceptance claim is traceable to the code. |
| T-59-06 | Elevation of Privilege | the Rust `provision` running as root in the guest | low | accept | The Rust `provision` uses require_root by design (parity with the Bash entrypoint, run as root in the guest) — the same trust model the Bash path used; no new privilege surface. |
| T-59-SC | Tampering | cargo/npm installs (none) | low | accept | This wave edits boot.sh only — no package installs. The identity-assertion tools (`readelf`/`file`/`ldd`) are base-image binutils/coreutils. |
</threat_model>

<verification>
- `tests/qemu/boot.sh` runs `agentlinux provision --user agent --yes` (the staged
  musl bin) by default, mirroring run.sh:337 — the QEMU gate proves the RUST
  provisioner (GATE-02 QEMU half wired).
- A provisioner-identity assertion fails the run non-zero if the artifact is not
  the static musl bin — a regression to the Bash path is caught mechanically
  (Risk #1 closed).
- `AGENTLINUX_LEGACY_TS=1` execs the Bash entrypoint (retained GATE-05 rollback);
  `plugin/bin/agentlinux-install` is retained in the tree.
- `test.yml` + `release.yml` Docker gates are PROVEN to run Rust by default (no
  workflow edit needed); the wiring status is documented in boot.sh.
- `bash -n tests/qemu/boot.sh` parses clean; the REMOTE_BATS heredoc + workflows
  are unchanged.
- CI/QEMU-GATED (not run locally, no KVM): the full QEMU matrix + live-CDN AGT-02
  QEMU gate run on the phase's nightly-qemu / release pipeline — documented, not
  faked.
</verification>

<success_criteria>
- The QEMU gate is re-wired to the Rust musl `provision` (the only GATE-02 code
  change) with a static-musl provisioner-identity guard — no false GATE-02 green
  on the Bash path (the #1 risk).
- The `AGENTLINUX_LEGACY_TS=1` Bash rollback branch is honored; the Bash
  entrypoint is retained (GATE-05).
- The Docker gates are proven (not assumed) to run Rust by default; no workflow
  edit is needed; the wiring is self-documenting.
- The full QEMU + live-CDN matrix is correctly WIRED to the Rust build and gated
  by CI (documented as the pipeline gate, not a local run).
</success_criteria>

<output>
Create `.planning/phases/59-full-validation-gate/59-02-SUMMARY.md` when done
</output>
