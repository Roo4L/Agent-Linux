---
phase: 54-testing-bedrock
plan: 03
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/nightly-mutation.yml
autonomous: true
requirements: [TEST-02, GATE-05, GATE-01]
tags: [ci, cargo-mutants, mutation-testing, github-actions, testing-bedrock]

must_haves:
  truths:
    - "cargo-mutants runs on agentlinux-core in CI and reports a mutation result"
    - "The per-PR mutants step is gated at zero un-skipped surviving mutants on the diff (--in-diff), landed as a gate this phase (not left advisory)"
    - "The full-crate mutation score runs nightly (reuse/extend the existing nightly-mutation workflow)"
    - "The mutants step sits behind the existing rust-job guard so a red mutants run can never block a non-Rust master hotfix (GATE-05)"
    - "cargo-mutants is pinned + cached and cannot hang (bounded per-mutant timeout)"
  artifacts:
    - ".github/workflows/test.yml rust job extended with a pinned cargo-mutants install + an --in-diff mutants step, both behind steps.guard.outputs.ready"
    - ".github/workflows/nightly-mutation.yml extended with a full-crate cargo-mutants job on agentlinux-core"
  key_links:
    - "The mutants steps use if: steps.guard.outputs.ready == 'true' (the same guard as every rust-job step) so GATE-05 holds — no Rust mutation run fires on a docs-only or non-Rust hotfix PR"
    - "cargo install is pinned to 27.1.0 and cached via the existing Swatinem/rust-cache so a silent upstream bump cannot change the mutation set mid-milestone"
---

<objective>
Wire cargo-mutants into CI as a gate on the pure agentlinux-core crate (TEST-02): a fast per-PR --in-diff run gated at zero un-skipped surviving mutants, plus a full-crate score nightly. Every mutants step sits behind the existing rust-job guard so a red mutation run can never block a non-Rust master hotfix (GATE-05).

Purpose: The mutation-testing practice that motivated the whole rewrite becomes operational and enforced (not aspirational) — every later port (Phase 55-57) then lands behind a live mutation gate. Per the resolved decision, this lands as a gate now (advisory-first is fine but it must be a gate this phase), encoded as a normal CI change — NOT a blocking human-decide checkpoint.

Output: an extended rust job in test.yml (pinned cargo-mutants install + guarded --in-diff gate) and a full-crate agentlinux-core job in nightly-mutation.yml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-testing-bedrock/54-RESEARCH.md
@.planning/phases/54-testing-bedrock/54-VALIDATION.md

# The CI job to extend (rust job at ~line 139; guard at ~152; rust-cache at ~164)
@.github/workflows/test.yml
# The nightly mutation workflow to extend with a full-crate Rust job
@.github/workflows/nightly-mutation.yml
# The pure crate cargo-mutants scopes to
@rust/crates/agentlinux-core/src/lib.rs
</context>

<artifacts_this_phase_produces>
This plan (54-03) produces:
- .github/workflows/test.yml — two new steps in the existing rust job, both behind if: steps.guard.outputs.ready == 'true': a pinned `cargo install --locked --version 27.1.0 cargo-mutants` step and a `cargo mutants --package agentlinux-core --minimum-test-timeout 20 --in-diff <diff>` gate step. No new job; the steps inherit the job's timeout-minutes:15 + Swatinem/rust-cache + GATE-05 guard.
- .github/workflows/nightly-mutation.yml — a new rust-mutants job running the full-crate `cargo mutants --package agentlinux-core --minimum-test-timeout 20` score nightly (advisory continue-on-error like the existing stryker/bash-mutator jobs), guarded to skip when rust/ is absent.
Note: .gitignore's mutants.out ignore was staged in Plan 01; this plan does not touch .gitignore. Cargo.toml is untouched — cargo-mutants is a CI-installed subcommand, not a crate dep.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the rust job with a guarded, pinned --in-diff cargo-mutants gate (TEST-02 / GATE-05)</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml lines 139-199 — the existing rust job: the guard step (id: guard, sets ready=false when no code changed or rust/Cargo.toml absent, ~152-157), the Swatinem/rust-cache@v2 with workspaces: rust (~164-167), the working-directory: rust convention, and timeout-minutes: 15 (~142). Add the mutants steps AFTER unit tests, BEFORE or after the musl build — all gated identically.
    - 54-RESEARCH.md section "cargo-mutants CI Gate (TEST-02)" — the exact install (`cargo install --locked --version 27.1.0 cargo-mutants`), the `--package agentlinux-core --minimum-test-timeout 20 --in-diff <(git diff origin/master...HEAD -- 'crates/agentlinux-core/**')` invocation, the advisory-to-gate ratchet, caching notes, and Pitfall 4 (un-timeout'd hang / un-gated blocks a hotfix)
    - The RESOLVED DECISION (from planning context): per-PR gate at zero un-skipped surviving mutants on --in-diff; full score nightly; land it as a GATE this phase, encoded as a normal task — NOT a checkpoint:human-decide.
  </read_first>
  <behavior>
    - On a PR that touches agentlinux-core, the rust job installs cargo-mutants (pinned 27.1.0, cache-hit no-op on warm runs) and runs `cargo mutants --package agentlinux-core --minimum-test-timeout 20 --in-diff` over the crate's diff; a surviving un-skipped mutant fails the step (the gate).
    - On a PR that touches no AgentLinux code or where rust/Cargo.toml is absent, steps.guard.outputs.ready is 'false' and the mutants steps are skipped — a red Rust run can never fire on a non-Rust hotfix (GATE-05).
    - The step cannot hang: --minimum-test-timeout 20 floors the per-mutant timeout so an infinite-loop mutant is killed by timeout rather than hanging the runner under the job's 15-min cap.
  </behavior>
  <action>
    Add two steps to the existing rust job in test.yml (do NOT create a new job — reuse the guard + rust-cache + timeout the job already has). Step A "Install cargo-mutants (pinned, cached)": `if: steps.guard.outputs.ready == 'true'`, run `cargo install --locked --version 27.1.0 cargo-mutants` (the Swatinem/rust-cache already in the job caches ~/.cargo incl. this binary, so warm runs are no-ops). Step B "Mutation gate (pure core, --in-diff)": `if: steps.guard.outputs.ready == 'true'`, working-directory: rust, run `cargo mutants --package agentlinux-core --minimum-test-timeout 20 --in-diff <(git diff origin/master...HEAD -- 'crates/agentlinux-core/**')`. Land it as a GATE (per the resolved decision): a surviving un-skipped mutant must FAIL the step — do NOT leave a `|| echo ::warning::` advisory bypass on the per-PR step. If a legitimate survivor needs to be excluded, annotate it in Rust with #[mutants::skip] and a justification comment rather than loosening the gate. Ensure the diff base resolves on PRs — if origin/master is not fetched by default, add a fetch or use the PR base ref the workflow already exposes; keep the guard in front so a non-Rust PR skips entirely. Do not add generic mutation advice — reference the concrete invocation above.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/test.yml')); steps=d['jobs']['rust']['steps']; names=[s.get('name','') for s in steps]; assert any('mutants' in n.lower() for n in names), 'no mutants step in rust job'; mut=[s for s in steps if 'mutant' in s.get('name','').lower()]; assert all(\"steps.guard.outputs.ready == 'true'\" in str(s.get('if','')) for s in mut), 'a mutants step is not behind the guard'; assert any('--in-diff' in str(s.get('run','')) for s in mut), 'no --in-diff gate'; assert any('27.1.0' in str(s.get('run','')) for s in steps), 'cargo-mutants not pinned to 27.1.0'; print('OK: guarded pinned --in-diff mutants gate present')"</automated>
  </verify>
  <acceptance_criteria>
    - The rust job in test.yml has an install step pinning cargo-mutants to 27.1.0 and a mutants gate step running `cargo mutants --package agentlinux-core --minimum-test-timeout 20 --in-diff`, both with `if: steps.guard.outputs.ready == 'true'` (the python yaml assertion passes).
    - The per-PR mutants step has NO `|| echo ::warning::` advisory bypass (grep the rust-job mutants step run block — it must not swallow a non-zero exit).
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` parses (valid YAML).
    - No new top-level job is added for the per-PR gate (it lives inside the existing rust job).
  </acceptance_criteria>
  <done>The existing rust job installs pinned cargo-mutants and runs a guarded --in-diff mutation gate at zero un-skipped survivors; it inherits the 15-min cap, rust-cache, and the GATE-05 guard so a red mutants run cannot block a non-Rust master hotfix.</done>
</task>

<task type="auto">
  <name>Task 2: Full-crate nightly mutation score on agentlinux-core (TEST-02)</name>
  <files>.github/workflows/nightly-mutation.yml</files>
  <read_first>
    - .github/workflows/nightly-mutation.yml (the existing stryker + bash-mutator jobs: schedule cron '0 4 * * *', workflow_dispatch, continue-on-error: true, a guard step that skips when the target source is absent — mirror this shape for the Rust job)
    - 54-RESEARCH.md section "cargo-mutants CI Gate" — the full-crate invocation (`cargo mutants --package agentlinux-core --minimum-test-timeout 20` without --in-diff) belongs nightly; the per-PR --in-diff gate keeps the merge gate honest under the 15-min cap while the full score runs out-of-band
  </read_first>
  <behavior>
    - Nightly (and on manual dispatch), a rust-mutants job runs the FULL-crate `cargo mutants --package agentlinux-core --minimum-test-timeout 20` and reports the mutation score; it is advisory (continue-on-error, matching the sibling stryker/bash-mutator jobs) so a nightly regression surfaces as a warning without blocking.
    - The job skips cleanly when rust/Cargo.toml is absent (guard), mirroring the existing "skip if source absent" pattern.
  </behavior>
  <action>
    Add a `rust-mutants` job to nightly-mutation.yml mirroring the existing jobs' shape: runs-on ubuntu-24.04, continue-on-error: true (advisory nightly, like stryker/bash-mutator), checkout, a guard step that sets ready=false when rust/Cargo.toml is absent, then install the Rust toolchain (dtolnay/rust-toolchain@stable), `cargo install --locked --version 27.1.0 cargo-mutants`, and run the full-crate score `cargo mutants --package agentlinux-core --minimum-test-timeout 20` from working-directory rust (append `|| echo "::warning::surviving mutants (nightly full-crate advisory)"` so the advisory nightly reports without failing the scheduled run — the ENFORCING gate is the per-PR --in-diff step from Task 1). Keep it out of the per-PR path. Do not add generic advice; use the concrete full-crate invocation.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/nightly-mutation.yml')); j=d['jobs']; assert 'rust-mutants' in j, 'no rust-mutants job'; s=j['rust-mutants']['steps']; assert any('agentlinux-core' in str(x.get('run','')) for x in s), 'no full-crate agentlinux-core mutants run'; assert j['rust-mutants'].get('continue-on-error') is True, 'nightly rust-mutants must be advisory (continue-on-error)'; print('OK: nightly full-crate rust-mutants job present')"</automated>
  </verify>
  <acceptance_criteria>
    - nightly-mutation.yml has a `rust-mutants` job running the full-crate `cargo mutants --package agentlinux-core --minimum-test-timeout 20`, advisory (continue-on-error: true), guarded to skip when rust/ is absent (the python yaml assertion passes).
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/nightly-mutation.yml'))"` parses (valid YAML).
    - The full-crate run is NOT on the per-PR path (it lives only in nightly-mutation.yml).
  </acceptance_criteria>
  <done>A nightly full-crate cargo-mutants score on agentlinux-core reports out-of-band (advisory), complementing the enforcing per-PR --in-diff gate from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> crates.io (cargo install) | The mutants gate installs cargo-mutants from crates.io in CI. Pinning + --locked bounds the supply-chain surface; no new privileged/network surface reaches the shipped product (this is CI-only test infra). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-06 | Denial of Service | un-timeout'd mutant hangs the runner | low | mitigate | `--minimum-test-timeout 20` floors the per-mutant timeout so an infinite-loop mutant is killed rather than hanging under the job's 15-min cap (Pitfall 4). |
| T-54-07 | Elevation of Privilege | a red Rust mutation run blocks a non-Rust master hotfix | medium | mitigate | Every mutants step is behind `if: steps.guard.outputs.ready == 'true'` (the existing rust-job guard, ready=false when no code changed or rust/Cargo.toml absent) — GATE-05: a red Rust run never fires on a non-Rust hotfix PR. |
| T-54-SC | Tampering | cargo-mutants supply chain (silent upstream bump) | low | mitigate | `cargo install --locked --version 27.1.0` pins the exact version so a silent bump cannot change the mutation set mid-milestone; cargo-mutants is ecosystem-canonical (RESEARCH Package Legitimacy Audit, OK). |
</threat_model>

<verification>
- `.github/workflows/test.yml` parses as valid YAML; the rust job has a pinned cargo-mutants install + a guarded `--in-diff` gate at zero un-skipped survivors; no `|| echo ::warning::` bypass on the per-PR gate.
- `.github/workflows/nightly-mutation.yml` parses; a `rust-mutants` job runs the full-crate score advisory (continue-on-error) guarded on rust/ presence.
- GATE-05: every mutants step behind `steps.guard.outputs.ready == 'true'` — a docs-only / non-Rust hotfix PR skips the Rust mutation steps entirely.
- GATE-01: this plan changes only CI workflow YAML; it does not touch any bats test or the shipped ajv path, so no behavior test is skipped or regressed.
</verification>

<success_criteria>
- TEST-02: cargo-mutants runs on the pure agentlinux-core crate in CI and reports a mutation result; the per-PR `--in-diff` step is a GATE at zero un-skipped surviving mutants (landed this phase, not left advisory), and the full-crate score runs nightly.
- GATE-05: the mutation gate can never block a non-Rust master hotfix (behind the rust-job guard); cargo-mutants is pinned + cached and cannot hang.
- The resolved threshold decision (advisory-first, gate at zero un-skipped survivors on --in-diff; full score nightly) is encoded as normal CI config — NO blocking human-decide checkpoint.
</success_criteria>

<output>
Create `.planning/phases/54-testing-bedrock/54-03-SUMMARY.md` when done.
</output>
