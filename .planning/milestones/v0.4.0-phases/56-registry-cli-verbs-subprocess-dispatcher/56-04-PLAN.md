---
phase: 56-registry-cli-verbs-subprocess-dispatcher
plan: 04
type: execute
wave: 3
depends_on: ["56-01", "56-02", "56-03"]
files_modified:
  - rust/crates/agentlinux/src/cmd/list.rs
  - rust/crates/agentlinux/src/cmd/install.rs
  - rust/crates/agentlinux/src/cmd/remove.rs
  - rust/crates/agentlinux/src/cmd/upgrade.rs
  - rust/crates/agentlinux/src/cmd/pin.rs
  - rust/crates/agentlinux/src/cmd/adopt.rs
  - rust/crates/agentlinux/src/dispatcher.rs
autonomous: true
requirements: [VERB-01, VERB-02, VERB-03, GATE-01, GATE-05]
must_haves:
  truths:
    - "The full CLI bats surface (40-registry-cli, 23-install-user, 10-installer CLI-adjacent, 50-agents non-network/systemd-gated) is GREEN on the Rust build, per-file (Docker OOM), with NO behavior-contract regression and NO newly-skipped test vs. the TS build (GATE-01)."
    - "The dispatcher's 6-case parity spec (dispatcher-stream.test.ts equivalents) + the SIGKILL-escalation case remain green — the VERB-02 regression floor holds after all six verbs land."
    - "Every network/systemd-gated case (the 50-agents real-install / apt / systemd cases) is explicitly enumerated as deferred to Phase 59's QEMU/full-matrix gate — not claimed as passing, not left ambiguous."
    - "master stays shippable: the Rust bin is staged ONLY in the flag-gated test harness; the provisioner still symlinks the TS bundle; the TS build is not regressed (GATE-05 parallel-track)."
  artifacts:
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-04-SUMMARY.md
  key_links:
    - "This plan runs the SAME `run.sh` override + staged Rust bin from Plan 01 across all four CLI bats files; it fixes only residual cross-verb parity gaps (shared-state, ordering, presence-overlay interactions) surfaced when the full files run end-to-end."
    - "The deferred-to-Phase-59 gated set (recorded in Plans 02/03) is consolidated here into the phase's verification record so Phase 59 knows the exact QEMU-only remainder."
---

<objective>
Wave 3 — the phase closeout: prove the FULL CLI bats surface green on the Rust
build and formally close VERB-01/02/03 + GATE-01/GATE-05.

Plans 02/03 landed the six verbs and greened their owning cases per-file. This
plan runs ALL four CLI bats files end-to-end against the staged Rust bin,
catches the residual cross-verb parity gaps that only surface when a whole file
runs (shared sentinel/cache state between cases, ordering, presence-overlay
interactions across list→install→adopt), fixes them in the `cmd/*.rs`, and
consolidates the network/systemd-gated remainder as the explicit Phase-59
deferral. It also re-asserts the dispatcher parity floor (VERB-02) after all
verbs are wired.

Purpose: the GATE-01 phase gate — full CLI bats green on the Rust build, no
regression / no newly-skipped, parity closed for the CLI surface.
Output: green per-file bats across 40/23/10/50 (non-gated) + a consolidated
deferral record; no source-file churn beyond residual parity fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-VALIDATION.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-01-PLAN.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-02-PLAN.md
@.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-03-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan's slice of the phase output:
- Green per-file CLI bats on the Rust build across 40-registry-cli, 23-install-user, 10-installer (CLI-adjacent), 50-agents (non-gated).
- A consolidated deferral record (in 56-04-SUMMARY.md) of the network/systemd-gated cases → Phase 59.
- Residual cross-verb parity fixes in `cmd/*.rs` (only where a full-file run surfaces a gap the per-verb runs missed).
Downstream: `/gsd-verify-work` (phase verification) consumes the green-bats + the deferral record; Phase 57 (provisioner) inherits the staged-Rust-bin harness.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full CLI bats green on the Rust build — 40/23/10/50 per-file, residual parity fixes</name>
  <files>rust/crates/agentlinux/src/cmd/list.rs, rust/crates/agentlinux/src/cmd/install.rs, rust/crates/agentlinux/src/cmd/remove.rs, rust/crates/agentlinux/src/cmd/upgrade.rs, rust/crates/agentlinux/src/cmd/pin.rs, rust/crates/agentlinux/src/cmd/adopt.rs</files>
  <read_first>
    - tests/bats/40-registry-cli.bats (READ IN FULL — every @test; run the WHOLE file end-to-end and catch cross-case shared-state/ordering/presence-overlay gaps the per-verb Plan-02/03 runs missed)
    - tests/bats/23-install-user.bats (READ IN FULL — INST-07 provisioning + guard on a non-agent user, end-to-end)
    - tests/bats/10-installer.bats (READ the CLI-adjacent @tests — which call `agentlinux <verb>` post-provision; verify those hit the Rust bin green; the pure-installer @tests are out of scope but must not regress)
    - tests/bats/50-agents.bats (READ IN FULL — enumerate EXACTLY which AGT cases `skip` under network/systemd gating [:114 and any other `skip` guards] vs. which run in plain Docker; the non-gated subset must be green, the gated subset is the Phase-59 deferral)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-02-PLAN.md + 56-03-PLAN.md (the per-verb gated-set records to consolidate) + 56-VALIDATION.md §"Manual-Only Verifications" (the explicit Phase-59-deferred families)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-01-PLAN.md Task 4 (the `run.sh` override + fail-loud staging this step drives)
  </read_first>
  <action>
    Run each CLI bats file WHOLE against the staged Rust bin (Plan-01 override),
    per-file (Docker OOM — NEVER the full `tests/bats/` dir in this VM):
    `AGENTLINUX_STAGE_RUST_CLI=1 ./tests/docker/run.sh ubuntu-24.04 40-registry-cli`,
    then `23-install-user`, then `10-installer`, then `50-agents`. For each file, triage
    every failure:
    - **Genuine parity gap** (a stdout byte mismatch, wrong exit, or a cross-verb
      shared-state / ordering / presence-overlay interaction that only appears when the
      whole file runs) → fix the corresponding `cmd/*.rs`. These are the residual gaps
      the per-verb Plan-02/03 runs could not surface (e.g. an `install` that leaves a
      sentinel a later `list` case reads; an `adopt --all` that a later `upgrade` case
      depends on).
    - **Network/systemd-gated `skip`** → leave it skipped (it is Phase-59), and RECORD
      it in the deferral list.
    Do NOT edit the bats files (they are the spec). Do NOT weaken a gate to make it pass.
    Do NOT silently fall back to the TS bundle — the Plan-01 fail-loud guard prevents a
    false-green; if the Rust bin is absent, fix the build.

    Continue until 40-registry-cli + 23-install-user + the 10-installer CLI-adjacent
    cases + the non-gated 50-agents cases are GREEN on the Rust build with NO newly-
    skipped test vs. the TS build (GATE-01: no behavior-contract regression). Confirm the
    TS build is NOT regressed by these fixes (the fixes are Rust-side only;
    `git diff --name-only` shows only `rust/` + no `plugin/cli/` churn).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting && for f in 40-registry-cli 23-install-user 10-installer 50-agents; do echo "===== $f ====="; AGENTLINUX_STAGE_RUST_CLI=1 ./tests/docker/run.sh ubuntu-24.04 $f 2>&1 | tail -15; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of `40-registry-cli`, `23-install-user`, `10-installer`, `50-agents` run per-file against the STAGED RUST bin; the non-gated cases are GREEN with no red and no newly-skipped test vs. the TS build (GATE-01).
    - The bats files are UNCHANGED (`git diff --name-only tests/bats/` empty); no gate was weakened to force a pass.
    - Every network/systemd-gated `skip` case is enumerated (file + @test name + gate reason) for the Phase-59 deferral — no gated case is claimed as a Rust pass.
    - Only `rust/` files changed (`git diff --name-only` shows no `plugin/cli/` churn) — the TS build is not regressed (GATE-05 parallel-track).
  </acceptance_criteria>
  <done>
    The full CLI bats surface (40/23/10-CLI/50-non-gated) is green on the Rust build with no regression / no newly-skipped; residual cross-verb parity gaps are fixed in `cmd/*.rs`; the gated remainder is enumerated for Phase 59; the TS build is untouched (GATE-01 phase gate met).
  </done>
</task>

<task type="auto">
  <name>Task 2: Parity closeout — dispatcher floor re-assert + full Rust suite + VERB-01/02/03 sign-off</name>
  <files>rust/crates/agentlinux/src/dispatcher.rs</files>
  <read_first>
    - rust/crates/agentlinux/src/dispatcher.rs (READ IN FULL — the 6-case parity module from Plan 01; confirm it still passes unchanged after all six verbs wired the dispatcher — the VERB-02 regression floor)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-VALIDATION.md §"Validation Sign-Off" (the checklist: every verb has a green bats file; dispatcher 6-case parity green; agentlinux-core stays pure; no 3 consecutive tasks without automated verify; nyquist_compliant flip)
    - .planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-RESEARCH.md §"Metadata" + §"Validation Architecture" (the phase-gate sampling: full Rust suite + per-file CLI bats before verify)
  </read_first>
  <action>
    Re-assert the VERB-02 dispatcher floor: run `cargo test -p agentlinux dispatcher` and
    confirm all 6 dispatcher-stream.test.ts parity cases + the SIGKILL-escalation case are
    STILL green after all six verbs wired `dispatch_recipe` (no regression from the verb
    integration). Run the full Rust workspace gate:
    `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings &&
    cargo fmt --all --check`. Confirm `agentlinux-core` is still PURE
    (`grep -rn 'std::process\|std::fs\|std::env' rust/crates/agentlinux-core/src/` prints
    nothing) — all Phase-56 I/O landed in the bin.

    Produce the phase-closeout record for 56-04-SUMMARY.md: (a) the per-verb → bats-file
    green matrix (list→40; install→40/23; remove→40; upgrade→40/50; pin→40; adopt→40); (b)
    the consolidated Phase-59-deferred gated-case list (from Task 1); (c) the VERB-01/02/03
    + GATE-01/05 sign-off statements (VERB-01: six verbs byte-compatible; VERB-02:
    dispatcher 6-case floor green + real-recipe dispatch green; VERB-03: the 6 `AGENTLINUX_*`
    from one typed source, ~25 recipes unchanged and green). Set the VALIDATION.md
    `nyquist_compliant: true` / `wave_0_complete: true` front-matter (the Wave-0 dispatcher +
    harness landed in Plan 01; the sampling continuity held — no 3 consecutive tasks without
    an automated verify across the phase). Do NOT change any verb logic in this task unless
    the dispatcher re-assert surfaces a regression (then fix the dispatcher, not the bats).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/stack-revisiting/rust && . "$HOME/.cargo/env" && cargo test -p agentlinux dispatcher 2>&1 | tail -12 && cargo test --workspace 2>&1 | tail -8 && cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5 && ( grep -rn 'std::process\|std::fs\|std::env' crates/agentlinux-core/src/ && echo "CORE-IMPURE-FAIL" || echo "CORE-PURE-OK" )</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p agentlinux dispatcher` still green — all 6 parity cases + the SIGKILL-escalation case hold after the six verbs wired the dispatcher (VERB-02 regression floor).
    - `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check` all clean.
    - `agentlinux-core` is still PURE: the grep for `std::process`/`std::fs`/`std::env` under `crates/agentlinux-core/src/` prints nothing (`CORE-PURE-OK`).
    - 56-04-SUMMARY.md records the per-verb→bats green matrix, the consolidated Phase-59-deferred gated list, and the VERB-01/02/03 + GATE-01/05 sign-off; VALIDATION.md `nyquist_compliant: true` + `wave_0_complete: true` set.
  </acceptance_criteria>
  <done>
    The VERB-02 dispatcher floor holds after full verb integration; the Rust workspace gate is clean; the pure core stayed pure; the phase-closeout record + Phase-59 deferral are written and VALIDATION.md is flipped to nyquist-compliant (VERB-01/02/03 + GATE-01/05 closed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged Rust bin ↔ TS bundle (parallel track) | The Rust bin runs ONLY in the flag-gated test harness; the provisioner symlink still points at the TS bundle on master — a Rust-side defect cannot reach a shipped `master` (GATE-05). |
| bats spec ↔ Rust implementation | The bats suite is the fixed contract; parity fixes flow one way (Rust → green), never by weakening a gate — a false-green (weakened bats or silent TS fallback) is the threat this closeout guards against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-14 | Spoofing | full-suite false-green | high | mitigate | Per-file bats against the STAGED RUST bin with the Plan-01 fail-loud guard (a missing Rust bin aborts, never falls back to TS); no bats gate is weakened; the "do not claim full-suite from partial" rule is honored — the gated set is enumerated, not silently passed. Task 1 asserts unchanged bats + only-`rust/`-churn. |
| T-56-15 | Repudiation | Phase-59 deferral ambiguity | low | mitigate | The network/systemd-gated cases are enumerated (file + @test + gate reason) in the summary, so Phase 59 has an unambiguous QEMU-only remainder — no "was this ever tested?" gap. Task 2 records it. |
| T-56-16 | Tampering | VERB-02 dispatcher regression | medium | mitigate | The dispatcher 6-case parity floor is re-run AFTER all verbs integrate, catching any regression the verb wiring introduced into the shared dispatch path (Task 2). |
| T-56-SC | Tampering | (no new crate installs this plan) | low | accept | Wave 3 adds no dependencies; it runs tests + records — no supply-chain surface. |
</threat_model>

<verification>
- Per-file CLI bats (40-registry-cli, 23-install-user, 10-installer CLI-adjacent, 50-agents non-gated) GREEN on the staged Rust bin — no regression / no newly-skipped (GATE-01).
- `cargo test -p agentlinux dispatcher` green — the VERB-02 parity floor holds after verb integration.
- `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check` clean.
- `agentlinux-core` stays PURE; only `rust/` changed (TS build not regressed — GATE-05).
- The Phase-59-deferred gated set is enumerated; VALIDATION.md flipped to `nyquist_compliant: true`.
</verification>

<success_criteria>
- The full CLI bats surface is green on the Rust build (per-file, no regression / no newly-skipped) — GATE-01 phase gate met.
- VERB-01 (six verbs byte-compatible) + VERB-02 (dispatcher floor + real-recipe dispatch) + VERB-03 (one typed source, ~25 recipes unchanged) are signed off.
- master stays shippable: the Rust bin is harness-only, the provisioner still symlinks TS, the TS build is untouched (GATE-05).
- The network/systemd-gated remainder is enumerated for Phase 59; the pure core stayed pure.
</success_criteria>

<output>
Create `.planning/phases/56-registry-cli-verbs-subprocess-dispatcher/56-04-SUMMARY.md` when done
</output>
