---
phase: 20-behavior-test-green-on-almalinux-9
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - tests/bats/20-agent-user.bats
  - tests/bats/50-agents.bats
autonomous: true
requirements: [EL-06, PAR-01]
must_haves:
  truths:
    - "BHV-01 asserts LANG=C.UTF-8 and LC_ALL=C.UTF-8 at the family-correct locale file (/etc/locale.conf on EL9, /etc/default/locale on Ubuntu) — same observable, never skipped"
    - "The harness starts the family-correct ssh unit (sshd on EL9, ssh on Ubuntu)"
    - "Both SSH-key-seeding sites follow the key write with a guarded restorecon -R -F ~agent/.ssh"
    - "All six invocation modes — including non-interactive SSH — pass on the almalinux-9 Docker row"
    - "setenforce 0 / SELINUX=disabled appears nowhere"
  artifacts:
    - path: "tests/bats/20-agent-user.bats"
      provides: "BHV-01 locale via distro_assert_locale; distro_ssh_unit start; guarded restorecon at the setup seed site"
      contains: "distro_assert_locale"
    - path: "tests/bats/50-agents.bats"
      provides: "guarded restorecon at the setup_file re-seed site; distro_ssh_unit start"
      contains: "distro_restore_ssh_context"
  key_links:
    - from: "authorized_keys write (~agent/.ssh)"
      to: "distro_restore_ssh_context /home/agent/.ssh"
      via: "guarded restorecon following both seed sites"
      pattern: "distro_restore_ssh_context"
    - from: "BHV-01 locale assertion"
      to: "distro_assert_locale"
      via: "family-correct locale file path"
      pattern: "distro_assert_locale"
---

<objective>
Wave 3 — EL-06 (six invocation modes + guarded restorecon) and the BHV-01 locale
generalization. Route the BHV-01 `/etc/default/locale` assertions in `20-agent-user.bats`
through `distro_assert_locale` (EL9 path `/etc/locale.conf`), replace the hardcoded
`systemctl start ssh` unit name with `distro_ssh_unit` (EL9 = `sshd`) at both setup sites,
and insert a guarded `distro_restore_ssh_context /home/agent/.ssh` immediately after each
of the two SSH-key-seeding writes (`20-agent-user.bats` `setup`, `50-agents.bats`
`setup_file`).

Purpose: EL-06 requires all six modes green with any `~agent/.ssh/authorized_keys` write
followed by `restorecon -R -F ~agent/.ssh` so a confined `sshd_t` can read the key under
real SELinux. On the Docker row SELinux is structurally unavailable (AppArmor host kernel,
no selinuxfs — RESEARCH §SELinux-in-Docker Verdict), so the restorecon call is a
deliberate guarded NO-OP there and the six modes go green via `openssh-clients` + `sshd`
(both from Wave 1); the REAL enforcing-SELinux six-modes proof is Phase 22 QEMU. The
restorecon CODE still lands here at both harness seed sites (there is NO installer seed
site — keys come from cloud-init). SELinux stays enforcing — `setenforce 0` is NEVER used.
Output: 20-agent-user.bats and 50-agents.bats generalized for locale + ssh unit +
guarded restorecon; six modes green on EL9; a documented note that enforcing SELinux is
unavailable on Docker and re-proven in Phase 22.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-RESEARCH.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-PATTERNS.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-02-SUMMARY.md

<interfaces>
<!-- distro.bash verbs this plan consumes (created in 20-02): -->
<!--   distro_assert_locale <LANG|LC_ALL>  → grep ^VAR=C.UTF-8 in the family locale file -->
<!--   distro_ssh_unit                     → 'sshd' (rhel) / 'ssh' (debian) -->
<!--   distro_restore_ssh_context <dir>    → rhel: command -v restorecon >/dev/null && restorecon -R -F "$dir" || true ; debian: : -->
<!-- Add `load 'helpers/distro'` next to the existing load lines in each file. -->

<!-- 20-agent-user.bats current BHV-01 locale asserts (lines 59-67), Debian path: -->
```bash
@test "BHV-01: /etc/default/locale has LANG=C.UTF-8" {
  run grep -E '^LANG=C\.UTF-8$' /etc/default/locale
  assert_exit_zero "BHV-01"
}
@test "BHV-01: /etc/default/locale has LC_ALL=C.UTF-8" {
  run grep -E '^LC_ALL=C\.UTF-8$' /etc/default/locale
  assert_exit_zero "BHV-01"
}
```
<!-- The `locale -a` test (lines 69-73) already passes portably (accepts C.utf8) — leave it. -->

<!-- 20-agent-user.bats setup() seed site (keys written lines 31-33, unit start line 37): -->
```bash
    install -d -m 0700 -o agent -g agent /home/agent/.ssh
    install -m 0600 -o agent -g agent \
      /root/.ssh/id_ed25519.pub /home/agent/.ssh/authorized_keys
    # ← insert: distro_restore_ssh_context /home/agent/.ssh
    systemctl start ssh >/dev/null 2>&1 || true   # ← distro_ssh_unit
```

<!-- 50-agents.bats setup_file() re-seed site (after 40-*.bats --purge/userdel -r), -->
<!-- keys at lines 56-57, unit start line 58: -->
```bash
  if [[ -f /root/.ssh/id_ed25519.pub ]] \
    && [[ ! -f /home/agent/.ssh/authorized_keys ]]; then
    install -d -m 0700 -o agent -g agent /home/agent/.ssh
    install -m 0600 -o agent -g agent \
      /root/.ssh/id_ed25519.pub /home/agent/.ssh/authorized_keys
    # ← insert: distro_restore_ssh_context /home/agent/.ssh
    systemctl start ssh >/dev/null 2>&1 || true   # ← distro_ssh_unit
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BHV-01 locale via distro_assert_locale + distro_ssh_unit in 20-agent-user.bats</name>
  <files>tests/bats/20-agent-user.bats</files>
  <read_first>tests/bats/20-agent-user.bats (load lines 19-20; setup() lines 28-43 incl. `systemctl start ssh` line 37; BHV-01 locale asserts lines 59-67; `locale -a` test lines 69-73); 20-PATTERNS.md "tests/bats/20-agent-user.bats" (lines 263-294).</read_first>
  <action>Add `load 'helpers/distro'` next to the existing loads (after line 20). Replace the two BHV-01 `@test` bodies (lines 59-67) so each asserts the SAME observable at the family-correct path: `distro_assert_locale LANG` and `distro_assert_locale LC_ALL` (the verb greps `^LANG=C.UTF-8` / `^LC_ALL=C.UTF-8` in `/etc/locale.conf` on EL9, `/etc/default/locale` on Ubuntu). Do NOT `skip` on EL9 and do NOT weaken to a `locale -a`-only check — the locked decision forbids weakening; assert the real file contents at the family path. Leave the test NAMES intact or update the description to name the observable (not the Debian path); leave the portable `locale -a` test at lines 69-73 untouched (it already accepts `C.utf8`). In `setup()` (line 37), change `systemctl start ssh` to `systemctl start "$(distro_ssh_unit)"` (EL9 unit = `sshd`). The `ss -lnt … ':22 '` readiness poll (lines 39-42) is portable once `iproute` is in the image (Wave 1).</action>
  <verify>
    <automated>bash -n tests/bats/20-agent-user.bats && grep -q "load 'helpers/distro'" tests/bats/20-agent-user.bats && grep -q 'distro_assert_locale' tests/bats/20-agent-user.bats && grep -q 'distro_ssh_unit' tests/bats/20-agent-user.bats && ! grep -qE '/etc/default/locale|systemctl start ssh\b' tests/bats/20-agent-user.bats && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `load 'helpers/distro'` present
    - `distro_assert_locale` used for both BHV-01 LANG and LC_ALL asserts
    - No bare `/etc/default/locale` string and no bare `systemctl start ssh` remaining in the file
    - No `skip` added to the BHV-01 tests; the `locale -a` test is unchanged
    - Per-file proof on almalinux-9 (full-suite-in-order, debug-only per-file under exec TMPDIR): BHV-01 LANG + LC_ALL green at `/etc/locale.conf`; on ubuntu-24.04 BHV-01 green at `/etc/default/locale`
  </acceptance_criteria>
  <done>BHV-01 asserts the locale observable at the family-correct path via distro_assert_locale; the harness starts the family-correct ssh unit; no weakening, no skip.</done>
</task>

<task type="auto">
  <name>Task 2: Guarded restorecon at both SSH-seeding sites (20-agent-user setup + 50-agents setup_file)</name>
  <files>tests/bats/20-agent-user.bats, tests/bats/50-agents.bats</files>
  <read_first>tests/bats/20-agent-user.bats (setup() seed site lines 28-43, keys at 31-33); tests/bats/50-agents.bats (load lines 27-28; setup_file() re-seed site lines 53-64, keys at 56-57, unit start line 58); 20-PATTERNS.md "Wave 3 — Guarded restorecon" (lines 378-423); 20-RESEARCH.md §SELinux-in-Docker Verdict (lines 67-81).</read_first>
  <action>At BOTH sites, immediately AFTER the `install -m 0600 … /home/agent/.ssh/authorized_keys` write and BEFORE the ssh-unit start, insert `distro_restore_ssh_context /home/agent/.ssh`. This verb is guarded (`command -v restorecon >/dev/null && restorecon -R -F "$dir" || true` on rhel; `:` no-op on debian) so it is safe on the Docker image where `policycoreutils`/`restorecon` may be absent — an UNGUARDED call would abort the harness (RESEARCH §Pitfall 3). In `50-agents.bats`: add `load 'helpers/distro'` next to the existing loads (after line 28), insert the guarded restorecon after line 57, and change `systemctl start ssh` (line 58) to `systemctl start "$(distro_ssh_unit)"`. Do NOT add any installer-side restorecon — the installer does not seed `~agent/.ssh` (keys come from cloud-init/external); both restorecon sites are in the test harness only. Do NOT add `setenforce 0` or `SELINUX=disabled` anywhere. In the plan SUMMARY, document plainly that enforcing SELinux is structurally unavailable on the Docker row (AppArmor host kernel) so the restorecon call is a deliberate no-op there, and the real enforcing-SELinux six-modes proof is Phase 22 QEMU.</action>
  <verify>
    <automated>bash -n tests/bats/50-agents.bats && grep -c 'distro_restore_ssh_context /home/agent/.ssh' tests/bats/20-agent-user.bats tests/bats/50-agents.bats && grep -q "load 'helpers/distro'" tests/bats/50-agents.bats && ! grep -rqE 'setenforce|SELINUX=disabled' tests/bats/ tests/docker/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `distro_restore_ssh_context /home/agent/.ssh` present in BOTH files, each immediately after the authorized_keys write
    - `50-agents.bats` has `load 'helpers/distro'` and uses `distro_ssh_unit` (no bare `systemctl start ssh`)
    - `grep -rE 'setenforce|SELINUX=disabled' tests/ plugin/ packaging/` returns NOTHING
    - No installer-side restorecon was added (only the two harness sites)
    - Suite-level gate: `bash tests/docker/run.sh almalinux-9` — all six invocation modes (interactive login, non-interactive SSH, cron, systemd User=agent, sudo -u agent, sudo -u agent -i) pass; BHV-02 + 30-runtime RT-01/02/04 + 50-agents SSH modes green
  </acceptance_criteria>
  <done>Both harness SSH-seed sites follow the key write with a guarded restorecon; 50-agents starts the family-correct ssh unit; six modes green on EL9; SELinux never disabled; the Docker-no-op / Phase-22-real-proof split is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ephemeral SSH keypair → ~agent/.ssh/authorized_keys | per-container keypair seeded in setup for the non-interactive SSH mode; never committed |
| confined sshd_t → authorized_keys (under real SELinux) | restorecon relabels the key so a confined sshd can read it (real effect Phase 22; no-op on Docker) |
| SELinux enforcement posture | locked: SELinux stays enforcing; disabling it is rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering/Repudiation | disabling SELinux to green the SSH mode | mitigate | Locked-rejected; guarded `restorecon` is the only sanctioned fix. CI grep asserts no `setenforce`/`SELINUX=disabled` anywhere (Task 2 acceptance). |
| T-20-08 | Information Disclosure | committed SSH keypair | accept | Keys generated per-container in setup, never reach the repo (existing design); the bind mount is read-only. |
| T-20-09 | Denial of Service | unguarded restorecon aborts the harness where policycoreutils is absent | mitigate | `distro_restore_ssh_context` guards on `command -v restorecon`; no-op on Docker. |
</threat_model>

<verification>
- `bash -n` + grep acceptance for both files.
- Wave gate: `bash tests/docker/run.sh almalinux-9` — six modes green (incl. non-interactive SSH), BHV-01/BHV-02 green; `bash tests/docker/run.sh ubuntu-24.04` stays green.
- Security grep: no `setenforce` / `SELINUX=disabled` in tests/, plugin/, packaging/.
</verification>

<success_criteria>
- BHV-01 locale asserted at the family-correct path via distro_assert_locale (no skip/weaken).
- Family-correct ssh unit started at both setup sites.
- Guarded restorecon follows both harness key-seed writes; no installer site invented.
- Six modes green on EL9; SELinux never disabled; Phase-22 enforcement-proof split documented.
</success_criteria>

<output>
After completion, create `.planning/phases/20-behavior-test-green-on-almalinux-9/20-03-SUMMARY.md`
</output>
