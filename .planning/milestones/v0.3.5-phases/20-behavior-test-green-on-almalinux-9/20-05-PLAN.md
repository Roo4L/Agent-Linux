---
phase: 20-behavior-test-green-on-almalinux-9
plan: 05
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - tests/bats/15-detection.bats
  - tests/bats/helpers/tty-driver.py
autonomous: true
requirements: [EL-08, PAR-01]
must_haves:
  truths:
    - "DET-03 npm-prefix probe is root-caused on EL9: confirmed a test/fixture detail (fixed here) OR escalated as a plugin/lib/as_user.sh product defect (flagged, out of test-only scope)"
    - "The DET-read-only invariant test passes on EL9 (needs diffutils from Wave 1)"
    - "tty-driver.py enforces a bounded pexpect timeout so a stuck EL9 prompt fails fast (≤ ~60s) instead of hanging ~13 min"
    - "The 15-preflight-ux TTY tests no longer hang the full suite on EL9"
  artifacts:
    - path: "tests/bats/helpers/tty-driver.py"
      provides: "bounded pexpect timeout converting the EL9 hang into a fast, diagnosable failure"
      contains: "timeout"
    - path: "tests/bats/15-detection.bats"
      provides: "DET-03 probe root-caused (assertion/fixture fix) or an escalation note"
      contains: "DET-03"
  key_links:
    - from: "tty-driver.py spawn/expect"
      to: "bounded timeout"
      via: "pexpect timeout= kwarg"
      pattern: "timeout"
    - from: "DET-03 probe"
      to: "as_user_login NPM_CONFIG_PREFIX on EL9"
      via: "sudo -u agent -i spike"
      pattern: "NPM_CONFIG_PREFIX"
---

<objective>
Wave 3 — the two RESEARCH spikes: the DET-03 npm-prefix probe root-cause (15-detection.bats)
and the defensive bounded pexpect timeout in `tty-driver.py` that turns the EL9
15-preflight-ux hang (~13 min unbounded wait) into a fast, diagnosable failure.

Purpose: both are MEDIUM-confidence items RESEARCH flagged as spikes (Assumptions A2/A3).
DET-03 #111 (`effective_prefix`/`NPM_CONFIG_PREFIX` via `as_user_login`) is LIKELY a
fixture/assertion detail — login-shell PATH+LANG propagate correctly on EL9 (proven via
SSH and the passing 22-agent-sudo modes) — but it MUST be verified, because if
`as_user_login` mis-exports `NPM_CONFIG_PREFIX` on EL9 it is a `plugin/lib/as_user.sh`
PRODUCT defect that is out of this test-only phase's scope and must be escalated, not
papered over. The tty-driver timeout is defensive: the underlying cause is the brownfield
fixture mis-setting EL9 state (fixed in Plan 20-02), but an unbounded pexpect wait should
never be able to hang the suite again. The DET-read-only invariant just needs `diffutils`
(Wave 1).
Output: DET-03 root-caused (test fix applied OR product escalation flagged with evidence);
tty-driver.py with a bounded pexpect timeout; the 15-preflight-ux hang eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-RESEARCH.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-PATTERNS.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-02-SUMMARY.md

<interfaces>
<!-- DET-03 probe lives in 15-detection.bats lines 184-208 (effective_prefix / -->
<!-- NPM_CONFIG_PREFIX via as_user_login). DET-read-only invariant at line 118 needs diffutils. -->
<!-- Spike command (run inside the booted almalinux-9 container): -->
```bash
sudo -u agent -i bash -lc 'echo NPM_CONFIG_PREFIX=$NPM_CONFIG_PREFIX; npm config get prefix'
# If NPM_CONFIG_PREFIX is the per-user prefix → DET-03 RED is a test/fixture detail (fix here).
# If it is empty/wrong → plugin/lib/as_user.sh product defect → ESCALATE (out of test-only scope).
```

<!-- tty-driver.py: a pexpect driver. Add a bounded timeout on spawn()/expect() so an -->
<!-- unexpected EL9 prompt fails fast instead of blocking ~13 min. Follow pexpect's -->
<!-- standard timeout= kwarg; no in-repo analog for the timeout shape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause DET-03 on EL9 — test/fixture fix or product escalation</name>
  <files>tests/bats/15-detection.bats</files>
  <read_first>tests/bats/15-detection.bats (DET-03 probe lines 184-208; DET-read-only invariant line 118); 20-PATTERNS.md "tests/bats/15-detection.bats — DET-03 spike" (lines 352-363); 20-RESEARCH.md §Open Questions 1 + Assumptions A2 (lines 249-252, 242); plugin/lib/as_user.sh (the login-shell export path).</read_first>
  <action>SPIKE FIRST, do not blind-edit. In a booted almalinux-9 container, run the §interfaces spike: `sudo -u agent -i bash -lc 'echo NPM_CONFIG_PREFIX=$NPM_CONFIG_PREFIX; npm config get prefix'`. Branch on the result: (A) if `NPM_CONFIG_PREFIX` resolves to the per-user prefix (the user-shell export is correct on EL9), then DET-03's RED is a test/fixture/assertion detail — fix it in 15-detection.bats so the DET-03 `@test` reads the value the same way the EL9 login shell exposes it (generalize the assertion to the family-correct observable; do NOT skip or weaken). (B) if `NPM_CONFIG_PREFIX` is empty or wrong, the login-shell export is broken on EL9 — this is a `plugin/lib/as_user.sh` PRODUCT defect that is OUT of this test-only phase's scope; do NOT edit product code here. Instead, document the evidence (the spike output) and ESCALATE: record it as a blocker in the plan SUMMARY and flag for a follow-up product plan/phase. The DET-read-only invariant test (line 118) needs no edit — it just requires `diffutils` (Wave 1) for its `diff`. State plainly in the SUMMARY which branch was taken and the spike evidence.</action>
  <verify>
    <automated>bash -n tests/bats/15-detection.bats && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The spike was run and its output recorded in the SUMMARY
    - Branch A: 15-detection.bats DET-03 generalized to the family-correct observable (no `skip`, no weaken); DET-03 green on almalinux-9 full-suite-in-order AND on ubuntu-24.04
    - Branch B: NO product edit made; an escalation note with the spike evidence is recorded in the SUMMARY and the DET-03 RED is explicitly flagged as a product follow-up (not silently left)
    - DET-read-only invariant test passes on almalinux-9 (with diffutils present)
  </acceptance_criteria>
  <done>DET-03 is root-caused on EL9: either fixed as a test/assertion detail (green on both rows) or escalated as a product defect with spike evidence — never papered over.</done>
</task>

<task type="auto">
  <name>Task 2: Add a bounded pexpect timeout to tty-driver.py</name>
  <files>tests/bats/helpers/tty-driver.py</files>
  <read_first>tests/bats/helpers/tty-driver.py (the pexpect spawn/expect calls); 20-PATTERNS.md "tests/bats/helpers/tty-driver.py — defensive timeout" (lines 367-375); 20-RESEARCH.md §Open Questions 2 + Assumptions A3 + §Don't Hand-Roll (lines 254-256, 243, 142).</read_first>
  <action>Add a bounded pexpect timeout so a stuck/unexpected prompt fails fast instead of blocking ~13 minutes (the EL9 15-preflight-ux hang at test ~138). Use pexpect's standard `timeout=` kwarg on `spawn()` and/or each `expect()` call; pick a bound generous enough for a real installer prompt cycle but well under the suite's patience (e.g. ~60s, or a sensible value matching the existing prompt cadence — read the file to choose). On timeout, exit non-zero with a clear diagnostic (which prompt was awaited) so the failure is diagnosable rather than a silent hang. This is DEFENSIVE — the underlying cause (brownfield fixture mis-setting EL9 state) is fixed in Plan 20-02 — but the timeout must land regardless so an unbounded wait can never hang the suite again. Do NOT hand-roll a manual sleep/poll loop; use pexpect's built-in timeout.</action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('tests/bats/helpers/tty-driver.py').read())" && grep -q 'timeout' tests/bats/helpers/tty-driver.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tty-driver.py` parses (`python3 -c "import ast; ast.parse(...)"`)
    - A bounded `timeout=` is set on the pexpect `spawn()`/`expect()` path (not `None`/infinite)
    - On timeout the driver exits non-zero with a diagnostic naming the awaited prompt
    - Suite-level proof: `bash tests/docker/run.sh almalinux-9` completes the 15-preflight-ux file without the ~13-min hang (full suite reaches files 18→60 in order); ubuntu-24.04 UX tests stay green
  </acceptance_criteria>
  <done>tty-driver.py enforces a bounded pexpect timeout; the EL9 TTY hang becomes a fast, diagnosable failure; the full suite runs to completion in order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login-shell env export → DET-03 probe | the probe reads NPM_CONFIG_PREFIX from the agent login shell to verify per-user npm ownership |
| pexpect driver → installer TTY | tty-driver.py drives interactive prompts in the privileged container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Repudiation | papering over a real EL9 product defect by editing the DET-03 assertion | mitigate | Spike-first; Branch B forbids product edits and mandates an escalation note with evidence — the defect is surfaced, not hidden. |
| T-20-13 | Denial of Service | an unbounded pexpect wait hangs the whole suite/CI | mitigate | Bounded `timeout=` converts the hang into a fast non-zero failure (Task 2). Test infra only. |
</threat_model>

<verification>
- `bash -n` / `ast.parse` + grep acceptance for both files.
- DET-03 spike evidence recorded; branch decision explicit.
- Wave gate: `bash tests/docker/run.sh almalinux-9` runs to completion (no TTY hang), DET-03 + DET-read-only resolved (green or escalated); `bash tests/docker/run.sh ubuntu-24.04` stays green.
</verification>

<success_criteria>
- DET-03 root-caused on EL9: fixed as a test detail (green both rows) or escalated with spike evidence.
- DET-read-only invariant green on EL9.
- tty-driver.py has a bounded pexpect timeout; the 15-preflight-ux hang is eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/20-behavior-test-green-on-almalinux-9/20-05-SUMMARY.md`
</output>
