---
phase: 20-behavior-test-green-on-almalinux-9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/docker/Dockerfile.almalinux-9
  - tests/docker/run.sh
autonomous: true
requirements: [PAR-01, EL-06, EL-08]
must_haves:
  truths:
    - "`diff`, `ssh`, and `ss` resolve inside the booted almalinux-9 container"
    - "bats stub harnesses can execve stubs written under /tmp (exec-able tmpfs)"
    - "systemd-in-Docker still reaches running/degraded on every row after the tmpfs change"
    - "The almalinux-9 full suite (`bash tests/docker/run.sh almalinux-9`) boots to running/degraded and the Phase-18 PATH-stub files (18-pkg-dispatch, 18-detect-el9) go green with the stubs UNTOUCHED — confirming the noexec-/tmp + missing-package substrate fix, not an assertion edit (supporting detail: 18-pkg-dispatch 20/20, 18-detect-el9 7/7)"
    - "All Ubuntu rows stay green byte-equivalent (the tmpfs flag is unconditional/shared)"
  artifacts:
    - path: "tests/docker/Dockerfile.almalinux-9"
      provides: "diffutils + openssh-clients + iproute (+ optional policycoreutils) in the dnf set"
      contains: "diffutils"
    - path: "tests/docker/run.sh"
      provides: "exec-able /tmp tmpfs for the shared docker run invocation"
      contains: "/tmp:exec"
  key_links:
    - from: "tests/docker/run.sh --tmpfs /tmp:exec"
      to: "BATS_TEST_TMPDIR stub execve (18-pkg-dispatch / 18-detect-el9)"
      via: "exec-able tmpfs default"
      pattern: "/tmp:exec"
    - from: "tests/docker/Dockerfile.almalinux-9 openssh-clients"
      to: "six-mode SSH assertions (BHV-02, 30/40/50/51)"
      via: "ssh client present in image"
      pattern: "openssh-clients"
---

<objective>
Wave 1 — Substrate. Fix the two root causes that flip ~40 false-RED tests green on
the almalinux-9 Docker row with ZERO assertion edits: (1) add the missing CLI tools
(`diffutils`, `openssh-clients`, `iproute`, and optionally `policycoreutils`) to the
`Dockerfile.almalinux-9` dnf block, and (2) flip the shared `tests/docker/run.sh`
tmpfs from `--tmpfs /tmp` (noexec) to `--tmpfs /tmp:exec` so PATH-stub harnesses can
`execve` their stubs.

Purpose: RESEARCH proved (live, in a booted almalinux:9 container) that ~2/3 of the
EL9 RED is substrate, not assertion logic. Both `18-pkg-dispatch` (14→0) and
`18-detect-el9` (3→0) go fully green with the stub harnesses UNTOUCHED the instant
`/tmp` is exec-able; `diff`/`ssh`/`ss` absence cascades across NO-MUTATION snapshots,
idempotency, and every six-mode SSH assertion. Doing substrate first collapses the
apparent PAR-01 surface to the genuine helper-generalization residue (Wave 2+).
Output: a rebuilt almalinux-9 image with the four tools present and an exec-able
/tmp shared across all rows; the Phase-18 stub files green on EL9 with no stub edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-RESEARCH.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-PATTERNS.md

<interfaces>
<!-- Current dnf block in Dockerfile.almalinux-9 (lines 88-99) — extend in place, -->
<!-- inside the single --setopt=install_weak_deps=False invocation. Do NOT add a -->
<!-- new RUN layer. -->
```dockerfile
RUN dnf install -y --setopt=install_weak_deps=False epel-release && \
    dnf install -y --setopt=install_weak_deps=False \
      systemd \
      cronie openssh-server \
      bats sudo \
      dbus-broker \
      jq \
      python3 \
      file \
      ca-certificates bash util-linux \
      procps-ng shellcheck && \
    dnf clean all && rm -rf /var/cache/dnf
```

<!-- Current shared docker run invocation in run.sh (tmpfs at line 116). run.sh is -->
<!-- the SINGLE entrypoint for all four targets (case "$TARGET" ~line 47-58); this -->
<!-- flag is therefore applied to Ubuntu rows too — that is intended and safe. -->
```bash
  --tmpfs /run --tmpfs /tmp \
```

<!-- systemd boot poll to re-verify after the tmpfs change (run.sh ~lines 130-143) -->
```bash
docker exec "$CID" systemctl is-system-running --wait   # must reach running/degraded
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diffutils + openssh-clients + iproute (+ policycoreutils) to the EL9 dnf block</name>
  <files>tests/docker/Dockerfile.almalinux-9</files>
  <read_first>tests/docker/Dockerfile.almalinux-9 (the dnf RUN block lines 88-99; the package-rationale comment block lines 69-87); 20-PATTERNS.md "Wave 1 — Substrate / Dockerfile.almalinux-9" (lines 36-72); 20-RESEARCH.md §Environment Availability table (lines 260-274).</read_first>
  <action>Inside the existing `dnf install -y --setopt=install_weak_deps=False` invocation (the second `dnf install`, lines 89-98), add three packages: `diffutils` (provides `diff` — absent → `diff: command not found` fails every NO-MUTATION snapshot + INST-02 idempotency + DET-read-only + UX-01/03 + REMEDIATE-02 + 40-registry-cli), `openssh-clients` (provides the `ssh` client — image only ships `openssh-server`; absent → BHV-02 + every six-mode SSH assertion in 30/40/50/51 fail), and `iproute` (provides `ss` for the sshd readiness poll in test setup; non-fatal but removes the blind poll). Optionally also add `policycoreutils` (provides `restorecon`) so the Wave-3 guarded restorecon call exercises the real binary path — the guard makes it safe to omit, so include it only if it does not pull heavy deps. Keep all additions inside the SAME `--setopt=install_weak_deps=False` invocation; do NOT add a separate RUN layer. Extend the package-rationale comment block (lines 69-87) in the same style, documenting why each new package is present. Do NOT add full `curl` or `coreutils` (they cause the EL9 curl-minimal / minimal-vs-full file conflicts the comment block already warns about).</action>
  <verify>
    <automated>grep -c 'diffutils' tests/docker/Dockerfile.almalinux-9 | grep -qv '^0$' && grep -q 'openssh-clients' tests/docker/Dockerfile.almalinux-9 && grep -q 'iproute' tests/docker/Dockerfile.almalinux-9 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'diffutils' tests/docker/Dockerfile.almalinux-9` ≥ 1
    - `grep -c 'openssh-clients' tests/docker/Dockerfile.almalinux-9` ≥ 1
    - `grep -c 'iproute' tests/docker/Dockerfile.almalinux-9` ≥ 1
    - The three packages sit inside the existing `--setopt=install_weak_deps=False` block (only ONE `dnf install` line for the package set; no new `RUN dnf install` layer was added)
    - No `curl` or `coreutils` added to the dnf set
  </acceptance_criteria>
  <done>The EL9 image dnf block installs diffutils, openssh-clients, iproute (and optionally policycoreutils) in the existing single invocation, with the rationale comment extended.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the shared run.sh tmpfs to /tmp:exec and re-verify systemd boot</name>
  <files>tests/docker/run.sh</files>
  <read_first>tests/docker/run.sh (the `docker run` invocation lines 111-119, tmpfs at line 116; the systemd boot poll lines 130-143); 20-PATTERNS.md "tests/docker/run.sh — SHARED across ALL rows" (lines 76-116); 20-RESEARCH.md §Code Examples "Exec-able tmpdir" (lines 187-198).</read_first>
  <action>On line 116, change `--tmpfs /run --tmpfs /tmp \` to `--tmpfs /run --tmpfs /tmp:exec \`. This is the most faithful single-point fix: tests still run under `/tmp`, but stubs written under `BATS_TEST_TMPDIR` (= `/tmp/bats-run-*`) become executable. Docker's default `--tmpfs /tmp` mounts `noexec`, so bash falls through a non-executable stub to the real `dnf`/`rpm`/`curl` (exit 0, empty capture → grep fails) or dies `126` (apt-get on EL9) — this is the proven root cause of 18-pkg-dispatch/18-detect-el9 RED. Apply the flag UNCONDITIONALLY (do NOT branch per-target): `run.sh` is the single entrypoint for all four targets, and exec-on-/tmp is the normal default outside Docker, so the Ubuntu rows execute identically (the same noexec /tmp was silently breaking the debian arm of the Phase-18 stub files too). After the change, rebuild + boot the almalinux-9 row and confirm the `systemctl is-system-running --wait` poll (lines 130-143) still reaches `running`/`degraded` (RESEARCH assumption A5 — exec /tmp must not perturb the systemd-in-Docker recipe).</action>
  <verify>
    <automated>grep -q -- '--tmpfs /tmp:exec' tests/docker/run.sh && ! grep -qE -- '--tmpfs /tmp( |$)' tests/docker/run.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `run.sh` contains `--tmpfs /tmp:exec`
    - No remaining bare `--tmpfs /tmp` (without `:exec`) in run.sh
    - The flag is unconditional (NOT inside a `case "$TARGET"` branch) — one shared invocation for all rows
    - Suite-level gate (Task wiring proof): `bash tests/docker/run.sh almalinux-9` boots systemd to running/degraded; `18-pkg-dispatch` reports 20/20 and `18-detect-el9` reports 7/7 with the stub harnesses unedited
  </acceptance_criteria>
  <done>run.sh mounts an exec-able /tmp for every target; systemd-in-Docker still boots; the two Phase-18 stub files are green on EL9 with no stub edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → privileged test container | `run.sh` launches `--privileged --cgroupns=host` containers with the repo bind-mounted read-only; this is test infrastructure, not a shipped artifact |
| image build → suite execution | Dockerfile dnf set defines which CLI tools the contract can exercise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | exec-able /tmp tmpfs lets harness stubs run code in the privileged container | accept | Test infra only; the container is ephemeral (`--rm`), the repo mount is read-only, and exec-on-/tmp is the normal default outside Docker. No shipped surface changes. |
| T-20-02 | Elevation | extra dnf packages (openssh-clients) enlarge the container toolset | accept | All four packages are stock AlmaLinux/EPEL; `openssh-server` is already present and enabled. No new privilege path; image is a CI/test artifact. |
| T-20-03 | Denial of Service | tmpfs change could break the systemd-in-Docker boot recipe | mitigate | Re-verify `systemctl is-system-running --wait` reaches running/degraded on every row after the change (Task 2 acceptance). |
</threat_model>

<verification>
- `grep` acceptance for both files (above).
- Wave gate: `bash tests/docker/run.sh almalinux-9` boots systemd and runs the full suite in order; confirm 18-pkg-dispatch (20/20) and 18-detect-el9 (7/7) are green with stubs unedited, and that the diff/ssh cascades have cleared from the substrate-dependent files.
- Ubuntu no-regression: `bash tests/docker/run.sh ubuntu-24.04` stays green (the tmpfs flag is shared/unconditional).
</verification>

<success_criteria>
- diffutils + openssh-clients + iproute present in the EL9 image; optionally policycoreutils.
- run.sh mounts `/tmp:exec` unconditionally; systemd still boots running/degraded.
- 18-pkg-dispatch and 18-detect-el9 green on EL9 with stub harnesses untouched.
- All Ubuntu rows remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/20-behavior-test-green-on-almalinux-9/20-01-SUMMARY.md`
</output>
