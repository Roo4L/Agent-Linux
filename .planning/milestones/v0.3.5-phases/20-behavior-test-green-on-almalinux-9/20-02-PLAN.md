---
phase: 20-behavior-test-green-on-almalinux-9
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tests/bats/helpers/distro.bash
  - tests/bats/helpers/brownfield.bash
autonomous: true
requirements: [PAR-01, EL-08]
must_haves:
  truths:
    - "A single distro-family fork point exists in tests/bats/helpers/distro.bash"
    - "distro.bash dispatches on /etc/os-release ID inside the container (no product libs required)"
    - "brownfield.bash builds correct EL9 fixture state (NodeSource RPM Node 22, dnf NOPASSWD grant, rpm -q presence gate)"
    - "Every debian arm is the current hardcoded line verbatim, so Ubuntu fixtures are byte-identical"
    - "REUSE-01/REMEDIATE-01..04/UX fixtures stop failing in setup on EL9"
  artifacts:
    - path: "tests/bats/helpers/distro.bash"
      provides: "family-dispatch verbs (distro_family, distro_locale_file, distro_assert_locale, distro_nodesource_repo_paths, distro_pkg_is_installed, distro_install_node22, distro_sudoers_pkg_line, distro_ssh_unit, distro_restore_ssh_context)"
      contains: "distro_family"
      min_lines: 60
    - path: "tests/bats/helpers/brownfield.bash"
      provides: "EL9-aware brownfield fixtures routed through distro.bash"
      contains: "distro_install_node22"
  key_links:
    - from: "tests/bats/helpers/brownfield.bash"
      to: "distro_install_node22 / distro_sudoers_pkg_line"
      via: "load 'helpers/distro' + verb calls"
      pattern: "distro_install_node22"
    - from: "distro_family"
      to: "/etc/os-release ID"
      via: "container-side os-release read"
      pattern: "os-release"
---

<objective>
Wave 2 — Helper generalization (the genuine PAR-01 / EL-08 scope, foundation half).
Create the single distro-family fork point `tests/bats/helpers/distro.bash` (the
verb set RESEARCH names), then route the biggest consumer — `brownfield.bash` — through
it so the four-state brownfield fixtures (Reuse / Create / Remediate / Bail) build
correct EL9 state instead of hardcoded `dpkg-query` / `curl deb.nodesource.com` /
`apt-get install nodejs` / NOPASSWD-for-apt.

Purpose: `brownfield.bash` is "the single biggest work item" (RESEARCH) — its hardcoded
Debian fixture sites fail in `setup`/`_brownfield_baseline` on EL9, cascading the entire
REUSE-03 / REMEDIATE-01..04 / UX-01/03 / 15-preflight-ux family to RED. distro.bash is
the contract all downstream Wave-3 plans (20-03/04/05) consume, so it is built first
(interface-first). Every verb's `debian` arm is the current hardcoded line lifted
byte-for-byte, so the Ubuntu rows execute identical commands — only the `case` selector
is new.
Output: distro.bash with the 9-verb dispatch set; brownfield.bash with all five Debian
fixture sites routed through it; a CI grep guard pinning apt/dpkg/deb/locale strings to
the debian arm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-RESEARCH.md
@.planning/phases/20-behavior-test-green-on-almalinux-9/20-PATTERNS.md
@.claude/skills/behavior-test-contract/SKILL.md

<interfaces>
<!-- Sourced-helper invariant (copy from invoke_modes.bash:9-13, assertions.bash:5-8, -->
<!-- brownfield.bash:26-27): NO `set -euo pipefail` at top — strict mode in a sourced -->
<!-- library leaks into bats and breaks TAP. Consumers add `load 'helpers/distro'`. -->

<!-- Family-detect entry verb (RESEARCH §Helper Design lines 125-130) — container-side, -->
<!-- no product libs required: -->
```bash
# tests/bats/helpers/distro.bash — sourced via `load 'helpers/distro'`. No set -euo (sourced).
distro_family() {
  [[ -n "${_AGENTLINUX_TEST_FAMILY:-}" ]] && { printf '%s' "$_AGENTLINUX_TEST_FAMILY"; return 0; }
  local id=""; [[ -r /etc/os-release ]] && id=$(. /etc/os-release; printf '%s' "${ID:-}")
  case "$id" in almalinux) _AGENTLINUX_TEST_FAMILY=rhel ;; *) _AGENTLINUX_TEST_FAMILY=debian ;; esac
  printf '%s' "$_AGENTLINUX_TEST_FAMILY"
}
```

<!-- distro_install_node22 full pattern (RESEARCH §Code Examples lines 213-225) — the -->
<!-- debian arm is exactly brownfield.bash:86-89 / 139-141 / 503-507 verbatim: -->
```bash
distro_install_node22() {
  case "$(distro_family)" in
    rhel)
      command -v node >/dev/null && rpm -q nodejs >/dev/null 2>&1 && return 0
      curl -fsSL https://rpm.nodesource.com/setup_22.x | bash - >/dev/null 2>&1
      dnf -y module reset nodejs >/dev/null 2>&1 || true
      dnf install -y nodejs >/dev/null 2>&1 ;;
    debian)
      dpkg-query -W -f='${Status}' nodejs 2>/dev/null | grep -q 'install ok installed' && return 0
      curl -fsSL https://deb.nodesource.com/setup_22.x | bash - >/dev/null 2>&1
      DEBIAN_FRONTEND=noninteractive apt-get install -y nodejs >/dev/null 2>&1 ;;
  esac
}
```

<!-- Full verb set required by the residue (RESEARCH §Helper Design table, lines 110-120; -->
<!-- 20-PATTERNS.md lines 166-176). debian arm = current verbatim line: -->
<!--   distro_family               rhel|debian from /etc/os-release ID (cached) -->
<!--   distro_locale_file          rhel:/etc/locale.conf            debian:/etc/default/locale -->
<!--   distro_assert_locale <VAR>  grep ^VAR=C.UTF-8 in the family locale file -->
<!--   distro_nodesource_repo_paths rhel:/etc/yum.repos.d/nodesource-nodejs.repo  debian:/etc/apt/sources.list.d/nodesource.sources -->
<!--   distro_pkg_is_installed <p> rhel: rpm -q   debian: dpkg-query -W -f='${Status}' | grep -q 'install ok installed' -->
<!--   distro_install_node22       (above) -->
<!--   distro_sudoers_pkg_line <u> rhel:'agent ALL=(ALL) NOPASSWD: /usr/bin/dnf'  debian:'... /usr/bin/apt-get, /usr/bin/apt' -->
<!--   distro_ssh_unit             rhel:sshd   debian:ssh -->
<!--   distro_restore_ssh_context <dir> rhel: command -v restorecon >/dev/null && restorecon -R -F "$dir" || true   debian: : -->

<!-- DON'T HAND-ROLL (RESEARCH §Don't Hand-Roll): where a PRODUCT lib is already sourced -->
<!-- (the 13/14/15 lib-chain tests), reuse the product verb nodesource_repo_paths -->
<!-- (plugin/lib/pkg.sh:145, already family-correct) rather than re-hardcoding repo paths. -->
<!-- Use distro_nodesource_repo_paths ONLY container-side where no product lib is loaded -->
<!-- (e.g. 10-installer INST-02). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/bats/helpers/distro.bash with the 9-verb family-dispatch set</name>
  <files>tests/bats/helpers/distro.bash</files>
  <read_first>tests/bats/helpers/detection.bash (smallest/cleanest verb file — copy its header + shape); tests/bats/helpers/invoke_modes.bash (lines 9-13 sourced-helper invariant); 20-PATTERNS.md "tests/bats/helpers/distro.bash (NEW helper)" (lines 122-205); 20-RESEARCH.md §Distro-Aware Helper Design (lines 104-132).</read_first>
  <action>Create the NEW helper as a SOURCED library (NO `set -euo pipefail` at top — strict mode in a sourced file leaks into bats and breaks TAP; state this invariant in the header, copying the wording from invoke_modes.bash lines 9-13). Open with the standard header doc-comment block (path line `# tests/bats/helpers/distro.bash` + purpose paragraph + design-invariants list, mirroring detection.bash lines 1-16). Implement all nine verbs from the §interfaces verb table using flat `distro_*` snake_case names: `distro_family` (cached, reads /etc/os-release ID → rhel/debian — use the exact entry verb from §interfaces), `distro_locale_file`, `distro_assert_locale <LANG|LC_ALL>`, `distro_nodesource_repo_paths`, `distro_pkg_is_installed <pkg>`, `distro_install_node22` (use the full pattern in §interfaces verbatim), `distro_sudoers_pkg_line <user>`, `distro_ssh_unit`, and `distro_restore_ssh_context <dir>` (rhel arm = guarded `command -v restorecon >/dev/null && restorecon -R -F "$dir" || true`; debian arm = `:` no-op). Each verb is a two-arm `case "$(distro_family)"` where the `debian` arm is the CURRENT hardcoded line from brownfield.bash/invoke_modes.bash/20-agent-user.bats lifted byte-for-byte (so Ubuntu stays identical) and the `rhel` arm is the EL9 equivalent from the verb table. Do NOT source any product lib here — distro.bash must work container-side standalone.</action>
  <verify>
    <automated>bash -n tests/bats/helpers/distro.bash && for v in distro_family distro_locale_file distro_assert_locale distro_nodesource_repo_paths distro_pkg_is_installed distro_install_node22 distro_sudoers_pkg_line distro_ssh_unit distro_restore_ssh_context; do grep -q "^${v}()" tests/bats/helpers/distro.bash || { echo "MISSING $v"; exit 1; }; done && ! grep -qE '^set -euo' tests/bats/helpers/distro.bash && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bats/helpers/distro.bash` exists and passes `bash -n`
    - All nine verbs defined (`grep -q '^distro_<verb>()'` for each)
    - No top-level `set -euo pipefail`
    - `distro_family` reads `/etc/os-release` and caches via `_AGENTLINUX_TEST_FAMILY`
    - `distro_restore_ssh_context` rhel arm is guarded (`command -v restorecon`); debian arm is `:`
    - Per-file smoke under exec TMPDIR: `docker exec <cid> bash -c 'cd /workspace && source tests/bats/helpers/distro.bash && distro_family'` prints `rhel` on almalinux-9 and `debian` on ubuntu-24.04
  </acceptance_criteria>
  <done>distro.bash provides the full family-dispatch verb set, container-side, with every debian arm verbatim and the restorecon verb guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Route brownfield.bash's five Debian fixture sites through distro.bash + add CI grep guard</name>
  <files>tests/bats/helpers/brownfield.bash</files>
  <read_first>tests/bats/helpers/brownfield.bash (sites: NOPASSWD-for-apt sudoers `setup_brownfield_host` lines 74-83; Node-present gate + NodeSource install lines 85-90; `_brownfield_baseline` NodeSource block lines 139-142; REMEDIATE-03 narrow grant line 285; second NodeSource block lines 503-507; header invariant lines 26-27); 20-PATTERNS.md "tests/bats/helpers/brownfield.bash" (lines 209-259).</read_first>
  <action>Add `load`-free sourcing: brownfield.bash is itself a helper sourced by tests; add a `source` of distro.bash relative to its own dir (or rely on the consuming test's `load 'helpers/distro'` ordering — match how brownfield.bash already obtains its siblings). Then replace the five hardcoded Debian sites with distro.bash verb calls, leaving the debian behavior byte-identical (the verb's debian arm IS the current line): (a) the NOPASSWD-for-apt sudoers fragment at lines 74-83 → use `distro_sudoers_pkg_line agent` to produce the grant line (rhel `/usr/bin/dnf`, debian `/usr/bin/apt-get, /usr/bin/apt`); keep the `visudo -cf` validation + `install -m 0440 -o root -g root` exactly as is. (b) the Node-present gate + NodeSource install at lines 85-90 → replace the whole `if ! dpkg-query … apt-get install nodejs` block with a single `distro_install_node22` call. (c) the `_brownfield_baseline` NodeSource block at lines 139-142 → `distro_install_node22`. (d) the REMEDIATE-03 narrow grant at line 285 (`/usr/bin/apt-get` only) → `distro_sudoers_pkg_line agent` narrow form (keep it the narrow single-binary grant per family). (e) the second NodeSource block at lines 503-507 → `distro_install_node22`. Leave the family-agnostic `0440` ADR-012 `NOPASSWD: ALL` sudoers line (line 136) untouched. After the edits, add a CI grep guard (a comment + a runnable check the executor verifies) so `grep -rn 'apt-get\|dpkg-query\|deb.nodesource' tests/bats/*.bats tests/bats/helpers/brownfield.bash` returns NOTHING (all such strings now live only in the debian arm of distro.bash).</action>
  <verify>
    <automated>bash -n tests/bats/helpers/brownfield.bash && grep -q 'distro_install_node22' tests/bats/helpers/brownfield.bash && ! grep -qE 'apt-get|dpkg-query|deb\.nodesource' tests/bats/helpers/brownfield.bash && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `brownfield.bash` passes `bash -n`
    - `grep -q 'distro_install_node22' tests/bats/helpers/brownfield.bash` (≥3 call sites replaced)
    - `grep -rnE 'apt-get|dpkg-query|deb\.nodesource' tests/bats/helpers/brownfield.bash` returns nothing (zero matches outside distro.bash)
    - `grep -rnE 'apt-get|dpkg-query|deb\.nodesource|/etc/default/locale|systemctl start ssh\b' tests/bats/*.bats tests/bats/helpers/*.bash` matches ONLY inside the debian arm of distro.bash
    - The `0440` `NOPASSWD: ALL` ADR-012 sudoers line is unchanged
    - Suite-level proof: on almalinux-9, `13-reuse` REUSE-03 E2E and `14-remediate` REMEDIATE-01..04 fixtures build (no longer fail in setup); on ubuntu-24.04 the same files stay green byte-equivalent
  </acceptance_criteria>
  <done>All five brownfield Debian fixture sites dispatch through distro.bash; the EL9 fixtures build NodeSource Node 22 + the dnf NOPASSWD grant; the grep guard confirms no apt/dpkg/deb strings leak outside the distro.bash debian arm; Ubuntu fixtures unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → fixture-built host state | brownfield fixtures install Node + sudoers grants into the privileged container to simulate brownfield hosts |
| family-dispatch fork | distro.bash selects which package manager / sudoers grant the fixtures use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Elevation | brownfield NOPASSWD sudoers fixtures grant `agent` passwordless package-manager access | mitigate | `distro_sudoers_pkg_line` emits a NARROW per-family grant (`/usr/bin/dnf` or `/usr/bin/apt-get`), validated via `visudo -cf` and installed `0440 root:root` — same posture as the current Debian fixture; the broad `NOPASSWD: ALL` is the unchanged ADR-012 line. Test infra, ephemeral container. |
| T-20-05 | Tampering | a stray apt/dpkg string outside the debian arm would silently break Ubuntu or weaken EL9 | mitigate | CI grep guard pins apt/dpkg/deb/locale strings to the distro.bash debian arm (Task 2 acceptance). |
| T-20-06 | Spoofing | distro_family misreads /etc/os-release → wrong arm | accept | Container-side read of a trusted in-image file; matches on `ID=almalinux` exactly (mirrors product `detect_distro`); verified by the per-file family smoke. |
</threat_model>

<verification>
- `bash -n` + verb-presence grep for distro.bash; `bash -n` + grep guard for brownfield.bash.
- Family smoke: `distro_family` prints `rhel` on almalinux-9, `debian` on ubuntu-24.04.
- Wave gate: `bash tests/docker/run.sh almalinux-9` — 13-reuse/14-remediate fixtures build (no setup failures); `bash tests/docker/run.sh ubuntu-24.04` stays green.
</verification>

<success_criteria>
- distro.bash exists with all nine verbs, sourced-safe, container-side.
- brownfield.bash routes all five Debian sites through distro.bash; grep guard clean.
- EL9 brownfield fixtures build correct state; Ubuntu fixtures byte-equivalent.
</success_criteria>

<output>
After completion, create `.planning/phases/20-behavior-test-green-on-almalinux-9/20-02-SUMMARY.md`
</output>
