# Phase 21 Plan 01 — Browser-launch deps + catalog verify (REC-01)

**Goal:** the three catalog agents install and pass health checks on AlmaLinux 9,
and the playwright-cli recipe makes Chromium genuinely launch on both families.

## Tasks

1. **Recipe — browser-launch deps** (`plugin/catalog/agents/playwright-cli/install.sh`)
   Add a family-dispatched deps step after `install --skills`: detect family via
   `/etc/os-release` (standalone, runner.ts does not export the family); debian →
   bundled Playwright `install-deps chromium`; rhel → explicit verified `dnf`
   list. Fail-closed (the deps are now part of the install contract). Verified
   on-box on both families before writing.

2. **Recipe — uninstall note** (`.../uninstall.sh`)
   Document the deliberate asymmetry: shared OS browser-deps are NOT removed on
   uninstall (removal could break unrelated host software).

3. **Test — AGT-06** (`tests/bats/50-agents.bats`)
   Lock the launch capability on both Docker rows: locate the downloaded
   Chromium, assert `ldd` reports 0 missing libs, assert a headless
   `--dump-dom about:blank` launch exits 0 and emits `<html>`. Same observable
   on both families (PAR-01).

4. **Docs** (`docs/internals/playwright.md`)
   Correct the inaccurate apt-deps narrative; describe the real, now-implemented
   family-dispatched deps step (Playwright install-deps on Ubuntu; explicit dnf
   list on EL9).

5. **Traceability** (`.planning/REQUIREMENTS.md`)
   REC-01 → Done with the resolution + AGT-06 reference.

## Verification
- `bats tests/docker/run.sh almalinux-9` and `ubuntu-24.04` both green
  (full suite, incl. new AGT-06).
- shellcheck clean; catalog schema valid; distro-leak guard green.

## Goal-backward check
REC-01 demands the three agents install + health-check on EL9 (Phase 20 proved
it) and the Chromium question resolved by smoke (done — and, per the user's
scope choice, the launch gap closed on both families and locked by AGT-06).
