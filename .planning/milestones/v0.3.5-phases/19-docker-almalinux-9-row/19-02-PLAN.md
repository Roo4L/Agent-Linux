---
phase: 19-docker-almalinux-9-row
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [HARN-01]

must_haves:
  truths:
    - ".github/workflows/test.yml `bats-docker` job carries an `almalinux-9` arm in the matrix (dimension generalized `ubuntu`→`target`) with `fail-fast: false` already present"
    - ".github/workflows/release.yml `gate-2-docker` job carries an `almalinux-9` arm in the matrix (dimension generalized `ubuntu`→`target`) with `fail-fast: false`"
    - "Each renamed matrix dimension's `${{ matrix.<name> }}` consumer is renamed in lockstep (same edit) so the Ubuntu arms still pass a non-empty target to run.sh"
    - "`gate-3-qemu` and `gate-4-pinned-combo` in release.yml are byte-for-byte unchanged (Phase 22 / ADR-011 — out of scope)"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "bats-docker matrix.target incl. almalinux-9; run step consumes matrix.target"
      contains: "almalinux-9"
    - path: ".github/workflows/release.yml"
      provides: "gate-2-docker matrix.target incl. almalinux-9; gate-3/gate-4 untouched"
      contains: "almalinux-9"
  key_links:
    - from: ".github/workflows/test.yml matrix.target"
      to: "tests/docker/run.sh almalinux-9 arm (Plan 01)"
      via: "run: bash tests/docker/run.sh ${{ matrix.target }}"
      pattern: "matrix.target"
    - from: ".github/workflows/release.yml gate-2 matrix.target"
      to: "tests/docker/run.sh almalinux-9 arm (Plan 01)"
      via: "run: bash tests/docker/run.sh ${{ matrix.target }}"
      pattern: "matrix.target"
---

<objective>
Wire the `almalinux-9` arm into the CI Docker matrix in the two workflows that
drive `tests/docker/run.sh` — `test.yml`'s `bats-docker` job and `release.yml`'s
`gate-2-docker` job — by generalizing each job's matrix dimension `ubuntu`→`target`,
appending `- almalinux-9`, and renaming the `${{ matrix.<name> }}` consumer in the
SAME edit. `fail-fast: false` is already present in both jobs so a red Alma arm
still reports the Ubuntu arms.

Purpose: HARN-01 (CI half). The alma arm calls `run.sh almalinux-9` (Plan 01) —
that arm may legitimately be partially red until Phase 20 (PAR-01); `fail-fast:
false` isolates it.
Output: an `almalinux-9` matrix arm in `test.yml` and `release.yml` gate-2 ONLY.

CRITICAL SCOPE: edit `test.yml` `bats-docker` and `release.yml` `gate-2-docker`
ONLY. Leave `release.yml` `gate-3-qemu` (`matrix.ubuntu` — QEMU EL9 is Phase 22)
and `gate-4-pinned-combo` (hardcoded `ubuntu-24.04` — ADR-011) byte-for-byte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-docker-almalinux-9-row/19-PATTERNS.md

<interfaces>
<!-- Exact target blocks. Each job owns its own strategy.matrix — adding the alma
     arm to the Docker matrix does NOT touch the QEMU matrix. -->

test.yml `bats-docker` (block form) — matrix at lines 130-135, consumer at 153:
```yaml
    strategy:
      fail-fast: false            # line 130 — already present, keep
      matrix:
        ubuntu:                   # line 132 → rename to `target:`
          - ubuntu-22.04
          - ubuntu-24.04
          - ubuntu-26.04
          # append: - almalinux-9
    ...
        run: bash tests/docker/run.sh ${{ matrix.ubuntu }}   # line 153 → ${{ matrix.target }}
```

release.yml `gate-2-docker` (inline flow-list form) — matrix at lines 136-138,
consumer at 142:
```yaml
    strategy:
      fail-fast: false            # line 136 — already present, keep
      matrix:
        ubuntu: [ubuntu-22.04, ubuntu-24.04, ubuntu-26.04]   # line 138 → target: [..., almalinux-9]
    ...
        run: bash tests/docker/run.sh ${{ matrix.ubuntu }}   # line 142 → ${{ matrix.target }}
```

DO NOT TOUCH (release.yml): gate-3-qemu `matrix.ubuntu: ['22.04','24.04','26.04']`
(line 155), `bash tests/qemu/boot.sh ${{ matrix.ubuntu }}` (line 209), the cache
keys (204/206) and artifact name (218) that reference `matrix.ubuntu`; and
gate-4-pinned-combo's hardcoded `bash tests/docker/run.sh ubuntu-24.04` (line 238).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test.yml bats-docker — matrix ubuntu→target + almalinux-9 arm</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    .github/workflows/test.yml `bats-docker` job (lines 114-153 — matrix block
    130-135, run step 153) and 19-PATTERNS.md §".github/workflows/test.yml".
  </read_first>
  <action>
In the `bats-docker` job ONLY:
1. Rename the matrix dimension `ubuntu:` (line 132) → `target:`.
2. Append `- almalinux-9` as a fourth list item after `- ubuntu-26.04`.
3. Rename the consumer (line 153) `run: bash tests/docker/run.sh ${{ matrix.ubuntu }}`
   → `${{ matrix.target }}` in the SAME edit (the dimension + its consumer are a
   unit — a mismatch passes an empty target to run.sh → exit 64 on every arm).
`fail-fast: false` (line 130) is already present — leave it. The
`Skip if no bats suite yet` guard step is unchanged.
  </action>
  <verify>
    <automated>command -v yamllint >/dev/null && yamllint -d relaxed .github/workflows/test.yml || python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/test.yml"))'; grep -q 'almalinux-9' .github/workflows/test.yml && grep -q '${{ matrix.target }}' .github/workflows/test.yml && ! grep -q 'matrix.ubuntu' .github/workflows/test.yml</automated>
  </verify>
  <done>
    `bats-docker` matrix dimension is `target` listing the three Ubuntu arms plus
    `almalinux-9`; the run step consumes `${{ matrix.target }}`; no `matrix.ubuntu`
    remains in test.yml; YAML parses; `fail-fast: false` retained.
  </done>
</task>

<task type="auto">
  <name>Task 2: release.yml gate-2-docker — matrix ubuntu→target + almalinux-9 (gate-3/4 untouched)</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    .github/workflows/release.yml `gate-2-docker` (lines 127-142), and confirm
    `gate-3-qemu` (148-221) + `gate-4-pinned-combo` (229-238) are NOT in your
    edit set; 19-PATTERNS.md §".github/workflows/release.yml" + 19-RESEARCH.md
    Pitfall 3 (renaming the QEMU/pinned-combo dimension is the warning sign).
  </read_first>
  <action>
In the `gate-2-docker` job ONLY (inline flow-list form):
1. Change `ubuntu: [ubuntu-22.04, ubuntu-24.04, ubuntu-26.04]` (line 138) →
   `target: [ubuntu-22.04, ubuntu-24.04, ubuntu-26.04, almalinux-9]`.
2. Change the consumer (line 142) `run: bash tests/docker/run.sh ${{ matrix.ubuntu }}`
   → `${{ matrix.target }}` in the SAME edit.
`fail-fast: false` (line 136) already present — leave it.
DO NOT TOUCH gate-3-qemu (`matrix.ubuntu` at 155, `boot.sh ${{ matrix.ubuntu }}`
at 209, cache keys 204/206, artifact name 218) or gate-4-pinned-combo's hardcoded
`run.sh ubuntu-24.04` at 238 — those keep `matrix.ubuntu` / hardcoded values
(Phase 22 / ADR-011). After editing, `matrix.ubuntu` MUST still appear (gate-3
retains it) — that is the correctness signal, not a leftover.
  </action>
  <verify>
    <automated>command -v yamllint >/dev/null && yamllint -d relaxed .github/workflows/release.yml || python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/release.yml"))'; grep -q 'target: \[ubuntu-22.04, ubuntu-24.04, ubuntu-26.04, almalinux-9\]' .github/workflows/release.yml && [ "$(grep -c 'tests/docker/run.sh ${{ matrix.target }}' .github/workflows/release.yml)" -eq 1 ] && grep -q "boot.sh \${{ matrix.ubuntu }}" .github/workflows/release.yml && grep -q 'tests/docker/run.sh ubuntu-24.04' .github/workflows/release.yml</automated>
  </verify>
  <done>
    `gate-2-docker` matrix is `target: [..., almalinux-9]` and its run step consumes
    `${{ matrix.target }}` (exactly one such Docker consumer); `gate-3-qemu` still
    uses `matrix.ubuntu` + `boot.sh ${{ matrix.ubuntu }}`; `gate-4-pinned-combo`
    still hardcodes `run.sh ubuntu-24.04`; YAML parses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow → CI runner | matrix fan-out controls which `run.sh` targets execute on the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Elevation | CI job token scope on the edited jobs | mitigate | Keep the existing `permissions: contents: read` default; add NO token scope. The alma arm reuses the unchanged gate-2/bats-docker job permissions. |
| T-19-06 | Tampering | Accidental edit to gate-3-qemu / gate-4 release gate | mitigate | Scope edits to `bats-docker` + `gate-2-docker` only; verify commands assert `matrix.ubuntu` survives in gate-3 and the hardcoded `run.sh ubuntu-24.04` survives in gate-4 (RESEARCH Pitfall 3 warning signs as gates). |
| T-19-07 | Spoofing/Input | Matrix value injection | accept | Matrix values are a fixed in-repo allow-list, not user input (ASVS V5 N/A). |

Note: CI-config edit only; no auth/session/crypto surface. No high-severity item.
</threat_model>

<verification>
- `test.yml` `bats-docker` matrix dimension is `target` with four arms incl. `almalinux-9`; run step consumes `${{ matrix.target }}`; no `matrix.ubuntu` in test.yml.
- `release.yml` `gate-2-docker` matrix is `target: [..., almalinux-9]`; exactly one Docker run step consumes `${{ matrix.target }}`.
- `release.yml` `gate-3-qemu` still uses `matrix.ubuntu` (+ `boot.sh ${{ matrix.ubuntu }}`); `gate-4-pinned-combo` still hardcodes `run.sh ubuntu-24.04`.
- Both workflow files parse as valid YAML.
</verification>

<success_criteria>
- The CI Docker matrix (PR-time `bats-docker` + release-gate `gate-2-docker`) spans the three Ubuntu arms plus `almalinux-9`, with `fail-fast: false` keeping the Ubuntu arms reporting if Alma is red.
- The QEMU release gate (gate-3) and pinned-combo gate (gate-4) are untouched — EL9 QEMU is Phase 22, pinned-combo is ADR-011.
</success_criteria>

<output>
After completion, create `.planning/phases/19-docker-almalinux-9-row/19-02-SUMMARY.md`
noting the two matrix edits, the lockstep consumer renames, and confirmation that
gate-3/gate-4 are unchanged (with the git-diff scope as evidence).
</output>
