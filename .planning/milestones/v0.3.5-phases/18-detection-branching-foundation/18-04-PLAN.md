---
phase: 18-detection-branching-foundation
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - plugin/bin/agentlinux-install
autonomous: true
requirements: [EL-02]
must_haves:
  truths:
    - "The entrypoint sources plugin/lib/pkg.sh in the lib sourcing block, immediately after distro_detect.sh and before idempotency.sh"
    - "ensure_jq installs jq via `pkg_install jq` (no inline apt-get)"
    - "run_purge removes the NodeSource repo files by iterating nodesource_repo_paths, and removes nodejs via pkg_remove / pkg_autoremove"
    - "The agent-user removal, sudoers rm, and log removal in run_purge stay distro-agnostic and unchanged"
    - "Ubuntu behavior is byte-for-byte preserved (debian arms of the verbs match the prior inline commands)"
  artifacts:
    - path: "plugin/bin/agentlinux-install"
      provides: "pkg.sh sourcing + ensure_jq + run_purge routed through pkg verbs"
      contains: "/pkg.sh"
  key_links:
    - from: "plugin/bin/agentlinux-install"
      to: "plugin/lib/pkg.sh"
      via: ". \"$LIB_DIR/pkg.sh\" after distro_detect.sh"
      pattern: "LIB_DIR/pkg.sh"
    - from: "plugin/bin/agentlinux-install::run_purge"
      to: "plugin/lib/pkg.sh::nodesource_repo_paths"
      via: "purge iterates the family repo paths (lockstep with 30-nodejs gate)"
      pattern: "nodesource_repo_paths"
---

<objective>
Wire pkg.sh into the entrypoint: source it at the correct point in the lib-loading order so
every provisioner and detect fragment inherits the verbs, then convert the two entrypoint
call sites — `ensure_jq` and `run_purge` — to pkg.sh verbs. This completes the EL-02
"all ~13 call sites route through pkg.sh" requirement for the entrypoint, and keeps `--purge`
correct on EL9 (it must clean up the yum repo file it created, not the apt one).

Purpose: Without sourcing pkg.sh here the verbs are undefined at runtime; without the purge
conversion, `--purge` on EL9 leaves the NodeSource repo behind.
Output: agentlinux-install sources pkg.sh; ensure_jq + run_purge routed (EL-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-detection-branching-foundation/18-RESEARCH.md
@.planning/phases/18-detection-branching-foundation/18-PATTERNS.md
@.planning/phases/18-detection-branching-foundation/18-02-PLAN.md

<interfaces>
<!-- Current entrypoint sourcing block (plugin/bin/agentlinux-install:154-159) -->
```bash
. "$LIB_DIR/distro_detect.sh"
. "$LIB_DIR/idempotency.sh"   # ← pkg.sh must be sourced BEFORE this line
. "$LIB_DIR/as_user.sh"
```
detect_distro runs later (:433/435), so $AGENTLINUX_DISTRO_FAMILY is set before any verb is CALLED
(sourcing order is safe — verbs are only invoked after detect_distro).

Current ensure_jq (:280-289):
```bash
if ! command -v jq >/dev/null 2>&1; then
  log_warn "jq not found; installing 'jq' package (required by detection layer)"
  DEBIAN_FRONTEND=noninteractive apt-get update
  DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends jq
fi
```

Current run_purge NodeSource + nodejs cleanup (:386-397):
```bash
rm -f /etc/apt/sources.list.d/nodesource.sources
rm -f /etc/apt/sources.list.d/nodesource.list
rm -f /etc/apt/preferences.d/nodejs
if [[ $remove_nodejs == true ]]; then
  DEBIAN_FRONTEND=noninteractive apt-get purge -y nodejs || log_warn "apt purge nodejs failed"
  DEBIAN_FRONTEND=noninteractive apt-get autoremove -y || true
fi
```

pkg.sh verbs available: pkg_install, pkg_remove, pkg_autoremove, nodesource_repo_paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source pkg.sh in the entrypoint lib-loading block</name>
  <files>plugin/bin/agentlinux-install</files>
  <read_first>
    - plugin/bin/agentlinux-install:139-160 (the sourcing block + ERR trap)
    - 18-PATTERNS.md "plugin/bin/agentlinux-install (MOD)" item 1 (lines 172-187) + 18-RESEARCH.md Assumption A3
  </read_first>
  <action>
    In plugin/bin/agentlinux-install insert a `. "$LIB_DIR/pkg.sh"` line (with the matching
    `# shellcheck source=../lib/pkg.sh` directive) immediately AFTER the `distro_detect.sh` source
    (:155) and BEFORE `idempotency.sh` (:156). locale_ensure's rhel arm uses write_file_atomic from
    idempotency.sh, but pkg.sh only DEFINES functions at source time (no calls), so sourcing pkg.sh
    before idempotency.sh is safe — the function is resolved at call time inside provisioner 10.
    Do not reorder any other source line.
  </action>
  <acceptance_criteria>
    - `grep -q 'LIB_DIR/pkg.sh' plugin/bin/agentlinux-install` succeeds
    - pkg.sh is sourced after distro_detect.sh and before idempotency.sh — verify ordering:
      `awk '/distro_detect.sh/{d=NR} /pkg.sh/{p=NR} /idempotency.sh/{i=NR} END{exit !(d<p && p<i)}' plugin/bin/agentlinux-install`
    - `shellcheck plugin/bin/agentlinux-install` exits 0
  </acceptance_criteria>
  <verify>
    <automated>shellcheck plugin/bin/agentlinux-install && awk '/distro_detect.sh/{d=NR} /pkg.sh/{p=NR} /idempotency.sh/{i=NR} END{exit !(d<p && p<i)}' plugin/bin/agentlinux-install</automated>
  </verify>
  <done>pkg.sh is sourced at the correct point; verbs are available to all provisioners and detect fragments.</done>
</task>

<task type="auto">
  <name>Task 2: ensure_jq + run_purge → pkg verbs</name>
  <files>plugin/bin/agentlinux-install</files>
  <read_first>
    - plugin/bin/agentlinux-install:278-289 (ensure_jq) and :383-410 (run_purge NodeSource + nodejs + agent-user cleanup)
    - 18-PATTERNS.md "plugin/bin/agentlinux-install (MOD)" items 2 & 3 (lines 189-201) + "Cross-Cutting Path Branch" (lines 361-374)
    - plugin/lib/pkg.sh (pkg_install, pkg_remove, pkg_autoremove, nodesource_repo_paths)
  </read_first>
  <action>
    1. ensure_jq (:286-287): replace the two apt-get lines with `pkg_install jq`. Keep the
       `command -v jq` guard and the log_warn.
    2. run_purge NodeSource repo removal (:387-389): replace the three hardcoded `rm -f` lines with a
       loop over `nodesource_repo_paths` (e.g. `while IFS= read -r f; do rm -f "$f"; done < <(nodesource_repo_paths)`)
       so EL9's `/etc/yum.repos.d/nodesource-nodejs.repo` is cleaned on purge — in lockstep with the
       30-nodejs idempotency gate and the detect gate.
    3. run_purge nodejs removal (:394-396): replace `apt-get purge -y nodejs` / `apt-get autoremove -y`
       with `pkg_remove nodejs || log_warn "purge nodejs failed"` and `pkg_autoremove || true`.
    Keep `rm -f /etc/sudoers.d/agentlinux` (:384), the agent-user removal (:402-409), and the log
    removal (:411+) distro-agnostic and unchanged.
  </action>
  <acceptance_criteria>
    - `grep -q 'pkg_install jq' plugin/bin/agentlinux-install` succeeds
    - `grep -q 'nodesource_repo_paths' plugin/bin/agentlinux-install` succeeds
    - `grep -q 'pkg_remove nodejs' plugin/bin/agentlinux-install` AND `grep -q 'pkg_autoremove' plugin/bin/agentlinux-install`
    - `grep -c 'apt-get' plugin/bin/agentlinux-install` is 0
    - `grep -q 'rm -f /etc/sudoers.d/agentlinux' plugin/bin/agentlinux-install` still succeeds (agnostic cleanup intact)
    - `shellcheck plugin/bin/agentlinux-install` exits 0
  </acceptance_criteria>
  <verify>
    <automated>shellcheck plugin/bin/agentlinux-install && test "$(grep -c apt-get plugin/bin/agentlinux-install)" = 0 && grep -q 'pkg_install jq' plugin/bin/agentlinux-install</automated>
  </verify>
  <done>ensure_jq and run_purge are distro-neutral; --purge cleans the family-correct NodeSource repo files; no apt-get remains in the entrypoint (EL-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → run_purge | `--purge` deletes system files (repo configs, sudoers drop-in, agent user) as root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Tampering | run_purge repo-path loop deleting wrong files | mitigate | Paths come only from `nodesource_repo_paths` (static in-code literals per family), not from user input or globbing; each is an explicit `rm -f <known path>` — no wildcard expansion |
| T-18-14 | Repudiation | purge leaving the EL9 NodeSource repo behind (incomplete teardown) | mitigate | Routing purge through the same `nodesource_repo_paths` source of truth as the install gate guarantees install/purge symmetry on both families |
| T-18-15 | Injection | pkg_install jq / pkg_remove nodejs args | accept | Args are fixed literals (jq, nodejs); no untrusted string reaches the verb |
</threat_model>

<verification>
- `shellcheck plugin/bin/agentlinux-install` clean.
- `grep -rn 'apt-get\|dpkg' plugin/bin/agentlinux-install` returns nothing.
- Sourcing order asserted (distro_detect → pkg → idempotency).
- Install/purge symmetry: both 30-nodejs and run_purge reference `nodesource_repo_paths`.
</verification>

<success_criteria>
The entrypoint sources pkg.sh at the correct order point and routes ensure_jq + run_purge through
pkg verbs, completing the entrypoint half of EL-02 with no residual apt-get and a purge that cleans
the family-correct NodeSource repo.
</success_criteria>

<output>
After completion, create `.planning/phases/18-detection-branching-foundation/18-04-SUMMARY.md`
</output>
