---
phase: 13-pillar-3-candidate-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
  - .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md
autonomous: true
requirements: [EXPL-02]
must_haves:
  truths:
    - "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md exists at exactly that path"
    - "File body is between 2 KB (2048 bytes) and 12 KB (12288 bytes)"
    - "File's FIRST section is `## Verdict` (literal heading) — Phase 15's grep anchor for the pillar-count decision"
    - "Verdict section contains exactly one bolded `**Verdict:**` line so `grep -E '\\*\\*Verdict:'` returns exactly one match; the line declares (b) Fold into Pillar 2 as sub-concern per CONTEXT.md lock"
    - "File ends with a `## Decision summary` section (literal heading) — Phase 15's grep anchor for the lift target"
    - "Decision summary names ≥2 table-stakes (the fold subsumes Pillar 2's existing curated catalog + admission criteria), ≥1 differentiator (the supply-chain monitoring commitment, may be `(none — intentional)` if Claude lands there but CONTEXT.md commits to the supply-chain commitment), ≥2 explicit non-goals (NG-1, NG-2, NG-3), recommended priority tag (`next-milestone` for the fold inheriting Pillar 2's tag, `opportunistic` for Appendix B Security Hardening theme), DOC-05 disposition (closes as N/A in 15-AUDIT.md)"
    - "Body cites ≥7 distinct grep tokens from `(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)` — at minimum: OWASP LLM Top 10 v2025, Lethal Trifecta OR Agents Rule of Two, ≥2 attacks from {Shai-Hulud, chalk/debug, TrustFall, Cline-via-markdown}, ≥2 defenses from {npm provenance, SLSA, cosign, Anthropic devcontainer, bubblewrap, capability-scoped sudoers}, the ADR-012 NOPASSWD tension"
    - "Body uses forward-looking voice (subject = 'we' / 'our roadmap' / explicit milestone tag) for every unshipped commitment — never 'AgentLinux + present-tense forbidden verb (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)' — voice-rule grep returns zero matches"
    - "Phase-close audit `14-AUDIT.md` exists at `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` (canonical path, NOT the ROADMAP success-criterion-5 typo `13-pillar-3-exploration` without 'candidate'); audit's first content line records the verdict so Phase 15's planner can grep it; audit cites file path + line range of `## Verdict` + line range of `## Decision summary` + verbatim grep transcripts; emits GREEN gate"
  artifacts:
    - path: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      provides: "Pillar 3 candidate exploration verdict (b) + Decision summary section authoritative for Phase 15"
      contains: "## Verdict"
    - path: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      provides: "Decision summary section Phase 15 lifts verbatim into STRATEGY.md (and DOC-05 closes as N/A on)"
      contains: "## Decision summary"
    - path: ".planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md"
      provides: "Phase-close audit with single-line verdict header + grep transcripts + GREEN gate verdict"
      contains: "GREEN"
  key_links:
    - from: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      to: ".planning/phases/14-pillar-3-candidate-exploration/14-CONTEXT.md"
      via: "decisions block lifted into doc body (verdict-(b) line, supply-chain monitoring commitment, NG-1/NG-2/NG-3, ADR-012 tension framing, priority tag)"
      pattern: "(NG-1|NG-2|NG-3|next-milestone|opportunistic|supply-chain|ADR-012)"
    - from: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      to: "docs/exploration/PILLAR-2-NOTES.md"
      via: "fold target — Decision summary names Pillar 2's curated catalog + ADR-011 + admission criteria as the table-stakes the fold subsumes"
      pattern: "(Pillar 2|ADR-011|curated catalog|catalog admission)"
    - from: ".planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md"
      to: "docs/exploration/PILLAR-3-CANDIDATE-NOTES.md"
      via: "single-line verdict header + grep transcripts + line-range citation of Verdict + Decision summary sections"
      pattern: "PILLAR-3-CANDIDATE-NOTES.md"
---

<objective>
Author the canonical pillar 3 candidate exploration verdict
(`docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`) and the phase-close audit
(`14-AUDIT.md`) for the EXPL-02 gate.

The exploration doc must lift the locked decisions from `14-CONTEXT.md` into a
published verdict that Phase 15 can grep-anchor and read in two ways:

1. The `## Verdict` heading (first section) carries one bolded `**Verdict:**`
   line declaring **(b) Fold into Pillar 2 as sub-concern. Security is not a
   separate pillar in v0.3.3.** This is the single decision point Phase 15
   reads to set its pillar count (2, not 3) and DOC-05 disposition (closes as
   N/A in `15-AUDIT.md`).
2. The `## Decision summary` heading (last section) carries the lift-target
   substance: the fold's subsumed table-stakes, the supply-chain monitoring
   differentiator, NG-1/NG-2/NG-3 non-goals, the priority tag (`next-milestone`
   inherited via the fold + `opportunistic` Appendix B theme), DOC-05
   disposition.

The audit must record the verdict on its first content line (so Phase 15's
planner can read it in one grep), cite file path + line range of `## Verdict`
section + line range of `## Decision summary` section + verbatim grep
transcripts, and emit GREEN. The audit lands at the canonical path
`.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md`; ROADMAP.md's
success-criterion 5 references `.planning/phases/13-pillar-3-exploration/...`
without "candidate" — that path is a typo predating the canonical phase slug;
the audit notes the divergence in a single line at the top.

Purpose: Decide and publish whether security is a pillar at AgentLinux (it is
not, under verdict (b)) and what the one substantive forward-looking
commitment from the security landscape is (active supply-chain monitoring +
curated catalog admission, folded into Pillar 2). Phase 15 does not re-open
these decisions; Phase 16's website ships 2 pillar cards (not 3); DOC-05
closes as N/A.

Output: Two new files —
- `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` (≥2 KB, ≤12 KB; first heading
  is `## Verdict` with one bolded `**Verdict:**` line; ends with `## Decision
  summary`)
- `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` (cites
  EXPL-02 evidence, single-line verdict header at top, GREEN gate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-pillar-3-candidate-exploration/14-CONTEXT.md
@.planning/phases/13-pillar-2-exploration/13-CONTEXT.md
@.planning/phases/13-pillar-2-exploration/13-01-PLAN.md
@.planning/phases/13-pillar-2-exploration/13-AUDIT.md
@docs/exploration/PILLAR-2-NOTES.md
@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@docs/decisions/012-agent-user-full-sudo.md
@docs/decisions/011-stability-first-version-pinning.md
@docs/decisions/006-curl-pipe-bash-plus-deb.md

<voice_rule>
**Voice rule (PITFALLS.md Pitfall #6, applied to the entire body — Phase 13
applied it only to the Direction subsection; Phase 14 applies it
project-wide because security claims about unshipped behaviour are the single
highest aspirational-drift risk in the milestone):**

- Every sentence about an UNSHIPPED behaviour MUST have grammatical subject =
  "we" / "our roadmap" / explicit milestone tag (e.g. "the v0.6+ Security
  Hardening theme", "in the next milestone we"). NEVER subject = "AgentLinux"
  + present-tense verb for unshipped behaviour.
- Sentences about SHIPPED behaviour (e.g. ADR-011 curated combo + pinned
  versions, ADR-006 SHA256 verify on installer, AGT-02 zero-EACCES
  self-update) may use "AgentLinux <verb>" delivered-fact voice with citations
  to v0.3.0 ADRs / bats tests / release artifacts. Where security verbs
  overlap with delivered-fact (e.g. "AgentLinux's installer SHA256-verifies
  the tarball before executing"), prefer the safer forward-looking phrasing
  ("our installer verifies SHA256 before executing") OR cite ADR-006 inline so
  the delivered-fact context is unambiguous.
- **Forbidden patterns (any line, anywhere in the doc body):**
  `AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents) <object>`.
  Phase-close gate runs `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` and MUST return zero matches.
- **Required patterns in any forward-looking commitment block:**
  "we will hold...", "our roadmap commits to...", "the v0.6+ Security
  Hardening theme...", "in the next milestone we...".

Phase 15's STRAT-11 grep gate runs against `docs/STRATEGY.md`. Phase 15 lifts
this doc's `## Decision summary` verbatim, so applying the rule here prevents
drift from propagating downstream. The whole body — not just the Decision
summary — must pass because Phase 15 may also lift framing prose into the
Considered-and-rejected / Trade-offs sections.
</voice_rule>

<grep_tokens>
**EXPL-02 success-criterion 3 — distinct named-reference grep tokens (≥7 required):**

The body of `PILLAR-3-CANDIDATE-NOTES.md` MUST contain literal text matching
at least 7 distinct tokens from this set:

```
OWASP
Lethal Trifecta
Rule of Two
Shai-Hulud
chalk
TrustFall
Cline
provenance
SLSA
cosign
bubblewrap
ADR-012
```

Minimum substantive coverage (not just any 7 tokens, but a credible threat-and-defense landscape):
- OWASP LLM Top 10 v2025 (1 token: OWASP)
- Lethal Trifecta OR Agents Rule of Two (1 token from: `Lethal Trifecta`, `Rule of Two`)
- ≥2 attacks from {Shai-Hulud, chalk/debug, TrustFall, Cline-via-markdown} — pick at least 2 (2 tokens)
- ≥2 defenses from {npm provenance, SLSA, cosign, Anthropic devcontainer, bubblewrap, capability-scoped sudoers} — pick at least 2 (2 tokens; note `Anthropic devcontainer` and `capability-scoped sudoers` do NOT match the regex set, so the picks must come from {provenance, SLSA, cosign, bubblewrap} to count toward the gate)
- ADR-012 NOPASSWD tension (1 token: ADR-012)

Total: 7 distinct tokens minimum. Recommended approach: cite all 12 tokens
across the body's three thematic sections (`## Threat landscape we
considered`, `## Defenses we considered`, `## ADR-012 tension`). The gate
becomes mechanical and the safety margin is large.

CONTEXT.md `<specifics>` makes this elegant: each token is cited as
**considered and either acknowledged-but-declined-as-pillar-substance OR
folded-into-Pillar-2-as-defense-in-depth**. Verdict (b) does not mean we
ignore the threat landscape; it means we acknowledge the landscape and decline
to commit to the substantive heavy defenses (cosign-signed catalog, bubblewrap
recipes, capability-scoped sudoers) as pillar substance. They live in
Appendix B as `opportunistic` v0.6+ themes.
</grep_tokens>

<doc_structure>
**Recommended structure for `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`
(Claude's discretion on prose voice + section ordering after `## Verdict`,
but the section anchors below are NOT discretionary):**

```markdown
# Pillar 3 Candidate Exploration — Security Hardening (declined as a pillar)

> Phase 14 verdict. Phase 15 lifts the `## Decision summary` section verbatim
> into `docs/STRATEGY.md` Appendix B's "Security Hardening" theme entry (NOT
> into a Pillar 3 section — verdict (b) means there is no Pillar 3).
> DOC-05 closes as N/A in `15-AUDIT.md`.
>
> Locked: 2026-05-10. Source raw material: `.planning/research/SUMMARY.md` §5
> (pillar-3 substance summary), `.planning/research/FEATURES.md` Pillar 3
> section (lines 97-200, threat-and-defense inventory),
> `.planning/research/PITFALLS.md` (voice rule + rejected-alternatives
> discipline). Locked decisions:
> `.planning/phases/14-pillar-3-candidate-exploration/14-CONTEXT.md`.

## Verdict

[FIRST section. The single-paragraph verdict + the bolded one-liner that the
grep gate anchors on. Exactly one line in the section matches `**Verdict:**`.]

**Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.

[2-4 sentences explaining the verdict in plain language: only 2 pillars ship
in `docs/STRATEGY.md`; one substantive forward-looking commitment — active
supply-chain monitoring + curated catalog admission — folds into Pillar 2 as
a sub-concern; "Security Hardening" stays as a v0.6+ `opportunistic` theme in
Appendix B, not as a pillar; DOC-05 (ADR-012 forward-reference to Pillar 3)
closes as N/A in `15-AUDIT.md`.]

## What folds into Pillar 2

[The supply-chain monitoring commitment. Forward-looking voice. Per
CONTEXT.md `<decisions>` "What folds into Pillar 2": our roadmap commits to
(1) monitoring public supply-chain disclosure news (Shai-Hulud-class npm worm
events, chalk/debug 2FA-phishing class events, TrustFall-class one-click RCEs
in coding agents, OWASP LLM Top 10 v2025 + Lethal Trifecta + Agents Rule of
Two as adopted reference frames); (2) refusing to bump pinned versions to
releases known to be compromised (the compat-guarded version pinning gate
already in Pillar 2 gains a security check alongside the compatibility
check); (3) keeping new / untested / unreviewed projects out of the catalog
by default (admission criteria: existing security-research track record +
behaviour-tested via TST-08 4-gate pipeline + maintainer reputation; no
sight-unseen admission). Explicit phrasing: "we monitor public disclosures;
we never line-by-line audit Claude Code's source." This rejects the
upstream-source-code-audit reading.]

## Threat landscape we considered

[Cites the threat-landscape grep tokens with one-line rejection / acknowledgment
rationale per item, per Pitfall #13 rejected-alternatives discipline. At
minimum cite: OWASP LLM Top 10 v2025, Lethal Trifecta OR Agents Rule of Two,
≥2 attacks from {Shai-Hulud, chalk/debug, TrustFall, Cline-via-markdown}.
Recommended: cite all 5+ to maximise grep-token margin. Each citation is
prose ("the Shai-Hulud npm worm of late 2025...") not a bare token list, so
the body reads as substantive considered-and-rejected analysis, not gate
gaming.]

## Defenses we considered

[Cites the defense-landscape grep tokens with one-line rejection rationale
per item — declines them as pillar substance, defers them to Appendix B as
v0.6+ `opportunistic` themes. At minimum: ≥2 from {npm provenance, SLSA,
cosign, bubblewrap, capability-scoped sudoers}. Recommended: cite npm
provenance, SLSA, cosign, bubblewrap, capability-scoped sudoers (5 picks,
margin). Anthropic devcontainer is good additional context but does NOT match
the EXPL-02 grep regex (the regex doesn't include "devcontainer" or
"Anthropic"). Pick from the 4 regex-matching defense tokens (provenance,
SLSA, cosign, bubblewrap) for gate counting.]

## ADR-012 tension

[The unresolved security debt. Per CONTEXT.md `<decisions>` "ADR-012
NOPASSWD ALL — noted but unresolved" and `<specifics>` "ADR-012 tension
paragraph adopts research §5E recommended phrasing in spirit": ADR-012 was a
defensible scope choice at v0.3.0 — the agent was framed as a trusted
coworker; the alternative (capability-scoped sudoers allowlists) was
rejected because Phase 5 showed agents need apt + systemctl + many other
things and an ever-growing allowlist is its own maintenance burden. After
Shai-Hulud / TrustFall / the Lethal Trifecta framing in late 2025, that
"trusted coworker" framing is harder to defend: a single successful prompt
injection on the agent — one bad README in one cloned repo — converts the
agent into an adversary with NOPASSWD root. Position: defensible v0.3.0
choice, recognized debt now; resolution is a v0.6+ `opportunistic` theme in
Appendix B (Security Hardening), NOT a pillar commitment. DOC-05 closes as
N/A in `15-AUDIT.md` because pillar 3 does not exist; the unresolved tension
is recorded in pillar 2's section as a known limitation. The token `ADR-012`
appears in this section literally for the grep gate.]

## Why verdict (b) and not (a/c/d)

[Per CONTEXT.md `<decisions>` "Why (b) and not (a/c/d)" — Pitfall #13
rejected-alternatives discipline. One paragraph or bullet per:
- (a) Full pillar — rejected. A full pillar 3 needs ≥2 honest already-shipped
  table-stakes, ≥1 differentiator, ≥2 explicit non-goals. ADR-006
  (curl-pipe-bash + SHA256) and ADR-011 (curated combo + pinned versions) are
  stability + installer-integrity mechanisms, not security pillars; stretching
  them into "security" framing forces aspirational drift per Pitfall #6.
- (c) Cross-cutting Guiding Principle — rejected. Same substance as (b) but
  in a less prominent location. Pillar 2 already commits to compat-guarded
  version pinning; supply-chain monitoring is a natural extension of the same
  mechanism. Folding into Pillar 2 keeps the commitment visible where
  reviewers actually read.
- (d) Explicit non-goal — rejected. Contradicts the supply-chain monitoring
  commitment. We are taking a position on supply-chain hygiene; we are not
  taking a position on agent runtime sandboxing or model-level guardrails.]

## Decision summary

[LAST section. Lifted into Phase 15 verbatim. Per CONTEXT.md `<decisions>`
"Decision summary structure (per EXPL-02 success criterion 4)":]

**Verdict (single line):** **Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.

[NOTE: this is the SECOND occurrence of the bolded `**Verdict:**` line.
Re-read EXPL-02 success criterion 2: `grep -E '\*\*Verdict:'` returns
**exactly one match** in the doc. Therefore the Decision summary section
restates the verdict WITHOUT the bolded `**Verdict:**` prefix. Use a
non-bolded form like `Verdict: (b) Fold into Pillar 2 as sub-concern.
Security is not a separate pillar in v0.3.3.` — same substance, different
formatting, no second grep hit. The single bolded line lives only in the
top `## Verdict` section.]

**What folds into Pillar 2 (the one new commitment):** Active supply-chain
monitoring + curated catalog admission, mechanism-shared with Pillar 2's
compat-guarded version pinning gate.

**Table-stakes that the fold subsumes (≥2):**
- Pillar 2's existing curated catalog with `pinned_version` per agent (ADR-011)
  — incidentally closes the `--all-latest` window for supply-chain attacks.
- Pillar 2's existing curated admission criteria — only claude-code, gsd, and
  playwright admitted to date; new projects are not admitted sight-unseen.

**Differentiators (≥1):** The supply-chain monitoring + compromised-version
refusal commitment named above. (Differentiator under verdict (b) lives in
Pillar 2's section in `docs/STRATEGY.md`.)

**Explicit non-goals (≥2):**
- **NG-1 — Not providing model-level guardrails.** Llama Guard, ShieldGemma,
  IBM Granite Guardian, Prompt Guard, NeMo Guardrails — properly the agent or
  model vendor's surface. We cite as context, never as deliverable.
- **NG-2 — Not vetting the content of upstream agent code.** We pin and
  monitor; we never line-by-line audit Claude Code's source. Catalog
  acceptance is by behaviour + provenance signals + maintainer reputation +
  the supply-chain monitoring above — not by upstream code review.
- **NG-3 — Not becoming a sandbox runtime.** No commitment to ship an
  `agentlinux harden` profile, capability-scoped sudoers, bubblewrap-based
  recipes, Landlock / seccomp-bpf wrappers, or iptables egress allowlists.
  These primitives exist already in the kernel and elsewhere (Anthropic's
  devcontainer demonstrates the pattern); a user who wants that posture
  composes the off-the-shelf primitives themselves. gVisor, Firecracker, Kata
  Containers exist and we do not compete.

**Recommended priority tag:**
- The fold inherits Pillar 2's `next-milestone` priority tag — pillar 3 does
  not exist under verdict (b), so there is no separate pillar-3 tag.
- Appendix B "Security Hardening" theme is tagged `opportunistic` for v0.6+
  (capability-scoped sudoers replacing ADR-012 NOPASSWD ALL, cosign-signed
  catalog releases, npm provenance verification in CI, bubblewrap-based
  per-recipe sandbox profile, iptables egress allowlist — all DECLINED as
  pillar substance under verdict (b), all eligible as `opportunistic` v0.6+
  Appendix B theme content).

**DOC-05 disposition:** Closes as N/A in `15-AUDIT.md`. Single-line rationale:
pillar 3 does not exist; the ADR-012 tension is recorded in pillar 2's
section as a known limitation (forward-pointer text but no ADR file edit
required).
```

**Section ordering and prose voice are Claude's discretion AFTER the `## Verdict`
opening section, BUT the following anchors are NOT discretionary:**

- Title `# Pillar 3 Candidate Exploration — Security Hardening (declined as a pillar)`
  (or equivalent H1 carrying the same substance).
- The FIRST section after the H1 is exactly `## Verdict` (literal heading,
  Phase 15's pillar-count grep anchor).
- The Verdict section contains exactly one line matching `**Verdict:**` —
  `grep -cE '\*\*Verdict:'` returns exactly 1.
- A trailing `## Decision summary` section (literal heading, Phase 15's
  lift-target grep anchor).
- The Decision summary section MUST contain: a non-bolded restatement of the
  verdict (no second `**Verdict:**` bolded line); ≥2 table-stakes bullets;
  ≥1 differentiator bullet (or `(none — intentional)` if Claude lands there,
  but CONTEXT.md commits to the supply-chain commitment as the differentiator);
  ≥2 non-goal bullets (NG-1, NG-2, NG-3 — all 3 recommended for full
  CONTEXT.md fidelity); the priority tag (`next-milestone` inherited via the
  fold + `opportunistic` for the Appendix B theme — both literals must
  appear); the DOC-05 disposition.
- Body must contain ≥7 distinct grep tokens from the EXPL-02 set.
- File body size between 2 KB (2048 bytes) and 12 KB (12288 bytes).
</doc_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/exploration/PILLAR-3-CANDIDATE-NOTES.md from CONTEXT.md decisions</name>
  <files>docs/exploration/PILLAR-3-CANDIDATE-NOTES.md</files>
  <read_first>
    - .planning/phases/14-pillar-3-candidate-exploration/14-CONTEXT.md (the entire file — `<decisions>` block is the substance to lift; verdict (b) lock + supply-chain monitoring commitment + NG-1/NG-2/NG-3 + ADR-012 tension framing + priority tag are all locked here; `<specifics>` carries the "we monitor public disclosures; we never line-by-line audit Claude Code's source" phrasing + the ADR-012 tension paragraph in spirit; `<deferred>` block tells you what NOT to commit to)
    - .planning/research/SUMMARY.md §5 (pillar-3 substance summary; threat landscape + defenses + ADR-012 tension recommended phrasing)
    - .planning/research/FEATURES.md lines 97-200 (Pillar 3 section — full threat-and-defense inventory; cite specific incidents like Shai-Hulud npm worm of late 2025 + chalk/debug 2FA phishing of Sept 8 2025 + TrustFall Adversa AI disclosure + Cline data-exfiltration-via-markdown-image; defense inventory cites npm provenance + SLSA + cosign + bubblewrap + capability-scoped sudoers + Anthropic devcontainer)
    - .planning/research/PITFALLS.md (Pitfall #6 voice rule, Pitfall #13 rejected-alternatives discipline, Pitfall #14 false-advertising warning — all directly applicable)
    - docs/exploration/PILLAR-2-NOTES.md (the prior phase's verdict — read it end-to-end as the voice/structure reference; this doc's `## Decision summary` section feeds back into Pillar 2's section in STRATEGY.md, so the two docs must be coherent)
    - .planning/phases/13-pillar-2-exploration/13-01-PLAN.md (mirror this plan's shape — the `<doc_structure>` template is the closest analog, adapted)
    - .planning/REQUIREMENTS.md (EXPL-02 success criteria 1–5)
    - .planning/ROADMAP.md (Phase 14 success criteria block)
    - docs/decisions/012-agent-user-full-sudo.md (ADR-012 — read so the tension paragraph quotes accurately + the in-doc cross-link is correct)
  </read_first>
  <action>
    Author `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` per the structure in
    the plan's `<doc_structure>` block. Lift CONTEXT.md's locked decisions
    verbatim where they are content (verdict (b) line, supply-chain monitoring
    commitment, NG-1/NG-2/NG-3 non-goals, priority tag inheritance, DOC-05
    disposition). Apply the voice rule (`<voice_rule>`) rigorously to the
    ENTIRE body — every claim about unshipped behaviour gets subject "we" /
    "our roadmap" / explicit milestone tag, never "AgentLinux + present-tense
    forbidden verb". The forbidden verb set is
    `(provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)`.

    Specifically (per CONTEXT.md `<decisions>`):

    - **Title:** `# Pillar 3 Candidate Exploration — Security Hardening (declined as a pillar)`
      or equivalent H1 carrying the same substance. The "(declined as a
      pillar)" suffix is recommended because it sets reader expectations
      before they hit the `## Verdict` section.
    - **`## Verdict` (FIRST section):** Open with the bolded one-liner per
      CONTEXT.md lock —
      `**Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.`
      This MUST be the only `**Verdict:**` bolded line in the entire file
      (the Decision summary at the bottom restates the verdict in non-bolded
      form). Follow with 2-4 sentences in plain language explaining the
      verdict's downstream consequences: only 2 pillars in `docs/STRATEGY.md`;
      DOC-05 closes as N/A; "Security Hardening" stays as a v0.6+ Appendix B
      `opportunistic` theme.
    - **`## What folds into Pillar 2`:** The single substantive forward-looking
      commitment — active supply-chain monitoring + curated catalog admission.
      Phrase per CONTEXT.md `<decisions>` "What folds into Pillar 2":
      (1) monitoring public supply-chain disclosure news (Shai-Hulud-class npm
      worm events, chalk/debug 2FA-phishing class events, TrustFall-class
      one-click RCEs in coding agents, OWASP LLM Top 10 v2025 + Lethal
      Trifecta + Agents Rule of Two as adopted reference frames);
      (2) refusing to bump pinned versions to compromised releases (security
      check on the compat-guarded gate); (3) curated catalog admission with
      named criteria (existing security-research track record + behaviour-
      tested via TST-08 4-gate pipeline + maintainer reputation; no
      sight-unseen admission). Include the explicit rejection of upstream
      source-code audit per CONTEXT.md `<specifics>`: literal phrasing along
      the lines of "we monitor public disclosures; we never line-by-line
      audit Claude Code's source." Forward-looking voice throughout — subject
      "we" / "our roadmap".
    - **`## Threat landscape we considered`:** Cite the threat-landscape grep
      tokens with one-line rejection / acknowledgment rationale per item.
      Required minimum: OWASP LLM Top 10 v2025 (token: `OWASP`), Lethal
      Trifecta OR Agents Rule of Two (tokens: `Lethal Trifecta`, `Rule of
      Two`), ≥2 attacks from {Shai-Hulud, chalk/debug, TrustFall, Cline-via-
      markdown} (tokens: `Shai-Hulud`, `chalk`, `TrustFall`, `Cline`).
      Recommended: cite all 5+ for safety margin. Each in prose with
      contextual detail from FEATURES.md lines 97-200 (e.g. "Shai-Hulud npm
      worm — CISA AA25 disclosure, late 2025, ~25,000 malicious repositories
      across ~350 maintainers"; "chalk/debug + 16 other packages compromise
      via phishing of npm maintainer 'qix' on 2025-09-08"; "TrustFall —
      Adversa AI disclosure, one-click RCE in Claude Code / Cursor / Gemini
      CLI / GitHub Copilot via crafted untrusted source"; "Cline data
      exfiltration via markdown image — embracethered.com Aug 2025, exploits
      auto-approve mode"). Acknowledgment voice: we acknowledge these as the
      late-2025 threat landscape; verdict (b) declines to commit to the
      heavy defenses as pillar substance.
    - **`## Defenses we considered`:** Cite defense-landscape grep tokens
      with one-line rejection rationale per item. The picks must come from
      tokens that match the EXPL-02 regex set: `provenance`, `SLSA`,
      `cosign`, `bubblewrap`. (Capability-scoped sudoers and Anthropic
      devcontainer are good context but do NOT match the regex; cite them
      anyway for substance, but make sure the gate-counted picks are from
      the regex-matching set.) ≥2 required, recommended: cite all 4
      regex-matching tokens for margin. Per CONTEXT.md framing: declined as
      pillar substance (verdict (b) means no pillar 3); deferred to Appendix
      B as v0.6+ `opportunistic` themes. Phrasing example: "npm provenance
      via Sigstore signing — closes the maintainer-account-takeover impact
      window — declined as pillar substance under verdict (b); we monitor
      adoption (12.6% of popular packages as of 2025) and may revisit in a
      v0.6+ Security Hardening theme."
    - **`## ADR-012 tension`:** Per CONTEXT.md `<specifics>` "ADR-012 tension
      paragraph adopts research §5E recommended phrasing in spirit":
      defensible scope choice at v0.3.0 (the agent was framed as a trusted
      coworker; capability-scoped sudoers allowlists were rejected because
      Phase 5 showed agents need apt + systemctl + many other things);
      recognized debt in late-2025 prompt-injection landscape (a single
      successful prompt injection on the agent — one bad README in one cloned
      repo — converts the agent into an adversary with NOPASSWD root, and the
      blast radius is the entire host); resolution is a v0.6+ `opportunistic`
      theme in Appendix B Security Hardening, NOT a pillar commitment. The
      token `ADR-012` MUST appear literally in this section for the grep
      gate. Adopt the spirit of FEATURES.md §5E recommended phrasing, but
      DON'T quote it verbatim — paraphrase with attribution to keep the doc
      coherent rather than collaged.
    - **`## Why verdict (b) and not (a/c/d)`:** Per CONTEXT.md `<decisions>`
      "Why (b) and not (a/c/d)" — Pitfall #13 rejected-alternatives
      discipline. One short paragraph or bullet per (a)/(c)/(d) covering:
      (a) Full pillar — rejected because needs ≥2 honest already-shipped
      table-stakes; ADR-006 + ADR-011 are stability/installer-integrity
      mechanisms, not security pillars; stretching forces aspirational drift.
      (c) Cross-cutting Guiding Principle — rejected because Pillar 2 fold
      keeps the commitment visible where reviewers read; cross-cutting is
      same substance in less-prominent location. (d) Explicit non-goal —
      rejected because contradicts the supply-chain monitoring commitment;
      we are taking a position on supply-chain hygiene; we are not taking
      a position on agent runtime sandboxing or model-level guardrails.
    - **`## Decision summary` (LAST section):** Lifted into Phase 15 verbatim.
      Per CONTEXT.md `<decisions>` "Decision summary structure":
      - Non-bolded verdict restatement (NOT `**Verdict:**` — that bolded form
        only exists once in the doc, in the top `## Verdict` section). Use
        e.g. "Verdict: (b) Fold into Pillar 2 as sub-concern..." or "**The
        verdict, restated:**" followed by plain text. The grep
        `grep -cE '\*\*Verdict:'` MUST return exactly 1.
      - **What folds into Pillar 2 (the one new commitment):** named.
      - **Table-stakes that the fold subsumes (≥2):** Pillar 2's existing
        curated catalog with `pinned_version` per agent (ADR-011) +
        Pillar 2's existing curated admission criteria.
      - **Differentiators (≥1):** the supply-chain monitoring + compromised-
        version refusal commitment.
      - **Explicit non-goals (≥2; CONTEXT.md commits to all 3):** NG-1, NG-2,
        NG-3 as detailed above.
      - **Recommended priority tag:** literal `next-milestone` (inherited via
        the fold) AND literal `opportunistic` (Appendix B Security Hardening
        theme). Both must appear in the Decision summary section.
      - **DOC-05 disposition:** closes as N/A in `15-AUDIT.md` with the
        single-line rationale.

    File-size constraint: aim for 5–10 KB body (substantive but not unbounded;
    this doc is denser than Phase 13's because it carries more named
    citations). Hard bounds are 2 KB ≤ size ≤ 12 KB.

    Forbidden content (per CONTEXT.md `<deferred>` and the
    scope_reduction_prohibition rule):
    - Do NOT commit to `agentlinux harden` profile — NG-3 declines pillar
      commitment; Appendix B opportunistic theme territory.
    - Do NOT commit to cosign-signed catalog releases as pillar substance —
      Appendix B opportunistic theme territory.
    - Do NOT commit to `npm audit signatures` CI lint or SBOM emission per
      release as pillar substance — Appendix B opportunistic theme territory.
    - Do NOT commit to `--ignore-scripts` opt-in policy as pillar substance —
      Appendix B opportunistic theme territory.
    - Do NOT commit to capability-scoped sudoers replacing ADR-012 NOPASSWD —
      Appendix B opportunistic theme territory.
    - Do NOT codify catalog admission criteria language beyond the spirit
      ("existing security-research track record + TST-08 behaviour-tested +
      maintainer reputation"); the exact policy locks when the catalog
      admission framework ships in a later milestone.
    - Do NOT use phrases like "v1", "v2", "simplified version", "static for
      now", "hardcoded for now", "placeholder", "future enhancement", "basic
      version", "minimal implementation" anywhere in the doc.
    - Do NOT introduce content that contradicts CONTEXT.md's locked decisions
      (verdict (a/c/d), pillar-3 sandbox commitment, model-level guardrails,
      upstream code audit).
    - Do NOT use the forbidden verb pattern `AgentLinux (provides|offers|
      ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|
      prevents) <object>` anywhere in the doc body — the Phase 15 STRAT-11
      voice-rule grep gate is enforced here as a project-wide gate (not just
      Direction subsection like Phase 13).
    - Do NOT leak Phase 15 content (do not author the strategy doc's pillar 3
      section here; verdict (b) means there is no Pillar 3 section to author).
    - Do NOT leak Phase 13 content (do not redefine pillar 2 substance; only
      reference it as the fold target).

    Per CONTEXT.md `<decisions>` "Claude's Discretion": section ordering after
    `## Verdict` is at Claude's discretion as long as the gate hits ≥7
    distinct tokens, the Verdict section is first, the Decision summary is
    last, and the bolded `**Verdict:**` line appears exactly once. Whether
    "Considered and rejected" content lives as separate sections (`## Threat
    landscape we considered` + `## Defenses we considered`) or as one
    consolidated section is at Claude's discretion. Whether each defense gets
    its own paragraph or a list with one-line rationales is at Claude's
    discretion.

    Note on the H1 + Verdict structure: the EXPL-02 success criterion 2 says
    "the file's FIRST section is a `## Verdict` heading." A `# H1` title
    above is acceptable (it is the document title, not a section). The first
    `##` heading must be `## Verdict`. Do NOT introduce any other `##`
    heading between the H1 and `## Verdict`.

    Reviewer pass (folded into Task 2's gate run, but flagged here): after
    the doc draft lands, before Task 2's grep gates, dispatch fact-checker +
    technical-writer reviewers per the AgentLinux project review loop
    (CLAUDE.md §Review Loop, doc-only file-type ruleset:
    `technical-writer + fact-checker + ai-deslop`). Pre-existing reviewer
    agents are already adapted to AgentLinux per `.claude/agents/fact-checker.md`
    + `.claude/agents/technical-writer.md` (commit 6d35d7e). Apply triage:
    fix valid findings (citation accuracy, voice-rule violations, scope
    creep), skip noise. Iterate until remaining comments are not actionable.
    The audit reflects the post-review file state. ai-deslop is OPTIONAL on
    this doc (research summaries / ADRs are listed as ai-deslop-skip in
    CLAUDE.md; this exploration doc sits in the same category — skip
    ai-deslop unless prose feels formulaic). The review loop runs as
    sub-tasks within this Task 1 — do NOT split into a separate task; the
    audit (Task 3) cites the post-review file.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md &amp;&amp; bytes=$(wc -c &lt; docs/exploration/PILLAR-3-CANDIDATE-NOTES.md) &amp;&amp; [ "$bytes" -ge 2048 ] &amp;&amp; [ "$bytes" -le 12288 ] &amp;&amp; echo "size OK ($bytes bytes)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns 0 (file exists)
    - `wc -c docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns a value in `[2048, 12288]` (between 2 KB and 12 KB)
    - First `##` heading in the file is `## Verdict`: `grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | head -1` shows `## Verdict` (the H1 `# ...` line is allowed and is not counted)
    - `grep -c '^## Verdict$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns exactly `1`
    - `grep -cE '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns exactly `1` (single bolded `**Verdict:**` line in the top Verdict section; Decision summary restates without the bold prefix)
    - The single `**Verdict:**` line declares (b): `grep -E '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -F '(b)'` returns 1 hit
    - `grep -c '^## Decision summary$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns exactly `1`
    - The `## Decision summary` heading is the LAST `##` heading in the file: `grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | tail -1` shows `## Decision summary`
    - `grep -E '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -oE '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' | sort -u | wc -l` returns a value `≥ 7` (≥7 distinct grep tokens — the EXPL-02 gate)
    - In the Decision summary section (lines from `## Decision summary` heading to EOF):
      - `grep -F 'next-milestone'` returns ≥1 hit (priority tag — fold inherits Pillar 2's tag)
      - `grep -F 'opportunistic'` returns ≥1 hit (priority tag — Appendix B Security Hardening theme)
      - At least 2 lines name table-stakes — recommended markers: `ADR-011`, `pinned_version`, `curated catalog`, `admission`
      - At least 1 line names the differentiator — recommended markers: `supply-chain monitoring`, `compromised`, `monitoring`, `disclosures`
      - At least 2 lines name explicit non-goals — recommended markers: `NG-1`, `NG-2`, `NG-3`
      - At least 1 line records DOC-05 disposition — recommended marker: `DOC-05` and one of `N/A` / `not applicable`
    - **Voice-rule project-wide check:** `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns ZERO matches anywhere in the file. This is the hard gate (Phase 15's STRAT-11 grep-gate equivalent applied here for safety because Phase 15 lifts content verbatim).
    - **Forbidden vocabulary check:** `grep -nE '\b(v1|v2|simplified version|static for now|hardcoded for now|placeholder|future enhancement|basic version|minimal implementation)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` returns 0 matches (regex is approximate; human review at audit time confirms no scope-reduction language slipped in. NOTE: this regex would false-positive on legitimate use of `v1` in something like "OWASP LLM Top 10 v2025" — manually filter these false positives during review; the spirit is "no scope-reduction vocabulary").
    - **Reviewer pass complete:** fact-checker + technical-writer reviewers dispatched per CLAUDE.md §Review Loop; valid findings addressed; remaining comments are not actionable; the file at audit-time reflects the post-review state.
  </acceptance_criteria>
  <done>
    File `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` exists, body is 2–12 KB,
    starts with `## Verdict` (after H1) carrying exactly one bolded
    `**Verdict:**` line declaring (b), ends with `## Decision summary`
    containing the lift-target substance (verdict restated unbolded, fold
    commitment, ≥2 table-stakes, ≥1 differentiator, NG-1+NG-2+NG-3,
    `next-milestone` + `opportunistic` priority tags, DOC-05 N/A
    disposition). Body cites ≥7 distinct EXPL-02 grep tokens. Voice rule
    passes project-wide (no forbidden `AgentLinux <verb>` pattern). No
    scope-reduction vocabulary leaked in. Reviewer pass (fact-checker +
    technical-writer) complete and findings addressed. Ready for grep-gate
    verification in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all EXPL-02 grep gates locally and capture transcripts</name>
  <files>docs/exploration/PILLAR-3-CANDIDATE-NOTES.md</files>
  <read_first>
    - docs/exploration/PILLAR-3-CANDIDATE-NOTES.md (the file just authored — read it end-to-end to find the line range of the `## Verdict` section AND the line range of the `## Decision summary` section for the audit's line-range citations in Task 3)
    - .planning/REQUIREMENTS.md (EXPL-02 success criteria 1–5 — the gates Task 2 verifies)
    - .planning/ROADMAP.md (Phase 14 "Success Criteria" block — same five criteria, in the form Phase-close audit will quote)
    - .planning/phases/13-pillar-2-exploration/13-AUDIT.md (the prior phase's audit — read so Task 3's audit format matches the established convention)
  </read_first>
  <action>
    Run each EXPL-02 gate command literally and capture both the command and
    its output. If any gate fails, return to Task 1, edit the doc to fix the
    failure, and re-run. Do NOT proceed to Task 3 until every gate passes.
    Capture the following gate transcripts (these become Task 3's audit
    evidence):

    1. **Gate 1 — file exists at exact path + size in [2 KB, 12 KB]:**
       ```
       test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md &amp;&amp; echo "EXISTS"
       wc -c docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       ```
       Pass: `EXISTS` printed AND `wc -c` value in `[2048, 12288]`.

    2. **Gate 2 — `## Verdict` is the FIRST `##` section + exactly one bolded `**Verdict:**` line declaring (b):**
       ```
       grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | head -1
       grep -c '^## Verdict$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       grep -cE '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       grep -E '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       ```
       Pass: first `##` heading is `## Verdict`; `^## Verdict$` count is 1;
       `\*\*Verdict:` count is exactly 1; the matched line contains `(b)`
       (the verdict declaration).

    3. **Gate 3 — ≥7 distinct named-reference hits from the EXPL-02 set:**
       ```
       grep -E '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
         | grep -oE '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' \
         | sort -u
       ```
       Pass: output is ≥7 distinct lines.

    4. **Gate 4 — `## Decision summary` is the trailing anchor and contains the required substance:**
       ```
       grep -n '^## Decision summary$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | tail -1
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       ```
       Pass: `grep -n` returns exactly 1 match; the last `##` heading line
       matches `## Decision summary`; the awk slice contains: a non-bolded
       verdict restatement; the fold commitment; ≥2 table-stakes lines; ≥1
       differentiator line; ≥2 non-goal lines (NG-1, NG-2, NG-3 recommended);
       both literal strings `next-milestone` AND `opportunistic` for priority
       tags; DOC-05 N/A disposition. Capture the start line (from `grep -n`)
       and the end line (`awk 'NF{ln=NR}END{print ln}' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`).
       These form the Decision-summary line range cited in the audit.

    5. **Gate 5 — Verdict-section line range identification (for audit citation):**
       ```
       grep -n '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       ```
       Capture all `##` heading line numbers. The Verdict section spans from
       the line of `## Verdict` to (the line of the next `##` heading − 1).
       Capture both numbers for Task 3.

    6. **Gate 6 — literal `next-milestone` AND `opportunistic` in Decision summary:**
       ```
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
         | grep -F 'next-milestone'
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
         | grep -F 'opportunistic'
       ```
       Pass: ≥1 hit on each literal.

    7. **Gate 7 — voice-rule project-wide hard gate (Phase 15 STRAT-11 equivalent applied here):**
       ```
       grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
       ```
       Pass: zero matches. ANY match returns Task 2 to Task 1 for rewriting
       to forward-looking voice ("we will hold...", "our roadmap commits
       to...", "the v0.6+ Security Hardening theme..."). Phase 15 lifts this
       doc verbatim, so any drift here propagates downstream.

    8. **Gate 8 — DOC-05 N/A disposition cited in Decision summary:**
       ```
       awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
         | grep -F 'DOC-05'
       ```
       Pass: ≥1 hit (DOC-05 is named in the disposition line).

    Save the verbatim outputs of all gates to a temporary buffer (the bash
    session) — Task 3 quotes them into `14-AUDIT.md`. Do NOT write the audit
    yet; Task 3 owns that file.
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md &amp;&amp; bytes=$(wc -c &lt; docs/exploration/PILLAR-3-CANDIDATE-NOTES.md) &amp;&amp; [ "$bytes" -ge 2048 ] &amp;&amp; [ "$bytes" -le 12288 ] &amp;&amp; first_h2=$(grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | head -1 | grep -F '## Verdict') &amp;&amp; [ "$(grep -c '^## Verdict$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md)" = "1" ] &amp;&amp; [ "$(grep -cE '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md)" = "1" ] &amp;&amp; grep -E '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -qF '(b)' &amp;&amp; hits=$(grep -E '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -oE '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' | sort -u | wc -l) &amp;&amp; [ "$hits" -ge 7 ] &amp;&amp; [ "$(grep -c '^## Decision summary$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md)" = "1" ] &amp;&amp; awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -qF 'next-milestone' &amp;&amp; awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -qF 'opportunistic' &amp;&amp; awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -qF 'DOC-05' &amp;&amp; [ -z "$(grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md)" ] &amp;&amp; echo "ALL GATES GREEN ($hits distinct tokens, $bytes bytes)"</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 passes: file exists; size in `[2048, 12288]` bytes
    - Gate 2 passes: first `##` heading is `## Verdict`; exactly 1 `^## Verdict$` line; exactly 1 `**Verdict:**` bolded line; the bolded line declares (b)
    - Gate 3 passes: `≥7` distinct named-reference tokens after `sort -u` (the EXPL-02 grep gate, success criterion 3)
    - Gate 4 passes: exactly 1 `## Decision summary` heading; section is the LAST `##` heading; section contains the verdict restated unbolded + fold commitment + ≥2 table-stakes + ≥1 differentiator + ≥2 non-goals + both `next-milestone` and `opportunistic` priority tags + DOC-05 N/A disposition
    - Gate 5: Verdict-section line range identified (start = line of `## Verdict`; end = line of next `##` heading − 1). Both numbers captured for Task 3.
    - Gate 6 passes: literals `next-milestone` AND `opportunistic` both appear in the Decision summary section
    - Gate 7 passes: zero matches on the voice-rule grep — no forbidden `AgentLinux <verb>` pattern anywhere in the file
    - Gate 8 passes: literal `DOC-05` appears in the Decision summary section's disposition line
    - Single combined `<automated>` command (above) returns `ALL GATES GREEN (...)` with exit code 0
    - Verdict-section AND Decision-summary line ranges identified for audit citation: Verdict start = output of `grep -n '^## Verdict$'`, Verdict end = (next `##` heading line) − 1; Decision summary start = output of `grep -n '^## Decision summary$'`, Decision summary end = last non-empty line in file (output of `awk 'NF{ln=NR}END{print ln}'`).
  </acceptance_criteria>
  <done>
    All EXPL-02 gates pass locally. The `<automated>` one-liner emits `ALL GATES GREEN`.
    Verdict section line range AND Decision summary line range identified.
    Verbatim grep transcripts captured for the audit. Ready for audit
    authoring in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author phase-close audit `14-AUDIT.md` with grep transcripts and GREEN gate verdict</name>
  <files>.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (EXPL-02 success criterion 5 — what the audit must contain)
    - .planning/ROADMAP.md Phase 14 "Success Criteria" block #5 (audit must cite file path + line range of `## Verdict` + line range of `## Decision summary` + verbatim grep transcripts; gate emits GREEN; audit's first content line records the verdict so Phase 15's planner can grep it; audit notes the canonical-vs-typo-path divergence)
    - docs/exploration/PILLAR-3-CANDIDATE-NOTES.md (the doc just authored — needed for line-range citations of `## Verdict` and `## Decision summary` sections)
    - .planning/phases/13-pillar-2-exploration/13-AUDIT.md (the prior phase's audit — match this format/shape)
  </read_first>
  <action>
    Author `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` as
    the phase-close audit doc. The audit closes EXPL-02 with cited evidence
    per success-criterion 5: file path + line range of `## Verdict` section +
    line range of `## Decision summary` section + verbatim grep transcripts.
    Gate verdict: GREEN.

    The audit's FIRST content line (after the frontmatter) MUST record the
    verdict so Phase 15's planner can read it in one grep. Use the literal
    form per the ROADMAP success criterion: a single line at the top of the
    audit body containing the verdict, e.g.
    `**Verdict (Phase 14, EXPL-02):** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.`
    This line is grep-able by Phase 15 via something like
    `grep -E '^\*\*Verdict \(Phase 14' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md`
    or `grep -E '\(b\) Fold' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md`
    — Phase 15's planner reads either form to set pillar count and DOC-05
    disposition.

    The audit MUST also note the canonical-vs-typo-path divergence per
    CONTEXT.md `<code_context>` "Established Patterns": ROADMAP.md success
    criterion 5 references
    `.planning/phases/13-pillar-3-exploration/14-AUDIT.md` (without
    "candidate"); the audit lands at the canonical path
    `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` (with
    "candidate" — matching the actual phase directory slug). The audit
    records this in a single line near the top so future readers can grep
    for both spellings without confusion.

    Required structure for the audit (same shape as v0.3.0 / v0.4.0 / Phase 13
    phase-close audit convention — file path + line range + grep transcripts
    as the evidence form for documentation-only requirements):

    ```markdown
    ---
    phase: 13
    phase_name: Pillar 3 Candidate Exploration
    milestone: v0.3.3
    status: shipped
    gate: GREEN
    date: 2026-05-10
    ---

    # Phase 14 Audit — Pillar 3 Candidate Exploration

    **Verdict (Phase 14, EXPL-02):** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.

    > Path note: this audit lands at the canonical path
    > `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md`
    > matching the phase directory slug. ROADMAP.md's Phase 14 success-
    > criterion 5 line references `.planning/phases/13-pillar-3-exploration/14-AUDIT.md`
    > (without "candidate") — that path is a typo predating the canonical
    > slug; the audit substance is unchanged.

    **Phase:** 13 — Pillar 3 Candidate Exploration
    **Closed:** 2026-05-10
    **Requirement:** EXPL-02
    **Gate verdict:** GREEN

    ## Headline

    `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` exists at the locked path
    with body [N] bytes (in `[2048, 12288]`). The doc's first section is
    `## Verdict` at line [start]; the bolded `**Verdict:**` line declares
    (b) Fold into Pillar 2 as sub-concern. The doc ends with
    `## Decision summary` at lines [start]–[end] (the section Phase 15 lifts
    verbatim into `docs/STRATEGY.md` Pillar 2 — under verdict (b), Phase 15
    ships 2 pillars (not 3) and DOC-05 closes as N/A in `15-AUDIT.md`). All
    five EXPL-02 success criteria pass; transcripts below. Voice-rule
    project-wide grep returns zero matches.

    ## Coverage table

    | Req | Description | Status |
    |-----|-------------|--------|
    | EXPL-02 | `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` with `## Verdict` first-section anchor + ≥7 named-reference grep hits + `## Decision summary` last-section anchor with substance + phase-close audit | ✅ All five success criteria pass; transcripts below |

    ## Requirement EXPL-02 — evidence

    ### Success criterion 1 — file exists at exact path; body in [2 KB, 12 KB]

    **File:** `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`

    Command:
    ```
    test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md && echo EXISTS
    ```

    Output:
    ```
    EXISTS
    ```

    Command:
    ```
    wc -c docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output:
    ```
    [N] docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Within bounds: 2048 ≤ [N] ≤ 12288. **PASS.**

    ### Success criterion 2 — `## Verdict` is FIRST section + single bolded `**Verdict:**` line declares one of (a/b/c/d)

    Command (first `##` heading):
    ```
    grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | head -1
    ```

    Output:
    ```
    [start_verdict]:## Verdict
    ```

    Command (count of `**Verdict:**` bolded lines — must be exactly 1):
    ```
    grep -cE '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output:
    ```
    1
    ```

    Command (the verdict line itself — declares (b)):
    ```
    grep -E '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output (verbatim):
    ```
    **Verdict:** (b) Fold into Pillar 2 as sub-concern. Security is not a separate pillar in v0.3.3.
    ```

    **Verdict-section line range: lines [start_verdict]–[(next ## heading) − 1].**

    First `##` heading is `## Verdict`. Single bolded `**Verdict:**` line.
    Verdict declared: **(b) Fold into Pillar 2 as sub-concern.** **PASS.**

    ### Success criterion 3 — ≥7 distinct named-reference hits

    Command:
    ```
    grep -E '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
      | grep -oE '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' \
      | sort -u
    ```

    Output (verbatim):
    ```
    [paste verbatim — list of distinct tokens, one per line, sorted]
    ```

    Distinct hit count: **[N]** (≥7 required). **PASS.**

    ### Success criterion 4 — `## Decision summary` last-section anchor + required substance

    **File:** `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`

    Command:
    ```
    grep -n '^## Decision summary$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output:
    ```
    [start_summary]:## Decision summary
    ```

    Command:
    ```
    awk 'NF{ln=NR}END{print ln}' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output:
    ```
    [end_summary]
    ```

    **Decision summary section line range: lines [start_summary]–[end_summary].**

    Substance check (manual scan of the section slice):

    - **Verdict restated (unbolded — preserves the single `**Verdict:**` invariant):** present at line ~[X].
    - **Fold commitment:** active supply-chain monitoring + curated catalog admission, named at line ~[X].
    - **Table-stakes count:** ≥2 — Pillar 2 curated catalog (ADR-011 + `pinned_version`) + curated admission criteria. Both cite shipped Pillar 2 substance.
    - **Differentiators count:** ≥1 — supply-chain monitoring + compromised-version refusal commitment.
    - **Non-goals count:** ≥2 (CONTEXT.md commits to all 3) — NG-1 (model-level guardrails), NG-2 (upstream code audit), NG-3 (sandbox runtime).
    - **Priority tags:** literal `next-milestone` (fold inherits Pillar 2's tag) AND literal `opportunistic` (Appendix B Security Hardening theme) both present.
    - **DOC-05 disposition:** closes as N/A in `15-AUDIT.md` with the named single-line rationale.

    Command (priority-tag literals):
    ```
    awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -F 'next-milestone'
    awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -F 'opportunistic'
    ```

    Output (verbatim — both ≥1 hit):
    ```
    [paste verbatim]
    ```

    Command (DOC-05 disposition):
    ```
    awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -F 'DOC-05'
    ```

    Output (verbatim — ≥1 hit):
    ```
    [paste verbatim]
    ```

    Exactly 1 `## Decision summary` heading (last `##` section); section
    contains verdict restated + fold commitment + ≥2 table-stakes + ≥1
    differentiator + ≥3 non-goals + both priority tags + DOC-05 disposition.
    **PASS.**

    ### Success criterion 5 — phase-close audit (this file) cites the above

    - ✅ This audit cites file path: `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md`.
    - ✅ This audit cites Verdict section line range: lines [start_verdict]–[(next ##) − 1].
    - ✅ This audit cites Decision summary section line range: lines [start_summary]–[end_summary].
    - ✅ This audit contains verbatim grep transcripts for criteria 1, 2, 3, 4.
    - ✅ This audit's first content line records the verdict (the line under the H1 — `**Verdict (Phase 14, EXPL-02):** (b) ...`) for Phase 15's planner grep.
    - ✅ This audit notes the canonical-vs-typo-path divergence (ROADMAP.md vs phase directory slug).
    - ✅ Gate verdict: GREEN.

    **PASS.**

    ## Voice-rule project-wide hard gate (Phase 15 STRAT-11 equivalent applied here)

    Command:
    ```
    grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output (verbatim — empty; grep exit code 1 == no matches):
    ```
    ```

    **Notes:** Zero matches. No `AgentLinux + present-tense forbidden verb`
    drift exists anywhere in the doc. Phase 15's STRAT-11 hard gate on
    `docs/STRATEGY.md` will pass cleanly when Phase 15 lifts the Decision
    summary verbatim (and any framing prose lifted into the Considered-and-
    rejected / Trade-offs sections of STRATEGY.md). No rewrite needed.

    ## CONTEXT.md fidelity spot-check

    Command:
    ```
    grep -E '(NG-1|NG-2|NG-3)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | wc -l
    ```

    Output: [N] lines reference the locked non-goal IDs from
    `14-CONTEXT.md` `<decisions>` block. All 3 IDs present.

    Command:
    ```
    grep -F 'supply-chain monitoring' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output: [≥1 line] — the locked forward-looking commitment is named.

    Command:
    ```
    grep -F 'ADR-012' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
    ```

    Output: [≥1 line] — the ADR-012 NOPASSWD tension is named explicitly.

    **Fidelity: PASS.**

    ## Files added/changed

    | Path | Change | Notes |
    |------|--------|-------|
    | `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` | NEW | [N] bytes; Verdict section at lines [s]–[e]; Decision summary at lines [s]–[e]. |
    | `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` | NEW (this file) | Phase-close audit; gate GREEN. |

    ## Phase-close gate

    All five EXPL-02 success criteria pass with cited evidence (file path +
    Verdict section line range + Decision summary section line range +
    verbatim grep transcripts). Voice-rule project-wide hard gate is fully
    clean. CONTEXT.md fidelity verified: verdict (b) locked + supply-chain
    monitoring commitment named + NG-1/NG-2/NG-3 non-goals present +
    `next-milestone` and `opportunistic` priority tags both present + DOC-05
    N/A disposition recorded + ADR-012 tension cited.

    **Gate: GREEN.**

    Phase 14 complete. Phase 15 (Strategy Doc + ADR-016 + Downstream Surface
    Updates) unblocked — pillar count locks at 2 (Pillar 1 + Pillar 2);
    Pillar 2's Decision summary (from Phase 13) absorbs the supply-chain
    monitoring sub-concern from this phase; "Security Hardening" lands as a
    v0.6+ `opportunistic` theme in `docs/STRATEGY.md` Appendix B; DOC-05
    closes as N/A in `15-AUDIT.md`.

    ## References

    - `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` — the doc this audit closes.
    - `docs/exploration/PILLAR-2-NOTES.md` — Phase 13's verdict; the fold
      target for the supply-chain monitoring commitment.
    - `.planning/REQUIREMENTS.md` — EXPL-02 acceptance criteria.
    - `.planning/ROADMAP.md` — Phase 14 success criteria (note: success
      criterion 5 references `.planning/phases/13-pillar-3-exploration/...`
      typo; canonical path is `13-pillar-3-candidate-exploration`).
    - `.planning/phases/14-pillar-3-candidate-exploration/14-CONTEXT.md` —
      locked decisions lifted into the doc body.
    - `.planning/phases/14-pillar-3-candidate-exploration/14-01-PLAN.md` —
      the plan this audit closes.
    - `.planning/phases/13-pillar-2-exploration/13-AUDIT.md` — prior phase's
      audit, format reference.
    - `docs/decisions/012-agent-user-full-sudo.md` — ADR-012 NOPASSWD tension
      cited in the doc.
    - Jira: [AL-7 — Project agenda redefinition](https://copiedwonder.atlassian.net/browse/AL-7).
    ```

    Substitute the bracketed placeholders ([N], [start_verdict],
    [start_summary], [end_summary], etc.) with the verbatim outputs captured
    in Task 2 and the line numbers identified there. The audit is the
    phase-close evidence artifact and must be self-contained — a future
    visitor running the same greps against any future commit gets the same
    answers (the grep is the spec).

    Once the audit is written, run a final sanity check that the grep
    transcripts in the audit match what re-running the same commands today
    would produce (defensive — catches any drift introduced between Task 2
    capture and audit authoring).
  </action>
  <verify>
    <automated>cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; test -f .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -q 'GREEN' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -q 'docs/exploration/PILLAR-3-CANDIDATE-NOTES.md' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -qE 'EXPL-02' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -qE 'Decision summary' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -qE '^## Verdict|## Verdict' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; grep -qE '\(b\)' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md &amp;&amp; head -10 .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md | grep -qF 'Verdict' &amp;&amp; echo "AUDIT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns 0 (file exists at canonical path with "candidate")
    - `grep -F 'GREEN' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns ≥1 hit (gate verdict recorded)
    - `grep -F 'docs/exploration/PILLAR-3-CANDIDATE-NOTES.md' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns ≥1 hit (file path cited)
    - `grep -F 'EXPL-02' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns ≥1 hit (requirement ID cited)
    - **Verdict header at top:** `head -10 .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md | grep -F 'Verdict'` returns ≥1 hit AND `head -10 .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md | grep -F '(b)'` returns ≥1 hit (Phase 15's planner can grep the verdict from the top of the audit)
    - **Path-divergence note:** `head -20 .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md | grep -E '(13-pillar-3-exploration|canonical|typo)'` returns ≥1 hit (the path-divergence acknowledgment is near the top per ROADMAP success criterion 5 + CONTEXT.md guidance)
    - `grep -F 'Decision summary' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns ≥1 hit (line-range citation present)
    - `grep -E '^## Verdict|^## Verdict$|## Verdict' .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` returns ≥1 hit (Verdict section line-range citation present)
    - Audit body contains verbatim grep transcripts for success criteria 1, 2, 3, 4 (manual scan: each transcript appears as a fenced code block with the command preceding it)
    - Audit body cites the Verdict section by line range (e.g. "lines 11–18") AND the Decision summary section by line range (e.g. "lines 95–155") — the line numbers match the actual file contents at audit-write time (re-run `grep -n '^## Verdict$'`, `grep -n '^## Decision summary$'`, `awk 'NF{ln=NR}END{print ln}'` to confirm; for Verdict-end, find the next `##` heading after `## Verdict` and use (its line − 1))
    - Voice-rule grep is included in the audit and shows zero matches (the transcript is a verbatim empty fenced block with the grep command preceding)
    - Final sanity re-run: re-running gates 3, 6, 7, 8 today reproduces the audit's quoted output verbatim (no drift between Task 2 capture and Task 3 audit-write)
    - Frontmatter present (`---`-fenced YAML block with `phase: 13`, `gate: GREEN`, `status: shipped`, `date: 2026-05-10` matching the Phase 13 audit shape)
  </acceptance_criteria>
  <done>
    `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` exists at
    the canonical path, cites EXPL-02 + file path + Verdict section line range
    + Decision summary section line range + verbatim grep transcripts for all
    five success criteria, records the verdict on its first content line for
    Phase 15's planner grep, notes the canonical-vs-typo-path divergence,
    emits GREEN gate. Phase 14 closed; Phase 15 unblocked at 2-pillar count.
  </done>
</task>

</tasks>

<verification>
**Phase-level verification (run after all 3 tasks complete):**

1. Both target files exist:
   ```
   test -f docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
   test -f .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md
   ```

2. EXPL-02 gates all pass (the canonical command set):
   ```
   # Gate 1: existence + size
   wc -c docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect [2048, 12288]

   # Gate 2: ## Verdict is FIRST section + single **Verdict:** line declares (b)
   grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | head -1  # expect ## Verdict
   grep -c '^## Verdict$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect 1
   grep -cE '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect 1
   grep -E '\*\*Verdict:' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -c '(b)'  # expect 1

   # Gate 3: ≥7 distinct named-reference hits
   grep -E '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' \
     docs/exploration/PILLAR-3-CANDIDATE-NOTES.md \
     | grep -oE '(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)' \
     | sort -u | wc -l  # expect ≥7

   # Gate 4: exactly 1 Decision summary heading + LAST ## section
   grep -c '^## Decision summary$' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect 1
   grep -nE '^## ' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | tail -1 | grep -F '## Decision summary'  # expect match

   # Gate 6: literal `next-milestone` AND `opportunistic` in Decision summary
   awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -cF 'next-milestone'  # expect ≥1
   awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -cF 'opportunistic'  # expect ≥1

   # Gate 7: voice-rule project-wide hard gate (zero matches)
   grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b' \
     docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect EMPTY (exit 1)

   # Gate 8: DOC-05 disposition in Decision summary
   awk '/^## Decision summary$/{flag=1} flag' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md | grep -cF 'DOC-05'  # expect ≥1

   # Audit cites all of the above
   grep -E '(GREEN|EXPL-02|PILLAR-3-CANDIDATE-NOTES.md|Decision summary|## Verdict)' \
     .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md  # expect ≥5 distinct hits
   ```

3. Audit-top verdict header present (Phase 15 planner-grep target):
   ```
   head -10 .planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md | grep -E '(Verdict.*\(b\)|\(b\) Fold)'
   ```
   Expect ≥1 hit on the verdict line near the top of the audit.

4. CONTEXT.md fidelity spot-check:
   ```
   # Locked non-goal IDs all referenced
   grep -E '(NG-1|NG-2|NG-3)' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md
   # Supply-chain monitoring commitment named
   grep -F 'supply-chain monitoring' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect ≥1
   # ADR-012 tension cited literally
   grep -F 'ADR-012' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect ≥1
   # Pillar 2 named as the fold target
   grep -F 'Pillar 2' docs/exploration/PILLAR-3-CANDIDATE-NOTES.md  # expect ≥1
   ```
</verification>

<success_criteria>
1. `docs/exploration/PILLAR-3-CANDIDATE-NOTES.md` exists at exactly that path; body is between 2 KB and 12 KB.
2. The file's FIRST section is `## Verdict` (literal heading, before any other `##` heading); the section contains exactly one bolded `**Verdict:**` line declaring (b) Fold into Pillar 2 as sub-concern.
3. Body cites ≥7 distinct EXPL-02 grep tokens from `(OWASP|Lethal Trifecta|Rule of Two|Shai-Hulud|chalk|TrustFall|Cline|provenance|SLSA|cosign|bubblewrap|ADR-012)`.
4. File ends with `## Decision summary` section (literal heading, last `##` section) containing: verdict restated unbolded, fold commitment, ≥2 table-stakes, ≥1 differentiator, NG-1+NG-2+NG-3 non-goals (≥2 required, all 3 recommended), priority tags `next-milestone` (fold inheritance) AND `opportunistic` (Appendix B theme) both present, DOC-05 N/A disposition recorded.
5. Voice rule project-wide: no `AgentLinux + present-tense forbidden verb` pattern anywhere in the doc — `grep -nE '^[^a-z]*AgentLinux (provides|offers|ensures|protects|defends|benchmarks|measures|hardens|isolates|detects|prevents)\b'` returns zero matches.
6. `.planning/phases/14-pillar-3-candidate-exploration/14-AUDIT.md` exists at the canonical path (with "candidate"); first content line records the verdict for Phase 15's planner grep; cites file path + `## Verdict` line range + `## Decision summary` line range + verbatim grep transcripts for criteria 1–4 + voice-rule transcript; notes canonical-vs-typo-path divergence; emits GREEN.
7. CONTEXT.md fidelity: verdict (b) lock honored + supply-chain monitoring commitment named + NG-1/NG-2/NG-3 non-goals present + `next-milestone` + `opportunistic` priority tags + DOC-05 N/A disposition + ADR-012 tension cited + Pillar 2 named as fold target.
8. No scope-reduction vocabulary leaked into the doc (`v1` (filtering false-positives like "v2025"), `v2`, `simplified version`, `placeholder`, `future enhancement`, `basic version`, `minimal implementation` — zero non-false-positive hits).
9. Reviewer pass (fact-checker + technical-writer per CLAUDE.md §Review Loop, doc-only file-type ruleset) complete; valid findings addressed; the file at audit-write-time reflects the post-review state.
</success_criteria>

<output>
After completion, create `.planning/phases/14-pillar-3-candidate-exploration/14-01-SUMMARY.md`
per the GSD execute-plan convention. The SUMMARY records what was built, the
file paths created, the verdict (b) and its grep transcript, the EXPL-02 grep
gate evidence (≥7 distinct tokens with the actual sorted token list), and any
decisions Claude made under the "Claude's Discretion" portions of CONTEXT.md
(section ordering after `## Verdict`, prose voice, whether Threat-landscape
and Defenses-considered were separate sections vs consolidated, whether
defenses cited as paragraphs vs list, whether Anthropic devcontainer was
cited beyond the regex-counted token set, the specific phrasing of the
supply-chain monitoring commitment, whether the reviewer pass surfaced
findings that required substantive edits).

Phase 14 closes when 14-AUDIT.md emits GREEN; Phase 15 unblocked at 2-pillar
count (DOC-05 closes as N/A; "Security Hardening" lands in Appendix B as
`opportunistic`).
</output>
