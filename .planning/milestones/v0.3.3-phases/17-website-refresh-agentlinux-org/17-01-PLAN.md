---
phase: 16-website-refresh-agentlinux-org
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/og-image.png
  - .planning/REQUIREMENTS.md
  - .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - SITE-07
  - SITE-08
  - SITE-09
  - SITE-10
  - SITE-11
  - SITE-12
tags:
  - website
  - documentation
  - phase-close-audit
  - milestone-close

must_haves:
  truths:
    - "Visitor reading agentlinux.org no longer sees `purpose-built Linux distribution`, `runs on a dedicated machine`, `apt install claude-code`, `QEMU VM images`, `Docker micro-VMs`, `Boots into a non-root user`, `in distro repos`, or `full operating system on a dedicated machine` anywhere on the page."
    - "Visitor reading the hero sees `Linux that gives coding agents a stable place to run — without you having to set it up.` as the value-prop."
    - "Visitor reading `#comparison` sees three solution paragraphs anchored on the canonical bug class (`sudo npm install -g` EACCES + recursive-shim self-update breakage) and the curated-combo bet, not on `full OS` / `dedicated machine` framing."
    - "Visitor reading FAQ #1 and #5 sees plugin-framed answers (no `Linux distribution` / `full operating system on a dedicated machine` language)."
    - "Visitor opens an agentlinux.org link preview on Slack/LinkedIn/Twitter and sees a PNG OG image (1200×630) — the SVG is preserved as source-of-truth at `assets/og-image.svg`."
    - "Voice-rule grep gate passes: `grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\\b' index.html` returns zero matches (SITE-06 HARD GATE)."
    - "REQUIREMENTS.md SITE-* amendment trail is recorded under a new `## Superseded Items (2026-05-24 Phase 17 scope re-cut)` block, mirroring the Phase 15/Phase 16 amendment-history precedent."
    - "Phase-close audit `17-AUDIT.md` exists, cites every SITE-XX evidence (kept + amended + superseded + N/A), and emits the v0.3.3 milestone-close gate GREEN."
  artifacts:
    - path: "index.html"
      provides: "Rewritten hero, OG/Twitter meta, `#features` cards, `#comparison` block, FAQ #1 + #5 — no contradictions with VISION.md or STRATEGY.md."
      contains: "Linux that gives coding agents a stable place to run"
    - path: "assets/og-image.png"
      provides: "PNG-rendered OG card (1200×630) referenced by `og:image` + `twitter:image` meta tags."
    - path: ".planning/REQUIREMENTS.md"
      provides: "SITE-* amendment block at the bottom (`## Superseded Items (2026-05-24 Phase 17 scope re-cut)`)."
      contains: "Superseded Items (2026-05-24 Phase 17 scope re-cut)"
    - path: ".planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md"
      provides: "Phase-close audit citing every SITE-XX disposition (KEEP / AMEND / SUPERSEDED / N/A) + milestone-close gate emission."
      contains: "Phase 17 gate: GREEN"
  key_links:
    - from: "index.html (hero value-prop, comparison blocks, feature cards)"
      to: "docs/VISION.md mission line + docs/STRATEGY.md `## What we're solving` diagnosis"
      via: "rewritten copy lifted lightly from VISION.md mission + STRATEGY.md bug-class framing"
      pattern: "Linux that gives coding agents a stable place to run"
    - from: "index.html `og:image` + `twitter:image` meta"
      to: "assets/og-image.png"
      via: "absolute URL `https://agentlinux.org/assets/og-image.png`"
      pattern: "og-image\\.png"
    - from: ".planning/REQUIREMENTS.md SITE-* amendment block"
      to: "17-AUDIT.md SITE-* disposition table"
      via: "Phase 15/15 amendment-precedent style"
      pattern: "Superseded Items \\(2026-05-24"
---

<objective>
Repair `index.html` so it no longer contradicts the post-Phase-14 vision (two
pillars, infrastructure-not-distro framing) and the post-Phase-15 strategy
(installable plugin, curated combos, bug-class diagnosis). Scope is
**contradiction-removal**, not expansion: rewrite the hero value-prop, the
OG/Twitter meta tags, five of the eight `#features` cards, the three
`#comparison` solution paragraphs + closing line, FAQ #1 + #5; convert the OG
image SVG → PNG. In the same commit window, amend `.planning/REQUIREMENTS.md`
SITE-01..SITE-11 to record the 2026-05-24 scope re-cut (six requirements
KEEP/AMEND, five SUPERSEDED, one N/A) and emit the Phase 17 phase-close audit
`17-AUDIT.md`. Phase 17 is the last v0.3.3 phase — the audit also closes the
milestone-coverage gate.

Purpose: a visitor reading agentlinux.org sees a page that matches the
project's current truth (installable Ubuntu plugin, the v0.3.0 reality) and
the strategy's forward direction (curated combos, brownfield-aware next),
without bragging about shipped versions or pushing doc links the project is
not ready for. SITE-06 voice-rule grep gate is the load-bearing structural
defense (mirrors VIS-07 + STRATR-06 precedent from Phases 14 + 15).

Output: edited `index.html`; new `assets/og-image.png` (SVG preserved at
`assets/og-image.svg`); amended `.planning/REQUIREMENTS.md`; new
`.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md` with the
Phase 17 + v0.3.3 milestone GREEN gate emission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md
@.planning/phases/16-strategy-roadmap-doc/16-AUDIT.md
@.planning/phases/15-vision-doc-and-downstream/15-AUDIT.md
@docs/VISION.md
@docs/STRATEGY.md
@index.html
@README.md
@CLAUDE.md

<interfaces>
<!-- The substance executor needs to know about, extracted from source docs -->
<!-- and the existing index.html layout. Use these verbatim where Task action -->
<!-- text says "copy verbatim into ...". -->

Voice-rule (load-bearing — SITE-06 HARD GATE; mirrors VIS-07 / STRATR-06):
  Forbidden pattern in index.html (zero matches required):
    grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' index.html
  Identity claims use: "Linux", "the plugin", "AgentLinux" with neutral verbs
    ("gives", "hands", "provisions", "comes with", "ships", "installs").
  Forward-looking claims use grammatical subject: "we", "our roadmap".

Hero value-prop (lifted lightly from docs/VISION.md ## Mission):
  "Linux that gives coding agents a stable place to run — without you having to set it up."

Strategy diagnosis substance (from docs/STRATEGY.md ## What we're solving):
  - Bug class: `sudo npm install -g` EACCES + recursive-shim that breaks
    Claude Code's own self-update path.
  - Integration thesis: distro maintainer + language vendor + agent vendor
    each work; the gaps between them are where users live.
  - The curated-combo bet: we hold the version set we have tested together
    on Docker + QEMU; the user decides when to move.

Existing index.html contradiction map (from 17-CONTEXT.md `<code_context>`):
  Line  22  — OG meta description (purpose-built Linux distribution …)
  Line  23  — OG image src (.svg → must repoint to .png)
  Line  29  — Twitter meta description (same string)
  Line  30  — Twitter image src (.svg → must repoint to .png)
  Line 656  — Hero value-prop paragraph
  Line 730-735 — #features Minimalistic card
  Line 737-742 — #features Automatic agent user card
  Line 744-749 — #features Easy-to-install package groups card
  Line 758-763 — #features Agents in the repos card (apt install claude-code)
  Line 765-770 — #features Frameworks and plugins card (distro repos)
  Line 779-784 — #features Multiple distribution formats card (QEMU/Docker micro-VM)
  Line 794-797 — #comparison Local machine solution paragraph
  Line 799-802 — #comparison Docker solution paragraph
  Line 804-807 — #comparison Generic VMs solution paragraph
  Line 809    — #comparison closing line
  Line 830-832 — FAQ #1 answer (What is AgentLinux?)
  Line 845-848 — FAQ #5 answer (How is this different from Docker?)

Out-of-scope guardrails (DO NOT touch per CONTEXT.md):
  - `#problem` dual-column human/agent framing (user explicit: kept).
  - `#features` 8-card grid structure (no `#pillars` restructure, no badges,
    no "Learn more →" links).
  - `#install` section (NOT added).
  - Top nav (NO `Vision` link added).
  - Footer (NO doc links added).
  - Visual styling (dark JetBrains Mono, crab mascot).
  - `.github/workflows/deploy.yml` (NO drift check wired).
  - Bats / Docker / QEMU harness (none touched this phase).
  - PR mobile-screenshot ritual (dropped this phase).

REQUIREMENTS.md amendment table (from 17-CONTEXT.md `<decisions>`):
  SITE-01 — AMEND   — Drop "delivered-fact" + "forward-looking" mandates;
                       new gate: `grep -c 'purpose-built Linux distribution' index.html` → 0.
  SITE-02 — SUPERSEDED — No `#features` → `#pillars` restructure.
                          New gate: `grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|distro repos' index.html` → 0.
  SITE-03 — SUPERSEDED — No status badges. Closes via SITE-02 supersession.
  SITE-04 — KEEP, narrow — Reframe path locked. Existing grep gate carries forward.
  SITE-05 — SUPERSEDED — No `#install` section.
  SITE-06 — KEEP    — Voice-rule grep gate unchanged (HARD GATE).
  SITE-07 — SUPERSEDED — No footer doc links, no nav `Vision` link.
  SITE-08 — KEEP    — OG/Twitter meta tags rewritten.
  SITE-09 — KEEP    — OG image SVG → PNG.
  SITE-10 — N/A     — No install snippet → no drift to check (already conditional).
  SITE-11 — SUPERSEDED — Mobile-screenshot ritual dropped.
  SITE-12 — KEEP    — Phase-close audit + milestone-close gate fires here.

Audit format (lift verbatim from 16-AUDIT.md / 15-AUDIT.md precedent):
  - Top metadata block: `> Phase:`, `> Authored:`, `> Gate:`.
  - `## Summary` section: 1-3 paragraphs; commits list; total requirements closed.
  - One `## SITE-XX — ...` section per requirement; each contains:
      Acceptance (from REQUIREMENTS.md): ...
      Evidence: grep/cmd + output + verdict.
  - `## Aggregate gate status` table.
  - "**Phase 17 gate: GREEN.**" line.
  - "**v0.3.3 milestone-close gate: GREEN.**" line (Phase 17 is the last phase).

Tooling for SVG → PNG (CONTEXT.md `## Claude's Discretion`):
  Preferred: `rsvg-convert` (apt: librsvg2-bin). If missing, install via:
    sudo apt-get update && sudo apt-get install -y librsvg2-bin
  (Sudo permitted per ADR-012 NOPASSWD; CLAUDE.md "Critical Rules" allows
   `sudo apt install` for the agent user.)
  Fallback order if `rsvg-convert` unavailable after install attempt:
    `magick` (ImageMagick) → `convert` → `inkscape`.
  Render command: rsvg-convert -w 1200 -h 630 -f png -o assets/og-image.png assets/og-image.svg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite hero value-prop + OG/Twitter meta tags + OG/Twitter image references (index.html lines 22, 23, 29, 30, 656)</name>

  <read_first>
    - index.html (lines 1-50 for meta block; lines 650-660 for hero)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` § "Hero copy (locked 2026-05-24)" + `<code_context>` line map for meta tags)
    - docs/VISION.md (lines 1-13 for the mission line that the hero value-prop is lifted from)
  </read_first>

  <files>index.html</files>

  <action>
    Make four surgical edits in index.html. Each edit replaces an exact existing string with an exact new string. Use the Edit tool with the exact `old_string` and `new_string` shown below — do not rewrite surrounding markup, do not change attribute order, do not touch the `og:image:width` / `og:image:height` lines.

    **Edit A — OG description (line 22):**
    old_string:
    `  <meta property="og:description" content="A purpose-built Linux distribution for AI coding agents. Full OS isolation, pre-configured toolchains, agents in the repos.">`
    new_string:
    `  <meta property="og:description" content="Linux that gives coding agents a stable place to run — without you having to set it up. An installable Ubuntu plugin that ships a dedicated agent environment and a curated, stability-tested toolchain.">`

    **Edit B — OG image src (line 23):**
    old_string:
    `  <meta property="og:image" content="https://agentlinux.org/assets/og-image.svg">`
    new_string:
    `  <meta property="og:image" content="https://agentlinux.org/assets/og-image.png">`

    **Edit C — Twitter description (line 29):**
    old_string:
    `  <meta name="twitter:description" content="A purpose-built Linux distribution for AI coding agents.">`
    new_string:
    `  <meta name="twitter:description" content="Linux that gives coding agents a stable place to run — without you having to set it up.">`

    **Edit D — Twitter image src (line 30):**
    old_string:
    `  <meta name="twitter:image" content="https://agentlinux.org/assets/og-image.svg">`
    new_string:
    `  <meta name="twitter:image" content="https://agentlinux.org/assets/og-image.png">`

    **Edit E — Hero value-prop (line 656):**
    old_string:
    `      <p class="hero-value-prop">A purpose-built Linux distribution that runs on a dedicated machine — virtual or physical — so your AI coding agent gets an entire OS to itself.</p>`
    new_string:
    `      <p class="hero-value-prop">Linux that gives coding agents a stable place to run — without you having to set it up.</p>`

    Do NOT touch the hero tagline ("Linux, for agents"), the hero title, the `<title>` element, the CTA button text, the nav, or anything else outside these five exact line replacements.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      grep -c 'purpose-built Linux distribution' index.html | grep -qx 0 &amp;&amp; \
      sed -n '651,659p' index.html | grep -c 'purpose-built Linux distribution' | grep -qx 0 &amp;&amp; \
      grep -Fc 'Linux that gives coding agents a stable place to run' index.html | grep -qx 3 &amp;&amp; \
      grep -cE 'property="og:image" content="[^"]*\.png"' index.html | grep -qx 1 &amp;&amp; \
      grep -cE 'name="twitter:image" content="[^"]*\.png"' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'href="#signup" class="hero-cta">Join the waitlist' index.html | grep -qx 1 &amp;&amp; \
      grep -c '&gt;Linux, for agents&lt;' index.html | grep -qx 1 &amp;&amp; \
      echo "TASK 1 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'purpose-built Linux distribution' index.html` returns `0` (SITE-01 amended gate + SITE-08).
    - Region-scoped: `sed -n '651,659p' index.html | grep -c 'purpose-built Linux distribution'` returns `0` (hero edit landed; the global zero-count for `runs on a dedicated machine` is enforced in Task 4 once FAQ #1 is rewritten).
    - `grep -Fc 'Linux that gives coding agents a stable place to run' index.html` returns `3` (hero, OG desc, Twitter desc).
    - `grep -cE 'property="og:image" content="[^"]*\.png"' index.html` returns `1` (SITE-09 — meta side).
    - `grep -cE 'name="twitter:image" content="[^"]*\.png"' index.html` returns `1`.
    - Hero CTA preserved: `grep -c 'href="#signup" class="hero-cta">Join the waitlist' index.html` returns `1`.
    - Hero tagline preserved: `grep -c '>Linux, for agents<' index.html` returns `1`.
    - `og:image:width="1200"` and `og:image:height="630"` lines untouched (visual diff confirms only lines 22, 23, 29, 30, 656 changed in this task).
  </acceptance_criteria>

  <done>
    Hero value-prop reads `Linux that gives coding agents a stable place to run — without you having to set it up.`; OG + Twitter meta descriptions both echo that line (with the OG description carrying the longer plugin-framed second sentence); OG + Twitter image references point to `.png`. All other meta + hero markup unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite five contradicting `#features` cards (index.html lines 730-735, 737-742, 744-749, 758-763, 765-770, 779-784)</name>

  <read_first>
    - index.html (lines 724-787 for the full #features section, all 8 cards)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` item 4 + `<code_context>` table)
    - docs/STRATEGY.md (lines 6-28 for "What we're solving" — bug class + curated combo substance)
    - README.md (lines 1-60 for plugin voice + verb choices)
  </read_first>

  <files>index.html</files>

  <action>
    Rewrite the bodies of FIVE of the EIGHT cards in `#features`. The 8-card grid structure stays. The two cards `Agent skills` (lines 751-756) and `Agent-friendly CLI tools` (lines 772-777) are NOT touched (they already align with the plugin reality per `<code_context>` table). The cards `Easy-to-install package groups` (lines 744-749) is rewritten because the current copy frames it as preset/profile framework (a v0.6+ theme — not shipped). Card titles + Lucide SVG icons are preserved unless a title is explicitly rewritten below.

    Use the Edit tool, one card at a time, with exact `old_string` / `new_string` replacement.

    **Card 1 — Minimalistic (lines 730-735), retitle + rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: minimize-2 (minimalistic) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 14 10 14 10 20"/><polyline points="20 10 14 10 14 4"/><line x1="14" x2="21" y1="10" y2="3"/><line x1="3" x2="10" y1="21" y2="14"/></svg>
          <div class="feature-title">Minimalistic</div>
          <p class="feature-desc">Nothing the agent doesn't need. No desktop environment, no GUI stack, no bloat. Every resource goes to the agent's workload.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: minimize-2 (minimalistic) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 14 10 14 10 20"/><polyline points="20 10 14 10 14 4"/><line x1="14" x2="21" y1="10" y2="3"/><line x1="3" x2="10" y1="21" y2="14"/></svg>
          <div class="feature-title">No-touch agent environment</div>
          <p class="feature-desc">One install command on your existing Ubuntu host. The agent user, its permissions, the runtime, the install paths — all decisions made, all best practices encoded.</p>
        </div>`

    **Card 2 — Automatic agent user (lines 737-742), keep title, rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: terminal -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 17 10 11 4 5"/><line x1="12" x2="20" y1="19" y2="19"/></svg>
          <div class="feature-title">Automatic agent user</div>
          <p class="feature-desc">Boots into a non-root user account automatically. Tools like Claude Code that refuse to run as <code>root</code> work out of the box — no setup required.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: terminal -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 17 10 11 4 5"/><line x1="12" x2="20" y1="19" y2="19"/></svg>
          <div class="feature-title">Automatic agent user</div>
          <p class="feature-desc">The installer provisions a dedicated <code>agent</code> user with a correctly-owned Node.js runtime and npm prefix. Tools like Claude Code that refuse to run as <code>root</code> work the first time — no <code>sudo npm install -g</code>, no EACCES.</p>
        </div>`

    **Card 3 — Easy-to-install package groups (lines 744-749), retitle + rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: package -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m7.5 4.27 9 5.15"/><path d="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z"/><path d="m3.3 7 8.7 5 8.7-5"/><path d="M12 22V12"/></svg>
          <div class="feature-title">Easy-to-install package groups</div>
          <p class="feature-desc">One command to install everything for web development, GUI testing, or any common agent workload. No hunting for individual packages.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: package -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m7.5 4.27 9 5.15"/><path d="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z"/><path d="m3.3 7 8.7 5 8.7-5"/><path d="M12 22V12"/></svg>
          <div class="feature-title">Curated catalog</div>
          <p class="feature-desc">A small, deliberately chosen set of agents and tools the project tests as a combination. The default version set is the one we have exercised on the Docker + QEMU matrix together.</p>
        </div>`

    **Card 5 — Agents in the repos (lines 758-763), retitle + rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: download -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
          <div class="feature-title">Agents in the repos</div>
          <p class="feature-desc">Popular AI agents — Claude Code, Codex, and more — available directly from distro repositories. <code>apt install claude-code</code> and go.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: download -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
          <div class="feature-title">One verb to install an agent</div>
          <p class="feature-desc">Pick an agent from the catalog and the plugin handles the rest — runtime ownership, install paths, the self-update path. <code>agentlinux install claude-code</code> and the next <code>claude update</code> goes through the agent's own updater without sudo.</p>
        </div>`

    **Card 6 — Frameworks and plugins (lines 765-770), keep title, rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: puzzle (frameworks) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19.439 7.85c-.049.322.059.648.289.878l1.568 1.568c.47.47.706 1.087.706 1.704s-.235 1.233-.706 1.704l-1.611 1.611a.98.98 0 0 1-.837.276c-.47-.07-.802-.48-.968-.925a2.501 2.501 0 1 0-3.214 3.214c.446.166.855.497.925.968a.979.979 0 0 1-.276.837l-1.61 1.61a2.404 2.404 0 0 1-1.705.707 2.402 2.402 0 0 1-1.704-.706l-1.568-1.568a1.026 1.026 0 0 0-.877-.29c-.493.074-.84.504-1.02.968a2.5 2.5 0 1 1-3.237-3.237c.464-.18.894-.527.967-1.02a1.026 1.026 0 0 0-.289-.877l-1.568-1.568A2.402 2.402 0 0 1 1.998 12c0-.617.236-1.234.706-1.704L4.315 8.685a.98.98 0 0 1 .837-.276c.47.07.802.48.968.925a2.501 2.501 0 1 0 3.214-3.214c-.446-.166-.855-.497-.925-.968a.979.979 0 0 1 .276-.837l1.61-1.61a2.404 2.404 0 0 1 1.705-.707 2.402 2.402 0 0 1 1.704.706l1.568 1.568c.23.23.556.338.877.29.493-.074.84-.504 1.02-.968a2.5 2.5 0 1 1 3.237 3.237c-.464.18-.894.527-.967 1.02Z"/></svg>
          <div class="feature-title">Frameworks and plugins</div>
          <p class="feature-desc">Agent frameworks like GSD for Claude Code available in distro repos. Install productivity extensions the same way you install anything else.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: puzzle (frameworks) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19.439 7.85c-.049.322.059.648.289.878l1.568 1.568c.47.47.706 1.087.706 1.704s-.235 1.233-.706 1.704l-1.611 1.611a.98.98 0 0 1-.837.276c-.47-.07-.802-.48-.968-.925a2.501 2.501 0 1 0-3.214 3.214c.446.166.855.497.925.968a.979.979 0 0 1-.276.837l-1.61 1.61a2.404 2.404 0 0 1-1.705.707 2.402 2.402 0 0 1-1.704-.706l-1.568-1.568a1.026 1.026 0 0 0-.877-.29c-.493.074-.84.504-1.02.968a2.5 2.5 0 1 1-3.237-3.237c.464-.18.894-.527.967-1.02a1.026 1.026 0 0 0-.289-.877l-1.568-1.568A2.402 2.402 0 0 1 1.998 12c0-.617.236-1.234.706-1.704L4.315 8.685a.98.98 0 0 1 .837-.276c.47.07.802.48.968.925a2.501 2.501 0 1 0 3.214-3.214c-.446-.166-.855-.497-.925-.968a.979.979 0 0 1 .276-.837l1.61-1.61a2.404 2.404 0 0 1 1.705-.707 2.402 2.402 0 0 1 1.704.706l1.568 1.568c.23.23.556.338.877.29.493-.074.84-.504 1.02-.968a2.5 2.5 0 1 1 3.237 3.237c-.464.18-.894.527-.967 1.02Z"/></svg>
          <div class="feature-title">Frameworks and plugins</div>
          <p class="feature-desc">Agent frameworks like GSD for Claude Code travel through the same catalog and the same install verb. The plugin keeps them on the curated version set across agent self-updates.</p>
        </div>`

    **Card 8 — Multiple distribution formats (lines 779-784), retitle + rewrite body:**
    old_string:
    `        <div class="feature-card">
          <!-- Lucide: layers (distribution) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22.54 12.43-8.58 3.91a2 2 0 0 1-1.66 0L3.46 12.5"/><path d="m22.54 16.43-8.58 3.91a2 2 0 0 1-1.66 0L3.46 16.5"/></svg>
          <div class="feature-title">Multiple distribution formats</div>
          <p class="feature-desc">Available as installation images, QEMU VM images, and Docker micro-VMs. Run it however your infrastructure expects — bare metal, cloud, or local.</p>
        </div>`
    new_string:
    `        <div class="feature-card">
          <!-- Lucide: layers (distribution) -->
          <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22.54 12.43-8.58 3.91a2 2 0 0 1-1.66 0L3.46 12.5"/><path d="m22.54 16.43-8.58 3.91a2 2 0 0 1-1.66 0L3.46 16.5"/></svg>
          <div class="feature-title">Meets you on your distribution</div>
          <p class="feature-desc">Installs on top of your existing Ubuntu host. We do not ask you to migrate, reinstall, or boot a custom image — the distribution maintainer's work is the foundation we build on.</p>
        </div>`

    Do NOT touch cards #4 (`Agent skills`, lines 751-756) or #7 (`Agent-friendly CLI tools`, lines 772-777). Do NOT touch the `<section id="features">` opening, the `<h2>` heading, the `.features-intro` paragraph, or the `.features-grid` container — only the inner content of the six rewritten cards changes.

    Also rewrite the intro paragraph at line 727 to drop the contradicting "entire operating system" framing:

    **Edit — .features-intro (line 727):**
    old_string:
    `      <p class="features-intro">AgentLinux isn't a generic distro with a few scripts bolted on. It's an entire operating system where every default is configured for an agent — not a human.</p>`
    new_string:
    `      <p class="features-intro">AgentLinux is an installable Ubuntu plugin. It ships the assembly already built — the agent user, the runtime, the catalog — so the user installs once and starts working with the agent.</p>`
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'Boots into a non-root user' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'No desktop environment, no GUI stack' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'entire operating system' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'agentlinux install claude-code' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'Curated catalog' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'Meets you on your distribution' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'class="feature-card"' index.html | grep -qx 8 &amp;&amp; \
      grep -c 'class="feature-title">Agent skills' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'class="feature-title">Agent-friendly CLI tools' index.html | grep -qx 1 &amp;&amp; \
      echo "TASK 2 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - All five contradicting strings absent: `grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html` returns `0` (SITE-02 amended gate).
    - `grep -c 'Boots into a non-root user' index.html` returns `0` (SITE-02 amended gate).
    - `grep -c 'No desktop environment, no GUI stack' index.html` returns `0`.
    - `grep -c 'entire operating system' index.html` returns `0` (intro rewrite — supports SITE-04 reframe).
    - `grep -c 'agentlinux install claude-code' index.html` returns `1` (new plugin verb).
    - 8-card grid preserved: `grep -c 'class="feature-card"' index.html` returns `8`.
    - Untouched cards still present: `Agent skills` and `Agent-friendly CLI tools` titles each match exactly once.
    - All Lucide `<svg>` icon blocks preserved (visual diff confirms only `<div class="feature-title">` text and `<p class="feature-desc">` body text changed in the six rewritten cards).
  </acceptance_criteria>

  <done>
    Six of the eight `#features` cards (and the section intro) now read in plugin voice; the 8-card grid layout is unchanged; no contradicting strings remain from CONTEXT.md's forbidden list; SITE-02 supersession-gate grep returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite `#comparison` solution paragraphs + closing line (index.html lines 794-809)</name>

  <read_first>
    - index.html (lines 789-811 for the full #comparison section)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` § "#comparison reframe (locked 2026-05-24)" + `<specifics>` § "Comparison anchor")
    - docs/STRATEGY.md (lines 5-19 for "What we're solving" diagnosis — the bug class anchor)
  </read_first>

  <files>index.html</files>

  <action>
    Reframe (not remove) the `#comparison` block. The three-block structure stays. Each `<h3>` heading is preserved (the three places agents try to run today — Local machine / Docker / Generic VMs); each `solution` paragraph is rewritten in place to anchor on the canonical bug class (`sudo npm install -g` EACCES + recursive-shim self-update breakage) and the curated-combo bet — not "full OS" / "dedicated machine" framing. The closing line is replaced with a one-line link back to `#signup`.

    Use the Edit tool, one block at a time, exact `old_string` / `new_string`.

    **Block 1 — Local machine (lines 794-797), retitle h3 + rewrite solution:**
    old_string:
    `      <div class="comparison-block">
        <h3>Local machine → a dedicated machine instead</h3>
        <p class="solution">Instead of sharing your machine and choosing between unsafe access or endless approval prompts, give the agent its own AgentLinux instance. A separate machine — virtual or physical — where the agent has full autonomy and your files are never at risk.</p>
      </div>`
    new_string:
    `      <div class="comparison-block">
        <h3>Local machine → a dedicated agent user instead</h3>
        <p class="solution">The plugin provisions an <code>agent</code> user with a correctly-owned Node.js runtime and npm prefix on your existing Ubuntu host. <code>sudo npm install -g claude</code> is replaced by an install path the agent owns — so <code>claude update</code> later goes through Claude Code's own updater without EACCES, and your <code>~/.ssh</code> is never on the table.</p>
      </div>`

    **Block 2 — Docker (lines 799-802), retitle h3 + rewrite solution:**
    old_string:
    `      <div class="comparison-block">
        <h3>Docker → a full OS instead of a locked box</h3>
        <p class="solution">AgentLinux is a complete operating system. The agent installs packages with <code>apt</code> mid-session, starts services with <code>systemctl</code>, and keeps state across restarts. No docker-in-docker gymnastics. Boots into a non-root user that tools like Claude Code require.</p>
      </div>`
    new_string:
    `      <div class="comparison-block">
        <h3>Docker → a real host with a real agent user</h3>
        <p class="solution">A container image freezes today's tool versions and rebuilds for every <code>apt install</code>. The plugin installs onto a real host the agent can keep using across restarts — <code>systemctl</code> works, mid-session installs work, and the agent user is in place before the first command runs.</p>
      </div>`

    **Block 3 — Generic VMs (lines 804-807), retitle h3 + rewrite solution:**
    old_string:
    `      <div class="comparison-block">
        <h3>Generic VMs → ready on boot instead of provisioning</h3>
        <p class="solution">AgentLinux images boot with everything pre-configured: non-root agent user, toolchains, agent-friendly CLI tools, and popular agents right in the repos. A purpose-built environment in seconds, not hours.</p>
      </div>`
    new_string:
    `      <div class="comparison-block">
        <h3>Generic VMs → the integration work, already done</h3>
        <p class="solution">A blank Ubuntu VM still hands you the integration work — agent user, runtime ownership, install paths, version compatibility. The plugin closes that gap in one install command and holds a curated version set we have exercised together on the Docker + QEMU matrix.</p>
      </div>`

    **Closing line (line 809), replace with a one-line signup link:**
    old_string:
    `      <p class="comparison-closing">Not another general-purpose distro hoping agents will figure it out. A go-to Linux distro choice for running your agents — built from the kernel config to the default shell for machines that write code.</p>`
    new_string:
    `      <p class="comparison-closing">If that fits the way you want to run agents, <a href="#signup">join the waitlist</a>.</p>`

    Also reframe the comparison intro paragraph at line 792 to drop the "OS designed for agents" framing (which clashes with the plugin reality):

    **Edit — .comparison-intro (line 792):**
    old_string:
    `      <p class="comparison-intro">Every option today was built for a different job. Here's what changes when the OS is designed for agents.</p>`
    new_string:
    `      <p class="comparison-intro">Every option today was built for a different job. Here's what changes when the integration work is already done.</p>`

    Do NOT touch the `<section id="comparison">`, the `<h2>` heading, or the `.comparison-block` / `.comparison-closing` CSS classes.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'Not another general-purpose distro' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'go-to Linux distro choice' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'sudo npm install -g claude' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'curated version set' index.html | grep -qx 1 &amp;&amp; \
      grep -c '&lt;a href="#signup"&gt;join the waitlist&lt;/a&gt;' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'id="comparison"' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'class="comparison-block"' index.html | grep -qx 3 &amp;&amp; \
      echo "TASK 3 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html` returns `0` (SITE-04 grep gate — unchanged from original spec).
    - `grep -c 'Not another general-purpose distro' index.html` returns `0` (CONTEXT.md: closing line dropped).
    - `grep -c 'go-to Linux distro choice' index.html` returns `0`.
    - `grep -c 'sudo npm install -g claude' index.html` returns `1` (bug-class anchor present in Local-machine block).
    - `grep -c 'curated version set' index.html` returns `1` (curated-combo bet present in Generic-VMs block).
    - `grep -c '<a href="#signup">join the waitlist</a>' index.html` returns `1` (closing-line replacement).
    - Section preserved: `grep -c 'id="comparison"' index.html` returns `1`; `grep -c 'class="comparison-block"' index.html` returns `3`.
  </acceptance_criteria>

  <done>
    Three `#comparison` solution paragraphs now anchor on the canonical bug class + curated-combo bet (per STRATEGY.md `## What we're solving` diagnosis). Closing line drops the "general-purpose distro" framing and links to `#signup`. Block structure unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Rewrite FAQ #1 (What is AgentLinux?) + FAQ #5 (How is this different from Docker?) (index.html lines 830-832, 845-848)</name>

  <read_first>
    - index.html (lines 825-849 for the full FAQ section)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` items 6 + 7; `<code_context>` table FAQ rows)
    - docs/VISION.md (lines 16-22 for "## Positioning" framing — the plugin description)
  </read_first>

  <files>index.html</files>

  <action>
    Rewrite the answers to FAQ #1 and FAQ #5 in place. Questions stay verbatim; only the `<p class="faq-answer">` text changes. FAQ #2 (When?), #3 (Free?), #4 (Agents?) are NOT touched (per CONTEXT.md `<code_context>` table: OK as-is).

    Use the Edit tool, exact `old_string` / `new_string`.

    **FAQ #1 answer (line 831):**
    old_string:
    `          <p class="faq-answer">A Linux distribution purpose-built for AI coding agents. It runs on a dedicated machine — virtual or physical — where the agent gets an entire OS configured for its workload, not for human desktop use.</p>`
    new_string:
    `          <p class="faq-answer">An installable Ubuntu plugin that ships a dedicated agent environment and a curated, stability-tested toolchain. One install command provisions the agent user, the runtime, the install paths, and a small catalog of supported agents — so the user installs once and starts working with the agent.</p>`

    **FAQ #5 answer (line 847):**
    old_string:
    `          <p class="faq-answer">Docker is a container runtime. AgentLinux is a full operating system on a dedicated machine. The agent gets persistent state, real process management, mid-session package installs, and an environment that survives restarts.</p>`
    new_string:
    `          <p class="faq-answer">Docker is a container runtime. AgentLinux installs onto your real Ubuntu host — the agent keeps state across restarts, <code>systemctl</code> works, mid-session <code>apt install</code> works, and the agent user the plugin provisions owns its own Node.js runtime so <code>claude update</code> never needs <code>sudo</code>.</p>`

    Do NOT touch the FAQ `<h2>`, the `.faq-list` container, the `<p class="faq-question">` lines, or the other three FAQ items.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      grep -c 'Linux distribution purpose-built' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'full operating system on a dedicated machine' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'runs on a dedicated machine' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'full operating system' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'dedicated machine' index.html | grep -qx 0 &amp;&amp; \
      grep -c 'installable Ubuntu plugin' index.html | grep -qx 2 &amp;&amp; \
      grep -c 'class="faq-item"' index.html | grep -qx 5 &amp;&amp; \
      grep -c 'class="faq-question">When will it be available' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'class="faq-question">Is it free' index.html | grep -qx 1 &amp;&amp; \
      grep -c 'class="faq-question">What agents does it support' index.html | grep -qx 1 &amp;&amp; \
      echo "TASK 4 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Linux distribution purpose-built' index.html` returns `0`.
    - `grep -c 'full operating system on a dedicated machine' index.html` returns `0`.
    - `grep -c 'runs on a dedicated machine' index.html` returns `0` (global zero-count — last occurrence eliminated by FAQ #1 rewrite in this task).
    - `grep -c 'full operating system' index.html` returns `0` (global zero-count — last occurrence eliminated by FAQ #5 rewrite in this task).
    - `grep -c 'dedicated machine' index.html` returns `0` (global zero-count — last occurrences eliminated by FAQ #1 + #5 rewrites in this task).
    - `grep -c 'installable Ubuntu plugin' index.html` returns `2` (one in OG description from Task 1, one in FAQ #1 from this task).
    - 5 FAQ items preserved: `grep -c 'class="faq-item"' index.html` returns `5`.
    - Untouched FAQ questions present: `When will it be available`, `Is it free`, `What agents does it support` — each grep returns `1`.
  </acceptance_criteria>

  <done>
    FAQ #1 and FAQ #5 answers re-read in plugin voice; FAQ #2/3/4 unchanged; no contradicting strings remain in the FAQ section.
  </done>
</task>

<task type="auto">
  <name>Task 5: Render `assets/og-image.svg` → `assets/og-image.png` (1200×630)</name>

  <read_first>
    - assets/og-image.svg (the source SVG, 1.7 KB)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` § "OG image SVG → PNG (locked 2026-05-24)" + `<interfaces>` § "Tooling for SVG → PNG")
  </read_first>

  <files>assets/og-image.png</files>

  <action>
    Render the existing SVG to a 1200×630 PNG. The SVG stays at `assets/og-image.svg` as source-of-truth; the PNG lands at `assets/og-image.png`.

    Step 1 — pick a renderer. Try in order until one succeeds:
    ```
    cd /home/agent/agent-linux/.claude/worktrees/agenda
    command -v rsvg-convert || sudo apt-get update &amp;&amp; sudo apt-get install -y librsvg2-bin
    command -v rsvg-convert
    ```
    (Sudo is permitted per ADR-012 NOPASSWD for the `agent` user; `librsvg2-bin` is the canonical apt package for `rsvg-convert` on Ubuntu.)

    Step 2 — render:
    ```
    rsvg-convert -w 1200 -h 630 -f png -o assets/og-image.png assets/og-image.svg
    ```

    If `rsvg-convert` is genuinely unavailable after the apt install attempt (e.g. air-gapped CI), fall back in this order:
    ```
    magick -background none -density 300 -resize 1200x630! assets/og-image.svg assets/og-image.png
    # or
    convert -background none -density 300 -resize 1200x630! assets/og-image.svg assets/og-image.png
    # or
    inkscape --export-type=png --export-width=1200 --export-height=630 --export-filename=assets/og-image.png assets/og-image.svg
    ```
    Document the tool actually used in the per-task commit message (e.g. `feat(16-01): render assets/og-image.png via rsvg-convert (SITE-09)`).

    Step 3 — verify the PNG exists at exactly 1200×630 (see acceptance criteria).

    Do NOT delete or modify `assets/og-image.svg`. Do NOT modify `assets/crab-mascot.svg` or `assets/favicon.svg`.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      test -f assets/og-image.png &amp;&amp; \
      test -f assets/og-image.svg &amp;&amp; \
      file assets/og-image.png | grep -qE 'PNG image data, 1200 x 630' &amp;&amp; \
      echo "TASK 5 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `assets/og-image.png` exists (`test -f` passes).
    - `assets/og-image.svg` still exists (`test -f` passes — source-of-truth preserved).
    - `file assets/og-image.png` output contains `PNG image data, 1200 x 630` (exact dimensions per SITE-09 spec + CONTEXT.md OG conventions).
    - Per-task commit message records which renderer was used.
  </acceptance_criteria>

  <done>
    A 1200×630 PNG of the OG card lands at `assets/og-image.png`; the SVG source is preserved at `assets/og-image.svg`. SITE-09 evidence-side (the image artifact) lands; SITE-09 meta-side (the `og:image` / `twitter:image` references) already landed in Task 1.
  </done>
</task>

<task type="auto">
  <name>Task 6: Amend `.planning/REQUIREMENTS.md` — record the 2026-05-24 Phase 17 scope re-cut as a `## Superseded Items` block</name>

  <read_first>
    - .planning/REQUIREMENTS.md (lines 81-103 for the SITE-01..SITE-11 block + lines 186-232 for the existing four `## Superseded Items (...)` blocks — establishes the audit-trail style)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (`<decisions>` § "REQUIREMENTS.md SITE-* amendment (locked 2026-05-24)" — the canonical disposition table)
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
    Append a new `## Superseded Items (2026-05-24 Phase 17 scope re-cut)` block to `.planning/REQUIREMENTS.md`. The block lands AFTER the existing 2026-05-23 Round 2 block (currently at line 223+) and BEFORE the `## Deferred Items` section (currently at line 234). Mirror the audit-trail style of the three existing blocks: explanatory paragraph + disposition table.

    Use the Edit tool. The target insertion point is the line `## Deferred Items` at line 234. Insert the new block before it by replacing the old_string with `old + new` where old_string is the `## Deferred Items` line plus a preceding blank.

    Plan content to insert (substance is locked by CONTEXT.md's `<decisions>` table):

    ```markdown
    ## Superseded Items (2026-05-24 Phase 17 scope re-cut)

    The Phase 17 SITE-* requirements were re-cut at phase-discuss time on
    2026-05-24 to "minimum-viable contradiction removal." The original spec
    aggressively restructured the page (`#features` → `#pillars` with status
    badges, new `#install` section with curl snippet, footer doc-link push,
    deploy-time install-snippet drift check, mobile-screenshot PR ritual).
    The maintainer re-scoped to contradiction-removal only — the 8-card grid
    stays, copy inside contradicting cards is rewritten in place, no `#install`
    snippet lands, no footer doc-links, no nav `Vision` link, no PR screenshot
    ritual. The under-radar posture from STRATEGY.md `## Guiding policy`
    (downprioritize "growing surface area before the current gap is closed")
    is the governing decision; deferred items go to `<deferred>` in 17-CONTEXT.md.
    Precedent: Phase 15 STRAT-* → VIS-* + STRATR-* reframe (2026-05-16),
    Phase 16 STRATR-02 spine reframe (2026-05-19), Phase 16 execution-principles
    rewrite + STRATR-01 size bump (2026-05-23), and Phase 16 strategy/roadmap
    split (2026-05-23 Round 2). SITE-12 (Phase-close audit + milestone-close
    gate) is added in the same window — its substance was the trailing
    success-criterion of the original Phase 17 entry; promoted to a numbered
    requirement here so the AUDIT can cite it.

    | Old ID | Disposition |
    |--------|-------------|
    | SITE-01 (hero rewrite carries delivered-fact line + forward-looking line; voice rule applies) | Amended in-place: SITE-01 (hero value-prop is rewritten so the string `purpose-built Linux distribution` no longer appears; `grep -c 'purpose-built Linux distribution' index.html` returns 0). Hero copy aligns with `docs/VISION.md` mission line ("Linux that gives coding agents a stable place to run — without you having to set it up."); SITE-06 voice-rule grep continues to enforce voice on the rewritten copy. The two-line "delivered-fact + forward-looking" structure was dropped in favour of a single-line vision-flavoured value-prop because the under-radar posture (STRATEGY.md `## Guiding policy`) means no shipped-version cite belongs in the hero. |
    | SITE-02 (8-card `#features` grid replaced with 2-card `#pillars`; ≤ 3-sentence card body + `Learn more →` doc-link per card) | Superseded. The 8-card grid is preserved; the five contradicting cards are rewritten in place. New grep gate: `grep -cE 'apt install claude-code\|QEMU VM images\|Docker micro-VMs\|in distro repos\|distro repositories' index.html` returns 0. The `#features` → `#pillars` restructure + per-card doc-links was an information-architecture move; the IA is shippable as-is once the copy stops contradicting the plugin reality. Closes via the new SITE-02 grep gate + the explicit decision recorded in 17-AUDIT.md. |
    | SITE-03 (status badges `[SHIPPED v0.3.0]` + `[v0.6+ ROADMAP]` per pillar card) | Superseded. No `#pillars` section → no pillar cards → no badges to apply. Closes via SITE-02 supersession + the explicit "stay under radar; no shipped-version cite in hero / cards" decision recorded in 17-AUDIT.md. |
    | SITE-04 (reframe or remove `#comparison`; align with STRATEGY.md `## Where we are now`) | Kept, narrowed: SITE-04 (reframe path locked; `#comparison` block is preserved as three blocks anchored to the canonical bug class — `sudo npm install -g` EACCES + recursive-shim breakage — and the curated-combo bet per STRATEGY.md `## What we're solving`). Existing grep gate `grep -cE 'AgentLinux vs (Docker\|VM\|micro-VM)' index.html` returns 0 carries forward unchanged. |
    | SITE-05 (new `#install` section with curl snippet + SHA256 verify line) | Superseded. No `#install` section lands this phase. The README curl snippet remains the canonical install reference; the site stays under-radar. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | SITE-06 (voice-rule grep gate on rendered HTML; zero matches required — HARD GATE) | Kept unchanged. Voice-rule grep gate continues to enforce on `index.html`: `grep -nE 'AgentLinux (benchmarks\|measures\|defends\|protects\|prevents\|hardens)\b' index.html` returns zero matches. HARD GATE per VIS-07 / STRATR-06 precedent. |
    | SITE-07 (footer doc-links to VISION/STRATEGY/STABILITY-MODEL/decisions; nav `Vision` link) | Superseded. No footer doc-links land this phase; no nav `Vision` link. Under-radar posture from STRATEGY.md `## Guiding policy` (downprioritize public engagement until critical mass) drives the deferral. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | SITE-08 (OG / Twitter meta tags rewritten; no `purpose-built Linux distribution` language) | Kept. `og:title`, `og:description`, `twitter:title`, `twitter:description` rewritten this phase to reflect the plugin framing; same grep gates as the original. |
    | SITE-09 (OG image SVG → PNG; 1200×630; SVG preserved as source-of-truth) | Kept. `assets/og-image.png` rendered (rsvg-convert / magick / inkscape at the build host's discretion); `assets/og-image.svg` preserved; `og:image` + `twitter:image` meta tags repointed to `.png`. Closes the v0.1.0 known issue. |
    | SITE-10 (deploy-time install-snippet drift check wired into `.github/workflows/deploy.yml`) | Closed N/A. The conditional path already in the spec applies: no `#install` snippet on the site (per SITE-05 supersession) → no drift to check. `.github/workflows/deploy.yml` is untouched this phase. |
    | SITE-11 (PR body for the website-refresh PR includes mobile + narrow-viewport screenshots ≤ 375 px wide of every changed section) | Superseded. The PR review pass (technical-writer + fact-checker + ai-deslop per CLAUDE.md `## Review Loop` HTML row) is sufficient. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | (none — additive) | New SITE-12: Phase-close audit `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md` cites every SITE-XX evidence (KEEP / AMEND / SUPERSEDED / N/A dispositions); gate emits GREEN. Milestone-close gate (v0.3.3) also fires from this phase — Phase 17 is the last v0.3.3 phase, so the audit closes the milestone-coverage gate alongside its own phase-close gate. The audit-file path uses the SDK-derived directory slug (`16-website-refresh-agentlinux-org`), not the abbreviated form in the original SITE-06 / SITE-10 / SITE-12 spec language (`16-website-refresh`). |

    Net active SITE requirements after the 2026-05-24 amendment: SITE-01 (amended), SITE-04 (narrowed), SITE-06 (kept; HARD GATE), SITE-08 (kept), SITE-09 (kept), SITE-12 (new). Six active, five superseded, one N/A (SITE-10). Phase-16 traceability table (line 172-182 above) reads forward against this disposition.

    ```

    The exact insertion mechanism with the Edit tool:

    old_string:
    ```

    ## Deferred Items
    ```

    new_string:
    ```

    ## Superseded Items (2026-05-24 Phase 17 scope re-cut)

    The Phase 17 SITE-* requirements were re-cut at phase-discuss time on
    2026-05-24 to "minimum-viable contradiction removal." The original spec
    aggressively restructured the page (`#features` → `#pillars` with status
    badges, new `#install` section with curl snippet, footer doc-link push,
    deploy-time install-snippet drift check, mobile-screenshot PR ritual).
    The maintainer re-scoped to contradiction-removal only — the 8-card grid
    stays, copy inside contradicting cards is rewritten in place, no `#install`
    snippet lands, no footer doc-links, no nav `Vision` link, no PR screenshot
    ritual. The under-radar posture from STRATEGY.md `## Guiding policy`
    (downprioritize "growing surface area before the current gap is closed")
    is the governing decision; deferred items go to `<deferred>` in 17-CONTEXT.md.
    Precedent: Phase 15 STRAT-* → VIS-* + STRATR-* reframe (2026-05-16),
    Phase 16 STRATR-02 spine reframe (2026-05-19), Phase 16 execution-principles
    rewrite + STRATR-01 size bump (2026-05-23), and Phase 16 strategy/roadmap
    split (2026-05-23 Round 2). SITE-12 (Phase-close audit + milestone-close
    gate) is added in the same window — its substance was the trailing
    success-criterion of the original Phase 17 entry; promoted to a numbered
    requirement here so the AUDIT can cite it.

    | Old ID | Disposition |
    |--------|-------------|
    | SITE-01 (hero rewrite carries delivered-fact line + forward-looking line; voice rule applies) | Amended in-place: SITE-01 (hero value-prop is rewritten so the string `purpose-built Linux distribution` no longer appears; `grep -c 'purpose-built Linux distribution' index.html` returns 0). Hero copy aligns with `docs/VISION.md` mission line ("Linux that gives coding agents a stable place to run — without you having to set it up."); SITE-06 voice-rule grep continues to enforce voice on the rewritten copy. The two-line "delivered-fact + forward-looking" structure was dropped in favour of a single-line vision-flavoured value-prop because the under-radar posture (STRATEGY.md `## Guiding policy`) means no shipped-version cite belongs in the hero. |
    | SITE-02 (8-card `#features` grid replaced with 2-card `#pillars`; ≤ 3-sentence card body + `Learn more →` doc-link per card) | Superseded. The 8-card grid is preserved; the five contradicting cards are rewritten in place. New grep gate: `grep -cE 'apt install claude-code\|QEMU VM images\|Docker micro-VMs\|in distro repos\|distro repositories' index.html` returns 0. The `#features` → `#pillars` restructure + per-card doc-links was an information-architecture move; the IA is shippable as-is once the copy stops contradicting the plugin reality. Closes via the new SITE-02 grep gate + the explicit decision recorded in 17-AUDIT.md. |
    | SITE-03 (status badges `[SHIPPED v0.3.0]` + `[v0.6+ ROADMAP]` per pillar card) | Superseded. No `#pillars` section → no pillar cards → no badges to apply. Closes via SITE-02 supersession + the explicit "stay under radar; no shipped-version cite in hero / cards" decision recorded in 17-AUDIT.md. |
    | SITE-04 (reframe or remove `#comparison`; align with STRATEGY.md `## Where we are now`) | Kept, narrowed: SITE-04 (reframe path locked; `#comparison` block is preserved as three blocks anchored to the canonical bug class — `sudo npm install -g` EACCES + recursive-shim breakage — and the curated-combo bet per STRATEGY.md `## What we're solving`). Existing grep gate `grep -cE 'AgentLinux vs (Docker\|VM\|micro-VM)' index.html` returns 0 carries forward unchanged. |
    | SITE-05 (new `#install` section with curl snippet + SHA256 verify line) | Superseded. No `#install` section lands this phase. The README curl snippet remains the canonical install reference; the site stays under-radar. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | SITE-06 (voice-rule grep gate on rendered HTML; zero matches required — HARD GATE) | Kept unchanged. Voice-rule grep gate continues to enforce on `index.html`: `grep -nE 'AgentLinux (benchmarks\|measures\|defends\|protects\|prevents\|hardens)\b' index.html` returns zero matches. HARD GATE per VIS-07 / STRATR-06 precedent. |
    | SITE-07 (footer doc-links to VISION/STRATEGY/STABILITY-MODEL/decisions; nav `Vision` link) | Superseded. No footer doc-links land this phase; no nav `Vision` link. Under-radar posture from STRATEGY.md `## Guiding policy` (downprioritize public engagement until critical mass) drives the deferral. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | SITE-08 (OG / Twitter meta tags rewritten; no `purpose-built Linux distribution` language) | Kept. `og:title`, `og:description`, `twitter:title`, `twitter:description` rewritten this phase to reflect the plugin framing; same grep gates as the original. |
    | SITE-09 (OG image SVG → PNG; 1200×630; SVG preserved as source-of-truth) | Kept. `assets/og-image.png` rendered (rsvg-convert / magick / inkscape at the build host's discretion); `assets/og-image.svg` preserved; `og:image` + `twitter:image` meta tags repointed to `.png`. Closes the v0.1.0 known issue. |
    | SITE-10 (deploy-time install-snippet drift check wired into `.github/workflows/deploy.yml`) | Closed N/A. The conditional path already in the spec applies: no `#install` snippet on the site (per SITE-05 supersession) → no drift to check. `.github/workflows/deploy.yml` is untouched this phase. |
    | SITE-11 (PR body for the website-refresh PR includes mobile + narrow-viewport screenshots ≤ 375 px wide of every changed section) | Superseded. The PR review pass (technical-writer + fact-checker + ai-deslop per CLAUDE.md `## Review Loop` HTML row) is sufficient. Closes via the explicit decision recorded in 17-AUDIT.md. |
    | (none — additive) | New SITE-12: Phase-close audit `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md` cites every SITE-XX evidence (KEEP / AMEND / SUPERSEDED / N/A dispositions); gate emits GREEN. Milestone-close gate (v0.3.3) also fires from this phase — Phase 17 is the last v0.3.3 phase, so the audit closes the milestone-coverage gate alongside its own phase-close gate. The audit-file path uses the SDK-derived directory slug (`16-website-refresh-agentlinux-org`), not the abbreviated form in the original SITE-06 / SITE-10 / SITE-12 spec language (`16-website-refresh`). |

    Net active SITE requirements after the 2026-05-24 amendment: SITE-01 (amended), SITE-04 (narrowed), SITE-06 (kept; HARD GATE), SITE-08 (kept), SITE-09 (kept), SITE-12 (new). Six active, five superseded, one N/A (SITE-10). Phase-16 traceability table (line 172-182 above) reads forward against this disposition.

    ## Deferred Items
    ```

    Do NOT modify the SITE-01..SITE-11 + SITE-12 requirement entries themselves (lines 83-103). The amendment block is purely additive — the disposition table is the authoritative interpretation that downstream gates and the AUDIT consume. (Phase 15 / 15 precedent: the original requirement bullets remain checked-against by their own original wording; the `## Superseded Items` block records the agreed-on interpretation.)

    Note that SITE-12 does not yet exist as a numbered entry in the SITE block; it is introduced by this amendment block (per the precedent of STRATR-07 introduced via the 2026-05-23 Round 2 amendment in REQUIREMENTS.md line 232). The AUDIT in Task 8 cites SITE-12 against this amendment block as the source of authority.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      grep -nF '## Superseded Items (2026-05-24 Phase 17 scope re-cut)' .planning/REQUIREMENTS.md | wc -l | grep -qx 1 &amp;&amp; \
      grep -c '## Superseded Items' .planning/REQUIREMENTS.md | grep -qx 5 &amp;&amp; \
      grep -c '| SITE-01 (' .planning/REQUIREMENTS.md | grep -qx 1 &amp;&amp; \
      grep -c '| New SITE-12: Phase-close audit' .planning/REQUIREMENTS.md | grep -qx 1 &amp;&amp; \
      grep -c '## Deferred Items' .planning/REQUIREMENTS.md | grep -qx 1 &amp;&amp; \
      echo "TASK 6 PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - Exactly one new heading present: `## Superseded Items (2026-05-24 Phase 17 scope re-cut)`.
    - Total `## Superseded Items` blocks: `5` (existing four from 2026-05-16, 2026-05-19, 2026-05-23, 2026-05-23 Round 2 + the new 2026-05-24 block).
    - All 11 disposition rows + the SITE-12 additive row present (12 rows total in the new disposition table).
    - `## Deferred Items` heading still present exactly once (insertion landed BEFORE it, not on top of it).
    - The original SITE-01..SITE-11 requirement entries at lines 83-103 are untouched (visual diff confirms no edits to that block).
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md carries a fifth `## Superseded Items` block recording the 2026-05-24 Phase 17 scope re-cut. The amendment introduces SITE-12 (phase-close audit + milestone-close gate). The audit-trail style mirrors Phase 15 / 15 precedent. Downstream gates and the AUDIT consume this block as the authoritative interpretation of SITE-* dispositions.
  </done>
</task>

<task type="auto">
  <name>Task 7: Run SITE-06 voice-rule HARD GATE + collateral SITE-* gates against the edited `index.html`</name>

  <read_first>
    - index.html (final state after Tasks 1-4)
    - .planning/REQUIREMENTS.md (final state after Task 6 — for the canonical grep commands)
    - .planning/phases/16-strategy-roadmap-doc/16-AUDIT.md (lines 230-255 + 575-585 for the STRATR-06 transcript format — `exit=$?` pattern)
  </read_first>

  <files>(no files modified — this task captures grep transcripts for Task 8 to consume)</files>

  <action>
    Run the canonical SITE-XX gate commands and capture each command's exact output to scratch. The transcripts feed directly into the Task 8 AUDIT. Do NOT modify any files in this task. Do NOT silently fix any failures — if a gate fails, STOP and report; the offending earlier task must be revisited.

    Execute, IN THIS ORDER, capturing stdout + exit code for each:

    ```
    cd /home/agent/agent-linux/.claude/worktrees/agenda

    # SITE-01 amended gate (hero rewrite — no purpose-built distribution string)
    grep -c 'purpose-built Linux distribution' index.html ; echo "exit=$?"

    # SITE-02 amended gate (5 contradicting strings absent)
    grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html ; echo "exit=$?"

    # SITE-04 grep gate (unchanged from original — no competition framing)
    grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html ; echo "exit=$?"

    # SITE-06 HARD GATE — voice-rule grep — zero matches required
    grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' index.html ; echo "exit=$?"

    # SITE-08 — OG/Twitter meta presence + no contradicting string
    grep -cE 'property="og:(title|description)"' index.html ; echo "exit=$?"
    grep -cE 'name="twitter:(title|description)"' index.html ; echo "exit=$?"

    # SITE-09 — PNG OG image file + meta references
    ls -la assets/og-image.png assets/og-image.svg
    file assets/og-image.png
    grep -cE 'property="og:image" content="[^"]*\.png"' index.html ; echo "exit=$?"
    grep -cE 'name="twitter:image" content="[^"]*\.png"' index.html ; echo "exit=$?"

    # Defensive — ensure the hero value-prop landed
    grep -Fc 'Linux that gives coding agents a stable place to run' index.html ; echo "exit=$?"

    # Defensive — preserved structure
    grep -c 'class="feature-card"' index.html ; echo "exit=$?"
    grep -c 'class="comparison-block"' index.html ; echo "exit=$?"
    grep -c 'class="faq-item"' index.html ; echo "exit=$?"
    grep -c 'id="problem"' index.html ; echo "exit=$?"
    grep -c 'id="comparison"' index.html ; echo "exit=$?"
    grep -c 'id="signup"' index.html ; echo "exit=$?"
    grep -c 'id="faq"' index.html ; echo "exit=$?"
    ```

    Expected outputs:

    | Gate | Expected stdout | Expected exit |
    |------|-----------------|---------------|
    | SITE-01 amended (purpose-built) | `0` | `exit=1` |
    | SITE-02 amended (5 strings) | `0` | `exit=1` |
    | SITE-04 grep | `0` | `exit=1` |
    | SITE-06 HARD GATE | (no lines) | `exit=1` |
    | SITE-08 og: | `2` | `exit=0` |
    | SITE-08 twitter: | `2` | `exit=0` |
    | SITE-09 file output | `PNG image data, 1200 x 630` | n/a |
    | SITE-09 og:image .png | `1` | `exit=0` |
    | SITE-09 twitter:image .png | `1` | `exit=0` |
    | Hero value-prop | `3` | `exit=0` |
    | feature-card count | `8` | `exit=0` |
    | comparison-block count | `3` | `exit=0` |
    | faq-item count | `5` | `exit=0` |
    | id="problem" | `1` | `exit=0` |
    | id="comparison" | `1` | `exit=0` |
    | id="signup" | `1` | `exit=0` |
    | id="faq" | `1` | `exit=0` |

    Save the entire transcript (each command + each output + `exit=...`) into the executor's scratch buffer for Task 8 to lift verbatim into the AUDIT. (No artefact file is produced by Task 7; the transcript travels in-memory to Task 8.)

    **HARD GATE failure handling:** if SITE-06 returns ANY match, stop and revisit Task 1, 2, 3, or 4 — the rewritten copy includes a forbidden verb on `AgentLinux`. If SITE-01 or SITE-02 amended gate returns non-zero count, stop and revisit Task 1 or Task 2. If SITE-09 PNG dimensions are not exactly `1200 x 630`, stop and revisit Task 5.
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      [ "$(grep -c 'purpose-built Linux distribution' index.html)" = "0" ] &amp;&amp; \
      [ "$(grep -c 'runs on a dedicated machine' index.html)" = "0" ] &amp;&amp; \
      [ "$(grep -c 'full operating system' index.html)" = "0" ] &amp;&amp; \
      [ "$(grep -c 'dedicated machine' index.html)" = "0" ] &amp;&amp; \
      [ "$(grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html)" = "0" ] &amp;&amp; \
      [ "$(grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html)" = "0" ] &amp;&amp; \
      [ -z "$(grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' index.html)" ] &amp;&amp; \
      [ "$(grep -cE 'property="og:image" content="[^"]*\.png"' index.html)" = "1" ] &amp;&amp; \
      file assets/og-image.png | grep -qE 'PNG image data, 1200 x 630' &amp;&amp; \
      echo "TASK 7 PASS — SITE-06 HARD GATE GREEN"
    </automated>
  </verify>

  <acceptance_criteria>
    - SITE-06 HARD GATE: `grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' index.html` returns EMPTY stdout + `exit=1`. Hard gate; failure = STOP.
    - SITE-01 amended gate: `grep -c 'purpose-built Linux distribution' index.html` returns `0`.
    - Defence-in-depth: `grep -c 'runs on a dedicated machine' index.html` returns `0` (global re-assert; primary gate is Task 4).
    - Defence-in-depth: `grep -c 'full operating system' index.html` returns `0` (global re-assert; primary gate is Task 4).
    - Defence-in-depth: `grep -c 'dedicated machine' index.html` returns `0` (global re-assert; primary gate is Task 4).
    - SITE-02 amended gate: 5-string grep returns `0`.
    - SITE-04 grep: `grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html` returns `0`.
    - SITE-08: `og:title` + `og:description` both present (count ≥ 2); `twitter:title` + `twitter:description` both present (count ≥ 2).
    - SITE-09: `assets/og-image.png` exists with `PNG image data, 1200 x 630`; both `og:image` and `twitter:image` meta references end in `.png`.
    - Structural sanity: 8 feature cards, 3 comparison blocks, 5 FAQ items, all four section ids (`problem`, `comparison`, `signup`, `faq`) present exactly once each.
    - Full transcript (commands + outputs + `exit=...`) preserved for Task 8 to lift verbatim.
  </acceptance_criteria>

  <done>
    SITE-06 voice-rule HARD GATE returns GREEN; all collateral SITE-* grep gates return their expected counts; the PNG exists at the right dimensions; the transcript is captured for Task 8.
  </done>
</task>

<task type="auto">
  <name>Task 8: Author `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md` — phase-close audit + v0.3.3 milestone-close gate</name>

  <read_first>
    - .planning/phases/16-strategy-roadmap-doc/16-AUDIT.md (the structural and stylistic precedent for the audit)
    - .planning/phases/15-vision-doc-and-downstream/15-AUDIT.md (lines 1-50 + 551-605 for the N/A close pattern + aggregate gate status table format)
    - .planning/REQUIREMENTS.md (lines 81-103 for SITE-XX requirement text + the new 2026-05-24 Superseded Items block from Task 6 for the dispositions)
    - .planning/phases/17-website-refresh-agentlinux-org/17-CONTEXT.md (for the explicit decisions to cite in supersession sections)
    - .planning/ROADMAP.md (for the milestone-close gate framing — Phase 17 is the last v0.3.3 phase)
  </read_first>

  <files>.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md</files>

  <action>
    Author the phase-close audit as a brand new file. Lift the structural pattern verbatim from `16-AUDIT.md` and the N/A-close pattern from `15-AUDIT.md` § DOC-05. The audit cites EVERY SITE-XX requirement (01-12), with each disposition (KEEP / AMEND / SUPERSEDED / N/A) cited against either (a) a grep transcript from Task 7 or (b) the 2026-05-24 Superseded Items block in REQUIREMENTS.md (committed in Task 6).

    Use the Write tool to create the file at exactly:
    `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md`

    Required sections (in order):

    1. **Top metadata block** — `> Phase:`, `> Authored:` (use today's date 2026-05-24), `> Gate:` (GREEN).

    2. **`## Summary`** — 2-3 paragraphs naming what Phase 17 did, the 2026-05-24 scope re-cut, the SITE-* amendment that landed in the same commit window, the reviewer pass result (technical-writer + fact-checker + ai-deslop per CLAUDE.md HTML row). Plus a `Plan commits:` list (single plan — `16-01`); reviewer pass record reused from the parallel autonomous mode that other phases inline-applied; total requirements closed: 12 (6 active gates PASS + 5 SUPERSEDED + 1 N/A).

    3. **`## SITE-01 — Hero value-prop rewrite (AMENDED 2026-05-24)`** — Acceptance restated from REQUIREMENTS.md (with reference to the 2026-05-24 amendment row); evidence: grep transcripts from Task 7 (`purpose-built Linux distribution` returns 0; the hero copy `Linux that gives coding agents a stable place to run` returns 3 occurrences across hero + OG + Twitter); verdict: PASS (amended gate satisfied).

    4. **`## SITE-02 — `#features` 8-card grid preserved with rewritten copy (SUPERSEDED 2026-05-24)`** — Cite the 2026-05-24 amendment row in REQUIREMENTS.md; evidence: new grep gate `grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html` returns 0; defensive `grep -c 'class="feature-card"' index.html` returns 8; verdict: PASS via supersession (the new gate enforces the no-contradiction contract). Note (audit-trail): the REQUIREMENTS.md SITE-02 amendment-row grep was deliberately strengthened from the 4-alternation form in `17-CONTEXT.md` (`apt install claude-code|QEMU VM images|Docker micro-VMs|distro repos`) to a 5-alternation form (`apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories`) to also catch the `distro repositories` surface form as forward-looking insurance; record this deviation in this section as a one-line audit note.

    5. **`## SITE-03 — Pillar card status badges (SUPERSEDED 2026-05-24)`** — Cite the 2026-05-24 amendment row; explicit decision: no `#pillars` section → no badges; closes via SITE-02 supersession + the under-radar posture from STRATEGY.md `## Guiding policy`. Verdict: closed SUPERSEDED.

    6. **`## SITE-04 — `#comparison` reframe (KEPT, narrowed)`** — Acceptance: existing grep gate `grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html` returns 0; evidence from Task 7 transcript; verdict: PASS.

    7. **`## SITE-05 — `#install` section (SUPERSEDED 2026-05-24)`** — Cite the 2026-05-24 amendment row; explicit decision: README remains the canonical install reference; the site stays under-radar per STRATEGY.md `## Guiding policy`. Verdict: closed SUPERSEDED.

    8. **`## SITE-06 — Voice-rule grep HARD GATE (KEPT — HARD GATE)`** — Acceptance: exact command from REQUIREMENTS.md; transcript from Task 7 (empty stdout + `exit=1`); verdict: PASS (HARD GATE GREEN). Mirror the STRATR-06 transcript style from `16-AUDIT.md` § STRATR-06.

    9. **`## SITE-07 — Footer doc-links + nav `Vision` link (SUPERSEDED 2026-05-24)`** — Cite the 2026-05-24 amendment row; explicit decision: under-radar posture from STRATEGY.md `## Guiding policy` (downprioritize public engagement until critical mass) drives the deferral. Verdict: closed SUPERSEDED.

    10. **`## SITE-08 — OG / Twitter meta tags rewritten (KEPT)`** — Acceptance from REQUIREMENTS.md; evidence: Task 7 grep counts (`og:` ≥ 2, `twitter:` ≥ 2) + the SITE-01 `purpose-built Linux distribution` zero-match transcript reused; verdict: PASS.

    11. **`## SITE-09 — OG image SVG → PNG (KEPT)`** — Acceptance from REQUIREMENTS.md; evidence: Task 7 transcripts (`file assets/og-image.png` shows `PNG image data, 1200 x 630`; both meta references end in `.png`; SVG preserved at `assets/og-image.svg`); verdict: PASS.

    12. **`## SITE-10 — Deploy-time install-snippet drift check (N/A 2026-05-24)`** — Mirror the `15-AUDIT.md` § DOC-05 N/A-close pattern verbatim: one-line decision "no install snippet on the site (per SITE-05 supersession) → no drift to check; `.github/workflows/deploy.yml` untouched this phase." Cite the 2026-05-24 amendment row + the SITE-05 supersession.

    13. **`## SITE-11 — Mobile / narrow-viewport PR screenshots (SUPERSEDED 2026-05-24)`** — Cite the 2026-05-24 amendment row; explicit decision: PR review pass (technical-writer + fact-checker + ai-deslop per CLAUDE.md `## Review Loop` HTML row) is sufficient. Verdict: closed SUPERSEDED.

    14. **`## SITE-12 — Phase-close audit + milestone-close gate (KEPT — additive 2026-05-24)`** — Cite the 2026-05-24 amendment row (which introduced SITE-12 as additive); evidence: this file exists at the canonical path. Self-referential close.

    15. **`## Reviewer pass record`** — Mirror the `16-AUDIT.md` § "Reviewer pass record" structure. Reviewers per CLAUDE.md HTML row: `technical-writer` + `fact-checker` + `ai-deslop`. Inline autonomous mode per the Phase 15 / 15 precedent. List comments returned + triage table (apply / decline + reasoning). If no actionable findings, record that explicitly (the Phase 16 round-1 precedent).

    16. **`## Aggregate gate status`** — Table with columns `Requirement | Verdict | Evidence source`. 12 rows (SITE-01..SITE-12). Verdicts: PASS / PASS-via-supersession / N/A. Evidence cites this audit's own section number for each.

    17. **`**Phase 17 gate: GREEN.**`** — terminal line.

    18. **`## v0.3.3 Milestone-close Gate`** — A short section emitting the milestone-close gate. Phase 17 is the last v0.3.3 phase (per ROADMAP.md `## Progress` table); list all 5 phases (12, 13, 14, 15, 16) with their per-phase AUDIT paths and PASS/GREEN verdicts. Total milestone requirements closed: 33 (per REQUIREMENTS.md `## Traceability` table line 184: 2 EXPL + 9 VIS + 6 STRATR + 5 DOC + 12 SITE = 34 with the new SITE-12; reconcile in this audit). Defensive cross-check: `ls .planning/phases/{12,13,14,15,16}-*/[0-9]*-AUDIT.md` shows 5 audit files. Terminal line: `**v0.3.3 milestone-close gate: GREEN.**`

    Style conventions (lift from 14/15 audits):
    - Every grep transcript is fenced with triple-backticks and labelled `Command:` / `Output:`.
    - Every `Verdict:` line bolds PASS / N/A / SUPERSEDED.
    - The AUDIT itself MUST pass the voice-rule grep (defensive practice — mirror 16-AUDIT.md § "Audit-itself voice rule" sanity check at the end).
    - Markdown table cells use the same `|...|...|` shape as 16-AUDIT.md.

    Final defensive grep at the end of the AUDIT body (per 16-AUDIT.md precedent at lines 287-298):

    ```
    grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md ; echo "exit=$?"
    ```

    Expected: `exit=1` (audit introduces no voice-rule regressions on itself).
  </action>

  <verify>
    <automated>
      cd /home/agent/agent-linux/.claude/worktrees/agenda &amp;&amp; \
      test -f .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      grep -c '^## SITE-' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md | grep -qx 12 &amp;&amp; \
      grep -qF '**Phase 17 gate: GREEN.**' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      grep -qF '**v0.3.3 milestone-close gate: GREEN.**' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      grep -qF '> Gate: GREEN' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      grep -qF '## Aggregate gate status' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      grep -qF '## Reviewer pass record' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md &amp;&amp; \
      [ -z "$(grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md)" ] &amp;&amp; \
      echo "TASK 8 PASS — Phase 17 + v0.3.3 milestone-close gates GREEN"
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists at exactly `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md`.
    - Exactly 12 `## SITE-XX` H2 sections (SITE-01..SITE-12).
    - `> Gate: GREEN` blockquote in top metadata block.
    - `**Phase 17 gate: GREEN.**` terminal line present.
    - `**v0.3.3 milestone-close gate: GREEN.**` terminal line present (separate from phase gate).
    - `## Aggregate gate status` table present (with 12 SITE rows).
    - `## Reviewer pass record` section present.
    - Defensive voice-rule grep on the audit itself returns empty (audit text introduces no `AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)` matches).
    - Every disposition (KEEP / AMEND / SUPERSEDED / N/A) is cited either against a Task-7 grep transcript or against the 2026-05-24 Superseded Items block in REQUIREMENTS.md (line refs OK).
    - Milestone-close section lists all five v0.3.3 phases (12, 13, 14, 15, 16) with their AUDIT paths.
  </acceptance_criteria>

  <done>
    `17-AUDIT.md` exists with every SITE-XX requirement cited, every disposition justified, the Phase 17 gate emitted GREEN, and the v0.3.3 milestone-close gate emitted GREEN. Format mirrors 15-AUDIT.md / 16-AUDIT.md precedent. The audit itself passes the defensive voice-rule grep.
  </done>
</task>

</tasks>

<verification>
After all 8 tasks complete, the executor must verify the full phase-close shape with a final cross-cutting check:

```bash
cd /home/agent/agent-linux/.claude/worktrees/agenda

# Phase 17 deliverables exist + pass their gates
test -f index.html
test -f assets/og-image.png
test -f assets/og-image.svg
test -f .planning/REQUIREMENTS.md
test -f .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md

# SITE-06 HARD GATE (zero matches required)
test -z "$(grep -nE 'AgentLinux (benchmarks|measures|defends|protects|prevents|hardens)\b' index.html)"

# SITE-01 amended (no purpose-built distribution string)
test "$(grep -c 'purpose-built Linux distribution' index.html)" = "0"

# SITE-02 amended (no five-string contradictions)
test "$(grep -cE 'apt install claude-code|QEMU VM images|Docker micro-VMs|in distro repos|distro repositories' index.html)" = "0"

# SITE-04 (no competition framing)
test "$(grep -cE 'AgentLinux vs (Docker|VM|micro-VM)' index.html)" = "0"

# SITE-09 (PNG at the right dims)
file assets/og-image.png | grep -qE 'PNG image data, 1200 x 630'

# REQUIREMENTS.md amendment block landed
grep -qF '## Superseded Items (2026-05-24 Phase 17 scope re-cut)' .planning/REQUIREMENTS.md
test "$(grep -c '## Superseded Items' .planning/REQUIREMENTS.md)" = "5"

# Audit emits both gates GREEN
grep -qF '**Phase 17 gate: GREEN.**' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md
grep -qF '**v0.3.3 milestone-close gate: GREEN.**' .planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md

# All five v0.3.3 phase audits exist
ls .planning/phases/12-*/12-*-AUDIT.md 2>/dev/null || ls .planning/phases/12-*/[0-9]*-AUDIT.md 2>/dev/null
ls .planning/phases/13-*/13-*-AUDIT.md 2>/dev/null || ls .planning/phases/13-*/[0-9]*-AUDIT.md 2>/dev/null
ls .planning/phases/14-*/15-AUDIT.md
ls .planning/phases/15-*/16-AUDIT.md
ls .planning/phases/16-*/17-AUDIT.md

# Reviewer pass per CLAUDE.md `## Review Loop` HTML row applied to index.html
#   technical-writer + fact-checker + ai-deslop reviews captured inline in 17-AUDIT.md § "Reviewer pass record"
```

All steps must succeed before the executor commits the final per-task atomic commits and runs the standard PR / review hand-off.
</verification>

<success_criteria>
Phase 17 is complete when:

- `index.html` contains zero matches for any of the eight forbidden strings (`purpose-built Linux distribution`, `runs on a dedicated machine`, `apt install claude-code`, `in distro repos`, `distro repositories`, `QEMU VM images`, `Docker micro-VMs`, `full operating system on a dedicated machine`, `Boots into a non-root user`) and the SITE-06 voice-rule HARD GATE grep returns zero matches.
- The hero value-prop reads `Linux that gives coding agents a stable place to run — without you having to set it up.` (echoed in OG + Twitter descriptions for a total of 3 occurrences).
- The 8-card `#features` grid, 3-block `#comparison` section, dual-column `#problem` framing, signup form, 5-item FAQ, top nav, and footer are all structurally preserved.
- `assets/og-image.png` exists at exactly 1200×630; `assets/og-image.svg` is preserved; OG + Twitter image meta tags reference the `.png`.
- `.planning/REQUIREMENTS.md` carries a new `## Superseded Items (2026-05-24 Phase 17 scope re-cut)` block (fifth such block in the file) recording the dispositions for SITE-01..SITE-11 and introducing SITE-12.
- `.planning/phases/17-website-refresh-agentlinux-org/17-AUDIT.md` exists with all 12 SITE-XX sections, the Phase 17 GREEN gate, the v0.3.3 milestone-close GREEN gate, an aggregate-gate table, and a reviewer-pass record. The audit itself passes the defensive voice-rule grep.
- The reviewer pass per CLAUDE.md `## Review Loop` HTML row (`technical-writer` + `fact-checker` + `ai-deslop`) is applied inline; comments triaged.
</success_criteria>

<output>
After completion, create `.planning/phases/17-website-refresh-agentlinux-org/17-01-SUMMARY.md` per the standard `$HOME/.claude/get-shit-done/templates/summary.md` template — capturing per-task commits, deviations from this plan, and any new decisions worth carrying forward to v0.3.4 (notably the under-radar posture's continuing implications for site CTAs).
</output>
