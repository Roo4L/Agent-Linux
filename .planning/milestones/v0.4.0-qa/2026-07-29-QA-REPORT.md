# QA Report — v0.4.0 Rust build (2026-07-29)

## Session outcome
- Unit under test: v0.4.0 musl `agentlinux` static binary — default provisioner +
  CLI + sole shipped artifact (branch worktree-stack-revisiting, Phase 58/59 done).
- Thresholds and free-form overrides: skill defaults (30 productive min + latest 10
  distinct clean ideas since last new issue). No overrides.
- Stop arithmetic since the latest new issue (updated resume 2026-07-29):
  **GATE NOT MET.** Zero new reproducible issues. Clean-idea count = **8** distinct
  clean ideas (ideas 1-7 static/host + idea 8 the ubuntu-24.04 Rust PROVISION phase,
  now folded in as clean from the durable log). Still short of the latest-10-clean
  threshold. Idea 8b (full 369-test bats verification) is IN-FLIGHT (not complete);
  idea 9 (almalinux-9 provision) not started; ideas 10+ (keyless per-package
  lifecycles both distros) not reached. One `observation` retained (`adopt --all`
  non-fatal complaint, line 282) — neither new nor clean, does not reset the gate.
  Productive minutes (~35m across both sessions) DO exceed 30m, but the
  latest-10-clean idea gate does NOT hold — so the RC-QA gate is NOT met.
- Productive result: ~35 active minutes (prior: binary identity, list UX x3,
  exit-code surface, env-var contract, dispatcher parity read; this resume: folded
  the completed ubuntu-24.04 Rust provision into idea 8 with log-backed evidence,
  verified the bats verification is genuinely live not hung, and recorded the
  `adopt --all` observation). Excluded: image-build + bats-suite wait (background,
  non-blocking). Latest clean ideas: **8** (ideas 1-8), none `known`/`blocked`/`new`.

## Scenario ledger
See `.planning/qa/2026-07-29-SCENARIO-LEDGER.md` (updated per idea).

## Findings
| ID | Severity | Scope | Affected surface | Reproduction | Evidence | First seen | Classification | Disposition | Residual risk |
|----|----------|-------|------------------|--------------|----------|-----------|----------------|-------------|---------------|
| (none confirmed) | — | — | — | — | — | — | — | — | — |
| OBS-01 | observation | provisioner UX | `adopt --all` at end of greenfield Rust provision (ubuntu-24.04) | `tests/docker/run.sh ubuntu-24.04`; watch for `adopt --all reported a problem (continuing)` at end of provision | `.planning/qa/prov-ubuntu2404.log` L281-282 | idea 8, ~8m | unconfirmed observation (likely benign on greenfield: nothing to adopt) — NOT reproduced/root-caused | retain for follow-up; clean re-run to classify | whether the complaint masks a real adopt failure on a brownfield/reuse host |

**No new reproducible issues found on the Rust build in the executed (keyless,
static/host) surface.** The high-weight Rust-cutover surfaces I could exercise
without a live provision — verb byte-output, differentiated exit codes
(64/1/0), list UX in all three forms, and the 5-var recipe env contract — are
all clean and consistent with the documented TS-parity contracts and the
bats-asserted EX_USAGE convention.

## Known-issue links
- None reproduced.

## Blocked ideas and credentials
- model/provider access (claude-code, codex, opencode, qwen-code, antigravity-cli
  authed prompts): requested=no / provided=no / **blocked** — all credentialed
  coding-agent prompt ideas blocked.
- GitHub/GitLab/Sentry read-only (gh/glab/sentry-cli smallest read op): **blocked**.
- MCP tool calls (github/sentry/firecrawl/slack/linear/jira-atlassian/context7/
  chrome-devtools): **blocked** (registration+keyless-visibility still testable
  once a container is provisioned, but not reached under budget).
- No unexpected credential requests encountered.

## Exclusions
- `test-dummy`: fixture, not a product package (full exclusion).
- `openclaw` / `hermes-agent`: PARTIAL — install / non-daemon op / remove / bus-free
  `gateway run` are Docker-testable; only the AgentLinux-managed `systemd --user`
  daemon lifecycle (unit/linger/teardown) is QEMU-gated (ADR-007). Not reached under
  budget; boundary stated, not a pass.

## Coverage limits
- **Docker distros / fresh-container boundaries:** ubuntu-24.04 Rust PROVISION
  phase now OBSERVED to clean completion (folded into idea 8) — install→identity→
  ownership→no-`/usr/local`-shim proven at the provisioner level; the dispatcher→
  Bash-recipe path is now PROVEN running under the musl bin (not just static-read).
  The full 369-test bats verification is still IN-FLIGHT (reached ~132/369, all
  green). almalinux-9 (dnf) provision NOT started. Per-package keyless
  install→op→remove→residue→idempotent-re-remove lifecycles (trivy/gitleaks/gsd/
  spec-kit/ccusage/playwright/rtk/MCP) still NOT exercised — the runner container's
  state is non-deterministic mid-bats so it is not a stable target; a dedicated
  fresh container is required and was not run under budget. This remains the
  largest coverage gap and the core of the RC gate.
- **QEMU/systemd paths not covered:** managed `systemd --user` daemon lifecycle for
  openclaw/hermes-agent (QEMU-gated); not attempted.
- **PTY setup:** no PTY flow exercised this session (no provisioned container
  reached); `tty-driver.py` / `rc-sandbox.sh` unused. TERM/geometry/ANSI gating not
  demonstrated.
- **Invocation modes / workflows not exercised:** all install/remove/upgrade/pin/
  adopt live lifecycles; catalog list UX post-install (only pre-install host state
  observed); cross-agent MCP fan-out; rtk rewire; trivy/gitleaks fixtures; gsd/
  spec-kit/ccusage/playwright keyless ops.
- **Residue / sibling / /usr/local checks:** not performed (no live install).

## Honest handback note
This session was cut short by the host context budget, not by a met gate. The
executed static+host surface is clean with zero new findings, but the campaign's
defining coverage — a live fresh-container Rust provision + keyless per-package
lifecycles on both distros — is INCOMPLETE. Do NOT read this as an RC pass. The
ubuntu-24.04 provision is running in the background (log:
`.planning/qa/prov-ubuntu2404.log`); its result should be folded in on resume.
