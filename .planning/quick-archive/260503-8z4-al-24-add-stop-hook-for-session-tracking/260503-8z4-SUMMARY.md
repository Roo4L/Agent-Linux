---
phase: quick-260503-8z4
plan: 01
quick_id: 260503-8z4
subsystem: claude-hooks
tags: [hooks, stop-hook, session-tracker, jira, AL-24]
requirements:
  - AL-24
related:
  - AL-23  # Mirror hook (review-reminder.sh) — see commit 1d4ea7c
dependency_graph:
  requires:
    - .claude/hooks/review-reminder.sh         # mirror source for shape
    - .claude/skills/session-tracker/SKILL.md  # skill the hook nudges Claude to invoke
    - .claude/settings.json                     # existing project-shared Stop matcher block
    - docs/decisions/010-review-loop-via-claude-md.md  # ADR-010 (Refinement allows this hook class)
  provides:
    - Project-wide Stop-hook reminder to track Jira deliverables for the AL project
    - One-shot guard pattern reused across hook variants (no extra cost beyond first stop)
  affects:
    - CLAUDE.md  # new "Session Tracking" section
    - docs/decisions/010-review-loop-via-claude-md.md  # Implementation pointer enumerates both hooks
tech-stack:
  added: []  # No new dependencies — pure bash + JSON + markdown
  patterns:
    - "stop_hook_active one-shot guard (ADR-010 Refinement 2026-05-02)"
    - "Reminder-only hook: emits text, does not invoke skills/subagents"
key-files:
  created:
    - .claude/hooks/session-tracker-reminder.sh
  modified:
    - .claude/settings.json
    - CLAUDE.md
    - docs/decisions/010-review-loop-via-claude-md.md
decisions:
  - "Reuse review-reminder.sh shape byte-for-byte (set -euo pipefail, stdin grep, JSON heredoc) — only the reason text differs"
  - "Co-locate both hooks under the same Stop matcher block in settings.json rather than two matcher blocks (simpler config; semantically equivalent because matcher is '*')"
  - "Amend ADR-010 Implementation pointer to enumerate both hooks instead of writing ADR-016 (the Refinement already establishes the reminder-hook category; this is a second instance of an existing pattern, not a new decision)"
metrics:
  duration: "~6 minutes"
  completed: "2026-05-03T06:41:58Z"
  tasks_completed: 3
  files_changed: 4
  commits: 3
---

# Quick 260503-8z4: Add Stop Hook for Session Tracking (AL-24) — Summary

One-liner: project-shared Stop hook that nudges Claude to invoke the
`session-tracker` skill (Jira project AL) when a session produced a concrete
deliverable, mirroring the AL-23 review-reminder hook shape and reusing the
ADR-010 Refinement that authorized that pattern.

## What landed

Three atomic commits, four files touched, no test regressions, no deferred items.

| # | Commit  | Type | Files | Purpose |
|---|---------|------|-------|---------|
| 1 | 0bbbfe6 | feat | `.claude/hooks/session-tracker-reminder.sh` (new, 0755) | Mirror review-reminder.sh shape; reason text points at session-tracker skill + AL Jira project |
| 2 | f0cb853 | feat | `.claude/settings.json` | Wire the new hook as a second `hooks.Stop[0].hooks[]` entry alongside review-reminder.sh |
| 3 | e9e69f7 | docs | `CLAUDE.md`, `docs/decisions/010-review-loop-via-claude-md.md` | New "Session Tracking" section in CLAUDE.md; ADR-010 Implementation pointer + Consequences bullet enumerate both hooks |

## Hook smoke test outputs

Both behavioral paths verified after Task 1:

```
$ echo '{"stop_hook_active":false}' | bash .claude/hooks/session-tracker-reminder.sh
{"decision":"block","reason":"Before stopping: did this session produce a concrete deliverable (MR, doc, decision artifact, ticket) that should be tracked in Jira? See .claude/skills/session-tracker/SKILL.md. The default project for this repo is AL (copiedwonder.atlassian.net, board 2). If yes, propose a tracking structure (single-issue / multi-deliverable / milestone) and create the Task / Sub-task with Motivation + Expected results, then transition to In Progress (or In Review if an MR is already open). If this was research-only / Q&A / .planning-only / no reviewable change, or you've already tracked this session, request stop again to pass through."}

$ echo '{"stop_hook_active":true}' | bash .claude/hooks/session-tracker-reminder.sh
   (no stdout, exit 0)
```

Reason text JSON validated via `python3 -c 'json.loads(...)'`. Length 629 chars.
Contains: `session-tracker`, `SKILL.md`, `AL`, `copiedwonder`, `Motivation`,
`Expected results` — all expected anchor terms.

## Settings wiring

`.claude/settings.json` now wires both hooks under one Stop matcher:

```json
{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": ".claude/hooks/review-reminder.sh" },
          { "type": "command", "command": ".claude/hooks/session-tracker-reminder.sh" }
        ]
      }
    ]
  }
}
```

Project-shared (not `.local`); both hooks resolve relative to the project root.
Neither path is gitignored.

## CLAUDE.md addition (verbatim)

A new section was inserted between "Review Loop" and "Commands":

```markdown
## Session Tracking

Concrete deliverables (MR, doc, decision artifact, ticket in another project)
get tracked in Jira project **AL** (copiedwonder.atlassian.net, board 2) per
`@.claude/skills/session-tracker/SKILL.md`. A second one-shot Stop-hook reminder
at `.claude/hooks/session-tracker-reminder.sh` (backstop, same ADR-010 refinement
that allows the review-reminder hook) nudges Claude to invoke the skill before
stopping. Skip for research-only / Q&A / `.planning/`-only sessions — request
stop again to pass through.
```

The pre-existing "Review Loop" section was not edited — its behavior, reviewer
routing list, and triage rules remain authoritative for code-review concerns.

## ADR-010 amendments (precise)

Two surgical edits inside the existing file. Status remains "Accepted"; the
Refinement section's rationale prose is untouched.

1. **Consequences bullet** — appended a second hook to the existing reminder-hook bullet:

   Before:
   ```
   - A reminder Stop hook with `stop_hook_active` one-shot guard is allowed
     (see Refinement below); see `.claude/hooks/review-reminder.sh`.
   ```
   After:
   ```
   - A reminder Stop hook with `stop_hook_active` one-shot guard is allowed
     (see Refinement below); see `.claude/hooks/review-reminder.sh` and
     `.claude/hooks/session-tracker-reminder.sh`.
   ```

2. **Refinement Implementation paragraph** — extended to enumerate both hooks
   and clarify the per-hook cost model:

   Before:
   ```
   Implementation: `.claude/hooks/review-reminder.sh`, wired via
   `.claude/settings.json` (project-shared).
   ```
   After:
   ```
   Implementation: `.claude/hooks/review-reminder.sh` and
   `.claude/hooks/session-tracker-reminder.sh`, both wired via
   `.claude/settings.json` (project-shared) under the same Stop matcher block.
   Each hook owns one reminder concern and short-circuits independently via
   `stop_hook_active`; worst case is one extra "block + re-stop" round-trip per
   hook per turn.
   ```

No new ADR was opened — AL-24 is a second instance of the reminder-hook
*category* established by ADR-010's 2026-05-02 Refinement, not a new
architectural decision. Opening ADR-016 to "redo what ADR-010 already
authorized" would have been ceremony without information.

## Deviations from Plan

None for the implementation itself. One pre-execution note:

- **The PLAN.md file did not exist on disk** at
  `.planning/quick/260503-8z4-al-24-add-stop-hook-for-session-tracking/`. The
  full task brief came from the orchestrator's `<constraints>` block, which
  fully specified outputs, the byte-for-byte mirror requirement, the skip lists
  for the docs commit, and the SUMMARY.md path. Executed against the
  constraints; this SUMMARY documents what was built. Logged as a process
  observation — not a code deviation.

## Review Pass

Per CLAUDE.md "Review Loop" the changed file types triggered:

- **Bash** (`.claude/hooks/session-tracker-reminder.sh`) → bash-engineer + security-engineer + qa-engineer
- **JSON config** (`.claude/settings.json`) → security-engineer (config trust surface), shellcheck/biome routes via pre-commit
- **Docs** (`CLAUDE.md`, `docs/decisions/010-review-loop-via-claude-md.md`) → no project-local writer/fact-checker reviewer; sanity-checked manually per constraints

Subagent dispatch via the `Task` tool was not used (this executor agent does not
spawn subagents). Self-review applied the published rubrics from
`.claude/agents/{bash-engineer,security-engineer,qa-engineer}.md` plus
deterministic linters:

| Tool / Rubric | File(s) | Result |
|---|---|---|
| `shellcheck --severity=warning --shell=bash --external-sources` | session-tracker-reminder.sh | Pass (exit 0, no findings) |
| `pre-commit run --files <four files>` | all changed files | Pass (large-files, merge-conflicts, JSON, EOF, trailing-ws, ShellCheck v0.10, shfmt, secrets — all green) |
| bash-engineer rubric (set/quoting/idempotency/pipefail/curl-pipe-bash) | session-tracker-reminder.sh | No findings — `set -euo pipefail` present, `"$input"` quoted, no state-changing ops, no `curl \| sh`, bash shebang correct |
| security-engineer rubric (eval/sudo/word-splitting/secrets/sudoers) | session-tracker-reminder.sh + settings.json | No findings — no `eval`, no env-var echoing, reason text contains only public project metadata, no privilege boundaries crossed |
| qa-engineer rubric (assertion strength) | hook smoke tests | Both paths asserted on output (decision:block content + empty stdout), not just exit code; six anchor terms verified in reason |
| Docs sanity check | CLAUDE.md + ADR-010 | New section parallels "Review Loop" structure; ADR-010 Status preserved; Refinement rationale unchanged; both hooks now enumerated in Implementation pointer |

One observation considered and **skipped as stylistic**: the ADR-010 Refinement
intro paragraph still says "a **reminder hook** that..." (singular). The
paragraph defines a *category*, not an instance, so the singular reads
correctly; the Implementation pointer at the end of the section enumerates the
two concrete instances. Editing the intro would dilute the
category-definition framing for no gain.

## Self-Check

- File `.claude/hooks/session-tracker-reminder.sh` exists, mode 0755, smoke tests pass
- File `.claude/settings.json` parses as JSON; both hook commands present
- File `CLAUDE.md` contains "Session Tracking" section + `session-tracker-reminder.sh` reference
- File `docs/decisions/010-review-loop-via-claude-md.md` Status remains "Accepted"; Implementation pointer enumerates both hooks
- Commit `0bbbfe6` exists in `git log`: `feat(quick-260503-8z4-01): add session-tracker reminder Stop hook`
- Commit `f0cb853` exists in `git log`: `feat(quick-260503-8z4-02): wire session-tracker-reminder Stop hook`
- Commit `e9e69f7` exists in `git log`: `docs(quick-260503-8z4-03): document session-tracker hook in CLAUDE.md + ADR-010`
- `pre-commit run --files <four files>` exits 0
- `shellcheck` on the new hook exits 0
- Worktree clean except for the planned SUMMARY.md (which orchestrator commits)

## Self-Check: PASSED

All claimed files exist, all claimed commits exist in `git log`, all linters
green, both behavioral paths of the hook verified end-to-end. No deferred
items. No deletions of pre-existing tracked files. AL-24 deliverable is
ready for the orchestrator's docs commit + Jira transition.
