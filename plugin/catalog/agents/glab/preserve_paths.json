{
  "preserve_paths": [
    "~/.config/glab/"
  ],
  "comment": "glab per-user config (config.yml + the token written by `glab auth login`). Preserved across `agentlinux remove` per CAT-04 — the CLI injects it into AGENTLINUX_PRESERVE_PATHS on every uninstall, matching the other coding agents so a remove never destroys the user's credentials. Only `agentlinux --purge` wipes the agent home."
}
